---
date: 2026-07-29
---

# Work Log — 2026-07-29

## signalfd fork 继承队列重绑定

**涉及文件：**
- `os/src/syscall/process/signal.rs` — 将 signalfd 的通知队列置于 `Mutex<Arc<EventWaitQueue>>` 中，并提供安全的队列克隆与重绑定方法。
- `os/src/task/task.rs` — 未共享 `sighand` 的 clone 在复制 fd table 后，将继承的 signalfd 绑定到子进程的新 `Sighand` 队列。
- `os/src/syscall/fs/sys_read.rs` — 阻塞 signalfd 读取持有队列 `Arc` 并在对应 `WaitQueue` 等待，避免依赖可变队列的借用引用。
- `os/src/syscall/{mod.rs,process/mod.rs}` — 在 syscall 模块内重新导出 `SignalFd`，供 clone 与读路径安全识别。
- `docs/03_fs/special-fds.md`、`docs/05_process/signal.md` — 同步 signalfd 队列所有权与 fork 重绑定语义。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -lc 'cd /app && make toolchain-preflight'` ✅
- `docker exec pxy-mangocore-os-dev-1 bash -lc 'cd /app && set -o pipefail && make kernel ARCH=rv64 PROFILE=normal 2>&1 | tail -3 && make kernel ARCH=la64 PROFILE=normal 2>&1 | tail -3'` ✅

**备注：** 未修改 `PollWaitQueue`、`EventQueueHandle` 或 `IndexNode`；不使用原始指针，也不改变 fork 的 `Sighand` 创建路径。

## WaitQueue 遗留模块用户态回归与 LTP 配置恢复

**涉及文件：**
- `os_test.conf` — 删除遗留的 `ltp_include=pipe13` 过滤项，保留 suite runner 配置。
- `user/src/syscall.rs` — 增加 SysV semaphore、`nanosleep(2)` 与类型安全的 `FUTEX_CMP_REQUEUE` 用户态 ABI 封装。
- `user/src/bin/regression/regression_ipc_sem.rs` — 覆盖 SysV semaphore 零值阻塞、子进程延迟 `semop(+1)` 释放及 5 秒 watchdog。
- `user/src/bin/regression/regression_nanosleep.rs` — 覆盖 50ms `CLOCK_MONOTONIC` 睡眠和被 SIGUSR1 打断后 `nanosleep` 剩余时间。
- `user/src/bin/regression/regression_futex_requeue.rs` — 覆盖共享页 waiter 的 `FUTEX_CMP_REQUEUE` 与第二 futex 显式唤醒。
- `user/src/bin/regression/main.rs` — 注册三项回归并将 TAP 总数更新为 19。

**验证：**
- `make toolchain-preflight` ✅ — 固定 nightly 工具链可用。
- `make user ARCH=rv64 PROFILE=normal` ✅ — 用户态 release 构建完成。
- `make test ARCH=rv64 PROFILE=regression` ❌ — QEMU 启动前被既有内核编译错误阻断：`os/src/task/task.rs:33` 引用私有的 `crate::syscall::process::SignalFd`（E0603）；本任务禁止修改内核，未越界修复。

**备注：** 所有新增无限期等待均有 5 秒 SIGTERM watchdog；完整命令、容器挂载、配置校验和与阻断原因见 `docs/Work_Log/evidence/2026-07-29/waitqueue-regression.md`。

## TCP waitqueue 条件闭包重入死锁修复

**涉及文件：**
- `os/src/fs/vfs/event.rs` — 删除会丢失任务唤醒的 `try_notify_events_*` 回退 API。
- `os/src/net/socket/{mod.rs,inet/stream/mod.rs}` — 恢复保证唤醒；为 TCP 增加无 poll 的 connect/recv/send 及 message 检查变体。
- `os/src/net/syscall/{connect.rs,recvfrom.rs,recvmsg.rs,sendto.rs,sendmsg.rs}` — 在进入 `wait_until_interruptible` 前 poll，条件闭包仅检查无 poll 状态。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make toolchain-preflight"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "set -o pipefail; cd /app && make kernel ARCH=rv64 PROFILE=normal 2>&1 | tail -3 && make kernel ARCH=la64 PROFILE=normal 2>&1 | tail -3"` ❌ — rv64 在编译本任务前已存在的 `src/task/task.rs:33` 导入 `crate::syscall::process::SignalFd` 时失败（`E0603`: `process` 为私有模块）；因此未运行 la64，未将失败归因于本次 TCP 改动。
- `cargo fmt --check` ⚠️ 未执行：容器固定 nightly 未安装 `rustfmt` 组件。

**备注：** `NET_INTERFACE.poll()` 只能在 waitqueue 锁外触发；以 `try_lock()` 跳过唤醒会重新引入丢失就绪通知。

## Pipe、signalfd 与 pidfd 唤醒回归测试

**涉及文件：**
- `user/src/syscall.rs` — 增加 signalfd、pidfd、epoll 和 ppoll 的最小用户态 syscall ABI 封装。
- `user/src/bin/regression/regression_pipe_wakeup.rs` — 以 64 个转发子进程覆盖 wake-one pipe 链式进度，并用 epoll 超时监督。
- `user/src/bin/regression/regression_signalfd.rs` — 覆盖默认阻塞 signalfd 读在延迟 SIGUSR1 前保持阻塞，以及信号号解码。
- `user/src/bin/regression/regression_pidfd.rs` — 覆盖 live child exit 与 WNOWAIT 已退出 child 的 pidfd `ppoll(POLLIN)` 就绪。
- `user/src/bin/regression/main.rs` — 注册三项回归并更新 TAP 总数。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — 用户态 release 构建完成。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | tail -30"` ❌ — 首轮 pipe/signalfd 通过，pidfd 的 `epoll_ctl(ADD)` 返回 -1；探针确认 pidfd 当前不具备 epoll 要求的 stream mode。测试改为同样有界的 `ppoll(POLLIN)` 路径；受本轮两次状态检查上限约束，最终修改未重跑。

**备注：** signalfd 以 5 秒 SIGTERM watchdog 防止通知回归时永久阻塞；pidfd 明确以 WNOWAIT 观察 zombie 后再打开，验证已退出目标的稳定就绪状态。

## Eventfd、epoll、futex 与 timerfd 阻塞唤醒回归测试

**涉及文件：**
- `user/src/syscall.rs` — 补充 `eventfd(2)` 与六参数 `futex(2)` 的用户态 syscall 封装。
- `user/src/bin/regression/regression_eventfd.rs` — 覆盖空计数器阻塞读、子进程写入显式唤醒，以及 3/5 的计数器读写语义。
- `user/src/bin/regression/regression_epoll.rs` — 覆盖 pipe 写端显式唤醒无限期 epoll 等待，以及空 pipe 的 100ms 超时。
- `user/src/bin/regression/regression_futex.rs` — 以 `MAP_SHARED | MAP_ANONYMOUS` 页面覆盖跨 fork 的 FUTEX_WAIT/FUTEX_WAKE 和值已变更的 `EAGAIN` 快路径。
- `user/src/bin/regression/regression_timerfd.rs` — 覆盖 50ms `CLOCK_MONOTONIC` one-shot timerfd 的阻塞读取与 8-byte expiration counter。
- `user/src/bin/regression/main.rs` — 注册四项回归并将 TAP 总数更新为 16。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — 用户态 release 构建完成。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | tail -40"` ❌ — 既有 `regression_pidfd` 在新增用例前失败（`epoll_ctl`/`epoll_wait` 均返回 -1），QEMU 以 exit code 143 退出；新增四项尚未执行。

**备注：** 阻塞等待由 eventfd 写、pipe 写、FUTEX_WAKE 和 timer expiry 明确驱动；每项无限期等待均有 5 秒 watchdog，仅用于失败时终止测试，避免永久挂起。

## Socket blocking I/O wakeup regression tests

**涉及文件：**
- `user/src/bin/regression/regression_net_unix_pair.rs` — 覆盖 Unix stream socketpair 的双向阻塞读写、关闭对端 EOF 唤醒和 datagram `sendto`/`recvfrom`。
- `user/src/bin/regression/regression_net_tcp_accept.rs` — 覆盖延迟子进程连接唤醒父进程阻塞 `accept`，并验证已接受连接传输数据。
- `user/src/bin/regression/regression_net_tcp_connect.rs` — 覆盖延迟子进程 `accept` 唤醒父进程阻塞 `connect`。
- `user/src/bin/regression/main.rs` — 注册三项网络唤醒回归并将 TAP 总数更新为 12。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — release user build 完成；输出仅包含既有 `inet_test` warnings。

**备注：** 每项网络回归均由 5 秒 watchdog 兜底，避免阻塞唤醒失效时永久挂起测试套件。

## WaitQueue one-shot handshake kernel tests

**涉及文件：**
- `os/src/kernel_tests/waitqueue.rs` — 注册 WaitQueue 状态机覆盖用例，并按行为拆分测试模块。
- `os/src/kernel_tests/waitqueue_blocking.rs` — 覆盖阻塞唤醒、无 10ms 回退、注册后通知、双队列清理、deadline 和陈旧 weak waiter。
- `os/src/kernel_tests/waitqueue_wake.rs` — 覆盖 FIFO 单唤醒、广播唤醒与 1000 次 spawn/wait/wake 压力。
- `os/src/kernel_tests/waitqueue_interrupt.rs` — 覆盖信号中断及信号与显式唤醒的 Ready/Interrupted 优先级。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make -C os ktest-run ARCH=rv64 PROFILE=normal"` ✅ — 64 passed, 0 failed。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=rv64 PROFILE=normal && make kernel ARCH=la64 PROFILE=normal"` ✅。

**备注：** 当前 ktest harness 仅暴露 `spawn_ktest_task(fn())` 与 `wake_at_most(1)`；FIFO 用例通过后者覆盖内部 `wake_one` 路径，信号用例通过 `send_signal_to_interruptible(SIGUSR1)` 定向唤醒唯一的阻塞 ktest worker。
