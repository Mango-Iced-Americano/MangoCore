## 2026-07-29

### VF2 every-loop paced operational 双向 ICMP 验收 [PASS]

**涉及文件：**
- `os/src/hal/arch/riscv/sbi.rs` — VF2 `vf2_console_getchar()`：32-bit DW APB UART MMIO 直读。LSR 偏移 `0x14`（×4 移位，非标准 NS16550A 的 `0x5`）、RBR 偏移 `0x0`；`#[cfg(feature = "board_vf2")]` 条件编译；读取 `u32` 后掩码至低 `u8` 返回。
- `os/src/task/processor.rs` — VF2-only 配置：每调度循环均轮询 UART（`should_poll_console = true`）；32 字节有界 drain 循环（`BOARD_VF2_CONSOLE_DRAIN_BUDGET = 32`），批量喂入 TTY line discipline。非 VF2 rv64 仅每 64 tick 轮询一次。
- `os/src/kernel_tests/platform.rs` — `test_vf2_serial_static_mmio()` 契约测试：验证 VF2 静态平台描述符恰好一个 `ns16550a` 串口、`DeviceKind::Serial`、MMIO 范围 `(0x1000_0000, 0x1000)`。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-paced-operational-final-{preflight,serial,events,pacing,host-to-board-ping,status}.txt` — 保存镜像/host preflight、完整串口 RX、精确 TX 时间线、逐字节 pacing、主机 ping 与完整状态。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-paced-operational-final-{arp-icmp.pcap,pcap-decoded.txt,verdict.md}` — 保存 ARP/ICMP 原始包、解码和验收结论。

**验证：**
- 单一 exclusive Python/pyserial 会话（`/dev/ttyUSB0`、115200）只使用 U-Boot volatile `setenv`；未发出 reset、DTR/RTS 属性赋值或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-everyloop-final.bin` SHA-256 为 `24fb3c5fbcb389b2c815e21d70f1c2971cd8f088cc6131c57c89c36ea4f7fbaf`；U-Boot TFTP 为 `7,453,336 (71ba98 hex)` bytes，CRC32B 为 `f9340567`，随后进入 `MangoCore:/#`。
- 本轮没有发送 unpaced marker；所有 kernel-shell 操作均为每字节写入后固定 sleep `0.050` 秒（含换行）再 flush。`ifconfig` 完整回显/返回 prompt，且 `eth0` 报告 `192.168.200.20`。
- board→host `/busybox ping -c4 192.168.200.1` 与 host→board `ping -c4 -W1 192.168.200.20` 都是 4 transmitted、4 received、0% loss。
- `tcpdump` exit 0、kernel drop 0；decoded pcap 含两个方向各四组 ICMP request/reply。`os_test.conf` 构建前后 SHA-256 均为 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22` 并完成字节比较。

**备注：**
- **paced 4/4 双向 ICMP PASS** 依赖三处源码改动构成的 VF2 UART 接收路径：`sbi.rs` 的 32-bit DW APB 移位寄存器直读、`processor.rs` 的 VF2-only 每循环轮询+32 字节有界 drain、`platform.rs` 的 VF2 UART 描述符 MMIO 契约。这三者使 TTY 在 pacing 约束下能够可靠消化 shell 命令并完成双向 ICMP。
- **unpaced TTY non-determinism 未被解决**：paced 成功不能外推为 unpaced 可靠性——实板 unpaced marker 此前持续观察到截断（`vf2-uartn`、`printf 'vf2-uartu`）。逐字节 `0.050s` sleep 补偿了 UART receive FIFO 与 TTY line discipline 间的时序间隙，但这不是驱动层修复。**不声称 unpaced 可靠性。**
- 未执行 git add/commit/push；Rust 源码改动仍未提交。无 `code_paths` 同步项；AI 使用报告无需更新。不添加 speculative interrupt 结论。

### VF2 every-loop paced ICMP acceptance [blocked at mandatory unpaced marker]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-paced-final-{preflight,serial,events,pacing,status}.txt` — 保存本轮 image/host preflight、完整 RX 串口、精确 TX 时间线、固定逐字符节流策略和失败状态。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-paced-final-verdict.md` — 固化本轮 marker 门禁失败、未跨越的网络边界和证据索引。

**验证：**
- 单一 exclusive Python/pyserial 会话（`/dev/ttyUSB0`、115200）观察到 `StarFive #`；仅使用 volatile `setenv`，未发出 reset、DTR/RTS 属性赋值或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-everyloop-final.bin` 的 SHA-256 仍为 `24fb3c5fbcb389b2c815e21d70f1c2971cd8f088cc6131c57c89c36ea4f7fbaf`；TFTP 为 `7,453,336 (71ba98 hex)` bytes，U-Boot CRC32B 为 `f9340567`，并进入 `MangoCore:/#`。
- required unpaced `printf 'vf2-uart-input-ok\n'` 本轮只收到 `printf 'vf2-uartn'` 和 `vf2-uartnMangoCore:/#`：没有完整 command echo 或 marker output，故不满足 marker 门禁。
- 为不跨越失败门禁，未启用已记录的每字符 `0.050` 秒 pacing，未配置 eth0，未运行任一方向的 `ping -c4`，也未启动 pcap；不能宣称网络验收通过。
- `os_test.conf` 与基线仍逐字节匹配（SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`）。

**备注：** 这是一项实板运行时观察，不作根因推断，也不声称 unpaced 长命令鲁棒性。未修改 kernel/user source、配置、firmware、U-Boot、PHY、GMAC 或 git；无 Rust 源码改动，故无 `code_paths` 同步项；AI 使用报告无需更新。

### VF2 shifted UART + bounded drain + every-loop poll 最终 shell 镜像仅 TFTP 暂存

**涉及文件：**
- `os_test.conf` — 构建期间临时设为 `mode=shell` 并追加 `skip_apk=true`；构建后按预构建 SHA-256/长度身份恢复。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-final-evidence.md` — 固化 Docker 环境、配置嵌入/恢复、强制 RV64/VF2 构建、raw 导出及镜像身份。
- `/srv/tftp/vf2/vf2-uart-everyloop-final.bin` — 从本次 RV64/VF2 最终 ELF 导出的新 raw 文件；未覆盖已有暂存镜像。

**验证：**
- Docker 内 `make -B kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 成功。
- 本次 CPIO 包含 `os_test.conf`；解出的配置含 `mode=shell`、`skip_apk=true`。CPIO SHA-256 为 `94177d0d8473831203c818d4b60c30dd22ce02955341a1a01e7a491a03cdc21d`。
- Docker `rust-objcopy --strip-all -O binary` 从最终 ELF 导出；raw 与新 TFTP 文件通过 `cmp -s` 一致：SHA-256 `24fb3c5fbcb389b2c815e21d70f1c2971cd8f088cc6131c57c89c36ea4f7fbaf`，7,453,336 bytes，IEEE CRC32 `f9340567`。
- `os_test.conf` 构建前后均为 SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`、3,849 bytes。

**备注：** 仅执行 Docker 构建、initramfs 校验、ELF→raw 导出、校验和与 TFTP 暂存；未接触源码、Git、串口、复位、实板或 U-Boot。未修改 Rust 源码，故无 `code_paths` 文档同步项；AI 使用报告已含当前模型，无需更新。

### VF2 shifted UART + 32-byte bounded drain 最终 shell 镜像仅 TFTP 暂存

**涉及文件：**
- `os_test.conf` — 构建期间临时设为 `mode=shell` 并追加 `skip_apk=true`；构建后按预构建 SHA-256/长度身份恢复。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-drain-final-evidence.md` — 固化 Docker 环境、配置嵌入/恢复、构建、raw 导出、无覆盖暂存及镜像校验记录。
- `/srv/tftp/vf2/vf2-uart-drain-final.bin` — 从本次 VF2 最终 ELF 直接导出的新 raw 文件；未覆盖已有暂存镜像。

**验证：**
- Docker 内 `make toolchain-preflight` 与 `make kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 成功。
- 构建期从 CPIO 解出的 `os_test.conf` 与 shell 构建配置逐字节匹配，确认嵌入 `mode=shell`、`skip_apk=true`；CPIO SHA-256 为 `68c20a9f2feb1e3c8beca5253af9d338fc72a8ef967eb66c38d5bcbf3be26bf7`。
- Docker `rust-objcopy --strip-all -O binary` 从最终 ELF 导出；暂存前目标不存在并使用 `cp --no-clobber`。raw 与 TFTP 文件通过 `cmp -s` 一致：SHA-256 `75524897c4d8057a533effa2f3a20666e8b5575681036290c272ab55392b0db3`，7,453,344 bytes，IEEE CRC32 `f40b794f`。
- `os_test.conf` 构建前后均为 SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`、3,849 bytes。

**备注：** 仅执行 Docker 构建、initramfs 校验、ELF→raw 导出、校验和与 TFTP 暂存；未接触源码、Git、串口、复位、实板或 U-Boot。AI 使用报告已含当前模型，无需更新。

### VF2 production shifted-32-bit UART RV64 shell 镜像仅 TFTP 暂存

**涉及文件：**
- `os_test.conf` — 仅在 Docker RV64/VF2 normal 构建期间临时改为 `mode=shell` 并追加 `skip_apk=true`；构建完成后从预构建快照逐字节恢复。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-final-shell-*` — 保存 Docker 容器/挂载、构建命令与日志、嵌入 initramfs 配置、最终 ELF→raw 导出、镜像身份、暂存保留和配置恢复证据。
- `/srv/tftp/vf2/vf2-uart-final-shell.bin` — 由本次最终 ELF 直接导出的新 raw 暂存文件；未覆盖先前暂存镜像。

**验证：**
- Docker 容器内 `make toolchain-preflight` 与 `make kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 均成功；`sbi.rs` 在构建前后哈希一致，最终 ELF、initramfs 和 raw 均晚于当前生产 UART 源。
- CPIO 检查包含 `bin/bash`、`busybox`、`os_test.conf`；嵌入配置同时含 `mode=shell`、`skip_apk=true`，两个 shell 二进制均无 `PT_INTERP`。
- Docker 内 `rust-objcopy --strip-all -O binary` 从最终 ELF 直接导出。`/srv/tftp/vf2/vf2-uart-final-shell.bin`：SHA-256 `97734bf164c15e2a0b6d0c55c29470810c4fa864eed6b297f9f7b80e2c258029`，7,453,336 bytes，IEEE CRC32 `a5ef3bf0`；与导出 raw 的 `cmp -s` 逐字节一致。
- 新 stage 目标复制前不存在，使用 `cp --no-clobber`；既有 `vf2-*.bin` 暂存身份前后一致。恢复后的 `os_test.conf` 与快照均为 SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`、3,849 bytes，并通过 `cmp -s`。

**备注：** 本轮仅 Docker 构建、raw 导出和主机 TFTP 目录暂存；未打开串口、未接触/复位板卡、未运行 U-Boot 或执行网络动作。未修改 Rust 源码，故无 `code_paths` 文档同步项；AI 使用报告已含 GPT-5.6-terra，毋须更新。

### VF2 UART layout probe RV64 shell 镜像仅 TFTP 暂存

**涉及文件：**
- `os_test.conf` — 构建期间仅将 `mode=run` 改为 `mode=shell` 并追加 `skip_apk=true`；构建后按预构建快照逐字节恢复。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-layout-probe-*` — 持久化 Docker 容器/命令/日志、当前 probe 源码新鲜性、嵌入配置、raw 导出、镜像身份、暂存保留和配置恢复证据。
- `/srv/tftp/vf2/vf2-uart-layout-probe.bin` — 新建独立 raw 暂存镜像；未覆盖既有 `vf2-normal-shell.bin` 或 `vf2-uart-console-shell.bin`。

**验证：**
- Docker 容器 `79040401ef8a`（`/root/projects/MangoCore -> /app`）内 `make toolchain-preflight` 与 `make kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 均 exit 0。
- 生成的 CPIO 包含 `bin/bash`、`busybox`、`os_test.conf`；解出的配置为 `mode=shell`、`skip_apk=true`，两个 shell 二进制均为静态链接且无 `PT_INTERP`。
- `sbi.rs` probe 源的构建前后 SHA-256 均为 `ad05a0c01b3c10bb20239ba4e33f4741380634fca2b28e9caef8861f83c7508c`；ELF、CPIO、raw 时间均晚于源码，且新鲜性证据记录了 `MARKED`、32-bit LSR 和两种 `0x55` RBR 判别源码标记。
- Docker 内 `rust-objcopy` 从本次 ELF 导出 raw 后，`/srv/tftp/vf2/vf2-uart-layout-probe.bin` 的 SHA-256 为 `1f6b639c74484cccc68b8e8dd06c6da0fd2b818021f13a9b2607e51e2275e154`、长度为 `7,494,328` bytes、IEEE CRC32 为 `4fb87daf`；与导出 raw 通过 `cmp -s` 逐字节一致。
- 暂存目标在 copy 前不存在；`vf2-normal-shell.bin` 和 `vf2-uart-console-shell.bin` 在暂存前后 SHA-256 与长度一致。恢复后的 `os_test.conf` 与快照同为 SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`、`3,849` bytes，并通过 `cmp -s`。

**备注：** 本轮仅 Docker 构建、raw 导出和宿主 TFTP 目录暂存；未打开串口、未接触或复位实板，未执行 U-Boot、DTR、网络或任何板端命令。未修改 Rust 源码，故无 `code_paths` 文档同步项；AI 使用报告无需更新。

### VF2 UART console-input fallback RV64 shell 镜像仅 TFTP 暂存

**涉及文件：**
- `os_test.conf` — 构建时仅临时改为 `mode=shell` 并追加 `skip_apk=true`；构建和打包检查后已按快照逐字节恢复。
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-console-shell-*` — 保存配置快照/恢复证明、Docker 命令与容器挂载、构建和 initramfs 检查、ELF→raw 导出、镜像身份及字节匹配证据。
- `/srv/tftp/vf2/vf2-uart-console-shell.bin` — 新建的 raw 暂存镜像；未触碰已有 `/srv/tftp/vf2/vf2-normal-shell.bin`。

**验证：**
- Docker 容器 `79040401ef8a`（`/root/projects/MangoCore -> /app`）内 `make toolchain-preflight` 与 `make kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 均 exit 0。
- 已检查本次 CPIO（SHA-256 `588c309b3eab4b827b88df6b9df7570b96368a1932038d8da74efb18b34bc858`）：嵌入 `bin/bash`、`busybox`、`os_test.conf`；两个 ELF 均为静态链接且无 `PT_INTERP`，嵌入配置含 `mode=shell` 与 `skip_apk=true`。
- 最终 ELF 由 Docker 内 `rust-objcopy` 直接导出 raw；`/srv/tftp/vf2/vf2-uart-console-shell.bin` SHA-256 为 `d7eba30cebcc6c66bda9fcc8ad15af70abb67b06a6279db0260b532a3dd3f8a4`、7,453,368 bytes、IEEE CRC32 `b91bada8`，与 raw 源通过 `cmp -s` 逐字节一致。
- 原有 `vf2-normal-shell.bin` 的 SHA-256 与长度在暂存前后保持 `81fcf2eaee20a05d85d8e4f481f40bb488388351ed98a5c911e30bdd3488baf4` / 7,453,336 bytes；`os_test.conf` 恢复后与快照均为 SHA-256 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`、3,849 bytes，`cmp -s` 通过。

**备注：** 本轮仅本机构建、导出和 TFTP 目录暂存；未打开串口，未接触/复位实板，未执行 U-Boot、DTR、网络测试或任何固件/PHY/GMAC 操作。未修改 Rust 源码，故无 `code_paths` 文档同步项；AI 使用报告无需更新。

### Fix legacy SBI ecall FID register (a6/x16) not zeroed in `sbi_call()`

**涉及文件：**
- `os/src/hal/arch/riscv/sbi.rs` — Add `in("x16") 0usize` to `sbi_call()` inline asm; update safety comment to reflect a6 operand

**验证：**
- Docker 容器 `mangocore-os-dev-1` (79040401ef8a) 内串行构建验证：`make toolchain-preflight` (exit 0) → `make kernel ARCH=rv64 PROFILE=normal` (exit 0) → `make kernel ARCH=la64 PROFILE=normal` (exit 0)
- `timeout 120s make run ARCH=rv64 PROFILE=normal` (exit 2) 受阻于镜像前提缺失：`build/rv64/release/normal/image/rootfs-rv.img` 不存在，QEMU 未进入 Guest；未见 SBI timer/console 回归信号
- git `782f7133-dirty`；串行构建顺序、log 时间戳与证据归档新鲜性均经 `verification-summary.txt` 校验
- `reboot()` 保持不变，继续使用自己的显式 SRST FID=0 初始化

**备注：**
- VF2 实板观察到的 `ext=0x2 func=0xf4240` 表明 legacy `console_getchar(which=2)` 呼出时 a6 包含来自调用者寄存器池的垃圾值（`0xf4240 = 1_000_000`），OpenSBI 将其解释为 EID 0x02 的 FID 参数
- 这是最小实板区分补丁；但不保证单独修复 shell 输入——可能还涉及 legacy 返回值的不同问题

### VF2 当前 a6=0 normal-shell 镜像构建与实板判别 [blocked]

**涉及文件：**
- `os_test.conf` — 仅在 RV64 normal 构建期间临时设为 `mode=shell`、`skip_apk=true`；构建后按原始快照字节级还原。
- `scripts/build_initramfs.sh` — 未修改；现有流程已构建并嵌入静态 `/bin/bash`、`/busybox` 与 shell 配置。
- `docs/Work_Log/evidence/2026-07-29/vf2-sbi-*` — 持久化配置快照、Docker 构建、镜像身份、TFTP 暂存、单一串口会话与 pcap/blocker 证据。

**验证：**
- Docker 内 `make toolchain-preflight` 与 `make kernel ARCH=rv64 PROFILE=normal BOARD=vf2 BLK_MODE=virt KERNEL_CMDLINE=mango.mode=normal` 均成功；构建期间的 initramfs 实际包含 `/bin/bash`、`/busybox`、`mode=shell` 和 `skip_apk=true`。
- 当前 ELF 导出的 raw 为 `/tmp/vf2-xmodem/kernel-vf2-normal-shell.bin`：SHA-256 `81fcf2eaee20a05d85d8e4f481f40bb488388351ed98a5c911e30bdd3488baf4`、7,453,336 bytes、CRC32B `5bc73b46`；与 `/srv/tftp/vf2/vf2-normal-shell.bin` 字节一致。
- `os_test.conf` 原始快照与恢复后的 SHA-256 均为 `5d78edc2d7733352046cad727983238de167c597ee6a223afbc980346aa6be22`，并完成字节比较。
- 单一 Python/pyserial 会话（`/dev/ttyUSB0`、115200、无 DTR/RTS 赋值）仅发送被动回车；未观察到 `StarFive #`。完整串口输出仅为 `sbi_ecall_handler: Invalid error 13 for ext=0x2 func=0xf4240`。未执行 reset、DTR、`saveenv`、TFTP、CRC、boot 或任何 shell/网络命令。

**备注：**
- 结论是阻断而非 a6=0 镜像的运行时结论：板卡未处于可用 U-Boot，必须进行物理 reset/power-cycle 后才能加载已暂存 raw 并继续 shell 输入和双向 ICMP 判别。
- 因此未配置 board `eth0`、未执行任一方向的 4 包 ping；主机 ping 明确跳过，ARP/ICMP pcap 只记录阻断期间的空观察窗口。未修改内核/用户源、U-Boot/SPI/PHY/GMAC 或持久环境。

### VF2 物理复位后 a6=0 normal-shell 实板判别 [首个运行时阻断]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-sbi-post-reset-*` — 独立于复位前日志的 staged-image 校验、完整串口捕获、U-Boot TFTP/CRC、ARP/ICMP pcap、状态与 blocker 判决。
- `docs/Work_Log/evidence/2026-07-29/vf2-sbi-post-reset-verdict.md` — 本轮可验收事实与边界。

**验证：**
- 用户物理复位后，唯一 Python/pyserial 会话（`/dev/ttyUSB0`、115200、exclusive）观察到 `StarFive #`；仅用易失 `setenv` 配置网络与 load address，未执行 `saveenv`、DTR 或软件/硬件 reset。
- U-Boot 将 `/srv/tftp/vf2/vf2-normal-shell.bin` 加载到 `0x40200000`，报告 `Bytes transferred = 7453336 (71ba98 hex)`；`crc32` 报告 `5bc73b46`，与 staged SHA-256 `81fcf2eaee20a05d85d8e4f481f40bb488388351ed98a5c911e30bdd3488baf4` 的记录身份一致。
- `go 0x40200000` 到达新镜像 `MangoCore:/#`；完整日志确认 embedded `mode=shell`、`skip_apk=true`。输入测试后不产生 marker，而是逐字符输出 `sbi_ecall_handler: Invalid error <ASCII decimal> for ext=0x2 func=0x0`。

**备注：**
- 这是复位后、新镜像的首个精确运行时 blocker：日志已证明 FID 为 `func=0x0`，不能再把现象归为旧镜像的 `func=0xf4240`；但 serial input 仍无法执行，故不作根因推断。
- 为避免跨越首个 blocker，未在 kernel shell 配置 `eth0`，也未执行 board→host 或 host→board 的 `ping -c 4`；pcap 仅含 U-Boot/TFTP 期间的 ARP。未修改 kernel/user source、配置、U-Boot/SPI/PHY/GMAC 或持久环境。

### VF2 UART-MMIO console shell 最终实板验收 [首个运行时阻断]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-final-*` — UART-MMIO 镜像身份、完整 U-Boot/TFTP/CRC/boot 串口、精确输入命令、ARP/ICMP pcap、状态与 blocker 结论。

**验证：**
- 用户物理复位后，唯一 Python/pyserial 会话（`/dev/ttyUSB0`、115200、exclusive）进入 `StarFive #`；只使用易失 `setenv`，没有 DTR、reset 或 `saveenv`。
- U-Boot 从 `/srv/tftp/vf2/vf2-uart-console-shell.bin` 加载 `7,453,368` bytes（`71bab8`），`crc32` 输出 `b91bada8`，再通过 `go 0x40200000` 到达 `MangoCore:/#`。
- 完整新日志确认 UART-MMIO image 到达 shell；唯一会话随后发送精确 `printf 'vf2-uart-input-ok\n'`，20 秒内收到了 **零** 个串口响应字节：无 command echo、无 marker、无 prompt、无 `Invalid error`。

**备注：**
- 这是首个原始运行时 blocker，不能据此推断根因。由于输入无法配置 `eth0`，两个方向的 `ping -c 4` 均未发送；pcap 仅保留 U-Boot/TFTP ARP，不得声称 ICMP 成功。
- tcpdump 已回收，staged image 按要求保留；未修改 source/config/firmware/U-Boot/PHY/GMAC 或持久环境。

### VF2 UART layout probe — DR32=55

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-layout-probe-*` — staged probe 身份、单一 pyserial TFTP/CRC/boot、原始 `0x55` 写入、完整串口、ARP capture 与分类结论。

**验证：**
- 用户物理复位后，唯一 pyserial owner（`/dev/ttyUSB0`、115200、exclusive）观察到 `StarFive #`；只执行易失 `setenv`，没有 DTR、reset 或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-layout-probe.bin` 在 `0x40200000` TFTP 为 `7,494,328` bytes，U-Boot `crc32` 输出 `4fb87daf`，与 staged SHA-256 `1f6b639c74484cccc68b8e8dd06c6da0fd2b818021f13a9b2607e51e2275e154` 的记录身份一致。
- `[UP]` 后仅写入一个原始字节 `0x55`（无文本 `55`、无 CR/LF）；完整串口原文为 `[UP]DR32=55`，分类为 **DR32=55**。本轮未见 `DR8=55` 或 load-fault marker。

**备注：**
- 此 layout probe 不运行 production ping；pcap 仅留存 U-Boot/TFTP ARP，不能外推网络验收。tcpdump 已回收，staged probe 保留。执行过程未修改 source/config/U-Boot/firmware；不从 runtime 证据推断临时 instrumentation 的源码生命周期。

### VF2 final production UART getter 验收 [首个运行时阻断]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-production-final-*` — staged production image 身份、完整 U-Boot/TFTP/CRC/boot 串口、精确 marker 命令、ARP/ICMP pcap、状态与 blocker 结论。

**验证：**
- 用户物理复位后，唯一 pyserial owner（`/dev/ttyUSB0`、115200、exclusive）到达 `StarFive #`；只使用易失 `setenv`，无 DTR、reset 或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-final-shell.bin` 在 `0x40200000` TFTP 为 `7,453,336` bytes（`71ba98`），U-Boot `crc32` 为 `a5ef3bf0`，再通过 `go 0x40200000` 到达 `MangoCore:/#`。
- 精确 `printf 'vf2-uart-input-ok\n'` 在完整 raw log 中只回显至 `MangoCore:/# printf 'vf2-uartu`，没有 marker、返回 prompt 或 `Invalid error`；20 秒观察窗口后停止。

**备注：**
- 这是本镜像的首个可观察运行时 blocker，不作根因推断。输入未完成，故未配置 `eth0`；board→host 与 host→board 的 `ping -c 4` 均未发送，pcap 仅含 U-Boot/TFTP ARP，不能宣称网络通过。
- tcpdump 已回收，staged image 保留；未修改 source/config/firmware/U-Boot/PHY/GMAC 或 git 状态。

### VF2 bounded-drain UART final 验收 [首个运行时阻断]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-drain-final-*` — staged bounded-drain image 身份、完整 U-Boot/TFTP/CRC/boot 串口、精确 marker 命令、ARP/ICMP pcap、状态与 blocker 结论。

**验证：**
- 用户物理复位后，唯一 pyserial owner（`/dev/ttyUSB0`、115200、exclusive）到达 `StarFive #`；仅用易失 `setenv`，没有 DTR、reset 或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-drain-final.bin` 在 `0x40200000` TFTP 为 `7,453,344` bytes（`71baa0`），U-Boot `crc32` 为 `f40b794f`，再通过 `go 0x40200000` 到达 `MangoCore:/#`。
- 精确 `printf 'vf2-uart-input-ok\n'` 仅回显至 `MangoCore:/# printf 'vf2-uart`；没有完整 command echo、marker、返回 prompt 或 `Invalid error`，20 秒观察窗口后停止。

**备注：**
- 这是本镜像的首个可观察运行时 blocker，不作根因推断。输入未完成，故未配置 `eth0`；board→host 与 host→board 的 `ping -c 4` 均未发送，pcap 仅含 U-Boot/TFTP ARP，不能宣称网络通过。
- tcpdump 已回收，staged image 保留；未修改 source/config/firmware/U-Boot/PHY/GMAC 或 git 状态。

### VF2 every-loop UART final 验收 [marker 通过，网络前阻断]

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/vf2-uart-everyloop-final-*` — staged every-loop image 身份、完整 U-Boot/TFTP/CRC/boot 串口、unpaced marker、ARP/ICMP pcap、状态与结论。

**验证：**
- 用户物理复位后，唯一 pyserial owner（`/dev/ttyUSB0`、115200、exclusive）到达 `StarFive #`；只使用易失 `setenv`，没有 DTR、reset 或 `saveenv`。
- `/srv/tftp/vf2/vf2-uart-everyloop-final.bin` 在 `0x40200000` TFTP 为 `7,453,336` bytes（`71ba98`），U-Boot `crc32` 为 `f9340567`，再通过 `go 0x40200000` 到达 `MangoCore:/#`。
- unpaced 精确 `printf 'vf2-uart-input-ok\n'` 完整回显、输出 `vf2-uart-input-ok` 并返回 prompt；完整 raw log 未见 `Invalid error`。
- 随后 unpaced `/busybox ifconfig eth0 192.168.200.20 netmask 255.255.255.0 up` 只回显至 `/busybox ifconfig eth0 192.16netmask 255.255.`，未返回 prompt；到此停止串口操作。

**备注：**
- marker 已通过，但 eth0 配置未完成，故 board→host 与 host→board 的 `ping -c 4` 均未发送；pcap 仅含 U-Boot/TFTP ARP，不能宣称网络通过。此处不作根因推断。
- tcpdump 已回收，staged image 保留；未修改 source/config/firmware/U-Boot/PHY/GMAC 或 git 状态。
