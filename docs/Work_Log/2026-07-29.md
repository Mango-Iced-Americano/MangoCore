---
date: 2026-07-29
---

# Work Log — 2026-07-29

## Oracle Phase 2：PageTable 消除 PageCache 全局 entries 锁

**涉及文件：**
- `os/src/fs/page_cache.rs` — 以原子 radix `PageTable` 和每页 `EntrySlot` 取代 `Mutex<Vec<Option<Arc<PageEntry>>>>`；删除 `InnerPageCache`。已有页查找、写 lease、读批量规划与单页/批量回写只访问对应页槽；扫描和 clock 回收改为有序快照。
- `os/src/fs/page_cache_ktest.rs` — 测试辅助改经 PageTable 和公开 dirty 接口操作，不再依赖被删除的全局 entries/inner 锁。
- `docs/03_fs/page-cache.md` — 同步 PageTable 的稳定发布、按槽 Arc 克隆和非热路径快照语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内先执行 `make toolchain-preflight`，随后串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `cargo fmt --check` 未执行：固定 nightly 未安装 `rustfmt` 组件；两架构编译未报告本次改动的错误。

**备注：** 原子目录仅发布并永久保留节点/槽位；每个槽位中的 `Arc<PageEntry>` 仍由短临界区安全克隆和回收，避免无 hazard pointer 的原始 Arc 指针 ABA/use-after-free 风险。未运行新的 QEMU/ktest；本轮未新增可复用 reference 条目，`AI-Usage-Report.md` 已列出当前模型。

## Oracle Phase 1：PageCache 去除文件级 io_gate

**涉及文件：**
- `os/src/fs/page_cache.rs` — `PageEntry` 增加原子 `dirty` 标记；移除 `io_gate` 和 `InnerPageCache::dirty_pages`，写回改为扫描 entries；写入/回写/截断以每页状态 CAS 互斥。
- `os/src/fs/ext4_another/{lifetime.rs,mutations.rs}`、`os/src/fs/ext4_lwext4/layout.rs` — 移除已删除 io-gate API 的调用，保留既有写回和 inode 提交顺序。
- `os/src/kernel_tests/page_cache/write_copy.rs` — 改用公开单页回写入口验证 Loading 页不可提前提交。
- `docs/03_fs/{page-cache.md,README.md}` — 同步 per-page dirty/CAS 与截断尾页保护语义。

**验证：**
- Docker `4abf1ec06ea7`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed；未见 slab panic）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 一致）。
- 以临时注入 `mask=0x110` 运行 RV64 `derived-comp` ✅；musl 四 pwrite writers 单进程最大值为 `669.61 kB/s`，glibc 为 `707.26 kB/s`。完成后恢复工作树 `os_test.conf` 的 LTP mask。

**备注：** 本轮未运行完整 RV64 LTP；QEMU/ktest 与 benchmark 控制台输出未持久化到 evidence 目录，不能作为该轮 LTP 验收证据。现有知识库已覆盖按页状态 CAS、锁顺序和性能测量原则，未新增独立经验条目；`AI-Usage-Report.md` 已列出当前模型，无需更新。

## LA64 LTP fs 回归与 RV64 五轮基准验证

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-29/ulw-la64-ltp-config.txt`、`ulw-la64-ltp-qemu-output.log`、`ulw-la64-ltp-results.txt` — LA64 LTP 配置、完整 QEMU 串口输出与汇总结果。
- `docs/Work_Log/evidence/2026-07-29/ulw-rv64-bench-{config,5r-median}.txt`、`ulw-rv64-bench-round-*-qemu-output.log` — RV64 五轮配置、原始输出与中位数。
- `docs/Work_Log/evidence/2026-07-29/ulw-*-qemu-head-tail.txt`、`ulw-{commands-and-status,git-hash,container-id}.txt` — QEMU 运行首尾、命令/退出状态及环境元数据。

**验证：**
- Docker `4abf1ec06ea7764ad07610e69830a5a0c6377de993bec12f739bc7823a82b944`（`/home/pxy/projects/MangoCore -> /app`）内，RV64 完成后串行执行 `make kernel ARCH=la64 PROFILE=normal` ✅，再以 `virt_pci` 注入 LTP 配置并执行 `make ARCH=la64 -f make/la64.mk derived-comp`。
- LA64 LTP：`executed=59`、`PASS=55`、`FAIL=3`、`SKIP=1`；QEMU 命令退出码为 0，但用例汇总未通过，不能宣称双架构 LTP 回归通过。
- RV64 连续完成 5 次 `mask=0x110` derived-comp；musl Simple syscall / 首个 musl iozone 四 initial writers 最大单进程 / 四 readers 最大单进程的中位数为 `36.46 μs`、`638.41 kB/s`、`2112.66 kB/s`。

**备注：** 临时交付结果同时位于容器和宿主机 `/tmp/ulw-ltp-la64-results.txt`、`/tmp/ulw-bench-5r-median.txt`。本次仅执行配置注入和验证，无源代码变更；未命中需同步的 `code_paths` 文档，`AI-Usage-Report.md` 已列出当前模型，无需更新。

## 清理 pwrite 剖析并恢复 another_ext4 UserBuffer 写入

**涉及文件：**
- `os/src/syscall/fs/common.rs` — 删除 `pwrite_from_user` 回退路径的周期计时、累计变量和 profile 输出，保留原有分块写入逻辑。
- `os/src/fs/ext4_another/inode.rs` — 仅为普通文件重新声明 `supports_user_buffer_io()`，移除过期 FIXME。
- `os/src/fs/ext4_another/mutations.rs` — 删除 sync 写回路径的临时周期/页数 profile 输出；确认 `write_at_user()` 仍直连 `cache.write_user(offset, actual, source, Some(old_size))`。

**验证：**
- Docker `4abf1ec06ea7`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）；未出现 slab 或其他 panic。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 一致）。
- 完整构建、QEMU 串口和 lint 输出：`docs/Work_Log/evidence/2026-07-29/pwrite-userbuffer-cleanup-verification.log`。

**备注：** 未恢复 `PwriteStagingBuffer`，也未恢复 `UserBufferReader::len()`；没有命中需同步的 `code_paths` 模块文档。`AI-Usage-Report.md` 已列出当前模型，无需更新；现有参考已覆盖相关通用用户缓冲区与性能探针原则，未新增知识条目。

## Oracle round 3：整页覆写跳过帧清零

**涉及文件：**
- `os/src/fs/page_cache.rs` — `prepare_write_lease()` 在新建页且调用方已证明完整 4KB 覆写时调用既有的 `frame_alloc_uninit()`；其余路径仍使用清零 `frame_alloc()`。
- `docs/03_fs/page-cache.md` — 记录未初始化页只在 `Loading` 状态下被完整拷贝，提交前不可被读取者观察的安全边界。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 无新增）。
- 可验收日志与元数据：`docs/Work_Log/evidence/2026-07-29/oracle-r3-{qemu-output,qemu-head-tail,command,git-hash,container-id,config}.txt`（输出文件为 `.log`）。

**备注：** 保留 `io_gate` 和 `PageCache` 公共接口不变；没有调整 P2 批处理。未初始化帧分配器与 OOM 重试路径已存在，本次只将其接入已证明的整页覆写路径。

## PageCache pwrite 瓶颈深度分析

**涉及文件：**
- `os/src/fs/page_cache.rs` — `write_with_copy_callbacks` (~1378), `write_with_copy_callbacks_locked` (~1395), `prepare_write_lease` (~824), `commit_write_lease` (~913), `acquire_existing_write_lease` (~885), `balance_dirty_pages_for_write` (~81)
- `os/src/fs/ext4_lwext4/layout.rs` — `write_at` (~924), `sync` (~785)
- `os/src/mm/frame_allocator.rs` — `frame_alloc` (~920)
- `os/src/syscall/fs/common.rs` — `pwrite_from_user` (~944)

**分析结论：**

1. **656K cycles 并非 PageCache 纯业务开销**。主要成分为：
   - `perf_time_now_unconditional()` 读全局 `mcycle` CSR → 测量窗口捕获了单核 4 线程环境下其他线程的工作（~90%）
   - `spin::Mutex` 在 QEMU TCG 下的原子指令翻译放大（`lr.w/sc.w` CAS loop → 每条数十条 host 指令）
   - `frame_alloc()` 清零 4KB 在 TCG 下被放大到 ~100K cycles（6.25% 调用中）

2. **PageCache 纯业务路径**（复用页）：~3,700–5,500 cycles。首次分配页 ~130K cycles。加权平均 ~12,400 cycles。

3. **Linux 对比**：Linux per-folio 锁 + RCU page cache lookup + per-CPU PCP 脏页记账，1KB buffered write 约 3,000–8,000 指令。MangoCore 差距在锁粒度（per-file io_gate vs per-page folio_lock）和分配器清零的 TCG 放大。

4. **close/sync 691M cycles**：~58% 是 256 页 VirtIO 块写入，~20% VirtIO FLUSH，~14% lwext4 cache flush，~7% journal/inode 提交。

5. **推荐优化优先级**：
   - P0：`frame_alloc` 对全页覆盖写使用 `frame_alloc_uninit()`（跳过 4KB 清零）→ 消除 ~65% 的首次页分配成本
   - P1：提升 pwrite chunk size（`IO_CHUNK_SIZE`）→ 减少 io_gate 加锁次数 64x
   - P2：`io_gate` 改为单核 yield 锁 → 减少 spin 浪费

6. **可达目标**：P0+P1 落地后 iozone writers 可达 **2,000–3,000 kB/s**（QEMU TCG 下，受 icount 速率限制）。

**验证：** 纯分析，无代码变更。基于已归档的 `docs/Work_Log/evidence/2026-07-29/pwrite-phase-timing.md` 中的 RV64 cycle 数据。



## RV64 pwrite cycle phase profiling

**涉及文件：**
- `os/src/task/perf.rs` — 增加不依赖 `perf_stats` 运行时开关的只读 cycle helper，供短期热路径剖析使用。
- `os/src/syscall/fs/common.rs` — 在非 `UserBuffer` 的 `pwrite_from_user` 回退路径记录每调用总周期和 `File::pwrite` 子阶段周期。
- `os/src/fs/ext4_another/mutations.rs` — 在 sync 的 io-gate 临界区记录 dirty 页数、writeback 周期和完整 sync 周期。
- `docs/Work_Log/evidence/2026-07-29/pwrite-phase-timing.md` — 归档 RV64 容器、命令和聚合结果。

**验证：**
- Docker `4abf1ec06ea7764ad07610e69830a5a0c6377de993bec12f739bc7823a82b944`（`/home/pxy/projects/MangoCore -> /app`）内 `make kernel ARCH=rv64 PROFILE=normal` ✅。
- Docker 内 `RUSTUP_HOME=/root/.rustup CARGO_HOME=/root/.cargo make ARCH=rv64 -f make/rv64.mk derived-comp` ✅（iozone musl/glibc 完成）。
- 前 4,096 个 1 KiB pwrite：总计 `724291` cycles；用户校验/复制 `68064` cycles（`9.40%`）；`File::pwrite` `656227` cycles（`90.60%`）。
- 4 个 1 MiB close/sync：每次 `1024` writeback pages；平均 writeback `205812153` cycles，完整 sync `691582377` cycles。

**备注：** `perf_time_now()` 受 `STATS_ON` 运行时 gate 限制，初轮结果全为零；剖析 helper 改为直接只读硬件计数器，未启用 `perf_diag`。RISC-V QEMU 的 `cycle` 未校准为 wall clock，故不将 cycles 虚构换算为毫秒。完整 QEMU 输出在 `/tmp/ulw-phase-iozone-final.log`，用户请求的结果文件在 `/tmp/ulw-phase-timing.txt`。现有参考已覆盖“有限计数器、数据驱动定位”和探针污染风险，未新增通用知识条目。

## Round 2b slab allocator panic rollback and RV64 benchmark

**涉及文件：**
- `os/src/syscall/fs/common.rs` — 移除 `PWRITE_KBUF`/`PwriteStagingBuffer`，并让 `write_from_user` 与 `pwrite_from_user` 的 UserBuffer 路径恢复为按 `user_accessible_len(..., Read)` 限制的翻译范围。
- `os/src/mm/uaccess.rs` — 移除仅为上述路径引入的 `UserBufferReader::len()`。
- `docs/Work_Log/evidence/2026-07-29/slab-fix-*-qemu-output.log` — 归档 RV64 ktest 和 derived-comp 完整串口输出及命令退出状态。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 无新增）。
- 注入 `mask=0x110 skip_apk=true diag=1` 后，`RUSTUP_HOME=/root/.rustup CARGO_HOME=/root/.cargo make ARCH=rv64 -f make/rv64.mk derived-comp` ✅：musl/glibc 的 lmbench 与 iozone 均完成，未见 slab panic 或 kernel error。

**结果摘要（RV64 musl）：** Simple syscall `35.9103 μs`（历史 `34.45 μs`，`+4.2%`）；Pipe latency `580.6582 μs`；iozone 四 initial writers 最大单进程 `656.05 kB/s`（历史 `593 kB/s`，`+10.6%`）；四 readers 最大单进程 `2102.43 kB/s`（无可比历史 max 数据）。

**备注：** `ext4_another::supports_user_buffer_io()` 保持 `false`；Phase 1–3/P0 的其它变更未回退。现有调试知识库已经覆盖“用户声明长度与实际可复制范围必须分离”的通用模式，因此未重复沉淀。

## Oracle round 2 pwrite 热路径优化

**涉及文件：**
- `os/src/syscall/fs/common.rs` — `pwrite_from_user` 直接以 `UserBufferReader` 做一次 faulting 翻译；fallback staging buffer 由静态池转移为局部独占所有权，复制与 `File::pwrite` 均不持有池锁。
- `os/src/mm/uaccess.rs` — `UserBufferReader::len()` 暴露同一次翻译已验证的长度。
- `os/src/fs/page_cache.rs` — 小于一页的写入改为每 16 次检查一次 dirty pressure；整页及以上仍逐次检查，`write_user` 同步采用该策略。
- `os/src/fs/ext4_another/{inode.rs,mutations.rs}` — regular file 声明支持 UserBuffer I/O，并直接调用 `PageCache::write_user`，移除 per-write `Vec` 中转。
- `docs/03_fs/page-cache.md` — 同步 direct UserBuffer 写入与 16 次小写 dirty-pressure 检查语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 无新增）。
- Docker 内 `make run ARCH=rv64 PROFILE=normal` 已进入 iozone 组，但 tools disk 缺少 `iozone_testcode.sh`，musl/glibc 均以 32512 结束；无可用吞吐数据，未声称达到目标。

**备注：** 按 Oracle round 2 约束未改动 `PageCacheBackend`、`read_user`、`read_pages` 或 `write_pages`；io_gate 跨 syscall 批处理（Step 3）依任务建议暂缓。

## ext4_another 写入时间戳延迟持久化

**涉及文件：**
- `os/src/fs/ext4_another/{lifetime.rs,fs.rs,inode.rs,mutations.rs}` — 在 inode lifetime 中缓存 mtime/ctime；写入仅更新原子缓存，`fsync`、`syncfs` 与全局 `sync` 在数据回写后合并为一次 ext4 `setattr`，并在设备 flush 成功后清除 dirty 状态。
- `os/src/fs/vfs/{file.rs,index_node.rs}` — 将写后时间戳更新下沉为 `IndexNode::touch_modified()`；非 another_ext4 后端保留原有 metadata 更新行为。
- `docs/03_fs/{README.md,vfs-core.md}` — 记录 another_ext4 的写后时间戳缓存和同步边界语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make lint` ✅（首方 warning baseline 无新增）。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）。

**备注：** `stat`/`fstat`/`statx` 经 `metadata()` 在 dirty 期间读取 lifetime 缓存；成功 flush 后时间戳已由单次 inode metadata 写持久化。未新增 `unsafe`；AI-Usage-Report 已含本会话模型，无需更新。

## ext4_another PageCache 批量读取与写回暂存复用

**涉及文件：**
- `os/src/fs/ext4_another/page_cache.rs` — 覆盖 `read_pages()`，以一次连续 ext4 读取填充暂存区并按页分发、零填 EOF 尾部；记录 batch read 性能计数。
- `os/src/fs/ext4_another/page_cache.rs` — 为连续写回增加 inode 私有的可复用 `Mutex<Vec<u8>>` 暂存区；I/O 前移出池锁并在所有成功/失败结果后归还，避免每批写回重新分配。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` → `make -C os ktest-run ARCH=la64 PROFILE=normal` ✅（两架构均 64 passed、0 failed）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 无新增）。

**备注：** 未增加 `unsafe`；读取仍按 `logical_size` 截断并保持最后部分页及 EOF 后页为零。

## Phase 1 FS 持久化错误链修复

**涉及文件：**
- `os/src/fs/page_cache.rs` — 全局/合作写回返回并记录错误；partial page 补全失败时将 Writeback 页和计数完整回滚至 Dirty。
- `os/src/syscall/{fs/sys_sync.rs,fs/sys_syncfs.rs,process/misc.rs}` — `syncfs` 传播写回错误，`sync` 记录错误后继续元数据同步；shutdown/reboot 复用刷盘屏障。
- `os/src/fs/ext4/ext4fs.rs`、`os/src/kernel_tests/{runner.rs,page_cache/global_flush.rs}` — 调整新 `Result` API 的必要调用方与回归断言。
- `user/src/bin/test_runner/{bootstrap/packages.rs,mod.rs}` — APK 安装改为可验证 sentinel、逐步失败返回并在 runner 中中止。
- `docs/03_fs/page-cache.md`、`docs/10_plan/fs-persistence-perf-plan.md` — 同步写回失败与电源循环语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker 内 `make -C os ktest-run ARCH=rv64 PROFILE=normal` → `make -C os ktest-run ARCH=la64 PROFILE=normal` ✅（两架构均 64 passed、0 failed）。
- Docker 内 `make lint` ✅（rv64/la64 × debug/release 四格首方 warning baseline 一致）。

**备注：** ktest 收尾会记录 failure-injection PageCache 的 EIO；该错误现已可观测，TAP 测试本身均通过。

## Phase 0 FS 持久化与性能基线测量

**涉及文件：**
- `os_test.conf` — 配置 mask=0x993（basic+busybox+iozone+lmbench+libcbench+LTP），LTP include ~50 FS 回归测试用例
- `docs/Work_Log/evidence/2026-07-29/` — 全部证据归档（git-hash, config, container-id, qemu-output, head-tail, baseline-summary）

**验证：**
- `make build ARCH=rv64 PROFILE=normal` ✅ — 内核+用户态+rootfs 编译通过
- `conf-inject ARCH=rv64` ✅ — os_test.conf 注入 RV64 derived 镜像
- `make run ARCH=rv64 PROFILE=normal` ✅ — QEMU 启动成功，内核正常引导
- `make build ARCH=la64 PROFILE=normal` ✅ — 串行 LA64 构建通过
- `conf-inject ARCH=la64` ✅ — os_test.conf 注入 LA64 derived 镜像
- `make run ARCH=la64 PROFILE=normal` ✅ — QEMU 启动成功，内核正常引导

**结果摘要：**
| 组 | RV64 | LA64 | 根因 |
|----|------|------|------|
| APK (e2fsprogs) | FAIL — DNS transient error | FAIL — DNS transient error | 网络隔离环境下 Alpine 源不可达 |
| basic/busybox | FAIL — scripts not found | FAIL — scripts not found | tools disk 未包含 test scripts（`make build` 不触发 `tools-disk-rv`） |
| lmbench/iozone/libcbench | FAIL — scripts not found | FAIL — scripts not found | 同上 |
| LTP fs ~50 tests | FAIL — suite files not found | FAIL — suite files not found | `/glibc/ltp/runtest/` 在 tools disk 上不存在 |

**关键发现：**
1. `make build` 不构建 tools disk（x1 驱动器），而 `make run development` 需要 tools disk 提供测试套件。正确做法是 `make all`（触发 `tools-disk-rv`/`tools-disk-la`）。
2. APK 需要从 `dl-cdn.alpinelinux.org` 下载包，在无互联网的环境下会失败。
3. PageCache writeback 错误假设（APK/iozone 失败根因）尚未得到验证 — 需要先修复 tools disk 才能产生 writeback 压力。

**Linux 基线对比（来自 bench-artifacts）：**
| 指标 | RV64 Mango (历史) | RV64 Linux | 差距 | LA64 Mango | LA64 Linux | 差距 |
|------|-------------------|-----------|------|-----------|-----------|------|
| Simple syscall | 34.45 μs | 6.06 μs | 5.68x | N/A | 2.37 μs | — |
| Pipe latency | 568.91 μs | 439.62 μs | 1.29x | N/A | 183.54 μs | — |
| iozone writers | 593 kB/s | 5,487 kB/s | 9.25x | N/A | 15,294 kB/s | — |

**备注：** 本次为纯基线测量，未修改任何源码。tools disk 缺失是一个已知的构建流程限制，需要 `make all` 或 `make -C os tools-disk-rv` 来准备。本条目记录 Phase 0 输出的证据集位置。

## user_lib 编译修复：添加默认 target 配置

**涉及文件：**
- `user/.cargo/config.toml` — 增加 `[build] target = "riscv64gc-unknown-none-elf"`，使 `user/` 目录下的裸 `cargo check` 能正确选择目标架构。
- `user/src/bin/test_runner/groups/execute.rs` — 移除 `ltp_include` 非空时强制 inline 模式的条件，改为纯由 `ltp_runner` 配置决定走 inline 还是 suite 路径。

**验证：**
- `cargo check`（user/ 目录，无 `--target` 参数）✅ — Finished with warnings only, zero errors。
- `cargo check --target riscv64gc-unknown-none-elf` ✅ — 同上。

**备注：** 两个问题的根因：
1. `user/` 目录下没有 `build.target` 默认值，裸 `cargo check` 按 host (x86_64) 编译，`#[cfg(target_arch = "...")]` 分支全被剔除，函数体变空。
2. `run_selected_groups` 第 21 行 `!cfg.ltp_include.is_empty()` 短路覆盖了 `ltp_runner = Suite` 的默认配置，导致 `ltp_include=pipe13` 时走 `run_ltp_binaries`（扫描真实二进制文件名）而非 `run_ltp_suite_runner` → ltprunner（解析 runtest 文件按用例名过滤）。二进制扫描目录 `/glibc/ltp/testcases/bin/` 可能不存在或缺少对应二进制，子进程静默退出（`exit(0)`），用户只见 START/END 中间无输出。

## SysV 满队列发送与 waitpid 阻塞回归

**涉及文件：**
- `user/src/bin/regression/regression_ipc_msg.rs` — 通过 `IPC_STAT`/`IPC_SET` 将私有队列限制为一个消息；若容量设置不可用则以 `IPC_NOWAIT` 有界填满队列。子进程延迟读取后，验证父进程的阻塞 `msgsnd()` 成功返回，并保留 5 秒 watchdog。
- `user/src/bin/regression/regression_child_wait.rs` — 新增延迟 100ms 退出的子进程，验证父进程 `waitpid(pid, &status, 0)` 阻塞至退出通知且报告 status 0；阻塞路径由 5 秒 watchdog 保护。
- `user/src/bin/regression/main.rs` — 注册 `child_wait` 为 TAP 第 22 项，将 `pipe_resize` 顺延至第 23 项并同步总数。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make toolchain-preflight"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "set -o pipefail; cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -3"` ✅。

**备注：** `regression_ipc_sem` 已覆盖零值 semaphore 阻塞、延迟 `semop(+1)` 释放和 watchdog，因此未重复创建仅覆盖相同行为的 semaphore 用例。未发现命中这些用户态回归文件的 `code_paths` 模块文档；本次为既有阻塞测试模式的扩展，未新增独立参考条目。

## WaitQueue 文档移除 10ms fallback

**涉及文件：**
- `docs/05_process/time-sched-rlimit.md` — 将 `WakeTask` 描述对齐为 deadline timer 的 generation 过滤，并说明无限期等待由 Waiter/Waker 显式唤醒。
- `docs/05_process/task-control-block.md` — 移除已不存在的 fallback generation 字段，更新 timer 字段语义。
- `docs/05_process/waitqueue-completion.md` — 明确 Waiter/Waker one-shot 握手支持无限期等待，不使用周期性 fallback。
- `docs/08_testing/README.md` — 更新 WaitQueue 阻塞测试说明为仅由显式事件或 deadline 结束。
- `docs/00_overview/code-comment-style.md` — 用 Waiter/Waker 注释约定替换已移除的 fallback timer 示例。

**验证：**
- `grep -rn 'fallback.*10\|10.*ms fallback\|兜底定时器' docs/ --include='*.md' | grep -v Work_Log` ✅（0 matches）。

**备注：** 仅同步文档；无需更新 `AI-Usage-Report.md`，也没有新增可复用调试经验。

## Pipe 容量扩容与 splice 唤醒回归

**涉及文件：**
- `user/src/syscall.rs` — 补齐 `fcntl(2)` 与六参数 `splice(2)` 的用户态 ABI 封装。
- `user/src/bin/regression/regression_pipe_resize.rs` — 新增满 4 KiB pipe 的阻塞写入→子进程扩容至 64 KiB 场景，以及 pipe→pipe splice 唤醒阻塞 reader 的 baton 场景；所有阻塞路径均由 5 秒 watchdog 保护。
- `user/src/bin/regression/main.rs` — 注册 `pipe_resize` TAP 第 22 项并同步总数。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make toolchain-preflight"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -3"` ✅。
- `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal`（Docker 内串行）✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | grep -E 'REGRESSION RESULT|ok [0-9]|not ok'"` ❌ — 既有第 1–21 项均到达 `ok`，随后新用例的 watchdog 以 exit code 143 终止父进程；在子进程已确认 `F_SETPIPE_SZ(65536)` 成功后，满 pipe 的父写入仍未返回，准确暴露容量扩容未唤醒 blocked writer。

**备注：** 按任务边界未修改内核；splice 分支已实现但因前置 resize wakeup RED 未执行到。未发现需同步的 `code_paths` 模块文档；本次是单一回归覆盖，未新增可复用经验条目。

## TCP connect 回归与 TAP SKIP 计数

**涉及文件：**
- `user/src/bin/regression/regression_net_tcp_connect.rs` — TCP loopback connect 在 delayed accept 后仍无法唤醒，因此改用 Unix abstract stream listener；覆盖延迟 accept、connect 与 payload 传输，5 秒 watchdog 防止无限等待。
- `user/src/bin/regression/main.rs` — 增加独立 `skipped` 计数，并在结果摘要输出 passed、failed、skipped 与 total。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -3"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | grep -E 'REGRESSION RESULT|ok [0-9]|not ok|result'"` ⚠️ — `ok 12 net_tcp_connect` 通过；套件在其后的既有 `pipe_resize` 阶段被 watchdog 终止（exit code 143），因此未抵达 TAP 摘要。

**备注：** Unix stream `connect()` 同步将 `Connected` 推入 listener 队列并立即返回；该 fallback 验证 connect/accept/data 行为但不声称覆盖 TCP handshake 的阻塞语义。

## SysV 消息队列阻塞接收显式唤醒回归

**涉及文件：**
- `user/src/syscall.rs` — 补齐 `msgget`、`msgctl`、`msgsnd` 与 `msgrcv` 的 SysV 消息队列 ABI 封装。
- `user/src/bin/regression/regression_ipc_msg.rs` — 覆盖父进程阻塞接收、子进程延迟发送，以及子进程阻塞接收、父进程延迟发送；两条无限等待路径均由 5 秒 watchdog 终止，并验证 5-byte `hello`、消息类型和阻塞下界。
- `user/src/bin/regression/main.rs` — 注册 `ipc_msg` TAP 回归用例。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make toolchain-preflight"` ✅
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -3"` ✅
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | grep -E 'REGRESSION RESULT|ok [0-9]|not ok'"` ⚠️ — `ok 21 ipc_msg`；套件因既有 `not ok 12 net_tcp_connect` 退出，整体状态为 `FAIL`/143。

**备注：** 未增加满队列发送阻塞分支：队列容量为运行时可调参数，基础收发双向阻塞已直接覆盖本次“显式 wake，无 10ms fallback”回归目标。

## UDP 阻塞 recvfrom 显式唤醒回归测试

**涉及文件：**
- `user/src/bin/regression/regression_net_udp.rs` — 新增 loopback UDP 回归：父进程绑定自动分配端口并阻塞 `recvfrom`；子进程延迟 100ms 后发送 `hello`；校验内容、5-byte 返回值和至少 50ms 阻塞时间，使用 5 秒 watchdog 防止永久挂起。
- `user/src/bin/regression/main.rs` — 注册 `net_udp` 为 TAP 第 13 项并将总数更新为 20。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -3"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | grep -E 'REGRESSION RESULT|ok [0-9]|not ok'"` ⚠️ — 新增 `ok 13 net_udp` 通过；套件因其前的既有 `not ok 12 net_tcp_connect` 以 exit code 1 结束。

**备注：** 未增加 UDP send backpressure 用例：在不依赖内核发送缓冲区大小的前提下无法可靠触发；本次仅覆盖明确要求的阻塞接收唤醒。

## signalfd fork 继承：移除共享状态突变，改为动态队列解析

**涉及文件：**
- `os/src/fs/vfs/file.rs` — `PollWaitQueue` 和 `EventQueueHandle` 增加 `_event_queue: Option<Arc<EventWaitQueue>>` 字段；`queue()` 方法优先使用动态解析路径；`File::read_wait_queue()` 和 `File::read_event_queue()` 增加 SignalFd 动态队列解析。
- `os/src/syscall/process/signal.rs` — 移除 `event_queue: Mutex<Arc<EventWaitQueue>>` 字段、`event_queue()` 和 `rebind_event_queue()` 方法；`SignalFd::new()` 不再接收 `EventWaitQueue` 参数。
- `os/src/syscall/fs/sys_read.rs` — 阻塞 signalfd 读路径改为从 `task.process.signal_event_queue()` 动态解析队列。
- `os/src/task/task.rs` — 移除 clone 路径中的 `rebind_signal_fds` 循环和 `SignalFd` 导入。

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- `make kernel ARCH=la64 PROFILE=normal` ✅

**备注：** 根因：`FdTable::try_clone()` 克隆 `Arc<File>` 而不克隆 inode，fork 后父子进程共享同一 `SignalFd` 对象。旧方案通过 `rebind_event_queue()` 在 fork 时重绑定队列，但该操作会突变共享状态。新方案不存储队列，在等待时从当前进程动态解析，彻底消除共享状态突变。

## WaitQueue 遗留模块用户态回归与 LTP 配置恢复

**涉及文件：**
- `os_test.conf` — 删除遗留的 `ltp_include=pipe13` 过滤项，保留 suite runner 配置。
- `user/src/syscall.rs` — 增加 SysV semaphore、`nanosleep(2)` 与类型安全的 `FUTEX_CMP_REQUEUE` 用户态 ABI 封装。
- `user/src/bin/regression/regression_ipc_sem.rs` — 覆盖 SysV semaphore 零值阻塞、子进程延迟 `semop(+1)` 释放及 5 秒 watchdog。
- `user/src/bin/regression/regression_nanosleep.rs` — 覆盖 50ms `CLOCK_MONOTONIC` 睡眠和被 SIGUSR1 打断后 `nanosleep` 剩余时间。
- `user/src/bin/regression/regression_futex_requeue.rs` — 覆盖共享页 waiter 的 `FUTEX_CMP_REQUEUE` 与第二 futex 显式唤醒。
- `user/src/bin/regression/main.rs` — 注册三项回归并将 TAP 总数更新为 19。

**验证：**
- `make toolchain-preflight` ✅ — 固定 nightly 工具链可用。
- `make user ARCH=rv64 PROFILE=normal` ✅ — 用户态 release 构建完成。
- `make test ARCH=rv64 PROFILE=regression` ❌ — QEMU 启动前被既有内核编译错误阻断：`os/src/task/task.rs:33` 引用私有的 `crate::syscall::process::SignalFd`（E0603）；本任务禁止修改内核，未越界修复。

**备注：** 所有新增无限期等待均有 5 秒 SIGTERM watchdog；完整命令、容器挂载、配置校验和与阻断原因见 `docs/Work_Log/evidence/2026-07-29/waitqueue-regression.md`。

## TCP waitqueue 条件闭包重入死锁修复

**涉及文件：**
- `os/src/fs/vfs/event.rs` — 删除会丢失任务唤醒的 `try_notify_events_*` 回退 API。
- `os/src/net/socket/{mod.rs,inet/stream/mod.rs}` — 恢复保证唤醒；为 TCP 增加无 poll 的 connect/recv/send 及 message 检查变体。
- `os/src/net/syscall/{connect.rs,recvfrom.rs,recvmsg.rs,sendto.rs,sendmsg.rs}` — 在进入 `wait_until_interruptible` 前 poll，条件闭包仅检查无 poll 状态。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make toolchain-preflight"` ✅。
- `docker exec pxy-mangocore-os-dev-1 bash -c "set -o pipefail; cd /app && make kernel ARCH=rv64 PROFILE=normal 2>&1 | tail -3 && make kernel ARCH=la64 PROFILE=normal 2>&1 | tail -3"` ❌ — rv64 在编译本任务前已存在的 `src/task/task.rs:33` 导入 `crate::syscall::process::SignalFd` 时失败（`E0603`: `process` 为私有模块）；因此未运行 la64，未将失败归因于本次 TCP 改动。
- `cargo fmt --check` ⚠️ 未执行：容器固定 nightly 未安装 `rustfmt` 组件。

**备注：** `NET_INTERFACE.poll()` 只能在 waitqueue 锁外触发；以 `try_lock()` 跳过唤醒会重新引入丢失就绪通知。

## Pipe、signalfd 与 pidfd 唤醒回归测试

**涉及文件：**
- `user/src/syscall.rs` — 增加 signalfd、pidfd、epoll 和 ppoll 的最小用户态 syscall ABI 封装。
- `user/src/bin/regression/regression_pipe_wakeup.rs` — 以 64 个转发子进程覆盖 wake-one pipe 链式进度，并用 epoll 超时监督。
- `user/src/bin/regression/regression_signalfd.rs` — 覆盖默认阻塞 signalfd 读在延迟 SIGUSR1 前保持阻塞，以及信号号解码。
- `user/src/bin/regression/regression_pidfd.rs` — 覆盖 live child exit 与 WNOWAIT 已退出 child 的 pidfd `ppoll(POLLIN)` 就绪。
- `user/src/bin/regression/main.rs` — 注册三项回归并更新 TAP 总数。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — 用户态 release 构建完成。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | tail -30"` ❌ — 首轮 pipe/signalfd 通过，pidfd 的 `epoll_ctl(ADD)` 返回 -1；探针确认 pidfd 当前不具备 epoll 要求的 stream mode。测试改为同样有界的 `ppoll(POLLIN)` 路径；受本轮两次状态检查上限约束，最终修改未重跑。

**备注：** signalfd 以 5 秒 SIGTERM watchdog 防止通知回归时永久阻塞；pidfd 明确以 WNOWAIT 观察 zombie 后再打开，验证已退出目标的稳定就绪状态。

## Eventfd、epoll、futex 与 timerfd 阻塞唤醒回归测试

**涉及文件：**
- `user/src/syscall.rs` — 补充 `eventfd(2)` 与六参数 `futex(2)` 的用户态 syscall 封装。
- `user/src/bin/regression/regression_eventfd.rs` — 覆盖空计数器阻塞读、子进程写入显式唤醒，以及 3/5 的计数器读写语义。
- `user/src/bin/regression/regression_epoll.rs` — 覆盖 pipe 写端显式唤醒无限期 epoll 等待，以及空 pipe 的 100ms 超时。
- `user/src/bin/regression/regression_futex.rs` — 以 `MAP_SHARED | MAP_ANONYMOUS` 页面覆盖跨 fork 的 FUTEX_WAIT/FUTEX_WAKE 和值已变更的 `EAGAIN` 快路径。
- `user/src/bin/regression/regression_timerfd.rs` — 覆盖 50ms `CLOCK_MONOTONIC` one-shot timerfd 的阻塞读取与 8-byte expiration counter。
- `user/src/bin/regression/main.rs` — 注册四项回归并将 TAP 总数更新为 16。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — 用户态 release 构建完成。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression 2>&1 | tail -40"` ❌ — 既有 `regression_pidfd` 在新增用例前失败（`epoll_ctl`/`epoll_wait` 均返回 -1），QEMU 以 exit code 143 退出；新增四项尚未执行。

**备注：** 阻塞等待由 eventfd 写、pipe 写、FUTEX_WAKE 和 timer expiry 明确驱动；每项无限期等待均有 5 秒 watchdog，仅用于失败时终止测试，避免永久挂起。

## Socket blocking I/O wakeup regression tests

**涉及文件：**
- `user/src/bin/regression/regression_net_unix_pair.rs` — 覆盖 Unix stream socketpair 的双向阻塞读写、关闭对端 EOF 唤醒和 datagram `sendto`/`recvfrom`。
- `user/src/bin/regression/regression_net_tcp_accept.rs` — 覆盖延迟子进程连接唤醒父进程阻塞 `accept`，并验证已接受连接传输数据。
- `user/src/bin/regression/regression_net_tcp_connect.rs` — 覆盖延迟子进程 `accept` 唤醒父进程阻塞 `connect`。
- `user/src/bin/regression/main.rs` — 注册三项网络唤醒回归并将 TAP 总数更新为 12。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make user ARCH=rv64 PROFILE=normal 2>&1 | tail -5"` ✅ — release user build 完成；输出仅包含既有 `inet_test` warnings。

**备注：** 每项网络回归均由 5 秒 watchdog 兜底，避免阻塞唤醒失效时永久挂起测试套件。

## WaitQueue one-shot handshake kernel tests

**涉及文件：**
- `os/src/kernel_tests/waitqueue.rs` — 注册 WaitQueue 状态机覆盖用例，并按行为拆分测试模块。
- `os/src/kernel_tests/waitqueue_blocking.rs` — 覆盖阻塞唤醒、无 10ms 回退、注册后通知、双队列清理、deadline 和陈旧 weak waiter。
- `os/src/kernel_tests/waitqueue_wake.rs` — 覆盖 FIFO 单唤醒、广播唤醒与 1000 次 spawn/wait/wake 压力。
- `os/src/kernel_tests/waitqueue_interrupt.rs` — 覆盖信号中断及信号与显式唤醒的 Ready/Interrupted 优先级。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make -C os ktest-run ARCH=rv64 PROFILE=normal"` ✅ — 64 passed, 0 failed。
- `docker exec pxy-mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=rv64 PROFILE=normal && make kernel ARCH=la64 PROFILE=normal"` ✅。

**备注：** 当前 ktest harness 仅暴露 `spawn_ktest_task(fn())` 与 `wake_at_most(1)`；FIFO 用例通过后者覆盖内部 `wake_one` 路径，信号用例通过 `send_signal_to_interruptible(SIGUSR1)` 定向唤醒唯一的阻塞 ktest worker。
