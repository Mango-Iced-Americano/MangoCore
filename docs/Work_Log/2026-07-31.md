## 2026-07-31

### RV64 bootstrap leaves 写入 Image header — VF2 pre-heap 无输出修复

**涉及文件：**
- `os/src/hal/arch/riscv/entry.asm` — 在构造低半 identity 与高半 alias 的 PTE 前，显式加载 `bootstrap_root_page_table`；不再误用残留的 `riscv_image_header` 指针作为 SATP root。
- `scripts/test-rv64-standard-fdt-boot-contract.sh` — 回归约束 entry 必须经实际 bootstrap 页表写入 leaf PTE。

**根因：** 保存 `RAW_IMAGE_PADDR` 后，`t0` 仍指向 Image header；旧代码随即把 root PTE 写入该 header 并将其设为 SATP root。QEMU 仅访问被偶然覆盖的根项仍能启动，VF2 在非页对齐 board DTB 的 pre-heap 路径中于 `Starting kernel ...` 后静默失效。

**验证：**
- RED：新增 bootstrap root contract 对旧 entry 报失败 ✅
- GREEN：RV64 FDT boot contract ✅；`make kernel ARCH=rv64 PROFILE=normal` ✅；artifact contract ✅；`make kernel ARCH=la64 PROFILE=normal` ✅

**备注：** 修复 Image 已部署至 TFTP；上一轮 panic loop 不响应 Ctrl-C，等待下一次物理重启完成实板验证。

### VF2 board FDT address alignment — accept standard word-aligned handoff

**涉及文件：**
- `os/src/hal/firmware/mod.rs` — 将 RV64 FDT 输入边界从错误的页对齐收紧改为 `u32` 所需的 4-byte 对齐。
- `os/src/hal/firmware/fdt.rs` — 在 header volatile `u32` 读取和 FDT snapshot 捕获路径使用同一 4-byte 对齐约束。
- `scripts/test-rv64-standard-fdt-boot-contract.sh` — 锁定“拒绝非 word-aligned、接受非页对齐 FDT”的启动 ABI 合同。

**根因：** VF2 U-Boot 按标准把板载 DTB 传入 `a1=0xf76df9b0`；该地址满足 `u32` 对齐，但不满足项目错误要求的 4 KiB 对齐，内核在 pre-heap FDT 解析前 panic：`RV64 boot requires an aligned FDT in a1`。

**验证：**
- RED：更新后的 FDT boot contract 在旧实现报 3 项失败 ✅
- GREEN：`scripts/test-rv64-standard-fdt-boot-contract.sh` ✅
- `make kernel ARCH=rv64 PROFILE=normal` ✅；`scripts/test-rv64-image-artifact.sh` ✅（9,735,376 bytes）
- `make kernel ARCH=la64 PROFILE=normal` ✅
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed, 0 failed, 1 skipped）
- VF2 第一次真实 `booti 0x40200000 - $fdtcontroladdr` 已验证 TFTP bytes、magic2 和首个 panic；修复 Image 已部署，等待重启后复验。

**备注：** pre-fix `booti` 后 Ctrl-C 未返回 U-Boot，必须物理重启；不会保存 U-Boot env。

### RV64 统一高-VMA FDT 启动 — AlreadyMapped panic 修复

**涉及文件：**
- `os/src/hal/arch/riscv/config.rs` — 新增 `KERNEL_PROGRAM_BASE = 0xffff_ffc0_4000_0000`，临时内核 ELF 映射窗口从低 0x2000_0000（与 early MMIO 恒等映射 PTE 碰撞）移至高半区
- `os/src/mm/kernel_space.rs` — `try_insert_program_area` 使用 `kernel_program_base()` 做边界检查；`KernelSpace::new()` 内核段改用 `kernel_map!`（经 `kernel_linked_to_phys` 转换）替代旧 `kernel_identical_map!`
- `os/src/mm/mod.rs` — 导出 `kernel_program_base`
- `os/src/task/task.rs` — `load_elf` 和 `TaskControlBlock::new` 使用 `kernel_program_base()` 做临时映射/解除

**根因：** `MMAP_BASE=0x2000_0000` 在高-VMA 布局下与 early MMIO（FDT `reg` 范围）的恒等映射 PTE 碰撞，`insert_program_area` 返回 `MemoryError::AlreadyMapped`。

**验证：** RV64 normal 编译 ✅、regression ✅（原始 panic 消失）

### RV64 trampoline PPN 转换修复 — sret 超时

**涉及文件：**
- `os/src/mm/kernel_space.rs` — 内核页表 trampoline PPN 改用 `kernel_linked_to_phys(strampoline)`
- `os/src/mm/address_space.rs` — 用户页表 trampoline（含 signal trampoline）统一用 `kernel_linked_to_phys`

**根因：** 高-VMA 链接后，`strampoline` 作为高半区虚拟符号被直接当物理 PPN 使用，导致 trampoline 映射到无效物理地址 → `sret` 无限异常循环（异常向量同样失效，Rust 页故障处理器无日志）。

**验证：** `make test ARCH=rv64 PROFILE=regression` → `=== REGRESSION PASS ===` ✅

### LA64 root Makefile BLK_MODE 修复 + rng import 兜底

**涉及文件：**
- `Makefile` — 移除 root `kernel`/`arch-build` target 对 LA64 的 `BLK_MODE=$(BLK_MODE)` 传递（root 默认 `virt` 覆盖了 `orchestration.mk` 的 `LA64_BLK_MODE=virt_pci`）
- `os/src/drivers/rng/mod.rs` — `boot_la_qemu` fill_entropy 加 `block_virt_pci` feature gate
- `os/src/main.rs` — `configure_runtime_console()` 加 `#[cfg(target_arch = "riscv64")]` gate

**验证：** `make kernel ARCH=la64 PROFILE=normal` ✅

### RV64 SYSTEM_TASK_LIMIT 运行时 FDT 钳位

**涉及文件：**
- `os/src/hal/arch/riscv/config.rs` — `SYSTEM_TASK_LIMIT` 编译期上界改 16 GiB；注释标注运行时钳位位于 `quota.rs`
- `os/src/task/quota.rs` — 强制点新增 `task_limit()` 用 `firmware::usable_memory_size()` 钳位

**验证：** RV64 normal 编译 ✅、regression ✅

### 残留 stale 注释清理

**涉及文件：**
- `os/src/hal/arch/riscv/mod.rs` — "QEMU/K210/FU740/VisionFive2" → "统一 FDT 启动"
- `os/src/drivers/serial/mod.rs` — 移除 K210 引用
- `os/src/hal/firmware/fdt.rs` — 移除硬编码 virtio 槽地址注释，保留排序说明

**验证：** RV64 normal 编译 ✅

### 最终串行双架构全量验证

| 命令 | 结果 |
|------|------|
| `make kernel ARCH=rv64 PROFILE=normal` | ✅ |
| `make test ARCH=rv64 PROFILE=regression` | ✅ `REGRESSION PASS` |
| `make -C os ktest-run ARCH=rv64 KTEST=platform,platform_resources,platform_fdt_snapshot` | ✅ 13/13 passed |
| `make kernel ARCH=la64 PROFILE=normal` | ✅ |

---

### 对齐 lint 与现行架构 feature 合同

**涉及文件：**
- `scripts/lint-check.sh` — RV64 lint feature 从已删除的 `board_rvqemu` 改为现行 `riscv`；LA64 从已删除的 `board_laqemu` 改为 `boot_la_qemu`，其余 block/initramfs/oom feature 保持不变。

**验证：**
- Docker 内 `make lint` 的原始失败 `package os ... does not have the feature board_rvqemu` 已消失；RV64/debug 已成功进入 rustc warning baseline 比较。
- lint 尚未通过：当前工作树在 `src/hal/firmware/fdt.rs`、`src/hal/platform/info.rs`、`src/fs/boot_block.rs`、`src/hal/arch/riscv/sbi.rs`、`src/kernel_tests/{block_publication,platform}.rs` 和 `src/fs/mod.rs` 产生新的 first-party warnings，不能用 rebaseline 掩盖。

**备注：** 前两项是本次 FDT/platform 变更需要后续消除的 warning；block publication、SBI 和其他测试文件属于已有并发 WIP。由于 warning baseline 未恢复，`make lint` 不能验收为通过。

### RV64 PCI ECAM 与 BAR MMIO 改为 FDT 驱动

**涉及文件：**
- `os/src/hal/platform/info.rs` — 新增不可变 `PciHost`，由 `PlatformInfo::pci_host()` 向驱动暴露 ECAM 与 BAR MMIO 资源。
- `os/src/hal/firmware/fdt.rs` — post-heap 从有效 enabled PCI host 的 `reg`/`ranges` 解析 `PciHost`；pre-heap 同时将 PCI memory `ranges` 加入 early MMIO 映射，避免 BAR 访问 page fault。
- `os/src/drivers/block/virtio_blk_pci.rs` — RV64 优先使用 FDT ECAM；仅缺少可用 host 时 warning 并回退到 `0x3000_0000`；清理正常 FDT 路径的临时信息输出。
- `os/src/drivers/rng/mod.rs` — `block_virt_pci` 构建下导入 VirtIO HAL。
- `os/make/rv64.mk`、`os/make/arch/rv64-settings.mk` — 支持 `BLK_MODE=virt_pci` 并为 RV64 QEMU 配置 PCI VirtIO block/net device。
- `os/src/kernel_tests/platform_fdt_snapshot.rs` — 覆盖 QEMU FDT 的 ECAM `0x3000_0000`、BAR window `0x4000_0000..0x8000_0000` 与启用的 host 语义。
- `docs/01_architecture/hal-and-platform.md`、`docs/09_debug/rv64-pci-ecam-not-mapped.md` — 同步 FDT PCI 资源与已修复的映射故障说明。

**验证：**
- Docker container `79040401ef8a`（`/root/projects/MangoCore -> /app`）内 `make toolchain-preflight` ✅
- 串行 `make kernel ARCH=rv64 PROFILE=normal && make kernel ARCH=la64 PROFILE=normal` ✅
- `timeout --foreground 90 make run ARCH=rv64 PROFILE=normal BLK_MODE=virt_pci` ✅：预期超时状态 `124`，QEMU 已发现两个 VirtIO PCI block device，并完成 root/tools 块设备注册与 PID1 启动。

**证据：** `docs/Work_Log/evidence/2026-07-31/rv64-pci-final-{command,config,environment,qemu-output,qemu-head-tail}.txt`；完整 QEMU 输出生成时间晚于相关 FDT/PCI 源码修改。

**备注：** 不修改 `MMAP_BASE` 或其他地址布局常量；LA64 继续走既有静态 PCI ECAM 路径。QEMU normal run 在测试盘缺失 suite 脚本后仍运行，90 秒 timeout 仅是 smoke 验收边界，不作为测试组通过声明。

### SYSTEM_TASK_LIMIT 改为 FDT 感知 + 清理过期板卡注释

**涉及文件：**
- `os/src/hal/arch/riscv/config.rs` — `SYSTEM_TASK_LIMIT` 的编译期 RAM 假设由硬编码 1 GiB 改为 16 GiB 保守上界（`const` 不能调用运行时函数，且 `manager.rs` 的 `DEFAULT_SIZE` 依赖其为 `const`）；运行时在 `task/quota.rs` 用 `firmware::usable_memory_size()` 做钳制。
- `os/src/task/quota.rs` — 新增 `task_limit()`：`min(SYSTEM_TASK_LIMIT, usable_memory_size() / (KERNEL_STACK_SIZE * 4))`（下限 512），`try_acquire()` 硬上限改用它。LA64 上 `usable_memory_size()` 走静态回退，钳制为恒等，行为不变。
- `os/src/hal/arch/riscv/mod.rs` — 模块注释去掉 K210/FU740/VisionFive2 过期引用，改为统一 FDT 启动。
- `os/src/drivers/serial/mod.rs` — 移除 "For K210, we use k210-pac directly" 过期注释。
- `os/src/hal/firmware/fdt.rs` — 排序注释去掉硬编码 virtio 地址（x0/x1 MMIO），保留按地址升序排序的说明。

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed, 0 failed, 1 skipped，`=== REGRESSION PASS ===`）

**备注：** 工作树含进行中的 vf2 板卡支持重构；初次 regression 编译失败（`pci_host` 缺失）是并发编辑的中间态，随后由同批 WIP 提交补全，重跑通过。未改动 LA64 代码。

### 修复 RV64 高 VMA 下的 trampoline 物理映射

**涉及文件：**
- `os/src/mm/kernel_space.rs` — 将内核页表中、SATP 切换前执行的 `strampoline` 链接虚拟地址转换为运行时物理地址。
- `os/src/mm/address_space.rs` — 将每个用户页表的 trampoline 与 signal trampoline 链接虚拟地址转换为运行时物理地址。

**验证：**
- `LOG=error make test ARCH=rv64 PROFILE=regression` ✅（输出 `=== REGRESSION PASS ===`）
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- `make kernel ARCH=la64 PROFILE=normal` ⚠️ 未通过：现有工作树中的 `virtio_blk_pci` unresolved import 与 `configure_runtime_console` 缺失，未改动 LA64 代码。

**备注：** 高链接内核 VMA 下，`trap_return()` 在切换到用户 SATP 前先通过内核页表跳入 trampoline；内核和用户两侧若把链接虚拟符号直接作为 PPN，首个异常的 `stvec` 也会落到同一错误页，因而不会进入 Rust 页故障处理器。
---
date: 2026-07-31
---

# Work Log — 2026-07-31

## PageCache read lookup 批量目录锁

**涉及文件：**
- `os/src/fs/page_cache.rs` — 多页 `lookup_read_range_fast()` 以 512 页为上限批量持有一次 `PageEntries` 读锁；计划 Vec 均在入锁前预分配，partial-valid 索引由有序去重的 `BTreeSet` 改为按升序扫描填入的 `Vec`；通过 `reference_and_load_flags()` 用一次 `AcqRel fetch_or` 同时保留逐页 `PG_REFERENCED` 标记并取得旧 flags 解码状态。
- `docs/03_fs/page-cache.md` — 同步多页 lookup 的锁范围、原子序与锁外 I/O/copy 约束。
- `docs/Work_Log/evidence/2026-07-31/read-batch-lock-20260801/` — Docker 构建、lint、双架构 ktest、A/B iozone 串口、命令和元数据。

**验证：**
- Docker `os-dev` 内串行 `make kernel ARCH=rv64 PROFILE=normal`、`make kernel ARCH=la64 PROFILE=normal` 均通过；`make lint` 四格首方 warning 基线均通过 ✅。
- 对最终 `os/src/fs/page_cache.rs` 运行 `rustfmt` 后，重新串行执行上述 RV64/LA64 normal kernel 构建，均通过；`rustfmt --check src/fs/page_cache.rs` 通过 ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` 与 `ARCH=la64` 均为 65/65 PASS（当前套件为 65 项）✅。
- 使用固定 `mask=0x110`、`perf_diag` 的 RV64 iozone 各完成 baseline/candidate 三次，六次均以 `run_selected_groups done` 结束 ✅。

**备注：** 当前 iozone 脚本固定 `-r 1k -s 1m`，实测 `pc_read_pages / pc_read_calls` 约 1.0，因此几乎全部落在单页 fast path，不执行本次多页 batch lookup。三次中位数的 musl/glibc `pc_lookup_cycles` 分别为 baseline 655.58M/573.33M、candidate 698.66M/597.91M；该工作负载不能验证目标的 1MiB、256 页 lookup 降幅。`pread` Children 中位数为 musl 7742.04→8058.82 kB/s（+4.1%）、glibc 7909.16→8111.55 kB/s（+2.6%）；pwrite 为 musl 8793.26→8968.28 kB/s（+2.0%）、glibc 8928.08→9276.59 kB/s（+3.9%）。需要另建 1MiB record 的读 workload 才能验收 Oracle 的 ≥50% lookup / ≥15% 读指标目标；未改动现有评测脚本。`rust-analyzer diagnostics /app/os --severity error` 不能接收 editor 的 `rust-analyzer.cargo.features` 设置，固定只用 default features；这会遗漏 RV64 所需的 `board_rvqemu`/`riscv`/`block_virt`，使 build.rs 和架构 cfg 路径失配，输出 451 项全局 `E0432`/macro-error（其中 `page_cache.rs` 有 52 项派生诊断）。该命令的 `[build] target` 可由 `os/.cargo/config.toml` 提供，但 feature 限制仍使其不能作为 clean LSP 门禁；`rust-src` 已安装且没有 `E0463`/sysroot 失败证据。

## PageCache read-stage profile gate alignment

**涉及文件：**
- `os/src/fs/page_cache.rs` — 仅将 `read()` 和 `read_user()` 的所有读路径计时边界从 core-profile `perf_time_now()` 切换为 memory-I/O-profile `perf_memory_io_time_now()`；未改变锁、控制流或写路径。
- `docs/Work_Log/evidence/2026-07-31/read-stages3-iozone-*` — 归档 RV64 perf_diag iozone 完整串口、配置、命令、容器/git 元数据和阶段比例。

**验证：**
- Docker `os-dev` 内串行 `make kernel ARCH=rv64 PROFILE=normal`、`make kernel ARCH=la64 PROFILE=normal` 和 `make lint` 均通过；lint 四格保持首方 warning 基线 ✅。
- `make build ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag`、配置注入和 `make -C os rv64-derived-run` 以 exit `0` 完成；musl/glibc iozone 均为 `exit_code=0`，日志以 `run_selected_groups done` 结束 ✅。

**备注：** `memory_io` profile 下 `pc_read_cycles_total`、`pc_lookup_cycles`、`pc_copy_cycles` 已恢复非零。iozone 未走 `read_user()`，所以其专属四个 `pc_read_*_cycles` 桶仍为 0；完整解释与读/写阶段占比见 `read-stages3-iozone-summary.md`。`docs/03_fs/page-cache.md` 已准确描述 memory-I/O profile 门控，无需同步修改。

## RV64 iozone read-stage data collection

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-31/read-stages2-iozone-qemu-output.log` — RV64 QEMU 的完整 serial 输出。
- `docs/Work_Log/evidence/2026-07-31/read-stages2-iozone-summary.md` — musl/glibc 吞吐、完整 blockio 快照与 PageCache 阶段对照。
- `docs/Work_Log/evidence/2026-07-31/read-stages2-iozone-metadata.md` — 命令、容器、挂载、git revision 和测试配置。

**验证：**
- Docker `os-dev` 内 `make -C os rv64-derived-run` 以 exit `0` 完成，两个 iozone libc 组均为 `exit_code=0`，并输出 `run_selected_groups done` ✅。

**备注：** 两个 iozone snapshot 的 `pc_read_lookup_cycles`、`pc_read_miss_fill_cycles`、`pc_read_valid_fill_cycles`、`pc_read_copy_cycles`、实际 `pc_lookup_cycles`/`pc_copy_cycles` 和 `pc_read_cycles_total` 全为 0；同时 `pc_read_calls/pages/miss` 非零。这与 ext4-another 尚未实现 `read_at_user`、iozone 走 kbuf fallback 一致。写路径四阶段计数器均非零，详细数据已归档。

## PageCache read_user 阶段性能打点

**涉及文件：**
- `os/src/fs/page_cache.rs` — 在单页、批量和 fallback 读取路径中分别记录 lookup、miss-fill、valid-fill，以及每次 `UserBuffer::write_at()` 的 copy 周期；未改变读取、锁或原子操作语义。
- `os/src/task/perf.rs` — 新增 read-user 四阶段计数器、record 函数、reset 和 feature-off stubs。
- `os/src/fs/sysfs/files/diag.rs` — 在 `/sys/kernel/stats/blockio` 导出四项 read-user 阶段周期值。

**验证：**
- Docker（`os-dev`，`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅；完整输出：`docs/Work_Log/evidence/2026-07-31/read-user-stages-build.log`。
- Docker 内 `make lint`：rv64/la64 × debug/release 四格 warning 基线均通过 ✅；完整输出：`docs/Work_Log/evidence/2026-07-31/read-user-stages-lint.log`。

**备注：** 仅补齐性能归因；QEMU/iozone 留待使用新增 `/sys/kernel/stats/blockio` 字段的后续诊断运行。

## PageCache valid-mask word AMO fast path

**涉及文件：**
- `os/src/fs/page_cache.rs` — 将 `PageEntry` 的 flags 和 valid-mask 原子字段改为 `AtomicU32`，保持 16B 布局断言；已存在目标 valid bits 的 1KiB 写入以 Relaxed load 直接返回，避免冗余 `fetch_or`。
- `docs/03_fs/page-cache.md` — 同步 word atomic、低 8 位 valid-mask 和无 AMO 快路径说明。

**验证：**
- Docker（`pxy-mangocore-os-dev-1`，`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os KTEST_QEMU_TIMEOUT=60 ktest-run ARCH=rv64 PROFILE=normal`：65/65 PASS ✅。
- `make lint`：rv64/la64 × debug/release 四格 warning 基线均通过 ✅。
- `mask=0x110` 的 RV64 iozone（1KiB record）完整通过；4 process pwrite parent throughput：musl 3455.68 kB/s、glibc 4816.66 kB/s，均超过 3100 kB/s 目标 ✅。

**备注：** `valid_mask` 仅置位、不清位，因此已覆盖的目标掩码不可能发生新的“变为全有效”转换；并发写入其他位仍由后续 `fetch_or` 合并。

## 1KiB pwrite PageCache 阶段计数恢复

**涉及文件：**
- `os/src/task/perf.rs` — 新增 PageCache 写入 lookup、lease、copy 与 commit 的 memory-I/O 周期原子值，并接入 reset 与 feature-off no-op。
- `os/src/fs/page_cache.rs` — 仅在 `write_user()` 调用路径收集四个阶段；所有 PageCache writeback retry 点仅在存在当前任务时调用调度器，否则短暂自旋，避免非任务上下文触发 `task/mod.rs:114`。
- `os/src/fs/sysfs/files/diag.rs` — 将四项计数导出至 `/sys/kernel/stats/blockio`。
- `docs/09_debug/perf_diag.md`、`docs/03_fs/page-cache.md` — 同步计数器 ABI 和写入阶段语义。

**验证：**
- Docker 内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` → `make build ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- 注入 `mask=0x110`、`diag=1`、`skip_apk=true` 后，`make -C os rv64-derived-run` 完成 musl/glibc iozone；日志没有 `task/mod.rs:114` panic，并打印完整 group-end blockio counters ✅。
- `timeout 60s make ktest ARCH=rv64 PROFILE=normal` 在本容器因首次 ktest 专用产物编译超时，未能验收通过；日志见 `docs/Work_Log/evidence/2026-07-31/perf-diag-ktest.log`。

**备注：** 新鲜数据已写入 `/tmp/ulw-precise4.txt`；两个 libc 均为 4,096 次 pwrite 窗口，完整 QEMU 及容器、配置、git 元数据保存在 `docs/Work_Log/evidence/2026-07-31/`。AI 工具报告已包含 GPT-5.6-terra，无需更新。
