# 2026-07-31 Work Log

## 2026-07-31

### B37 affinity-aware 新任务初始放置

**状态：`pass`；默认 CPU0-only mask 保持不变。** `publish_task()` 不再无条件把普通
新任务投到 CPU0，而是按任务继承的 `cpus_allowed`、当前可调度 CPU 和近似负载选择首次
owner。这样父线程已经收紧到非 CPU0 mask 后执行 clone/fork，子任务不会在首次发布时触发
“CPU0 不在允许集合”断言。远程 Running/Blocking affinity、默认全核 mask 和 steal 仍未实现。

**实现与原因：**

- `run_queue::select_runnable_cpu()` 统一过滤 affinity、online、scheduler-ready 和 STOP，
  preferred CPU 的负载不超过最小值 `+1` 时保留 locality，否则选择
  `nr_running + current_present` 最小且 ID 最小的 CPU。计数是无锁快照，只决定放置质量，
  owner 仍由 runqueue 临界区和 `sched_state` 提交。
- `CpuTaskState.current_present` 在 current 槽安装后 Release 置位，在 idle 栈真实取走 current
  后 Release 清零。此前的 `cpu_has_current()` 需要取得 processor 锁；用原子提示避免 Blocked
  wake 在持有 `TASK_MANAGER` 时新增 `TASK_MANAGER -> processor` 锁序。
- `publish_task()` 只有在本 CPU 没有 current 的启动窗口显式走 CPU0。init/ktest runner 的
  发布早于 BSP 进入 `run_tasks()`，此时 scheduler mask 尚无 bit0；把它交给通用过滤会制造
  空候选。普通 clone/fork 均有 current，走通用选择器并把调用 CPU 作为 locality 提示。
- `spawn_ktest_task_on()` 设置单 bit mask 后改走普通 `publish_task()`。现有 AP 定向测试因此
  动态覆盖生产选择器，不增加测试专用生产字段或临时 IPI reason。
- current 自迁移、Queued affinity 和 Blocked wake 复用同一个选择器；目标 owner 的最终合法性
  仍由各自已有的 runqueue/registry 协议复核，不把负载快照当作同步原语。

**DeepSeek 协作与人工裁决：**

- 首轮只读审查与 B38 预审均因 Codex 在其运行期间继续编辑 tracked diff，被本地包装器按
  “visible Git state changed” fail-closed；模型进程没有写源码。报告指出启动期 scheduler mask
  和潜在 processor 锁序，最终实现分别采用显式 bootstrap 分支与原子 current 提示。
- 首次验证又因误用旧容器 `lzm-cagent-run` 的 `/app` 挂载而在 gateway 首项失败；该结果没有
  进入通过证据。最终任务固定到 `mangocore-smp-integration-20260725-os-dev-1`，四项构建/focused
  与两项初赛均保持源码指纹不变。
- DeepSeek 建议当前默认 bit0 不会改变普通任务行为；人工补充边界是：已由
  `sched_setaffinity` 改成非 CPU0 mask 的父线程，其 clone 子任务现在会按继承 mask 合法发布，
  这正是 B37 要修复的行为，不应描述为“所有 clone 行为完全不变”。

**验证：**

- 被测 HEAD：`85526b978994f4ab31191290e7b914fec69e52a6`；功能 diff SHA-256：
  `af6397f9af924b2994a1cb59d8e7ce480b3cd9bb55d9c1f2e96ecf1a2bcd26f3`。
- RV64/LA64 `CORE_NUM=8` kernel build 分别用时 133.645/134.758 秒，exit 0。
- RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 分别用时 141.275/138.218 秒，均为
  23/23；第 2/11/13/14/23 项和最终 PASS 均存在，无 panic、非法 affinity、owner mismatch
  或 timeout。
- RV64 初赛 312/314，仅两套 busybox `kill 10`；LA64 初赛 308/314，仅两套
  `test_brk` 各 1/3 与两套 busybox `kill 10`。双架构 fork/clone 和其余 busybox 项无新增失败。
- 六个 child 均 exit 0、无 forbidden marker、无 mutation，源码前后指纹一致。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b37-affinity-placement-summary.md`。

**经验与文档同步：** 通用放置策略的负载值只能是提示，正确性必须由最终 owner 容器复核；
启动期 CPU0 是 scheduler mask 尚未发布时的显式例外。已同步调度器、锁/测试边界、SMP 计划、
根 `AGENTS.md` 和 AI 使用报告。

**下一步建议：** B38 为远程 Running/Blocking affinity 增加 owner 安全点请求与完成确认。
不得远程直接写一个排除当前 owner 的 mask 后立即返回；也不得把 current 借用 queued-only
`Migrating` 状态表示已经切离。

### B38 远程 Running/Blocking affinity owner 交接

**状态：`pass`；没有新增调度状态。** 正 TID 的 `sched_setaffinity()` 现在可以处理正在其它
CPU 运行或正进入阻塞窗口的线程。若新 mask 仍包含当前 owner，写侧在线性化点原地更新；若
排除 owner，则 syscall 必须等 owner 在现有任务安全点完成
`Running(source) -> Queued(target)` 后才返回，避免 mask 与 current owner 短暂不一致。

**实现与原因：**

- 每个 TCB 增加唯一 `remote_affinity_request` 槽；请求对象只保存不可变的 mask、target 和
  `Pending/Applied/Retry` 完成态。它是 owner 交接的会合对象，不是第二套任务状态机，也不改变
  `TaskStatus` 的调度所有权真值。
- 远程写侧先按 affinity/online/scheduler/STOP 规则选择目标并同步该任务的内核栈映射，再在
  请求槽锁内复核 `Running(owner)`、本地迁移请求与既有远程请求。锁外发送 RESCHEDULE，调用者
  用既有 `suspend_current_and_run_next()` 协作等待；因此不在持锁区等待 IPI、TLB ack 或调度。
- owner 已切回 idle 栈后，`finish_switch_out()` 持请求槽锁取走请求、发布新 mask，并通过单个
  目标 runqueue 完成状态/容器交接，最后以 Release CAS 标记 Applied。真实锁嵌套是
  `remote_affinity_request -> 单个 RunQueue`；它保证第二个写者不能插入检查与交接之间，且没有
  `RunQueue -> remote_affinity_request` 反向路径。
- `begin_interruptible_sleep()` 固定使用
  `TASK_MANAGER -> remote_affinity_request`，把 `Running -> Blocking`、registry 登记与旧请求取消
  放在同一临界区；远程调用者观察到 Retry 后再等待状态稳定，复用 Running 或 Blocked 协议。
  `mark_zombie()`、current 自迁移和显式 migration 同样通过请求槽取消旧请求，防止退出或本地
  写侧与远程写侧共同声明 owner。
- syscall 保留 Linux raw mask/TID/uaccess 语义；这里只替换非 current Running/Blocking 的
  `EOPNOTSUPP`，New/Zombie 或已离开可管理容器的对象仍返回不支持。
- focused 新增第 15 项 `smp::running_affinity_waits_for_owner_handoff`：先以 bit0|bit1 验证
  owner 合法时不迁移，再以 bit0 验证远程调用直到 CPU1 current 已释放才返回，任务最终恰好
  在 CPU0 恢复一次并成为 Zombie。STOP 顺延到第 24 项，证明没有遗留队列或请求污染。

**锁序与证据边界：**

- 当前新增关系为 `task.inner -> remote_affinity_request -> 单个 RunQueue` 与
  `TASK_MANAGER -> remote_affinity_request -> 单个 RunQueue`；实际路径不同时持有
  `task.inner` 和 `TASK_MANAGER`。请求完成只做原子 CAS，不唤醒 WaitQueue、不取得 manager。
- 动态用例覆盖一个远程请求者与稳定 Running owner，并覆盖 mask 包含/排除 owner 两条分支。
  两个请求者同时更新同一远程 TID、以及确定性命中 `Running -> Blocking` 瞬态，仅完成了槽锁、
  Retry 和锁序源码审计，尚未做竞争压力测试。
- 普通用户任务默认 mask 仍是 bit0；B38 完成写侧协议不等于已经开放默认全核调度。

**DeepSeek 协作与人工裁决：**

- 首轮冻结验证发现 `manager.rs` 通过父模块重导入私有请求类型导致编译失败；采用 sibling
  module 直接导入修正，没有把内部协议暴露成公共 API。
- 最终冻结任务保持源码指纹不变，完成双架构 build、8 核 focused 与初赛。DeepSeek 汇总中
  “取得 runqueue 前已释放请求槽”的描述与源码不符，且一处 TAP 数量统计不准确；人工按源代码
  和 raw TAP 修正为“持请求槽跨单个目标 runqueue 临界区”与 24/24，不传播模型误述。
- 曾误用 `cargo fmt -- <files>` 导致 formatter 扩散到整个 crate；所有无关机械格式化均已撤回，
  最终 tracked diff 只包含 B38 功能、测试与文档，不以格式噪声扩大审查面。

**验证：**

- 被测 HEAD：`4da0579a90f15dbf2a2a914b246e3c011d31ba00`；功能 diff SHA-256：
  `4875482b6e06f089eb1c3060a6c20259c902a66a6e44c19ca746c6b42c44b465`；测试前后源码一致。
- Docker 内严格串行 RV64/LA64 `CORE_NUM=8` kernel build，分别用时 135.158/137.229 秒，exit 0。
- Docker 内 RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 分别用时 135.585/138.235 秒，均为
  24/24；新第 15 项、用户 affinity probe 和第 24 项 STOP 均明确 PASS，online mask 为 `0xff`。
- 初赛 `mask=0x003`：RV64 312/314，仅两套 busybox `kill 10`；LA64 308/314，仅两套 basic
  `test_brk` 各 1/3 与两套 busybox `kill 10`。无新增 fork/clone/exec 失败、panic、timeout、
  forbidden marker 或源码 mutation。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b38-running-affinity-summary.md`。

**经验与文档同步：** 远程运行态属性更新需要“请求—owner 安全点提交—完成确认”，请求对象
只承载握手，不能成为第二份 owner 状态；所有报告必须准确写出请求槽跨目标 runqueue 的实际
锁嵌套。已同步调度器、锁序、测试门禁、SMP 计划、根 `AGENTS.md` 和 AI 使用报告。

**下一步建议：** B39 在共享子系统门禁允许后，把普通任务默认 affinity 从 bit0 放宽到
configured online mask，并用真实 fork/clone workload 验证多核分布；若门禁尚未满足，则先做
远程 affinity 多写者与 Blocking 交界压力用例，再进入默认放宽。

### B39 Per-CPU 调度 tick 与 CPU0 全局 timer owner

**状态：`pass`；普通任务默认 mask 仍为 bit0。** B38 后没有直接放宽默认 affinity，因为
AP 当时只靠 syscall/yield/远程 RESCHEDULE 到达调度安全点。若把普通 CPU-bound 用户任务放到
AP，一个不执行 syscall 的循环可能永久占住该 CPU。B39 先补齐每 CPU 本地调度 tick，同时保持
全局 timer callback 只有 CPU0 一个执行者，为后续默认全核调度建立必要的抢占基础。

**实现与原因：**

- `PerCpu` 增加独立的 `sched_tick_deadline_ns`；删除全局
  `NEXT_SCHED_TICK_NS`。每个 CPU 只推进自己的 100 Hz 绝对 deadline，避免多个 CPU 对同一
  deadline 重复 compare/exchange，或一个 CPU 的迟到 tick 改写另一个 CPU 的调度节拍。
- hard timer IRQ 仍只发布本 CPU deferred 标志。`run_task_safe_point()` 与 AP idle 循环在
  任务安全点消费标志、设置 `need_resched` 并重新编程 one-shot timer；不在中断帧中拿普通锁
  或直接 context switch，保持现有安全点抢占模型。
- CPU0 独占全局 timer queue、timeout、timerfd 和网络后台 poll。CPU0 每次取本地 tick 与
  全局最早 deadline 的较小值；AP 只推进本地 quantum，绝不重复执行全局 callback。
- AP 插入更早全局 timer 时，在释放 timer queue 锁后发送独立
  `TIMER_REPROGRAM` IPI。handler 只 Release/Acquire 发布原子请求，CPU0 在安全点重新读取
  queue。没有复用 `RESCHEDULE`，因为纯 deadline 变化不等于当前任务需要切换。
- `timer_cpu_init()` 成为 CPU0/AP 共用入口：先发布本 CPU deadline、编程 one-shot timer，
  最后开放 timer interrupt。RISC-V 不再在 `machine_init()` 提前开 timer；LoongArch 开
  timer line 时保留 ECFG 中已经开放的 IPI line，并把 InitVal 对齐到硬件要求的 4 倍数。
- LA64 `CLOCK_FREQ` 从 `static mut` 改为 AtomicUsize。CPU0 在 `machine_init()` 中 Release
  发布，AP 在 scheduler-ready 后 Acquire 读取；这样多核 timer 初始化不再依赖“运行期没人
  再写”的外部 unsafe 约定。
- AP 仍可用 relaxed atomic 累加调度/timer 性能计数，但 `print_snapshot` 会继续读取 FS/net
  全局状态并打印 console。人工沿 deferred 调用链发现这一隐藏入口后，把格式化快照以及
  timer/scheduler 周期触发限制到 CPU0，避免 AP timer 绕过共享子系统门禁。
- focused 新增第 8 项 `smp::user_timer_preempts_on_secondary_cpu`。它让 CPU1 先运行不含
  syscall、yield 和数据 load/store 的用户死循环，再把 kernel helper 排在同 CPU 后面；helper 只有
  在本地 timer 真正切出用户任务后才能执行，并验证该任务已经成为 `Queued(1)`。清理路径只在
  超时失败后才发送 RESCHEDULE，不能制造成功假象。

**官方规范依据：**

- RISC-V 依据 SBI TIME extension：`set_timer` 接受绝对 `stime_value`，未来 deadline 会清除
  pending timer interrupt，因此代码保存绝对 deadline 并在开放中断源前先完成编程。
- LoongArch 依据官方 Vol.1：stable counter 是每核稳定计数源，timer 使用本地 line 11；
  `TCFG.InitVal` 低两位固定为零，`TICLR` 写 1 清除。由此采用 per-CPU one-shot、4 倍数
  InitVal 和“先编程后开放”顺序。

**DeepSeek 协作与人工裁决：**

- 设计审查运行时 Codex 仍在编辑 tracked diff，包装器按协议 fail-closed；其 stdout 只作为
  线索，不计作冻结审查通过。
- DeepSeek 提醒同时检查 timer/reprogram 两种 pending、隔离 AP 全局 callback、覆盖 AP idle
  deferred 三个风险，均经人工源码复核后纳入。其复用 `RESCHEDULE` bit 的建议被拒绝，以免
  timer 设备请求无故触发任务切换。
- 首轮六个验证 child 都在编译期暴露 `hal::arch` 中间层漏重导出
  `enable_local_timer_interrupt`；没有 QEMU 运行，按 RED 处理。补齐双架构两条 re-export 后
  重新冻结，后续构建、focused 和初赛任务均保持源码指纹不变。
- `CLOCK_FREQ` 原子化后，DeepSeek 按风险缩小为双架构 build + LA64 8 核 focused，三项均
  PASS，并确认只有 CPU0 一个运行期前写者。其“可以直接提交”建议未被机械采纳；人工继续
  审计并发现 AP 性能快照路径，完成 CPU0 限制后才进入最终冻结复检。

**验证：**

- 被测 HEAD：`e5e115d1fc0c4cf338d86e91b01a3bd3f2db039e`；生产源码 diff SHA-256：
  `3d3670bfc12e1702d0256dd9d12c23666a9a74ea40fc257901e36b10af2431e6`。
- Docker 内串行 LA64/RV64 `CORE_NUM=8` kernel build，分别用时约 130.9/131.4 秒，exit 0。
- Docker 内 RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 分别用时 136.2/135.8 秒，
  均为 25/25；第 8 项真实 AP timer 抢占与第 25 项 STOP 均通过，online mask 为 `0xff`。
- 初赛 `mask=0x003`：RV64 312/314，仅两套 busybox `kill 10`；LA64 308/314，仅两套
  basic `test_brk` 各 1/3 与两套 busybox `kill 10`。fork/clone/exec、sleep/times/gettimeofday
  无新增失败；无 panic、timeout、forbidden marker 或源码 mutation。
- 人工最后收口 LA64 原子频率与 CPU0-only 性能快照后，精确冻结任务
  `smp-b39-final-freeze` 又串行通过 RV64/LA64 8 核 build（128.301/132.396 秒）和 LA64
  8 核 focused（135.687 秒、25/25）。三项 before/after 完整 tracked diff 指纹均为
  `eec8bfde6f0b626296b7002bb83eb6b079b7f12e597e77d248c16d7e43bafbd6`，无源码 mutation。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b39-percpu-timer-summary.md`。

**经验与文档同步：** “本地调度时钟”与“全局 timer callback”必须分开建模：前者是每 CPU
抢占进度，后者必须有唯一 owner；硬中断只发布请求，锁与调度留在既有安全点。该规则已经同步
到启动/trap、HAL、调度器、锁序、测试、SMP 计划、根 `AGENTS.md` 和 AI 使用报告。它属于
MangoCore 当前调度架构的项目约束，未另行扩张通用 debugging reference。

**下一步建议：** B40 先实现跨 CPU `exit_group` 的 sibling stop/ack 闭环，再复用到多线程
`exec` 与致命信号。默认全核调度一旦开放，远端 sibling 可能仍在使用共享 MM/PCB；若没有
“请求停止—owner 安全点退出—全部 ack—最后释放共享资源”的顺序，timer 与 affinity 已就绪
也仍可能发生 use-after-free。该节点属于 task 核心职责，可与队友负责的 FS/net/driver
并发安全审计并行推进。

### B40 跨 CPU 线程组退出与 clone 发布门禁

**状态：`pass`；已提交为 `f1797a85`（`smp: make thread-group exit owner-safe`）。**
旧 `exit_group_and_run_next()` 会由发起
CPU 直接移除并清理其他 sibling，即使目标仍是 `Running(remote)`；这可能释放远端 CPU 正在
使用的内核栈、用户映射和 TCB。B40 改成“发起者只发布停止请求，owner CPU 在安全点自清理，
最后一个清理完成者收尾进程”的分布式退出协议。

**实现与原因：**

- PCB 新增私有 `ThreadGroupState`，用同一把 `thread_group` 锁串行化成员发布和永久退出
  gate。`group_exit_code: AtomicU64` 以 `u32 code + 1` 编码，零值表示未退出；安全点可以
  Acquire 读取而不进入组锁。
- `publish_thread()` 在组锁内完成“检查 gate、登记 Weak 成员、领取 live token、执行一次
  `New -> Queued(cpu)`”。`try_publish_task[_on]()` 在 gate 已关闭时返回 `EAGAIN`，clone
  syscall 走未发布子任务的既有回滚路径。由此不存在“退出快照完成后又出现未被停止的新线程”。
- 第一个 group-exit 调用者在组锁内 Release 发布退出码并取得成员快照，随后在锁外给 sibling
  排队不可忽略的 `SIGKILL`：Blocked/Blocking 被唤醒，Queued/Running owner 收到
  `RESCHEDULE`，Migrating 使用在线 CPU 广播兜底。发起者不替远端线程释放任何资源。
- `run_task_safe_point()` 优先检查永久退出码。所有真实清理统一收敛到
  `finish_current_exit()`：先保存 IRQ 状态，再在受控开中断窗口完成用户清理与 TLB 等待，
  最后回到关中断的调度入口；因此从 trap-return 的 IRQ-off 安全点退出也仍能响应 shootdown。
- `sleep_interruptible()` 在完成 `Blocking` 登记并释放 `TASK_MANAGER` 后复查 group-exit；
  若停止请求刚好穿过 `Running -> Blocking` 窗口，就由阻塞线程自唤醒，避免永久睡眠。
- `remove_thread()` 的 live token 改到 clear-child-tid、futex、用户资源回收和 TLB commit
  之后，以 AcqRel 发布。只有返回 `Some(0)` 的最后清理者能够执行进程级收尾。
- focused 新增 `smp::group_exit_stops_remote_sibling`：CPU0 发起真实 group-exit，CPU1 上的
  Running sibling 与稳定 Blocked sibling 都在 owner 安全点成为 Zombie；退出 gate 关闭后
  的 late sibling 精确返回 `EAGAIN`、保持 New 且从未运行，最终 live count 为零、进程 Zombie。

**官方设计核对：**

- Linux `do_group_exit()`/signal group-exit 路径使用共享退出状态通知线程组，而每个线程仍从
  自己的 `do_exit()` 清理；`de_thread()` 另有等待其他线程退出的同步语义。B40 因而不把
  永久退出和多线程 exec 混成一个协议。
- DragonOS 的 `ProcessManager::exit()` 同样让当前执行流完成自身退出与调度切换。MangoCore
  保留现有 TCB/PCB 模型，只迁移“不能由发起 CPU 远程析构运行中线程”这一所有权原则。

**DeepSeek 协作与人工裁决：**

- 初轮只读审查与双架构 build/focused 均通过，用于确认锁序、late-clone 门禁和 owner 自清理
  主体设计。
- 首次最终冻结任务启动后，人工审查发现 `bool::then_some(encoded - 1)` 会先求值参数：
  `encoded == 0` 时 debug 构建下溢 panic、release 下静默回绕。立即终止该过期任务，改为显式
  `if encoded == 0` 分支后重新冻结；失败任务不计作验证证据。
- DeepSeek 最终报告把稳定 Blocked 用例扩大解释成“确定性覆盖 Blocking 登记瞬态”。人工没有
  接受该表述：动态测试直接覆盖 Running、稳定 Blocked 和 late clone；精确的
  Running→Blocking 瞬态目前依靠 manager 锁与退出状态复查的源码时序证明。

**验证：**

- 被测 HEAD：`4b679747afbdd53a7c9c8450bf3bb8e6ca19adba`；冻结生产 diff SHA-256：
  `4e9da109b605d8eafd84e88cdc38a2a747a566e10a8e2a9b5f09c31baf453e8f`；六个 child
  前后指纹一致、无 mutation。
- Docker 内串行 RV64/LA64 `CORE_NUM=8` kernel build 均 exit 0，约 138/135 秒。
- Docker 内 RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 26/26；新第 25 项和终端
  STOP 第 26 项明确 PASS，online mask 均为 `0xff`。
- 初赛 `mask=0x003`：RV64 312/314，仅两套 busybox `kill 10`；LA64 308/314，仅两套
  `test_brk` 各 1/3 与两套 busybox `kill 10`，与已接受基线完全一致。无 panic、timeout 或
  forbidden marker。
- 冻结验证之后只补充了少量源码注释和本文档/证据，没有改变可执行代码，因此没有机械重跑
  同一六项矩阵。

完整证据见
`docs/Work_Log/evidence/2026-07-31/smp-b40-group-exit-summary.md`。

**已知边界与下一步：** B40 解决永久 `exit_group` 和致命信号最终退出所需的 stop/owner-cleanup
闭环，但没有宣称完成多线程 `exec`。B41 应实现“调用者保留、其余 sibling 停止并完成 ack”
的临时会合协议，等待全部 sibling 不再使用旧 MM 后才能替换地址空间；同时补一个可控 barrier
测试，确定性命中 `Running -> Blocking` 交界，而不是仅依靠稳定 Blocked 场景。

### B41 多线程 exec 临时停止、live ack 与 MM 替换门禁

**状态：`pass`；已提交为 `59548ccd`。** 旧 exec 会由发起 CPU
直接从 runqueue 摘除 sibling 并调用其线程资源清理；对远端 `Running(cpu)` 任务而言，
这会在 owner 尚未切离旧 MM/内核栈时释放资源。B41 改成临时会合协议：exec owner 只发
停止请求，sibling 在自己的 CPU 安全点完成清理并发布 live ack，owner 收齐后才替换映像。

**涉及文件：**

- `os/src/task/process.rs`
- `os/src/task/task.rs`
- `os/src/task/manager.rs`
- `os/src/task/mod.rs`
- `os/src/task/process_manager.rs`
- `os/src/task/completion.rs`
- `os/src/task/run_queue.rs`
- `os/src/syscall/process/clone.rs`
- `os/src/kernel_tests/smp.rs`
- 进程、调度、trap、锁序、SMP 计划、AI 使用报告与本日志对应文档

**实现与原因：**

- `ThreadGroupState` 增加唯一 `ExecState`，对外由 `ExecSession` 管理。`begin_exec()` 在
  线程组锁内同时拒绝永久 group exit、并发 exec 和 late `CLONE_THREAD` 首次发布，
  再形成 live sibling 快照；`exec_owner_tid` 只是安全点快速读取的 Release/Acquire
  快照，不取代锁内权威会话。
- `request_sibling_exit()` 统一服务永久 group exit 和临时 exec：投递私有 SIGKILL，
  唤醒 Blocking/Blocked，向 Queued/Running owner 发送 RESCHEDULE。它不摘远端
  runqueue，不调用远端 TCB 的资源清理。
- `run_task_safe_point()` 先处理永久 group-exit 码，再处理“当前 tid 是 active exec
  sibling”。这个优先级保证 group exit 覆盖临时 exec，并使用统一进程退出码；
  exec sibling 则只以线程级 SIGKILL 退出，不把整个进程错误转成永久退出。
- `remove_thread()` 仍位于 clear-child-tid、robust/futex、用户 PTE 撤销和
  `TlbFlush::execute()` 之后。live count 降为 1 时，它在线程组锁内克隆 Completion，
  解锁后 `complete()`；因此 owner 被唤醒时，所有同 PCB sibling 已不再使用旧用户映像。
  开始快照为空或 VM `Arc` 引用计数都不能替代这项权威 ack。
- 两条 ELF loader 共享 `install_exec_image()`，避免两份停止/提交顺序漂移。新
  AddressSpace、栈、auxv 和 trap context 先完整构造；会合完成后才更新当前 TCB、
  CLOEXEC、exe、VM、sighand 和 futex，最后 `ExecSession::finish()` 重新开门。
  删除门禁前按 `Arc::strong_count()` 提前 `recycle_data_pages()` 的旧优化，因为同 PCB
  线程不各自长期持有 VM Arc，无法用引用计数排除 late clone。
- 如果旧 VM 仍被另一个 PCB 共享（如 `CLONE_VM | CLONE_VFORK`），提交时只撤销当前
  PCB 在旧 VM 中的 user-resource 槽，再替换本 PCB 的 VM；其它 PCB 的 Arc 继续保持
  旧地址空间存活。
- WaitQueue/Completion 的普通等待继续忽略用户信号，但会识别 group exit/exec
  生命周期请求，先摘除 waiter 再返回 `Interrupted`。vfork child 已经 publish 后，
  父线程使用 `CloneScheduleOutcome::StopCaller` 释放 syscall 栈上的 parent/child Arc，
  再进入统一安全点，不能误走 unpublished cleanup。
- 删除 `run_queue::remove()`、`remove_tasks_from_queues()` 和 TaskManager 批量摘除辅助，
  从生产 API 上移除“请求 CPU 远程夺取 owner 后代清资源”的旧做法。
- `finish_current_exit()` 额外修正一个独立竞态：线程级清理前读到的 group-exit 码
  不是最终线性化点，另一个 live sibling 仍可能随后发布永久退出。只有 live token
  归零后再次 Acquire 读取，才决定 wait 可见的进程退出码。

**官方/参考核对：**

- Linux v6.6 `fs/exec.c::de_thread()` 在替换映像前等待线程组收缩，并把永久 group
  exit 与 exec 的“保留调用者”语义分开；MangoCore 保留自身 TCB/PCB 模型，只迁移
  owner 自清理、关门和等待完成这三个必要不变量。
- Linux v6.6 `kernel/fork.c` 的发布边界说明 late thread creation 必须与 exec/退出
  串行化。MangoCore 使用现有 `thread_group -> 单个 RunQueue` 临界区完成锁内最终复核。
- DragonOS 当前 exec 路径仍以当前任务更新映像；本次只借鉴其“不由其它 CPU 析构当前
  执行流”的所有权原则，没有照搬数据结构。

**DeepSeek 协作与人工裁决：**

- 设计审查确认旧远端 cleanup、提前回收 VM、Blocking 漏唤醒和 late publication 均为
  实质风险，并建议让所有等待原语响应生命周期停止；GPT/Codex 逐条沿生产调用链复核后纳入。
- 首个最终验证任务 `smp-b41-validation-r8` 的前五个冻结 child 全部有效，但 DeepSeek
  在读取日志时误把第六个允许槽再次用于 `rv64-preliminary`，没有运行 LA64 preliminary。
  包装器按“执行数少于要求”正确 fail-closed；重复任务被人工终止，不把总任务状态伪报为 PASS。
- 另起只允许一次 `la64-preliminary` 的 `smp-b41-la-preliminary-r9` 补齐缺项并成功。
  DeepSeek 文本中“`exec.wait()` 不存在”和“可跳过 LA 回归”的说法与源码/门禁冲突，
  均未采纳；最终结论由 child manifest、原始 TAP/judge 和源码指纹决定。

**验证：**

- 被测 HEAD：`f1797a85e5b91e5f80ba1d8aa005e183e1936f02`；生产源码 diff SHA-256：
  `dff2949af9e355cc1c5382f869e26068d014307e28de1872dc2507a457949d55`。
- Docker 镜像 `zhouzhouyi/os-contest:20260510`，image ID
  `sha256:60e9bfa0ecdc6be93d9beb6b1d249f34163b08e32e97f090590a93a92e9357ac`，
  repo digest `sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`；
  双架构 QEMU 均为 10.0.2。
- Docker 内 RV64/LA64 `CORE_NUM=8` kernel build 均 exit 0，分别用时
  129.485/133.289 秒。
- RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 27/27，分别用时
  138.659/133.912 秒；第 25 项 group exit、第 26 项
  `exec_stops_remote_sibling` 和第 27 项 STOP 均通过，online mask 为 `0xff`。
- 初赛 `CORE_NUM=8 mask=0x003`：RV64 用时 343.210 秒、raw 312/314，仅两套
  busybox `kill 10`；LA64 用时 373.459 秒、raw 308/314，仅两套 `test_brk`
  各 1/3 与两套 busybox `kill 10`。clone/fork/exec/exit/wait/waitpid 均满分；
  无 panic、timeout、forbidden marker。
- 六个有效 child 的 HEAD、tracked diff 和工作树状态 before/after 一致，
  `mutation_detected=false`。
- 冻结验证后只删除了 `load_elf()` 上方一条与新实现矛盾的旧优化注释；最终生产源码
  diff SHA-256 为
  `7d66fa128f57f723bb4ea4c24dd1a5e80689c11b91ac2d0509e16599daf101f3`。
  该变更不改变 Rust token 或可执行逻辑，按注释级 T0 不重跑上述矩阵。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b41-exec-summary.md`。

**已知边界与下一步：** B41 已解决“旧 MM 在 sibling 仍执行时被替换”的 SMP 安全问题，
但尚未实现 Linux 非 leader exec 后把调用线程身份接管为 leader（`gettid()==getpid()`）的
完整语义，也未系统重做 `CLONE_FILES/CLONE_SIGHAND` 等跨 PCB 共享资源的 exec unshare
边界。下一 task 节点应先补真实用户态非 leader 多线程 exec 与身份/资源语义，再考虑把普通
任务默认 affinity 放宽到全核。

### B42 exec 跨 PCB 共享资源隔离

**状态：`pass`；随本节点提交。** B41 已保证同 PCB sibling 全部退出，但 fd table、
sighand 和 private futex table 还可能通过 `CLONE_FILES`、`CLONE_SIGHAND` 或共享 VM
被其它 PCB 持有。旧代码直接在共享对象上 close/reset/clear，会让另一个进程无故丢失 fd、
信号动作或 futex waiter。

**涉及文件：**

- `os/src/task/process.rs`
- `os/src/task/task.rs`
- `os/src/task/signal/action.rs`
- `os/src/kernel_tests/smp.rs`
- exec、PCB、调度协作、syscall、锁序与 SMP 计划文档

**实现与原因：**

- `ProcessControlBlock::reset_exec_resources()` 成为 exec 资源提交的唯一入口，并断言
  live thread 已收缩为 1。它在复制临时 `Arc` 之前读取 fd table/sighand 的引用计数：
  唯一对象保留原地快路径，共享对象才复制，避免辅助函数自身的 clone 把所有执行都误判为共享。
- 当前 PCB 最终持有的 fd table 才执行 CLOEXEC/fcntl lock 清理；sighand 同样只修改
  当前对象，且 `reset_for_exec()` 只清用户 handler、保留 POSIX 要求跨 exec 继承的
  显式 `SIG_IGN`。
- private futex key 绑定旧地址空间，因此不复用也不清空旧表，而是无条件安装新的空表。
  被替换的旧表先移出 `ProcessInner`，释放 `process.inner` 后才析构，避免 WaitQueue
  释放任务引用时沿析构链回入进程锁。
- 两条 ELF loader 继续共用 `install_exec_image()`；资源隔离位于 sibling live ack 和
  永久 group-exit 复查之后、TCB/MM 最终提交之前。OOM 已越过可回滚点时结束 exec 会话并
  让当前进程走统一退出，不尝试恢复已停止的 sibling。
- focused 用例持有三种旧对象的外部 `Arc`，再走生产重置入口，验证新旧对象分离、旧 fd
  仍存在而当前副本的 CLOEXEC fd 已关闭、旧 signal action 未被清除。

**官方核对与 DeepSeek 协作：**

- Linux v6.6 `begin_new_exec()` 在 de-thread 后执行 `unshare_files()`、
  `unshare_sighand()` 和 close-on-exec；内核注释明确说明 sighand 必须先解除与其它
  进程的共享。MangoCore 沿用现有 PCB/Arc 模型实现同一隔离语义，没有引入第二套资源容器。
- DeepSeek 的首轮冻结审查发现最初实现把 `Arc` clone 和 `strong_count()` 写在同一
  tuple 中，Rust 从左到右求值会让计数永远大于 1。该版本未被接受；最终代码先读共享状态、
  后 clone，并额外把旧 futex 析构移到锁外。最终六项验收均基于修正后的冻结指纹。

**验证：**

- 被测 HEAD：`59548ccd5f0734388f6b1419dd2eda5b9f4e645a`；tracked diff SHA-256：
  `d8a35abd316d307901acce6febee716028c68d4718188e42cbe30fd386ce22ae`。
- Docker 内严格串行 RV64/LA64 8 核 build，分别约 132.19/133.73 秒，exit 0。
- RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 28/28，分别约
  138.22/135.81 秒；新增 `exec_does_not_mutate_shared_resources` 双架构通过。
- 初赛 `CORE_NUM=8 mask=0x003`：RV64 312/314、LA64 308/314；精确失败集合与 B41
  一致，分别仅含两套 busybox `kill 10`，以及 LA64 两套 basic `test_brk` 各 1/3。
- 六个 child 的 HEAD、tracked diff 和工作树状态 before/after 一致，
  `mutation_detected=false`；无新增 warning、panic、timeout 或 forbidden marker。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b42-exec-resource-summary.md`。

**已知边界与下一步：** B42 解决资源隔离，不改变线程身份。B43 应让非 leader exec
调用者接管 PCB 的 leader TID，使 `gettid()==getpid()`，并同步 task registry、
Per-CPU current hint、OOM tracker 与迟到 TCB 析构；实现应交换既有 ID handle，不增加
第二个“有效 TID”字段或独立身份状态机。

### B43 非 leader exec 的 PID/TID 身份接管

**状态：`pass`；随本节点提交。** B41/B42 已保证 exec owner 独占线程组并安全隔离共享
资源，但非 leader owner 仍保留原线程 TID，成功后不满足 Linux 的
`gettid() == getpid()`。B43 在不增加 leader 状态机的前提下交换既有 TID handle。

**涉及文件：**

- `os/src/task/{task,process,registry,processor,manager}.rs`
- 直接读取旧公开 `tid`/`exit_signal` 的 syscall、signal、console 调用点
- `os/src/kernel_tests/smp.rs`
- exec、PCB、task/scheduler、syscall、锁序、测试和 SMP 计划文档

**实现与原因：**

- TCB 用原子 `tid` 保存高频数值真值，用受锁 `tid_handle` 保存 allocator 所有权。
  普通生命周期仍不可变，只有 registry 中的 exec 事务可以交换；PCB 删除容易漂移的
  `leader_tid` 副本，只保留稳定 PID 和 `pid_handle`。
- `exchange_exec_tids()` 在 task registry 锁内确认 owner 的旧 TID 映射、确认旧 leader
  属于同 PCB 且已 Zombie，再让 owner 接管 PID handle。旧 leader 尚存时接管 owner 的旧
  handle；已经析构时旧 handle 在锁外释放。
- registry 从 owner 旧 TID 重键为 PID 后才更新 Per-CPU current TID 和 OOM active
  tracker。`Drop` 改为“键仍指向自身才删除”，解决旧 leader 迟到析构误删新映射的 ABA
  风险。
- 成功 exec 统一把新 leader 的退出信号恢复为 `SIGCHLD`；非 leader owner 不再是额外线程，
  因此释放 thread quota。身份接管放在映像提交和 `ExecSession::finish()` 之后，不嵌套
  thread-group、registry、processor 和 task-manager 锁。
- 完整 ktest TCB 构造后立即登记 TID，线程组/runqueue 发布仍由独立入口完成。这样 late
  publish 测试和生产 clone 都遵守“完整 TCB 可按 TID 查找、New 状态尚无调度 owner”的
  同一边界。
- 新 focused 用例让 CPU0 非 leader owner 对 CPU1 旧 leader 执行真实多线程 exec 身份
  收尾，核对 PID/TID、registry、Per-CPU hint、退出信号、handle 交换、迟到 Drop 和最终
  TCB 回收。

**官方核对：**

- Linux v6.6 `fs/exec.c::de_thread()` 在其它线程退出后调用 `exchange_tids()`，让非
  leader 调用者接管旧 leader PID，并设置 `exit_signal = SIGCHLD`。MangoCore 沿用现有
  weak registry/TCB/PCB 结构实现相同语义，没有复制 Linux 的 tasklist 数据结构。

**验证：**

- 被测 HEAD：`18eae3a0e81c165a28dc6a0a7818ca0229424347`；focused 与 RV64 初赛
  冻结 tracked diff SHA-256：
  `851994375f58140f48d7eb37f593885aedeaef6c4e8456a30a44fd464cd08c16`。
- Docker 内串行 RV64/LA64 8 核 build 均 exit 0；随后双架构
  `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 29/29，分别用时 138.946/135.849 秒。
- 初赛 `mask=0x003`：RV64 312/314（343.614 秒），仅两套 busybox `kill 10`；
  LA64 judge 为 308/314（353.764 秒），仅两套 `test_brk` 各 1/3 与两套 busybox
  `kill 10`。LA64 wrapper 因测试期间并行补写 Markdown 检出 tracked diff 改变而标记
  `mutation_detected`，不是测试失败；原始进程 exit 0、marker 完整、可执行源码未变，
  因此不为纯文档指纹重复同一长测。
- 最终 focused runner 无 mutation、panic、timeout 或 forbidden marker。验证后只修正
  一条已经过时的 ktest 构造注释并同步文档，没有改变可执行逻辑。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b43-exec-identity-summary.md`。

**已知边界与下一步：** B43 完成 exec 身份语义，但普通用户任务仍不能因这一节点默认全核
开放。B44 应把 `membarrier` 的 PRIVATE_EXPEDITED 注册状态下沉到 AddressSpace，并实现
独立的 memory-barrier IPI/ack；它可以复用等待 IRQ guard，但不能伪装成 TLB generation
或把注册状态继续放在线程 TCB。

### B44 跨 CPU membarrier

**状态：`pass`；随本节点提交。** 原实现只在 TCB 上记录兼容注册，并对 GLOBAL/PRIVATE
直接返回成功，无法兑现多核用户同步算法要求的内存序。

**涉及文件：**

- `os/src/mm/{address_space,tlb}.rs`
- `os/src/smp.rs`
- `os/src/task/task.rs`
- `os/src/syscall/process/mm.rs`
- `os/src/kernel_tests/smp.rs`
- MM、syscall、锁序、测试和 SMP 计划文档

**实现与原因：**

- PRIVATE_EXPEDITED 注册状态下沉到 `AddressSpace`。同 MM 线程自然共享，fork/exec
  的新 MM 不继承；不再给每个 TCB 维护一份可能互相矛盾的副本。
- PRIVATE 在 VM 锁内冻结历史 cached CPU mask，解锁后才进入
  `synchronize_memory()`。与快照并发首次激活的 CPU 在同一 VM 锁后执行 full fence，
  因而“先激活”和“先快照”两个次序都不会漏过 barrier。
- 每 CPU 增加独立 memory-barrier request/ack 和 `MEMORY_BARRIER` reason。
  发送侧在用户访问两端执行 full fence，远端 handler 只执行原子操作和 full fence；
  多个发送者通过单调序号安全合并，不把 membarrier 混入 TLB generation。
- TLB 等待 guard 重命名为通用 `IpiWaitIrqGuard`，同时服务既有 shootdown 与
  membarrier。等待期间可响应反向 IPI，但不持 VM、task.inner、runqueue 或普通锁。
- 新 ktest 从 syscall 分发入口验证 QUERY、未注册 EPERM、幂等注册、CPU1 共享 MM
  激活、PRIVATE 精确 request/ack 和 GLOBAL 全 AP request/ack，没有增加测试专用
  生产状态机。

**官方核对与 DeepSeek 协作：**

- 对照 Linux v6.6 `kernel/sched/membarrier.c`：注册归 MM、发送前后 full barrier、
  目标侧 `ipi_mb()` 和 PRIVATE 的 MM CPU 选择语义一致；MangoCore 使用现有历史 mask
  与固定 per-CPU mailbox，没有复制 Linux rq/cpumask 数据结构。
- DeepSeek 冻结只读审查确认生命周期、两种快照竞态、reason 合并与 STOP 锁序；
  采纳函数级竞态注释和精确 request 增量断言。其把 GLOBAL 位误认成 QUERY 的建议与
  Linux UAPI 定义冲突，未采纳。

**验证：**

- 被测 HEAD：`9cc10dc7005390aa8b24c8d912f8cacc3b66d9ec`；最终可执行源码 diff
  SHA-256：`a1af1e31042d8055a8c040c3fb1ef835351334f2b4211237eaac46f45335b051`。
- Docker 内串行 RV64/LA64 8 核 normal build 均 exit 0，分别用时
  130.326/131.385 秒。
- 精确最终 diff 上，RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 30/30，
  分别用时 137.168/136.312 秒；RV64 `KREPEAT=2` 为 59/59。
- 初赛 `CORE_NUM=8 mask=0x003`：RV64 312/314（348.201 秒），LA64
  308/314（357.023 秒），失败身份与 B43 基线完全一致。
- 四项最终任务均 `mutation_detected=false`，无 panic、timeout 或 forbidden marker。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b44-membarrier-summary.md`。

**已知边界与下一步：** 历史 CPU mask 仍会产生保守的额外 PRIVATE IPI；没有 CPU
hotplug 时不影响正确性。B45 应收紧 trap context 的 Rust 借用边界，删除会让
`&'static mut TrapContext` 逃出临时 TCB guard 的 `current_trap_cx()`，但不为此新增
wrapper、状态机或跨用户复制持锁。

### B45 trap context 借用边界

**状态：`pass`；随本节点提交。** 原 `get_trap_cx()` 从 `&self` 返回
`&'static mut TrapContext`，`current_trap_cx()` 又从临时 inner guard 直接返回该引用。
锁释放后调用方仍可保存和使用可变引用，Rust 类型系统无法再表达 trap frame 的唯一写者。

**涉及文件：**

- `os/src/task/{task,processor,mod,signal/mod}.rs`
- `os/src/hal/arch/{riscv,loongarch64}/trap/mod.rs`
- `os/src/syscall/process/signal.rs`
- `os/src/kernel_tests/smp.rs`
- trap、TCB、clone、signal、锁序、测试和 SMP 计划文档

**实现与原因：**

- 唯一入口改为 `trap_context_mut(&mut self) -> &mut TrapContext`。引用生命周期现在绑定
  `task.inner` guard；删除会越过临时 guard 的 `current_trap_cx()`，不新增 wrapper 或
  第二套所有权状态。
- init 和 clone 使用显式短作用域初始化 trap frame。父上下文先按值复制并释放
  `task.inner`，再进入 fork PTE/CoW 与 TLB ack；child 初始化 guard 在 SHM 克隆和
  registry 工作前结束。
- LA64 未对齐模拟只在锁内快照 PC 和 store 源寄存器，逐字节用户访存在锁外进行，
  最后重新加锁校验 PC 并提交 load 结果。这样缺页/shootdown 不跨普通锁，也不会用整份
  `TrapContext` 写回覆盖无关字段。
- trap return 最终交给汇编的裸地址仍是明确 owner 边界：Rust guard 已释放，当前任务
  仍由本 CPU current 槽独占，汇编立即恢复用户现场。`sys_sigreturn()` 仍跨用户 frame
  读取持锁，明确留给下一节点，不把 B45 的借用修复夸大为完整信号锁序修复。

**DeepSeek 协作与人工裁决：**

- 冻结只读设计审查确认原 `'static mut` 不健全，支持生命周期绑定和删除 current
  helper。其建议整份 trap context snapshot/commit 未采纳，因为会扩大覆盖面；只按值
  快照实际需要的字段。
- DeepSeek 串行执行双架构 focused 与初赛四项冻结任务并汇总。GPT/Codex 独立核对每个
  child 的 TAP/judge、失败集合、退出码和 before/after 指纹，没有把模型自报 PASS 当作
  唯一证据。

**验证：**

- 被测 HEAD：`2c1e4689bfc8af96500ef7698bd93c3a31fca83e`；最终可执行源码 diff
  SHA-256：`b7ee48be016f5be19725e7eba5be807ea4bb5b2b3e17bd0b5f24935d6a477f6d`。
- 首次 RV64 build 因新借用规则暴露两个 `E0505` 和一个 `E0596`；显式收短 guard
  后，Docker 内串行 RV64/LA64 8 核 normal build 均 exit 0，分别用时
  130.497/130.621 秒。
- RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 均为 30/30，分别用时
  136.346/133.942 秒；初赛分别为 RV64 312/314（349.772 秒）、LA64
  308/314（355.234 秒），失败身份与 B44 基线完全一致。
- 四个最终 QEMU child 均无源码 mutation、panic、timeout 或 forbidden marker。
  现有矩阵不保证触发 LA64 未对齐异常，因此未把该路径描述为动态覆盖。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b45-trap-context-borrow-summary.md`。

**已知边界与下一步：** B46 应让 `sys_sigreturn()` 先在锁内快照用户 SP，锁外读取
sigmask/machine context/LSX，最后一次短锁原子安装恢复态；同时用架构 `TrapContext`
方法替代 signal 保存/恢复中的布局强转。不要在同一节点顺手重写整个 `do_signal()`，
其 action/pending/SA_RESETHAND 线性化需要单独设计。
