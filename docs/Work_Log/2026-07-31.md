# 2026-07-31 Work Log

## 2026-07-31

### B37 affinity-aware 新任务初始放置

**状态：`pass`；默认 CPU0-only mask 保持不变。** `publish_task()` 不再无条件把普通
新任务投到 CPU0，而是按任务继承的 `cpus_allowed`、当前可调度 CPU 和近似负载选择首次
owner。这样父线程已经收紧到非 CPU0 mask 后执行 clone/fork，子任务不会在首次发布时触发
“CPU0 不在允许集合”断言。远程 Running/Blocking affinity、默认全核 mask 和 steal 仍未实现。

**实现与原因：**

- `run_queue::select_runnable_cpu()` 统一过滤 affinity、online、scheduler-ready 和 STOP，
  preferred CPU 的负载不超过最小值 `+1` 时保留 locality，否则选择
  `nr_running + current_present` 最小且 ID 最小的 CPU。计数是无锁快照，只决定放置质量，
  owner 仍由 runqueue 临界区和 `sched_state` 提交。
- `CpuTaskState.current_present` 在 current 槽安装后 Release 置位，在 idle 栈真实取走 current
  后 Release 清零。此前的 `cpu_has_current()` 需要取得 processor 锁；用原子提示避免 Blocked
  wake 在持有 `TASK_MANAGER` 时新增 `TASK_MANAGER -> processor` 锁序。
- `publish_task()` 只有在本 CPU 没有 current 的启动窗口显式走 CPU0。init/ktest runner 的
  发布早于 BSP 进入 `run_tasks()`，此时 scheduler mask 尚无 bit0；把它交给通用过滤会制造
  空候选。普通 clone/fork 均有 current，走通用选择器并把调用 CPU 作为 locality 提示。
- `spawn_ktest_task_on()` 设置单 bit mask 后改走普通 `publish_task()`。现有 AP 定向测试因此
  动态覆盖生产选择器，不增加测试专用生产字段或临时 IPI reason。
- current 自迁移、Queued affinity 和 Blocked wake 复用同一个选择器；目标 owner 的最终合法性
  仍由各自已有的 runqueue/registry 协议复核，不把负载快照当作同步原语。

**DeepSeek 协作与人工裁决：**

- 首轮只读审查与 B38 预审均因 Codex 在其运行期间继续编辑 tracked diff，被本地包装器按
  “visible Git state changed” fail-closed；模型进程没有写源码。报告指出启动期 scheduler mask
  和潜在 processor 锁序，最终实现分别采用显式 bootstrap 分支与原子 current 提示。
- 首次验证又因误用旧容器 `lzm-cagent-run` 的 `/app` 挂载而在 gateway 首项失败；该结果没有
  进入通过证据。最终任务固定到 `mangocore-smp-integration-20260725-os-dev-1`，四项构建/focused
  与两项初赛均保持源码指纹不变。
- DeepSeek 建议当前默认 bit0 不会改变普通任务行为；人工补充边界是：已由
  `sched_setaffinity` 改成非 CPU0 mask 的父线程，其 clone 子任务现在会按继承 mask 合法发布，
  这正是 B37 要修复的行为，不应描述为“所有 clone 行为完全不变”。

**验证：**

- 被测 HEAD：`85526b978994f4ab31191290e7b914fec69e52a6`；功能 diff SHA-256：
  `af6397f9af924b2994a1cb59d8e7ce480b3cd9bb55d9c1f2e96ecf1a2bcd26f3`。
- RV64/LA64 `CORE_NUM=8` kernel build 分别用时 133.645/134.758 秒，exit 0。
- RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 分别用时 141.275/138.218 秒，均为
  23/23；第 2/11/13/14/23 项和最终 PASS 均存在，无 panic、非法 affinity、owner mismatch
  或 timeout。
- RV64 初赛 312/314，仅两套 busybox `kill 10`；LA64 初赛 308/314，仅两套
  `test_brk` 各 1/3 与两套 busybox `kill 10`。双架构 fork/clone 和其余 busybox 项无新增失败。
- 六个 child 均 exit 0、无 forbidden marker、无 mutation，源码前后指纹一致。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b37-affinity-placement-summary.md`。

**经验与文档同步：** 通用放置策略的负载值只能是提示，正确性必须由最终 owner 容器复核；
启动期 CPU0 是 scheduler mask 尚未发布时的显式例外。已同步调度器、锁/测试边界、SMP 计划、
根 `AGENTS.md` 和 AI 使用报告。

**下一步建议：** B38 为远程 Running/Blocking affinity 增加 owner 安全点请求与完成确认。
不得远程直接写一个排除当前 owner 的 mask 后立即返回；也不得把 current 借用 queued-only
`Migrating` 状态表示已经切离。

### B38 远程 Running/Blocking affinity owner 交接

**状态：`pass`；没有新增调度状态。** 正 TID 的 `sched_setaffinity()` 现在可以处理正在其它
CPU 运行或正进入阻塞窗口的线程。若新 mask 仍包含当前 owner，写侧在线性化点原地更新；若
排除 owner，则 syscall 必须等 owner 在现有任务安全点完成
`Running(source) -> Queued(target)` 后才返回，避免 mask 与 current owner 短暂不一致。

**实现与原因：**

- 每个 TCB 增加唯一 `remote_affinity_request` 槽；请求对象只保存不可变的 mask、target 和
  `Pending/Applied/Retry` 完成态。它是 owner 交接的会合对象，不是第二套任务状态机，也不改变
  `TaskStatus` 的调度所有权真值。
- 远程写侧先按 affinity/online/scheduler/STOP 规则选择目标并同步该任务的内核栈映射，再在
  请求槽锁内复核 `Running(owner)`、本地迁移请求与既有远程请求。锁外发送 RESCHEDULE，调用者
  用既有 `suspend_current_and_run_next()` 协作等待；因此不在持锁区等待 IPI、TLB ack 或调度。
- owner 已切回 idle 栈后，`finish_switch_out()` 持请求槽锁取走请求、发布新 mask，并通过单个
  目标 runqueue 完成状态/容器交接，最后以 Release CAS 标记 Applied。真实锁嵌套是
  `remote_affinity_request -> 单个 RunQueue`；它保证第二个写者不能插入检查与交接之间，且没有
  `RunQueue -> remote_affinity_request` 反向路径。
- `begin_interruptible_sleep()` 固定使用
  `TASK_MANAGER -> remote_affinity_request`，把 `Running -> Blocking`、registry 登记与旧请求取消
  放在同一临界区；远程调用者观察到 Retry 后再等待状态稳定，复用 Running 或 Blocked 协议。
  `mark_zombie()`、current 自迁移和显式 migration 同样通过请求槽取消旧请求，防止退出或本地
  写侧与远程写侧共同声明 owner。
- syscall 保留 Linux raw mask/TID/uaccess 语义；这里只替换非 current Running/Blocking 的
  `EOPNOTSUPP`，New/Zombie 或已离开可管理容器的对象仍返回不支持。
- focused 新增第 15 项 `smp::running_affinity_waits_for_owner_handoff`：先以 bit0|bit1 验证
  owner 合法时不迁移，再以 bit0 验证远程调用直到 CPU1 current 已释放才返回，任务最终恰好
  在 CPU0 恢复一次并成为 Zombie。STOP 顺延到第 24 项，证明没有遗留队列或请求污染。

**锁序与证据边界：**

- 当前新增关系为 `task.inner -> remote_affinity_request -> 单个 RunQueue` 与
  `TASK_MANAGER -> remote_affinity_request -> 单个 RunQueue`；实际路径不同时持有
  `task.inner` 和 `TASK_MANAGER`。请求完成只做原子 CAS，不唤醒 WaitQueue、不取得 manager。
- 动态用例覆盖一个远程请求者与稳定 Running owner，并覆盖 mask 包含/排除 owner 两条分支。
  两个请求者同时更新同一远程 TID、以及确定性命中 `Running -> Blocking` 瞬态，仅完成了槽锁、
  Retry 和锁序源码审计，尚未做竞争压力测试。
- 普通用户任务默认 mask 仍是 bit0；B38 完成写侧协议不等于已经开放默认全核调度。

**DeepSeek 协作与人工裁决：**

- 首轮冻结验证发现 `manager.rs` 通过父模块重导入私有请求类型导致编译失败；采用 sibling
  module 直接导入修正，没有把内部协议暴露成公共 API。
- 最终冻结任务保持源码指纹不变，完成双架构 build、8 核 focused 与初赛。DeepSeek 汇总中
  “取得 runqueue 前已释放请求槽”的描述与源码不符，且一处 TAP 数量统计不准确；人工按源代码
  和 raw TAP 修正为“持请求槽跨单个目标 runqueue 临界区”与 24/24，不传播模型误述。
- 曾误用 `cargo fmt -- <files>` 导致 formatter 扩散到整个 crate；所有无关机械格式化均已撤回，
  最终 tracked diff 只包含 B38 功能、测试与文档，不以格式噪声扩大审查面。

**验证：**

- 被测 HEAD：`4da0579a90f15dbf2a2a914b246e3c011d31ba00`；功能 diff SHA-256：
  `4875482b6e06f089eb1c3060a6c20259c902a66a6e44c19ca746c6b42c44b465`；测试前后源码一致。
- Docker 内严格串行 RV64/LA64 `CORE_NUM=8` kernel build，分别用时 135.158/137.229 秒，exit 0。
- Docker 内 RV64/LA64 `CORE_NUM=8 KTEST=smp KREPEAT=1` 分别用时 135.585/138.235 秒，均为
  24/24；新第 15 项、用户 affinity probe 和第 24 项 STOP 均明确 PASS，online mask 为 `0xff`。
- 初赛 `mask=0x003`：RV64 312/314，仅两套 busybox `kill 10`；LA64 308/314，仅两套 basic
  `test_brk` 各 1/3 与两套 busybox `kill 10`。无新增 fork/clone/exec 失败、panic、timeout、
  forbidden marker 或源码 mutation。

完整摘要见
`docs/Work_Log/evidence/2026-07-31/smp-b38-running-affinity-summary.md`。

**经验与文档同步：** 远程运行态属性更新需要“请求—owner 安全点提交—完成确认”，请求对象
只承载握手，不能成为第二份 owner 状态；所有报告必须准确写出请求槽跨目标 runqueue 的实际
锁嵌套。已同步调度器、锁序、测试门禁、SMP 计划、根 `AGENTS.md` 和 AI 使用报告。

**下一步建议：** B39 在共享子系统门禁允许后，把普通任务默认 affinity 从 bit0 放宽到
configured online mask，并用真实 fork/clone workload 验证多核分布；若门禁尚未满足，则先做
远程 affinity 多写者与 Blocking 交界压力用例，再进入默认放宽。
