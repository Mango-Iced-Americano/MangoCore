---
date: 2026-07-30
---

# Work Log — 2026-07-30

## RV64 regression ELF staging collision diagnosis

**涉及文件：**
- `os/src/hal/arch/riscv/config.rs`、`os/src/mm/{kernel_space,mod}.rs`、`os/src/task/task.rs` — 将 RV64 临时内核 ELF 映射窗口移出低地址 identity/MMIO 区间，并统一使用架构专用基址。

**验证：**
- Docker `make test ARCH=rv64 PROFILE=regression`：原 `MemoryError::AlreadyMapped` panic 已消失；首次用户态恢复后仍在 QEMU 120 秒门禁超时，未通过。
- GDB 确认首次任务到达 `__restore`，尚未形成可验收的运行时 PASS 证据。

**备注：** 临时调试日志已移除；后续需继续定位用户页表切换后的 trampoline 取指路径。

## VF2 实板 U-Boot FDT 只读复核

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-30/vf2-live-fdt-mmc-probe.md` — 归档已确认 `/dev/ttyUSB0` 的本次 live U-Boot 只读查询及完整输出。

**验证：**
- 已确认 `/dev/ttyUSB0` 为目标 VisionFive 2 串口；以 115200 打开后仅发送授权的回车、`version`、`fdt addr`、`fdt print /aliases`、`fdt print /soc/sdio0@16010000` 与 `fdt print /soc/sdio1@16020000`。
- U-Boot `2021.10` 输出 `StarFive #`；working FDT 为 `f76df9b0`；`mmc0`/`mmc1` 分别指向 `sdio0@16010000`/`sdio1@16020000`。
- 两个节点均为 enabled `snps,dw-mshc`，各自有 64 KiB `reg`、双 clock specifier、reset、assigned clock rate、FIFO depth 32，bus width 分别为 8/4。

**备注：** 未执行 reset、boot、TFTP、网络、存储写入、U-Boot 环境变更或 DTR/RTS ioctl。本次仅验证 U-Boot 当前工作 FDT；当前 `boot_rv_uboot_go` 仍将 `a1` 视为非 DTB，因而不能把此结果写成 MangoCore 内核 FDT runtime discovery 已上板通过。

## Boot ABI profile 化与静态设备目录移除

**涉及文件：**
- `os/Cargo.toml`、`os/build.rs`、`os/make/{rv64,la64}.mk`、`os/make/arch/{rv64,la64}-settings.mk` — 删除 `board_*` Cargo selector；由 Make 的 `BOARD` 输入映射为唯一且 target-compatible 的 boot ABI profile：`boot_rv_sbi_fdt`、`boot_rv_uboot_go`、`boot_la_qemu` 或 `boot_la_uboot_dmw`。
- `os/make/la64.mk` — 清除 2K1000 core-test/shell uImage 配方中仍会传递给 Cargo 的废弃 `board_core_test` 与 `board_shell` selector；保留 `sata_scratch_rw` 和 `gmac_2k1000` capability feature。
- `os/src/hal/platform/{mod,info}.rs`、`os/src/hal/device/manager.rs`、`os/src/hal/firmware/{mod,fdt}.rs` — 静态 boot contract 仅携带 handoff/cmdline，不再伪造硬件设备；可用 FDT 时从持久快照构建 `PlatformInfo`，driver 经 `DeviceManager` 按 compatible/kind 查询资源。
- `os/src/drivers/{block,net,rng}/` — RV64 VirtIO block/net/RNG 不再使用固定 MMIO 地址，改由 FDT 发现；非 FDT ABI 不发明 `PlatformInfo.devices`。
- `scripts/test-canonical-entrypoint-contract.sh` — dry-run 合同检查每个正式入口只选择一个 boot profile，且 kernel Cargo feature list 不允许 `board_`。
- `docs/Work_Log/evidence/2026-07-30/boot-profile-regression.md` — 本轮 Docker/QEMU 验收证据。

**验证：**
- `bash scripts/test-canonical-entrypoint-contract.sh`（Docker）✅ — 所有 RV64/LA64 normal/regression formal/ktest dry-run entrypoint 均选中唯一 boot profile，且没有 kernel Cargo `board_` feature。
- `make -C os -n la64-2k1000-core-tests` 与 `make -C os -n la64-2k1000-shell`（Docker）✅ — uImage recipe 的 kernel feature list 不含 `board_`。
- `make kernel ARCH=rv64 PROFILE=normal`（Docker）✅。
- `make kernel ARCH=la64 PROFILE=normal`（Docker；在 RV64 后串行）✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=platform,platform_resources,platform_fdt_snapshot KTEST_QEMU_TIMEOUT=60`（Docker）✅ — TAP `1..13`，13 passed、0 failed，运行时从 `RiscvFdt` 快照发现 VirtIO block `0x10001000`。
- `make test ARCH=rv64 PROFILE=regression`（Docker）✅ — TAP `1..23`，22 passed、0 failed、1 skipped（既有 `pipe_resize` known kernel bug），`[L4 REGRESSION RESULT: PASS]`。
- `git diff --check` ✅；Makefile `.mk` 无可用 LSP server，Rust LSP 仍未安装且此前已拒绝安装；`make lint` 本次未执行。

**备注：** `boot_rv_uboot_go` 保持不信任 `a1` 为 DTB 的 ABI 合同，VF2 `booti` handoff 和 DW-MMC 驱动仍为后续工作；LA64 的 PCI/DMW 启动行为是 boot ABI，不通过静态 `PlatformInfo.devices` 伪造设备。没有新增 AI 工具/模型，`references/` 无需更新：本次约束已由 `AGENTS.md` 覆盖。

## RV64 DTB pre-BSS 持久化快照修复

**根因：** 当前 RV64 QEMU ktest 环境下，OpenSBI 将 DTB 放置在物理地址 `0x82200000`，该地址恰好落在内核旧版 BSS 段覆盖范围内。`mem_clear()` 将 BSS 清零时，DTB 原始数据被擦除，导致 post-heap `build_platform_info()` 解析到全零 FDT 头部，回退到静态 fallback。

**设计：** 在 `rust_main()` 中 `bootstrap_init()` 之后、`populate_memory_regions()` 之内、`mem_clear()` 之前，`capture_fdt_snapshot()` 验证 DTB magic (`0xd00dfeed`) 和合法 `totalsize`，通过 `ptr::copy` 将完整 DTB 字节复制到 `#[link_section = ".data.boot"]` 标注的 `FDT_SNAPSHOT` 静态缓冲区。该缓冲区位于 `.data` 段（不受 BSS clear 影响），容量固定为 `MAX_FDT_SNAPSHOT_SIZE = 2 MiB`。`readelf` 验证：快照地址 `0x80944038`、大小 `0x200008`，位于 `.data` `[0x80467000, 0x80b45000)` 且早于 BSS 起始 `0x80b85000`。

**仅 `RiscvFdt` 协议：** `has_valid_dtb()` 检查 `BootProtocol::RiscvFdt` 以及 `dtb_paddr != 0` 和页面对齐；`UbootGo`、`LoongArchLegacy`、`Test` 不走快照路径，继续使用静态 fallback。`build_platform_info()` 从 `fdt_snapshot()` 返回的 `&[u8]` slice 构造 `fdt::Fdt`，不再从原始 firmware 物理地址读取。

**涉及文件：**
- `os/src/hal/firmware/mod.rs` — `FdtSnapshot` 结构体、`MAX_FDT_SNAPSHOT_SIZE`、`#[link_section = ".data.boot"] static mut FDT_SNAPSHOT`、`has_valid_dtb()` gate、`populate_memory_regions()` 中先捕获快照再解析 memory regions
- `os/src/hal/firmware/fdt.rs` — `read_totalsize()` 验证 DTB magic 和大小，`capture_fdt_snapshot()` 执行 `ptr::copy` 到全局快照；`fdt_snapshot()` 返回不可变 slice；`build_platform_info()` 改用快照而非原始 paddr；`parse_memory_regions()` 从快照重新解析 DTB memory 节点，原始 paddr 仅用于 `dtb_start`/`dtb_end` 物理保留区间计算
- `os/src/hal/platform/mod.rs` — `init_platform_info()` 现在调用零参数的 `build_platform_info()`，在 `mm::init()` 之后从持久化快照构造 PlatformInfo
- `os/src/kernel_tests/platform_fdt_snapshot.rs` — 强化既有 `test_captures_qemu_boot_fdt_raw_properties()`（`boot_rv_sbi_fdt` 条件编译），在 snapshot 实现后验证 QEMU RISC-V FDT handoff 下 `FirmwareKind::Fdt`、virtio-mmio raw property 正确保留

**验证（严格 RED→GREEN）：**
- RED：实现前，QEMU ktest `platform_fdt_snapshot::captures_qemu_boot_fdt_raw_properties` 预期失败（`RiscvFdt` 协议检测到但 DTB 已被 BSS clear 擦除 → `FirmwareKind::Static` → 断言失败）
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=platform_fdt_snapshot KTEST_QEMU_TIMEOUT=60`（Docker）✅ — TAP `1..4`，4 passed、0 failed，包含 QEMU 实机 FDT:
  ```
  TAP version 13
  1..4
  ok 1 platform_fdt_snapshot::preserves_live_vf2_mmc_node_shapes
  ok 2 platform_fdt_snapshot::rejects_absent_and_malformed_raw_resources
  ok 3 platform_fdt_snapshot::exact_compatible_rejects_malformed_raw_snapshot
  ok 4 platform_fdt_snapshot::captures_qemu_boot_fdt_raw_properties
  # results: 4 passed, 0 failed, 4 total
  [KTEST RESULT: PASS]
  ```
- `make kernel ARCH=rv64 PROFILE=normal`（Docker）✅
- `make kernel ARCH=la64 PROFILE=normal`（Docker；在 RV64 后串行）✅
- 快照范围验证：`readelf -sW` 确认 `FDT_SNAPSHOT`（地址 `0x80944038`，大小 `0x200008`，section index 3）位于 `sdata=0x80467000`（section 2）与 `edata=0x80b45000`（section 3）之间且早于 `sbss=0x80b85000`（section 4）；`rust_main` 中打印 `dtb_paddr=0x82200000` 确认 handoff 地址；捕获时机由调用顺序和源码保证
- Rust LSP 不可用（此前拒绝安装）；`make lint` 本次未执行

**备注：** 本次修复无新增 AI 工具/模型，已在 `AI-Usage-Report.md` 的工具清单覆盖范围内；`references/` 无需更新 — DTB 生命周期约束已由 `AGENTS.md` 的 HAL 部分覆盖。

## VF2 MMC 原始属性 fixture 完整性修正

**涉及文件：**
- `os/src/kernel_tests/platform_fdt_snapshot.rs` — 对比完整的两个 16-byte `clocks` specifier、全部已观测 raw 属性及第二个 clock specifier；增加 QEMU RISC-V FDT handoff 条件下的 parser-to-snapshot 断言。
- `os/src/kernel_tests/platform_fdt_fixture.rs` — 将完整 VF2 `sdio0`/`sdio1` 测试数据拆出，保留 `compatible`、`reg`、clock/reset/assigned/pinctrl/status/phandle 和零长度 `u-boot,dm-spl` 的原始字节。
- `os/src/kernel_tests/mod.rs` — 注册 fixture 子模块。

**验证：**
- RED：完整 16-byte clock fixture 配合旧 8-byte accessor assertion 时，`make ktest ARCH=rv64 PROFILE=normal KTEST=platform_fdt_snapshot KTEST_QEMU_TIMEOUT=60`（Docker）QEMU TAP 如预期报 `FDT snapshot lost a required MMC property`。
- `make ktest ARCH=rv64 PROFILE=normal KTEST=platform_fdt_snapshot KTEST_QEMU_TIMEOUT=60`（Docker）✅ — TAP `1..3`，3 passed、0 failed，`[KTEST RESULT: PASS]`。
- Rust LSP 不可用（此前拒绝安装）；`platform_fdt_snapshot.rs`/`platform_fdt_fixture.rs` 分别为 197/99 pure LOC。

**备注：** 当前 QEMU ktest 以 `RiscvFdt` handoff 启动并调用既有 platform 初始化路径，但 pre-heap 内存解析会将 DTB 作为 reserved memory，post-heap `build_platform_info()` 的 `is_ram_phys_addr()` 检查因此采用 Static fallback；测试仅在实际 `FirmwareKind::Fdt` 可访问时断言 QEMU raw `compatible`。未修改生产代码、boot gate、驱动或实板证据；无新增 AI 工具/模型或可复用调试模式。

## FDT Phase 1 全节点原始属性快照

**涉及文件：**
- `os/src/hal/platform/info.rs` — `DeviceInfo` 增加稳定 parent path、`RawProperty` 快照与严格的 exact-length raw accessor；缺失和长度错误分别返回 `Absent`/`Malformed`。
- `os/src/hal/firmware/fdt.rs` — 保留所有 FDT 节点（包括无 compatible 的父节点/provider 节点）及每项原始属性名/字节；`BootProtocol::RiscvFdt` gate 未变，`UbootGo` 仍走静态 fallback。
- `os/src/hal/device/manager.rs` — 增加仅按 enabled 与精确 compatible 枚举的查询，原有 valid-resource 查询行为保持不变。
- `os/src/kernel_tests/{mod,platform_resources,platform_fdt_snapshot}.rs` — 新增 VF2 `snps,dw-mshc` 双节点形状和 raw property fail-closed ktest，并更新既有资源 fixture。
- `docs/10_plan/vf2-fdt-dw-mmc-roadmap.md` — 对齐 raw snapshot Phase 1 边界，移除过时的 gate 放宽、HAL MMC 分类和硬编码资源建议。

**验证：**
- RED：`make ktest-run ARCH=rv64 PROFILE=normal KTEST=platform_fdt_snapshot`（Docker）在实现前因缺少 `parent_path`、`RawProperty` 和 strict accessor 按预期编译失败。
- `make ktest-run ARCH=rv64 PROFILE=normal KTEST=platform_fdt_snapshot`（Docker）✅ — QEMU TAP `1..2`，`platform_fdt_snapshot::preserves_live_vf2_mmc_node_shapes` 与 `platform_fdt_snapshot::rejects_absent_and_malformed_raw_resources` 均通过，`[KTEST RESULT: PASS]`。
- `make kernel ARCH=rv64 PROFILE=normal`（Docker）✅。
- `make kernel ARCH=la64 PROFILE=normal`（Docker；在 RV64 后串行）✅。
- `make lint-format`（Docker）未执行格式检查：固定 nightly 缺少 `rustfmt` component；Rust LSP 同样未安装且此前已拒绝安装。

**备注：** 未增加 `DwMmc`/`Mmc` DeviceKind、驱动、MMIO 映射、时钟/复位/IRQ 写入、块设备发布或 VF2 生产常量；clock/reset/phandle bytes 仍由未来驱动按 binding 解释。本次没有新增 AI 工具/模型或可复用调试模式，`AI-Usage-Report.md` 和 references 无需更新。

## VF2 live U-Boot FDT MMC discovery

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-30/vf2-live-fdt-mmc-probe.md` — 归档 VF2 工作 DTB 的 MMC 节点、别名、`/chosen`、`booti` 能力和完整只读串口输出。
- `docs/10_plan/vf2-fdt-dw-mmc-roadmap.md` — 将 DTB 资源证据状态更新为已采集 MMC 节点字段，并保留完整 DTB blob 和启动 ABI 实装作为后续门禁。

**验证：**
- 单一 exclusive pyserial 会话（`/dev/ttyUSB0`、115200）✅ — `fdt addr -c` 与 `fdt addr` 均为 `f76df9b0`；别名确认 `mmc0=/soc/sdio0@16010000`、`mmc1=/soc/sdio1@16020000`。
- `fdt print /soc/sdio0@16010000` 与 `fdt print /soc/sdio1@16020000` ✅ — 两者均为 enabled `snps,dw-mshc`，各有 64 KiB MMIO、BIU/CIU clock、reset、32-depth FIFO；总线宽度分别为 8 与 4。
- `help booti` ✅ — U-Boot 2021.10 明确支持 `booti <Image> - <DTB>`。

**备注：** 未发送 reset、DTR/RTS 属性赋值、`saveenv`、存储写入、TFTP 或网络命令。工作 DTB 中没有 `interrupts`、`starfive,sysreg` 或 `data-addr`，因此后续 PIO-first 驱动不得从该 DTB 推断这些资源。
## another_ext4 PageCache 一次初始化与 MountFS 写门禁去锁

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — 使用 `spin::Once<Result<Arc<PageCache>, SyscallErr>>` 持有每个 inode 的不可变 PageCache；热路径借用已初始化的 `Arc`，不再升级 filesystem weak ref 或取得 page-cache mutex。`Once` 的自动 drop 仍释放该 `Arc`。
- `os/src/fs/ext4_another/mutations.rs` — regular-file 读写与 resize 改为使用一次初始化缓存；仅实际需要直接 ext4 操作的路径获取 filesystem `Arc`。
- `os/src/fs/vfs/mount.rs` — mount flags 改为 `AtomicU32`；写保护检查只 acquire-load `RDONLY` 位，remount 通过 release-store 发布新 flags。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=ext4_another` ✅（17 passed，含 `depth_one_leading_hole_truncate_succeeds`）。全量 ktest 在第 43 项后触发既有 30 秒 QEMU 外层期限；没有测试断言失败。
- `make lint` ✅（rv64 debug/release：176 first-party warnings；la64 debug/release：179）。
- 注入 `mask=0x110` 后运行 RV64 iozone 组：测试盘缺少 `/musl`、`/glibc` 下的 `iozone_testcode.sh`，两组均为 32512；因此没有可报告的新吞吐数据。

**备注：** vendored `spin 0.7.1` 不支持 fallible `try_call_once`，故将初始化结果存入 `Once<Result<...>>`；filesystem 已不可达时的 `EIO` 是稳定终态。未引入新的 `unsafe`。`docs/03_fs/vfs-core.md` 已是 draft 且明确不涵盖 MountFS 实现细节，无需同步；AI 工具报告已包含当前模型，无需更新。

## pwrite VFS/MountFS 周期细分

**涉及文件：**
- `os/src/task/perf.rs` — 增加四个 memory-I/O profile 门控的 VFS/MountFS 周期原子计数、no-op 存根与 reset 清零，避免跨诊断窗口累积。
- `os/src/fs/vfs/file.rs` — 在 `File::pwrite_user()` 成功路径记录 mode、memfd seals 和 `touch_modified()` 的互不重叠周期。
- `os/src/fs/vfs/mount.rs` — 在 `MountFSInode::write_at_user()` 中记录 `ensure_mount_writable()` 周期。
- `os/src/fs/sysfs/files/diag.rs` — 将四项新增计数导出到 `/sys/kernel/stats/blockio`。
- `docs/09_debug/perf_diag.md` — 同步 `blockio` 的 pwrite 边界归因能力。

**验证：**
- Docker `4abf1ec06ea`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` → `make kernel ARCH=la64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- RV64 derived image 注入 `mask=0x110`、`diag=1` 后完成 `derived-comp`；musl/glibc iozone 各采集 4,096 次 1KiB pwrite ✅。
- 合并 8,192 次样本：`touch_modified()` 平均 1,694,123.10 cycles，是最大的单项；完整计算见 `/tmp/ulw-granular3.txt`。

**备注：** 新计数器首轮遗漏 reset，导致第二个 libc 窗口累积首窗口数据；补齐 `reset_all_counters()` 后重建并重跑。`PWRITE_EXT4_*` 与 PageCache 计数器是组级 write-path 统计，不能与 pwrite-only 分母相减；因此报告仅用同 scope 的 VFS 边界计数做结论。持久证据位于 `docs/Work_Log/evidence/2026-07-30/granular5-pwrite-rv64-*`。

## perf_diag 无串口 pwrite 边界归因

**涉及文件：**
- `os/src/task/perf.rs` — 增加 memory-I/O profile 门控的 pwrite 周期/计数原子值、reset/no-op 支持和专用时钟 helper；避免 core profile 未启用时 rdcycle 返回零。
- `os/src/syscall/fs/common.rs` — 在 UserBuffer 直连与 fallback 两个 `pwrite_from_user` 路径记录用户访问和 `File::pwrite(_user)` 区间，不引入 serial 输出。
- `os/src/fs/ext4_another/mutations.rs` — 在 direct/fallback 写入路径记录 ext4 setup 与成功后的尺寸/dirty-cache retention 后处理。
- `os/src/fs/sysfs/files/diag.rs` — 在 `/sys/kernel/stats/blockio` 导出五个 pwrite 诊断字段。
- `docs/Work_Log/evidence/2026-07-30/granular4-pwrite-rv64-*` — 保存完整 QEMU 串口和零输出 counter 分析。

**验证：**
- Docker `4abf1ec06ea`（`/home/pxy/projects/MangoCore -> /app`）内 `make kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- RV64 `mask=0x110`、`diag=1` `derived-comp` ✅；musl/glibc iozone 均完成，每组各 4,096 次成功 1KiB pwrite。
- 合并平均：UserBuffer `36,045.21` cycles，File/VFS→inode `953,754.17`，ext4 setup `201,277.09`，ext4 post-work `19,258.43`；显式非 PageCache 边界合计 `256,580.73` cycles/call。

**备注：** 首轮计数为零的运行态证据确认 pwrite 实际走 fallback `write_at` 路径，且原始 `perf_time_now()` 受未启用的 core profile 门控；改用 memory-I/O profile 后两个 iozone 窗口均得到非零测量。既有“高频性能探针污染”经验已覆盖，无新增通用沉淀；`AI-Usage-Report.md` 无需更新。

## 1KiB pwrite UserBuffer 热路径 rdcycle 分层采样

**涉及文件：**
- `os/src/syscall/fs/common.rs`、`os/src/fs/page_cache.rs`、`os/src/fs/ext4_another/mutations.rs` — 临时加入并在采样后移除 `rdcycle`/`println!` 分层标记，生产逻辑与输出保持不变。
- `docs/Work_Log/evidence/2026-07-30/granular-pwrite-rv64-*` — 保存 RV64 构建、配置注入、完整 QEMU 串口、首尾摘要、命令、容器映射、git hash 与聚合结果。

**验证：**
- Docker `4abf1ec06ea`（`/home/pxy/projects/MangoCore -> /app`）内 `make kernel ARCH=rv64 PROFILE=normal` ✅。
- `mask=0x110`、`diag=1` 的 RV64 `derived-comp` ✅；musl 和 glibc iozone 各采到 4,096 个配对 1KiB pwrite 样本（合计 8,192）。
- 聚合 PageCache 直接数据路径：lease `12,689.05` cycles（43.44%）、1KiB `UserBuffer::read_at` `8,473.35` cycles（29.01%）、lookup `3,524.84` cycles（12.07%）。完整汇总：`/tmp/ulw-granular-timing.txt`。

**备注：** 内层 `println!` 的串口成本会计入 ext4 与 pwrite 外层区间，故 `cache.write_user`（`1,060,337.93` cycles）和 `File::pwrite_user`（`2,856,896.62` cycles）仅作调用链归因，不能视为无探针生产耗时；已知“高频性能探针污染”模式已有 reference 覆盖，无新增沉淀。`AI-Usage-Report.md` 已含本会话模型记录，无需更新。

## perf_diag iozone I/O 放大计数

**涉及文件：**
- `os/src/task/perf.rs` — 增加 journal commit、device flush、实际 VirtIO 读写请求和成功 writeback batch/page 的 profile-aware 计数与 reset/no-op 支持。
- `dependency/another_ext4/src/ext4{_defs/block,journal_transaction}.rs`、`os/src/fs/ext4_another/blockdev.rs` — 通过无侵入 BlockDevice diagnostic hook 在 durable journal commit 成功后记录 journal payload；不改变提交顺序或错误处理。
- `os/src/drivers/block/{virtio_blk,virtio_blk_pci}.rs`、`os/src/fs/page_cache.rs` — 按实际 DMA fallback 分片记录 VirtIO 请求，按成功 writeback run 记录批次。
- `os/src/fs/sysfs/files/diag.rs`、`user/src/bin/test_runner/{groups/execute,instrumentation/drift}.rs` — 将 counters 导出到 `blockio`，并让 `diag=1` 真正对每个脚本组重置、采样和打印该文件。
- `docs/09_debug/perf_diag.md` — 同步计数器 ABI 与 diag 窗口语义。
- `docs/Work_Log/evidence/2026-07-30/io-amplification-rv64-*` — 保存配置、容器映射、git hash、完整 QEMU 串口输出、首尾摘要和命令状态。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` → `make kernel ARCH=la64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- RV64 `mask=0x110`、`diag=1` 的 `make ARCH=rv64 -f make/rv64.mk derived-comp` ✅；musl/glibc iozone 两个窗口均打印新 blockio counters。
- 合并 iozone 窗口：36,695 成功 writeback 页（143.34 MiB）、244 journal commits、1,636 flushes、160,034 VirtIO writes（平均 4,999.90 bytes/request）、160,440 VirtIO reads、150 writeback batches。

**备注：** 标准 `iozone_testcode.sh` 包含目标 `-t 4 -i 0 -i 1 -r 1k -s 1m` 及其他子测试，故放大比按成功 writeback 数据而非固定 4 MiB 归一化；完整分析已写入 `/tmp/ulw-io-amplification.txt`。`AI-Usage-Report.md` 已含本会话模型记录，无需更新；现有 references 已覆盖 VirtIO 分片模式，无新增通用知识条目。

## another_ext4 writeback journal batching

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — 将单页 append 纳入 JBD2 deferred transaction；对于已映射覆盖写入，在持有 transactional/inode mutation guards 时直接写数据，避免落入 direct metadata domain 提前提交 deferred journal。
- `dependency/another_ext4/src/ext4/data_write.rs` — 提取仅处理已映射数据块的内部写入核心；普通 direct caller 仍获取 direct metadata guard，journaled caller 不会绕过其持有的 transactional guard。
- `.agents/skills/mango-workflow/references/debugging-patterns.md` — 记录 deferred journal 被 direct metadata barrier 意外击穿的诊断模式。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内 `make toolchain-preflight` ✅。
- 串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（`[KTEST RESULT: PASS]`）。
- normal QEMU 成功启动到 runner；当前工具盘缺少 `/musl`、`/glibc` iozone 脚本且未启用 perf_diag sysfs，无法产生当前改动的新计数窗口，未将其记作性能验收。

**备注：** 不对 `Ext4FileSystem::flush_device()` 增加时间限流：它保留给 sync 边界，避免损坏 fsync/sync durability；背景回写改为不进入 direct metadata domain，journal 仅在 deferred transaction 达到容量/环压力或显式同步时提交。

## PageCache fsync EAGAIN yield retry 与最终双架构 LTP

**涉及文件：**
- `os/src/fs/page_cache.rs` — 将全量、范围、单页及批量回写路径的有界 `EAGAIN` 重试从 `spin_loop()` 改为 `suspend_current_and_run_next()`；页面仍保持既有写回认领与错误传播语义。
- `docs/03_fs/page-cache.md` — 说明 `fsync`/全量回写的瞬态冲突按上限 100 次调度让出重试。
- `docs/Work_Log/evidence/2026-07-30/ulw-final-*-ltp-*` — 保存最终 LTP 配置、完整 QEMU 输出、首尾摘要、命令、git hash 与容器挂载信息。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- RV64：`mask=0x800` 52 用例 `derived-comp` ✅（`52 PASS / 0 FAIL / 0 SKIP`）。
- LA64：同配置族 59 用例 `derived-comp` ✅（`55 PASS / 3 expected FAIL / 1 SKIP`）；预期失败为 `creat05`、`open04`、`statx01`。

**备注：** `fsync01` 在 LA64 仍因缺少可格式化支持文件系统而 SKIP；此非新增失败。`AI-Usage-Report.md` 已记录本会话模型 `GPT-5.6-terra`，无需更新。

## PageCache fsync transient EAGAIN retry

**涉及文件：**
- `os/src/fs/page_cache.rs` — 同步写回在短暂 `Loading`/`Writeback` 状态及后端 `EAGAIN` 时最多自旋重试 100 次；单页和批量写回在重试前保持页面认领，且单页填充失败时恢复 Dirty 状态与全局计数。
- `os/src/kernel_tests/page_cache/{mod.rs,writeback_retry.rs}` — 注册 transient-`EAGAIN` 后端 fixture，验证 `writeback_all()` 在三次临时失败后成功落盘并清除 dirty 状态。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（65 passed、0 failed；新增 `page_cache::writeback_retries_transient_eagain` 通过）。
- `make lint` ✅（rv64 debug/release：176 首方 warning；la64 debug/release：179 首方 warning）。
- `mask=0x110` 的 RV64 第 1 轮 QEMU 实测进入 iozone 并完成此前各阶段，但在 pwrite/pread 阶段超过 10 分钟被宿主命令超时终止；未观察到 `fsync: Resource temporarily unavailable` 或 `iozone: interrupted`。因此双架构五轮中位数尚未形成，不能将旧样本作为本次修复的基准结论。

**备注：** 写回后端的 metadata/journal gate 也会短暂返回 `EAGAIN`；该错误现在在 PageCache sync 路径内消化，永久 I/O 错误仍向上返回。证据：`docs/Work_Log/evidence/2026-07-30/fsync-eagain-rv64-round1-{inject,qemu-output}.log`。

## Packed flags + RwLock 最终双架构 iozone/lmbench 三轮基准

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-30/{rv64,la64}-round{1,2,3}-*` — 每轮从 immutable official x0 重新派生并注入 `mask=0x110`、`skip_apk=true`；保存配置、注入日志、完整 QEMU 输出、首尾摘要和退出状态。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内先 RV64 后 LA64 串行执行三轮 `derived-comp`，六轮均以 0 退出。
- 三轮算术均值：RV64 musl simple syscall `39.09 μs`、iozone 首个 write/read writers max `655 kB/s`、readers max `2,115 kB/s`、children writers `2,321 kB/s`；LA64 分别为 `4.27 μs`、`580 kB/s`、`3,135 kB/s`、`2,279 kB/s`。

**备注：** 对照 Linux 日志计算的表格已写入 `/tmp/ulw-final-comparison.md`；吞吐 Gap 表示 Linux/Mango，延迟 Gap 表示 Mango/Linux。

## PageCache pwrite flags 与目录读锁优化

**涉及文件：**
- `os/src/fs/page_cache.rs` — 将页目录改为 `RwLock<Vec<Option<Arc<PageEntry>>>>`；查找、快照和统计使用读锁，发布、删除及扩容使用写锁。将 `state`、`dirty` 和独立 flags 合并为一个 packed `PG_*` 原子字节；写入 lease 保持既有 dirty 位，仅加锁、复制后一次发布 dirty/up-to-date，写回保留失败与 redirty 的 dirty/counter 恢复语义。
- `os/src/fs/page_cache_ktest.rs` — 令竞争写回 fixture 通过 packed flags 认领、完成及恢复页面，继续覆盖 writeback 失败后的 redirty retry。
- `os/src/fs/ext4_another/lifetime.rs` — 为 dirty PageCache retention 加入 acquire/release fast gate；释放时在 mutex 内重检 generation，避免与新写入竞争时错误 unpin。
- `docs/03_fs/{page-cache,README}.md` — 同步目录锁、packed flags 与 writeback 语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed；含 writeback failure/redirty retry 覆盖）。
- `make lint` ✅（rv64 debug/release：176 首方 warning；la64 debug/release：179 首方 warning）。
- `mask=0x110` QEMU group 已启动，但当前测试盘缺少 `/musl` 与 `/glibc` 的 `lmbench_testcode.sh`、`iozone_testcode.sh`；两组均返回 32512，因此没有可报告的 iozone 吞吐或本次优化百分比。

**备注：** `PageState` 保留为公开诊断投影，避免改变 PageCache API；内部状态由 orthogonal `PG_*` 位驱动。`page_cache.rs`（2377 纯代码行）和 `lifetime.rs`（406 行）均为既有超限文件；本次只作限定的热路径修改，未安全拆分其既有职责。

## PageCache / another_ext4 性能实验回退与双架构回归

**涉及文件：**
- `os/src/fs/page_cache.rs` — 移除原子 radix `PageTable`，恢复由 `Mutex<Vec<Option<Arc<PageEntry>>>>` 管理的 `PageEntries`；目录锁只用于短暂的查找、发布、删除和快照克隆。
- `os/src/fs/ext4_another/lifetime.rs` — 移除 inode 小写合并缓冲与一次性 dirty-cache pin；保留时间戳缓存。
- `os/src/fs/ext4_another/mutations.rs` — 恢复每次成功写入后的 dirty PageCache 保留；保留 UserBuffer 直连 PageCache 路径。
- `os/src/fs/ext4_another/inode.rs` — 移除与待写缓冲相关的读、metadata 和同步冲刷路径。
- `docs/03_fs/page-cache.md` — 同步 `PageEntries` 的 mutexed 向量目录与快照扫描设计。
- `.gitignore` — 显式纳入当日持久化验证证据，避免结果仅保留在容器 `/tmp`。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内 `make toolchain-preflight` ✅。
- 串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅。
- `make lint` ✅（rv64 debug/release：176 首方 warning；la64 debug/release：179 首方 warning）。
- 5 轮 QEMU 基准中位数：RV64 syscall `36.52µs`、writers `2312.30 kB/s`、readers `8261.20 kB/s`、writers_aggr `1845.19 kB/s`、musl pipe `574.49µs`；LA64 syscall `4.62µs`、writers `1888.65 kB/s`、readers `11513.62 kB/s`、writers_aggr `1353.37 kB/s`、musl pipe `458.11µs`。
- LTP：完整 RV64 为 `57 PASS / 1 FAIL (statx01) / 1 SKIP`；完整 LA64 为 `55 PASS / 3 FAIL (creat05, open04, statx01) / 1 SKIP`；排除 `statx01,fsync01,dup203-dup207` 的 RV64 最终集为 `52 PASS / 0 FAIL / 0 SKIP`。

**证据：** `docs/Work_Log/evidence/2026-07-30/` 保存五轮汇总、逐轮 QEMU 输出及双架构 LTP 输出。

**备注：** 本次是性能实验的回退与回归验证，不将该批基准与此前写合并版本作吞吐提升结论。

## Oracle P0/P1：another_ext4 小 pwrite 写合并

**涉及文件：**
- `os/src/fs/ext4_another/lifetime.rs` — 新增每 inode、最多一页的连续写缓冲；缓冲锁在 PageCache 复制前释放，读取、同步、关闭、截断和全局 sync 都会先冲刷；脏 PageCache pin 改为 `AtomicBool` 一次性取得并在成功同步释放后复位。
- `os/src/fs/ext4_another/mutations.rs` — 小于 `PAGE_SIZE` 的内核/用户缓冲写入仅在相邻时合并；整页及以上 UserBuffer 保持原有直连 `PageCache::write_user` 路径；`write_direct`、`write_sync` 走不合并的 write-through 路径。
- `os/src/fs/ext4_another/inode.rs` — 读和 metadata 可观察路径先冲刷待写数据，保证 close/fsync 之外的可见性。
- `os/src/fs/vfs/file.rs` — `O_DIRECT`、`O_SYNC`/`O_DSYNC` 在内核与 UserBuffer 写路径均分派到 inode direct/sync 钩子，避免这些文件描述符进入写合并。
- `lint-baseline/{rv64,la64}-{debug,release}.txt` — 删除已由本次清理的 `mutations.rs` unknown warning 四格基线项。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）内 `make toolchain-preflight` ✅。
- 串行 `make kernel ARCH=rv64 PROFILE=normal` → `make kernel ARCH=la64 PROFILE=normal` ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅（64 passed、0 failed）。
- `make lint` ✅（rv64 debug/release：176 首方 warning；la64 debug/release：179 首方 warning）。
- RV64 iozone group 已尝试，但当前测试盘缺少 `/musl/iozone_testcode.sh` 与 `/glibc/iozone_testcode.sh`，两者均以 `32512` 结束；因此没有可报告的吞吐提升数值。

**备注：** 该实验已在同日后续条目中完整回退；该条仅保留为历史记录。
