---
date: 2026-08-04
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-04

<a id="2026-08-04-wp7-final-rv64-003-pass"></a>
## WP7 最终 §8.2 RV64 8 核 `0x003` 门禁：PASS

**Mango Gate:** skill loaded=yes；references=none（执行已确定的最终竞赛门禁；不进行新的故障分析）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp7-final-rv64-003-source-fingerprint.txt` 与 `fs-net-smp-wp7-final-rv64-003-{conf-inject,serial,judge}.log` — 最终门禁的冻结来源、配置注入、完整 QEMU 串口与官方 judge 输出。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp7-final-rv64-003-verdict.md` — raw/semantic 账本、硬条件与 PASS 裁决。

**验证：**

- Docker 内严格串行执行 `make -C /app/os conf-inject CONF_ARCH=rv64 CONF_BLK_MODE=virt` 后，`CORE_NUM=8 make -C /app/os rv64-derived-run` 与官方 judge 均 exit 0；复用冻结 `mask=0x003`、`ltp_libc=both` 配置，未使用 `make run`、`0xFFF` 或源代码重建。
- X1 工具盘 P2（FAT32）启动扇区在运行前含 `55aa` 与 `FAT32   `；此前 `test_mount/test_umount` 的 `EINVAL` 根因是该生成产物的 P2 被清零，而非内核 mount 路径。debug agent 重建后本次无须再次重建。
- raw `312/314`，两 libc `test_pipe` 均 `4/4`，归一化 `+0`，semantic `312/314`（基线 `>=312/314`）。`test_mount` 和 `test_umount` 在 musl/glibc 均为 `5/5`；仅剩 busybox-musl/glibc 各一项允许的 `busybox kill 10=0/1`。
- `configured=8`、`online_mask=0xff`；四组均有 START/END/`exit_code=0`，存在 `run_selected_groups done`，无 panic/fatal trap/deadlock/runner failure/Assert Fatal，官方 judge 识别 314 points。

**备注：** 门禁期间未修改生产源码，LSP diagnostics 不适用；无新增可泛化源码模式，不追加 references。无新的 AI 工具/模型或需单列的重要 AI 辅助成果，`AI-Usage-Report.md` 无需更新。

<a id="2026-08-04-rv64-vda2-fat32-rebuild"></a>
## RV64 `/dev/vda2` mount `EINVAL` 根因判定与工具盘重建：PASS

**Mango Gate:** skill reused=yes；references=`debugging-patterns.md`（块设备/文件系统故障定位）。

**涉及文件：**

- `os/src/syscall/fs/sys_mount.rs`、`os/src/fs/filesystem.rs` — 仅临时加入并已删除诊断输出；确认 `detect_fs_layout()` 返回 `None` 后，`sys_mount()` 在 550–553 行返回 `EINVAL`。
- `scripts/make_mbr_tools_disk.py`、`os/make/tools-disk.mk` — 检查既有 P2 FAT32 格式化逻辑；未修改源码，通过 `make -C /app/os tools-disk-rv` 重建过期/损坏的 X1 工具盘产物。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp2-gate-config.conf` — 三次 fresh-derived `mask=0x003` 直接路径复测所用配置。

**验证：**

- 重建前，`/dev/vda2`（X1 `/dev/vdb2`）的 MBR P2 存在但其 FAT32 boot sector 为全零；`test_mount`/`test_umount` 在两个 libc 中均为 `0/5`，返回 `-22`。重建后 P2 含 `55aa`、`FAT32   ` 与 `bps=512`，同一路径 mount/umount 恢复成功。
- Docker 内串行 `CORE_NUM=8`：fresh-derived `mask=0x003` 直接路径连续三次通过；因镜像未提供 `mkfs.ext4`，单独 LTP mount 集合的 12 项均为 `TCONF(32)`，不能作为该块设备路径的额外结论。
- `make -C /app/os ktest-run ARCH=rv64 PROFILE=normal KTEST=net_smp`：`10/10 PASS`；`KTEST=fs_smp`：`8/8 PASS`；`KTEST=ext4`：`7/7 PASS`（`lw_path_isolation` 按零盘 fixture 条件 SKIP，但 runner 计为通过）。均为 Docker 内 8 核 RV64 串行运行。

**备注：** 问题属于生成的工具盘状态，不是内核挂载逻辑回归；未保留生产 Rust 改动，临时 trace 已移除，故 LSP diagnostics 不适用。未形成可泛化的源码修复模式，不追加 references；无新增 AI 工具/模型，`AI-Usage-Report.md` 无需更新。

<a id="2026-08-04-wp7-rv64-pristine-discrimination"></a>
## WP7 RV64 0x003 pristine-image 判别：FAIL（可复现回归，非镜像复用伪象）

**Mango Gate:** skill loaded=yes；references=`harness-patterns.md`、`debugging-patterns.md`（长时 QEMU/镜像状态与 LTP 故障判别）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-phase5-rv64-003-pristine-{command,conf-inject,serial,judge}.log` — 由官方压缩 x0 重建后的一次串行 RV64 8 核门禁及完整证据。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-phase5-rv64-003-pristine-verdict.md` — 输入哈希、raw/semantic 账本、LTP 对照和裁决。

**验证：**

- Docker 内：从 `fs-img-dir/sdcard-rv.img.xz` 重提取 x0；提取结果、替换前备份与 `sdcard-rv.img` 均为 SHA-256 `95973543db6b84a9a5e70f30da466ce292867aff5b689fb14c88dc9406e378b8`。`conf-inject` 按脚本的 official→derived copy 路径只成功注入一次冻结的 WP2 `0x003` 配置，随后 `CORE_NUM=8 make -C os rv64-derived-run` 与 judge 都退出 0。
- raw/semantic 均为 `292/314`（`test_pipe` 两 libc 均 raw `4/4`，归一化 `+0`）；相对 `312/314` 基线新增 basic-musl/glibc 各自的 `test_mount=0/5`、`test_umount=0/5`，不属于允许 multiset。
- 四个失败块均为 `mount /dev/vda2 ./mnt` 返回 `-22` (`EINVAL`)，不是 `EBUSY`、`EEXIST` 或只读错误。WP7 `0xA70` LTP 的 mount 相关输出是独立的 tmpfs `EOPNOTSUPP (95)`，且没有 `/dev/vda2`/`test_mount`/`test_umount` 记录；不能解释这四个失败。

**备注：** 判定为可复现的 kernel/runtime 回归，而非 image reuse harness artifact；按任务约束未修复。原 `sdcard-rv.img` 已恢复并 `cmp` 验证。无生产 Rust 源码改动，LSP diagnostics 不适用；`AI-Usage-Report.md` 已检查，`GPT-5.6-terra` 已在工具清单中，无需更新。

<a id="2026-08-04-fs-net-smp-wp7-phase5-fail"></a>
## WP7 FS/Net SMP Phase-5 broad gate 与文档收尾：FAIL

**Mango Gate:** skill loaded=yes；references=harness-patterns.md（涉及 iozone/iperf/netperf 性能门禁与长时间 QEMU 证据）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-phase5-*` — 七份冻结 config/hash、严格串行 priority runner、完整 conf-inject/serial/judge、manifest 与 FAIL verdict。
- `docs/01_architecture/lock-order.md` — 固化 F0–F3、I0–I3、N0–N3、OUTPUT_LOCK 和禁止反向关系。
- `docs/03_fs/{page-cache,ext4}.md`、`docs/06_net/{README,architecture,device-stack-and-poll,inet-common,net-core-iface,routing,tcp,udp}.md` — 标记 current 并同步已实现锁边界与未声明范围。
- `AGENTS.md`、`docs/10_plan/fs-net-smp-execution.md` — 更新完成能力边界、强制保留 exclusions，收束 WP7 为 FAIL。

**验证：**

- Docker 内严格串行 `CORE_NUM=8`：`0xA70` RV64（exit 124，partial judge `195.5582336942699/1647`）→ LA64（exit 124，partial judge `113.7983655323569/791`）→ `0x003` RV64（exit 0，raw/semantic `292/314`）→ LA64（exit 0，raw/semantic `308/314`）。
- RV64 `0x003` 除允许的 busybox `kill 10=0/1` 外，新增两 libc 的 `test_mount=0/5`、`test_umount=0/5`；不属于 §8.2 允许 multiset，故 Phase-5 FAIL。LA64 保持允许的 `test_brk=1/3` + busybox `kill 10=0/1`。
- 单 mask `0x010/0x020/0x040/0x200/0x800` 与性能 3-run median：NOT RUN。两个 `0xA70` 各耗满 40 分钟，最终 RV64 出现硬失败；继续运行不会扭转 gate，且完整矩阵超过约 2.5 小时预算。

**备注：** 未修改生产 Rust 源码；无须 LSP diagnostics。A→D：无新的可复用根因（仅记录原始 FAIL，不推断）；已检查本文档同步范围与 `AI-Usage-Report.md`，未使用新的 AI 工具/模型或产生需单列的 AI 成果。仍不声明 per-socket parallel smoltcp data path、lock-free mount table、default all-core user scheduling 或完整 FS/Net cross-subsystem audit。

<a id="2026-08-04-wp6-gate-rerun-pass"></a>
## WP6 §8.2 双架构 8 核初赛非回归门禁重跑：PASS

**Mango Gate:** skill loaded=yes；references=none（执行既定竞赛门禁，未进入故障根因调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp6-gate-{manifest,command,verdict}.md` — 固定输入、严格串行命令、硬条件与 PASS 裁决。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp6-gate-source-fingerprint.txt` 与 `fs-net-smp-wp6-gate-{rv64,la64}-{serial,judge}.log` — 修复后前后源码指纹、完整串口与官方 judge 输出。

**验证：**

- Docker 内严格串行 RV64 → judge → LA64 → judge；复用 SHA-256 `c9ecca95e8bd5e8dfd1999b80bf68f83d2f74fd50468de6345fb8fef57903805` 的 `mask=0x003` config，未使用 `make run` 或 `0xFFF`，未触发 kernel/build/tools-disk。
- 两架构均 `CORE_NUM=8`，serial 均有 `configured=8`、`online_mask=0xff`；四组均 START/END/`exit_code=0`，`run_selected_groups done` 与 recipe exit 0，未见 panic/fatal/deadlock/runner failure/Assert Fatal。
- RV64 raw `309/314`；basic-glibc `test_pipe` 满足 §8.2 的唯一归一化条件，semantic `312/314`，归一化后仅 busybox-musl/glibc `busybox kill 10=0/1`。LA64 raw/semantic `308/314`，仅 basic-musl/glibc `test_brk=1/3` 与 busybox-musl/glibc `busybox kill 10=0/1`。

**备注：** 运行前/后 HEAD、tracked diff、非忽略 untracked-content digest 与 visible status 一致；本工作包未改生产源码。无新增跨任务经验需沉淀；已检查 `AI-Usage-Report.md`，未使用新的 AI 工具/模型或产生需单列的重要 AI 辅助成果，无需更新。无源码修改，LSP diagnostics 不适用。

<a id="2026-08-04-wp6-poll-worker-boot-fix"></a>
## WP6 poll-worker 早期启动阻塞回归修复（L2/T3）

**Mango Gate:** skill loaded=yes；references=debugging-patterns.md。

**涉及文件：**

- `os/src/main.rs` — normal boot 先发布 PID1，再发布固定 CPU0 的 net poll worker，避免 worker 首次调度先进入 `wait_event_interruptible_lazy()` 的 Blocking 路径。
- `docs/Work_Log/evidence/2026-08-04/wp6-boot-{repro-before,ab-worker-disabled,fixed}.log` — 原始 RED、禁用 worker 的 A/B 以及最终 derived 运行完整串口。
- `docs/Work_Log/evidence/2026-08-04/wp6-ktest-{net_smp,fs_smp,ext4}.log` — 4 核零盘 focused 回归完整串口。

**验证：**

- RED：Docker 内 `CORE_NUM=8 make -C os rv64-derived-run` 在 150 秒退出码 124；最后输出为 `/dev/vdb2` MBR 注册，未出现 PID1 或测试组。
- A/B：仅移除 normal worker publication 后，完全相同的 derived 双盘运行进入 `basic-musl`，并完成 `mask=0x003` 的四组，退出码 0。
- GREEN：`CORE_NUM=8 make -C os rv64-kernel-build-only PROFILE=normal && CORE_NUM=8 make -C os rv64-derived-run` 完成；串口含 `[init] pseudo-fs mount framework ready`、`OS COMP TEST GROUP START basic-musl`、四个 `exit_code=0` 和 `run_selected_groups done`。
- Docker 内 `CORE_NUM=4 make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=net_smp`：10/10 PASS；`KTEST=fs_smp`：8/8 PASS；`KTEST=ext4`：7/7 PASS。
- 本会话无可用 LSP diagnostics 工具；最终 RV64 release build 成功。未执行 LA64 或本次 §8.2 judge，不能将本条描述为双架构竞赛门禁通过。

**备注：** `run_queue::publish()` 以 FIFO `push_back()` 发布，且 `fetch()` 在无 nice 任务时 `pop_front()`；PID1 先入队可保证首次 CPU0 dispatch 不由会立即 Blocking 的 worker 抢占。worker 的 generation 条件在首次 wait 前和 `prepare_to_wait()` 后均 Acquire 重查，因此保持 producer-before-register 与 register 后的无丢唤醒保证。`docs/01_architecture/boot-and-trap.md` 已为 draft，未细述此 worker 排序；`docs/08_testing/README.md` 对 ktest 分支位置描述仍准确。`AI-Usage-Report.md` 已列出当前模型，无需更新。

<a id="2026-08-04-fs-net-smp-wp6-gate-fail"></a>
## WP6 §8.2 双架构 8 核初赛非回归门禁：FAIL

**Mango Gate:** skill loaded=yes；references=none（执行既定竞赛门禁，未进入故障根因调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp6-gate-{manifest,command,verdict}.md` — 冻结输入、Docker 串行命令、timeout 停止点与最终 FAIL 判定。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp6-gate-source-fingerprint.txt` 与 `fs-net-smp-wp6-gate-{rv64,la64}-{serial,judge}.log` — 前后源码指纹、完整 RV64 串口，以及未运行 judge/LA64 的 fail-fast 记录。

**验证：**

- Docker 内 RV64 `conf-inject` 成功，`CORE_NUM=8 make -C os rv64-derived-run` 的 serial 已见 `configured=8` 与 `online_mask=0xff`，但 QEMU 在 600 秒 executor timeout 前未到任何测试组 START/END 或 `exit_code=0`；遗留 QEMU child 已 SIGTERM 清理。
- 按 §8.2 硬条件/fail-fast：RV64 judge、raw/semantic 和 failure multiset 均未产生；LA64 未运行。不能以 WP2–WP5 基线替代本次结果，门禁结论为 FAIL。
- 运行前/超时后 HEAD、tracked diff、非忽略 untracked-content digest 和 visible status 一致；门禁期间未编辑生产源码。未执行 LSP（无源码改动），且构建/QEMU 验证未通过，未声称通过。

**备注：** 此条仅归档执行结果，不修复或推断 timeout 根因；无新增跨任务经验可沉淀。已检查 `AI-Usage-Report.md` 的既有工具清单：未使用新的 AI 工具/模型或产生需要单列的 AI 辅助成果，无需更新。

<a id="2026-08-04-fs-net-smp-wp6-poll-worker"></a>
## WP6 generation poll worker（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes；references=none（已批准功能实现，非故障或性能调试）。

**涉及文件：**

- `os/src/net/config.rs` — 新增 `NetPollControl` 的 requested/completed generation、IRQ deferred wake、CPU0 worker、逐栈 try-lock poll 和锁外 DHCP/UDP/socket/epoll 通知；`try_poll_irq()` 改为 publish-only，并将 ktest hook 移到 `IrqBeforePublish`。
- `os/src/main.rs`、`os/src/task/mod.rs` — normal 与 ktest boot 均创建 CPU0-pinned worker；normal worker 使用独立 kernel PCB，ktest 由 ktest reaper 管理。
- `os/src/task/manager.rs` — CPU0 deferred timer 仅发布 generation，随后在安全点转换 `deferred_wake`；`WaitQueue::new()` 允许静态 poll control 初始化。
- `os/src/fs/eventpoll.rs`、`os/src/net/socket/inet/{stream/mod.rs,datagram/udp.rs}` — epoll scan 和 socket 路径只 kick + 扫描已发布状态；移除 TCP WaitQueue 条件闭包及 UDP readiness scan 内的同步 poll。

**验证：**

- 静态审计：IRQ 路径仅执行 ktest hook、`requested.fetch_add(Release)` 与 `deferred_wake.store(Release)`；不触碰目录、DeviceStack、WaitQueue、smoltcp、分配或输出。
- 静态审计：worker 对每轮 snapshot target 写入 `completed(Release)` 后重新读取 `requested(Acquire)`；busy stack 重新发布 generation；所有 DHCP、UDP 分发和 socket/event wake 均在 DeviceStack 锁外。
- `make` / `cargo` / QEMU / LSP：**未运行**（协调器明确要求本子任务不运行；本会话没有可用 LSP diagnostics）。`net_smp::irq_poll_is_publish_only`、`poll_worker_no_lost_wake` 与双架构 T3 门禁由协调器串行确认。
- 协调器 rv64 首轮编译反馈的 E0015/E0308/const-allocation 已机械修复：poll queue 改为 `Mutex<Option<WaitQueue>>` + 首次等待惰性创建，worker 入口改为 `fn()`，并恢复误设为 `const` 的 `ActiveTracker::new()`；等待下一轮串行 rv64 重建。

**备注：** no-lost-wake 覆盖 producer-before-register、prepare-to-wait 窗口、register 后 task wake、IRQ deferred wake、poll 中 generation 变化和 stale wake 六种交错；`docs/06_net/device-stack-and-poll.md` 已是 draft 且仍含 WP5 前结构，待 WP7 统一同步。无新的跨任务经验需沉淀；`AI-Usage-Report.md` 无需更新。

<a id="2026-08-04-fs-net-smp-wp5-gate-pass"></a>
## WP5 §8.2 双架构 8 核初赛非回归门禁：PASS

**Mango Gate:** skill loaded=yes；references=none（执行既定竞赛门禁，非故障或性能调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp5-gate-{manifest,command,verdict}.md` — 冻结输入、严格串行命令、硬条件与最终判定。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp5-gate-source-fingerprint.txt` 与 `fs-net-smp-wp5-gate-{rv64,la64}-{serial,judge}.log` — 前后源码指纹、完整 QEMU 串口与官方 judge JSON。

**验证：**

- Docker 内严格串行 RV64 → LA64；复用 SHA-256 `c9ecca95e8bd5e8dfd1999b80bf68f83d2f74fd50468de6345fb8fef57903805` 的 `mask=0x003` config，未使用 `make run` 或 `0xFFF`，未触发 kernel/build/tools-disk。
- 两架构 `CORE_NUM=8`，serial 都有 `configured=8`、`online_mask=0xff`；四组均 START/END/`exit_code=0`，且 `run_selected_groups done`、recipe exit 0、无 kernel panic/fatal trap/deadlock/runner failure/Assert Fatal。
- RV64 raw/semantic `312/314`，仅 busybox-musl/glibc `busybox kill 10=0/1`；LA64 raw/semantic `308/314`，仅 basic-musl/glibc `test_brk=1/3` 与 busybox-musl/glibc `busybox kill 10=0/1`。两架构 `test_pipe` 都 raw `4/4`，§8.2 normalization 均为 +0。

**备注：** 前后 HEAD、tracked diff、非忽略 untracked-content digest 与 visible status 均不变，本门禁未编辑生产源码。已检查 `AI-Usage-Report.md`：本次只执行既定 gate、未使用新的 AI 工具/模型或产生需要单列的重要 AI 成果，无需更新；无新的跨任务经验需沉淀。

<a id="2026-08-04-fs-net-smp-wp5-borrow-fix"></a>
## WP5 DeviceStack rv64 借用检查修复（等待协调器复构建）

**Mango Gate:** skill reused=yes；references=debugging-patterns.md（协调器已提供精确编译诊断）。

**涉及文件：**

- `os/src/net/config.rs` — `try_poll_stack()`、`poll_once()` 解构同一 `DeviceStackInner` guard 的 `iface/device/sockets` 再调用 `Interface::poll()`；`tcp_connect()` 同样解构 `iface/sockets`；为 `_add_socket()` 和 legacy direct remove 显式绑定栈锁 guard。

**验证：**

- 静态复核已报告的 E0499/E0597 位置及同类活动路径；未运行 `make` / `cargo` / QEMU，等待协调器串行 rv64 重建确认。

**备注：** 这是 Rust borrow checker 的机械修复，不改变 WP5 route directory、DeviceStack 锁序或 WP6 poll-worker 边界；无新的跨任务经验需沉淀，`AI-Usage-Report.md` 无需更新。

<a id="2026-08-04-fs-net-smp-wp5-device-stack"></a>
## WP5 per-device DeviceStack 路由目录（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes；references=none（已批准功能实现，非故障或性能调试）。

**涉及文件：**

- `os/src/net/config.rs` — 以短持 `NetDirectory` 和逐设备 `DeviceStackCell::inner` 取代全局 smoltcp 锁；route ID 单调分配、栈内 binding 重验、两阶段 remove/rebind。
- `os/src/net/socket/inet/datagram/udp.rs` — 将 smoltcp UDP drain 与 OS socket/EventWaitQueue 分发拆为栈锁内/外两个阶段。
- `os/src/net/socket/{packet.rs,inet/stream/{mod.rs,inner.rs}}`、`os/src/net/ioctl.rs` — 改为 route ifindex/目标栈 API，不再直接读取全局接口内部结构。
- `os/src/net/routing.rs` — 删除已被栈内 binding 取代的全局 `SocketBinding`。
- `docs/01_architecture/lock-order.md` — 记录 WP5 N0/N2/N3 锁序和禁止反向嵌套。

**验证：**

- 静态审计：routed access 固定为目录确认 `Active + protocol` → upgrade `Weak` → 释放目录 → 单栈锁内按 route ID/protocol 重验 binding → smoltcp 操作；旧 route 无法命中重用的 `SocketHandle` slot。
- 静态审计：add 先建立 local binding 后发布目录；remove 先撤目录再删 local binding/socket；UDP rebind 经 `Migrating` 且 source/target 栈锁不重叠。
- `make` / `cargo` / QEMU 未运行（协调器要求串行执行）；本会话无 LSP diagnostics 工具。双架构 build、`net_smp::route_handle_reuse_rejected` 与 `per_stack_poll_progress` 的实际 GREEN 由协调器执行。

**备注：** `try_poll_irq()` 的 WP1 `IrqAfterTryLockBeforeDrop` hook 保持不变，仍保留 RED，等待 WP6 publish-only poll worker；未发现独立于既有 SMP 文档的跨任务经验，`AI-Usage-Report.md` 的工具/案例清单无需更新。

<a id="2026-08-04-fs-net-smp-wp4-gate-pass"></a>
## WP4 §8.2 双架构 8 核初赛非回归门禁：PASS

**Mango Gate:** skill reused=yes；references=none（执行既定竞赛门禁，非故障或性能调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp4-gate-{manifest,command,verdict}.md` — 冻结输入、串行命令、硬条件与判定。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp4-gate-source-fingerprint.txt` 与 `fs-net-smp-wp4-gate-{rv64,la64}-{serial,judge}.log` — 前后源码指纹、完整 QEMU 串口与官方 judge JSON。

**验证：**

- Docker 内严格串行 RV64 → LA64；复用 SHA-256 `c9ecca95e8bd5e8dfd1999b80bf68f83d2f74fd50468de6345fb8fef57903805` 的 `mask=0x003` config，未使用 `make run` 或 `0xFFF`，未触发 kernel/build/tools-disk。
- 两架构 `CORE_NUM=8`，serial 都有 `configured=8`、`online_mask=0xff`；四组均 START/END/`exit_code=0`，且 `run_selected_groups done`、recipe exit 0、无 kernel panic/fatal trap/deadlock/runner failure/Assert Fatal。
- RV64 raw/semantic `312/314`，仅 busybox-musl/glibc `busybox kill 10=0/1`；LA64 raw/semantic `308/314`，仅 basic-musl/glibc `test_brk=1/3` 与 busybox-musl/glibc `busybox kill 10=0/1`。两架构 `test_pipe` 都 raw `4/4`，§8.2 normalization 均为 +0。

**备注：** 前后 HEAD、tracked diff、非忽略 untracked-content digest 与 visible status 均不变，本门禁未编辑生产源码。`AI-Usage-Report.md` 已检查，本次只执行既定 gate、未使用新的 AI 工具/模型或产生需要单列的重要 AI 成果，无需更新；无新的跨任务经验需沉淀。

<a id="2026-08-04-fs-net-smp-wp4-port-registry"></a>
## WP4 per-netns PortRegistry（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes；references=none（功能实现，非故障或性能调试）。

**涉及文件：**

- `os/src/task/net_namespace.rs` — 每个 netns 持有 N0 `PortRegistry`。
- `os/src/net/socket/inet/common/port.rs`、`port/registry.rs` — reserve→bind→commit/abort、协议/netns 隔离和 key+token+Weak 精确释放。
- `os/src/net/socket/{mod.rs,inet/stream/mod.rs,inet/datagram/udp.rs}` — TCP/UDP 的内核 bind snapshot、reservation 安装及 Drop 精确 release。
- `docs/06_net/inet-common.md` — 同步事务和锁序契约。

**验证：**

- 静态审计：registry 锁只涵盖 prune/select/conflict/commit/abort；`socket.bind()`、DeviceStack 和 socket reservation 安装均在 N0 锁外；Reserved 与 Bound 均参与冲突判定。
- `git diff --check` 通过；`make` / `cargo` / QEMU 未运行（协调器明确要求串行执行）。本会话无可用 LSP diagnostics，`rustfmt` 命令不可用；双架构 build 和 `net_smp` GREEN 交由协调器。

**备注：** UDP owner 由 reservation token 加 Weak identity 精确匹配，Drop 不会删除同 bucket reuse peer；`AI-Usage-Report.md` 已检查，未使用新的 AI 工具或模型，无需更新；未发现需沉淀的跨任务调试经验。

<a id="2026-08-04-fs-net-smp-wp3-gate-pass"></a>
## WP3 §8.2 双架构 8 核初赛非回归门禁：PASS

**Mango Gate:** skill reused=yes；references=none（执行既定竞赛门禁，非故障或性能调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp3-gate-{manifest,command,verdict}.md` — 冻结输入、串行命令、硬条件和门禁判定。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp3-gate-source-fingerprint.txt` 与 `fs-net-smp-wp3-gate-{rv64,la64}-{serial,judge}.log` — 源码状态、完整 QEMU 串口与官方 judge JSON。

**验证：**

- Docker 中严格串行 RV64 → LA64；复用 SHA-256 `c9ecca95e8bd5e8dfd1999b80bf68f83d2f74fd50468de6345fb8fef57903805` 的 `mask=0x003` config，未使用 `make run` 或 `0xFFF`。
- 两架构 `CORE_NUM=8`，serial 都有 `configured=8`、`online_mask=0xff`；四组均 START/END/`exit_code=0`，且 `run_selected_groups done`、recipe exit 0、无 panic/fatal/deadlock/trap/runner failure。
- RV64 raw/semantic `312/314`，仅 busybox-musl/glibc `busybox kill 10=0/1`；LA64 raw/semantic `308/314`，仅 basic-musl/glibc `test_brk=1/3` 与 busybox-musl/glibc `busybox kill 10=0/1`。两架构 `test_pipe` 均 raw `4/4`，§8.2 normalization 均为 +0。

**备注：** 前后 HEAD 与 tracked diff digest 均不变，本门禁未编辑生产源码。证据目录为 ignored 归档产物；`AI-Usage-Report.md` 已检查，本次仅执行既定 gate、无新的 AI 工具/模型或重要 AI 辅助成果，无需更新；无新的跨任务经验可沉淀。

<a id="2026-08-04-fs-net-smp-wp3-ext4-directory-lock"></a>
## WP3 ext4 目录锁协议实现（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes；references=none（功能实现，非故障或性能调试）。

**涉及文件：**

- `os/src/fs/ext4/layout.rs` — `Ext4OSInode` 增加 FS canonical 的 `dir_gate`。
- `os/src/fs/ext4/ext4fs.rs` — 增加 `rename_gate` 与 Weak inode gate registry；为目录操作、rename victim 顺序、缓存访问与 reclaim 接入目录锁协议。
- `docs/03_fs/ext4.md`、`docs/10_plan/fs-net-smp-execution.md` — 同步 WP3 目录锁契约和 agent A 完成状态。

**验证：**

- 静态锁序审计：cross-FS check → per-FS `rename_gate` → ancestor-first/ino-order parents → directory-first/ino-order victims → children/negative/lookup caches → unlock → 延迟零链接 inode 回收；同目录 rename 只取一次 parent write gate。
- 静态 reclaim 审计：negative dentry、children stale Weak 及 budgeted prune 仅在从 registry clone Arc 后 `try_write(dir_gate)`；无路径反向获取 parent gate。
- `git diff --check` 通过；`make` / `cargo` / QEMU 未运行（协调器明确要求串行执行）。本会话没有 LSP diagnostics，`rustfmt` 命令也不可用；双架构 build、`fs_smp` 8/8 与 ext4 ktest 由协调器执行。

**备注：** 复用既有 `InodeLock` 作为 wrapper 内元数据提交标记，实际跨 wrapper 命名空间互斥由 canonical `dir_gate` 保证。无新增可跨任务复用的调试经验；`AI-Usage-Report.md` 已检查，`GPT-5.6-terra` 已列出，无需更新。

<a id="2026-08-04-fs-net-smp-wp2-gate-pass"></a>
## WP2 §8.2 双架构 8 核初赛非回归门禁：PASS

**Mango Gate:** skill reused=yes；references=none（执行既定竞赛门禁，非故障定位或性能调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp2-gate-{manifest,verdict}.md` — official derived-competition 路径、硬条件、raw/semantic 分数与失败多重集。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp2-gate-{rv64,la64}-{serial,judge,command,conf-inject,tools-disk}.log` — 双架构完整串口、judge JSON、命令展开及输入准备证据。

**验证：**

- 严格串行 RV64 → LA64；两架构均为 `CORE_NUM=8`，serial 含 `configured=8`、`online_mask=0xff`，四组均 START/END/`exit_code=0`，QEMU recipe exit 0。
- RV64 raw/semantic `312/314`；仅允许的 busybox-musl/glibc `busybox kill 10=0/1`。
- LA64 raw/semantic `308/314`；仅允许的 basic-musl/glibc `test_brk=1/3` 与 busybox-musl/glibc `busybox kill 10=0/1`。
- `test_pipe` 在两架构两个 basic 组均 raw `4/4`，§8.2 semantic normalization 增量均为 0；无 panic、fatal trap、deadlock 或 runner failure。

**备注：** 冻结 config 注入流程将 immutable 官方 x0 复制为 derived x0，因此实际运行使用 `rv64-derived-run`/`la64-derived-run` 的 competition QEMU 双盘路径，不使用缺失 development rootfs 的 `make run`。为提供 x1，执行 `tools-disk-rv/la`（未调用 kernel/build 目标）；`AI-Usage-Report.md` 无需更新。无新增可跨任务复用的调试经验。

<a id="2026-08-04-fs-net-smp-wp2-gate-input-failure"></a>
## WP2 §8.2 双架构 8 核初赛门禁：RV64 rootfs 缺失

**Mango Gate:** skill loaded=yes；references=none（执行既定门禁，非故障定位或性能调试）。

**涉及文件：**

- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp2-gate-*` — 冻结配置、源码指纹、环境/命令清单、下载原始输出和 fail-fast verdict。

**验证：**

- Docker 容器内 `make toolchain-preflight` 通过：`nightly-2026-05-10`；创建 ignored 的 `fs-img-dir` 后官方镜像下载、解压均成功。
- 按协调器修正指令，完整 build 已严格串行完成（RV64 → LA64，均 exit 0）；RV64 已完成 `mask=0x003` 配置注入。
- `CORE_NUM=8 make run ARCH=rv64 PROFILE=normal` 仍在内核启动前因缺少 `build/rv64/release/normal/image/rootfs-rv.img` 退出 2；按硬条件/fail-fast 规则未启动 LA64 gate，故没有 raw/semantic score 或非回归结论。

**备注：** 下载前后源码指纹一致；完整环境、配置 SHA-256 和失败输出已归档。没有新增可跨任务复用的调试经验，且未修改生产源码；`AI-Usage-Report.md` 无需同步。

<a id="2026-08-04-fs-net-smp-wp2-pagecache"></a>
## WP2 PageCache per-page data 锁与用户 I/O 边界（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes; references=none（功能实现，非性能/故障调试）

**涉及文件：**

- `os/src/fs/page_cache.rs` — 以 `op_gate` 取代 `io_gate`，增加 `PageEntry.data` closure 访问、固定 kernel/user I/O API，并让 writeback、truncate、失效和回收遵守新锁序。
- `os/src/fs/tmpfs/mod.rs` — 读用户缓冲区改为 PageCache bounded bounce；内核写使用 `write_kernel`，删除重复的 truncate tail zero。
- `os/src/fs/fat32/fat_inode.rs`、`os/src/fs/ext4_lwext4/layout.rs` — 迁移内核 PageCache 调用到固定 API。
- `os/src/kernel_tests/fs_smp.rs` — ktest inode 与保护性 PageCache 用例改用新 API，保留 hook 交错点。
- `docs/03_fs/page-cache.md` — 同步 `op_gate`、per-page data lock 与 bounded-bounce API/锁序。

**验证：**

- 静态核验：`PageCacheTestHook` 已位于 `write_kernel` 的 entry 获取后、页字节复制前，并且 writer 在整个 hook/copy 区间持有 `op_gate.read()`；`truncate_with_backend` 的 `op_gate.write()` 因此只能在 writer 完成后摘页，`pagecache_user_write_vs_truncate` 的 RED 交错转为串行结果。
- 静态调用点审计：除 VFS `File::read_user/write_user` 接口外，crate 内不再调用 PageCache 的旧 `read_user`、`write_user`、`with_io_gate` 或 `write_without_balance`。
- `make` / `cargo` / QEMU / LSP：**未运行**（协调器明确要求本子任务不执行构建、测试或 QEMU；本会话没有可用 LSP diagnostics 工具）。WP2 双架构编译、ktest GREEN、LTP/IOzone 与 §8.2 门禁由协调器串行完成。

**备注：** PageCache 已不保留 `io_gate`；`op_gate` 与 `entries → inner → PageEntry.data` 的单向顺序覆盖本批路径。Rust direct-map slice 的 unsafe 面仅在私有 helper 内，由 data lock 和 HRTB closure 限定；Miri 不适用于此裸机路径，且按协调器“不得 cargo”约束未执行。

<a id="2026-08-04-fs-net-smp-wp2-ext4-io-txn"></a>
## WP2 ext4 PageCache 事务边界（L3/T3，等待协调器验证）

**Mango Gate:** skill loaded=yes; references=none（功能实现，非性能/故障调试）

**涉及文件：**

- `os/src/fs/ext4/layout.rs` — 为每个 `Ext4OSInode` 加入 `io_txn`，并记录固定锁序与 uaccess 禁止条件。
- `os/src/fs/ext4/ext4fs.rs` — 内核和用户写路径收敛到 bounce 后的事务 helper：snapshot → extent 分配 → `write_kernel` → 按实际前缀提交 EOF；`resize` 使用 `truncate_with_backend`。
- `os/src/fs/ext4/file.rs` — 将 extent 回收抽为不发布 EOF 的 `truncate_inode_persistent`，供 PageCache 截断事务的 persistent 阶段调用。

**验证：**

- 静态核验 `fs_smp::pagecache_user_write_vs_truncate`：PageCache `op_gate` 会阻止 truncate 在 writer 的 entry/copy 区间摘页；ext4 额外以 `io_txn → op_gate` 串行 extent、EOF 和 truncate。
- `make` / `cargo` / QEMU：**未运行**（本子任务按协调器指令不执行；WP2 的双架构构建、ktest GREEN 与 §8.2 门禁由协调器串行完成）。
- `docs/03_fs/ext4.md`、`docs/03_fs/page-cache.md` 已检查：前者仍准确描述 ext4+PageCache 分层，后者由 PageCache owner 同步 API/锁细节；无需在本子任务改写。

**备注：** 当前 PageCache 工作树尚未落入固定 API；ext4 已按约定的 `read_kernel`、`write_kernel`、`read_at_user`、`truncate_with_backend` 适配。用户写先在 ext4 锁外复制 bounded bounce，随后复用 `write_kernel`，避免持有 `io_txn` 跨 faultable uaccess。

<a id="2026-08-04-0234-fs-net-smp-wp1-red-baseline"></a>
## 02:34 — WP1 ktest RED 基线（fs_smp/net_smp harness，L2/T2）

**Date:** 2026-08-04 02:34 +08:00
**Author:** AI:DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:fs, area:net, type:harness, risk:low]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 新增 `fs_smp`/`net_smp` 两组 ktest 与可测性 hook，双架构确认强制 RED 基线成立
（PageCache writer/truncate 竞争、IRQ poll 阻塞、UDP port 保留/释放竞争），其余用例为保护性
GREEN；WP1 = L2/T2，未触发 §8.2。RED→GREEN 交接给 WP2（PageCache）、WP4（PortRegistry）、
WP6（poll worker）。

**Files:**

- `os/src/fs/page_cache.rs` — 注入 `PageCacheTestHook`（生产 `new()=None`，零行为变化）。
- `os/src/net/config.rs` — 注入 `NetPollTestHook`（`AtomicUsize` 编码 `0=None`）。
- `os/src/kernel_tests/fs_smp.rs` — 8 用例，自建 `FsSmpMemBlock`/tmpfs fixture。
- `os/src/kernel_tests/net_smp.rs` — 10 用例，loopback/veth fixture。
- `os/src/kernel_tests/mod.rs` — 注册 `fs_smp`/`net_smp` 两组。
- `os/src/fs/mod.rs` — `pub(crate)` re-export `PageCacheTestHook`。
- `docs/10_plan/fs-net-smp-{adaptation,execution}.md` — WP1 计划与执行台账（本批次依据）。
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp1-*` — 冻结证据（manifest/verdict/fingerprint/4 条串口日志）。

**Verification:**

- Docker 双架构 `CORE_NUM=8` kernel build 串行通过（rv64 → la64；WP1 阶段已验，本次归档不重跑）。
- 双架构 `CORE_NUM=4` ktest 重跑并归档完整串口日志（本次执行）：
  - `fs_smp`：rv64/la64 均 TAP 1..8 = 7 passed / 1 failed；
    RED = `pagecache_user_write_vs_truncate`，reason 精确为
    `writer completed after truncate detached page entry`；无 panic，`[KTEST RESULT: FAIL]` 正常收尾。
  - `net_smp`：rv64/la64 均 TAP 1..10 = 7 passed / 3 failed；RED 及 reason：
    `irq_poll_is_publish_only` = `IRQ poll blocked after releasing try_lock`、
    `port_reserve_exactly_once` = `concurrent bind reserved one UDP endpoint more than once`、
    `udp_reuse_release_exact_owner` = `closing one UDP reuse owner removed the whole port bucket`；
    无 panic，正常收尾。
  - 两架构结果逐项一致；`make ktest-run` 宿主退出码恒为 0，RED 以串口判定为准。
- §8.2 初赛非回归门禁：**未运行**（WP1 无用户路径改动，首个需 §8.2 的门禁点是 WP2）。

**Notes:** 保护性 GREEN 归属：pagecache 并发 RED 由 WP2 拥有；net RED 由 WP4（port）与 WP6
（publish-only IRQ）拥有；`ext4_create_same_name_exactly_once`/`ext4_cross_rename_opposite_order`
因无 native in-memory ext4 fixture 降级为 tmpfs 保护性用例，ext4/tmpfs 目录锁 RED 由 WP3 接手。
文档同步（mango-workflow D）：`page-cache.md`/`device-stack-and-poll.md`/`architecture.md` 的
`code_paths` 命中本次改动文件，但 hook 为生产零行为变化，文档描述与源码一致，无需改动；
`fs-net-smp-execution.md` 状态清单的 evidence+Work Log 项由本条目闭环。

**Related:** `docs/10_plan/fs-net-smp-adaptation.md`（§11.1、§13）、
`docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp1-manifest.md`、
`docs/Work_Log/evidence/2026-08-04/fs-net-smp-wp1-verdict.md`

<a id="2026-08-04-0700-smp-b96-zombie-owner"></a>
## 07:00 — B96 TCB zombie 唯一 owner 收口

**Date:** 2026-08-04 07:00 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将 TCB 终态入口收紧为本 CPU 的 `Running(cpu_id()) -> Zombie`，删除
interruptible registry 中不可达的 zombie 摘取、按 PID 清理、计数和 CPU0 周期扫描。
终态 Arc 现在只有 owner CPU `local_zombies` 一个容器 owner；原 `stale_zombie` profile
阶段同步改名为实际语义 `taskq_stats`。

**Files:**

- `os/src/task/{task,manager,processor,mod}.rs` — 状态门禁、唯一回收容器和诊断阶段收口。
- task/scheduler、锁序、运行时服务与 SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b96-zombie-owner-summary.md` — 调用图与冻结证据。

**Verification:**

- DeepSeek max 只读调用图审查：无阻塞项；group-exit/exec/fatal/OOM 都不会直接
  `Blocked/New -> Zombie`，clone 发布失败直接 drop。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，141.221 s。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34，141.395 s。
- 两项均 `online_mask=0xff`、exit 0、无 panic/timeout/fatal trap、mutation false；
  unique-owner、owner-local zombie、group-exit、exec sibling stop 和 STOP 均实际 PASS。

**Notes:** DeepSeek 建议的示例若把实际 `Running(other_cpu)` 作为 CAS expected，会错误地
允许远端终止；GPT 改为显式匹配 `Running(cpu_id())`。auto-reap 在 last current 切回 idle
前只能清已入队 sibling，last current 仍由随后 idle 收尾回收。未扩张到初赛/Futex/FS/Net
门禁，因为本补丁没有修改这些路径；双架构完整 SMP suite 是更直接的 T3 证据。

**Related:** `docs/05_process/scheduler.md`、`docs/05_process/task-control-block.md`、
`docs/01_architecture/lock-order.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b96-zombie-owner-summary.md`

<a id="2026-08-04-0642-smp-b95-production-ipi"></a>
## 06:42 — B95 测试专用 IPI 协议退出生产状态

**Date:** 2026-08-04 06:42 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:hal, type:refactor, type:test, risk:interrupt]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除早期 bring-up 仅供 ktest 使用的 PING/ROUND_TRIP reason、三项
PerCpu ack/pending 字段和 AP idle deferred reply 分支。focused 用例改为直接验证生产
`MEMORY_BARRIER` request/ack，覆盖 BSP→单 AP、BSP→全部 AP、每个 AP→BSP 64 轮以及
syscall IRQ-on 窗口，不再为测试维持第二套生产协议。

**Files:**

- `os/src/smp.rs` — 删除测试专用 reason、状态、API 和 handler/idle 分支。
- `os/src/kernel_tests/smp.rs` — 用正式 membarrier sequence/ack 替换 ping/round-trip。
- 启动/trap 与 SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b95-production-ipi-summary.md` — 冻结审查和验证证据。

**Verification:**

- DeepSeek max 冻结只读审查：无阻塞项；reason 编号没有汇编、硬件或持久 ABI 依赖。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，141.806 s；新用例 #5/#6/#9 实际 PASS。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34，136.518 s；新用例 #5/#6/#9 实际 PASS。
- 两项均 `online_mask=0xff`、exit 0、无 panic/timeout/fatal trap，且
  `mutation_detected=false`。

**Notes:** AP helper 发布完成结果时仍未必离开自身 kernel stack，因此测试在释放最后
两个显式 Arc 前同时等待 `Zombie` 和目标 CPU current 槽清空。受控 IRQ 窗口结束后统一
执行正式 task safe point，避免把 timer deferred work 遗留给后续用例。普通用户默认
affinity 与 FS/Net/Driver 边界均未改变。“生产协议成熟后删除测试专用协议”的模式已沉淀到
`harness-patterns.md`。

**Related:** `docs/01_architecture/boot-and-trap.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b95-production-ipi-summary.md`

<a id="2026-08-04-0610-smp-b94-heap-trace-owner"></a>
## 06:10 — B94 heap_trace 缓冲所有权收口

**Date:** 2026-08-04 06:10 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 让 `TRACE: Mutex<TraceState>` 直接拥有 heap_trace 的 active/site
定长表，删除两个 `static mut`、锁内裸指针发布以及手写
`unsafe impl Send`。所有可变引用现在都从 mutex guard 派生；约 25.6 MiB
全零表位于 `.bss.heap_trace`，不增加镜像文件负载。

**Files:**

- `os/src/mm/heap_trace.rs` — 锁对象直接拥有数组，删除 raw pointer 初始化和 unsafe accessor。
- `docs/04_mm/initialization-and-kernel-space.md`、SMP 计划 — 同步所有权和 BSS 合同。
- `docs/Work_Log/evidence/2026-08-04/smp-b94-heap-trace-owner-summary.md` — 审查、验证和 runner 裁决。

**Verification:**

- DeepSeek 冻结只读审查：无阻塞项；确认自动 `Send`、六份 linker script
  的 `sbss..ebss` 覆盖、全零 TicketMutex 表示和 heap/trace 锁序不变。
- DeepSeek Docker RV64/LA64 `EXTRA_FEATURES=heap_trace CORE_NUM=8 KTEST=smp`：
  ✅ 均 34/34，145.360 s / 143.659 s，无 panic/timeout/mutation。
- GPT 在 Docker 中用精确 `heap_trace5TRACE$` 符号复核：双架构大对象均为
  `0x19a0048` / 26,869,832 bytes，RV64 section 5、LA64 section 2770 均是
  `NOBITS .bss`。

**Notes:** 首个 RV64 recipe 因 awk 花括号未转义在启动构建前失败；修正后
RV64 真实通过。LA64 旧配方又先命中 1-byte `TRACE_ENABLED`，且未使用
`set -e`，DeepSeek 错把 `size=1` 判为架构差异；GPT 拒绝该 ELF 结论，改用精确
符号、fail-fast 和十六进制转换后才验收。两次 QEMU 本身均是真实 PASS；
失败/误判只影响本地 ELF 判定层。FS/Net/Driver 未进入本节点。

**Related:** `docs/04_mm/initialization-and-kernel-space.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b94-heap-trace-owner-summary.md`

<a id="2026-08-04-0537-smp-b93-tlb-diagnostics"></a>
## 05:37 — B93 Per-CPU TLB shootdown 生产诊断

**Date:** 2026-08-04 05:37 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:diagnostics, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 在 TLB 同步发起 CPU 记录真正跨核的 shootdown：五种互斥后端、精准区间
页数、尝试覆盖的远端 CPU 数、端到端总/最大 raw timer ticks 和错误数。local-only flush
不计入；诊断不修改 `MmuGather/TlbFlush` 的 frame 所有权，也不进入 hard IRQ 计时。

**Files:**

- `os/src/smp.rs` — per-CPU TLB 计数、统一记录函数及 kernel/user 各退出路径。
- `os/src/panic_diag.rs` — 将类型和成本拆成两行逐 CPU 输出。
- `docs/04_mm/page-table-and-tlb.md`、`docs/10_plan/smp-8core-implementation.md` —
  记录统计口径并完成 Phase 6 对应条目。
- `docs/Work_Log/evidence/2026-08-04/smp-b93-tlb-diagnostics-summary.md` — 设计裁决、审查和冻结证据。

**Verification:**

- DeepSeek clean-tree 设计审查与冻结补丁审查：无 P0；两个所谓 P1 均为口径确认或既有
  fail-stop 行为，无需修改生产协议。
- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，135.8 s / 147.9 s。
- DeepSeek Docker RV64/LA64 `CORE_NUM=8 KTEST=smp`：✅ 均 34/34，138.4 s / 148.4 s。
- 四个 runner 均 `mutation_detected=false`、`timed_out=false`，无 panic、stale TLB、
  active-MM/generation/slot 残留或 frame 提前释放标记。

**Notes:** GPT 未采纳 per-target handler 计数、ASID rollover 专用字段、trace buffer 和生产
测试 hook；已有 request/ack、B92 IPI reason 和五类发起侧统计足以交叉定位。也未采纳
“不统计精准页数/不计 RFENCE 时间”的建议：区间长度和 RV64 固件调用端到端耗时正是性能
判断所需事实。`remote_targets` 明确表示尝试 fanout，固件错误时仍记录全部计划目标。

**Related:** `docs/04_mm/page-table-and-tlb.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b93-tlb-diagnostics-summary.md`

<a id="2026-08-04-0513-smp-b92-ipi-diagnostics"></a>
## 05:13 — B92 Per-CPU IPI 生产诊断

**Date:** 2026-08-04 05:13 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:hal, type:diagnostics, risk:interrupt]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 在不改变 mailbox、doorbell 和 request/ack 协议的前提下，为每 CPU 增加
IPI hard handler、doorbell 失败及逐 reason 发布/消费计数。发布按目标 CPU 数累计，
消费按 mailbox 实际取得的 bit 累计；同类 bit 可合并，所以二者差值不作为丢中断判据。

**Files:**

- `os/src/smp.rs` — 统一 doorbell 失败记录点，并增加固定数组形式的 IPI 诊断。
- `os/src/panic_diag.rs` — panic 快照输出 reason 顺序和每 CPU 计数。
- `docs/01_architecture/boot-and-trap.md`、`docs/10_plan/smp-8core-implementation.md` —
  记录计数口径、内存序和 Phase 6 完成项。
- `docs/Work_Log/evidence/2026-08-04/smp-b92-ipi-diagnostics-summary.md` — 审查和冻结验证证据。

**Verification:**

- DeepSeek 设计/补丁审查：无 P0/P1；GPT 将诊断计数统一为 `Relaxed`，并拒绝新增与既有
  `ipi_send_failures` 重复的 doorbell 字段。
- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，134.279 s / 149.400 s。
- DeepSeek Docker RV64/LA64 `CORE_NUM=8 KTEST=smp`：✅ 均 34/34，
  140.890 s / 150.643 s。
- 四个采纳 runner 均 `mutation_detected=false`、`timed_out=false`，无 panic、fatal trap、
  IPI/TLB 失败或 forbidden marker。

**Notes:** 首轮 RV64 build 本身 exit 0，但验证运行期间 GPT 又同步了两处文档，runner 正确
报告 `mutation_detected=true`；该轮被立即中止且未作为证据。代码和架构文档冻结后用新
job 完整重跑四项。reason 统计忽略尚未登记名字的高位，避免诊断设施把 hard IRQ 变成
新的 panic 点。普通用户 affinity 与 FS/Net/Driver 边界均未改变。

**Related:** `docs/01_architecture/boot-and-trap.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b92-ipi-diagnostics-summary.md`

<a id="2026-08-04-0438-smp-b91-scheduler-diagnostics"></a>
## 04:38 — B91 Per-CPU 调度生产诊断

**Date:** 2026-08-04 04:38 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, type:diagnostics, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 为每个 `CpuTaskState` 增加始终可用的 context switch、实际跨核
migration、成功 work steal 和 runqueue 峰值计数，并接入 panic 的逐 CPU 无锁快照。
计数不参与调度：migration 只在任务相对 `last_cpu` 真正于另一 CPU 进入 `Running`
时登记，首次运行和 queued 搬运不计；steal 只在 owner 交接成功后登记。

**Files:**

- `os/src/task/{processor,run_queue,task}.rs` — per-CPU 计数、精确记录点和快照。
- `os/src/panic_diag.rs` — 输出每核 switch/migration/steal/rq-peak。
- 调度、运行时服务、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b91-scheduler-diagnostics-summary.md` — 审查与冻结验证证据。

**Verification:**

- DeepSeek 只读补丁审查：无阻塞项，可进入双架构验证；唯一建议是无需采纳的参数收口风格项。
- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，137 s / 138 s。
- DeepSeek Docker RV64/LA64 `CORE_NUM=8 KTEST=smp`：✅ 均 34/34，137 s / 141 s。
- 四个 runner 均 `mutation_detected=false`，无 panic、fatal trap、调度状态不变量失败、
  重复 owner 或 timeout；用例内预期用户 Store/PageModify fault 不属于内核失败。

**Notes:** 首次把验证任务误交给 `cc-job.py` 的 read-only profile，worker 明确把四项标成
NOT RUN；未采纳其“可提交”文字。随后改用 `cc-agent-test.py` 的受限 Docker gateway，
把双架构 build/8 核 ktest 四项全部设为 required recipe 后重新验收。核心缺口审查未发现
新的 task/mm/hal P0/P1；`get_bytes_array()` 涉及 MM/PageCache/FS，共享子系统负责人合并
前不单独修改，普通用户默认 affinity 继续保持 CPU0-only。

**Related:** `docs/05_process/scheduler.md`、`docs/01_architecture/runtime-services.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b91-scheduler-diagnostics-summary.md`

<a id="2026-08-04-0407-smp-b90-time-source"></a>
## 04:07 — B90 时间源全局可变状态收口

**Date:** 2026-08-04 04:07 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:hal, area:timer, type:cleanup, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除 `timer.rs` 中从未有生产读者的 `TIME_SOURCE static mut`、
`TimeSource/init_time_source()` 和只为该注册表存在的 RISC-V `MTime` 硬编码
旁路。架构无关时间读取本来就使用 HAL `get_time()/get_clock_freq()`；现在
代码结构与这个单一生产数据流一致，realtime 只保留原子 offset。

**Files:**

- `os/src/timer.rs` — 删除不可达的无同步时钟源注册表和 MTIME 实现。
- `docs/01_architecture/hal-and-platform.md`、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b90-time-source-summary.md` — 可达性与冻结构建证据。

**Verification:**

- 全仓生产源码无 `TimeSource/init_time_source/MTime/TIME_SOURCE` 调用。
- DeepSeek Docker RV64 normal build：✅ exit 0，133.798 s。
- DeepSeek Docker LA64 normal build：✅ exit 0，137.631 s。
- 基线 HEAD `de128346ebe0828668de781d5bb3a7e748002246`，tracked diff SHA-256
  `fa8c3620cb52808126b2a3b86f2d8fd14a89af49d6536a30ddb9afd3e4b3d3ca`；两项
  `mutation_detected=false`，无禁止标记。

**Notes:** 该变更不修改运行时计时算法、timer IRQ、deadline 或 CPU owner，是不可达
旧状态的 T1 收口。因此按自适应门禁只做双架构编译和指纹检查，不重复
B89 已通过的双架构 8 核 SMP 长测。FS/Net/Driver 未进入本批。

**Related:** `docs/01_architecture/hal-and-platform.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b90-time-source-summary.md`

<a id="2026-08-04-0356-smp-b89-frame-lock"></a>
## 03:56 — B89 单页帧分配锁外清零

**Date:** 2026-08-04 03:56 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:refactor, type:performance, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将普通 `frame_alloc()` 拆为“全局锁内唯一领取 PPN”和“锁外
清零/发布 tracker”两阶段。新增的私有 `FrameReservation` 持有未发布 PPN；
`reserve_one()` 只更新 recycled/fresh 元数据，`into_tracker()` 按需清零 4 KiB 并在
构造 `Arc` 前完成 owner 移交。OOM/非 OOM 路径都用显式局部变量保证锁
guard 在清零前已释放。

**Files:**

- `os/src/mm/frame_allocator.rs` — 单页 reservation、锁外清零与回滚 owner。
- 帧分配、锁序、测试、SMP 计划与 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b89-frame-lock-summary.md` — 设计裁决与冻结验证证据。

**Verification:**

- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，严格串行。
- DeepSeek Docker RV64/LA64 `CORE_NUM=8 KTEST=smp`：✅ 均 34/34。
- 基线 HEAD `6ecbec2ec65e3e45c1425388b895e934f3779d89`，tracked source diff
  SHA-256 `879b8ceab969239e098eeb3d1aeec293362cd4bed7dad2f9627032f45a8d7c72`；
  四项 `mutation_detected=false`，无 panic、timeout、fatal、double free 或重复回收标记。

**Notes:** DeepSeek 首份设计示例在 `into_tracker()` 中未解除 reservation Drop，会把已
发布 tracker 的 PPN 再次归还；GPT 拒绝该示例并用 `Option<PhysPageNum>` 解除。
补丁审查又指出 `expect()` 因 `PhysPageNum: Copy` 存在易被后续 panic 点放大的双
owner 窗口；最终改为 `take()` 先转移所有权。模型对 perf 失败计数和
`Arc::new()` 计时范围的疑问由 GPT 用旧 `alloc()` 源码核验，两者均保持原口径。
连续帧和显式 uninit 分配未改；FS/Net/Driver 未进入本批。

**Related:** `docs/04_mm/frame-allocator.md`、`docs/01_architecture/lock-order.md`、
`docs/08_testing/README.md`、`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b89-frame-lock-summary.md`

<a id="2026-08-04-0325-smp-b88-frame-zero"></a>
## 03:25 — B88 帧清零 raw pointer 边界收口

**Date:** 2026-08-04 03:25 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除只被 `FrameTracker::new()` 使用、却从安全函数返回
`&'static mut [u64]` 的 `PhysPageNum::get_dwords_array()`。帧清零现在从刚被 allocator 唯一
领取的 PPN 局部建立 raw direct-map pointer；既有 8×u64 手工展开、尾部循环、清零范围和
perf 计时均保持不变，没有将性能敏感路径改写为 `write_bytes`。

**Files:**

- `os/src/mm/address.rs` — 删除通用 `'static mut [u64]` helper。
- `os/src/mm/frame_allocator.rs` — 在唯一 owner 的清零窗口内局部建立 raw pointer。
- frame allocator、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b88-frame-zero-summary.md` — 冻结验证证据。

**Verification:**

- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，无新增 warning。
- DeepSeek Docker RV64/LA64 8 核 SMP：✅ 均 34/34。
- 基线 HEAD `289b37fe72f2e6194a8ac1166bf5bc78c3bef38c`，tracked source diff
  SHA-256 `c44c38628f4d14c5da2576bcb721aa23d143a4cbc64ed65d265145ce5ee95330`；
  四项 `mutation_detected=false`，无 panic、timeout 或 fatal trap。

**Notes:** allocator 在写锁内先从 recycled 栈摘除并清 owner bit，或推进 fresh cursor；
因此清零开始时其他 CPU 已不可能再领取同一 PPN。B88 只收口引用边界，尚未缩短 allocator
锁覆盖清零的临界区；该 SMP 性能问题留给带独立设计审查的后续节点。

**Related:** `docs/04_mm/frame-allocator.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b88-frame-zero-summary.md`

<a id="2026-08-04-0311-smp-b87-trap-context"></a>
## 03:11 — B87 trap context 直映射借用收口

**Date:** 2026-08-04 03:11 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:task, area:hal, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除 `PhysAddr` 上四个无人使用却可安全返回 `'static` 引用的通用 direct-map
helper，以及只由 trap context 使用的 `PhysPageNum::get_mut()`。现在只有持有
`&mut TaskControlBlockInner` 的 `trap_context_mut()` 能从 raw direct-map pointer 建立
`&mut TrapContext`，返回生命周期由 `task.inner` guard 约束。trap frame 的 PPN、地址、布局、
字段和双架构汇编 ABI 均未改变。

**Files:**

- `os/src/mm/address.rs` — 删除五个无法表达所有权的通用引用 helper。
- `os/src/task/task.rs` — 在 TCB owner 内局部建立 trap context 可变引用并写明安全证明。
- trap、锁序、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b87-trap-context-summary.md` — 冻结验证证据。

**Verification:**

- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，严格串行。
- DeepSeek Docker RV64/LA64 8 核 SMP：✅ 均 34/34。
- 基线 HEAD `8381657818f19e441a60988166de3dc1c8d411e3`，tracked source diff
  SHA-256 `3e30ccb16e587c7d4de2dca14a0ceac1684610936bebc65b23f151522b8420b5`；
  四项 `mutation_detected=false`，无 panic、timeout 或 fatal trap。

**Notes:** B87 没有新增测试 hook 或临时字段。`get_bytes_array()` 涉及 MM/PageCache/FS 的
共享所有权，本节点刻意不顺带修改；只处理调用域完整、能独立证明的 trap context 边界。

**Related:** `docs/01_architecture/boot-and-trap.md`、`docs/01_architecture/lock-order.md`、
`docs/08_testing/README.md`、`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b87-trap-context-summary.md`

<a id="2026-08-04-0254-smp-b86-pte-borrow"></a>
## 02:54 — B86 页表可变借用边界收口

**Date:** 2026-08-04 02:54 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将 `PhysPageNum` 的原始 PTE 视图从“安全函数返回 `'static mut`”收口为
crate-private unsafe 只读/可写接口；双架构只读 walker 不再借道可变 walker，所有 PTE
writer 和 `block_and_ret_mut*()` 必须持有 `&mut PageTable`。外层 `AddressSpace` VM 锁仍
负责跨 CPU 串行化，Rust 可变借用则在页表底层阻止共享引用制造别名。PTE 位、TLB 失效
范围、generation 和 frame retirement 语义均未改变。

**Files:**

- `os/src/mm/address.rs`、`os/src/mm/page_table.rs` — raw PTE view 与 trait 独占合同。
- `os/src/hal/arch/riscv/sv39.rs`、`os/src/hal/arch/loongarch64/laflex.rs` — 双架构读写
  walker 分离；LA64 在 invalidate 前复制返回 PPN，缩短可变借用。
- MM、锁序、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b86-pte-borrow-summary.md` — 冻结验证证据。

**Verification:**

- 首轮任务因 GPT 在提交验证后又删除重复注释，wrapper 检出 tracked diff 指纹变化并正确
  判为 FAILED；该轮不作为最终验收证据。
- 第二轮 RV64 normal build 通过；LA64 编译 RED 暴露 Rust 2018 数组迭代得到引用，以及
  可变 PTE 借用跨越 `invalidate_page()` 两个问题。按根因修复后主动终止该轮剩余测试。
- 最终冻结轮：DeepSeek Docker LA64 normal build ✅；RV64/LA64 8 核 SMP 均 ✅ 34/34。
- 最终 tracked source diff SHA-256 为
  `8fa1b0b0b8f0e1084fe99d5b581b71a4d928d5d1648e7a98caed2090f395fc9e`，三项门禁
  `mutation_detected=false`，无 panic、timeout 或 fatal trap。

**Notes:** 本节点没有加入临时测试、诊断字段或生产 hook。B84 的真实权限降级和 B85 的
并发生产 writer 已覆盖 PTE/TLB 运行语义；本节点的新增门禁主要由编译器借用检查承担。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/01_architecture/lock-order.md`、
`docs/08_testing/README.md`、`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b86-pte-borrow-summary.md`

<a id="2026-08-04-0224-smp-b85-concurrent-pte"></a>
## 02:24 — B85 真实并发 PTE writer 与 shootdown 隔离

**Date:** 2026-08-04 02:24 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除仅直接调用 TLB 同步原语的并发用例，改由全部在线 CPU 共享同一
`AddressSpace`，每核在独立的常驻 `MAP_SHARED` 页上经生产
`AddressSpace::write -> mprotect -> MmuGather/TlbFlush` 路径交替执行 8 轮 R/RW
权限切换。VM 锁保证 PTE 修改线性化，锁外 flush 则允许多个 generation 和固定 shootdown
slot 真实交错；所有 writer 在共同完成屏障前保持 MM active，确保每次修改都必须面向完整
8 核 mask。

**Files:**

- `os/src/kernel_tests/smp.rs` — 生产 PTE writer 并发门禁、IRQ-open 完成屏障和收尾断言。
- MM、测试和 SMP 实施计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b85-concurrent-pte-summary.md` — 冻结验证证据。

**Verification:**

- DeepSeek Docker 双架构 normal build：✅ RV64/LA64 退出码均为 0，严格串行执行。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，目标用例为 `ok 25`。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34，目标用例为 `ok 25`。
- 两架构均无 panic、timeout、fatal trap、active-MM bit 遗留、generation 落后或
  range-to-full fallback；目标用例后的 #26—#34 全部通过。
- 冻结 HEAD `f83d6c349198742d7748dba7b14a911a6a1f0f28`，tracked diff SHA-256
  `b90377b86a59af7564827170d528d771938f0cf3380a88b80f6903e9cca3af7b`，
  `mutation_detected=false`。

**Notes:** 页必须使用 `MAP_SHARED`，否则私有匿名映射的 CoW 初始写保护会让 mprotect
无法直接表达本用例需要的 R/RW PTE 切换。完成等待用 `with_local_interrupts_enabled()`，
否则某 CPU 等待其他 writer 时可能屏蔽本地 IPI，反过来阻塞其他 CPU 正在等待的 TLB ack。
本节点没有增加生产测试钩子或临时计数器；普通用户默认全核 affinity 仍受共享子系统门禁约束。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b85-concurrent-pte-summary.md`

<a id="2026-08-04-0200-smp-b84-mprotect"></a>
## 02:00 — B84 真实远端 mprotect 降权与 LA64 W/D 修复

**Date:** 2026-08-04 02:00 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, type:fix, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将 B82 的 CPU1 用户 stale-TLB 探针延伸到真实
`mprotect(RW -> R)`：旧 RW 映射先完成一次 store，CPU0 经生产
`AddressSpace::write -> MmuGather/TlbFlush` 降权并收齐远端 ack 后才放行第二次 store；
它必须触发 SIGSEGV 且 frame canary 不变。LA64 首轮稳定 RED 由此定位到页表权限位错误，
而不是 `INVTLB` payload：`revoke_write()` 只清 page-walk W、没有清进入 TLB 并约束 store
的 D。底层原语改为同步清 W/D，并删除两个调用点的重复清 D。

**Files:**

- `os/src/hal/arch/loongarch64/laflex.rs` — 统一 LA64 写保护的 W/D 不变量。
- `os/src/kernel_tests/smp.rs` — CoW、同 VPN remap、mprotect 三阶段真实用户探针。
- MM、测试和 SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b84-mprotect-summary.md` — 官方依据、RED→GREEN 与冻结证据。

**Verification:**

- LoongArch 官方卷一：✅ W 只供 page walk 使用，TLB EntryLo 以 D 触发 store 修改异常；
  `INVTLB 0x5` 删除完整的 non-global ASID+VA 项。
- Linux v6.6 LoongArch `pte_wrprotect()`：✅ 同时清 `_PAGE_WRITE | _PAGE_DIRTY`。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，远端 store 以 StorePageFault/SIGSEGV 结束。
- LA64 修复前冻结 RED：✅ 33/34，唯一失败为 store 绕过 mprotect，证明测试能捕获缺陷。
- DeepSeek Docker 双架构 normal build：✅；LA64 修复后 8 核 SMP：✅ 34/34，出现预期
  PageModifyFault/SIGSEGV，无全用户 TLB fallback。
- LA64 8 核初赛 `mask=0x003`：✅ 308/314，精确失败集合与既有基线一致。

**Notes:** 本节点没有为 mprotect 新建第二套 shootdown 或架构 fallback；修复位于所有 LA64
撤销写权限路径共享的最底层 PTE 原语。下一 MM 节点改用真实并发 PTE writer 压测多发起者
slot/generation；普通用户默认全核 affinity 仍受队友负责的共享子系统门禁约束。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b84-mprotect-summary.md`

<a id="2026-08-04-0112-smp-b83-exec-inactive"></a>
## 01:12 — B83 exec inactive ack 与 noreturn Arc 生命周期

**Date:** 2026-08-04 01:12 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, type:fix, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 非 leader exec 不再把“用户资源已清理”等同于“已离开 CPU”。
live token 继续作为 MM/TLB 资源 ack；idle 收尾在撤销 current 槽后另行发布
inactive ack，全部 sibling inactive 后才允许 owner 安装新 MM 并交换 TID。同时修复
`run_task_safe_point()` 进入 noreturn 退出前没有释放 current `Arc`、令旧 leader TCB
永久滞留在废弃内核栈上的真实根因。

**Files:**

- `os/src/task/{task,process,manager}.rs` — inactive 状态、exec pending/Completion 与 noreturn Arc 释放。
- `os/src/kernel_tests/smp.rs` — 拆分 exec 四个失败阶段，并验证旧 leader inactive 后的延迟析构。
- task/process/lock-order/SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b83-exec-inactive-summary.md` — 根因、协作裁决和冻结证据。

**Verification:**

- Linux v6.6 `fs/exec.c::de_thread()` 对照：✅ 旧 leader inactive 后才交换身份。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，根因用例由 5 秒超时变为 PASS。
- DeepSeek Docker LA64 normal build：✅ exit 0。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34。
- 两架构最终运行均无 panic/timeout/fatal trap/stale-TLB marker，mutation false。

**Notes:** live token 和 inactive ack 不能再合并：前者允许释放用户 MM 资源，
后者允许处理与旧 TCB/内核栈生命周期相关的身份交换。临时诊断打印已删除。
本节点形成的 noreturn Arc 规则已写入 lock-order 文档，无需再重复写入
debugging references。

**Related:** `docs/01_architecture/lock-order.md`、`docs/05_process/process-control-block.md`、
`docs/05_process/task-process-scheduler.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b83-exec-inactive-summary.md`

<a id="2026-08-04-0012-smp-b82-user-remap-tlb"></a>
## 00:12 — B82 真实用户 CoW 与同 VPN 重映射 TLB 证明

**Date:** 2026-08-04 00:12 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, area:timer, type:test, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 扩展 B53 的真实用户 stale-TLB 探针：CPU1 在同一 VPN 上依次观察旧页、CoW
页和 `munmap + MAP_FIXED_NOREPLACE` 重映射页；CPU0 两次修改均走生产
`AddressSpace::write -> MmuGather/TlbFlush -> range shootdown`，不新增生产测试钩子。
首轮 LA64 暴露 `quiesce_local_timer_interrupt()` 只清 TICLR pending、未停止尚未到期倒计时；
按 LoongArch 官方 `TCFG.En` 定义改为先停计数再清 pending，并修正寄存器 wrapper 的反向读取。

**Files:**

- `os/src/kernel_tests/smp.rs` — 双阶段真实用户 load 探针、第三页生命周期和失败清理。
- `os/src/hal/arch/loongarch64/{time.rs,register/timer/tcfg.rs}` — timer 真正静默与 En 读值修正。
- SMP、trap/timer、TLB 与测试文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b82-user-remap-tlb-summary.md` — 冻结证据。

**Verification:**

- LoongArch 官方架构手册 V1.10 §7.6.2/§7.6.5 — ✅ `TCFG.En=1` 才倒计时，TICLR 仅清
  timer interrupt flag；据此拒绝仅清 pending 的伪静默。
- DeepSeek max 只读并发审查 — ✅ 同一 VM 写事务内先撤 PTE、安装新 PPN，锁外 shootdown
  收齐 ack 后才释放退休 frame；GPT 复核补上模型遗漏的 remap frame 超时保活。
- 首轮 Docker 双架构 8 核 SMP — RV64 新用例 PASS；LA64 用户探针成功但 timer 隔离后验失败，
  定位为 TCFG 仍倒计时；两架构另有既有 exec identity 5 秒边界失败。
- timer 修复后 Docker 双架构 `CORE_NUM=8 KTEST=smp KREPEAT=1` — ✅ 新用例均 PASS、
  `online_mask=0xff`、无 panic/fatal trap；LA64 不再出现 timer 隔离错误。
- 完整套件两架构均为 33/34：唯一失败仍是
  `smp::exec_owner_becomes_group_leader` 在 5002/5003ms 超时，作为下一 task 节点处理；因此
  本节点定向状态为 pass，完整 SMP suite 状态为 partial，不把进程退出码 0 误记为全量 PASS。
- Docker `make lint` — ❌ warning baseline stale；同时报告 5 个新增 `unknown` 与 5 个已消失
  `unknown`，跨 RNG/ext4/user_mapper/smp/time 等既有文件，未重录全仓 baseline。随后
  `cargo fmt --check` 也显示 develop 工作树存在大范围既有格式漂移；本提交不借机格式化队友模块。
- 冻结 HEAD `e5a013f375a79621dc33ef13c79351e481922c3e`，tracked diff SHA-256
  `852fcea09b5caf1b2ae646e0aae21252345bcf6371f2bd711085242a020d0a35`，两架构
  `mutation_detected=false`。

**Notes:** 单次 `AddressSpace::write()` 让旧 TLB 在锁内修改期间仍可合法读取旧页，而解锁后的
同步返回保证下一次用户 load 必须看到新 PPN；退休 frame 在 ack 前由 gather 持有。该证据覆盖
CoW 和 unmap/remap，不覆盖 `mprotect` 降权后的 store fault，也不解除普通用户默认 CPU0 限制。
本节点没有形成超出现有 debugging/harness references 的新通用模式；AI Usage Report 已增加
B82 证据行。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/01_architecture/boot-and-trap.md`、
`docs/08_testing/README.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b82-user-remap-tlb-summary.md`
