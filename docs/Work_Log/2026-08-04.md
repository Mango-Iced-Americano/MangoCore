<a id="2026-08-04-0311-smp-b87-trap-context"></a>
## 03:11 — B87 trap context 直映射借用收口

**Date:** 2026-08-04 03:11 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:task, area:hal, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除 `PhysAddr` 上四个无人使用却可安全返回 `'static` 引用的通用 direct-map
helper，以及只由 trap context 使用的 `PhysPageNum::get_mut()`。现在只有持有
`&mut TaskControlBlockInner` 的 `trap_context_mut()` 能从 raw direct-map pointer 建立
`&mut TrapContext`，返回生命周期由 `task.inner` guard 约束。trap frame 的 PPN、地址、布局、
字段和双架构汇编 ABI 均未改变。

**Files:**

- `os/src/mm/address.rs` — 删除五个无法表达所有权的通用引用 helper。
- `os/src/task/task.rs` — 在 TCB owner 内局部建立 trap context 可变引用并写明安全证明。
- trap、锁序、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b87-trap-context-summary.md` — 冻结验证证据。

**Verification:**

- DeepSeek Docker RV64/LA64 normal build：✅ exit 0，严格串行。
- DeepSeek Docker RV64/LA64 8 核 SMP：✅ 均 34/34。
- 基线 HEAD `8381657818f19e441a60988166de3dc1c8d411e3`，tracked source diff
  SHA-256 `3e30ccb16e587c7d4de2dca14a0ceac1684610936bebc65b23f151522b8420b5`；
  四项 `mutation_detected=false`，无 panic、timeout 或 fatal trap。

**Notes:** B87 没有新增测试 hook 或临时字段。`get_bytes_array()` 涉及 MM/PageCache/FS 的
共享所有权，本节点刻意不顺带修改；只处理调用域完整、能独立证明的 trap context 边界。

**Related:** `docs/01_architecture/boot-and-trap.md`、`docs/01_architecture/lock-order.md`、
`docs/08_testing/README.md`、`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b87-trap-context-summary.md`

<a id="2026-08-04-0254-smp-b86-pte-borrow"></a>
## 02:54 — B86 页表可变借用边界收口

**Date:** 2026-08-04 02:54 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, type:refactor, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将 `PhysPageNum` 的原始 PTE 视图从“安全函数返回 `'static mut`”收口为
crate-private unsafe 只读/可写接口；双架构只读 walker 不再借道可变 walker，所有 PTE
writer 和 `block_and_ret_mut*()` 必须持有 `&mut PageTable`。外层 `AddressSpace` VM 锁仍
负责跨 CPU 串行化，Rust 可变借用则在页表底层阻止共享引用制造别名。PTE 位、TLB 失效
范围、generation 和 frame retirement 语义均未改变。

**Files:**

- `os/src/mm/address.rs`、`os/src/mm/page_table.rs` — raw PTE view 与 trait 独占合同。
- `os/src/hal/arch/riscv/sv39.rs`、`os/src/hal/arch/loongarch64/laflex.rs` — 双架构读写
  walker 分离；LA64 在 invalidate 前复制返回 PPN，缩短可变借用。
- MM、锁序、测试、SMP 计划和 AI 使用文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b86-pte-borrow-summary.md` — 冻结验证证据。

**Verification:**

- 首轮任务因 GPT 在提交验证后又删除重复注释，wrapper 检出 tracked diff 指纹变化并正确
  判为 FAILED；该轮不作为最终验收证据。
- 第二轮 RV64 normal build 通过；LA64 编译 RED 暴露 Rust 2018 数组迭代得到引用，以及
  可变 PTE 借用跨越 `invalidate_page()` 两个问题。按根因修复后主动终止该轮剩余测试。
- 最终冻结轮：DeepSeek Docker LA64 normal build ✅；RV64/LA64 8 核 SMP 均 ✅ 34/34。
- 最终 tracked source diff SHA-256 为
  `8fa1b0b0b8f0e1084fe99d5b581b71a4d928d5d1648e7a98caed2090f395fc9e`，三项门禁
  `mutation_detected=false`，无 panic、timeout 或 fatal trap。

**Notes:** 本节点没有加入临时测试、诊断字段或生产 hook。B84 的真实权限降级和 B85 的
并发生产 writer 已覆盖 PTE/TLB 运行语义；本节点的新增门禁主要由编译器借用检查承担。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/01_architecture/lock-order.md`、
`docs/08_testing/README.md`、`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b86-pte-borrow-summary.md`

<a id="2026-08-04-0224-smp-b85-concurrent-pte"></a>
## 02:24 — B85 真实并发 PTE writer 与 shootdown 隔离

**Date:** 2026-08-04 02:24 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 删除仅直接调用 TLB 同步原语的并发用例，改由全部在线 CPU 共享同一
`AddressSpace`，每核在独立的常驻 `MAP_SHARED` 页上经生产
`AddressSpace::write -> mprotect -> MmuGather/TlbFlush` 路径交替执行 8 轮 R/RW
权限切换。VM 锁保证 PTE 修改线性化，锁外 flush 则允许多个 generation 和固定 shootdown
slot 真实交错；所有 writer 在共同完成屏障前保持 MM active，确保每次修改都必须面向完整
8 核 mask。

**Files:**

- `os/src/kernel_tests/smp.rs` — 生产 PTE writer 并发门禁、IRQ-open 完成屏障和收尾断言。
- MM、测试和 SMP 实施计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b85-concurrent-pte-summary.md` — 冻结验证证据。

**Verification:**

- DeepSeek Docker 双架构 normal build：✅ RV64/LA64 退出码均为 0，严格串行执行。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，目标用例为 `ok 25`。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34，目标用例为 `ok 25`。
- 两架构均无 panic、timeout、fatal trap、active-MM bit 遗留、generation 落后或
  range-to-full fallback；目标用例后的 #26—#34 全部通过。
- 冻结 HEAD `f83d6c349198742d7748dba7b14a911a6a1f0f28`，tracked diff SHA-256
  `b90377b86a59af7564827170d528d771938f0cf3380a88b80f6903e9cca3af7b`，
  `mutation_detected=false`。

**Notes:** 页必须使用 `MAP_SHARED`，否则私有匿名映射的 CoW 初始写保护会让 mprotect
无法直接表达本用例需要的 R/RW PTE 切换。完成等待用 `with_local_interrupts_enabled()`，
否则某 CPU 等待其他 writer 时可能屏蔽本地 IPI，反过来阻塞其他 CPU 正在等待的 TLB ack。
本节点没有增加生产测试钩子或临时计数器；普通用户默认全核 affinity 仍受共享子系统门禁约束。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b85-concurrent-pte-summary.md`

<a id="2026-08-04-0200-smp-b84-mprotect"></a>
## 02:00 — B84 真实远端 mprotect 降权与 LA64 W/D 修复

**Date:** 2026-08-04 02:00 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, type:fix, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 将 B82 的 CPU1 用户 stale-TLB 探针延伸到真实
`mprotect(RW -> R)`：旧 RW 映射先完成一次 store，CPU0 经生产
`AddressSpace::write -> MmuGather/TlbFlush` 降权并收齐远端 ack 后才放行第二次 store；
它必须触发 SIGSEGV 且 frame canary 不变。LA64 首轮稳定 RED 由此定位到页表权限位错误，
而不是 `INVTLB` payload：`revoke_write()` 只清 page-walk W、没有清进入 TLB 并约束 store
的 D。底层原语改为同步清 W/D，并删除两个调用点的重复清 D。

**Files:**

- `os/src/hal/arch/loongarch64/laflex.rs` — 统一 LA64 写保护的 W/D 不变量。
- `os/src/kernel_tests/smp.rs` — CoW、同 VPN remap、mprotect 三阶段真实用户探针。
- MM、测试和 SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b84-mprotect-summary.md` — 官方依据、RED→GREEN 与冻结证据。

**Verification:**

- LoongArch 官方卷一：✅ W 只供 page walk 使用，TLB EntryLo 以 D 触发 store 修改异常；
  `INVTLB 0x5` 删除完整的 non-global ASID+VA 项。
- Linux v6.6 LoongArch `pte_wrprotect()`：✅ 同时清 `_PAGE_WRITE | _PAGE_DIRTY`。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，远端 store 以 StorePageFault/SIGSEGV 结束。
- LA64 修复前冻结 RED：✅ 33/34，唯一失败为 store 绕过 mprotect，证明测试能捕获缺陷。
- DeepSeek Docker 双架构 normal build：✅；LA64 修复后 8 核 SMP：✅ 34/34，出现预期
  PageModifyFault/SIGSEGV，无全用户 TLB fallback。
- LA64 8 核初赛 `mask=0x003`：✅ 308/314，精确失败集合与既有基线一致。

**Notes:** 本节点没有为 mprotect 新建第二套 shootdown 或架构 fallback；修复位于所有 LA64
撤销写权限路径共享的最底层 PTE 原语。下一 MM 节点改用真实并发 PTE writer 压测多发起者
slot/generation；普通用户默认全核 affinity 仍受队友负责的共享子系统门禁约束。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/08_testing/README.md`、
`docs/10_plan/smp-8core-implementation.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b84-mprotect-summary.md`

<a id="2026-08-04-0112-smp-b83-exec-inactive"></a>
## 01:12 — B83 exec inactive ack 与 noreturn Arc 生命周期

**Date:** 2026-08-04 01:12 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, type:fix, type:test, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 非 leader exec 不再把“用户资源已清理”等同于“已离开 CPU”。
live token 继续作为 MM/TLB 资源 ack；idle 收尾在撤销 current 槽后另行发布
inactive ack，全部 sibling inactive 后才允许 owner 安装新 MM 并交换 TID。同时修复
`run_task_safe_point()` 进入 noreturn 退出前没有释放 current `Arc`、令旧 leader TCB
永久滞留在废弃内核栈上的真实根因。

**Files:**

- `os/src/task/{task,process,manager}.rs` — inactive 状态、exec pending/Completion 与 noreturn Arc 释放。
- `os/src/kernel_tests/smp.rs` — 拆分 exec 四个失败阶段，并验证旧 leader inactive 后的延迟析构。
- task/process/lock-order/SMP 计划文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b83-exec-inactive-summary.md` — 根因、协作裁决和冻结证据。

**Verification:**

- Linux v6.6 `fs/exec.c::de_thread()` 对照：✅ 旧 leader inactive 后才交换身份。
- DeepSeek Docker RV64 8 核 SMP：✅ 34/34，根因用例由 5 秒超时变为 PASS。
- DeepSeek Docker LA64 normal build：✅ exit 0。
- DeepSeek Docker LA64 8 核 SMP：✅ 34/34。
- 两架构最终运行均无 panic/timeout/fatal trap/stale-TLB marker，mutation false。

**Notes:** live token 和 inactive ack 不能再合并：前者允许释放用户 MM 资源，
后者允许处理与旧 TCB/内核栈生命周期相关的身份交换。临时诊断打印已删除。
本节点形成的 noreturn Arc 规则已写入 lock-order 文档，无需再重复写入
debugging references。

**Related:** `docs/01_architecture/lock-order.md`、`docs/05_process/process-control-block.md`、
`docs/05_process/task-process-scheduler.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b83-exec-inactive-summary.md`

<a id="2026-08-04-0012-smp-b82-user-remap-tlb"></a>
## 00:12 — B82 真实用户 CoW 与同 VPN 重映射 TLB 证明

**Date:** 2026-08-04 00:12 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:mm, area:hal, area:timer, type:test, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 扩展 B53 的真实用户 stale-TLB 探针：CPU1 在同一 VPN 上依次观察旧页、CoW
页和 `munmap + MAP_FIXED_NOREPLACE` 重映射页；CPU0 两次修改均走生产
`AddressSpace::write -> MmuGather/TlbFlush -> range shootdown`，不新增生产测试钩子。
首轮 LA64 暴露 `quiesce_local_timer_interrupt()` 只清 TICLR pending、未停止尚未到期倒计时；
按 LoongArch 官方 `TCFG.En` 定义改为先停计数再清 pending，并修正寄存器 wrapper 的反向读取。

**Files:**

- `os/src/kernel_tests/smp.rs` — 双阶段真实用户 load 探针、第三页生命周期和失败清理。
- `os/src/hal/arch/loongarch64/{time.rs,register/timer/tcfg.rs}` — timer 真正静默与 En 读值修正。
- SMP、trap/timer、TLB 与测试文档同步。
- `docs/Work_Log/evidence/2026-08-04/smp-b82-user-remap-tlb-summary.md` — 冻结证据。

**Verification:**

- LoongArch 官方架构手册 V1.10 §7.6.2/§7.6.5 — ✅ `TCFG.En=1` 才倒计时，TICLR 仅清
  timer interrupt flag；据此拒绝仅清 pending 的伪静默。
- DeepSeek max 只读并发审查 — ✅ 同一 VM 写事务内先撤 PTE、安装新 PPN，锁外 shootdown
  收齐 ack 后才释放退休 frame；GPT 复核补上模型遗漏的 remap frame 超时保活。
- 首轮 Docker 双架构 8 核 SMP — RV64 新用例 PASS；LA64 用户探针成功但 timer 隔离后验失败，
  定位为 TCFG 仍倒计时；两架构另有既有 exec identity 5 秒边界失败。
- timer 修复后 Docker 双架构 `CORE_NUM=8 KTEST=smp KREPEAT=1` — ✅ 新用例均 PASS、
  `online_mask=0xff`、无 panic/fatal trap；LA64 不再出现 timer 隔离错误。
- 完整套件两架构均为 33/34：唯一失败仍是
  `smp::exec_owner_becomes_group_leader` 在 5002/5003ms 超时，作为下一 task 节点处理；因此
  本节点定向状态为 pass，完整 SMP suite 状态为 partial，不把进程退出码 0 误记为全量 PASS。
- Docker `make lint` — ❌ warning baseline stale；同时报告 5 个新增 `unknown` 与 5 个已消失
  `unknown`，跨 RNG/ext4/user_mapper/smp/time 等既有文件，未重录全仓 baseline。随后
  `cargo fmt --check` 也显示 develop 工作树存在大范围既有格式漂移；本提交不借机格式化队友模块。
- 冻结 HEAD `e5a013f375a79621dc33ef13c79351e481922c3e`，tracked diff SHA-256
  `852fcea09b5caf1b2ae646e0aae21252345bcf6371f2bd711085242a020d0a35`，两架构
  `mutation_detected=false`。

**Notes:** 单次 `AddressSpace::write()` 让旧 TLB 在锁内修改期间仍可合法读取旧页，而解锁后的
同步返回保证下一次用户 load 必须看到新 PPN；退休 frame 在 ack 前由 gather 持有。该证据覆盖
CoW 和 unmap/remap，不覆盖 `mprotect` 降权后的 store fault，也不解除普通用户默认 CPU0 限制。
本节点没有形成超出现有 debugging/harness references 的新通用模式；AI Usage Report 已增加
B82 证据行。

**Related:** `docs/04_mm/page-table-and-tlb.md`、`docs/01_architecture/boot-and-trap.md`、
`docs/08_testing/README.md`、
`docs/Work_Log/evidence/2026-08-04/smp-b82-user-remap-tlb-summary.md`
