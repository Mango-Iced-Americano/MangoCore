---
date: 2026-07-19
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log：2026-07-19

<a id="2026-07-19-another-ext4-wave-2-read-only-bridge"></a>
## Wave 2：another_ext4 只读 VFS bridge

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-terra
**Tags:** [area:fs, type:feature, type:correctness]
**Mango Gate:** skill loaded=yes; references=none（非调试/性能任务）

**Summary:** 接入 feature-gated `ext4_another` VFS bridge。挂载只接受 `load_read_only_checked` 验证的介质，未实现运行时回退；普通文件经按文件系统实例与 inode 编号隔离的 Mango PageCache 读取。所有 VFS 写入、变更操作及 writable/`O_TRUNC` 打开均稳定返回 `EROFS`。复核时还将 `read_at` 的 fd 私有数据锁在 PageCache/块 I/O 前显式释放，避免跨等待点持锁。

**Files:**
- `os/src/fs/ext4_another/{blockdev.rs,errno.rs,fs.rs,inode.rs,mutations.rs,page_cache.rs,mod.rs}`：只读适配器、错误映射、FileSystem/IndexNode、PageCache backend 和变更拒绝宏。
- `os/src/fs/{mod.rs,ext4_backend.rs}`：feature-gated bridge 注册及 another backend 路由。
- `os/src/kernel_tests/{ext4_another.rs,mod.rs}`：feature-gated 无效介质、只读读取与 `EROFS` ktest 注册。
- `docs/03_fs/{README.md,init-and-rootfs.md}`：同步只读、无回退和 PageCache 语义。

**Verification:**
- Docker 串行 `make -C os rv64-kernel-build-only EXT4_BACKEND=another` ✅。
- Docker 串行 `make -C os la64-kernel-build-only EXT4_BACKEND=another` ✅。
- Docker `rustfmt --check --edition 2018`（`ext4_another/inode.rs`、`kernel_tests/ext4_another.rs`）✅；`git diff --check` ✅。
- 未执行 QEMU（任务明确禁止）。

**Evidence:** `docs/Work_Log/evidence/2026-07-19/another-ext4-wave2-build.md`

**Notes:** 未添加 writable API、journal recovery、fsync/syncfs 行为或运行时 fallback；未发现需要追加到 references/ 的新通用模式。

<a id="2026-07-19-wave-1-integration-validation"></a>
## Wave 1 集成验证：后端 facade 与可恢复 BlockDevice 合并

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-terra
**Tags:** [area:fs, area:drivers, type:correctness, type:validation]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 合并后验证发现，后端选择将 `ext4_lwext4` 正确 feature-gate 后，lwext4 专用 ktest 与 sysfs 诊断仍无条件引用该模块；同时非法 feature 组合会使 facade 的多个 `Result` block 同时参与类型检查，给 `compile_error!` 拒绝路径附加 E0308。仅对这些选择边界作 cfg 收口：非 lwext4 不注册/读取 lw 专用测试与诊断，facade 仅在精确单选时编译对应分支，非法选择保留 `ENODEV` 类型占位并只报已文档化的 compile error。

**Files:**
- `os/src/fs/ext4_backend.rs`：精确单选 cfg，令 zero/mixed feature guard 只产生预期 `compile_error!`。
- `os/src/fs/sysfs/files/diag.rs`：lwext4 指标访问仅在 lwext4 backend 下编译；其它后端的专用诊断返回 unavailable。
- `os/src/kernel_tests/ext4.rs`：lwext4 专用 ktest 仅在 lwext4 backend 下注册与编译。

**Verification:**
- Docker 串行 `make -C os rv64-kernel-build-only EXT4_BACKEND=lwext4` ✅。
- Docker 串行 `make -C os la64-kernel-build-only EXT4_BACKEND=lwext4` ✅。
- Docker `make -C os rv64-kernel-build-only EXT4_BACKEND=legacy` ✅（修复前为 4 个 E0433）。
- Docker `make -C os rv64-kernel-build-only EXT4_BACKEND=another` ✅；candidate facade 保持 `ENODEV`。
- Docker `cargo check --no-default-features` zero/mixed backend 选择 ✅：各自只报文档化的 `compile_error!`，无 E0308/E*。
- `sh scripts/test_check_another_ext4_isolation.sh`、`sh scripts/check_another_ext4_isolation.sh`、`sh -n` ✅。
- 未执行 QEMU 或测试套件（任务明确禁止）。

**Evidence:** `docs/Work_Log/evidence/2026-07-19/wave1-integration-validation.md`

**Notes:** 未修改运行时 adapter、可写/journal/PageCache 路径、默认 backend 或工作负载配置；没有新增可复用 reference，现有 AGENTS.md 已覆盖 Docker 串行双架构与 feature 边界约束。

<a id="2026-07-19-wave-1-task-2-block-device-fallibility"></a>
## Wave 1 Task 2：BlockDevice 可恢复 I/O 错误与真实 flush 能力

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-terra
**Tags:** [area:drivers, area:fs, type:correctness, type:validation]
**Mango Gate:** skill loaded=yes; references=none（非调试/性能任务）

**Summary:** 将 Mango `BlockDevice` 的读写与 flush 收敛为可恢复的 `Result` 契约，并以设备实际能力决定 flush 是否可用。分区设备完整转发底层读、写、flush 的错误与 capability；直接需要 fatal 行为的既有调用点显式保留 `.expect(...)`，而 errno 边界将设备失败稳定映射为 `EIO` 或 `BackingStoreFailure`。不引入 another_ext4 adapter、VFS 路由或 PageCache 结构性重构。

**Files:**
- `os/src/drivers/block/{block_dev.rs,mod.rs,partition.rs}`：新增 `BlockDeviceError`/`BlockDeviceResult`、fallible read/write/flush 合约、可靠 flush capability、分区范围校验和错误转发。
- `os/src/drivers/block/{virtio_blk.rs,virtio_blk_pci.rs,mem_blk.rs,sata_blk.rs}`、`dependency/virtio-drivers/src/device/blk.rs`：移除可恢复驱动 panic，传播驱动错误，并按 virtio feature 提供真实 flush 能力。
- `os/src/fs/{dev/block.rs,ext4/**,ext4_lwext4/blockdev.rs,fat32/bitmap.rs,filesystem.rs,mod.rs,page_cache.rs,swap.rs}`：在各层显式处理或保留既有致命语义；lwext4 不支持的 flush 返回 `-EOPNOTSUPP`，其余设备错误映射为 I/O 失败。
- `os/src/kernel_tests/{block_device.rs,mod.rs}`：新增分区读写、flush 错误传播和不支持 flush 的编译期内核测试。

**Verification:**
- Docker 串行 `make -C os -B rv64-kernel-build-only` ✅。
- Docker 串行 `make -C os -B la64-kernel-build-only` ✅。
- Docker `rustfmt --check --edition 2018`（全部本任务改动 Rust 文件）✅。
- 未执行 QEMU（任务明确禁止）。新内核测试已包含在两次内核编译中，但未作运行时断言。

**Notes:** 未发现须追加到 references/ 的跨模块新模式；本项目既有 AGENTS.md 已覆盖 errno、Docker 串行双架构构建和可恢复错误边界规则。工作区其他 another_ext4 基线改动未触碰。

<a id="2026-07-19-ext4-backend-exactly-one"></a>
## Wave 1 Task 1：ext4 后端编译期唯一选择与统一挂载 facade

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-terra
**Tags:** [area:fs, type:build, type:validation]
**Mango Gate:** skill loaded=yes; references=none（非调试/性能任务）

**Summary:** 将 ext4 后端选择收敛为编译期 feature 合约：默认 lwext4；legacy 与 another 只能显式选择；零选或混选均由 `compile_error!` 拒绝。启动、`mount_block_fs` 和 `sys_mount` 统一经 `ext4_backend::open()` 路由。another 仍未接入 VFS，选择后返回 `ENODEV`，不发生运行时驱动切换或回退。

**Files:**
- `os/Cargo.toml`、`os/make/ext4_backend.mk`、`os/{Makefile,make/rv64.mk,make/la64.mk}`：定义三个互斥 feature、默认 lwext4 和 `EXT4_BACKEND` 映射；仅 lwext4 选择构建 C 库。
- `os/src/fs/{mod.rs,ext4_backend.rs}`、`os/src/syscall/fs/sys_mount.rs`：新增 shared facade 并收口启动、块设备和 syscall 挂载入口。
- `scripts/{check_another_ext4_isolation.sh,test_check_another_ext4_isolation.sh}`：fixture 覆盖 zero/mixed/bypass RED 与有效选择 GREEN。
- `docs/03_fs/{init-and-rootfs.md,README.md}`：同步 facade 和构建选择边界。

**Verification:**
- `sh scripts/test_check_another_ext4_isolation.sh` ✅（zero、mixed、bypass 均 RED；valid selection GREEN）。
- `sh scripts/check_another_ext4_isolation.sh` ✅。
- Docker 串行 `make -C os rv64-kernel-build-only MODE=release LOG=warn` ❌：与 la64 相同，已有未跟踪的 `os/src/kernel_tests/block_device.rs` 依赖不存在的 BlockDevice API；本任务 facade 不再有独立编译错误。
- Docker 串行 `make -C os la64-kernel-build-only MODE=release LOG=warn` ❌：已有未跟踪的 `os/src/kernel_tests/block_device.rs` 依赖不存在的 `BlockDeviceError`、`flush`、`supports_reliable_flush` API；本任务禁止变更 BlockDevice/测试工作负载，未越界修复。
- 未执行 QEMU（按任务要求）。

**Notes:** 未沉淀 reference；本次只新增编译期选择与路由收口，不实现 VFS adapter、可写支持、BlockDevice 或 PageCache 改动。

<a id="2026-07-19-another-ext4-phase-1-remediation-2"></a>
## Phase 1 another_ext4 remediation-2 最终编译证据与文档校正

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-luna
**Tags:** [area:fs, type:validation, type:documentation]
**Mango Gate:** skill loaded=yes; references=none（仅文档与证据记录更新）

**Summary:** 按最终 Mango pin `6887c41ef212b483a6841c87cb4d4b025b8d2c1b` 更新 Phase 1 文档。remediation-2 已通过 isolation gate，并按 RV64 后 LA64 串行完成四次 Docker 编译，早期失败作为已修复诊断保留。

**Evidence:**
- 证据目录：`docs/Work_Log/evidence/2026-07-19/another-ext4-phase1-build/`。
- fork lineage：`dragonos` 到 `sync` 的 subtree split 为 `571b85084fade21f5c26726a78e71356210c4f86`；Mango `mango` 分支最终 pin 为 `6887c41ef212b483a6841c87cb4d4b025b8d2c1b`。
- isolation gate：`remediation-2-isolation-gate.log`，exit 0。
- 四次最终编译均 exit 0：`remediation-2-rv64-default.log`、`remediation-2-rv64-feature-on.log`、`remediation-2-la64-default.log`、`remediation-2-la64-feature-on.log`。汇总见 `remediation-2-result-status.txt`。
- `remediation-artifact-verification.txt` 确认早期失败日志仍被保留为 retained diagnostics，不能再作为最终状态判定。

**Verification boundary:** 本次证据只证明 Docker 串行 compile isolation。没有执行 QEMU、another_ext4 runtime activation、语义验证、baseline runner 或性能测量，因此不形成 runtime、性能或 baseline 结论。`scripts/run_lwext4_baseline.sh` 仍为 NO-GO。

**Files:**
- `docs/10_plan/another-ext4-migration.md`：更新最终 pin、fork lineage、remediation-2 四次通过及编译与运行时边界。
- `docs/10_plan/another-ext4-baseline.md`：引用证据路径，区分 compile isolation 与 runtime validation，保留 baseline runner NO-GO。
- `docs/Work_Log/2026-07-19.md`：记录最终证据、历史失败诊断和验证边界。

<a id="2026-07-19-another-ext4-phase-1-validation"></a>
## Phase 1 another_ext4 source/build 隔离验证与文档同步

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-terra
**Tags:** [area:fs, type:validation, type:documentation]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md — 静态 gate 假阴性排查

**Summary:** 完成 non-commit Phase 1 的静态隔离检查和文档校正；范围只包括已发布 fork、固定 gitlink、可选非默认 Cargo feature 与既有默认 feature 构建结果的记录。检查发现 isolation script 的调用签名字面量断言已陈旧，故本次不能合法宣称 GREEN。

**Files:**
- `docs/10_plan/another-ext4-migration.md`：以已发布远端、`mango` 分支和完整 pin 更新供应链与 Phase 1 完成边界。
- `docs/10_plan/another-ext4-baseline.md`：记录 Wave 3 默认 feature 构建结果，继续排除 QEMU、运行时路由和性能结论。
- `docs/Work_Log/2026-07-19.md`：记录本次静态门禁与范围。

**Observed Phase 1 status:**
- 早期条目记录的已发布远端为 `git@github.com:Mango-Iced-Americano/another_ext4.git`、`mango` 分支和 `03a5ee5cde8d1bf54d38b542dc42e66e978f56ae`；该值已由 remediation-2 最终 pin `6887c41ef212b483a6841c87cb4d4b025b8d2c1b` supersede，不再作为最终状态。
- Wave 3 的默认 feature 构建记录已由 remediation-2 的 isolation gate 和四次最终 Docker 编译 supersede；本条仍保留为历史执行记录，不能替代最终证据目录中的四份 remediation-2 日志。
- Phase 1 的实现范围仅为 source/build 隔离；没有 QEMU、没有 another_ext4 runtime ext4 route，也没有启动 lwext4 或显式 mount 路径变化。实际启动调用为 lwext4 的 `open_ext4rs(crate::drivers::BLOCK_DEVICE.clone())`，显式 mount 为旧 ext4 的 `open_ext4rs(blk_dev.clone())`，且未发现 `ext4_another` 路由。
- isolation script 对无参数 `open_ext4rs()` 的精确字面量检查与上述实际调用不匹配，输出 `another_ext4 isolation gate: default startup path no longer selects lwext4`；在禁止修改脚本和源码的本任务范围内，不能将此假阴性绕过为 GREEN。
- `scripts/run_lwext4_baseline.sh` 仍是 Oracle NO-GO 草案，未获准产出 baseline 或性能结论。

**Verification:**
- `scripts/check_another_ext4_isolation.sh`：❌ 输出 `another_ext4 isolation gate: default startup path no longer selects lwext4`；根因是无参数字面量断言与当前带 `BlockDevice` 参数的 lwext4 调用不匹配。
- `sh -n scripts/check_another_ext4_isolation.sh`：✅。
- `git diff --check`：✅；staged submodule diff：✅，显示 `.gitmodules` 的发布 SSH URL/`mango` 分支和 `03a5ee5cde8d1bf54d38b542dc42e66e978f56ae` gitlink。

**Notes:** 未创建 commit，未改动 Rust/VFS/PageCache/mount/startup、Cargo manifest/lockfile、`.gitmodules`、CI、submodule checkout 或脚本；没有发现需要沉淀到 references/ 的新通用模式。

<a id="2026-07-19-another-ext4-phase-0-docs"></a>
## Phase 0 another_ext4 迁移与基线文档

**Date:** 2026-07-19 +08:00
**Author:** AI:gpt-5.6-luna
**Tags:** [area:fs, type:design, type:baseline]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md#渐进性能退化调试方法论, debugging-patterns.md#性能问题

**Summary:** 记录 Mango 后端映射、another_ext4 来源决策、Phase 0 迁移门禁和新的基线证据协议。

**Files:**
- `docs/10_plan/another-ext4-migration.md`：新增迁移边界、来源供应链、缓存身份、I/O 契约、九阶段门禁、A/B 策略和性能决策规则。
- `docs/10_plan/another-ext4-baseline.md`：新增历史归档引用、基线 manifest、重复样本、环境控制、指标和证据归档协议。
- `scripts/check_another_ext4_isolation.sh`：新增只读 Phase 1 gate，验证未来固定 submodule、非默认 feature 和 lwext4 路由不变量。
- `scripts/run_lwext4_baseline.sh`：新增 Docker-only 串行 RV64→LA64 基线 runner，保护原始配置/镜像并归档可用诊断状态。
- `docs/Work_Log/2026-07-19.md`：记录本次文档设计活动。

**Observed mapping and baseline activity:**
- 确认仓库现有计划文档位置是 `docs/10_plan`，未创建 `docs/plan/`。
- 记录当前后端映射：启动路径使用 lwext4，显式 mount 仍存在旧 ext4 分歧。
- 查阅 `testresult/archive_20260709_164909` 中的历史 QEMU 命令、FS-test、lmbench 和 IOZone 摘要，并在基线文档中标记为仅供参考，不作为当前验证。
- 记录该归档缺少当前 commit、container、镜像 hash、重复运行信息及 BlockDevice/PageCache counters，且历史 sysfs lwext4 counters 返回 ENOENT。
- 初始设计阶段曾因本地 GitHub 凭据问题无法查询远端；现已由 Phase 1 发布事实替代：远端为 `git@github.com:Mango-Iced-Americano/another_ext4.git`，`mango` 分支 pin 为 `03a5ee5cde8d1bf54d38b542dc42e66e978f56ae`。
- 在 `/tmp/opencode` 建立临时本地 fork 验证 DragonOS subtree 提取：`45931ee3b3e66892533563f73023021a83f89b2d` 两次得到 `571b85084fade21f5c26726a78e71356210c4f86`，同步 clone 未导入 tags；临时路径不作为 MangoCore submodule。
- `scripts/check_another_ext4_isolation.sh` 经 `sh -n` 通过，并在当前未配置 `.gitmodules` 时按预期 RED；未用临时本地 URL 伪造 submodule。
- `scripts/run_lwext4_baseline.sh` 经 `sh -n` 通过；未执行 Docker、Make、QEMU 或真实 baseline，因此没有新增性能结论或证据目录。
- Oracle 对 baseline runner 的只读审查结论为 NO-GO：需先补齐失败门禁、Docker 互斥、`disk.img`/`disk-la.img` 副本保护、正式 evidence 字段与重复样本协议；未执行 QEMU。
- 初始设计阶段观察到的账号可见性限制不再代表当前供应链状态；组织 fork 的 `mango` 分支现已发布，并以 `03a5ee5cde8d1bf54d38b542dc42e66e978f56ae` 固定。

**Verification:**
- 文档内容和路径审阅：✅
- Rust、Cargo、Makefile、子模块和 backend source：⏭ 未修改；isolation gate shell script 已新增但未改变构建或运行时路由。
- Rust、Cargo、Makefile、子模块和 backend source：⏭ 未修改；baseline runner 仅新增测试编排，不改变后端路由。
- 初始设计活动未执行 Builds/QEMU/runtime activation/performance validation；后续 Wave 3 已在 Docker 中串行成功完成 RV64、LA64 默认 feature build，但没有 QEMU 或 runtime activation。
- Fork publication/submodule pin：后续 Phase 1 已发布 `git@github.com:Mango-Iced-Americano/another_ext4.git` 的 `mango` 分支，并固定 gitlink 为 `03a5ee5cde8d1bf54d38b542dc42e66e978f56ae`。

**Notes:** 初始条目只记录设计活动；后续 Phase 1 已完成发布 fork、固定 pin 与 Wave 3 Docker 默认 feature 双架构构建记录，但仍不宣称 QEMU 运行、runtime ext4 route、功能语义或性能验证。baseline runner 仍是未验收 NO-GO 草案；没有发现需要沉淀到 references/ 的新通用模式。

**Related:** `docs/10_plan/another-ext4-migration.md`; `docs/10_plan/another-ext4-baseline.md`
