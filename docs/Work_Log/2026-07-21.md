## 2026-07-21

### another_ext4 registered bridge prepare-failure evidence

**涉及文件：**
- `os/src/kernel_tests/ext4_another/recording_device.rs` — 测试专用注入器同时覆盖 Mango `write_block` 和 `write_blocks`。
- `os/src/kernel_tests/ext4_another/prepare_failure_bridge.rs` — 经 `Ext4FileSystem → IndexNode::write_at → PageCache` 注入一次失败，retry 断言真实 extent query、sync/readback 成功。
- `os/src/kernel_tests/ext4_another/mod.rs` — 注册 focused KTest。

**验证：** 私有 LA64 RED 记录了 injector coverage 缺口；GREEN `ext4_another_prepare` 7/7 PASS。证据见 `evidence/2026-07-21/another-ext4-bridge-failure-la64/`。

### another_ext4 prepare cache：epoch-token publication 与事务集中失效

**涉及文件：**
- `dependency/another_ext4/src/ext4/prepared_extent.rs` — probe 交付 epoch token；publication 仅 token 未变时成功，否则返回 rejection。
- `dependency/another_ext4/src/ext4/low_level.rs` — prepare miss 的 extent I/O 后仅接受同 token publication；rejection 丢弃结果并在同 inode guard 内重新 probe/query。
- `dependency/another_ext4/src/ext4/journal.rs` — 所有成功 transaction commit 的 cache publication 后统一递增 prepare-cache epoch，覆盖 direct-range、reclaim/truncate 与 recovery 事务。
- `dependency/another_ext4/src/ext4/{extent,mod}.rs` — 增加非诊断设备也可观察的 extent-query-attempt 计数，供 failure/retry bridge 证明真实查询发生。
- `os/src/kernel_tests/ext4_another/{prepare_cache,prepare_perf,mod}.rs` — RED→GREEN token-retry、failure/retry query、direct-range cache interaction KTests；性能门禁允许每个实际 allocation 的有界 token retry。

**验证：**
- 私有 LA64 RED：`ext4_another_prepare` 4/5 PASS，失败断言为 `probe miss did not reject publication after its mapping transaction changed epoch`。
- 私有 LA64 GREEN：`ext4_another_prepare` 6/6 PASS；包含 mapped cache、token retry、失败 retry 与 direct-range transaction invalidation。
- 私有 LA64 GREEN：`ext4_another_perf` 1/1 PASS；64 次 1KiB append：`calls=32`、`extent_query_calls=65`、`allocation_calls=16`，仍远低于每 write prepare；remount 数据校验通过。
- Docker 串行 RV64 与 LA64 kernel-only build ✅。

**备注：** 完整 serial、命令、状态、容器映射和 source fingerprint 位于 `evidence/2026-07-21/another-ext4-epoch-token-la64/`。未改默认配置、canonical image/shared disk，也未执行 Git。

### another_ext4 正向 mapped-extent prepare cache（epoch 失效）

**涉及文件：**
- `dependency/another_ext4/src/ext4/prepared_extent.rs` — 新增固定 64 槽、仅正向成功映射的 advisory cache；entry 绑定共享 metadata-mutation epoch，cache mutex 只覆盖 probe/publication。
- `dependency/another_ext4/src/ext4/{mod,low_level,rw}.rs` — prepare 在同 inode mutation guard 内 probe→extent I/O→epoch-validated publication；所有成功 inode 持久化与 direct-range 分配递增 epoch，writeback 仍通过 `write_data_only()` 重查真实映射。
- `os/src/kernel_tests/ext4_another/{mod,prepare_cache,prepare_perf}.rs` — 新增 mapped-hit、resize invalidation、hole、失败 prepare/retry、fsync/remount bridge KTests；1KiB 诊断将 extent-query 降低作为硬门禁。

**验证：**
- Docker 私有 LA64 RED bridge KTest：`mapped buffered prepare re-queried the extent tree`，预期失败；完整日志在 `evidence/2026-07-21/another-ext4-prepare-cache-red-la64/red-test-retry-qemu-output.log`。
- Docker 私有 LA64 GREEN `ext4_another_prepare`：5/5 PASS，覆盖 mapped hit、resize invalidation、稀疏 hole、失败后 retry 与现有 partial-allocation recovery。
- Docker 私有 LA64 `ext4_another_perf`：PASS 且 clean remount 数据校验通过。配对同 workload 指标：`extent_query_calls 97→49`，`extent_query_cycles 209416→134987`；allocation 仍为 16，未引入 eager/delayed allocation。
- Docker 串行 `make rv64-kernel-build-only EXT4_BACKEND=another EXTRA_FEATURES=perf_diag` ✅；随后 `make la64-kernel-build-only EXT4_BACKEND=another EXTRA_FEATURES=perf_diag` ✅。

**备注：** 这是 focused KTest 因果指标下降，不是 iozone/LTP 或 baseline parity 声明。私有镜像、容器映射、完整串口日志与命令状态在上述 evidence 目录；任务禁止 Git，故 `git-hash.txt` 明确记录未收集。`docs/` YAML `code_paths` 仅命中本 Work_Log；AI-Usage-Report 已列当前模型，无需更新。

### another_ext4 LA64 iozone 诊断 delta 运行证据

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-21/la64-another-diag-gatefix-source.conf` — 仅 `mode=run`、`mask=0x010` 的 A/B 输入；runner 私有复制后注入 run identity 与诊断开关。

**验证：**
- Docker 私有 LA64/another 运行确认 `[ext4] backend: another_ext4`，并在 musl、glibc 两组均输出 `reason=before`、4 条非零 `reason=interval` 及 `reason=timeout` 的 `[ext4-ab] diag-delta`。
- 代表性 musl interval：`prep_calls=28672`、`inode_read_calls=116538`、`extent_query_calls=37644`、`allocation_calls=2058`、`inode_persist_calls=31027`、`lock_hold_calls=57377`、`prepare_failures=0`。
- 两个 iozone 子进程均由 guest 480 秒预算终止；外层工具在 25 分钟总预算终止 runner，因此 `result-status.txt` 为 `outcome=fail`。修复后的 runner cleanup 记录 `owner_state=group_absent`，没有遗留受管 QEMU 进程组。

**备注：** 这是 guest 诊断格式、运行期计数和 timeout 输出的有效证据，不是 benchmark 成功、A/B parity 或 workload-success 证据。完整日志与 fail-closed 状态位于 `docs/Work_Log/evidence/2026-07-21/ext4-backend-ab-la64-another-la64-another-diag-gatefix-rerun/`；无需更新 AI-Usage-Report 或额外 reference。

### ext4 A/B runner dash 进程组清理兼容性

**涉及文件：**
- `scripts/run_ext4_backend_ab.sh` — 将受身份验证的负 PGID `kill` 从 bash 专用的 `kill ... -- -PGID` 改为 dash 可接受的 `kill ... -PGID`，覆盖 TERM 与 KILL 回退路径。

**验证：**
- RED：`setsid dash -c 'kill -0 -- -$$'` → `dash: kill: Illegal number: -`，与 LA64 A/B 清理日志一致。
- GREEN：`bash -n scripts/run_ext4_backend_ab.sh`、`setsid dash -c 'kill -0 -$$'` ✅；`bash scripts/run_ext4_backend_ab.sh --self-test` ✅。
- 先前超时运行的 owner PID/PGID/session/starttime 与记录一致；已仅按该记录终止其进程组，清理命令退出 0。该运行在启动阶段被外层 720 秒工具预算终止，未到达 iozone，不能作为 `diag-delta` 或 benchmark 结果。

**备注：** 失败运行与完整启动日志保留在 `docs/Work_Log/evidence/2026-07-21/ext4-backend-ab-la64-another-la64-another-diag-gatefix/`；其 `outcome=running` 是旧 cleanup 缺陷造成的历史状态，不是 PASS。`docs/` 无该脚本的 YAML `code_paths` 绑定；AI-Usage-Report 已列出当前模型。此为跨容器 `/bin/sh` 兼容性模式，已在本日志记录，无需重复沉淀 reference。

### another_ext4 prepare 阶段诊断 KTest 运行期门控

**涉及文件：**
- `os/src/kernel_tests/ext4_another/prepare_perf.rs` — KTest 期间用 RAII 临时启用 `STATS_ON`，并在成功、错误或提前返回时恢复原值；生产诊断仍仅由 initproc 的 `stats_on` 控制。

**验证：**
- Docker 串行 `make rv64-kernel-build-only EXT4_BACKEND=another EXTRA_FEATURES=perf_diag` ✅。
- Docker 串行 `make la64-kernel-build-only EXT4_BACKEND=another EXTRA_FEATURES=perf_diag` ✅。
- Docker LA64 `KTEST=ext4_another_perf KTIMEOUT_MS=60000 make la64-ktest EXT4_BACKEND=another EXTRA_FEATURES=perf_diag` ✅：`[KTEST RESULT: PASS]`；阶段计数非零（inode read=69、extent query=97、allocation=16、inode persist=35、lock hold=65）。完整串口日志与命令退出状态见 `docs/Work_Log/evidence/2026-07-21/la64-ext4-another-perf-ktest.{log,command}`。

**备注：** 修复了 KTest 在运行时门控关闭时错误断言新增阶段计数的测试夹具问题；未修改 another_ext4 分配、PageCache 或诊断生产启停算法。`docs/` 中无本次源码路径的 YAML `code_paths` 绑定；AI-Usage-Report 已列出当前模型，无需更新；该模式仅限此 KTest 夹具，不沉淀通用 reference。

### ext4 A/B iozone guest 完成标记（fail-closed）

**涉及文件：**
- `scripts/run_ext4_backend_ab.sh` — 仅接受 `mode=run`、仅 iozone 的 `mask=0x010` A/B 配置；将已在 runner 校验的 run ID/backend 追加到每次运行私有的 `config.txt`，拒绝源配置预置保留身份字段。
- `user/src/bin/initproc.rs` — 严格解析 `ext4_ab_run_id` 和白名单 backend；汇总每个实际脚本的最终退出结果。仅当所有组均可验证、失败数为零且至少一个 iozone libc 运行成功时，恰好输出一次 guest workload-success 标记。

**验证：**
- `bash -n scripts/run_ext4_backend_ab.sh` + `bash scripts/run_ext4_backend_ab.sh --self-test` ✅：验证私有 guest 配置身份注入；`failures=1`、`perf_samples=0` 和缺失 marker 均被 runner 拒绝。
- Docker 串行 `make rv64-kernel-build-only && make la64-kernel-build-only` ✅。
- Docker LA64 私有镜像 iozone 运行：musl 在既有 480 秒组超时后被终止，未输出 workload-success marker ✅ fail-closed；完整日志、配置、命令、退出状态、容器挂载与新鲜度记录见 `docs/Work_Log/evidence/2026-07-21/ext4-ab-protocol-la64-guest-marker-20260721/`。

**备注：** 此次 QEMU 运行不是性能成功或 benchmark parity 证据；因任务禁止 Git，失败证据目录刻意未写入 Git 元数据。`docs/` 中没有本次路径的 YAML `code_paths` 绑定，AI-Usage-Report 已包含当前模型；无需沉淀新的通用 reference。

### ext4 backend A/B Oracle P0/P1 runner 阻断门禁

**涉及文件：**
- `scripts/run_ext4_backend_ab.sh` — 证据目录和容器私有镜像目录均改为 token 绑定的原子独占 claim；拒绝复用，初始原子写入 `outcome=running`，最终状态仅接受同一 token 替换，避免陈旧 PASS。
- `scripts/run_ext4_backend_ab.sh` — owner record 增加 container ID、PID/starttime、PGID/session 和随机 token；清理验证身份并枚举/终止整个独占 session/process group，leader 已退出但 child 残留、owner 缺失/不匹配或任何成员残留都会 fail-closed，且不会全局匹配 QEMU。
- `scripts/run_ext4_backend_ab.sh` — 限制 run ID、timeout 和 feature 值的字符集，记录完整未跟踪文件列表与内容 SHA-256；新增 guest workload-success marker 门禁和确定性 dummy-process 自检。

**验证：**
- `bash -n scripts/run_ext4_backend_ab.sh` ✅。
- `bash scripts/run_ext4_backend_ab.sh --self-test` ✅：覆盖目录并发/reuse 拒绝、陈旧 PASS token 拒绝、leader 退出后 child 存活的 process-group 清理、缺失 guest marker、QEMU 状态/marker/hash 缺失和非零状态。
- 未运行 Docker、QEMU 或构建。

**备注：** 当前 guest **没有**可验证的 `[ext4-ab] workload-success run_id=<id> backend=<backend> failures=0 perf_samples=<positive>` 标记；现有 `[ltprunner] done` 不含 backend/run identity，且 LTP runner 即使失败也返回 0。runner 因此会写入 `guest-completion-blocker.txt` 并拒绝 PASS；补齐 guest 协议前不得进行 benchmark 验收。`docs/` 的 `code_paths` 未引用本脚本，AI-Usage-Report 已包含当前模型。

### ext4 backend A/B runner QEMU 完整性门禁与源码指纹

**涉及文件：**
- `scripts/run_ext4_backend_ab.sh` — 将 QEMU 包装到每次运行独有的容器 session/process group，并以 run ID、PID、starttime 验证身份；清理只终止该受管进程组，进程仍存活、身份不匹配或曾需清理均使结果失败。
- `scripts/run_ext4_backend_ab.sh` — `result-status.txt` 仅在原子完成全部门禁后写入 `outcome=pass`；外层 QEMU 状态、内层状态、completion marker、QEMU 摘录和两份 post-run hash 任一缺失/非零均失败，收到 HUP/INT/TERM 同样失败。
- `scripts/run_ext4_backend_ab.sh` — 增加 root `describe/status/diff` checksum、递归 submodule HEAD/status/diff checksum 及对应命令退出状态证据；增加无 Docker/QEMU 的 `--self-test` fixture 覆盖完整性失败路径。

**验证：**
- `bash -n scripts/run_ext4_backend_ab.sh` ✅。
- `bash scripts/run_ext4_backend_ab.sh --self-test` ✅：缺失/非零 QEMU status、缺失 completion marker、两份 post-run hash 缺失和 hash 记录未完成均被判为完整性失败；完整 fixture 通过。
- 未运行 Docker、构建或 QEMU（本次仅允许脚本级确定性验证）。

**备注：** `docs/` 的 `code_paths` 未引用该脚本；`AI-Usage-Report.md` 已包含 `openai/gpt-5.6-terra`，无需更新。未修改默认 backend、测试配置或磁盘镜像。

### another_ext4 1KiB buffered append eager missing-run batching（已撤销）

**涉及文件：**
- `dependency/another_ext4/src/ext4/{journal,low_level}.rs` — 删除小于一个文件系统块写入的 eager missing-run 扩展与 Journal 范围提交，使小写恢复 legacy prepare 路径；保留 checked range 和 partial-allocation `i_blocks` 恢复。
- `os/src/kernel_tests/ext4_another/{prepare,prepare_perf,mod}.rs` — 删除 eager 专用 KTest；perf KTest 保留 stale cleanup、有限统计输出及 clean remount 数据校验，但不再将统计值作为性能阈值。

**验证：**
- Docker LA64 `make -f make/la64.mk ktest-run ... mango.test=ext4_another_prepare ... EXTRA_FEATURES=perf_diag` ✅：3/3 PASS（范围 `EFBIG`、legacy partial-allocation `i_blocks` 恢复及 stats bridge）。
- Docker LA64 `make -f make/la64.mk ktest-run ... mango.test=ext4_another_perf ... EXTRA_FEATURES=perf_diag` ✅：恢复路径记录 `calls=64`、`requested=64`、`mapped=48`、`missing=16`、`metadata_io=172`；这些值仅用于诊断。
- RV64/LA64 kernel-only 构建：待本次撤销后重新执行。

**备注：** eager 实现已在 LA64 private-image iozone 初始写入中回归（约 `214 KB/s`，此前约 `340 KB/s`），且其 `metadata_io` 不是完整元数据成本的可靠代理；因此 `metadata_io=24` 的原性能结论被拒绝并撤销。历史命令、容器和 QEMU 摘录仍保留在 `docs/Work_Log/evidence/2026-07-21/another-ext4-eager-range-*`，仅供追溯，不构成当前验收证据。未修改默认 backend、测试配置或共享磁盘镜像。

### another_ext4 buffered prepare 范围校验与 partial allocation `i_blocks` 恢复

**涉及文件：**
- `dependency/another_ext4/src/ext4/{mod,low_level,data_write}.rs` — 统一 checked byte→logical range 解析；删除不能证明无锁/无 inode I/O 的 direct-range H1 计数；legacy 分配错误在已开始分配时重算并持久化 `i_blocks`，持久化恢复失败则 fail-stop 并返回恢复错误。
- `os/src/kernel_tests/ext4_another/{prepare,recording_device,mod}.rs` — 以一次性 legacy 写失败注入覆盖 `EFBIG`、原始分配错误保留、`i_blocks` 恢复与独立 clean remount 持久化。

**验证：**
- Docker RV64 `make rv64-kernel-build-only` ✅（2026-07-21，容器 `/app` 挂载自 `/home/pxy/projects/MangoCore`）。
- Docker LA64 `make la64-kernel-build-only` ✅。
- Docker LA64 private-image `make la64-ktest KTEST=ext4_another_prepare KTIMEOUT_MS=30000` ✅：2/2 PASS（范围 `EFBIG` 与 partial-allocation remount 恢复）。
- Docker RV64 private-image `make rv64-ktest KTEST=ext4_another_prepare KTIMEOUT_MS=30000` 在第一个测试前超时；其副本源于受外部 QEMU 占用的共享盘，已清理，不作为验收证据。
- Oracle 复审 ✅：确认 H1 无效性能论断已删除，checked range、partial-allocation error precedence、depth transition `i_blocks` accounting 与 LA64 KTest 覆盖通过。

**备注：** 未修改默认 backend、测试配置或共享磁盘镜像。docs `code_paths` 未命中本次源码，AI-Usage-Report 已列出 `openai/gpt-5.6-terra`。现有 `perf_diag` 仅导出 PageCache/块 I/O；another_ext4 prepare/extent/inode 计数尚未导出，后续性能实验不得据此作出该层归因。
