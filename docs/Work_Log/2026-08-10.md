---
date: 2026-08-10
timezone: Asia/Shanghai
schema: work-log-v2
---

## Batch consecutive another_ext4 unlink transactions

**Date:** 2026-08-10 +08:00
**Author:** AI:Codex
**Tags:** [area:fs, area:ext4, type:performance, type:correctness]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** Consecutive regular-file unlinks now share a bounded deferred JBD2 transaction instead of forcing one four-barrier commit per syscall. Directory lookup returns a validated block locator so unlink avoids two full rescans. Deferred metadata is published to live caches and block read-through before syscall return, while fsync/syncfs, journal pressure and shutdown remain durability boundaries. Directory removal remains synchronous until parent-link lifetime batching has dedicated coverage; the previously reverted asynchronous checkpoint/tail protocol was not restored.

**Files:**
- `dependency/another_ext4` (`f01dfd9`) — total-credit accounting, deferred read-through/cache publication, locator-based removal, bounded file-unlink batching and ring-pressure retry.
- `os/src/kernel_tests/ext4_another/{mod.rs,sync.rs}` — verify two consecutive unlinks perform no intervening flush, disappear from lookup immediately and commit at sync.

**Verification:**
- another_ext4 focused deferred transaction tests: 6 passed.
- another_ext4 full host suite: 139 passed, 2 pre-existing baseline assertions failed (`commit_flush_failure_never_publishes_or_checkpoints`, `direct_range_plan_filters_small_or_oversized_writes`); neither failure intersects modified paths.
- RV64 disk-backed ktest: 145 passed, 10 skipped, 0 failed; new unlink batching test passed.
- LA64 disk-backed ktest: 137 passed, 10 skipped, 0 failed; new unlink batching test passed.

**Notes:** Raw ktest and BuildStorm logs remain untracked and are not committed.

## 修正 BuildStorm 诊断构建门禁并恢复后台采样

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:buildstorm, type:diagnostics, type:performance]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 终止上一轮未编入 `perf_diag` 的 BuildStorm 采样；确认该轮只能观察阶段进度，不能提供 `/sys/kernel/stats/*` 计数。将“诊断内核特性与用户态采集器必须先握手”沉淀为可复用经验，随后使用 `EXTRA_FEATURES=perf_diag` 重建 RV64 诊断内核。统计采集器同时改为 fail-closed：控制节点握手失败时不派生采集进程，运行中统计路径消失时退出采集子进程，避免无效轮询或子进程误入主测试流程。

**Verification:**
- 上一轮专用 QEMU/BuildStorm 进程已停止；队友独立 QEMU 未触碰。
- Docker 容器：`mangocore-smp-final-merge-20260806-os-dev-1`；挂载 `/home/lzm/projects/MangoCore-smp-final-merge-20260806 -> /app`。
- RV64 诊断内核已生成；rustc 命令包含 `--cfg feature="perf_diag"`、`--cfg feature="perf_stats"` 和 `--cfg feature="log_off"`。
- RV64、LA64 normal `perf_diag` 内核已在独立 build root 中串行编译通过。
- 后台诊断实例使用独立新 overlay 恢复运行（启动 PID 2515815）；启动快照确认 `perf_diag=true`、`perf_stats=true`、`scheduler_counter_schema_version=4`，`BUILDSTORM_STATS_BEGIN/END sample=0` 已出现且没有 stats 节点 `ENOENT`。
- 串口日志位于 `/home/lzm/projects/MangoCore-smp-final-merge-20260806/build/diag-round-20260810/qemu-output-run2.log`；sample 22 同时观察到 8 个 current 并进入 core/std 波峰，随后越过 ax-posix-api，持续采集到 sample 33（峰值后约 5 分钟）后结束。镜像、日志和构建产物均不纳入 git。

**Notes:** 后台 QEMU 仅由旁路日志监测，不因后续构建或代码修复而停止；队友独立 QEMU 不触碰。

# Work Log — 2026-08-10

## Add Linux/QEMU and host BuildStorm reference bounds

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area=buildstorm, type=performance, area=qemu, area=filesystem]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 只读分析用户提供的 Linux/QEMU 基线包，以及既有宿主机 ArceOS BuildStorm R3 全量基线，为后续 BuildStorm 优化建立“QEMU SMP 能力上限”和“Cargo 依赖图固有限制”两条参照。原始压缩包、日志和采样数据均留在工作区外或未跟踪目录，不加入 git。

**Linux/QEMU reference:** Linux RV64 6.17.13、LA64 6.18，QEMU 10.0.2，1 GiB，分别使用 `-smp 1` 与 `-smp 4`。同一份 4-process iozone workload 从 1 vCPU 到 4 vCPU 时，RV64 initial-writer/reader throughput 分别提升 4.26x/3.14x，LA64 分别提升 3.49x/2.28x；pipe bandwidth 分别提升 1.91x/2.02x。说明该 QEMU 环境能够从多 vCPU 获得显著并行收益，不能把 MangoCore 的低平均并行度简单归因于 `-smp` 或 MTTCG 未工作。

**Host BuildStorm reference:** 权威 R3 是 8 CPU/8 GiB、clean target、`CARGO_BUILD_JOBS=8` 的完整成功构建，52.472 秒产出 1,679,184-byte ELF。容器 CPU 平均消耗 3.295 核；0–20 秒多数窗口约 1.0–1.34 核，35–50 秒才达到约 6.94–7.84 核。由此可见 Cargo/stdlib/ArceOS 依赖图本身包含长串行阶段，8 核配置不等于全程 8 核利用率。

**Comparison with Mango E1:** Mango E1 timed window 的约 1.96 guest CPU 有效运行量相当于宿主 clean BuildStorm 平均 3.295 核的约 59.5%，以此粗略估计的并行度上限差距约 1.68x，而不是相对 8 核的 4.08x。该比值只用于方向判断：宿主与 guest 的计数域、阶段边界和环境不同，不能作为成绩或严格 A/B。

**Limitations:** Linux 1-core 与 4-core 样本运行日期不同，内存为 1 GiB，使用 snapshot disk，且内容是 lmbench/iozone/libcbench/network baseline，并非 BuildStorm；glibc iozone/netperf 部分因镜像依赖缺失失败。后续只采用成功的 musl/内核无关指标和核数扩展趋势，不使用失败项，也不把 Linux microbenchmark 的绝对值直接外推到 MangoCore BuildStorm。

**Decision:** 暂不修改 scheduler placement 或 another_ext4 journal。下一轮优先围绕 Cargo timed phase 的串行等待、进程生命周期和 guest/host 同阶段 runnable 差值做有界分析；只有出现远端 runqueue 积压或 timed journal 占比上升的新证据，才重新打开相应内核优化方向。

## E1 timed-window sample: timed phase is not journal-bound

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area=buildstorm, type=performance, area=scheduler, area=another-ext4]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 在移除 `write_pages` 逐事件串口输出并重建诊断内核后，使用官方 RV64 pub golden、8 CPU/8 GiB、MTTCG、`LOG=off`、`profile=all` 完成一次 `BUILDSTORM_BEGIN` 后 180 秒窗口（sample 20→26）。本轮只做定因采样，不是正式成绩，且宿主上仍有队友独立 BuildStorm，因此 wall-time 结果不作跨运行比较。

**Observed timed counters (sample 20→26):**
- `task_run_slice_ticks_total` 增加 3,525,207,913（约 352.5 CPU-s），折合约 1.96/8 个 guest CPU 有效运行。
- `steal_attempts` 增加 108,950（约 605/s）；其中 `steal_no_remote_ready` 增加 107,058（约 98.3%），`steal_no_eligible_candidate` 仅增加 1,772。`candidate_found=ktlb_sync_calls=success` 增加 120，`steal_recheck_failed=0`。
- CPU0 `steal_attempts=0`、`idle_busy_loops_cpu0=0`；CPU0 runqueue 快照始终为空或仅含当前任务。
- timed 阶段 journal 仅增加 5 commits、20 phase flushes；journal commit/flush ticks 增加约 0.745/0.734 CPU-s。VirtIO bridge hold 增加约 23.2 CPU-s、wait 增加约 1.85 CPU-s；PageCache user read 增加 11,613，writeback/journal 没有持续增长。

**Decision:** 正式计时段不满足 another_ext4/journal 主瓶颈条件，也不满足 last-CPU placement 主瓶颈条件；低并行度更符合 Cargo/ArceOS 依赖图或用户态串行等待。前置清理阶段的 journal/bridge 放大仍可作为独立启动成本记录，但本轮不实施 bounded journal batching 或 wake placement 改动。

**Verification:** `BUILDSTORM_BEGIN` 已出现；采样在 `BUILDSTORM_COMPILE` 完成前停止，未运行不限时 BuildStorm。无逐事件 `write_pages` 输出；日志留在容器临时目录，未加入 git。

## Remove per-writeback diagnostic serial output before E1

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area=buildstorm, type=diagnostics, area=another-ext4]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** E1 前置采样发现 `ext4_another/page_cache.rs` 在 `perf_diag` 下仍逐次输出 `[ext4_another] write_pages ...`，会污染 QEMU 串口和 BuildStorm 时序。移除该逐事件输出，仅保留聚合计数器与阶段计时；本改动不改变写回语义。

**Files:**
- `os/src/fs/ext4_another/page_cache.rs` — 删除 writeback 每次调用的串口打印。
- `docs/Work_Log/2026-08-10.md` — 记录 E1 诊断门禁修正。

**Verification:** 当前 E1 采样已停止；需在 Docker 内重新串行构建 RV64/LA64，并重新执行独立 RV64 E1。旧采样含 14 条逐事件日志，不作为低扰动定因证据。

**Notes:** 未触碰队友容器、进程、镜像或日志；任何 BuildStorm 日志仍不纳入 git。

## Batch 0 focused validation and short BuildStorm profile-all

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:buildstorm, type:performance, area:scheduler, area:another-ext4]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 完成低扰动诊断入口后的双架构构建和聚焦 QEMU 验证，并运行一次绑定宿主 CPU 的短时 RV64 `profile=all` BuildStorm 采样。该采样只用于定位瓶颈，不作为正式成绩。

**Verification:**
- Docker `make kernel ARCH=rv64 PROFILE=normal`、`ARCH=la64` — ✅。
- RV64 focused ktest（waitqueue/sched/smp/page_cache_sync/ext4_another）— ✅ 87/87。
- LA64 focused ktest（smp/ext4_another）— ✅ 59/59。
- `make lint ARCH=rv64 MODE=release` — ⚠️ 既有 lint baseline 漂移，报告多项历史 warning，未见本批新增编译错误。
- 短时 profile-all 采集 9 个快照；正式 `BUILDSTORM_BEGIN` 尚未出现即停止 QEMU，因此不宣称完整 BuildStorm 完成。

**Observed counters:** sample2→8（约 180 秒）`task_run_slice_ticks_total` 增加约 356 CPU-s（10 MHz 计数器），约 1.98/8 核有效并行度；CPU0 `steal_attempts=0`、`idle_busy_loops_cpu0=0`，AP 仅增加 `idle_wait_loops`。steal 候选、KTLB sync、success 始终相等（83），`steal_recheck_failed=0`；窗口内 steal attempts 增加 109,356（约 608/s），其中 no-eligible 增加 96,285，未出现原先报告的 CPU0 steal 风暴。wake 中“last CPU 忙且存在 idle CPU”仅累计 59 次。

**Observed FS/IO:** 同一窗口 journal commit 增加 1,234、journal flush 增加 4,939（约 4 flush/commit）；VirtIO bridge wait/hold 累计分别增加约 157.7/176.8 CPU-s，journal commit/flush ticks 约 173.4/170.6 CPU-s，而 PageCache user read/write 计数在初始加载后基本不再增长。该阶段未进入 `BUILDSTORM_BEGIN`，所以结论限定为“预构建/启动阶段 another_ext4 journal + bridge 明显占主导”，不能替代正式 Cargo timed window。

**Notes:** teammate 的 `pxy-mangocore-smp-os-dev-1` 后台 BuildStorm 属于独立工作区，未检查、未终止、未修改。采样日志、镜像和 overlay 均未纳入 git。

## BuildStorm 定因采样入口与低扰动调度快照

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:buildstorm, type:diagnostics, area:scheduler]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 为下一轮短时 timed profile 增加 `memory_io` 入口；core profile 改为 5 秒 taskq 快照，all/memory_io 使用 30 秒全量快照；调度诊断导出每 CPU current/runqueue 快照以及“last CPU 忙且存在 idle CPU”wake 条件计数；移除 another_ext4 journal 逐事务串口打印，避免诊断输出污染 BuildStorm 时序。

**Files:**
- `user/src/bin/init.rs` — 增加 memory_io profile，并按 profile 降低采样输出频率。
- `os/src/task/perf.rs` — 增加 wake 条件计数并升级 scheduler counter schema 到 v4。
- `os/src/task/manager.rs` / `os/src/task/run_queue.rs` — 记录 wake 时 last CPU 与 idle CPU 的关系。
- `os/src/fs/sysfs/files/diag.rs` — 导出每 CPU current/runqueue 快照及新计数。
- `os/src/fs/ext4_another/blockdev.rs` — 删除 journal 逐事件串口输出，保留聚合计数。

**Verification:** Docker 内串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `ARCH=la64` 均通过；RV64 focused ktest（waitqueue/sched/smp/page_cache_sync/ext4_another）87/87 通过，LA64 focused ktest（smp/ext4_another）59/59 通过。诊断字符串预留 4 KiB 以容纳 per-CPU 快照。`make lint ARCH=rv64 MODE=release` 仅因既有 lint baseline 漂移失败，未发现本批代码专属编译错误。

**Notes:** 仅保留聚合诊断；BuildStorm 原始日志、镜像和 overlay 不纳入提交。该批不改变调度或文件系统语义。

## BuildStorm block/MM profile collector plumbing

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:buildstorm, type:diagnostics]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 修正 BuildStorm PID1 只识别 `buildstorm.stats=core` 的限制。现在支持 `core` 与 `all` profile；`all` 会按 sample 读取 taskq、pagecache、blockio、vm、heap、pagefault、ext4、mount、net 诊断文件，扩大单文件读取缓冲区并保持默认 profile 不产生统计开销。

**Files:**
- `user/src/bin/init.rs` — 增加 profile 解析、all 诊断文件采样和动态统计控制输出。
- `docs/Work_Log/2026-08-10.md` — 记录本次实验入口修复。

**Verification:**
- 原有 core profile BuildStorm 数据已保存；修复前 `buildstorm.stats=all` 只运行 BuildStorm、不产生 stats 快照，结果作废。
- 修复后的 RV64 block/MM profile 在独立 BUILD_ROOT 中完成编译并启动，采集 6 个周期快照；taskq、pagecache、blockio、vm、heap、pagefault、ext4、mount、net 均成功读取，无采集失败标记。
- 诊断结果显示 `steal_candidate_found == steal_ktlb_sync_calls == steal_success`，`steal_recheck_failed=0`；VirtIO DMA pool reserve/fallback 无失败，原始日志和镜像不纳入 git。

**Notes:** 这是诊断采样入口修复，不是 VM/heap/frame 性能优化；第六步仍延后。该轮 QEMU 为阶段性计数实验，未作为正式 BuildStorm 成绩。

## PageCache 连续 demand-read

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:page-cache, type:performance, area:another-ext4]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 为 PageCache 后端增加页对齐的 `read_contiguous()` 扩展。缺页填充现在按最多 16 页（64 KiB）分块读取连续 miss run；通用后端默认逐页回退，another_ext4 直接把连续区间读入单个 staging buffer，避免“后端 staging → PageEntry → 用户”的重复后端逐页调用，同时保留 generation 检查、EOF 零填充和 publish-after-I/O 语义。

**Files:**
- `os/src/fs/page_cache.rs` — 新增 `MAX_DEMAND_READ_PAGES` 和 `PageCacheBackend::read_contiguous()`，重写 `fill_miss_runs()` 的受控连续读取路径。
- `os/src/fs/ext4_another/page_cache.rs` — another_ext4 覆盖连续读取，按 visible size 处理跨 EOF 区间并复用现有 `readpages` 诊断计数器。
- `docs/09_debug/perf_diag.md` — 补充 `ext4_pc_readpages_*` 计数器与 64 KiB demand-read 窗口说明。
- `docs/Work_Log/2026-08-10.md` — 记录本批设计和验证。

**Verification:**
- Docker `make kernel ARCH=rv64 PROFILE=normal` — ✅。
- Docker `make kernel ARCH=la64 PROFILE=normal` — ✅。
- Docker `make -C os rv64-ktest PROFILE=normal KTEST=page_cache_sync KTEST_QEMU_TIMEOUT=60` — ✅ 6/6。
- Docker `make -C os rv64-ktest PROFILE=normal KTEST=ext4_another KTEST_QEMU_TIMEOUT=90` — ✅ 20/20，覆盖稀疏写、leading-hole、truncate、fsync/syncfs、close/replay、symlink remount。
- Docker `make -C os la64-ktest PROFILE=normal KTEST=page_cache_sync,ext4_another KTEST_QEMU_TIMEOUT=120` — ✅ 26/26；VirtIO PCI block 和 another_ext4 路径正常。
- `git diff --check` — ✅。
- 全仓 `cargo fmt --all -- --check` 仍被已有非本批格式差异阻断；未对无关文件做格式化改写。
- 原始 BuildStorm 日志、镜像和构建产物未纳入 git。

**Notes:** 连续读取窗口固定为 64 KiB，避免 demand fault 临时内存随用户请求无界增长。只有 another_ext4 覆盖该接口；其他后端行为保持原逐页兼容路径。BuildStorm 长测安排在本批提交后统一回归。

## VirtIO 小型 DMA 描述符池

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:virtio, type:performance, area:buildstorm]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 在保留原有 64 KiB 数据 DMA 池和单页 fallback 的前提下，为 block VirtIO 的固定小描述符增加 32 个单页复用槽位。请求头、状态字节和间接描述符优先从该池取得，池耗尽或初始化失败时仍回退到原有路径，不改变任意用户缓冲区的所有权语义。

**Files:**
- `os/src/drivers/block/virtio_dma_pool.rs` — 新增小描述符槽位状态、轮询分配、物理地址查找和回收；数据槽位的 generation 状态机保持不变。
- `os/src/drivers/block/virtio_blk.rs`、`os/src/drivers/block/virtio_blk_pci.rs` — block MMIO/PCI HAL 的 `share/unshare` 接入小池，并保留非 block/未知尺寸缓冲区的 fallback。
- `os/src/task/perf.rs` — 增加 header/status/indirect 小池命中计数器及 reset/stub。
- `os/src/fs/sysfs/files/diag.rs` — 导出小池启用状态和三类命中计数器。
- `docs/09_debug/perf_diag.md` — 同步 block I/O 诊断字段说明。

**Verification:**
- Docker `make kernel ARCH=rv64 PROFILE=normal` — ✅。
- Docker `make kernel ARCH=la64 PROFILE=normal` — ✅。
- Docker `make -C os rv64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39；VirtIO MMIO block 启动、挂载 another_ext4 正常。
- Docker `make -C os la64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39；VirtIO PCI block 启动、DHCP 初始化和挂载 another_ext4 正常。
- 本批没有运行 BuildStorm 长测；完成 PageCache 批次后再按计划串行回归。
- 原始 BuildStorm 日志、镜像和构建产物未纳入 git。

**Notes:** 小池仅按 block VirtIO 已知描述符长度（16/1/32 或 48 字节）启用；不改变网络 DMA、数据大缓冲区或未知长度 share 路径。池分配失败只禁用小池，不影响原有大数据池和正确性 fallback。

## 新任务 idle-first 放置与 BuildStorm 计数器复验

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:scheduler, type:performance, area:buildstorm]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 将新建任务的初始 CPU 选择从阻塞唤醒 locality 路径中分离；允许集合存在空闲 CPU 时优先投递空闲 CPU，并增加可比较的放置计数器。

**Files:**
- `os/src/task/run_queue.rs` — 复用 runnable mask/load 计算，新增 `select_new_task_cpu()` 的 idle-first 选择器；阻塞唤醒继续使用原 `select_runnable_cpu()`。
- `os/src/task/manager.rs` — 新任务发布改用 idle-first 选择器。
- `os/src/task/perf.rs` — 调度计数器 schema 从 2 升为 3，增加 `new_task_idle_available`、`new_task_selected_idle`、`new_task_kept_busy_parent` 及 reset/stub。
- `os/src/fs/sysfs/files/diag.rs` — 导出新任务放置计数器。
- `docs/05_process/scheduler.md`、`docs/09_debug/perf_diag.md` — 同步 idle-first 选择语义、schema v3 和计数器字段说明。

**Verification:**
- 静态 `git diff --check` — ✅
- RV64 诊断内核（`perf_stats perf_diag`）及 BuildStorm 启动 — ✅；`profile_ok=true`、`reset_ok=true`、`stats_on_ok=true`。
- RV64 8 核/8 GiB、`LOG=off`、`buildstorm.stats=core` 的正式 900 秒窗口 — ✅；从 `BUILDSTORM_BEGIN mode=multi` 到 sample22 后由监视器正常 TERM，终止是窗口控制而非 guest 失败。
- 900 秒首尾增量：`steal_attempts=526501`（约 585/s），CPU0 `steal_attempts=0`、`idle_busy_loops=0`、`idle_wait_loops=0`；`candidate_found=ktlb_sync_calls=success=364`，`steal_recheck_failed=0`；`steal_no_remote_ready=515204`、`steal_no_eligible_candidate=10933`。
- 新任务放置计数首尾增量：`new_task_idle_available=139`、`new_task_selected_idle=139`、`new_task_kept_busy_parent=52`；说明已有空闲 CPU 时全部采用 idle-first。
- Docker `make kernel ARCH=rv64 PROFILE=normal` — ✅（现有 warning 基线，无新增错误）。
- Docker `make kernel ARCH=la64 PROFILE=normal` — ✅（现有 warning 基线，无新增错误）。
- Docker `make -C os rv64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39。
- Docker `make -C os la64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39。
- Docker `make lint` — ⚠️ 在首个 `rv64/debug` 阶段因仓库现有 lint baseline stale/新增历史 warning 退出（`function_casts_as_integer`、多处 `unknown`/`unused_must_use` 等，均不涉及本次调度文件）；未继续其它三个格子。
- 本次原始 BuildStorm 日志不纳入 git。

**Notes:** 本轮不修改普通阻塞唤醒的最近 CPU locality，不调整 steal、VirtIO DMA、PageCache 或 BuildStorm 参数；工作区已有镜像/日志等未跟踪文件保持不动。CPU0 空转/steal 已归零，但 `idle_wait_loops_cpu0=0` 表明该窗口内 CPU0 更可能持续有运行任务，不能据此宣称 CPU0 WFI 等待计数增长；需在 focused 测试中补充 CPU0 真空闲唤醒验证。

## 第一轮 BuildStorm 诊断：阻塞式统计采集器

**Date:** 2026-08-10  +08:00
**Author:** AI:Codex
**Tags:** [area:buildstorm, type:performance, area:diagnostics]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 统计采集器由 busy-yield sleep 改为 `sys_nanosleep` 阻塞等待，避免采集器自身长期占用一个 runnable CPU，污染 BuildStorm 并行度与 CPU0 idle 计数。

**Files:**
- `user/src/usr_call.rs` — 新增 `sleep_blocking()`，仅供诊断采集器使用；保留旧 `sleep()` 语义不变。
- `user/src/bin/init.rs` — BuildStorm 统计采集循环改用阻塞等待。

**Verification:**
- RV64、LA64 normal `perf_diag` 内核在本轮专用 build root 中串行编译成功。
- 使用官方 RV64 pub golden 的独立 qcow2 overlay、8 CPU/8 GiB、MTTCG、`buildstorm.stats=all` 启动诊断；统计节点握手和首个全量快照成功。
- 日志、镜像 overlay 和构建产物不纳入 git。
