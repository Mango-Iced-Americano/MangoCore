---
date: 2026-08-10
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-10

## 新任务 idle-first 放置与 BuildStorm 计数器复验

**Date:** 2026-08-10  +08:00  
**Author:** AI:Codex  
**Tags:** [area:scheduler, type:performance, area:buildstorm]

**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 将新建任务的初始 CPU 选择从阻塞唤醒 locality 路径中分离；允许集合存在空闲 CPU 时优先投递空闲 CPU，并增加可比较的放置计数器。

**Files:**
- `os/src/task/run_queue.rs` — 复用 runnable mask/load 计算，新增 `select_new_task_cpu()` 的 idle-first 选择器；阻塞唤醒继续使用原 `select_runnable_cpu()`。
- `os/src/task/manager.rs` — 新任务发布改用 idle-first 选择器。
- `os/src/task/perf.rs` — 调度计数器 schema 从 2 升为 3，增加 `new_task_idle_available`、`new_task_selected_idle`、`new_task_kept_busy_parent` 及 reset/stub。
- `os/src/fs/sysfs/files/diag.rs` — 导出新任务放置计数器。
- `docs/05_process/scheduler.md`、`docs/09_debug/perf_diag.md` — 同步 idle-first 选择语义、schema v3 和计数器字段说明。

**Verification:**
- 静态 `git diff --check` — ✅
- RV64 诊断内核（`perf_stats perf_diag`）及 BuildStorm 启动 — ✅；`profile_ok=true`、`reset_ok=true`、`stats_on_ok=true`。
- RV64 8 核/8 GiB、`LOG=off`、`buildstorm.stats=core` 的正式 900 秒窗口 — ✅；从 `BUILDSTORM_BEGIN mode=multi` 到 sample22 后由监视器正常 TERM，终止是窗口控制而非 guest 失败。
- 900 秒首尾增量：`steal_attempts=526501`（约 585/s），CPU0 `steal_attempts=0`、`idle_busy_loops=0`、`idle_wait_loops=0`；`candidate_found=ktlb_sync_calls=success=364`，`steal_recheck_failed=0`；`steal_no_remote_ready=515204`、`steal_no_eligible_candidate=10933`。
- 新任务放置计数首尾增量：`new_task_idle_available=139`、`new_task_selected_idle=139`、`new_task_kept_busy_parent=52`；说明已有空闲 CPU 时全部采用 idle-first。
- Docker `make kernel ARCH=rv64 PROFILE=normal` — ✅（现有 warning 基线，无新增错误）。
- Docker `make kernel ARCH=la64 PROFILE=normal` — ✅（现有 warning 基线，无新增错误）。
- Docker `make -C os rv64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39。
- Docker `make -C os la64-ktest PROFILE=normal KTEST=smp KTEST_QEMU_TIMEOUT=60` — ✅ 39/39。
- Docker `make lint` — ⚠️ 在首个 `rv64/debug` 阶段因仓库现有 lint baseline stale/新增历史 warning 退出（`function_casts_as_integer`、多处 `unknown`/`unused_must_use` 等，均不涉及本次调度文件）；未继续其它三个格子。
- 本次原始 BuildStorm 日志不纳入 git。

**Notes:** 本轮不修改普通阻塞唤醒的最近 CPU locality，不调整 steal、VirtIO DMA、PageCache 或 BuildStorm 参数；工作区已有镜像/日志等未跟踪文件保持不动。CPU0 空转/steal 已归零，但 `idle_wait_loops_cpu0=0` 表明该窗口内 CPU0 更可能持续有运行任务，不能据此宣称 CPU0 WFI 等待计数增长；需在 focused 测试中补充 CPU0 真空闲唤醒验证。
