---
date: 2026-08-10
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-10

<a id="2026-08-10-2330-vendor-submodules"></a>
## 23:30 — 评测机修复：vendor 全部 submodule 与 fdt 进主仓库，默认跳过 rootfs

**Date:** 2026-08-10 23:30 +08:00
**Author:** AI:opencode/Sisyphus | Human:user
**Tags:** [area:build, type:infra, area:net]

**Mango Gate:** skill loaded=yes; references=none — 构建分发修复，非调试/性能任务

**Summary:** 评测机（/coursegrader/submit）make all 报 `No rule to make target
'dependency/lwext4_rust/c/elf-linux-gnu.cmake'`。验证证据链：lwext4_rust 的 submodule
指针 5e6d52e 的 git 树中确实包含该文件（ls-tree 确认），评测机缺失 = 代码分发环节未
携带 submodule 内容（archive/非递归 clone，无 .git 时 prepare-cargo-config 的
submodule 更新被 `if [ -d .git ]` 跳过且无报错，与日志吻合）。修复：删除全部 gitlink
与 .gitmodules（another_ext4/buddy_system_allocator/lwext4_rust 含嵌套 c/lwext4），
fdt git 依赖改 path，源码 vendor 进主仓库（合计约 5.5MB），评测分发零 github 依赖。
同时 BUILD_ROOTFS ?= 0 默认跳过 rootfs（make all 只产出 Image/kernel-la，评测用不上，
也消除 bench_runner/bash-rv 等 cp 噪音；run 目标不依赖 fs-img，本地不受影响）。

**涉及文件：**
- `os/Cargo.toml` — fdt git 依赖 → `path = "../dependency/fdt"`（432K 源码入库）
- `os/Makefile` — BUILD_ROOTFS ?= 0 默认跳过 rootfs（保留 BUILD_ROOTFS=1 逃生门）
- `os/make/rv64.mk` / `os/make/la64.mk` — all 目标 fs-img 条件化
- `.gitmodules` — 删除；`dependency/{another_ext4,buddy_system_allocator,lwext4_rust,fdt}` 转为普通跟踪目录

**验证：**
- 容器全量构建（fresh HOME、proxy env 清空）：`make all` EXIT=0，2m56s
- Image 10MB + kernel-la 110MB 产物；0 处 rootfs/tools-disk 阶段；零 git fetch
- os/Cargo.lock 随 path 依赖自动更新
- 已 push origin + gitlab main（afabff9f）

**备注：** vendor 前验证了 submodule HEAD=gitlink 指针且该 commit 含缺失文件，排除
"本地脏状态/远端缺文件"的误诊；嵌套 c/lwext4 的 .git 指针文件一并删除，git add 无
embedded-repo 警告；工作树其余 gitlink（dependency/rustsbi、virtio-drivers 等）均为
普通目录非 submodule，不受影响。

<a id="2026-08-10-2230-network-coldstart"></a>
## 22:30 — submission 分支网络救火：镜像化 rustup、submodule/cargo git 代理覆盖、官方容器冷启动实测

**Date:** 2026-08-10 22:30 +08:00
**Author:** AI:opencode/Sisyphus | Human:user
**Tags:** [area:build, type:infra, area:net]

**Mango Gate:** skill loaded=yes; references=none — 构建/网络配置任务，非调试/性能任务

**Summary:** 从 perf-minibuild-profile stash 后切出 submission 分支完成网络救火：
rustup 默认镜像 rsproxy.cn（容器内实测 USTC ~160B/s 限速、rsproxy ~5MB/s）；
GIT_SUBMODULE_PROXY 统一代理 submodule（git -c）与 cargo git 依赖（~/.gitconfig
insteadOf——实测 libgit2 不读 GIT_CONFIG_COUNT 环境变量）；BUILD_TOOLS_DISK 默认 0
跳过 tools 盘（消除 Alpine 包下载依赖）；cargo 超时放宽 600s + 重试 10 次。
官方线容器冷启动实测：有网络 make all EXIT=0（3m09s）；--network none 预置
工具链+submodule+fdt 缓存后 make all EXIT=0（2m29s）全离线，证明剩余网络依赖
仅 rustup 下载、git submodule、fdt git 依赖三项。

**涉及文件：**
- `scripts/rustup-setup.sh` — 默认 RUSTUP_DIST_SERVER/UPDATE_ROOT=rsproxy.cn（env 可覆盖）；
  fresh CARGO_HOME 自举复制 rustup 二进制；GIT_SUBMODULE_PROXY 时先 mkdir -p $HOME 再写
  ~/.gitconfig insteadOf（cargo libgit2 只读 gitconfig 文件）
- `Makefile` — prepare-cargo-config 支持 GIT_SUBMODULE_PROXY → git -c url.<proxy>.insteadOf
- `os/Makefile` — 同步 GIT_SUBMODULE_PROXY；BUILD_TOOLS_DISK ?= 0 默认跳过 tools 盘
  （tools-alpine/apk/cpython 均不再触发）；publish-compatibility 对缺失 tools 产物容错
- `os/make/common/orchestration.mk` — export CARGO_HTTP_TIMEOUT=600 / CARGO_NET_RETRY=10

**验证：**
- zhouzhouyi/os-contest:20260510 冷启动（fresh HOME、proxy env 全清空、rsproxy 镜像）：
  `make all` EXIT=0，3m09s；rv64 Image 10MB + la64 kernel-la 110MB + 双架构 rootfs 产物
- --network none 场景1（fresh HOME）：rustup 在 DNS 层失败（唯一前置网络依赖）
- --network none 场景2（预置三依赖 + 清空 build 目录）：`make all` EXIT=0，2m29s 全离线
- 代理实测（容器内 git 协议）：ghproxy.net 3-4s 稳定可用；gh-proxy.com 可用但慢(12-34s)；
  gitclone.com 502、ghfast.top/gh-proxy.com(curl)/moeyy/ghps 等失效或 403
- 沙箱宿主禁 loop 设备（宿主机自身 losetup EPERM）→ 测试环境 MANGO_ROOTFS_NO_LOOP=1
  debugfs fallback 填充 rootfs（仅测试 clone 内改动，生产脚本未动）

**备注：** docker.educg.net/cg/os-contest:20250614 官方评测镜像本机不可达（评测内网），
冷启动用同源 zhouzhouyi/os-contest 镜像验证；github 直连在容器内时段性不稳
（秒级~133s 超时波动，fdt 曾 3 次重试失败），评测机建议显式设置 GIT_SUBMODULE_PROXY；
fresh HOME 下 rustup toolchain install 收尾会因 CARGO_HOME 无 rustup 本体报错，脚本已自举。

<a id="2026-08-10-1700-eval-buildstorm-mask"></a>
## 17:00 — 评测机格式 mask=0x6000 跑 cagent + buildstorm，直接执行官方脚本

**Date:** 2026-08-10 17:00 +08:00
**Author:** AI:opencode/Sisyphus | Human:user
**Tags:** [area:test, type:feature, area:fs]

**Mango Gate:** skill loaded=yes; references=none — 适配评测脚本格式，非性能/调试任务

**Summary:** test_runner 的 buildstorm/cagent 组改为 chroot 后直接执行镜像内官方
`/glibc/{buildstorm,cagent}_testcode.sh`，用 os_test.conf mask=0x6000 先跑 cagent 再跑
buildstorm；在评测机格式 QEMU 命令 + 新鲜 qcow2 镜像上完成 rv64/la64 双架构验证，
确认 judge 兼容输出且 E0463（镜像损坏）消失。

**Files:**
- `user/src/bin/test_runner/groups/catalog.rs` — DEFAULT_ORDER 将 cagent 移到 buildstorm 前；
  cagent timeout 1200s → 180s（10 个 testcase 并行秒级完成，脚本末尾 wait simple_llm_server
  是死等待，由 runner timeout kill）
- `user/src/bin/test_runner/groups/execute.rs` — 删除内嵌 buildstorm STEP1-8 脚本分支，
  buildstorm/cagent 与其他组一致 chroot + chdir(/glibc) 后 `exec ./<script>`，直接跑官方脚本
- `os_test.conf` — mask=0x2000（仅 buildstorm）→ 0x6000（cagent bit14 + buildstorm bit13）

**Verification:**
- `make build ARCH=rv64 PROFILE=normal` — ✅
- `make build ARCH=la64 PROFILE=normal` — ✅（tools-disk-la 生成 disk-la.img 3.4GB）
- 评测机格式 QEMU（x0=qcow2 fresh overlay、x1=disk/disk-la.img、virtio-net、无 rng、4 核 4G）：
  - rv64 — ✅ 启动读 /sdcard/os_test.conf mask=0x6000 → cagent 组 `./cagent_testcode.sh`
    10/10 testcase pass → timeout 180s 杀（exit_code=9）→ buildstorm 组 `./buildstorm_testcode.sh`
    TOOLCHAIN ok + MINIBUILD ok → arceos 编译推进（unicode-ident 正常，253+/446 被杀中止）
  - la64 — ✅ 同流程 cagent 8+/10 pass → buildstorm TOOLCHAIN/MINIBUILD ok → 编译推进（179/446）
  - 结论：**E0463 是旧镜像损坏（孤儿 inode）导致**，新鲜镜像下 unicode-ident 正常编译

**Notes:**
- 官方 cagent_testcode.sh 末尾 `wait` 会等 simple_llm_server（后台 job 永不退出）卡死，
  依赖 test_runner timeout 兜底；judge 只解析 `testcase cagent NAME pass|reject` 行，不影响评分
- 新解压 pub 镜像 (sdcards-fresh/) e2fsck 干净（0 orphan）；旧 sdcards-final/ 有 50 个孤儿 inode
- qcow2 overlay 通过 `make buildstorm` buildstorm-input 每次 rm+create，golden 只读不被污染；
  评测机格式命令下 x0 用 qcow2 overlay 指向注入 conf 的 golden
- 手工注入 conf 到 golden：e2fsck -fy + debugfs `rm/write /os_test.conf`（inject_os_test_conf.sh
  会复制 derived 镜像，不适合 golden）
- la64 评测机命令模板中 `bus=virtio-mmio-bus.0` 在 loongarch64 无效（QEMU: Bus not found），
  需去掉 bus 参数（与 arch/la64-settings.mk 的 BLK_DEV_x0=-device virtio-blk-pci,drive=x0 一致）
- os_test.conf 随包提交（用户要求），本地开发调试后需记得评估是否恢复 mask

**官方 judge 脚本评分验证**（oscomp/testsuits-for-oskernel/judge/，完整串口日志直接喂入）：
- `judge_cagent-glibc.py`：差异化权重 + 时间奖励。easy=13.5/medium=20/hard(fs-search)=27，
  elapsed < timeout/2 时 +10% 权重奖励。实测 rv/la 均为 **199.1/199.1 满分**
  （10/10 pass，elapsed 1.7-3.0s 远小于阈值拿满奖励）
- `judge_buildstorm-glibc.py`：TOOLCHAIN ok=8 + MINIBUILD ok=12 + COMPILE ok=true=40
  + time=120×clamp((2B−t)/B,0,1)（B: rv=1616s/la=1985s，按 8/12 核参考）。实测 20/180
  （COMPILE 未跑到，QEMU 被杀中止）。注意本地 4 核 TCG 双实例远超 2B，time 分拿不到，
  评测机 8 核单实例才可能逼近 baseline
- judge 解析只按行前缀匹配，marker/进度/tail dump 均不影响评分；cagent 满分为 199.1
  而非"每 pass 10 分"的 100 分

**Related:** 006dcf51（提交并入 develop + push ae715e98..006dcf51）
