## 2026-08-03

### CI 修复最终验收与提交准备

**涉及文件：**
- `.github/workflows/ci.yml` — 修正已删除的 RV64/LA64 feature 名称，使 CI clippy 命令可执行。
- `os/src/net/config.rs`、`os/src/task/manager.rs` — DHCP 探测延后到调度期，且 timer IRQ 不再执行可能等待 VirtIO completion 的网络轮询。
- `os/src/drivers/block/virtio_blk.rs`、`os/src/fs/boot_block.rs` — 仅识别 VirtIO block transport，并把工具盘 `vdb1` 挂载至 `/tools`。
- `os/make/tools.mk`、`user/tools/loongarch64/` — 对称预装 LA64 e2fsprogs 与运行时依赖，使用实际 Alpine loader `ld-musl-loongarch64.so.1`。
- `user/src/bin/test_runner/bootstrap/early.rs` — 保持 NTP 时钟同步的有界轮询与 fallback。
- `os_test_ci.conf` — 固化 CI competition 的 `0xFDF` 测试配置。

**验证：**
- Docker CI 等价 RV64 clippy：`cd /app/os && cargo clippy --features "riscv block_virt" -- -W warnings` ✅（exit 0；项目既有 warning 保持 non-fatal）。
- Docker `make toolchain-preflight` ✅；随后串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `make kernel ARCH=la64 PROFILE=normal` ✅。
- `rustfmt --check`（全部变更 Rust 文件）✅；LA64 `mke2fs` ELF interpreter 为 `/tools/lib/ld-musl-loongarch64.so.1`，RPATH 为 `$ORIGIN/../lib`，依赖库随工具盘提交。
- `make lint` ⚠️：RV64 debug 的 first-party warning 基线在 `cb64aed2` 合并后已漂移（包含未改的 `dw_mshc/*`、FDT、VF2 文件）；CI 使用的直接 clippy 命令已通过，未为规避基线而修改无关代码或基线。

**备注：** 已将 9.7MiB 未跟踪 `Image` 判定为 QEMU 内核镜像构建产物并清理；未修改 VF2 文件。现有子系统文档未因本次 CI 修复产生语义变更，未更新 AI-Usage-Report（无新增模型或工具）。

### 修复 test-runner 时钟同步无网络卡死

**涉及文件：**
- `user/src/bin/test_runner/bootstrap/early.rs` — 将依赖 `get_time()` 的无界 `sleep(50)` 轮询替换为有界 `waitpid(WNOHANG)` + 调度让步；计时、轮数、fork/wait/exit 失败均会进入 fallback 时钟，超时只终止仍可等待的自有 NTP 子进程。

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- `make kernel ARCH=la64 PROFILE=normal` ✅（与 RV64 串行执行）
- RV64 QEMU ✅：无 NIC/loopback 配置输出 `[test-runner] ntpd unavailable; using fallback clock`，随后进入 test-runner、LTP 组，并由 QEMU 正常关机（exit 0）。
- `rustfmt --check user/src/bin/test_runner/bootstrap/early.rs` ✅；`git diff --check` ✅。

**证据：** `docs/Work_Log/evidence/2026-08-02/clock-sync-fix-*`

**备注：** 已检查 `docs/` 的 `code_paths`，没有文档引用该源文件；现有调试模式库已涵盖早期启动外部 NTP 必须 bounded best-effort，因此未重复沉淀。AI-Usage-Report 无新增工具或模型记录。
