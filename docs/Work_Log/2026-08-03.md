---
date: 2026-08-03
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-03

<a id="2026-08-03-1910-develop-batch3-waitqueue"></a>
## 19:10 — develop Batch 3 WaitQueue 通知 token 核心融合

**Date:** 2026-08-03 19:10 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, type:integration, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 将 develop 中“唤醒必须留下登记级结果”的核心思路迁入现有 SMP WaitQueue，
但不照搬旧单核调度状态。每轮等待以 `WaitEntry` 保存一次性通知，TaskStatus 继续唯一表达
CPU/runqueue ownership；提前 wake、multi-queue 唯一领取、deadline 跨轮失效和 default-stop
路径统一收口。FS/Net/Driver producer 尚由队友负责，generic 10ms I/O fallback 暂不删除。

**Files:**

- `os/src/task/manager.rs` — WaitEntry token、精确登记清理、单/多队列 checked block 与 timer generation。
- `os/src/task/signal/mod.rs` — default-stop 等待复用相同 token 协议。
- `os/src/kernel_tests/waitqueue.rs` — 永久 early-wake 精确窗口测试，不增加生产测试 hook。
- task/scheduler/WaitQueue/signal/SMP 文档、`AGENTS.md`、AI 披露与调试经验同步。
- `docs/Work_Log/evidence/2026-08-03/develop-batch3-waitqueue-core-summary.md` — 证据摘要。

**Verification:**

- DeepSeek 最终冻结 Docker 串行 build — ✅ RV64 131.9s、LA64 136.5s。
- 双架构 `CORE_NUM=8 KTEST=waitqueue` — ✅ 各 5/5；新增
  `early_wake_cancels_block` 两架构均真实执行，无 panic、timeout、forbidden marker 或 mutation。
- 双架构 `CORE_NUM=8 mask=0x003` — 功能非回归 ✅ RV64 raw/semantic 312/314；LA64 raw
  305/314，唯一额外 3 分为既有 `test_pipe` 并发 `cpid` 串口交织，pipe 写入成功且四组完整，
  按既有账本 semantic 308/314。两个 QEMU 进程均 exit 0、`online_mask=0xff`、无 timeout。
- 生产代码冻结 tracked diff SHA-256 为
  `cfad6e72757ebc42c1a834ca7a5db9b08cfc888d4341a6cf0687aefa0300ab28`。

**Notes:** 首轮验证测试误调用私有 `wake_one()` 而编译 RED；没有为测试扩大生产 API，改用
公开 `wake_at_most(1)` 后转绿。初赛 recipe 重生成四个已跟踪的 mke2fs/mkfs.ext4 二进制，
导致 runner 正确报告 mutation；测试前指纹证明这些文件原本干净，运行后已精确恢复，因此该轮
只作为功能非回归证据，不冒充完整绿色指纹门禁。DeepSeek 对交错分析有帮助，但其“所有远程
wake 均动态覆盖”的外推未采纳；两 CPU 同时竞争 multi-queue entry、deadline 跨轮误唤醒仍
登记为 NOT RUN。全面 producer 通知与 generic fallback 退役属于队友模块合入后的后续批次。

**Related:** `docs/05_process/waitqueue-completion.md`、
`docs/Work_Log/evidence/2026-08-03/develop-batch3-waitqueue-core-summary.md`

<a id="2026-08-03-1627-dynamic-memory-8g"></a>
## 16:27 — 双架构运行期内存拓扑与 8 GiB 适配

**Date:** 2026-08-03 16:27 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:mm, area:hal, area:boot, type:integration, risk:architecture]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 将 FDT/固件发布的运行期 DRAM bank、保留区和地址空洞贯穿帧分配器、
启动清零、内核映射元数据及用户可见内存统计。LA64 恒等映射 dirty 位图改为按最高
DRAM 末端在堆就绪后建立；QEMU 内存参数可用 `QEMU_MEMORY` 覆盖，并增加永久 MM 门禁
证明最高可用页越过旧 `MEMORY_END`。2K1000 继续通过同一接口消费静态 fallback。

**Files:**

- `os/src/hal/firmware/`、`os/src/mm/{frame_allocator,mod,sysctl}.rs` — 动态 usable-range
  迭代、分配区注册和内存统计。
- `os/src/hal/arch/loongarch64/laflex.rs`、`os/src/hal/arch/{mod,riscv/mod}.rs` —
  运行期 LA64 dirty 元数据与双架构初始化 hook。
- `os/src/main.rs`、`os/make/{rv64,la64,qemu-profiles}.mk` — 跨内存洞安全清零和
  `QEMU_MEMORY` 参数。
- `os/src/kernel_tests/mm.rs` — 固件末页、allocator、映射元数据和 ABI 总量永久门禁。
- MM/HAL 架构文档、`AGENTS.md` 与本证据摘要同步更新。

**Verification:**

- 当前 HAL/FDT 批次先提交为 `21ded299` (`hal: integrate firmware platform discovery`)。
- DeepSeek Docker 串行 normal build — ✅ RV64 131s、LA64 132s；2K1000 uImage build
  131s，静态 fallback 未被动态 QEMU 路径破坏。
- 首轮双架构 `CORE_NUM=8 QEMU_MEMORY=8G KTEST=mm` — RED：新增动态内存门禁均通过，
  RV64 报告 8189 MiB/最高页 `0x27ffff000`，LA64 报告 8190 MiB/最高页
  `0x26ffff000`，但两个既有 fault-in 用例因 linker 回收页被范围检查误拒绝而失败。
- 无锁热路径收敛后的最终冻结 diff `3bd1913b...` — ✅ RV64 144.303s、LA64 133.298s，双架构
  6/6 MM ktest、`[KTEST RESULT: PASS]`、`dynamic_above_static=true`；两项均 exit 0、
  无 mutation、panic、timeout 或 forbidden marker。

**Notes:** `is_allocatable_ram_phys_addr()` 不能永久排除 `[skernel, ekernel)`：initramfs
等完整 payload 页在复制后会以 `ReclaimedRegion` 转交给分配器。最终保持该接口原有的
无锁拓扑语义，只验证整页属于固件可用 RAM；页表/VMA/FrameTracker 负责实时所有权，避免
每页 uaccess 为诊断性后验检查争用 allocator 锁。
DeepSeek 首轮把相同失败推测为 TLB 时序问题，GPT/Codex 依据 allocator 生命周期和
RED→GREEN 证据予以纠正。动态内存工作包经人工授权后随本次提交落盘；
本地 `cc-codex/` 任务、原始日志和临时工作树仍不入 Git。

**Related:** `docs/Work_Log/evidence/2026-08-03/dynamic-memory-8g-summary.md`、
`docs/04_mm/frame-allocator.md`、`docs/04_mm/initialization-and-kernel-space.md`

<a id="2026-08-03-1235-la64-efi-fdt-integration"></a>
## 12:35 — LA64 EFI/FDT 双平台固件发现

**Date:** 2026-08-03 12:35 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:hal, area:boot, area:smp, type:integration, risk:architecture]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 补齐 LoongArch 设备树启动链：QEMU 与 2K1000 入口均保留 `a2` EFI system
table，由有界、无堆的 EFI64 最小解析器查找 FDT GUID，再进入与 RV64 共用的 FDT 资源和
PlatformInfo 解析器。QEMU 缺少合法 FDT 时明确失败；2K1000 因现有 U-Boot 可能不安装 FDT
配置表，保留仅限实板 feature 的静态资源回退。

**Files:**

- `os/src/hal/{boot,firmware,platform}/` — 协议中立入口参数、EFI→FDT、双架构解析与
  QEMU/2K1000 策略边界。
- `os/src/hal/arch/{loongarch64,riscv}/` — LA64 入口参数保留和六份 linker 的
  `.bss.boot < sbss` 合同。
- `os/src/main.rs`、`os/Cargo.toml`、`os/Cargo.lock` — 启动接线、平台诊断与共享依赖。
- `docs/01_architecture/{boot-and-trap,hal-and-platform}.md`、`AGENTS.md` — 同步启动协议、
  安全边界和实板回退范围。

**Verification:**

- 官方协议/实现对照 — ✅ Linux LoongArch boot ABI 和 QEMU 9.2.1 源码均证明 `a2` 为 EFI
  system table；QEMU 通过 EFI configuration table 登记 FDT，2K U-Boot 仅在 `fdt_addr`
  有效时登记，因此实板回退具有必要性。
- DeepSeek Docker LA64/RV64 QEMU 8 核 FDT 门禁 — ✅ 两架构启动合同分别通过；LA64 识别
  2 个 RAM、1 个 reserved、9 个 early-MMIO 和 38 个设备，RV64 共享 parser 回归通过，
  均到达 `online_mask=0xff`。完整 ktest 分别为既有 32/34 与 33/34，未虚报全绿。
- DeepSeek Docker ELF 布局 — ✅ `.bss=NOBITS`，2 MiB 快照地址 `0x80c2b000` 小于
  `sbss=0x80e2b000`；首轮 exit 141 已证实为 recipe 的 `pipefail + grep -q` SIGPIPE，修正
  本地 recipe 后同一冻结源码 exit 0。
- DeepSeek Docker 2K1000 build — ✅ exit 0，uImage 10,489,112 bytes，load/entry 均为
  `0x90000000`；实板启动为 NOT RUN。

**Notes:** 当前 HAL 已能完整描述 QEMU 的双 RAM node，但既有 frame allocator 日志仍显示
旧静态单区间，这是后续 MM consumer 迁移边界，不表示 FDT 漏解析。2K1000 仍按本轮 SMP
范围保持单核；静态回退不会编译进 LA QEMU。工作树保持未提交供人工审查，本地
`cc-codex/` 任务和日志不进入 Git。

**Related:** `docs/Work_Log/evidence/2026-08-03/la64-fdt-integration-summary.md`、
`docs/01_architecture/hal-and-platform.md`

<a id="2026-08-03-1136-develop-batch2-hal-integration"></a>
## 11:36 — develop Batch 2 HAL 数据源融合

**Date:** 2026-08-03 11:36 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:hal, area:boot, area:smp, type:integration, risk:architecture]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 将 develop 的 BootInfo、FDT 固件发现、PlatformInfo 与 DeviceManager 融入
现有 SMP 启动链，但不替换已验收的 AP/IPI/timer/TLB/ASID 架构后端。BSP 先冻结入口参数，
完成 CPU-local bootstrap 后再于清 BSS 前复制固件资源；堆就绪后发布不可变平台模型。
RV64 timer 改用 FDT `/cpus/timebase-frequency`，LA64 保持 CPUCFG 与静态平台回退。

**Files:**

- `os/src/hal/{boot,firmware,platform,device}/` — 两阶段只读固件/平台模型与查询接口。
- `os/src/main.rs`、`os/src/bootargs.rs`、`os/src/hal/arch/riscv/time.rs` — BSP 启动接线、
  FDT cmdline fallback 与真实 timer 频率。
- `os/Cargo.toml`、`os/Cargo.lock` — RV64-only fallible FDT 解析依赖。
- `docs/01_architecture/{boot-and-trap,hal-and-platform}.md`、`AGENTS.md` — 启动顺序、边界与
  后续消费者迁移约束。

**Verification:**

- DeepSeek 只读源码审查 — ✅ 修正 LA64 携带无用 2 MiB FDT buffer、隐式 Once 递归、
  target cfg、公共命名面和 staged API warning；拒绝用调度 tick 替代 LA64 CPUCFG。
- Docker RV64 `CORE_NUM=8 KTEST=smp` — ⚠️ 进入全部 34 项、`online_mask=0xff`，33/34；
  FDT 得到 1 GiB RAM、9 个 early-MMIO、53 个节点和 10 MHz timebase。唯一失败是既有
  `exec_owner_becomes_group_leader` 在真实 5 秒边界 5003ms 超时。
- Docker LA64 `CORE_NUM=8 KTEST=smp` — ⚠️ 启动顺序修正后从“无串口输出”恢复到
  `LoongArchLegacy + Static`、CPUCFG 100 MHz 和 `online_mask=0xff`，32/34；失败为已知
  stale-TLB timer-isolation 敏感点一次波动及同一 exec 5001ms 边界。
- DeepSeek Docker 串行最终 kernel build — ✅ RV64 127s、LA64 140s，源码指纹一致；
  新 HAL 文件无 unused/dead-code/function-pointer/unfulfilled-expectation warning。

**Notes:** 本批只建立 HAL 数据源，不把 frame allocator、动态 early-MMIO 页表或
FS/Net/Driver 消费者半迁移进来；这些仍分别属于后续 MM 批次和队友模块。8 核运行中的三个
失败均已如实保留，不能把总体结果写成全绿；其中正确 timebase 暴露出的 exec 测试裕量应在
独立 task/exec 批次决定是优化协议还是调整永久测试合同。Batch 2 当前保持未提交供人工审查，
本地 `cc-codex/` 证据不进入 Git。

**Related:** `docs/01_architecture/boot-and-trap.md`、
`docs/01_architecture/hal-and-platform.md`

<a id="2026-08-03-1015-develop-batch1-integration"></a>
## 10:15 — develop Batch 1 构建与测试基础设施融合

**Date:** 2026-08-03 10:15 +08:00
**Author:** AI:GPT/Codex | Human:Pneuma
**Tags:** [area:build, area:test, area:ci, type:integration, risk:low]
**Mango Gate:** skill loaded=yes; references=none — 非性能/故障调试

**Summary:** 将 `develop@f514cc3f` 分叉后的 CI 评分、全分支触发、NTP 有界等待、
e2fsprogs 工具盘构建及双架构运行库融合到 SMP 集成分支。保留 SMP 当前
`board_rvqemu`/`board_laqemu` feature，未提前引入依赖 HAL v2 的 `boot_la_qemu`；测试
runner 仅接入预装 e2fsprogs 快速路径，未提前引入 develop 的 FS runner 返回值重构。

**Files:**

- `.github/workflows/ci.yml`、`os_test_ci.conf`、`scripts/score_test.py` — CI 触发、评分表和零样本处理。
- `os/make/tools.mk`、`user/tools/{riscv64,loongarch64}/` — e2fsprogs、musl 解释器和运行库。
- `user/src/bin/test_runner/bootstrap/{early,packages}.rs` — NTP 有界等待和预装工具复用。
- `docs/08_testing/ci-scoring.md` — 同步双架构并行 job、评分公式和 artifact 现状。
- `docs/Work_Log/evidence/2026-08-03/develop-batch1-integration-summary.md` — 验证环境与结果摘要。

**Verification:**

- Docker `python3 -m py_compile scripts/score_test.py` 与 focused 零样本/`--table` 检查 — ✅。
- Docker `scripts/test-make-layering-contract.sh` — ✅。
- 双架构 `mke2fs` ELF 类型与 `/tools/lib` program interpreter 检查 — ✅。
- Docker 串行 `make kernel ARCH=rv64 PROFILE=normal`、`ARCH=la64` — ✅。
- Docker `make lint` — ❌ 既有 SMP warning baseline 漂移；报告文件均不在本批修改范围，
  未执行 `--capture-baseline` 掩盖问题。

**Notes:** 本批没有修改内核运行时核心路径，因此未运行完整 SMP/QEMU 压力矩阵；工作树
保留为未提交状态，等待人工审查。来源提交为 `b9e75f7a`、`448fb147`、`16071cb4`、
`b012983a`、`1ee82a15`、`fb4b895d`、`aa3c4fbd`、`049bc572`、`c6f828b0`、
`a6ef6d78`、`b9511c63`。

**Related:** `docs/08_testing/ci-scoring.md`、
`docs/Work_Log/evidence/2026-08-03/develop-batch1-integration-summary.md`

<a id="2026-08-03-0045-smp-b79-interval-timer"></a>
## 00:45 — B79 进程级 legacy interval timer

**Date:** 2026-08-03 00:45 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, area:syscall, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** `ITIMER_REAL/VIRTUAL/PROF` 从 TCB 私有状态迁入 PCB 独立 owner。REAL 由
monotonic heap action 按 generation/deadline 领取，VIRTUAL/PROF 由 trap-return 与
schedule-out 安全点按线程组 user 或 user+system CPU 累计领取；owner 锁内只提交状态，
锁外才进入 signal queue、runqueue 和 timer heap。

**Files:**

- `os/src/task/{process,task,manager,processor}.rs` — PCB timer owner、生命周期、安全点与 callback。
- `os/src/syscall/process/time.rs` — Linux 式 set/get ABI、三类时钟域和锁外 copyout。
- `os/src/{hal/arch/riscv/trap,syscall/utils,task/threads}.rs` — 删除旧 TCB remaining 刷新旁路。
- 时间、TCB、WaitQueue、锁序、SMP 计划、`AGENTS.md`、AI 披露与调试经验同步。
- `docs/Work_Log/evidence/2026-08-03/smp-b79-interval-timer-summary.md` — 冻结证据。

**Verification:**

- Linux 6.6 `kernel/time/itimer.c` 与 `kernel/fork.c` 对照 — ✅ timer 属于线程组；普通
  fork 创建新 timer 状态，exec 保留，REAL 使用 monotonic elapsed clock。
- Docker 双架构 `CORE_NUM=8` kernel build — ✅ RV64 125.158s、LA64 133.627s。
- 双架构 8 核 focused + 初赛门禁 — ✅ RV64 406.791s、LA64 438.430s；musl/glibc
  `setitimer01/02` 的 REAL/VIRTUAL/PROF 全部 PASS，`online_mask=0xff`；初赛保持
  RV64 312/314、LA64 308/314，既有 `busybox kill 10` 非满分项未扩大。
- 四项 accepted job 源码指纹一致；生产 diff SHA-256 为
  `3a2858cd39f0a25368a0e89eb6bfe5e8a2bd4d8e1db0b85ba566f309f77edf4d`。

**Notes:** DeepSeek 的早期只读审查误称普通 fork 继承 interval timer，已由 Linux 6.6
`copy_signal()` 的新 `signal_struct` 初始化路径纠正。运行验收后模型又把 REAL 与 wall-clock
调整列为缺口；REAL 实际使用 monotonic elapsed time，不随墙钟重定位。多 sibling 在不同 CPU
上精确跨越 VIRTUAL/PROF deadline 的动态注入仍为 NOT RUN；表锁提供唯一领取的静态证明。

**Related:** `docs/01_architecture/lock-order.md`、
`docs/Work_Log/evidence/2026-08-03/smp-b79-interval-timer-summary.md`

<a id="2026-08-03-0140-smp-b80-posix-timer-pending"></a>
## 01:40 — B80 POSIX timer 精确 pending 与 overrun

**Date:** 2026-08-03 01:40 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, area:syscall, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** POSIX timer 不再用进程 shared signal bitmap 猜测某个 timer 是否已有事件。每个
timer 对象以 `timer ID + instance_seq` 标识 pending，同一 timer 的重复到期合并为 overrun，
不同 timer 即使使用同一个非实时 signal 也分别排队。signal dequeue 后锁外回到 timer owner
固化本次 siginfo 和最近交付 overrun；delete/exec/exit 以精确身份执行反向锁外清理。

**Files:**

- `os/src/task/signal/{pending,mod,delivery}.rs` — timer 事件身份、SI_TIMER 字段和精确合并规则。
- `os/src/task/process.rs` — instance/arm/pending 三类状态、overrun 生命周期和双锁拆分。
- `os/src/task/manager.rs`、`os/src/syscall/process/time.rs` — wall/CPU/realtime 到期统一记录事件。
- `os/src/syscall/process/signal.rs` — signalfd 返回真实 timer ID/overrun，sender 字段归零。
- 时间、signal、锁序、SMP 计划、`AGENTS.md`、AI 披露与调试经验同步。
- `docs/Work_Log/evidence/2026-08-03/smp-b80-posix-timer-pending-summary.md` — 冻结证据。

**Verification:**

- Linux 6.6 `kernel/time/posix-timers.c`、`kernel/signal.c` 与
  `include/linux/posix-timers.h` 对照 — ✅ 每个 timer 持有唯一预分配 sigqueue；同一 timer
  已排队时只累计 overrun，dequeue 释放 signal lock 后再完成 timer rearm/overrun。
- Docker 双架构 `CORE_NUM=8` kernel build — ✅ RV64 129.165s、LA64 136.298s。
- 双架构 8 核 POSIX timer gate — ✅ RV64 78.480s、LA64 80.501s；两架构 musl/glibc
  `timer_settime01/02` 各 40 项，共 80/80 通过。
- 四项最终 job 均 exit 0、无 forbidden marker、无 timeout、无源码 mutation；冻结 tracked diff
  SHA-256 为 `34313121a86219eea2bfca2a3a465a78d7551a6487b14093a62f23e623458172`。

**Notes:** 首轮 build review 在 RV64 build 期间源码继续修改，worker 正确以 mutation 拒绝该轮，
没有把它计入证据；最终四项均基于同一冻结指纹。B78 已完成 CPU 消耗驱动的 process/thread
timer 到期，不能沿用模型报告中“仍未实现”的旧结论。两个 timer 共用 signal、精确
getoverrun 回环、delete/recreate pending ABA 和 signalfd timer 字段专项仍为 NOT RUN；当前
基础 LTP 不能替代这些交错验证。

**Related:** `docs/01_architecture/lock-order.md`、
`docs/Work_Log/evidence/2026-08-03/smp-b80-posix-timer-pending-summary.md`

<a id="2026-08-03-0155-smp-b81-shared-signal-hint"></a>
## 01:55 — B81 shared signal hint 锁内发布

**Date:** 2026-08-03 01:55 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, area:signal, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** process shared signal queue 的四个 mutation 写点不再于解锁后发布
`shared_pending_hint`。queue mutation、完整位图重算和 Release store 现在属于同一个 signal
临界区，Acquire fast reader 不会再被较早 writer 的锁外 stale store 长期覆盖。POSIX timer
discard/finalize 仍在 signal 解锁后执行，未引入双锁嵌套。

**Files:**

- `os/src/task/process.rs` — 四个 shared queue writer 锁内发布 hint，读端使用 Acquire。
- signal/锁序/SMP 计划、`AGENTS.md`、AI 披露与调试经验同步。
- `docs/Work_Log/evidence/2026-08-03/smp-b81-shared-signal-hint-summary.md` — 冻结证据。

**Verification:**

- 静态全调用面审查 — ✅ shared queue 只有 enqueue、两类 dequeue 和精确 timer remove 四个
  mutation，全部在同一 `self.signal` mutex 内发布 hint；timer owner 只在解锁后进入。
- Docker 双架构 `CORE_NUM=8` kernel build — ✅ RV64 134.984s、LA64 134.831s。
- 双架构 8 核 sigtimedwait gate — ✅ RV64 140.681s、LA64 139.029s；两架构
  `online_mask=0xff`，glibc `sigtimedwait01` 各 11 TPASS，TFAIL/TBROK/TCONF 均为 0。
- 四项 job 均 exit 0、无 forbidden marker、无 timeout、无源码 mutation；冻结 tracked diff
  SHA-256 为 `e81919e2a0904ecd52047f4db968ca54110ec3e49140adcb0a5b2d83f0d862a0`。

**Notes:** 修复成立的关键是 mutex 把所有 writer store 纳入同一全序；Release/Acquire 只负责
无锁发布关系。若仅把旧锁外 Relaxed store 改成 Release，旧 writer 仍能晚于新 writer 覆盖，
因此不算根因修复。普通 LTP 没有精确注入三 CPU stale-store 顺序，该动态交错登记为 NOT RUN，
以完整 mutation 搜索和锁序证明作为静态证据；本批没有重复 B80 timer 或初赛矩阵。

**Related:** `docs/01_architecture/lock-order.md`、
`docs/Work_Log/evidence/2026-08-03/smp-b81-shared-signal-hint-summary.md`

<a id="2026-08-03-2022-develop-batch4-waitqueue-lossless"></a>
## 20:22 — develop Batch 4 WaitQueue 无损事件通知

**Date:** 2026-08-03 20:22 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:task, area:net, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 普通 WaitQueue 不再持有队列锁执行登记后的 condition，队列锁现在只保护 waiter
链表。EventWaitQueue 删除锁冲突时静默丢 wake 的 `*_if_unlocked` 接口；TCP、RAW、packet 与
PTY 生产者统一使用可靠通知。每轮 `WaitEntry` token 与 checked block 共同保存发生在
`Running -> Blocking` 之前的通知，不扩张 TaskStatus 状态机。

**Files:**

- `os/src/task/manager.rs` — 普通 wait 的“短锁登记—锁外条件—checked block—短锁清理”协议。
- `os/src/fs/vfs/event.rs`、`os/src/net/`、`os/src/fs/dev/pty.rs` — 删除有损通知并迁移生产者；
  packet 在通知前显式释放 socket inner 锁，RAW send 同步补发写就绪事件。
- `os/src/kernel_tests/waitqueue.rs` — 永久 same-queue notification 回归。
- WaitQueue、锁序、测试清单、SMP 计划、AI 披露与两份调试经验同步。
- `docs/Work_Log/evidence/2026-08-03/develop-batch4-waitqueue-lossless-summary.md` — 冻结证据。

**Verification:**

- Linux 6.8 `include/linux/wait.h` 静态对照 — ✅ `prepare_to_wait_event` 后先检查 condition，
  再进入 schedule；MangoCore 采用相同的“登记与条件检查不由 waitqueue 锁包裹”分层。
- Docker 双架构 `CORE_NUM=8` kernel build — ✅ RV64 132.069s、LA64 131.369s。
- 双架构 8 核 `KTEST=waitqueue KREPEAT=20` — ✅ RV64、LA64 均 120/120，
  `online_mask=0xff`。
- 双架构初赛四组功能标记 — ✅ 两架构 QEMU exit 0、required marker 完整、无 panic/timeout；
  basic 128/128、busybox 118/119，只有既有 `busybox kill 10` 非满分项。
- 初赛确定性 runner 最终状态 — ⚠️ `partial`：`tools-alpine` 对四个 tracked `mke2fs`/
  `mkfs.ext4` 重复执行 `patchelf`，触发 source mutation 拒绝。四文件由测试前指纹确认原本干净，
  测试后已精确恢复；该基础设施问题拆到下一独立批次。

**Notes:** `accept` 的 pre-poll 继续保留为减少重复网络轮询的性能策略，而不是规避 WaitQueue
自锁的正确性补丁。普通 condition 可以推进生产者；locked wait 仍需遵守调用者业务锁约束。
splice 等剩余事件生产者尚未全面迁移，因此 generic 10ms I/O fallback 暂不删除。生产语义测试
使用的 tracked diff SHA-256 为
`16c53720feb81b071c9a98a61eb0a8f1ae17e849bc311722eca1c05563e6f36a`；其后仅同步注释和文档。

**Related:** `docs/05_process/waitqueue-completion.md`、
`docs/Work_Log/evidence/2026-08-03/develop-batch4-waitqueue-lossless-summary.md`

<a id="2026-08-03-2040-develop-batch4-patchelf-idempotence"></a>
## 20:40 — develop Batch 4.1 工具 ELF 准备幂等化

**Date:** 2026-08-03 20:40 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:build, area:test, type:fix, risk:tooling]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** `tools-alpine-rv/la` 不再对已正确配置的 tracked ext4 工具无条件执行
`patchelf`。共享 Make 宏先核对 interpreter、RPATH 值与 `DT_RPATH` 类型，仅在合同不匹配时
改写；缓存命中路径保持文件字节不变，恢复确定性 runner 的 source fingerprint 合同。

**Files:**

- `os/make/tools.mk` — 双架构共享 `ensure_alpine_tool_elf` 幂等检查。
- SMP 计划、AI 披露、harness 经验和本日日志同步。
- `docs/Work_Log/evidence/2026-08-03/develop-batch4-patchelf-idempotence-summary.md` — 冻结证据。

**Verification:**

- Docker `make -n -C os tools-alpine-rv` — ✅ Make 展开后的 shell 比较保留字面量
  `$ORIGIN/../lib`，宏语法与参数展开正确。
- DeepSeek max 只读审查 — ✅ 三项 ELF 合同均覆盖；RV64/LA64 loader 参数正确；缺失或损坏
  ELF 会进入修复分支而非静默跳过。
- Docker 双架构 `CORE_NUM=8 mask=0x003` 初赛 — ✅ RV64 376.025s、LA64 373.063s；两架构
  QEMU exit 0、required marker 完整、forbidden marker 为空、无 timeout。
- 两架构 `mutation_detected=false`；before/after tracked diff SHA-256 均为
  `60126f026963195ce3851d330ea2c9bb419523cc838f61a680b9ce8032a3f66d`。
- 功能摘要保持 basic 21/21、busybox 107/108；只有既有 `busybox kill 10` 非满分项。

**Notes:** 没有关闭或放宽 mutation 检查；修复的是产生污染的工具目标。该提交只处理测试准备
幂等性，不包含 WaitQueue 或信号语义改动。

**Related:** `docs/08_testing/README.md`、
`docs/Work_Log/evidence/2026-08-03/develop-batch4-patchelf-idempotence-summary.md`

<a id="2026-08-03-2204-develop-batch5-signalfd"></a>
## 22:04 — develop Batch 5 signalfd 动态 sighand 等待域

**Date:** 2026-08-03 22:04 +08:00
**Author:** AI:GPT/Codex + DeepSeek | Human:Pneuma
**Tags:** [area:smp, area:signal, area:vfs, type:integration, type:fix, risk:concurrency]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** signalfd 的等待队列不再固化在 fork 可共享的 inode/File 中。`Sighand` 持有通知域，
VFS 通过 `ReadWaitSource::CurrentSighand` 在 read/poll 时动态解析；普通 fork 得到新队列，
`CLONE_SIGHAND` 才共享。线程 private 与进程 shared pending 都先在权威锁内发布、解锁后通知。
同步修复用户库把四参数 wait4 错接到三参数 wrapper 的 RV64 ABI 问题。

**Files:**

- `os/src/task/{process.rs,signal/}` — sighand 通知域与锁外 producer 通知。
- `os/src/fs/vfs/`、`os/src/syscall/fs/` — 动态读等待源和通用阻塞路径接入。
- `os/src/syscall/process/signal.rs` — signalfd 等待源、mask 规范化和更新通知。
- `user/src/syscall.rs`、`user/src/bin/regression/` — wait4 第四参数置零和永久 signalfd 回归。
- signal/VFS/锁序/测试/SMP 计划、`AGENTS.md` 与两份复用经验同步。
- `docs/Work_Log/evidence/2026-08-03/develop-batch5-signalfd-summary.md` — 冻结证据。

**Verification:**

- Linux 6.6 `fs/signalfd.c` 与 signalfd(2) 静态对照 — ✅ wait/poll 使用 current sighand；
  mask 更新和信号入队通知 sighand，fork 继承 epoll registration 的限制已记录。
- DeepSeek max 最终只读审查 — ✅ 动态 owner、fork/CLONE_SIGHAND、锁序、原始指针生命周期
  和 VFS read/poll 覆盖未发现阻塞项；无条件通知与 Linux 生产者语义一致。
- Docker 双架构 `CORE_NUM=8` L4 regression — ✅ RV64 143.427s、LA64 142.620s；两架构
  7/7、`online_mask=0xff`、signalfd blocking/inherited 子项与 clone_vm_second_slot 全部 PASS。
- 两项退出码均 0，无 forbidden marker、panic、fatal trap 或 timeout；测试前后 source
  fingerprint 一致，`mutation_detected=false`。
- 同一生产内核在本批早期的 Docker 双架构 normal build 与 `mask=0x003` 初赛均通过；
  之后仅修复用户态 wait4 wrapper 和回归用例，不重复运行 12 分钟初赛矩阵。

**Notes:** 首轮 RV64 signalfd 与 clone probe 同时报 `EFAULT`，详细日志显示实际 signal 数据已
到达但 wait/reap 异常。对照 LA64 后确认三参数 wrapper 只在 LA 汇编桥中偶然清空第 4 参数；
改用 `syscall4(..., rusage=0)` 后同一 RV64 回归转绿。该问题不是 vfork、TLB 或 signalfd
通知竞态。epoll 在 fork 前捕获的 sighand 队列不会自动改绑 child，保持 Linux 已记录语义。

**Related:** `docs/03_fs/special-fds.md`、`docs/05_process/signal.md`、
`docs/Work_Log/evidence/2026-08-03/develop-batch5-signalfd-summary.md`
