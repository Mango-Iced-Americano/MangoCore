---
date: 2026-08-05
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-05

<a id="2026-08-05-batch-e-native-ext4-inode-transaction"></a>
## Batch E — native ext4 canonical inode snapshot transaction

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:fs, area:smp, type:bugfix]
**Mango Gate:** skill loaded=yes; references=none — 已知静态并发缺口的定向修复，不是运行时故障调试。

**Summary:** 将 native ext4 的 per-wrapper `io_txn` 提升为 `Ext4FileSystem` 按 inode 号 canonicalize 的 Weak transaction registry。完整 inode snapshot 的读取、修改和 commit 现在在同一个 canonical transaction 内串行，避免 data EOF/extent 提交与 metadata/nlink 提交互相覆盖。

**Files:**
- `os/src/fs/ext4/{ext4fs.rs,layout.rs,ext4_inode.rs}` — 增加 registry/全序事务 helper，wrapper 使用共享 Arc，并将 writeback API 私有化为 `commit_inode_snapshot*`。
- `os/src/fs/ext4/{file.rs,direntry.rs,balloc.rs,extent.rs,ialloc.rs}` — 使 native create/link/unlink/rmdir/rename、直接 write/truncate、extent/block 回收与 legacy directory removal 的 commit 服从 canonical transaction。
- `docs/{01_architecture/lock-order.md,10_plan/fs-net-smp-execution.md,10_plan/fs-net-smp-adaptation.md}` — 同步 canonical transaction 与多 inode 锁序。

**Verification:**
- 源码路径审阅：`write_back_inode*` 已替换为仅 ext4 模块可见的 `commit_inode_snapshot*`；所有现有 commit 调用由 canonical transaction 上层路径覆盖。
- Rust LSP diagnostics — ⏭ not run：当前 workspace 未配置可用 Rust LSP。
- `make` / `cargo` / QEMU — ⏭ not run：按协调者指令由其串行执行。

**Notes:** 固定锁序为 `rename_gate → parent dir_gate(s) → victim dir_gate(s) → inode_txn(s, inode 升序) → PageCache op_gate → PageEntry.data → short cache lock`；registry lock 只用于 upgrade-or-create，绝不跨 transaction guard。`os/src/task/manager.rs` 是既有未触及 diff。无需更新 AI-Usage-Report：本会话模型已在工具清单中，Oracle 审计结论由任务输入提供，未引入新的 AI 工具或模型。

<a id="2026-08-05-batch-d-8core-gate"></a>
## Batch D — §8.2 dual-architecture 8-core competition gate

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:smp, area:fs, area:net, type:verification]
**Mango Gate:** skill reused=yes; references=none — 正式 gate 执行，不是性能或故障调试。

**Summary:** 对 Batch D PortRegistry transactional auto-bind 的普通用户网络路径改动，完成正式 `mask=0x003`、`ltp_libc=both`、`CORE_NUM=8` 双架构 derived-competition 非回归门禁。

**Files:**
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-batchD-gate-*` — source fingerprint、串行 build/QEMU serial、tools-disk P2 hexdump、judge、validation、命令清单及 verdict。
- `docs/Work_Log/2026-08-05.md` — 记录本 gate 结论；未修改生产源码。

**Verification:**
- `CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` — ✅ Docker exit 0。
- `CORE_NUM=8 make kernel ARCH=la64 PROFILE=normal` — ✅ Docker exit 0，严格在 RV64 后执行。
- RV64 `CORE_NUM=8 make -C os rv64-derived-run` — ✅ exit 0；raw/semantic `312/314`；允许 failure 仅两组 `busybox kill 10=0/1`。
- LA64 `CORE_NUM=8 make -C os la64-derived-run` — ✅ exit 0；raw/semantic `308/314`；允许 failure 为两组 `test_brk=1/3` 与两组 `busybox kill 10=0/1`。
- 两架构：tools-disk P2 `FAT32   ` + `55aa`、`configured=8`、`online_mask=0xff`、四组 exit 0、judge 314 点、`test_mount`/`test_umount` 均 5/5 — ✅。
- LSP diagnostics — N/A：仅 Markdown/evidence，未修改 Rust 源文件。

**Notes:** LA64 `tools-disk-la` target 的实际工具盘产物名为 `disk-la.img`；并不存在 `disk.img`。未调用 tools-disk rebuild。无需更新 AI-Usage-Report：未使用新 AI 工具/模型或产生新的关键 AI 辅助发现。

<a id="2026-08-05-batch-d-portregistry-autobind"></a>
## Batch D — PortRegistry transactional auto-bind

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:net, type:bugfix, smp]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 所有 INET auto-bind 改为通过 per-netns `PortRegistry` 的 reservation 事务，补齐未连接 UDP 首次 `sendto`/`sendmsg` 的源端点建立。

**Files:**
- `os/src/net/socket/inet/common/{port.rs,port/registry.rs,address.rs}` — 增加 `ensure_auto_bound`/`AutoBindPurpose`，删除裸 ephemeral 候选器及无 socket owner 的端口分配。
- `os/src/net/socket/{mod.rs,inet/stream/{mod.rs,inner.rs,lifecycle.rs},inet/datagram/udp.rs}` — Socket trait 端点协商、TCP/UDP 状态机移除内部 auto-bind，并保持 reservation Drop 释放。
- `os/src/net/syscall/{connect.rs,listen.rs,sendto.rs,sendmsg.rs}` — 在持有 socket Arc 时建立 transactional auto-bind；UDP 首发在进入 smoltcp/本地交付前获得本地端点。
- `docs/06_net/{inet-common.md,udp.md,socket-trait-and-fd.md,syscall-layer.md}` — 同步端口事务、trait 与首发语义。

**Verification:**
- `git diff --check` — ✅
- Rust LSP diagnostics — ⏭ not run: workspace 未配置 `.opencode/.codex` LSP，当前环境无 `rustfmt`。
- `make` / `cargo` / QEMU — ⏭ not run: 按协调者指令由其串行执行。

**Notes:** 代码审阅确认锁序保持 `N0 reserve → unlock → N1/N2 socket.bind → unlock → N0 commit/abort`；无 faultable uaccess 位于 registry 临界区。`os/src/task/manager.rs` 为预存未触及 diff。无需更新 AI-Usage-Report：本会话模型已列入工具清单，未引入新工具或模型。
