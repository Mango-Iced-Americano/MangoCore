---
date: 2026-08-05
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-05

<a id="2026-08-05-net-worker-irq-on-window"></a>
## P0-3：CPU0 网络 worker 在 IRQ-on 窗口执行同步 VirtIO TX + AF_PACKET raw-egress 收口

**Date:** 2026-08-05（收尾于 2026-08-06，记录归入当日）
**Author:** AI:Sisyphus-Junior/deepseek-v4-flash
**Tags:** [area:net, area:smp, area:driver, type:concurrency-fix, type:ktest]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md（ktest 窗口/断言模式）、debugging-patterns.md — 中断窗口 + worker 调度边界为 L3 改动，方案已由 Oracle 裁决。

**Summary:** 消除 CPU0 网络 worker 在 IRQ-off 下同步走 VirtIO 发送的死锁风险：worker 的真实扫描包进 `with_local_interrupts_enabled` 的 IRQ-on 窗口（hard IRQ 仍只发布原子状态），窗口关闭后才发布 completion/唤醒并 `run_task_safe_point()` 兑现 timer/IPI deferred 状态；新增 `physical_poll_active` 标记与 `VirtIONetWrapper::transmit` 生产断言；AF_PACKET 改为经每栈有界 raw-egress 队列由 worker 发送；boot 期 DHCP 紧循环探针（IRQ-off 直接 `eth_iface.poll`）替换为持久 DHCP socket 由 worker 异步推进（与 develop a0a37247 同一根因修复，不迁移其全局 deferred TX 队列）。

**Files:**
- `os/src/hal/arch/riscv/sbi.rs` / `os/src/hal/arch/loongarch64/sbi.rs` — 迁移 `pub fn irq_enabled() -> bool`（rv64 `sstatus::read().sie()`，la64 `CrMd::read().is_interrupt_enabled()`），移植自 develop a0a37247。
- `os/src/hal/arch/mod.rs`、`os/src/hal/arch/loongarch64/mod.rs`、`os/src/hal/mod.rs` — 导出链补 `irq_enabled`。
- `os/src/net/config.rs` — ① `NetPollControl` 增 `poll_active: AtomicBool` + `physical_poll_active()`；② `net_poll_worker` 每轮：IRQ-off 清门/快照 → 置 poll_active + ENTER → `with_local_interrupts_enabled(|| poll_each_stack_bounded())` → EXIT/清 poll_active → `completed.store` + completion wake_all → `run_task_safe_point()`（全部 N2/VirtIO/event 锁释放后）；③ `DeviceStackInner` 增 `raw_egress: VecDeque<Vec<u8>>`（同 N2 锁域），`transmit_on_stack` 改为 push + `request_poll()`，满 32 帧返回 `ENOBUFS`，`poll_each_stack_bounded` 在 `try_poll_stack` 前 drain；④ 持久 DHCP socket 取代 boot 紧循环探针（`capture_dhcp_event`/`commit_dhcp_event` 由 worker 提交）；⑤ 静态计数器 `NET_POLL_WINDOW_ENTER/EXIT`、`VIRTIO_TX_ENTER/COMPLETE/CPU_MASK/BYTES`。
- `os/src/drivers/net/virtio_net.rs` — `transmit` 进入 `net.send` 前生产 `assert!(irq_enabled() && cpu_id==BOOT_CPU_ID && physical_poll_active())`，并递增 TX 计数器。
- `os/src/kernel_tests/net_smp.rs` — 新增 `net_smp::virtio_tx_window_counters`（跑一轮 worker 窗口，断言 `ENTER==EXIT>0`、`TX_ENTER==TX_COMPLETE`、CPU mask 只含 bit0；loopback-only 环境优雅 SKIP）。

**Verification:**
- `CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` — ✅ exit 0（无新增 warning，grep 确认改动文件零 warning）。
- `CORE_NUM=8 make kernel ARCH=la64 PROFILE=normal` — ✅ exit 0（严格在 RV64 后串行）。
- RV64 `CORE_NUM=4 make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=net_smp` — ✅ 协调者独立重跑 `# results: 9 passed, 1 skipped, 0 failed, 10 total`；原有 9 个测试全过，`virtio_tx_window_counters` 在 loopback-only 下按设计 SKIP（窗口不变量已验证），0 panic。（子代理首报 7/3 计数有误，以协调者复跑为准。）
- LA64 同命令 — ✅ 协调者独立重跑 `# results: 10 passed, 0 skipped, 0 failed, 10 total`；**`virtio_tx_window_counters` 真实 PASS**：la64 ktest 挂载了 PCI virtio-net（`[net] DHCP configured eth0=10.0.2.15 gateway=10.0.2.2`），VirtIO TX 在 IRQ-on 窗口内发生、断言未触发、计数器一致（ENTER==EXIT、TX_ENTER==TX_COMPLETE、CPU mask 仅 bit0）。
- 协调者 §8.2 双架构 8 核门禁（mask=0x003）：RV64 `derived-run` exit 0、judge `TOTAL 312/314`（与基线一致，test_mount/test_umount 正常）；LA64 exit 0、judge `TOTAL 308/314`（与基线一致）；两架构 `[net] DHCP configured eth0=10.0.2.15` 均经 worker 异步 DHCP 路径成功，test-runner 正常推进到 clock sync，全程 0 panic/assert。
- 其余 ktest 组回归（协调者复跑）：RV64 与 LA64 均为 `mm` 5/5、`ext4` 8/8、`fs_smp` 7/7、`fs_fat_smp` 1/1。

**Notes:** boot DHCP 探针移除是断言成立的必要前提（IRQ-off 物理发送本就有死锁风险，develop a0a37247 用全局 deferred TX 队列、本工作包用"worker 独占 IRQ-on 物理发送"解决，用户明确不迁移第二套全局队列）。hard IRQ 路径（`kick_from_irq`、timer/IPI handler）仍只发布原子状态，窗口内持锁不被重入。`transmit_on_stack` 的 push 与 worker 的 drain 在同一 `DeviceStackInner` 锁域互斥。无新 AI 工具/模型，AI-Usage-Report 无需更新。

<a id="2026-08-05-fat32-cluster-table-smp-ktest"></a>
## FAT32 簇表并发 ktest：卷级 mutation 锁合并验证 + 挂载路径根因修复

**Date:** 2026-08-05（工作包收尾于 2026-08-06 凌晨，记录归入当日）
**Author:** AI:Sisyphus-Junior/deepseek-v4-flash
**Tags:** [area:fs, area:fat32, area:smp, area:ktest, type:concurrency-fix]
**Mango Gate:** skill loaded=yes; references=none — 已知并发缺口验证 + 编译期挂载路径修复，不是运行时故障调试。

**Summary:** 完成 FAT32 簇表并发 ktest 工作包：验证并修正 `bitmap.rs` 卷级 `Mutex<FatMutationState>` 锁合并（hint+vacant_clus 单临界区，alloc/free 互斥），新增 `fs_fat_smp` 零盘 ktest 组（CPU1/CPU2 双用户探针经 syscall 真实驱动 alloc/free → remount → 簇表完整性扫描）。过程中发现一个**既存**问题并根因修复：`BPB::is_valid/data_sector_count` 只认 `tot_sec32` 与 `>=66625` 簇，1 MiB mkfs.vfat 卷（tot_sec16=2048、1984 簇）无法挂载；同时把 `last_and_unused`/`unused_not_last` 读端与写端（`as_bytes_mut()[0]`）统一为直接读首字节，消除对 union 字段重叠的隐式依赖。

**Files:**
- `os/src/fs/fat32/bitmap.rs` — 锁合并补全：vacant 缓存复用路径同样先写候选 EOC 再链接前驱（与扫描路径一致，维持"链上绝无 FREE 簇"不变量）；`free()` 断链断言改为校验 `cluster_list.last()`（调用方 tail-first pop，首项=链末簇），释放循环断言只校验链末簇 next 为 EOC（tail-first 释放的正常形态），并新增 `max_cluster_exclusive()` 供 checker 使用。
- `os/src/fs/fat32/layout.rs` — ① `BPB::data_sector_count` 支持 `tot_sec16` 回退（FAT 规范：总扇区 <65536 时 tot_sec32=0），饱和减防下溢；`is_valid` 放宽为 FAT32 标记判定（root_ent_cnt==0、fat_sz16==0、fat_sz32>0、簇数>0），不再要求 `tot_sec16==0`/`>=66625`——1 MiB 测试卷（tot_sec16=2048）因此可挂载。② `last_and_unused`/`unused_not_last` 读端统一为 `as_bytes()[0]`（写端 `as_bytes_mut()[0]` 落 0x00/0xE5），与写端显式一致；标准布局下该偏移与旧 `long_entry.ord` 等价，改动消除对 union 字段重叠的隐式依赖，无行为回归。
- `os/src/kernel_tests/mem_block.rs`（新）— `MemBlockDevice`（有界内存块设备）+ `from_initramfs_file`（initramfs 文件装入内存块设备，容量 max(镜像,1MiB)）。
- `os/src/kernel_tests/fs_fat_smp.rs`（新）— `fs_fat_smp::cluster_alloc_free_consistency`：`from_initramfs_file("/tools/test-fat.img")` → `BlockSizeAdapter`(512B) → `EasyFileSystem::open` → 挂载 `/mnt/fat-smp` → CPU1 `FatAllocWriter`（openat(O_CREAT|WRONLY|TRUNC)→ftruncate(2048)→write→close，12 轮）与 CPU2 `FatAllocFree`（openat→ftruncate(2048)→ftruncate(512)→close→unlinkat，12 轮）并发 → flush 根目录项 → 丢弃 mount/fs → 同一块设备 remount → 遍历簇表（环/双归属/越界/孤儿簇校验）。缺镜像时返回 `SKIP:`。
- `os/src/kernel_tests/probe.rs` — 新增双架构 `FatAllocWriter`/`FatAllocFree` 探针汇编（退出码 1=openat、2=ftruncate、3=write 短写/unlinkat）与 `ProbeResult` 变体。
- `os/src/kernel_tests/mod.rs` — 注册 `("fs_fat_smp", kt_fs_fat_smp::tests())`。
- `os/src/kernel_tests/ext4/block_device.rs` — `TestMemBlock` 替换为共享 `MemBlockDevice`。
- `scripts/build_initramfs.sh` — 生成 1 MiB FAT32 `/tools/test-fat.img`（mkfs.vfat 缺失时优雅跳过）。

**Verification:**
- `CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` — ✅ exit 0。
- `CORE_NUM=8 make kernel ARCH=la64 PROFILE=normal` — ✅ exit 0（严格在 RV64 后串行）。
- RV64 `CORE_NUM=4 make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=fs_fat_smp` — ✅ 连跑 6/6 `KTEST RESULT: PASS`（1 passed, 0 failed）。
- LA64 同命令 — ✅ 连跑 3/3 `KTEST RESULT: PASS`。
- 修复前：RV64 ktest 约 2/5 运行 `freer_exit=3`（unlinkat ENOENT）。回退 `layout.rs` 后 ktest panic `data cluster count exceeds FAT capacity`，证明 `is_valid`/`data_sector_count` 放宽是 1 MiB 卷挂载的必需前提；`last_and_unused`/`unused_not_last` 读首字节与写端自洽，独立实验（`#[repr(align(8))]` union + packed 成员）确认标准布局下新旧读端均读偏移 0，无行为差异。修复后 0 失败。

**Notes:** ktest 探针全程经 syscall 驱动 FAT alloc/free，runner 不直接调用 inode 方法。挂载路径使用 `BlockSizeAdapter` 把平台 4096B 块适配到 FAT 原生 512B 扇区，与生产 `sys_mount` 同层。锁序（inode_lock → file_content → fat_mutation → 块设备）由既有调用链保证，探针并发验证通过。`06-fat32-persistence-and-inode-identity.md` code_paths 含 bitmap.rs，但其内容（block_id 语义 postmortem）与本次锁合并不冲突，无需更新；无需更新 AI-Usage-Report（未引入新 AI 工具/模型）。

<a id="2026-08-05-ktest-skip-clearance"></a>
## ktest SKIP 占位清零：五个占位全部兑现或处置

**Date:** 2026-08-05
**Author:** AI:Sisyphus（本会话协调者）
**Tags:** [area:ktest, area:smp, type:test-infra]
**Mango Gate:** skill loaded=yes; references=none — 测试基建与断言兑现，不是性能或故障调试。

**Summary:** 用户要求 ktest 组中 5 个静态 SKIP 占位（`KernelTest::skip` 使用 `skipped_test_stub`，从不执行函数体）必须"移组 / 改成真正有用的测试 / 删除"。逐一兑现：2 个实现为真实并发用户探针测试、1 个移入正确分组实现、1 个实现为纯内核路由重验测试、1 个按用户决定删除。零盘 ktest 从此 0 SKIP。

**Files:**
- `os/src/kernel_tests/probe.rs` — 新增 4 组双架构用户探针汇编：`TmpfsLookup`（循环 openat 直到 ENOENT）、`TmpfsUnlink`（unlinkat）、`EventFdWrite`（eventfd write 8 字节）、`EpollEdge`（epoll_create1/ctl/pwait ×2/read，失败退出码 2-6 区分阶段）；新增 `build_epoll_edge_reader`/`build_eventfd_writer`/`build_probe_with_fd` 构造器。
- `os/src/kernel_tests/fs_smp.rs` — `tmpfs_lookup_unlink_generation` 实现为 CPU1 循环 lookup + CPU2 unlink 并发测试（unlink 恰好一次成功、stale lookup 立即 ENOENT、最终树无残留）；`truncate_tail_zero_after_extend` 移出本组。
- `os/src/kernel_tests/ext4.rs` — `ext4::truncate_tail_zero_after_extend` 移入并实现为顺序数据完整性测试（extend 零填充 → truncate 尾零 → 已写数据保持），使用零盘 `FsSmpCacheInode`。
- `os/src/kernel_tests/net_smp.rs` — `route_handle_reuse_rejected` 实现为纯内核测试（veth pair + `add_routed_socket_on`/`routed_ifindex`/`remove_routed`，验证句柄单调不复用、移除后不 alias 新 socket）；`epollet_concurrent_edge` 实现为双探针 EPOLLET edge 测试（writer 写一次 eventfd，reader 第一次 pwait 恰好 1 事件、数据未 drain 时第二次 pwait 必须返回 0）；`tcp_dual_sender_exact_bytes` 按用户决定删除（TCP 握手时序在裸探针下脆弱，数据完整性已由用户态 iperf/netperf 覆盖）。
- `docs/10_plan/fs-net-smp-execution.md` — 同步 close-out 后的零盘 ktest 计数（fs_smp 7P/0S、net_smp 9P/0S、ext4 8P/0S）。

**Verification:**
- 双架构串行 build：`CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` 与 `ARCH=la64` 均 exit 0。
- 双架构 `CORE_NUM=4` focused ktest 全绿：RV64 与 LA64 均为 `fs_smp` 7/7、`net_smp` 9/9（含新 `route_handle_reuse_rejected`、`epollet_concurrent_edge`）、`ext4` 8/8（含移入的 truncate 测试）、`mm` 5/5；**0 skipped**。
- 调试过程发现并修正：epoll syscall 号为项目自定义（`SYSCALL_EPOLL_CREATE1=20/CTL=21/PWAIT=22`，非标准 291/292/293）；首次实现用错标准号导致 epoll_create1 返回负值，探针失败退出码区分法定位后修正。

**Notes:** 探针失败退出码按阶段区分（2=create1、3=ctl、4=首次 pwait 非 1、5=二次 pwait 非 0、6=read 非 8），便于将来回归定位。epoll 系列 syscall 号与 Linux 标准不同是项目既有 ABI 选择，探针常量须与 `syscall_id.rs` 保持一致。无新增可泛化调试模式（epoll 自定义编号已属项目 ABI 事实，记录于代码注释）；`AI-Usage-Report.md` 无需更新。

<a id="2026-08-05-batch-e-native-ext4-inode-transaction"></a>
## Batch E — native ext4 canonical inode snapshot transaction

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:fs, area:smp, type:bugfix]
**Mango Gate:** skill loaded=yes; references=none — 已知静态并发缺口的定向修复，不是运行时故障调试。

**Summary:** 将 native ext4 的 per-wrapper `io_txn` 提升为 `Ext4FileSystem` 按 inode 号 canonicalize 的 Weak transaction registry。完整 inode snapshot 的读取、修改和 commit 现在在同一个 canonical transaction 内串行，避免 data EOF/extent 提交与 metadata/nlink 提交互相覆盖。

**Files:**
- `os/src/fs/ext4/{ext4fs.rs,layout.rs,ext4_inode.rs}` — 增加 registry/全序事务 helper，wrapper 使用共享 Arc，并将 writeback API 私有化为 `commit_inode_snapshot*`。
- `os/src/fs/ext4/{file.rs,direntry.rs,balloc.rs,extent.rs,ialloc.rs}` — 使 native create/link/unlink/rmdir/rename、直接 write/truncate、extent/block 回收与 legacy directory removal 的 commit 服从 canonical transaction。
- `docs/{01_architecture/lock-order.md,10_plan/fs-net-smp-execution.md,10_plan/fs-net-smp-adaptation.md}` — 同步 canonical transaction 与多 inode 锁序。

**Verification:**
- 源码路径审阅：`write_back_inode*` 已替换为仅 ext4 模块可见的 `commit_inode_snapshot*`；所有现有 commit 调用由 canonical transaction 上层路径覆盖。
- Rust LSP diagnostics — ⏭ not run：当前 workspace 未配置可用 Rust LSP。
- `make` / `cargo` / QEMU — ⏭ not run：按协调者指令由其串行执行。

**Notes:** 固定锁序为 `rename_gate → parent dir_gate(s) → victim dir_gate(s) → inode_txn(s, inode 升序) → PageCache op_gate → PageEntry.data → short cache lock`；registry lock 只用于 upgrade-or-create，绝不跨 transaction guard。`os/src/task/manager.rs` 是既有未触及 diff。无需更新 AI-Usage-Report：本会话模型已在工具清单中，Oracle 审计结论由任务输入提供，未引入新的 AI 工具或模型。

<a id="2026-08-05-batch-d-8core-gate"></a>
## Batch D — §8.2 dual-architecture 8-core competition gate

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:smp, area:fs, area:net, type:verification]
**Mango Gate:** skill reused=yes; references=none — 正式 gate 执行，不是性能或故障调试。

**Summary:** 对 Batch D PortRegistry transactional auto-bind 的普通用户网络路径改动，完成正式 `mask=0x003`、`ltp_libc=both`、`CORE_NUM=8` 双架构 derived-competition 非回归门禁。

**Files:**
- `docs/Work_Log/evidence/2026-08-04/fs-net-smp-batchD-gate-*` — source fingerprint、串行 build/QEMU serial、tools-disk P2 hexdump、judge、validation、命令清单及 verdict。
- `docs/Work_Log/2026-08-05.md` — 记录本 gate 结论；未修改生产源码。

**Verification:**
- `CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` — ✅ Docker exit 0。
- `CORE_NUM=8 make kernel ARCH=la64 PROFILE=normal` — ✅ Docker exit 0，严格在 RV64 后执行。
- RV64 `CORE_NUM=8 make -C os rv64-derived-run` — ✅ exit 0；raw/semantic `312/314`；允许 failure 仅两组 `busybox kill 10=0/1`。
- LA64 `CORE_NUM=8 make -C os la64-derived-run` — ✅ exit 0；raw/semantic `308/314`；允许 failure 为两组 `test_brk=1/3` 与两组 `busybox kill 10=0/1`。
- 两架构：tools-disk P2 `FAT32   ` + `55aa`、`configured=8`、`online_mask=0xff`、四组 exit 0、judge 314 点、`test_mount`/`test_umount` 均 5/5 — ✅。
- LSP diagnostics — N/A：仅 Markdown/evidence，未修改 Rust 源文件。

**Notes:** LA64 `tools-disk-la` target 的实际工具盘产物名为 `disk-la.img`；并不存在 `disk.img`。未调用 tools-disk rebuild。无需更新 AI-Usage-Report：未使用新 AI 工具/模型或产生新的关键 AI 辅助发现。

<a id="2026-08-05-batch-d-portregistry-autobind"></a>
## Batch D — PortRegistry transactional auto-bind

**Date:** 2026-08-05
**Author:** AI:Sisyphus-Junior/openai-gpt-5.6-terra
**Tags:** [area:net, type:bugfix, smp]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 所有 INET auto-bind 改为通过 per-netns `PortRegistry` 的 reservation 事务，补齐未连接 UDP 首次 `sendto`/`sendmsg` 的源端点建立。

**Files:**
- `os/src/net/socket/inet/common/{port.rs,port/registry.rs,address.rs}` — 增加 `ensure_auto_bound`/`AutoBindPurpose`，删除裸 ephemeral 候选器及无 socket owner 的端口分配。
- `os/src/net/socket/{mod.rs,inet/stream/{mod.rs,inner.rs,lifecycle.rs},inet/datagram/udp.rs}` — Socket trait 端点协商、TCP/UDP 状态机移除内部 auto-bind，并保持 reservation Drop 释放。
- `os/src/net/syscall/{connect.rs,listen.rs,sendto.rs,sendmsg.rs}` — 在持有 socket Arc 时建立 transactional auto-bind；UDP 首发在进入 smoltcp/本地交付前获得本地端点。
- `docs/06_net/{inet-common.md,udp.md,socket-trait-and-fd.md,syscall-layer.md}` — 同步端口事务、trait 与首发语义。

**Verification:**
- `git diff --check` — ✅
- Rust LSP diagnostics — ⏭ not run: workspace 未配置 `.opencode/.codex` LSP，当前环境无 `rustfmt`。
- `make` / `cargo` / QEMU — ⏭ not run: 按协调者指令由其串行执行。

**Notes:** 代码审阅确认锁序保持 `N0 reserve → unlock → N1/N2 socket.bind → unlock → N0 commit/abort`；无 faultable uaccess 位于 registry 临界区。`os/src/task/manager.rs` 为预存未触及 diff。无需更新 AI-Usage-Report：本会话模型已列入工具清单，未引入新工具或模型。
