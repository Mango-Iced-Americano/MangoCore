# 2026-07-27 Work Log

## 2026-07-27

### SMP-P2.5-B15：切栈后调度所有权交接与统一唤醒

**根因与设计收敛：**

- 旧 B15 草案让通用 `TaskManager::add()` 同时接受 `New` 和 `Running`，yield
  又在 `__switch` 之前清空 `Processor.current` 并把仍使用自身内核栈的任务改成
  `Queued`。这会在真正多核后允许同一 TCB 被另一 CPU 提前取走。
- 最终方案没有引入 `SwitchAction`、claim token、`WakePending` 或 `Exiting`，只在
  `New/Queued(cpu)/Running(cpu)/Blocked/Zombie` 基础上增加必要瞬态
  `Blocking(cpu)`，表示“已经登记睡眠意图，但任务仍由当前 CPU 执行”。
- `Processor.current` 保留到 `__switch` 真正返回 idle 栈；idle 随后统一调用
  `finish_switch_out()`，根据最终状态提交 `Running -> Queued`、
  `Blocking -> Blocked` 或把 Zombie Arc 移入回收队列。

**实现与不变量：**

- 删除通用调度 `add_task()`，以 `publish_task()` 明确限定
  `New -> Queued(CPU0)`；保留与旧代码语义一致的 `fetch_task(cpu)`，并把内部入口
  统一命名为 `publish_task/begin_interruptible_sleep/finish_switch_out/fetch_task`。
- yield 不再自行改状态或入队；block 在 `TASK_MANAGER` 锁内执行
  `Running(cpu) -> Blocking(cpu)` 并登记 interruptible registry。早到 wake 执行
  `Blocking(cpu) -> Running(cpu)` 且不入队，晚到 wake 执行
  `Blocked -> Queued(CPU0)` 并加入队首，两者由同一个 CAS 循环裁决。
- timer、signal、WaitQueue、Completion、legacy timeout 和 realtime timer 均不再
  在外部预写调度状态；统一 wake 返回本次是否赢得唤醒权。必成功的 publish、fetch
  和 switch-out 迁移在所有构建中 fail-stop，删除 `smp_debug` 和非法迁移计数。
- exit 路径只借用 `Processor.current` 持有的 TCB，不 clone 一个永远不会返回析构的
  本地 Arc；只有切回 idle 后才把 retained Arc 转入 zombie queue，避免提前释放
  当前内核栈，也避免退出栈帧泄漏引用。
- `scheduler_state_has_unique_owner` 只调用生产 API，覆盖提前取消 Blocking、完整
  Completion block/wake、helper publish/fetch/zombie 和重复 wake；删除直接伪造
  `try_sched_transition` 的测试代码及所有生产实验字段。

**审查与已知技术债：**

- 冻结源码只读复核结论为 `ACCEPT WITH CHANGES`，未发现实际并发 Bug；采纳
  `try_sched_transition` 的 `#[must_use]` 和 ktest 延迟 live-count/rusage 注释建议。
- 旧 nice-aware `pop_fair_ready()` 仍在全局 `TASK_MANAGER` 锁内读取
  `task.inner.sched_vruntime/sched_nice`。当前不存在反向同时持锁路径，因此没有形成
  锁环，但这是 Phase 3 拆 per-CPU runqueue 时必须消除的既有技术债。
- 当前仍只有 CPU0 进入 legacy scheduler，AP 只运行 SMP 服务循环；本批没有实现
  per-CPU Processor/runqueue、远程 enqueue、任务迁移或跨核退出协议。

**Docker 验证：**

- 基线 HEAD：`4dbae121cb35502a660631c0ae545ececcfd5861`；最终可执行源码差异
  SHA-256：`f5de06284a7f3bab618da6669358ee7257f1506a46093afbc5bb44b1fdbe7ef7`。
- RV64 normal kernel build：退出 0，136.666 秒；LA64 normal kernel build：
  退出 0，138.537 秒。两架构严格串行并使用 `nightly-2026-05-10`。
- RV64 `CORE_NUM=4 KTEST=smp KREPEAT=2`：退出 0，140.055 秒；LA64 同配置：
  退出 0，129.526 秒。两者均报告 `online_mask=0xf`、TAP `1..19`、
  `scheduler_state_has_unique_owner` 在第 9/18 项 PASS、terminal STOP 仅第 19 项，
  最终 19 passed / 0 failed 与 `[KTEST RESULT: PASS]`。
- RV64 `CORE_NUM=4 KTEST=waitqueue KREPEAT=1`：退出 0，138.692 秒，4/4 PASS。
  因本地网关首次生成的子任务 ID 超过 64 字符并占用一槽，没有补跑 LA64
  WaitQueue；该失败发生在测试提交前，不是内核失败，也不作为双架构证据。
- 5 个真实 recipe 均无 panic、timeout、forbidden marker 或源码漂移。网关已在
  本地改为稳定短摘要子任务 ID；`cc-codex/` 保持 Git 忽略，不上传协作材料。

**文档、证据与阶段结论：**

- 已同步调度器、TCB、WaitQueue、exit、时间、clone、锁序和 SMP 实施方案；文档
  明确 interruptible registry 是等待登记簿，`Blocking` 与 current 的短暂重叠不代表
  两个 runnable owner。
- 已将本次 GPT/Codex 实现与 DeepSeek 只读审查补入 AI 使用披露；只记录角色、人工
  裁决和验证结论，不提交本地 `cc-codex/` prompt、模型日志或交接产物。
- 本地确定性摘要位于
  `docs/Work_Log/evidence/2026-07-27/smp-b15-summary.md`。
- `SMP-P2.5-B15=pass`；整体 Phase 2.5 仍为 `partial`，下一独立功能是本地
  `TlbBatch` 收口，而不是立即让用户任务上 AP。
- 本包没有产生超出现有 SMP 状态机/锁序文档的新通用内核调试模式，不追加
  workflow references。

### SMP-P2.5-B16：用户 PTE 的本地 TLB 提交边界

**问题与设计选择：**

- 旧实现由 `Vma`、缺页、CoW、mprotect、OOM 和退出路径分散调用
  `PageTable` 的带刷新方法。这种结构无法在 Phase 4 统一接入 active CPU
  mask、generation 和 shootdown ack，也无法用类型边界表达“旧 PTE 失效前不得
  复用物理页”。
- B16 只实现当前 CPU0-only 调度前提下的本地协议，不提前假设远端
  IPI/ack 已存在。`TlbPublication` 因此显式分为
  `Unpublished/LocalOnly/Published`；`Published` 在任何 PTE 写入前 fail-stop，
  防止将 local flush 伪装成 SMP shootdown。
- 实现前对照 RISC-V Privileged ISA、LoongArch ISA 与 Linux LoongArch 主线实现。
  RV64 的 `sfence.vma va, zero` 可对指定 VA 失效所有 ASID 的本 hart 项；
  LA64 `INVTLB 0x3` 失效本核全部 `G=0` 项。由于当前 LA64 ASID 仍归
  TCB，页表对象不能安全使用“当前 ASID + VA”的 `INVTLB 0x5`，故 B16
  保守清除本核全部非全局项。

**实现与安全不变量：**

- 新增 `mm/tlb_batch.rs`，为 map/unmap、block-write、PPN、flags 和 dirty
  修改提供唯一用户页表提交入口。`PageTable` 同步增加 raw/no-flush
  原语和 `flush_tlb_page()`；原有安全方法仍供内核页表路径使用。
- `UserMapper` 改为借用 `TlbBatch`，用户 PTE 的缺页、filemap、CoW、
  fork、mmap/munmap/mprotect/madvise、OOM/swap、exec、trampoline 和 zombie
  清理均经过同一边界。静态审计确认 raw/no-flush 用户写入没有另外调用者。
- batch 只修改一个 VPN 时请求页级刷新，出现第二个不同 VPN 后升级为
  本核全量刷新。RV64 因此保留单页 fault/CoW fast path；LA64 在 MM-owned
  ASID 完成前由 HAL 保守升级为全部非全局项失效。
- unmap 先清 PTE，再把从 VMA 移出的 `Arc<FrameTracker>` 交给
  `defer_frame()`。batch 严格按“PTE write → local flush → frame drop”提交；
  `Drop` 在 `?` 和提前返回时兜底，延迟队列 `try_reserve` 失败时先结束
  当前小批次，只放弃合并刷新效率。
- CoW 替换 PPN 后才延迟旧 frame；若后续 flags 修改失败，先恢复旧
  PPN，再延迟曾短暂出现在 PTE 中的新 frame。映射中途失败的回滚也先
  unmap 已发布 PTE，避免 VMA 已失去 frame 所有权但页表仍指向该页。
- `Frame::zip/swap_out/force_swap_out()` 改为返回被替换的 resident
  `Arc<FrameTracker>`，OOM 路径因此能在 PTE 清除后将 frame 交给 batch。
  `VmPageStore` 新增无分配的范围扫描入口，使批量 discard/unmap 不需为 VPN
  列表再分配内存。
- `release_for_zombie()` 与 exec 旧 MM 回收不再直接清 VMA，而是先通过
  `unmap_all()` 提交 PTE/TLB/frame，之后才释放页表页。LA64 software dirty
  fault 也不再从 token 重建临时页表，而是在权威 `AddressSpace` 锁内提交 batch。

**DeepSeek 只读审查与人工裁决：**

- 实施前审计指出旧单页 unmap 存在“先从 VMA 移除 frame，后清 PTE”的
  生命周期风险，直接促成 `deferred_frames` 设计。
- 冻结源码审查结论为 `ACCEPT`，无 P0/P1；它发现 LA64 旧安全接口的
  `invalidate_page()` 仍使用当前 ASID 精确失效。虽然 B16 用户路径已不经过该
  接口，仍采纳建议，将非内核分支改为本核全部非全局失效，消除未来误用风险。
- 未采纳将“有 deferred frame 却没有 PTE 修改”的 `assert!` 降为
  `debug_assert!`；该条件意味着可能在未失效 TLB 前释放物理页，属于释放构建
  也必须 fail-stop 的安全不变量。

**Docker/QEMU 验证：**

- 基线 HEAD：`8c3da24c605de2104d6ad43de5c4e2372cc9567f`；受测源码差异
  SHA-256：`8c4c89ce05261dd5339b3291811c5b8da7fde782405e1420fcb3b267df40f118`。
  两个 recipe 的 HEAD、tracked diff、status 和 untracked content 指纹在测试前后一致，
  `mutation_detected=false`。
- RV64 `CORE_NUM=1 KTEST=mm KREPEAT=2`：退出 0，126.287 秒，TAP `1..8`，
  8 passed / 0 failed，`local_tlb_batch_map_protect_unmap` 两轮 PASS。
- LA64 同配置：退出 0，133.659 秒，TAP `1..8`，8 passed / 0 failed，
  新用例两轮 PASS。两架构均无 timeout、panic 或 forbidden marker。
- 双架构 PASS 后只同步了 MM 文档，并更正 `page_fault.rs`/`mapper.rs`/`vma.rs`
  的三处模块级注释，说明用户 PTE 经 batch、`PageMapper` 仅保留内核路径。没有再修改
  Rust item、生成代码或测试逻辑，因此没有为纯注释变化重复执行数分钟的
  双架构 QEMU；上述 SHA-256 绑定的是实际受测代码快照。
- 新用例使用生产 API 执行映射、Store fault-in、mprotect 降权、Load/Store
  权限检查、munmap 与同 VPN 重新映射。它证明双架构本地路径可运行，
  但没有直接观测硬件 TLB 内容，也不代替 Phase 4 的 generation/ack、远端 victim
  与 ack 前 frame 不复用测试。

**阶段结论与边界：**

- `SMP-P2.5-B16=pass`；Phase 2.5 的调度所有权与本地 TLB batch 退场条件
  已完成。下一主线阶段是 Phase 3 per-CPU runqueue/current slot，但用户任务
  在远端 shootdown 完成前继续固定 CPU0。
- B16 未实现 active CPU mask、TLB generation、SBI RFENCE/IPI fallback、LA64
  shootdown slot/ack、MM-owned ASID/epoch 或 kernel-global shootdown；这些均保留给 Phase 4。
- 本包没有产生超出项目现有 TLB/生命周期准则的新通用调试模式，不追加
  workflow references。

### B16 补充回归：双架构 8 核初赛 basic + busybox

**范围与执行方式：**

- 使用本地忽略的 `cc-codex` 网关把构建、QEMU 和日志初审交给 DeepSeek；固定
  `effort=max`，严格串行执行 RV64 后 LA64，各运行一次。实际构建与 QEMU 均在
  当前工作树对应 Docker 容器内完成，未提交或上传 prompt、模型日志及测试产物。
- “初赛部分”按仓库 mask 定义收敛为 `mask=0x003`，即 basic 与 busybox；每个
  架构均使用 `CORE_NUM=8`、比赛式单 socket/8 core/单 thread 拓扑、官方 x0 的
  派生副本及本架构 x1 工具盘。未扩展到 `0xFFF` 决赛全量。

**结果与证据：**

- RV64：确定性 recipe PASS，退出 0，430.993 秒；启动日志为
  `configured=8 ... online_mask=0xff`。basic-musl、basic-glibc、busybox-musl、
  busybox-glibc 均到达结束标记且脚本 `exit_code=0`；judge 为 312/314，其中
  两个 busybox 组各有 `busybox kill 10` 一项未匹配。
- LA64：确定性 recipe PASS，退出 0，426.074 秒；启动日志为
  `configured=8 ... online_mask=0xff`。同四组均到达结束标记且脚本
  `exit_code=0`；judge 为 305/314，未满分项为 basic-musl 的 `test_brk`
  1/3、basic-glibc 的 `test_brk` 1/3 与 `test_pipe` 1/4，以及两个 busybox
  组各自的 `busybox kill 10` 0/1。
- 两次运行均无 kernel panic、fatal trap、runner failure、QEMU timeout 或源码
  指纹漂移。DeepSeek 完成日志归纳后由 GPT 再次核对 child `result.json`、完整
  stdout、四组标记、脚本退出码、judge JSON 与失败项列表。

**人工裁决与边界：**

- 两个 recipe 的“PASS”表示 8 核最小启动和初赛组完整执行通过，不等于 judge
  满分。当前没有同一代码快照的 1 核对照运行，因此不能仅凭本次证据断言上述
  丢分均为单核既存问题或与 SMP 无关；LA64 `test_pipe` 的交错输出在单核抢占下
  也可能发生，不能直接归因于多核并行。
- 两架构 normal 启动阶段均出现 APK DNS transient error 和可选 e2fsprogs 安装
  错误，但 basic/busybox 随后完整执行。为使后续 focused 初赛配方不再混入网络
  环境噪声，本地配方已增加 `skip_apk=true`；本次受测日志仍如实记录原始错误。
- AP 当前仍 park，用户任务继续只在 CPU0 调度；本次不证明跨核用户调度、远端
  TLB shootdown、FS/NET 多核并发安全或 Phase 3/4 完成。

### SMP-P3-B17：Per-CPU current 槽与真实 Arc 生命周期

**问题与方案：**

- 旧 `PROCESSOR`、`CURRENT_TASK_PTR` 和伪造的 `current_task_ref() -> &'static TCB`
  依赖“只有 CPU0 调度”的事实。AP 一旦进入调度器，全局 current 会让一个 CPU
  读到另一个 CPU 的任务，裸指针生命周期也无法继续证明。
- 每个 `PerCpu` 现内嵌 `CpuTaskState`，独占本 CPU 的 `Processor`、current `Arc`、
  idle context、PID/TID 快照和诊断 syscall ID。`current_task()` 在本地 processor
  锁内 clone `Arc` 后立即释放锁；panic 路径先验证 CPU-local 地址，再用
  `try_lock()` 非阻塞读取，避免早期 panic 或锁重入产生二次故障。
- PID/TID 在 current 槽存续期间不变，保留 Per-CPU 快照；父 PID、UID/GID、
  PGID/SID 和页表 token 可能动态变化，改读 TCB/PCB 权威原子 hint，删除需要
  setter 同步刷新的全局影子缓存。
- dispatch 先独立获取 `task.inner` 中的上下文指针，再获取 processor 锁发布
  current；锁在 `__switch` 前释放，消除了 `processor -> task.inner` 嵌套。

**生命周期审查与人工裁决：**

- DeepSeek 第一次基于冻结 diff 串行执行双架构 Docker build，两个架构都稳定暴露
  同一组 22 个编译错误：1 个重复 import 和 21 个 `Arc<T>`/`&T` 调用点不匹配。
  它建议恢复一个引用适配层；人工未采纳，因为这会重新引入容易误用的借用外观，
  而且不能表达 current owner 的真实生命周期。最终逐个调用点显式借用 `&task`。
- 编译修复后继续人工审查所有 `!` 返回路径，发现只靠类型检查仍会遗漏本地 `Arc`
  跨 context switch 的引用泄漏：`schedule()` 和架构 `asm!(noreturn)` 不展开旧 Rust
  栈帧。exit/group-exit/ktest zombie、fatal signal、exec 失败和双架构 trap return
  均在不返回边界前显式 drop；`do_signal()` 改为返回真实 `Arc`，不再伪造静态引用。
- DeepSeek 最终只读审查确认：processor 锁不跨 `__switch`，panic 读取可安全降级，
  可变身份读取权威对象，且 source fingerprint 在每个 recipe 前后一致。AI 的首次
  适配层建议没有直接进入代码，最终结论仍由 GPT/Codex 对源码与日志复核后给出。

**Docker/QEMU 验证：**

- 冻结 HEAD：`ddd884448cf0038b888241c8fdc392f7713c8ee8`；受测 tracked diff
  SHA-256：`3c6a3b950fdf8e3563b19f14834679cc5038e999120424a6683994b962098a5b`。
- RV64 normal kernel build：退出 0，126.560 秒；LA64 normal kernel build：
  退出 0，128.362 秒。两架构严格串行并使用 Docker 内 `nightly-2026-05-10`。
- RV64 `CORE_NUM=4 KTEST=smp KREPEAT=2`：退出 0，133.504 秒；LA64 同配置：
  退出 0，129.796 秒。两者均打印 `online_mask=0xf`、TAP `1..19`、
  `scheduler_state_has_unique_owner` 两轮 PASS、19 passed / 0 failed 和
  `[KTEST RESULT: PASS]`；所有 recipe 均 `mutation_detected=false`。
- 最终测试后只同步架构文档与工作日志，没有再修改 Rust 代码，因此没有为纯文档
  变化重复运行双架构 QEMU。lint 与普通用户态回归本批 NOT RUN。

**阶段结论与边界：**

- `SMP-P3-B17=pass`：仓库 Rust 源码不再存在全局 `PROCESSOR`、current-task 裸指针、
  `current_task_ref()` 或身份 refresh helper。
- ready queue、interruptible registry 和 zombie queue 仍由全局 `TASK_MANAGER`
  管理，生产任务仍固定 CPU0，AP 仍不运行普通任务。本批不证明远程 enqueue、迁移、
  用户态信号跨核或 TLB shootdown。
- 下一独立功能 B18 将 ready queue 拆入 Per-CPU，但先让所有生产入队仍明确选择
  CPU0；这样先验证队列归属与锁序，再在后续批次接入 AP scheduler、远程唤醒和 IPI。
- 本包未产生超出当前 SMP 锁序与生命周期文档的新通用调试模式，不追加 workflow
  reference。

### 修复 another_ext4 depth=1 leading hole 的 EIO

**涉及文件：**
- `dependency/another_ext4/src/ext4_defs/extent.rs` — 非空内部 extent 节点在查询首索引前选择左侧子节点；叶节点仍将未覆盖区间返回 ENOENT。
- `os/src/fs/ext4_another/{errno,page_cache,mutations}.rs` — PageCache `read_page()` 日志标为 `op=read`，并保留完整 `Ext4Error`。
- `os/src/kernel_tests/ext4_another/{mod,persistence}.rs` — 覆盖 depth=1 leading-hole 的写入分配与截断路径。

**验证：**
- Docker RV64 KTest：46 passed, 0 failed；新增 leading-hole 写入和截断测试均通过。
- Docker RV64 LTP：gf23、gf26、gf29 在 musl 与 glibc 均 PASS。
- Docker：`make kernel ARCH=rv64 PROFILE=normal`、`make kernel ARCH=la64 PROFILE=normal` ✅（串行）。

**备注：** `rv64-derived-run` 的测试组结束后由 timeout 终止 QEMU，但两套 libc 的目标 gf 用例均已输出 PASS；证据位于 `docs/Work_Log/evidence/2026-07-27/gf-leading-hole-*`。未命中需同步的模块 `code_paths` 文档；AI-Usage-Report 已覆盖本次模型。

### 修复 unlink09 inode flags 与 another_ext4 非覆盖 extent 误报 EIO

**涉及文件：**
- `os/src/syscall/fs/{sys_ioctl,sys_unlinkat}.rs` — regular-file freeze no-op，及 FS_IOC_GETFLAGS/SETFLAGS 与 immutable/append-only unlink EPERM。
- `os/src/fs/ext4_another/{inode,lifetime}.rs` — 在共享 inode lifetime 保存测试所需 inode flags。
- `dependency/another_ext4/src/ext4/data_write.rs` — 将非覆盖 extent 作为 ENOENT，使 buffered-write 回退到分配路径而非误报 EIO。

**验证：**
- Docker RV64 定向 LTP：`unlink09` PASS；修复前 debug trace 将 gf23/gf26/gf29 EIO 定位为 `prepare_buffered_write_with_data`。
- 修改后 RV64 → LA64 串行 kernel build ✅。

**备注：** 聚焦 RV64 QEMU 复测后 `link05`/`unlink09` 均 PASS；两侧 pwrite 行为符合既有 musl-only 已知失败、glibc PASS。`gf23`、`gf26`、`gf29` 仍返回 EIO，`extent_query()` 非覆盖检查补齐后仍未消除，不能验收为已修复；完整串口日志见 `evidence/2026-07-27/gf-extent-coverage-qemu-output.log`。运行时实际 unlink09 失败命令为 `FS_IOC_GETFLAGS/SETFLAGS`，不是 FIFREEZE。

### 修复 another_ext4 硬链接目录扩容时的 metadata gate 冲突

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — `link()` 使用兼容的 direct metadata domain；孤儿重链仍由 `link_inode()` 内部事务处理，目录扩容调用 `alloc_block()` 时不再被外围 exclusive gate 拒绝为 `EAGAIN`。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make kernel ARCH=rv64 PROFILE=normal` ✅。
- Docker `pxy-mangocore-os-dev-1`：以 `ltp_include=link05` 运行 `make rv64-derived-run PROFILE=normal`；musl 与 glibc 均完成 1000 次硬链接，`link05` PASS ✅。

**备注：** 运行时探针确认原失败固定在第 185 次目录项插入：外围 transactional gate 已持有 exclusive 位，而目录新块分配中的 `alloc_block()` 需要 direct gate，因而返回 `EAGAIN`。未命中需同步的模块 `code_paths` 文档；AI-Usage-Report 已列出当前模型，无需更新。

### 修复 PageCache 回写回调重入 I/O gate 死锁

**涉及文件：**
- `os/src/fs/page_cache.rs` — 将单页、范围和预算回写入口拆分为获取 `io_gate` 的公开入口与假定已持锁的内部实现；新增 `writeback_page_locked()`，保留跨写入、回写和截断的串行化。
- `os/src/kernel_tests/page_cache/write_copy.rs` — before-copy 重入回调改为调用持锁单页回写路径，覆盖 Loading 页面不会提前回写且不会二次锁定 `spin::Mutex`。
- `docs/03_fs/page-cache.md` — 记录 I/O gate 回调必须使用持锁回写辅助函数的约束。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight && make kernel ARCH=rv64 PROFILE=normal && make kernel ARCH=la64 PROFILE=normal` ✅（按 RV64 → LA64 串行）。
- Docker `pxy-mangocore-os-dev-1`：`make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅，KTest `44 passed, 0 failed, 44 total`；此前测试 16 在 180 秒上限内停在测试 15 后。

**备注：** 验收证据已归档到 `docs/Work_Log/evidence/2026-07-27/io-gate-*`，且日志时间晚于两个被测源文件。既有调试模式库已涵盖 `spin::Mutex` 非重入锁的链式调用风险，未重复沉淀；AI-Usage-Report 已列出当前模型，无需更新。

### 修复 another_ext4 冷缓存部分块截断后扩容泄露旧数据

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — `truncate_inode()` 在缩容且新 EOF 未块对齐时，先定位最后保留逻辑块；若该块已映射则读回、清零 `[new_size % BLOCK_SIZE, BLOCK_SIZE)` 并写回，再释放 EOF 后的 extent。稀疏末块保持未分配。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight && cd /app/dependency/another_ext4 && cargo check` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 子模块提交 `2518f9a fix(ext4): zero tail of last retained block on truncate shrink`；未运行 QEMU 定向用例。固定 nightly 未安装 `cargo-fmt`，格式检查不可用；AI-Usage-Report 已列出当前模型，无需更新。

### another_ext4 截断释放 extent

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — 新增 `Ext4::truncate_inode()`；扩容只提交 `i_size`，缩容在事务中按 extent 尾部及块组边界释放数据/树元数据块、更新 `i_blocks` 并写回带校验和 inode。
- `os/src/fs/ext4_another/mutations.rs` — `resize()` 改为调用 `truncate_inode()`；缩容在同一 PageCache I/O gate 内回写、截断缓存及持久化保留尾页后再释放后端 extent。
- `os/src/fs/page_cache.rs` — 删除逐页零填充的 `zero_truncate_with_backend()` 回退，提供持有 I/O gate 时的正常截断入口。
- `docs/03_fs/page-cache.md` — 同步 another_ext4 原生 extent 释放后的截断和尾页清零顺序。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`cd /app/dependency/another_ext4 && cargo check` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 未运行 QEMU；固定 nightly 未安装 `cargo-fmt`，`cargo fmt --check` 无法执行。AI-Usage-Report 已列出当前模型，无需更新。

### 修复 another_ext4 硬链接重名检查吞没查找错误

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — `link()` 仅将 `dir_find_entry()` 的 `ENOENT` 视为名称可用；现有目录项返回 `EEXIST`，其他查找错误原样传播。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`cd /app/dependency/another_ext4 && cargo check` ✅
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 仅命中当天工作日志的 `code_paths` 引用，无模块文档需要同步；AI-Usage-Report 已列出当前模型，无需更新；未运行 QEMU。

### 修复 another_ext4 inode 清理与脏 PageCache pin 安装竞态

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — `link_new_inode_or_free()` 仅将 `ENOENT` 视为可创建；同名和目录查找失败路径均先释放新 inode，再返回 `EEXIST` 或原始查找错误。
- `os/src/fs/ext4_another/mutations.rs` — 在 `write_with_after_copy()` 的 I/O gate 临界区内递增 generation 后立即保留脏 PageCache，避免 `sync()` 在写回完成与 pin 安装之间清零 generation。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`cd dependency/another_ext4 && cargo check` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 未命中需同步的模块 `code_paths` 文档；AI-Usage-Report 已列出 GPT-5.6-terra 与 Oracle，无需更新。本次未运行 QEMU。

### 修复 another_ext4 resize 的截断数据、I/O 串行化和 generation 竞态

**涉及文件：**
- `os/src/fs/page_cache.rs` — 让写入及所有回写入口持有同一 `io_gate`；提供 bridge 内部的持锁回写路径，并在 another_ext4 缩容时逐页持久化零数据后才发布新 EOF。
- `os/src/fs/ext4_another/mutations.rs` — `resize()` 为所有大小发布递增 `size_generation`；缩容通过零填充回退路径避免 grow 暴露旧块内容；`sync()` 在 I/O gate 内完成回写、size 提交和 generation CAS。
- `os/src/fs/ext4_another/lifetime.rs` — `sync_lifetimes()` 在每个 inode 的 I/O gate 内读取 generation、回写并提交大小；初始快照缺 cache 时会重新读取 lifetime cache，避免并发 resize 安装 cache 后被旧同步提交覆盖。
- `docs/03_fs/page-cache.md` — 同步 I/O gate 与 another_ext4 无 extent-release API 时的零填充语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** another_ext4 的公开低层 API 只有 `commit_inode_size()`，不会释放 extent；其私有 tail-removal helper 不可由 bridge 调用。因此本次不改 submodule，而是在旧逻辑大小仍可供后端写入时将 `[new_size, old_size)` 全部写零并回写，再提交新大小。容器固定 nightly 未安装 `rustfmt`，未能执行格式化命令。

### 修复 another_ext4 同名目录项的并发创建竞态

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — 在 `namespace_lock` 保护的 `link_new_inode_or_free()` 与 `link()` 中，在发布目录项前原子检查同名项并返回 `EEXIST`；新 inode 的失败路径仍由既有逻辑释放。
- `os/src/fs/ext4_another/namespace.rs` — 保留现有 `lookup()` 快路径；正确性由 another_ext4 锁内检查保证。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight && cd dependency/another_ext4 && cargo check` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ❌，被工作区既有的 PageCache API 不匹配阻断：`Arc<PageCache>` 缺少 `zero_truncate_with_backend` 与 `writeback_all_with_io_gate_held`（`os/src/fs/ext4_another/{mutations,lifetime}.rs`）；与本次 submodule 改动无关。

**备注：** another_ext4 子模块提交：`2e233b3 fix(ext4): make duplicate-name checks atomic`。未命中需同步的 `code_paths` 文档；AI-Usage-Report 已覆盖本次模型，无需更新。

### 修复 another_ext4 同步期间的 generation 释放竞态

**涉及文件：**
- `os/src/fs/ext4_another/lifetime.rs` — `sync_lifetimes()` 在写回前快照 generation；只有写回、size 提交和设备 flush 完成后 generation 仍等于快照值，才释放脏 PageCache。
- `os/src/fs/ext4_another/mutations.rs` — per-inode `sync()` 采用相同的写回前快照规则，避免两条同步入口行为不一致。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight && make kernel ARCH=rv64 PROFILE=normal && make kernel ARCH=la64 PROFILE=normal` ✅（按 RV64 → LA64 串行）。

**备注：** growfiles 的 `write(..., 128000) = -1/EIO` 不可能来自 `frame_alloc()`，该路径原样返回 `ENOMEM`。当前 PageCache 写入路径仅会在需要保留既有页数据时由后端 `read_page()` 传播 another_ext4 的 `EIO`；新扩展页会零填充而不读取后端。当天已有的脏 PageCache 强引用修复也已避免 logical size 尚未提交时重建缓存并读取旧物理 EOF。未运行可用的 gf20/gf23/gf29 运行时复现，故未将剩余 backend `EIO` 归因到块分配器。

### 修复 another_ext4 设备号传播与截断尾页持久化

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — `create_with_data()` 将 syscall 传入的 `dev_t` 传递给 `mknod()`，保留字符/块设备的 major/minor。
- `os/src/fs/page_cache.rs` — `truncate()` 在 I/O gate 内清零保留末页尾部，并将其转为 Dirty；回写期间的尾页修改使用 `PG_REDIRTIED` 保留。
- `os/src/fs/ext4_another/mutations.rs` — 截断后立即回写部分末页，使清零数据在缓存回收后仍可从 ext4 后端读取。
- `docs/03_fs/page-cache.md` — 同步截断回写语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** `sys_mknodat()` 已仅向字符/块设备的 `create_with_data()` 传递 `dev`；本次修正其在 another_ext4 中被覆盖为零的问题。未运行 QEMU 定向用例；AI-Usage-Report 受文件写权限限制未更新。

### 修复 /dev/urandom 未就绪时的 EAGAIN

**涉及文件：**
- `os/src/fs/dev/urandom.rs` — `/dev/urandom` 在安全随机流未就绪时改用既有的 bootstrap ChaCha20 流；仅在该内部回退也意外失败时使用原子 PRNG，保证返回完整 buffer。
- `os/src/fs/mod.rs` — 分别注册允许回退的 `/dev/urandom` 和保持 fail-closed 行为的 `/dev/random`，避免改变 `/dev/random` 的 `EAGAIN` 语义。
- `docs/07_driver/random.md`、`docs/03_fs/devfs.md` — 同步两种设备在熵未就绪时的 ABI。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** `random::fill_bytes()` 在 `ready == false` 时返回 `NotReady`，此前被 `/dev/urandom` 映射为 `EAGAIN`。本次不修改熵池、CSPRNG 或 `getrandom(2)`；聚焦 LTP `getrandom03/getrandom04` 未在本次构建验证中运行。AI-Usage-Report 已列出本次模型，无需更新。

### 修复 another_ext4 特殊节点创建与硬链接提交语义

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — 覆盖 `create_with_data()`，将 FIFO、字符/块设备和 socket 路由到 `mknod()`。
- `os/src/fs/ext4_another/namespace.rs` — `mknod()` 保留 setuid/setgid/sticky 位；移除硬链接后无缓存效果且可能失败的元数据读取。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 未命中需要同步的 `code_paths` 文档；不修改 cyclictest 链接和已延期的 generation/EEXIST 原子性问题；AI-Usage-Report 无需更新。

### 修复 another_ext4 新写可执行文件重开时的 PageCache 首块陈旧数据

**涉及文件：**
- `os/src/fs/ext4_another/lifetime.rs` — 在 inode size 未同步期间强持有脏 PageCache，并以额外 pin 约束其生命周期；成功同步后按 generation 安全释放。
- `os/src/fs/ext4_another/page_cache.rs` — 后端仅持有共享 `logical_size`，避免 lifetime 与脏 PageCache 的引用环。
- `os/src/fs/ext4_another/mutations.rs` — 写成功后保留脏缓存；仅在数据、inode size 和设备 flush 均成功且 generation 未变化后释放。
- `os/src/kernel_tests/ext4_another/{mod,persistence}.rs` — 增加“写入 ELF magic → 释放原 inode → 重开并读取”的回归测试。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 旧实现只以 `Weak<PageCache>` 关联 lifetime。原 inode 释放而 inode size 尚未同步时，新 inode 会创建新缓存；后端以逻辑大小请求 `another_ext4::read()`，却被旧物理 EOF 短读并保留零填充，导致 ELF/shebang 首字节错误。未命中需同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### LA64 musl cyclictest 复用 glibc 二进制

**涉及文件：**
- `user/src/bin/test_runner/bootstrap/libraries.rs` — 在现有 musl/glibc 库链接完成后，将 `/musl/cyclictest` 符号链接至 `/glibc/cyclictest`，避开 musl 的 `sched_getparam` ENOSYS stub。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** 未替换任何 cyclictest 二进制，且未命中需要同步的 `code_paths` 模块文档；AI-Usage-Report 无需更新。

### RV64 全量映像与聚焦 LTP 验证尝试

**涉及文件：**
- `os_test.conf` — 配置 LTP-only inline runner 和 12 个 link/mkfifo/symlink/ftruncate 聚焦用例。
- `docs/Work_Log/evidence/2026-07-27/` — 保存构建、QEMU、配置、容器挂载和命令证据。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make build ARCH=rv64 PROFILE=normal` ✅
- QEMU RV64 以 `-m 2G -smp 1` 成功启动并退出，未发现运行期 kernel panic ✅
- `make testsuits-download` 后重试；musl 与 glibc 两侧仍均缺少已配置的 LTP runtest suite，聚焦用例未执行 ⚠️

**备注：** 因 `/musl/ltp/runtest/*` 与 `/glibc/ltp/runtest/*` 不存在，不能将 link01/link02/link05/dup05/symlink01/symlink03/ftruncate01/linktest 声称为 GREEN；完整日志见 `evidence/2026-07-27/qemu-output.log`，前两次同类失败尝试已单独归档。未改动内核源码，未命中需要同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### 修复 another_ext4 symlink 已存在名称语义

**涉及文件：**
- `os/src/fs/ext4_another/namespace.rs` — 在 `symlink()` 调用后端创建前以 `lookup()` 检查父目录中是否已有同名目录项，并返回 `EEXIST`。

**验证：**
- Docker `os-dev`（`/home/pxy/projects/MangoCore -> /app`）：
  - `make toolchain-setup` ✅
  - `make kernel ARCH=rv64 PROFILE=normal` ✅
  - `make kernel ARCH=la64 PROFILE=normal` ✅

**备注：** 未命中需要同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### 增加 another_ext4 特殊节点创建

**涉及文件：**
- `os/src/fs/ext4_another/namespace.rs` — 为可写命名空间 inode 宏增加 `mknod()`，支持 FIFO、字符设备、块设备和 socket，并按 Linux `dev_t` 编码拆分设备号。

**验证：**
- Docker `os-dev`（`/home/pxy/projects/MangoCore -> /app`）：
  - `make toolchain-preflight` ✅
  - `make kernel ARCH=rv64 PROFILE=normal` ✅
  - `make kernel ARCH=la64 PROFILE=normal` ✅

**备注：** 未命中需要同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### 修复 PageCache 部分页截断尾部残留数据

**涉及文件：**
- `os/src/fs/page_cache.rs` — `truncate()` 在保留的最后部分页中将新 EOF 后的缓存字节清零，避免后续稀疏扩展读到截断前数据。
- `docs/03_fs/page-cache.md` — 同步部分页截断的零填充语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make toolchain-preflight` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=rv64 PROFILE=normal` ✅
- Docker `pxy-mangocore-os-dev-1`：`make kernel ARCH=la64 PROFILE=normal` ✅（在 RV64 后串行执行）

**备注：** `read_regular()` 已以 `logical_size` 限制读取长度；`resize()` 既有的 `cache.truncate(len)` 调用现在也会清零末页尾部。AI-Usage-Report 的工具清单已覆盖本次使用的模型，无需更新。

### 应用 3 个已知修复并验证 LTP

**涉及文件：**
- `os/src/syscall/fs/sys_mount.rs` — 移除 RDONLY remount 拒绝检查块；将 RDONLY 加入 non_atime_mod 可修改标志列表。
- `os/src/fs/ext4_lwext4/layout.rs` — 移除 `link()` 中的 generation 一致性检查，仅保留 inode_id 匹配。
- `user/src/bin/test_runner/bootstrap/layout.rs` — 将 `rmdir /<libc> && ln -s` 改为 `(rmdir ... || true); ln -sf`，使 symlink 创建不依赖 rmdir 成功。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make user ARCH=rv64 PROFILE=normal && make kernel ARCH=rv64 PROFILE=normal` ✅
- config: `mask=0x800, ltp_runner=inline, ltp_suites=syscalls, ltp_include={open01,close01,read01,write01,access01,...}`
- LTP musl + glibc: 24/24 用例 PASS（chdir01 SKIP 32 为期望的 TCONF）✅
- 具体测试：access01, brk01, chmod01, close01, dup01, dup201, fcntl01, getdents01, getgid01, getpid01, getuid01, link01, mmap01, munmap01, open01, pipe01, poll01, read01, rmdir01, select01, stat01, symlink01, unlink01, write01 全部 PASS

**备注：** 使用 `make rv64-derived-run`（derived-competition 配置）而非 `make rv64-run`（development 配置）；前者通过 conf-inject 在 sdcard-rv-derived.img 上正确应用 os_test.conf。未命中需同步的文档 code_paths；AI-Usage-Report 已列出当前模型，无需更新。
