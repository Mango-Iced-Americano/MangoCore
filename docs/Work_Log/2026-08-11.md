---
date: 2026-08-11
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-11

<a id="2026-08-11-mm-demand-fault-r4"></a>
## BuildStorm MM demand-fault 优化与第四轮诊断采集

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:mm, area:smp, type:performance, type:diagnostics]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** ELF lazy 与 MAP_PRIVATE file fault 改用完整覆盖的未清零帧，避免先清零再覆盖；CPU0/AP 在真正 idle 的 10 ms tick 安全点以每次最多 2 页、全局高水位 256 页补充锁外清零池，需求分配优先消费且低内存停充。补齐 page-fault 的 PTE map、frame allocation、frame-store 阶段计数，修正 syscall 返回用户态后的 current ID 残留，并增加按任务/CPU 连续匿名缺页的空间局部性分类。本轮不启用猜测性匿名 PTE fault-around，先以计数器作为后续决策门禁。

**Files:**
- `os/src/mm/{filemap,vma,frame_allocator,user_mapper,page_fault}.rs` — 完整覆盖帧、idle prezero pool、缺页阶段和匿名局部性记录。
- `os/src/task/{processor,perf}.rs` — idle tick refill 入口及 schema/counter/reset/stub。
- `os/src/syscall/mod.rs`、`os/src/fs/sysfs/files/diag.rs` — current syscall 生命周期修正与诊断导出。
- `scripts/monitor_buildstorm_peak.py` — QEMU ownership 同时校验 `/proc/<pid>/exe`，避免误杀 launcher 后遗留 orphan。
- `.agents/skills/mango-workflow/references/harness-patterns.md` — 沉淀后台 QEMU 精确归属和退出复查门禁。

**Verification:**
- Docker `make kernel ARCH=rv64 PROFILE=normal MODE=release LOG=off` — ✅。
- Docker `make kernel ARCH=la64 PROFILE=normal MODE=release LOG=off`（RV 后串行）— ✅。
- Docker RV64 `EXTRA_FEATURES=perf_diag`、8 CPU、BuildStorm stats-all 内核 — ✅。
- RV64 官方 pub、8 CPU/8 GiB、MTTCG diag run-r4 — ✅采集并按 `serial_tail_after_peak` 停止；`BUILDSTORM_TOOLCHAIN ok`、`BUILDSTORM_MINIBUILD ok`、`BUILDSTORM_BEGIN mode=multi` 存在，峰值触发后保留至 sample 122。原始结果位于 `build/diag-mm-r4/evidence/run-r4/`，未做性能分析且不纳入 Git。
- `make lint` — ⚠️ 仓库 warning baseline 漂移导致 fail-closed，报告包含大量本轮未修改文件；未重写 baseline。

**Notes:** 诊断 monitor 首次 TERM 命中了包含完整 QEMU 命令的 launcher shell；精确解析实际 QEMU executable 后只终止本轮 kernel/overlay 对应实例，并修复 monitor 归属判断。日志、pcap、overlay、官方镜像及历史未跟踪产物均不提交。

<a id="2026-08-11-pagefault-diag-phase"></a>
## 缺页分类与 BuildStorm 峰值诊断补齐

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:mm, type:diagnostics, type:performance]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 统一 page fault action 的返回与计时，补齐 ELF/filemap/共享写等之前因提前 return 而丢失的分类；新增访问类型、完成/重试/错误计数和 pagefault schema v2。BuildStorm 诊断监视器改为阶段感知峰值判定，并能从 launcher PID 解析到自有 QEMU 子进程，避免误触发或误操作队友实例。

**Files:**
- `os/src/mm/page_fault.rs` — action 统一转为 `FaultOutcome`，记录 classify 阶段与每次 fault 结果。
- `os/src/task/perf.rs` — 增加 pagefault action/access/outcome 计数器及 reset/stub。
- `os/src/fs/sysfs/files/diag.rs` — 导出 pagefault schema、访问类型和结果字段。
- `scripts/monitor_buildstorm_peak.py` — 修正高峰触发条件、记录 BuildStorm 任务数并解析 QEMU 子进程。

**Verification:**
- `python3 -m py_compile` — ⏭ 未写入 `__pycache__`（工作区权限拒绝，改用脚本静态审查）。
- `make kernel ARCH=rv64 PROFILE=normal`（Docker）— ✅。
- `make kernel ARCH=la64 PROFILE=normal`（Docker）— ✅。
- diag QEMU（RV64 pub，8 CPU/8 GiB，MTTCG，`buildstorm.stats=all`）— ✅；在 sample80（active=9、runnable=2、busy≈6.50）触发峰值，sample116 完成峰后 36 个快照后停止。峰后 sample84→114 的 `rustc core` 长尾主要为 user≈119.55s、kernel≈33.25s、blocked≈0、runnable_wait≈0.147s；`steal_recheck_failed=0`，`candidate_found=success=ktlb_sync_calls`。

**Notes:** 新 pagefault schema v2 满足 `attempts=completed+retries+errors`，sample114 为 `594363=537949+56414+0`，action 计数也闭合。峰后无 journal commit 增长，VirtIO bridge hold/wait 约 0.93s/0.03s，主要尾部是单个 `rustc --crate-name core` 用户态计算。诊断日志、镜像和其他未跟踪产物保持不变。

<a id="2026-08-11-vfs-dirent-snapshot"></a>
## VFS getdents64 复用完整目录快照

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:fs, type:performance, type:correctness]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** `getdents64` 保留 `list_dirents()` 返回的完整目录项，不再按名称重复查找 inode 和元数据。

**Files:**
- `os/src/fs/vfs/file.rs` — 将目录快照升级为 `(name, inode_id, file_type)`，直接编码返回记录。
- `docs/03_fs/vfs-core.md` — 记录目录流的稳定快照与 rewind 语义。

**Verification:**
- `git diff --check` — ✅
- 双架构编译、ktest、QEMU、BuildStorm — ⏭ 未运行：按用户要求完成实现后等待指令。

**Notes:** 日志、镜像和其他未跟踪产物未纳入提交。

<a id="2026-08-11-filemap-inflight-claim"></a>
## FileMap fault-around 先 claim 后读取

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:mm, area:fs, type:performance, type:concurrency]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 并发 fault-around 在后端 I/O 前取得每页唯一所有权，竞争者等待发布或失败通知，不再重复读取相同窗口。

**Files:**
- `os/src/fs/page_cache.rs` — 增加 batch read claim 目录、统一清理和 page-state 等待协议。
- `os/src/task/perf.rs` — filemap schema 升级为 v3，并记录 claim conflict。
- `os/src/fs/sysfs/files/diag.rs` — 导出 fault-around claim conflict 计数。
- `docs/04_mm/filemap-and-page-cache.md` — 记录所有权、代际重验与错误清理契约。

**Verification:**
- `git diff --check` — ✅
- 双架构编译、ktest、QEMU、BuildStorm — ⏭ 未运行：按用户要求完成实现后等待指令。

**Notes:** claim 不向普通读者发布 Loading frame；错误和代际冲突均先释放所有权再通知等待者。

<a id="2026-08-11-another-ext4-directory-cache"></a>
## another_ext4 代际安全目录查询缓存

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:fs, type:performance, type:concurrency]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 为 Cargo 高频 stat/open 路径复用 another_ext4 完整目录扫描，并在全部命名空间事务后精确失效相关父目录。

**Files:**
- `os/src/fs/ext4_another/lifetime.rs` — 增加 inode-generation 共享的目录快照、发布重验和失效 generation。
- `os/src/fs/ext4_another/inode.rs` — `find/list/list_dirents` 统一使用缓存快照。
- `os/src/fs/ext4_another/namespace.rs` — create/link/unlink/rename 等成功后失效父目录；跨目录 rename 同时失效两端。
- `docs/03_fs/README.md` — 记录 another_ext4 目录缓存一致性契约。

**Verification:**
- `git diff --check` — ✅
- 新增 `stale_directory_scan_cannot_publish_after_invalidation` 测试代码 — ⏭ 未执行。
- 双架构编译、ktest、QEMU、BuildStorm — ⏭ 未运行：按用户要求完成实现后等待指令。

**Notes:** 缓存绑定 ext4 inode generation；mutation 后正在途中的旧 listdir 结果无法发布。

<a id="2026-08-11-heap-allocator-attribution"></a>
## Heap allocator 分核与路径归因计数器

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:mm, type:diagnostics, type:performance]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 补齐评估全局 heap allocator SMP 扩展性所需的分操作、分核、直方图、slab refill 和 recovery 计数。

**Files:**
- `os/src/mm/heap_allocator.rs` — 在既有 runtime profile 门控下记录请求、锁类型、slab 结果、buddy failure 与 recovery。
- `os/src/mm/slab.rs` — 向上层报告 slab allocation 是否触发新页 refill。
- `os/src/task/perf.rs` — 新增 heap schema v1、分 CPU 数组、wait/hold 直方图和重置/stub。
- `os/src/fs/sysfs/files/diag.rs` — 在 `/sys/kernel/stats/heap` 导出完整字段。
- `docs/04_mm/oom-and-locking.md` — 记录计数口径和判读方法。

**Verification:**
- `git diff --check` — ✅
- 双架构编译、ktest、QEMU、BuildStorm — ⏭ 未运行：按用户要求完成实现后等待指令。

**Notes:** 日志不提交；原始 tick 只用于相同架构与 QEMU 配置下的 A/B。

<a id="2026-08-11-user-tlb-doorbell-retry"></a>
## LA64 用户 TLB doorbell 交付重试

**Date:** 2026-08-11 +08:00
**Author:** AI:Codex
**Tags:** [area:smp, area:mm, type:bugfix, type:performance]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** BuildStorm 在 LA64 MTTCG 压力下暴露 user-TLB shootdown 超时；超时快照显示目标 CPU 的 mailbox reason 仍为 pending 且首次发送无错误，说明 payload 已发布但一次性硬件 doorbell 被延迟或合并。现对尚未确认的在线 CPU 每 10 ms 重发幂等 doorbell，并使用独立的 5 秒 user-TLB 等待预算；payload、sequence、generation、ASID 与 frame-retirement 契约保持不变，预算耗尽后仍 fail-stop。

**Files:**
- `os/src/smp.rs` — 为 range/full user-TLB shootdown 增加 missing-target doorbell 重试。
- `.agents/skills/mango-workflow/references/harness-patterns.md` — 沉淀 QEMU MTTCG 下 IPI mailbox/doorbell 的诊断和修复模式。

**Verification:**
- `make kernel ARCH=rv64 PROFILE=normal` — ✅
- `make kernel ARCH=la64 PROFILE=normal` — ✅
- `make -C os ktest-run ARCH=la64 PROFILE=normal KTEST=smp CORE_NUM=8 MODE=release LOG=off` — ✅ 39/39
- 官方 LA pub BuildStorm 8 核 8 GiB 不限时回归 — ⏭ 修复提交后执行。

**Notes:** 失败现场和正式测试日志只保存在 `build/formal-buildstorm-20260811/`，不纳入 Git。
