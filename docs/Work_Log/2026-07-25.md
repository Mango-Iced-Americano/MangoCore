# 2026-07-25 Work Log

## 2026-07-25

### SMP-P2-B11：timer hard IRQ 与 deferred 安全点分离

**范围与根因：**

- CPU0 原 timer IRQ 会持 timer/task/network 路径中的普通锁、执行 callback，
  并可能直接 context switch。若为 AP→BSP、shootdown 或 STOP 在长 syscall
  中打开内核中断，这条旧路径可在任意内核位置调度，破坏锁和栈不变量。
- 本包只建立 timer hard/deferred 边界，不提前启用 CPU0 IPI、不让 AP 运行
  timer，也不混入 STOP、RESCHEDULE、runqueue 或 TLB。

**实现与并发语义：**

- `PerCpu` 增加 timer pending、hard IRQ 次数和 deferred 批次计数。hard IRQ
  只静默本地 one-shot、执行原子诊断计数并以 Release 发布 pending；
  多次 IRQ 可以合并，因为软件 timer 与调度 tick 都基于绝对 deadline。
- RV64 通过 SBI 把 timer compare 写到 `usize::MAX` 清 pending；LA64
  对 TICLR 执行 W1C，非周期 TCFG 到零后保持停止。两架构均由安全点按完整
  timer queue 重编程下一次事件。
- 旧 kernel timer、legacy timeout、timerfd、网络 poll 和调度判定移动到
  `run_deferred_timer_work()`。`trap_return()` 在信号处理前调用任务安全点，
  scheduler 在取得 Processor/ready queue 锁前调用 idle 安全点；完整处理
  期间保持本地中断关闭，最后才在显式边界决定是否让出 CPU。
- 审计发现旧 `record_timer_interrupt()` 达到阈值时会在 IRQ 中
  `print_snapshot()`。本包把 hard path 收敛为纯原子计数，并用原子 epoch
  将周期性快照去重后移到 deferred 安全点，避免诊断旁路重新引入 console 锁。
- 新 focused test 连续执行两轮真实内核 timer IRQ：先证明 IRQ 到达后
  deferred 计数未动、pending 已发布且当前 TID 不变，再显式消费安全点并
  证明恰好完成一批；第二轮同时验证 one-shot 已成功重编程。

**Docker 验证：**

- RV64 根 Make facade `CORE_NUM=2` normal kernel build：退出 0；
- LA64 根 Make facade `CORE_NUM=2` normal kernel build：退出 0；
- RV64 `CORE_NUM=2 KTEST=smp`：6/6 PASS；
- LA64 `CORE_NUM=2 KTEST=smp`：6/6 PASS；
- 四项可信验证的源码差异 SHA-256 均为
  `b78d41e003d047bc5076927f8a8c038f72d0bcb3c54ac897d4687a5df643a12b`，
  运行前后指纹一致；
- 验证后仅按 rustfmt 建议调整四处 import/export/条件换行，当前内核差异
  SHA-256 为
  `66118e70ce5f0aa77249e19d0a85c8857ba1066c187c1ad23cf6672413ce4952`；
  机械换行不改变编译产物，因此没有重复构建或 QEMU；
- `make lint` 退出 2，仍是既有 warning baseline/parser 差异，并生成
  root-owned 用户工具 stub；stub 已可恢复地移到
  `/tmp/mangocore-b11-lint-stubs.rFlIHH/`，没有采集或覆盖 baseline。
- changed-file rustfmt check 仍因 `perf.rs`、两架构 trap 等文件中本包之外的
  既有格式差异退出 1；没有为通过检查而格式化整文件、扩大本次 diff。
- 确定性摘要位于
  `docs/Work_Log/evidence/2026-07-25/smp-b11-summary.md`。

**阶段结论：**

- `SMP-P2-B11=pass`，CPU0 内核 timer 已不在 IRQ 中持普通锁、执行 callback、
  打印或切换任务；双架构两轮 one-shot/deferred 闭环已验证。
- 整体 Phase 2 仍为 `partial`：CPU0 IPI mask、AP→BSP、交叉发送、长 syscall
  中断窗口、STOP 和 idle lost-wakeup 尚未完成。
- 本包没有形成新的通用调试故障模式，不追加 workflow references；timer
  边界和后续依赖已同步到启动/trap 架构文档与 SMP 实施方案。

### SMP-P2-B10：通用 IPI reason 与 BSP→多 AP 广播

**范围与依赖审计：**

- 原目标包含 AP→BSP PONG。实现前检查发现 CPU0 已打开 timer，而双架构
  内核 trap 尚未把 timer 改成只记原子状态的 deferred handler；此时为了
  接收 PONG 打开内核中断，会重新暴露“任意内核位置从 timer IRQ 直接调度”
  的旧路径。因此本包没有增加测试专用 timer 屏蔽窗口，而是收窄为通用
  reason API 和 CPU0→全部 AP 广播。
- AP→CPU0、交叉发送和真正 ping-pong 被明确放到 deferred timer 之后，
  不是以本次广播结果外推为已经支持。

**实现与并发语义：**

- 新增 `IpiReason`，把 mailbox 的语义固定为可合并的幂等 reason bit；
  它只承诺“至少处理一次”，计数协议必须另带 sequence/ack。
- 新增 `send_ipi_mask()` 和单目标包装。发送广播时先用 Release 发布全部
  目标 mailbox，再逐个触发硬件 doorbell，为整轮建立统一的
  “publication before delivery”批次边界；每个 AP 仍只读取本地 mailbox。
- 若某个 doorbell 失败，仍继续通知本轮其余目标并在最后返回首个错误。
  失败目标的 reason 保留，不能对已经并发可见的原子状态做不安全回滚。
- focused ktest 先快照三个 AP 各自的下一 ack，再使用一个共同的一秒期限
  等待全部完成；这样既验证独立 per-CPU ack，也避免每个目标各自延长超时。

**Docker 验证：**

- RV64 根 Make facade `CORE_NUM=4` 内核构建：退出 0；
- LA64 根 Make facade `CORE_NUM=4` 内核构建：退出 0；
- RV64 `CORE_NUM=4 KTEST=smp`：`boot_hw_id=1`、`online_mask=0xf`，
  5/5 PASS；真实覆盖逻辑 CPU0 与硬件 hart 0 不恒等时的三个 AP 广播；
- LA64 `CORE_NUM=4 KTEST=smp`：`boot_hw_id=0`、`online_mask=0xf`，
  5/5 PASS；
- `make lint` 退出 2，不能记为 PASS。原始 cargo 输出显示
  `src/smp.rs` 唯一被报告的 warning 位于 B09 已存在的
  `_start as usize`，不在 B10 diff；lint 解析器把同类 warning 后续缺少
  `#[warn(...)]` note 的条目标成 `unknown`，现有 baseline 也未覆盖后续
  SMP 文件。lint 还生成了未跟踪的用户工具 stub，已从工作树可恢复地移到
  `/tmp`，没有采集新 baseline，也没有把工具问题混入 B10。
- 确定性摘要位于
  `docs/Work_Log/evidence/2026-07-25/smp-b10-summary.md`。

**阶段结论：**

- B10 的通用 reason 和 BSP→多 AP 广播功能通过双架构四核测试；
- 整体 Phase 2 仍为 `partial`，且仓库 lint 门禁仍为 `FAIL`；后者应作为
  独立工具链/基线工作包处理，不能用更新 baseline 掩盖；
- 本包没有新增锁、堆分配或中断中调度；广播内存序和 timer 依赖边界已同步
  到启动/trap 架构文档和 SMP 实施方案。

### SMP-P2-B09：双架构最小 IPI mailbox/ack 闭环

**范围与设计：**

- 本包只建立 BSP→AP 的单播 PING，闭合
  `Release mailbox → hardware doorbell → kernel trap → Acquire consume
  → Release ack`。不混入 timer、STOP、RESCHEDULE、runqueue 或 TLB，
  使第一次跨核中断的故障边界保持可审查。
- `PerCpu` 增加 `pending_ipi` 和 PING ack，并为各字段补充中文所有权与
  状态语义注释。reason 是可合并 bitset，不是事件队列；测试串行等待
  前一次 ack 后才复用同一 bit。
- handler 只执行原子操作，不分配、不打印、不获取普通锁、不直接调度。
  AP 仍不进入旧调度器，只从永久 park 升级为 IPI-only idle loop。

**双架构实现：**

- RV64 通过 SBI v0.2 IPI extension 发送 doorbell，使用
  `hart_mask=1, hart_mask_base=目标硬件 hart`，不依赖 logical ID 与
  OpenSBI hart ID 恒等。AP 只打开 SSIE；新的 `__kern_trap` 保存全部可见
  GPR、原始 `sp`、`sstatus` 和 `sepc`，清 SSIP 后处理 mailbox。
- LA64 QEMU 运行期使用 IOCSR vector 1，与 slave boot ROM 的启动
  vector 0 分离。AP 只打开 IPI line；内核 trap 在读取 BADV/打印诊断前
  先清 level-triggered source 并走 mailbox fast path。
- 两架构 trap 返回后分别回到 `wfi`/`idle 0`，等待下一次 doorbell。
  2K1000LA 仍按计划保持单核，运行期发送接口明确返回不支持。

**规范依据：**

- RISC-V SBI 规范定义 IPI extension EID `0x735049`、FID 0，并规定目标
  supervisor 以 SSIP 观察 IPI；
- LoongArch 架构手册规定 IPI 为 line-based interrupt；Loongson IPI
  控制器以 `CORE_CLEAR` 写 1 清状态；
- QEMU LoongArch IPI 实现确认 `CORE_ENSET/CORE_CLEAR/IPI_SEND` 的
  IOCSR 偏移及 doorbell 目标编码，slave boot ROM 在跳转内核前清启动
  vector 0。

**Docker 验证：**

- RV64 `CORE_NUM=2 KTEST=smp`：退出 0，`online_mask=0x3`，4/4 PASS；
- LA64 `CORE_NUM=2 KTEST=smp`：退出 0，`online_mask=0x3`，4/4 PASS；
- 两个入口均包含实际编译、链接和 QEMU，严格串行执行；没有额外重复
  build-only、单核或 4/8 核矩阵。
- 可提交的确定性摘要位于
  `docs/Work_Log/evidence/2026-07-25/smp-b09-summary.md`。

**阶段结论：**

- `SMP-P2-B09=pass`，证明双架构 BSP 能通过真实硬件/固件 IPI 唤醒 AP，
  内核 trap 可安全返回 idle loop；
- 整体 Phase 2 仍为 `partial`：广播、交叉发送、并发 reason、STOP、
  10,000 次 ping-pong、deferred timer 和长 syscall 中断窗口尚未完成；
- 本包未发现需要沉淀为通用 debugging/performance reference 的新模式；
  mailbox 内存序、trap frame 和运行期 vector 所有权已同步到架构文档与
  实施方案。

### SMP-P1-B08：AP 切换独立 idle stack 后再发布 online

**范围与撤销：**

- 先完整撤销未提交的 B07 validation-only patch。该 patch 只增加全局初始化
  计数和栈地址 probe，没有建立新的运行期能力，还会让启动主链被验证代码
  打断，因此不保留。
- B08 改为闭合一个真实启动不变量：AP 完成本地 bootstrap 后必须离开
  firmware boot stack，在 logical CPU 独占的 idle stack 上发布
  `idle=true`，随后才允许发布 `online=true`。

**实现与理由：**

- 双架构 HAL 按构建期 `configured_cpu_count()` 分配页对齐的静态 idle
  stack；RV64 每槽 64 KiB，LA64 每槽 128 KiB。只分配 configured 数量，
  避免单核实板承担 8 核静态内存成本。
- idle 区使用 `.bss.idle_stack`，由链接脚本既有 `.bss.*` 通配符放在
  `sbss..ebss`。它与正在参与 BSS 清零的 boot stack 生命周期不同：AP
  观察 `BOOT_PHASE` Acquire 前不会使用 idle 区，因此应由 CPU0 正常清零；
  同时该区仍在 `skernel..ekernel` 内，不会被 frame allocator 回收。
- 双架构新增发散的 naked trampoline。标准 C ABI 把 logical ID、栈顶和
  Rust 入口分别放入 `a0/a1/a2`；汇编只更新 `sp`、断开旧 frame/return
  链并间接跳转，刻意保留 RV64 `tp` / LA64 `$r21` 的 PerCpu 指针。
- `PerCpu` 新增 `idle: AtomicBool`。AP 在新栈上的第一个 Rust 入口先校验
  `cpu_id()`，以 Release 发布 idle，再通过既有 Release CAS 发布 online；
  BSP Acquire 观察 online 后即可依赖 AP 已完成切栈。CPU0 保持
  `idle=false` 并继续独占旧调度器。
- `KTEST=smp` 新增 AP idle context 断言。该状态不是一次性测试 probe，
  Phase 2 会继续把它用于 idle/远程唤醒协议。

**官方规范与独立审查：**

- RISC-V psABI 与 LoongArch ELF ABI 均要求标准过程入口的 `sp` 维持 128-bit
  对齐；两架构页对齐基址和页倍数槽宽满足该约束。Rust Reference 要求
  naked body 完整承担 ABI 并明确返回或发散，本实现签名为 `-> !` 且不
  fall through。
- DeepSeek 以只读方式自主审查当前 diff 和双架构日志，结论为没有必须修的
  缺陷、现有 focused 证据已经足够，应停止追加测试并转入 IPI/可唤醒 idle
  功能。其建议把 timer、runqueue、页表一起纳入下一包会扩大并发范围，未
  直接采纳；下一包应先闭合最小 IPI mailbox/ack 协议。

**Docker 验证：**

- RV64 `CORE_NUM=8 KTEST=smp`：`boot_hw_id=6`、
  `online_mask=0xff`，3/3 PASS；真实覆盖非 hart0 冷启动映射和 7 个 AP
  idle stack 切换。
- LA64 `CORE_NUM=8 KTEST=smp`：`boot_hw_id=0`、
  `online_mask=0xff`，3/3 PASS。
- 最终 ELF 反汇编确认 RV64 为
  `mv sp,a1; li s0,0; li ra,0; jr a2`，LA64 为
  `addi.d sp,a1,0; addi.d fp,zero,0; addi.d ra,zero,0; jr a2`。
  `IDLE_STACKS` 均为页对齐 BSS 符号并位于 `sbss..ebss`。
- 未重复执行独立 build、单核或用户态全量回归：双架构 8 核 focused ktest
  已包含实际编译、链接和本工作包唯一改变的 AP 路径；CPU0 调度不变量也在
  同一次运行中覆盖。

**阶段结论：**

- `SMP-P1-B08=pass`；AP 的 online 现在承诺“已在独立 idle stack 上”，
  而不再只是“bootstrap 已返回”。
- 整体 Phase 1 仍为 `partial`：可唤醒 idle loop、内核 trap/IPI、
  console 多核串行化和其余 PerCpu 字段尚未完成。
- 本批没有新增独立 debugging/performance reference；启动栈生命周期和
  online 发布顺序已同步到启动架构文档与 SMP 实施方案。

### 将 develop 集中合并到 SMP 并验证五个既有 SMP 提交

**合并范围：**

- SMP parent `43c2c5e0` 包含当前五个启动期 SMP 提交；集中合并
  `origin/develop@88abaa1f`，没有逐提交 rebase 或逐提交重复测试。
- 采用 develop 的 `nightly-2026-05-10` 单工具链、统一 Make facade、
  `OUT_DIR` initramfs、CPIO→PID1 启动路径和新 CI；保留 SMP 的逻辑 CPU0
  映射、双架构独立 boot stack、BSP/AP 分流、Release/Acquire online
  协议、CPU-local trap 恢复和 focused ktest。

**冲突处理与修正：**

- `os/src/main.rs` 由逻辑 CPU0 独占 develop 新的全局初始化路径；BSS、MM、
  machine 和 random 初始化完成后才 Release AP，initramfs、驱动和 PID1
  仍由 CPU0 负责。
- LoongArch `_start` 迁移到新 nightly 要求的
  `#[unsafe(naked)] + naked_asm!`，同时保留 CPUID 边界检查和 8 槽独立栈。
- 双架构新 Make 分层统一接收 `CORE_NUM=1/2/4/8`，导出同源
  `MANGO_CORE_NUM`，并生成
  `-smp cpus=N,sockets=1,cores=N,threads=1`；development、regression 和
  ktest 显式使用 MTTCG。
- 合并双方的 7 月 24/25 日 Work Log，更新启动/trap 文档、文档索引和
  Agent 规则，未丢弃任一方记录。
- 验证发现 develop 删除 `run_test.sh` 后仍保留旧入口 fail-closed 契约，
  因此恢复只输出串行 runner 迁移提示并退出 64 的 shim，未恢复旧并行实现。
- 把 `CORE_NUM` 非法值检查改为立即展开的声明，保持 arch settings
  declaration-only；同时修正 layering 检查器误把 `export VAR := value`
  当作 target 的规则。

**Docker 集中验证：**

- 环境：镜像 `zhouzhouyi/os-contest:20260510`，digest
  `sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`；
  Rust `1.97.0-nightly`；双架构 QEMU `10.0.2`。
- `make kernel ARCH=rv64 PROFILE=normal CORE_NUM=2`：PASS。
- `make kernel ARCH=la64 PROFILE=normal CORE_NUM=2`：PASS；严格在 RV64
  完成后串行执行。
- 双架构 `CORE_NUM=2 KTEST=smp`：均为 `online_mask=0x3`、2/2 PASS。
- RV64 `CORE_NUM=1 KTEST=smp`：`online_mask=0x1`、2/2 PASS，证明默认
  单核仍走同一 SMP 数据结构并正确退化；LA64 单核不重复运行，因为其
  架构入口已由双核覆盖，共享退化逻辑已由 RV64 覆盖。
- QEMU topology、非法 `CORE_NUM=3`、QEMU command matrix、canonical
  entrypoint 和 Make layering 契约全部 PASS。

**证据与阶段结论：**

- 可提交摘要：
  `docs/Work_Log/evidence/2026-07-25/smp-develop-integration/summary.md`；
  完整本地 `.log` 受仓库忽略规则保护，不进入提交。
- 本次为 L3/T3 合并工作包。Phase 1 最小启动不变量在新 develop 基线上
  保持 `pass`，整体 Phase 1 仍为 `partial`：IPI、内核 trap、idle
  context 和 AP 调度尚未实施。
- 经验检查：既有 debugging reference 已包含 RISC-V 非固定 Boot HART 与
  LoongArch mailbox+IPI 模式，本批未产生新的跨任务调试模式。
- 文档同步：已更新 `AGENTS.md`、启动/trap 文档和根文档索引；实施方案状态
  仍为 `proposed`，无需提前改为 `implemented`。

### SMP-P1-B05：让双架构用户 trap 恢复运行期 CPU-local 指针

**涉及文件：**

- `os/src/smp.rs` — 将启动期锚点升级为运行期接口，新增先验证静态
  `PER_CPUS` 范围和表项对齐、再安全索引的 `cpu_id()`。
- `os/src/hal/{mod.rs,arch/mod.rs}`、双架构 `arch/*/mod.rs` — 统一导出
  `install_cpu_local()` 和 `cpu_local_ptr()`。
- 双架构 `trap/{context.rs,trap.S,mod.rs}` — 增加内核私有
  `kernel_cpu_local`，用户返回前刷新；入口先保存用户 `tp/$r21`，再装入
  当前 CPU 的内核指针并切换内核栈。
- `docs/01_architecture/boot-and-trap.md`、
  `docs/10_plan/smp-8core-implementation.md` — 同步寄存器所有权、trap
  顺序、LA64 LSX 对齐和 Phase 1 `partial` 边界。
- `docs/Work_Log/evidence/2026-07-25/smp-phase-1/` — 保存完整双架构
  build、两核用户 regression、反汇编和 manifest。

**设计与安全边界：**

- TrapContext 页只有内核 `R|W`、没有用户 `U` 权限，用户不能伪造下一次
  trap 装入的 PerCpu 指针。
- `trap_return()` 在 `do_signal()` 之后按当前 CPU 刷新字段，覆盖初始
  context、exec 新建、fork/clone 复制和未来迁移留下的旧值。
- RV64 入口先保存用户 TLS `tp`，LA64 入口先保存用户 `$r21`，随后才覆盖
  为内核指针；原有 restore 路径仍恢复完整用户寄存器。
- `kernel_cpu_local` 位于两架构 slot 70。LA64 `LsxRegs` 需要 16 字节
  对齐，因此 LSX 区域移到 slot 72；Rust 编译期偏移断言和最终 ELF
  反汇编共同证明布局。
- 内核上下文切换不保存 CPU-local GPR，因为该状态归 CPU 而非归任务。
  本批不实现 IPI、内核 trap、AP 调度、idle context、TLB 或完整 PerCpu。

**Docker 验证：**

- RV64、LA64 `CORE_NUM=2` build-only 的实际编译和链接进程均退出 0。
  确定性 Runner 因 Makefile 切换受跟踪的架构生成文件而记录
  `mutation_detected=true` 并 fail closed；该包装器结果原样保留，不能
  冒充 Runner PASS。
- 双架构用户态 regression 均使用
  `-smp cpus=2,sockets=1,cores=2,threads=1`，达到
  `online_mask=0x3`，6/6 用例通过并输出
  `L4 REGRESSION RESULT: PASS`，QEMU 状态 0、无 panic。
- 反汇编确认 RV64 `sd tp,0x20(sp)` 早于
  `ld tp,0x230(sp)`，随后才 `ld sp,0x228(sp)`；LA64
  `st.d $r21,168(sp)` 早于 `ld.d $r21,560(sp)`，随后才加载
  slot 69 的内核栈。LA64 LSX 首地址为 576，即 `72 * 8`。
- 原始关键代码 diff 为 `+114/-27`；其中接口重命名、注释和诊断占较大
  比例，关键语义约 90～100 行，保持在批准预算附近。

**阶段与维护结论：**

- `SMP-P1-B05=pass`；运行期 `cpu_id()` 与用户 trap CPU-local 恢复闭合。
- 整体 Phase 1 仍为 `partial`：完整 PerCpu、idle stack/context、最小
  trap/IPI 向量、console 多核串行化和全局初始化计数尚未完成。
- 本批没有产生独立 debugging/performance reference；可复用的不变量已
  写入启动/trap 架构文档和 SMP Agent 规范适用的审查清单。

### SMP Agent 工作包规模与注释语言规则调整

**涉及文件：**

- `AGENTS.md` — 将 SMP 工作包从“关键代码约 100 行”调整为默认无行数
  限制、按独立功能或完整不变量闭环；仅复杂逻辑、多锁或高风险并发协议
  使用约 200 行审查目标。
- `docs/10_plan/smp-agent-execution-spec.md` — 同步修改申请、范围失效、
  执行纪律、汇报、禁止事项和模板，清除旧的 100/150 行门槛。
- 两份规则都明确：新增或修改的 SMP 注释尽量使用简体中文，代码标识符、
  寄存器、指令和专有名词可以保留英文。

**验证与维护结论：**

- 验证档位为 T0；`git diff --check` 通过，未运行编译和 QEMU。
- 本次只改变协作规范，不改变内核代码、测试判定或当前 SMP 阶段状态。
- 没有新增调试或性能经验条目；规则已直接沉淀到统一入口和专项规范。

### SMP-P1-B06：PerCpu online 成为 CPU 在线状态唯一真相源

**涉及文件：**

- `os/src/smp.rs` — 为每个 cache-line 对齐的 `PerCpu` 增加
  `online: AtomicBool`，删除全局 `ONLINE_MASK`，新增
  `configured_cpu_count()`、`online_cpu_mask()` 和唯一发布 helper。
- `os/src/kernel_tests/{mod.rs,smp.rs}` — 注册 `KTEST=smp`，验证 configured
  CPU 全部 online，以及 Phase 1 旧调度器仍只在 CPU0 运行。
- `docs/01_architecture/boot-and-trap.md`、
  `docs/10_plan/smp-8core-implementation.md` — 同步 online 所有权、内存序、
  focused test 和 Phase 1 `partial` 边界。

**设计与安全边界：**

- `PER_CPUS` 显式放入 `.data.boot`，因此 LA64 AP 在 CPU0 清 BSS 前安装
  CPU-local 指针时，逻辑身份和原子字段不会被后续清零。
- 每个 CPU 只写自己的 `online`。`compare_exchange(false, true, Release,
  Relaxed)` 发布本地 bootstrap 结果，并把重复发布转为可诊断的 panic。
- BSP 和运行期查询逐项执行 Acquire load；看到 true 即可观察对应 CPU 在
  Release 前完成的本地初始化。Phase 1 不支持 CPU offline，状态只会
  false→true，因此扫描到中间快照是允许的，等待循环会继续重试。
- 删除共享 `fetch_or` 后，AP 不再争用一个全局 online cache line，启动等待、
  日志和后续调用方也不会在 `ONLINE_MASK` 与 `PerCpu` 之间形成双真相。
- 本批不新增锁、unsafe、IPI、调度迁移、timer、idle context 或 TLB 语义。

**Docker 验证：**

- `make rv64-kernel-build-only CORE_NUM=2`：退出 0。
- `make la64-kernel-build-only CORE_NUM=2`：首次因 GitHub 连接超时无法更新
  `buddy_system_allocator` 失败；确认容器已有 lockfile 指定的
  `fb7c19c09162b07f7f45d11a939b96c84d358193` 后，仅补齐 Cargo 本地缓存，
  使用 `CARGO_NET_OFFLINE=true` 重跑，退出 0。未修改依赖声明或 lockfile。
- 双架构 `CORE_NUM=2 KTEST=smp`：均输出 `online_mask=0x3`，两个测试
  2/2 PASS，`[KTEST RESULT: PASS]`，QEMU 和文件系统 teardown 退出 0。
- RV64 `CORE_NUM=1 KTEST=smp`：输出 `online_mask=0x1`，两个测试 2/2
  PASS，证明无 AP 时 CPU0 走同一发布协议。LA64 单核未重复运行，因为共享
  退化逻辑已由 RV64 覆盖，LA64 架构启动路径已由双核 focused test 覆盖。
- 双架构 ELF 中 `PER_CPUS` 符号大小均为 `0x200`，类型位于 PROGBITS
  `.data`；符号表中不存在 `ONLINE_MASK`。

**阶段与维护结论：**

- `SMP-P1-B06=pass`；CPU 在线状态已经由对应 PerCpu 表项唯一持有并通过
  Release/Acquire 发布和读取。
- 整体 Phase 1 仍为 `partial`：其余 PerCpu 字段、idle stack/context、
  最小 trap/IPI 向量、console 多核串行化和全局初始化计数尚未完成。
- 本批没有产生新的 debugging/performance reference；同步不变量已写入
  启动架构文档和实施计划。

### 修复并本地验证并行五层 CI

**涉及文件：**
- `.github/workflows/ci.yml` — 保持五个执行层均只依赖 `toolchain-contracts`；clippy 改为 `mango-kernel-core` 的双目标 `--lib` 检查，cargo test 改为不构建内核二进制的 `--lib` 单元测试；comp 使用注入过配置的 derived 镜像运行。
- `docs/Work_Log/2026-07-25.md` — 记录本次 CI 命令和 Docker 验证结果。

**验证：**
- `docker compose exec -T -e RUSTUP_HOME=/root/.rustup -e CARGO_HOME=/root/.cargo os-dev bash -lc 'cd /app && cargo clippy --lib -p mango-kernel-core --target riscv64gc-unknown-none-elf -- -W warnings && cargo clippy --lib -p mango-kernel-core --target loongarch64-unknown-linux-gnu -- -W warnings'` ✅
- `docker compose exec -T -e RUSTUP_HOME=/root/.rustup -e CARGO_HOME=/root/.cargo os-dev bash -lc 'cd /app && cargo test --lib -p mango-kernel-core'`（两次 CI 架构步骤）✅，各 147 个真实单元测试通过。
- `make -C os rv64-ktest && make -C os la64-ktest` ✅；`make test ARCH=rv64 PROFILE=regression && make test ARCH=la64 PROFILE=regression` ✅。
- `make all MODE=release`、双架构 `conf-inject`（rv64 `virt`、la64 `virt_pci`）✅；derived competition QEMU 命令 dry-run 已确认消费注入后的镜像。按本次范围跳过长时间 comp QEMU 运行。

**备注：** `os` 是仅二进制包；可测试的 workspace 库为 `mango-kernel-core`。跨编译目标的 `cargo test` 无法执行宿主测试，因此两个 CI 测试步骤都运行该库的宿主单元测试；clippy 仍按 rv64/la64 target 分别编译库。

### 修正五层 CI 的完整内核检查与工具链准备

**涉及文件：**
- `.github/workflows/ci.yml` — 两个 clippy 步骤改为在 `/app/os` 以架构板级 feature 编译完整内核；cargo test 显式使用各自交叉目标；cargo-test、ktest、regression 和 comp 均在启动 Docker 后显式执行 `make toolchain-setup`。
- `docs/Work_Log/2026-07-25.md` — 记录本轮 CI 配置修正及本地语法验证。

**验证：**
- `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml")); print("YAML OK")'` ✅
- `docker compose config --quiet` ✅
- `git diff --check` ✅

**备注：** 五个执行层仍全部仅依赖 `toolchain-contracts`，未引入层间串行依赖；CI 运行结果待推送后通过 GitHub Actions 验收。本次仅修改 CI/YAML，未命中 Rust 源码的文档 `code_paths`，无需同步子系统文档或 AI 使用报告。

### 触发 cleanup 分支的 CI 验收

**涉及文件：**
- `.github/workflows/ci.yml` — 将 `chore/clean-up` 加入 push 分支过滤，确保该分支的 CI 修复推送会实际创建五层工作流。
- `docs/Work_Log/2026-07-25.md` — 记录首次推送后 `gh run list` 未返回运行的触发条件修正。

**验证：**
- 两次 `gh run list --repo Mango-Iced-Americano/MangoCore --branch chore/clean-up --limit 1 ...` 未返回运行；原因是该分支不在 push 过滤列表中。

**备注：** 该改动不影响五层的 `needs: [toolchain-contracts]` 并行拓扑。

### 修复 CI 工具链组件和工作目录

**涉及文件：**
- `rust-toolchain.toml` — 将 clippy 纳入声明式工具链组件，令每个 `toolchain-setup` 供应完整的 CI 工具。
- `scripts/test-rustup-setup.sh` — 扩展 fixture 和精确参数断言以覆盖新增 clippy 组件。
- `.github/workflows/ci.yml` — cargo-test 使用可执行单元测试的 host target（裸机目标没有 `libtest`）；la64 clippy 与 host cargo-test 通过 `CARGO_BUILD_TARGET` 覆盖 `os/.cargo/config.toml` 的 rv64 默认目标；comp 下载镜像前切换至 `/app`。

**验证：**
- Actions run `30119009418`：toolchain-contracts、ktest、regression ✅；clippy 因缺少 `cargo-clippy` 退出 1；cargo-test 对 `riscv64gc-unknown-none-elf` 缺少 `std` 和 `test` crate 退出 101；comp 在 `/root` 查找 `fs-img-dir` 失败退出 2。

**备注：** 双架构完整内核编译仍由 clippy 的板级 features 覆盖；`mango-kernel-core` 的 147 个单元测试只能在 host 上执行，交叉目标的库缺少可运行的标准 test harness。

### 清除完整内核 clippy 的阻断项

**涉及文件：**
- `os/src/syscall/process/ids.rs` — 用零偏移替代 `FIELD_OFFSET * 0`，消除确定性零值运算。
- `os/src/hal/arch/loongarch64/trap/mod.rs` — 去除自赋值和单次循环，保留内核态未对齐访问模拟的错误分支与读写语义。
- `os/src/hal/arch/loongarch64/register/base/prcfg.rs` — 纠正三段反向 bit range 的起止顺序。

**验证：**
- `sh scripts/test-rustup-setup.sh` ✅
- Docker 内双架构完整 clippy（rv64 `board_rvqemu block_virt`；la64 `board_laqemu block_virt_pci`）✅
- Docker 内 `CARGO_BUILD_TARGET=x86_64-unknown-linux-gnu cargo test --lib -p mango-kernel-core` ✅
- Docker 内 `make kernel ARCH=rv64 PROFILE=normal`、`make kernel ARCH=la64 PROFILE=normal` 串行通过 ✅

**备注：** run `30119009418` 暴露的 clippy hard errors 已转为本地 GREEN；保留现有 warning 基线，CI 仅以 `-W warnings` 上调警告级别。

### 对齐 preflight fixture 的 clippy 组件

**涉及文件：**
- `scripts/test-rustup-preflight.sh` — 合成 toolchain layout 纳入 clippy，使 manifest 驱动的完整 preflight 场景反映新增组件。

**验证：**
- Actions run `30120604423` 显示仅 `toolchain-contracts` 的 manifest-driven success 因 fixture 缺少 clippy 失败；该 fixture 已与 manifest 对齐，待下一轮 CI 验收。

### 创建 CI 测试镜像目录

**涉及文件：**
- `.github/workflows/ci.yml` — comp 下载镜像前创建未跟踪的 `fs-img-dir`，修复 run `30120899751` 中 `make testsuits-download` 的工作目录失败。

### Redesign CI pipeline to 5-layer sequential build

Replaced the single `full-test` job with a 5-layer pipeline on the `rvqemu` platform:

| Layer | Job | What | Timeout |
|-------|-----|------|---------|
| 0 | `toolchain-contracts` | Toolchain provisioning contracts (unchanged) | 5 min |
| 1 | `clippy` | `cargo clippy --features "board_rvqemu block_virt" -- -D warnings` | 15 min |
| 2 | `cargo-test` | `cargo test --features "board_rvqemu block_virt"` | 15 min |
| 3 | `ktest` | `make -C os rv64-ktest` (kernel self-tests, no disks) | 15 min |
| 4 | `regression` | `make test ARCH=rv64 PROFILE=regression` (initramfs only) | 15 min |
| 5 | `comp` | `make -f make/rv64.mk comp` with mask=0xFDF, QEMU_TIMEOUT=7200 | 350 min |

**涉及文件：**
- `.github/workflows/ci.yml` — 完全重写，移除了双架构 serial full-test，改为 rv64-only 5 层顺序流水线

**关键变更：**
- 所有 job 使用 `docker compose up -d` / `docker compose exec` / `docker compose down` 模式
- 每层使用 `needs:` 确保顺序执行
- 显式传入 `RUSTUP_HOME=/root/.rustup` 和 `CARGO_HOME=/root/.cargo`
- comp job 包含磁盘清理、缓存、镜像下载解压、config 注入全流程
- 移除 LA64 相关步骤（rv64 only for speed）
- 保留 `toolchain-contracts` job 不变

**验证：**
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` ✅ — YAML 语法合法
- 未做双架构编译验证（仅 CI 配置变更，不涉及内核源码）

**备注：** 需要确保 `make -C os rv64-ktest` 和 `make -C os conf-inject` 等命令在 Docker 容器内路径正确。`make rv64-ktest` 在根目录 Makefile 中不存在，已更改为 `make -C os rv64-ktest`。
