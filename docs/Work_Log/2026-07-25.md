## 2026-07-25

### 修复并本地验证并行五层 CI

**涉及文件：**
- `.github/workflows/ci.yml` — 保持五个执行层均只依赖 `toolchain-contracts`；clippy 改为 `mango-kernel-core` 的双目标 `--lib` 检查，cargo test 改为不构建内核二进制的 `--lib` 单元测试；comp 使用注入过配置的 derived 镜像运行。
- `docs/Work_Log/2026-07-25.md` — 记录本次 CI 命令和 Docker 验证结果。

**验证：**
- `docker compose exec -T -e RUSTUP_HOME=/root/.rustup -e CARGO_HOME=/root/.cargo os-dev bash -lc 'cd /app && cargo clippy --lib -p mango-kernel-core --target riscv64gc-unknown-none-elf -- -W warnings && cargo clippy --lib -p mango-kernel-core --target loongarch64-unknown-linux-gnu -- -W warnings'` ✅
- `docker compose exec -T -e RUSTUP_HOME=/root/.rustup -e CARGO_HOME=/root/.cargo os-dev bash -lc 'cd /app && cargo test --lib -p mango-kernel-core'`（两次 CI 架构步骤）✅，各 147 个真实单元测试通过。
- `make -C os rv64-ktest && make -C os la64-ktest` ✅；`make test ARCH=rv64 PROFILE=regression && make test ARCH=la64 PROFILE=regression` ✅。
- `make all MODE=release`、双架构 `conf-inject`（rv64 `virt`、la64 `virt_pci`）✅；derived competition QEMU 命令 dry-run 已确认消费注入后的镜像。按本次范围跳过长时间 comp QEMU 运行。

**备注：** `os` 是仅二进制包；可测试的 workspace 库为 `mango-kernel-core`。跨编译目标的 `cargo test` 无法执行宿主测试，因此两个 CI 测试步骤都运行该库的宿主单元测试；clippy 仍按 rv64/la64 target 分别编译库。

### 修正五层 CI 的完整内核检查与工具链准备

**涉及文件：**
- `.github/workflows/ci.yml` — 两个 clippy 步骤改为在 `/app/os` 以架构板级 feature 编译完整内核；cargo test 显式使用各自交叉目标；cargo-test、ktest、regression 和 comp 均在启动 Docker 后显式执行 `make toolchain-setup`。
- `docs/Work_Log/2026-07-25.md` — 记录本轮 CI 配置修正及本地语法验证。

**验证：**
- `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml")); print("YAML OK")'` ✅
- `docker compose config --quiet` ✅
- `git diff --check` ✅

**备注：** 五个执行层仍全部仅依赖 `toolchain-contracts`，未引入层间串行依赖；CI 运行结果待推送后通过 GitHub Actions 验收。本次仅修改 CI/YAML，未命中 Rust 源码的文档 `code_paths`，无需同步子系统文档或 AI 使用报告。

### 触发 cleanup 分支的 CI 验收

**涉及文件：**
- `.github/workflows/ci.yml` — 将 `chore/clean-up` 加入 push 分支过滤，确保该分支的 CI 修复推送会实际创建五层工作流。
- `docs/Work_Log/2026-07-25.md` — 记录首次推送后 `gh run list` 未返回运行的触发条件修正。

**验证：**
- 两次 `gh run list --repo Mango-Iced-Americano/MangoCore --branch chore/clean-up --limit 1 ...` 未返回运行；原因是该分支不在 push 过滤列表中。

**备注：** 该改动不影响五层的 `needs: [toolchain-contracts]` 并行拓扑。

### 修复 CI 工具链组件和工作目录

**涉及文件：**
- `rust-toolchain.toml` — 将 clippy 纳入声明式工具链组件，令每个 `toolchain-setup` 供应完整的 CI 工具。
- `scripts/test-rustup-setup.sh` — 扩展 fixture 和精确参数断言以覆盖新增 clippy 组件。
- `.github/workflows/ci.yml` — cargo-test 使用可执行单元测试的 host target（裸机目标没有 `libtest`）；la64 clippy 与 host cargo-test 通过 `CARGO_BUILD_TARGET` 覆盖 `os/.cargo/config.toml` 的 rv64 默认目标；comp 下载镜像前切换至 `/app`。

**验证：**
- Actions run `30119009418`：toolchain-contracts、ktest、regression ✅；clippy 因缺少 `cargo-clippy` 退出 1；cargo-test 对 `riscv64gc-unknown-none-elf` 缺少 `std` 和 `test` crate 退出 101；comp 在 `/root` 查找 `fs-img-dir` 失败退出 2。

**备注：** 双架构完整内核编译仍由 clippy 的板级 features 覆盖；`mango-kernel-core` 的 147 个单元测试只能在 host 上执行，交叉目标的库缺少可运行的标准 test harness。

### 清除完整内核 clippy 的阻断项

**涉及文件：**
- `os/src/syscall/process/ids.rs` — 用零偏移替代 `FIELD_OFFSET * 0`，消除确定性零值运算。
- `os/src/hal/arch/loongarch64/trap/mod.rs` — 去除自赋值和单次循环，保留内核态未对齐访问模拟的错误分支与读写语义。
- `os/src/hal/arch/loongarch64/register/base/prcfg.rs` — 纠正三段反向 bit range 的起止顺序。

**验证：**
- `sh scripts/test-rustup-setup.sh` ✅
- Docker 内双架构完整 clippy（rv64 `board_rvqemu block_virt`；la64 `board_laqemu block_virt_pci`）✅
- Docker 内 `CARGO_BUILD_TARGET=x86_64-unknown-linux-gnu cargo test --lib -p mango-kernel-core` ✅
- Docker 内 `make kernel ARCH=rv64 PROFILE=normal`、`make kernel ARCH=la64 PROFILE=normal` 串行通过 ✅

**备注：** run `30119009418` 暴露的 clippy hard errors 已转为本地 GREEN；保留现有 warning 基线，CI 仅以 `-W warnings` 上调警告级别。

### 对齐 preflight fixture 的 clippy 组件

**涉及文件：**
- `scripts/test-rustup-preflight.sh` — 合成 toolchain layout 纳入 clippy，使 manifest 驱动的完整 preflight 场景反映新增组件。

**验证：**
- Actions run `30120604423` 显示仅 `toolchain-contracts` 的 manifest-driven success 因 fixture 缺少 clippy 失败；该 fixture 已与 manifest 对齐，待下一轮 CI 验收。

### Redesign CI pipeline to 5-layer sequential build

Replaced the single `full-test` job with a 5-layer pipeline on the `rvqemu` platform:

| Layer | Job | What | Timeout |
|-------|-----|------|---------|
| 0 | `toolchain-contracts` | Toolchain provisioning contracts (unchanged) | 5 min |
| 1 | `clippy` | `cargo clippy --features "board_rvqemu block_virt" -- -D warnings` | 15 min |
| 2 | `cargo-test` | `cargo test --features "board_rvqemu block_virt"` | 15 min |
| 3 | `ktest` | `make -C os rv64-ktest` (kernel self-tests, no disks) | 15 min |
| 4 | `regression` | `make test ARCH=rv64 PROFILE=regression` (initramfs only) | 15 min |
| 5 | `comp` | `make -f make/rv64.mk comp` with mask=0xFDF, QEMU_TIMEOUT=7200 | 350 min |

**涉及文件：**
- `.github/workflows/ci.yml` — 完全重写，移除了双架构 serial full-test，改为 rv64-only 5 层顺序流水线

**关键变更：**
- 所有 job 使用 `docker compose up -d` / `docker compose exec` / `docker compose down` 模式
- 每层使用 `needs:` 确保顺序执行
- 显式传入 `RUSTUP_HOME=/root/.rustup` 和 `CARGO_HOME=/root/.cargo`
- comp job 包含磁盘清理、缓存、镜像下载解压、config 注入全流程
- 移除 LA64 相关步骤（rv64 only for speed）
- 保留 `toolchain-contracts` job 不变

**验证：**
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` ✅ — YAML 语法合法
- 未做双架构编译验证（仅 CI 配置变更，不涉及内核源码）

**备注：** 需要确保 `make -C os rv64-ktest` 和 `make -C os conf-inject` 等命令在 Docker 容器内路径正确。`make rv64-ktest` 在根目录 Makefile 中不存在，已更改为 `make -C os rv64-ktest`。
