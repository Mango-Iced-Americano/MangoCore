## 2026-07-25

### 修正 VF2 GMAC0 RGMII RX 时钟寄存器并增加 ktest PHY 诊断

**涉及文件：**
- `os/src/drivers/net/gmac_jh7110/mmio.rs` — 将 GMAC0 RX 时钟控制寄存器改为 JH7110 AON CRG 的 `0x1c`，并定义 RMII/RGMII 父时钟选择位。
- `os/src/drivers/net/gmac_jh7110.rs` — 显式选择外部 RGMII RXC 输入，不再向错误的 RMII-RTX 分频寄存器写入伪 gate 位。
- `os/src/drivers/net/gmac_jh7110/phy.rs` — 提供只读 YT8531C `0xA001`、`0xA010`、`0xA012`、`0x000c` 扩展寄存器诊断快照。
- `os/src/drivers/net/gmac_jh7110/ktest.rs`、`os/src/kernel_tests/gmac.rs` — 仅在 GMAC ktest RX 失败时输出 PHY 快照及 AON RX mux 值。

**验证：**
- 指定 VF2 Docker 构建（`board_vf2 block_virt oom_handler`，`mango.mode=ktest mango.test=gmac`）✅；release 产物已部署至 `/tmp/opencode/vf2-direct-tftp/kernel-rv`。
- 实板启动未完成：`/dev/ttyUSB0` 的 CH340 串口可打开但无输出，DTR/RTS reset 与 U-Boot reset 均失败，尚未获得本次补丁的 ktest 结果。

**备注：** 上游 JH7110 时钟驱动定义 GMAC0 RX mux 在 `0x1c`，默认父为外部 `gmac0_rgmii_rxin`；此前 `0x10` 实际为 GMAC0 RMII_RTX 分频器。暂未沉淀 reference：必须在实板 A/B 证实后再记录为可复用根因。未命中 `docs/` 的 `code_paths`，AI 使用报告无需更新。

### PageCache 正常写入延迟回写

**涉及文件：**
- `os/src/fs/page_cache.rs` — 移除固定脏页背景/节流写回；仅在至少 65536 脏页且脏页达到当前空闲帧 75% 时启动 256 页紧急写回。
- `docs/03_fs/page-cache.md`、`docs/03_fs/README.md` — 同步正常 write、同步点和紧急写回水位的语义。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight && make -B kernel ARCH=rv64 PROFILE=normal && make -B kernel ARCH=la64 PROFILE=normal` ✅。
- 指定的 `KTEST=ext4_another` 命令输出 `1..0`：当前未提交的 `os/Cargo.toml` 将默认 ext4 后端切换为 `ext4_lwext4_backend`，故 another_ext4 KTest registry 为零；追加 another feature 会触发互斥后端编译错误，未取得 14/14 结果 ⚠️。
- 指定 iozone QEMU 启动的正常内核实际报告 `[ext4] backend: lwext4`，在首个 4 MiB/1 KiB writer 表头后达到 240 秒外层时限；没有 writer throughput，不能报告 2500 → 4000+ KB/s ⚠️。

**备注：** `fsync`/`fdatasync`/最后一次 close 的 per-inode `writeback_all()` 路径未改变。当前 `page_cache.rs` 有 2331 pure LOC，超过项目 250 LOC 门限；按本轮“仅 page_cache.rs 与 ext4_another/page_cache.rs”源码范围未扩展为模块拆分，后续应独立重构。

### RV64 KTest ext4_another PASS + LTP FS suite 冒烟（timeout 前 16/16 PASS）

**涉及文件：**
- `os/src/drivers/block/mod.rs` 等 — 未直接修改，仅测试验证

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=ext4_another` → TAP `1..14`, `[KTEST RESULT: PASS]` ✅
- LTP FS suite（900s timeout）→ 16 cases executed (gf01-gf16), 16/16 PASS, 0 FAIL, 0 panic. QEMU terminated by timeout at gf17. Full 86-case suite requires longer timeout (estimated ~45min+).
- Working tree clean after commit `185f9a64`

**备注：** 900s timeout 不足完成全量 86 例 FS suite（每例约 30-60s）。后续需增加 timeout 或将 suite 分批执行。所有完成用例均 PASS，无回归。

### LA64 ext4_another KTest 与 FS LTP 冒烟

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/la64-ktest-*` — 保存 LA64 KTest 构建、128 MiB clean ext4 fixture、完整 QEMU 串口、命令、配置、提交标识和容器映射。
- `docs/Work_Log/evidence/2026-07-25/la64-ltp-fs-*` — 保存注入的 `fs`/musl 配置、完整 QEMU 串口、命令和首尾日志。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`make -C os ktest-run ARCH=la64 PROFILE=normal KTEST=ext4_another KTEST_QEMU_TIMEOUT=120` 退出 0；TAP `1..14`，`14 passed, 0 failed`，`[KTEST RESULT: PASS]` ✅。
- LA64 FS LTP 冒烟：注入 `mask=0x800`、`ltp_runner=suite`、`ltp_libc=musl`、`ltp_suites=fs` 后，musl 与 glibc 两组均执行 68 例且均为 `passed=0, failed=68, skipped=0`。每例在启动 bash 时以 `bash: applet not found` 返回 127；x1 `/app/empty-disk.img` 未检测到文件系统，`/tools/bin/apk.static` 也不存在，故该结果不能用于 FS 语义验收 ⚠️。
- 两次 QEMU 串口均无运行时 kernel panic；KTest 和 LTP 日志时间晚于所测 `kernel-la` 产物。

**备注：** `mango.mode` 由构建期 `MANGO_CMDLINE` 固化，直接向既有 `kernel-la` 传 `-append 'mango.mode=ktest ...'` 不会进入 KTest；本次改用 `ktest-run` 重建并运行测试内核。`ltp_libc=musl` 仍实际运行两个 libc group，是独立的 runner 配置选择问题，未在本轮扩大范围。

### LTP suite runner 退出码标签与 tools-disk 实测

**涉及文件：**
- `user/src/bin/ltprunner.rs` — 以计算出的 `PASS`/`FAIL`/`SKIP` 标签输出 case 退出码；退出码 0 不再被错误标记为 FAIL。
- `docs/Work_Log/evidence/2026-07-25/ltp-fs-tools-*` — 保存 RV64/LA64 构建、容器挂载、配置和 FS suite 的完整 QEMU 串口证据。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `make kernel ARCH=la64 PROFILE=normal` ✅。
- RV64 QEMU 使用 x0 `sdcard-rv.img` 与 x1 `build/rv64/release/normal/tools/disk.img` 启动；`gf01` 至 `gf14` 的实际 `TPASS` 和 runner 标签均为 `PASS LTP CASE … : 0` ✅。
- 用户指定的 600 秒外层时限在 `gf15` 期间终止（exit 124），因此 suite 未输出 `done` 汇总，不能声称已取得完整 executed/passed/failed/skipped 计数；已记录可确认的部分结果为 14 executed / 14 passed / 0 failed / 0 skipped。

**备注：** 该配置实际报告 `ltp-musl`（尽管注入配置要求 `ltp_libc=glibc`），是与本次标签修复独立的配置选择问题；未在本轮扩展修复范围。

### another_ext4 close 改为 per-inode sync

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — `close()` 从文件系统级 `sync_all()` 改为该 inode 的 `sync()`；保留该路径的本 inode PageCache 回写、inode size commit 和设备 flush，避免扫描所有 lifetime、写回全部 inode cache。
- `docs/Work_Log/evidence/2026-07-25/close-sync-*` — 保存 KTest、iozone 和 LTP inline 的 QEMU 串口与命令记录。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `make kernel ARCH=la64 PROFILE=normal` ✅。
- RV64 `ext4_another` KTest：`1..14`、`14 passed, 0 failed`、`[KTEST RESULT: PASS]` ✅。
- RV64 iozone（x0-only，mask `0x010`）：首组四 initial writers 为 `2520.49 kB/sec`，对比同日 `2484.82 kB/sec` 基线为 `+1.4%`；无 kernel panic。QEMU 在 glibc 后续组运行中到达 300 秒外层时限，不能据此宣称全套 iozone 完成。
- RV64 LTP inline（x0-only，先 broad、再 `open01` 至 `rename01` focused include）：两次均只有 `ltp-glibc` group START/END，`DONE/FAIL/SKIP LTP CASE` 均为 0，且无 kernel panic。该 runner 在无法打开或枚举 `ltp/testcases/bin` 时静默退出，故本轮**未取得可用于 FS 正确性判断的 LTP case 数据**。

**备注：** LTP inline 的空执行是独立 harness 可观测性/路径问题，未将 0 case 误报为通过；后续需让 inline runner 对 `ltp/testcases/bin` 打开失败显式报错后再取 FS 结果。

### KTest ext4 fixture 与 FS LTP 基线

**涉及文件：**
- `os/make/common/orchestration.mk` — 新增每次 KTest 启动前重建的 128 MiB ext4 x0 fixture。
- `os/make/qemu-profiles.mk`、`os/make/{rv64,la64}.mk` — KTest profile 改为附加该 fixture 到 slot 0；两架构保持一致。
- `os/make/image-roles.mk`、`docs/architecture/image-role-contract.md` — 更新 KTest 不再是零盘 profile 的镜像角色合同。
- `docs/Work_Log/evidence/2026-07-25/ktest-ext4-another-*`、`ltp-fs-*` — 归档 KTest RED/GREEN、LTP 配置、QEMU 串口、命令和容器元数据。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `make kernel ARCH=la64 PROFILE=normal` ✅。
- RV64 `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=ext4_another`：`1..14`、`14 passed, 0 failed`、`[KTEST RESULT: PASS]` ✅。
- RV64 FS LTP 配置（`fs,fs_perms_simple,fs_readonly,fcntl-locktests`）实际运行两个 libc group；均为 `executed=142, passed=0, failed=142, skipped=0`。无 kernel panic，但 x1 未被识别，`/tools/bin/apk.static` 与 bash applet 缺失，全部以 127 失败 ⚠️。

**备注：** 本次仅建立失败基线；LTP 结果不能作为文件系统语义通过依据。`ltp_libc=glibc` 当前仍触发 musl 与 glibc 两组，后续应单独排查 runner 配置解析及 x1 tools-disk 探测。

### 合并 DragonOS upstream another_ext4 (readlink, checksum, Box, sparse file)

**涉及文件：**
- `dependency/another_ext4/` — 将 DragonOS 上游分支 `dragonos/dragonos`（8bc3842）合并到 `mango`（11778c9），`--allow-unrelated-histories`
- `dependency/another_ext4/Cargo.toml` — 添加缺失的 `spin = "0.7"` 依赖

**合并策略：**
- DragonOS 胜利：build/test 基础设施（ext4_test、ext4_fuse、Cargo.lock）
- Mango 胜利：所有 src/ 实现文件（完整 JBD2 格式解析、alloc_lock、BLOCK_UNINIT、deferred journal commit、mapped overwrite fast path、InodeReclaimHandle 等）
- DragonOS 的 `flush_all`(block_cache) 和空 stub `trans_abort`/`trans_start` 未合并（mango 已有完整实现）
- 删除未引用的 `src/ext4_defs/cache.rs`（DragonOS 移除，mango 不引用）

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅

**备注：** `spin` crate 在 mango 源码中被 `spin::Mutex` 使用，但 `Cargo.toml` 未声明依赖（pre-existing）；已补加 `spin = "0.7"`。所有 mango 性能提交（deferred journal、mapped overwrite fast path、BLOCK_UNINIT、alloc_lock）均保留。未命中需同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### another_ext4 最终回归、性能结论与收尾

**涉及文件：**
- `os/src/lang_items.rs`、`user/src/lang_items.rs` — 适配 pinned nightly 的 `PanicInfo::message()` API，恢复双端构建。
- `docs/Work_Log/evidence/2026-07-25/{rv64-kernel-build,ktest-*,iozone-*}` — 归档最终构建、KTest、iozone 配置、容器映射、完整串口和命令状态。

**验证：**
- 已合并 `develop` 并完成启动/virtio 合并修复；deferred journal writeback 已提交为 `6210c3ad`。
- Docker `pxy-mangocore-os-dev-1`：`make -B kernel ARCH=rv64 PROFILE=normal` ✅。
- RV64 `ext4_another` KTest：`1..14`、`14 passed, 0 failed`、`[KTEST RESULT: PASS]` ✅。
- RV64 iozone（`mask=0x010`）完成；4 initial writer 吞吐为 `2484.82 kB/sec`（各轮约 2351–2485 kB/sec），与约 2500 KB/s 基线一致 ✅。

**备注：** 性能诊断确认瓶颈是小写入的用户态/syscall 固定成本，而非 ext4 批处理；`write_data_only()` 内部吞吐为 34.6 MiB/s。临时诊断计数器已清理。未命中需同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### another_ext4 iozone 写回批次诊断（未接受投机性优化）

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/another-ext4-iozone-diag-*` — 保存 RV64 iozone 的配置、容器映射、完整串口、命令、退出状态与构建日志。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）中以临时原子计数器构建 RV64，并运行 `mask=0x010` iozone；首个四 initial writers 为 `2420.40 kB/sec`，与约 2500 KB/s 基线一致。
- 计数快照：`write_pages()` 32 次调用写回 7,183 页（平均 224 页/调用）；最后一批为 255 页。`write_data_only()` 26 次调用处理 5,647 个 4 KiB 块（平均 217 块/调用），累计 637 ms；约 22.1 MiB 数据的该路径吞吐约 36 MiB/s。
- 临时计数器已删除；Docker 串行 `make kernel ARCH=rv64 PROFILE=normal` 与 `make kernel ARCH=la64 PROFILE=normal` ✅。

**备注：** PageCache 已持续形成接近 256 页上限的批次，`write_data_only()` 也按数百块批量处理；因此“1–4 页/块的小批次”假设被否定，未提高写回阈值或修改批处理。QEMU 在 120 秒外层预算后以 124 终止，但已完成可复现的 writer 吞吐与诊断快照；下一轮应量化 1 KiB `write()` 的用户态/syscall/页缓存复制成本，而不是改写回批次。未命中需同步的 `code_paths` 文档；AI-Usage-Report 无需更新。

### PageCache 定时请求的增量异步写回

**涉及文件：**
- `os/src/fs/page_cache.rs` — PageCache timer 仅在至少有一个完整 16 页批次已脏时置位延迟写回请求，下一写入者在进程上下文批量回写最多 16 页；避免每个 100 Hz tick 把不足一个批次的写入同步落盘，保留 `fsync`/`close` 的全量同步写回和 256 MiB 紧急水位。
- `os/src/task/manager.rs` — 每个 100 Hz scheduler tick 调用 PageCache 的 16 页定时写回请求钩子。
- `user/src/lang_items.rs`、`os/src/lang_items.rs` — 适配 pinned nightly 中 `PanicInfo::message()` 直接返回 `PanicMessage` 的 API。
- `os/Cargo.toml` — 声明既有 `preload_payloads` cfg feature，使 `unexpected_cfgs` deny lint 可通过。
- `docs/Work_Log/evidence/2026-07-25/` — 保存 Docker 构建、KTest 和 iozone QEMU 运行证据。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）RV64 `make -B kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- RV64 KTest（fresh 128 MiB ext4 x0）：`1..43`、`43 passed, 0 failed`、`[KTEST RESULT: PASS]` ✅。
- LA64 `make -B kernel ARCH=la64 PROFILE=normal EXTRA_FEATURES=perf_diag` ⚠️：现有 `#[unsafe(naked)]` / `core::arch::naked_asm` 兼容性错误，非本改动引入。
- 直接在 timer interrupt 中执行 VirtIO 写回会使 QEMU iozone 无输出直至超时；已按任务约定退回为 timer 标记、下一写入者回写。最终 300 秒 iozone A/B 未完成，不能宣称达到 5000 KB/s 目标。
- 2026-07-25 当前容器的 RV64 重建在 `user/src/lang_items.rs` 因 `PanicInfo::message()` 已返回 `PanicMessage` 而非 `Option` 失败；该文件不在本任务授权修改范围内，因此新的完整批次门槛尚不能重新编译或 QEMU 验证。
- Docker 串行 `make -B kernel ARCH=rv64 PROFILE=normal` 与 `ARCH=la64` ✅；RV64 fresh 128 MiB ext4 x0 KTest 为 `43 passed, 0 failed`（前 14 项均 PASS）✅。
- RV64 无 `perf_diag` iozone（2 GiB QEMU、empty x1）首个 4 writer children throughput 仅 `347.56 KB/s`，低于基线 `2392 KB/s`；300 秒到期前未达到目标，不能作为性能通过或提交依据。

**备注：** 定时器硬中断不能同步等待依赖中断推进的 VirtIO completion；注册表快照和后端 I/O 仍严格在锁外运行。未修改 AI 工具清单；AI-Usage-Report 无需更新。

### another_ext4 write() 同步链路核查（未接受无收益改动）

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/another-ext4-close-scope-{ktest,iozone}-*` — 归档本轮 KTest 与 iozone 串口、配置及退出状态。

**验证：**
- RV64 `make -B kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- 专用 x0/x1 镜像上的 `ext4_another` KTest：`1..14`、`14 passed, 0 failed`、`[KTEST RESULT: PASS]` ✅。
- LA64 构建 ⚠️：现有 `#[unsafe(naked)]` / `core::arch::naked_asm` 的 6 个兼容性错误，与本轮未接受的 ext4 改动无关。
- iozone 的 300 秒预算完成 musl 全套及 glibc 前半段后超时（exit 124）；musl 首个四 writer 样本为 1629.87 KB/s，不能与先前 2392 KB/s 单点基线作有效 A/B 比较。

**备注：** 代码已具备延迟写入：`write_at()` 仅复制并标脏 PageCache；`write_pages()` 在 writeback 时把连续页合并为一次 `write_data_only()`。真正的同步等待在 `block_device.write_blocks()`，由 PageCache 的协作式 background writeback 在 writer 上下文调用。未接受“close 仅同步当前 inode”的试探性变更，因为该变更未给出可比较的吞吐改善；最终无源码变更，AI-Usage-Report 无需更新。

### deferred writeback 后 RV64 ext4_another KTest 回归

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/ext4_another-rv64-{config,git-hash}.txt`、`ext4_another-rv64-qemu-output.log` — 归档本次注入配置、提交标识与完整 QEMU 串口输出。

**验证：**
- Docker `pxy-mangocore-os-dev-1`：`make ktest-build-only ARCH=rv64 PROFILE=normal KTEST=ext4_another EXTRA_FEATURES=perf_diag` ✅。
- RV64 KTest TAP：`1..14`，`[KTEST RESULT: PASS]`，QEMU exit 0；即 14 passed、0 failed ✅。

**备注：** `mango.mode` 经编译期 `MANGO_CMDLINE` 固化，QEMU `-append` 不会覆盖已构建内核；本次先重建并 stage KTest 内核。未修改源码；无需同步 `code_paths` 文档或 AI-Usage-Report。

### ext4_another 延迟 journal commit 写回

**涉及文件：**
- `dependency/another_ext4/src/ext4/{rw.rs,low_level.rs,journal_transaction.rs}` — 让后续 buffered write 从事务镜像读取 inode；跳过会抢先刷掉 deferred journal 的 direct data-only 写入；修正带入 deferred 镜像后的当前事务 credit 计数。
- `os/src/fs/ext4_another/inode.rs` — close 在释放文件私有状态后同步文件系统，作为 durability 边界。
- `dependency/another_ext4/src/ext4/{extent.rs,low_level.rs}`、`dependency/another_ext4/src/ext4_defs/inode.rs` — 补齐测试 fixture 的新增字段，恢复单元测试编译。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）：`cargo test deferred_writeback_batches_share_one_journal_commit` ✅（1 passed）。
- `make kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅。
- `make kernel ARCH=la64 PROFILE=normal EXTRA_FEATURES=perf_diag` ⚠️ 未通过：现有 `core::arch::naked_asm` unresolved import 等 6 个 LA64 编译错误，与本次 ext4 修改无关。
- KTest ⚠️ 未运行到用例：启动要求 x0 为 clean development ext4 device，但当前环境未提供该设备。

**备注：** 延迟批次中的旧 metadata image 不能消耗下一次 writeback 事务自己的 credits；容量预留仍按 deferred + current 两部分计算。未命中额外 `code_paths` 文档；AI-Usage-Report 无需更新。

### ext4_another drift_window 360 秒复测与 iozone 回退

**涉及文件：**
- `os_test_drift.conf`、`os_test_iozone.conf` — 分别注入三窗口 musl drift 配置和 `mode=run, mask=0x010, diag=1` 的 iozone 回退配置；未修改内核或用户态源码。

**验证：**
- Docker `pxy-mangocore-os-dev-1`（`/home/pxy/projects/MangoCore -> /app`）中，RV64 drift QEMU 使用 `timeout 360` 运行。串口实际进入 six-window、双 libc 的默认路径；musl `Simple syscall` W0..W5 分别为 35.8803、36.1690、40.1500、38.9481、37.7153、39.9379 µs，随后刚进入 glibc W0 即达 360 秒上限。
- drift 串口没有 `fs_{open,close,stat,read,write,pagefault}_{count,cycles}` 行。当前 `RuntimeConfig::apply()` 只解析 `drift_pre_mask`，不解析 `drift_windows`、`drift_measure`、`drift_libc`；因此该配置中的 `3` 和 `musl` 未生效，使用默认 six windows / 双 libc。
- iozone 回退也以 360 秒上限运行并完成 musl 的自动测量、write/read、random-read、backwards、stride、fwrite/fread、pwrite/pread、pwritev/preadv 组；日志持续显示 `ext4_another write_pages`，但 `prepare_cycles=0 commit_cycles=0`，且同样没有 sysfs FS counter 快照；外层 QEMU 最终 exit 124。

**备注：** 当前已证明 ext4_another 写路径被 iozone 触发，但没有从 guest 输出 `/sys/kernel/stats/ext4` 的机制，不能据此计算 open/close/stat/read/write/pagefault cycles/call，也不能负责任地指定“最高 cycles/call”瓶颈。下一步必须先让 drift runner 解析三个 `drift_*` 字段并在每窗口输出 pre/post sysfs snapshot（或 run 后进入 shell 显式导出该文件），再重跑该实验；本轮不把缺失数据误报为性能结论。未命中 `code_paths` 文档；AI-Usage-Report 无需更新。

### 部署 ext4_another 文件系统热路径 perf_diag 计数器

**涉及文件：**
- `os/src/task/perf.rs`、`os/src/fs/sysfs/files/diag.rs` — 新增并导出 `fs_{open,close,stat,pagefault,read,write}_{count,cycles}`，计数器随 `/sys/kernel/stats/reset` 清零。
- `os/src/fs/ext4_another/inode.rs`、`os/src/fs/ext4_another/mutations.rs` — 在实际 inode open/close/metadata/read_at/write_at 实现记录完整调用耗时；`fs.rs` 与后端 `page_cache.rs` 没有这些操作的实现入口，保持其现有 PageCache backend 计数器不变。
- `os/src/mm/filemap.rs` — 在三类成功文件映射缺页路径记录 page fault 总次数和 cycles。
- `os_test_drift.conf`、`docs/09_debug/perf_diag.md` — 新增四窗口 musl `drift_window` 配置，并记录 ext4 stats 接口。
- `docs/Work_Log/evidence/2026-07-25/{build-rv64.log,build-la64.log,conf-inject-rv64.log,qemu-output.log,config.txt,git-hash.txt,container-id.txt}` — 归档构建、注入和 QEMU 实验证据。

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal EXTRA_FEATURES=perf_diag` ✅
- RV64 QEMU：确认 `[kernel] perf_diag features: perf_stats=true perf_diag=true`；`drift_window` 已进入 W0/W1/W2，Simple syscall 分别为 38.4014、38.4150、40.0966 µs；180 秒预算在 W2 后超时，未完成 W3，且日志未打印 sysfs pre/post counter 快照。
- `make kernel ARCH=la64 PROFILE=normal EXTRA_FEATURES=perf_diag` ⚠️ 未通过：现有 `#[unsafe(naked)]` / `core::arch::naked_asm` 兼容性错误（6 errors），与本次计数器无关。
- `cargo fmt --check` ⚠️ 未通过：既有 `os/src/hal/arch/loongarch64/boot.rs` 解析失败（`#[unsafe(naked)]`）；本次 Rust 文件未被格式化器验证。

**备注：** 本轮未获取完整四窗口 sysfs delta，故不能负责任地报告 FS cycles/call 或前三瓶颈；下一轮应先修复/补齐 initproc drift pre/post stats 输出，再以 `ext4` stats 文件计算窗口差分。未发现需要沉淀的新增通用模式；AI-Usage-Report 无需更新。

### RV64 ext4_another KTest 复测（EBUSY 修复后）

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/rv64-ext4_another-*` — 归档容器、配置、命令、QEMU 串口输出和退出状态。

**验证：**
- RV64 KTest TAP：`1..14`，`14 passed, 0 failed, 14 total`，`[KTEST RESULT: PASS]`，QEMU exit 0 ✅

**备注：** KTest 的 `mango.mode` 在构建时经 `MANGO_CMDLINE` 固化；因此先以 `KTEST=ext4_another` 重建 KTest 内核，再用独占的派生 x0/x1 镜像启动。14 项均通过，无失败测试。未修改源码；无需同步 `code_paths` 文档或 AI-Usage-Report。

### 修复启动阶段 sdcard 重复挂载与测试路径遮蔽

**涉及文件：**
- `user/src/bin/init.rs`、`user/src/bin/init/mounts.rs` — PID1 不再重新挂载内核已挂载的 x0 `/sdcard` 与 x1 `/tools`；仅保留后续持久目录 bind-mount。
- `user/src/bin/test_runner/bootstrap/layout.rs` — 删除 initramfs 占位 `.gitkeep` 后，以 `/sdcard/musl` 和 `/sdcard/glibc` 替换空目录为符号链接。
- `user/src/bin/test_runner/process.rs`、`user/src/bin/test_runner/groups/execute.rs`、`scripts/build_initramfs.sh` — 使用 initramfs BusyBox 提供稳定 shell bootstrap。
- `docs/Work_Log/evidence/2026-07-25/sdcard-bootstrap-*` — 归档 Docker 环境、构建日志、完整 QEMU 串口输出、命令、配置校验和及日志首尾。

**验证：**
- `make build ARCH=rv64 PROFILE=normal` ✅
- RV64 quick boot：`[kernel] official fs (x0) mounted at sdcard`；没有第二次 `/sdcard` mount 的 `EBUSY/-16`，lmbench 已进入 `lmbench_testcode.sh`（不再报 `not found`）✅
- `make kernel ARCH=la64 PROFILE=normal` ⚠️ 未通过：现有 `#[unsafe(naked)]` / `core::arch::naked_asm` 兼容性错误（6 errors），与本次用户态启动路径无关。

**备注：** `/musl` 与 `/glibc` 在 CPIO 中含 `.gitkeep`，因此“空目录”不能直接 `rmdir`；先移除占位文件才能可靠创建指向 sdcard 的符号链接。未命中需要更新的 `code_paths` 文档；AI-Usage-Report 无需更新。

### RV64 ext4_another KTest 复测

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-25/ext4_another-rv64-ktest-*` — 归档 Docker 容器信息、内核命令行、QEMU 完整串口输出、命令及 commit 标识。

**验证：**
- Docker 容器 `pxy-mangocore-os-dev-1`：使用 RV64 ktest 内核启动 `ext4_another` 14 项套件，QEMU exit 0。
- TAP 结果：`1 passed, 13 failed, 14 total`，`[KTEST RESULT: FAIL]`。
- 唯一通过项：`ext4_another::rejects_unreliable_flush_before_media_parse`。

**备注：** 其余 13 项均在执行前返回 `ktest requires a clean ext4 block device in slot 0`；日志未出现 panic。已附加 x0 的干净 sdcard 和 x1 空工具盘，但 ktest 启动路径仍未将块设备注册到 slot 0，因而本次不能宣称 ext4_another 正确性通过。没有修改 Rust 源码；不需要同步 `code_paths` 文档或 AI-Usage-Report。

### 修复 RV64 virtio-blk x0 探测

**涉及文件：**
- `os/src/drivers/block/virtio_blk.rs` — 仅遍历 RISC-V QEMU 内核已映射的 virtio MMIO 槽位，覆盖 QEMU 分配 x0 的 bus.7。
- `docs/Work_Log/evidence/2026-07-25/virtio_blk-rv64-*` — 归档 Docker 环境、命令、配置摘要和完整 QEMU 串口日志。

**验证：**
- `make kernel ARCH=rv64 PROFILE=normal` ✅
- QEMU：`[kernel] block device 0: official fs (MMIO 0x10008000)`；`[kernel] official fs (x0) mounted at sdcard` ✅
- `make kernel ARCH=la64 PROFILE=normal` ⚠️ 未通过：在本改动之外的 `#[unsafe(naked)]` / `core::arch::naked_asm` 编译错误处失败。

**备注：** 暂停 QEMU 的 GDB 读取显示槽位 0、1、2 的 device ID 为 0，槽位 7 为 virtio block（device ID 2）。未命中需要同步的 `code_paths` 文档；AI-Usage-Report 无需更新。
