# Work Log — 2026-07-18

## 完成最终合同矩阵与验证器审查

**涉及文件：**
- `docs/architecture/2026-07-18-mangocore-contract-matrix.yaml` — 补齐 `os/Makefile` 的 `toolchain-preflight` 条目，明确只读、无 Rustup 变更和无下载语义。
- `docs/architecture/2026-07-18-mangocore-contract-map.md` — 在 os 目标表中补齐同一只读工具链合同。
- `docs/architecture/2026-07-18-verify-contract-map.sh` — 将当前已确认的工具链与依赖迁移路径列入显式脏路径 manifest，保留未知路径拒绝；增加 os 条目断言和矛盾 preflight 字段负向探针。

**验证：**
- 完整合同 verifier：exit 0，全部检查通过。
- 矩阵 YAML 语法校验：通过。
- 负向探针：将 `preflight: make toolchain-preflight` 临时改为 `make toolchain-setup` 后 verifier 非零退出，确认矛盾工具链字段仍被拒绝。
- 未运行构建、QEMU、LTP 或 Git 命令。

**备注：** 脏路径门禁只允许当前已分类的工具链迁移、两个依赖迁移子树、12 个已知迁移笔记删除项、架构合同文档和 Work Log，未使用全局路径白名单；其他路径仍会硬失败。

## 修正合同矩阵的镜像与 LA64 组件一致性

**涉及文件：**
- `docs/architecture/2026-07-18-mangocore-contract-matrix.yaml` — 将 `20260510` 明确限定为 development 镜像，保留 CI 的 `20260104` 区分；补齐 LA64 的 `llvm-tools-preview`。
- `docs/architecture/2026-07-18-verify-contract-map.sh` — 增加 development-only 镜像角色和 LA64 与 root 组件合同相等的断言。

**验证：**
- 合同矩阵 verifier 的新增断言通过；整体 exit 1 仅因既有未声明脏路径和原先缺失的 `toolchain-preflight` 矩阵条目。
- 窄内容检查通过，确认 development 镜像角色、CI 镜像标签和 LA64 组件字段均符合合同。
- 临时矛盾矩阵负向检查通过，确认 verifier 分别拒绝 CI 角色混入 `20260510` 和 LA64 缺少 `llvm-tools-preview`。

**备注：** 仅修改架构合同文档、既有 verifier 和工作日志；未修改源码、构建配置、Docker/CI、测试配置、证据或子模块。

## 修正 2026 工具链合同文档一致性

**涉及文件：**
- `AGENTS.md` — 统一根目录 `nightly-2026-05-10`，明确显式 setup、只读 preflight，以及共享架构生成状态导致的串行构建要求。
- `CLAUDE.md` — 删除双 nightly 和 Rustup override 竞态表述，补充 LA64 target 与正常路径不下载或修改 Rustup 的说明。
- `how-to-run.md` — 补充 setup/preflight 流程，修正串行原因和 development Docker 镜像标签。
- `docs/architecture/2026-07-18-mangocore-contract-matrix.yaml` — 将 development image 更新为 `zhouzhouyi/os-contest:20260510`，并增加单根工具链合同字段。
- `docs/architecture/2026-07-18-mangocore-contract-map.md` — 修正 `env` 的只读 preflight 描述。
- `docs/architecture/2026-07-18-verify-contract-map.sh` — 增加对矩阵工具链、镜像和串行字段的只读事实检查。

**验证：**
- 仅执行文档字段、旧字符串和 shell 语法检查，未运行构建、QEMU、测试或 Git 命令。
- 确认目标文档不再包含 `nightly-2025-01-18`、`nightly-2024-05-01` 或正常路径的 Rustup override 竞态表述。
- 确认 LA64 target 仍为 `loongarch64-unknown-linux-gnu`，矩阵 development image 为 `zhouzhouyi/os-contest:20260510`。

**备注：** 未修改源码、Cargo/Make 配置、Docker/CI、子模块、测试配置或历史证据；未新增 reference，AI-Usage-Report 无需更新。

## 关闭最终验收独立审查缺口：Cargo 后态内容审计与 QEMU 语义收紧

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/final-clean-r7-cargo-audit-{pre-setup,post-setup,post-rv64,post-la64}.txt` — 对容器私有 Cargo home 的 `registry`、`git`、`cache` 在四个阶段逐项列举、哈希并分类。
- `docs/Work_Log/evidence/2026-07-18/final-clean-r7-{formal-preflight,rv64,la64,serial-order,negative-*,cleanup}.*` — 新的一次性 Docker 本地副本、无代理离线串行构建、Git/registry 网络解析负向检查和清理证据。
- `docs/Work_Log/2026-07-18.md` — 仅更正 QEMU/LTP 表述；未修改产品源码、构建配置、依赖、子模块或测试配置。

**验证：**
- 私有 Cargo home 在 setup 前与 setup 后的 `registry`、`git`、`cache` 均不存在；RV64 后及 LA64 后唯一条目是 `registry/CACHEDIR.TAG`（SHA-256 `6d9d1d216e0f83abc5e5662ca62c92b4f23009466b54fa27321a69acdb778bb2`），明确分类为目录标记而非 crate index/cache/source。`git` 与 `cache` 始终不存在。
- 正式阶段记录 `CARGO_NET_OFFLINE=true`、proxy=ABSENT；r7 RV64 14:55:51Z–14:58:29Z exit 0 后才开始 LA64，LA64 14:58:29Z–15:00:58Z exit 0。两份 raw log 均无 Git resolution、registry 网络解析、buddy allocator Git URL 或 fetch。
- r7 容器已删除，宿主脏快照和递归子模块状态只读保留；未清理或修改任何无关工作树内容。

**备注：** r6 QEMU 结果只代表 RV64 启动冒烟：日志包含缺失 `/tools/bin/apk.static` 与 LTP `no cases found`，因此不构成 LTP、完整测试或 LA64 运行时通过。新的独立复核 `final-clean-r7-independent-review.md` 为 PASS，确认 Cargo 内容审计、setup/formal 网络边界、串行顺序、脏快照隔离与该措辞均满足本次范围。无 `.rs` 改动，`code_paths` 不适用；AI-Usage-Report 无需更新，未新增可复用 reference。

## 最终干净 Docker 验收：nightly-2026-05-10 离线串行双架构矩阵

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/final-clean-r6-*` — 新建最终一次性容器的只读源挂载、容器本地副本、私有 Rustup/Cargo、显式 setup、离线 RV64→LA64 构建、ELF、QEMU、负向检查与清理证据。
- `docs/Work_Log/evidence/2026-07-18/final-clean-r{2,3,4,5}-*` — 保留并明确标注此前门禁失败：linked-worktree `.git` 不能在容器副本中使用、空 Cargo home 缺 Rustup shim、全局 `CARGO_TARGET_DIR` 破坏 user 产物拓扑、收紧 PATH 遗漏 LA64 objcopy；这些记录不作为验收成功证据。
- `docs/Work_Log/2026-07-18.md` — 记录本次最终构建验收结论；未修改产品源码、依赖、子模块、Makefile、toolchain pin、Docker/Compose、CI 或外部分支。

**验证：**
- 新建 `mango-final-clean-20260718-r6`，镜像为 `zhouzhouyi/os-contest:20260510`（image ID `sha256:60e9bfa0…`）；宿主快照只读挂载 `/src`，复制到容器本地 `/work/src`。`final-clean-r6-copy-receipt.txt` 与两份 source manifest 证明副本内容一致；动态验收证据与 build cache 被排除出自引用清单，所有 Git metadata 仅因 linked-worktree 绝对路径在容器内无效而移除。
- 仅 setup 使用临时 `127.0.0.1:7890` proxy；`make toolchain-setup` exit 0，preflight 明确为 `nightly-2026-05-10` / Rust `1.97.0-nightly`，含 RV64、LA64 targets。正式命令清空所有 proxy、启用 `CARGO_NET_OFFLINE=true`、使用容器私有 `RUSTUP_HOME`/`CARGO_HOME` 和副本内 per-package target。
- `final-clean-r6-serial-order.txt`：RV64 14:37:56Z–14:40:31Z exit 0，随后 LA64 14:40:31Z–14:43:03Z exit 0；两个 raw build log 是同一容器副本的唯一正式矩阵。
- `final-clean-r6-elf-metadata.txt`：`kernel-rv` 为 `ELF64 / Machine: RISC-V`（SHA-256 `bd2749…`），`kernel-la` 为 `ELF64 / Machine: LoongArch`（SHA-256 `331dbd…`）。两份 `final-clean-r6-negative-*` 均确认无 `buddy_system_allocator` Git 解析、无正常构建 Rustup mutation、无 `unsupported relocation type 0x6e`；Rustup 前后清单也一致。
- 可用的无下载 RV64 QEMU 启动冒烟实际运行并 exit 0；`final-clean-r6-qemu-output.raw.log` 与 head/tail 显示 OpenSBI、initproc 和测试编排器启动，但 `/tools/bin/apk.static` 缺失且 LTP 报 `no cases found`，因此不宣称 LTP 或完整测试通过。`final-clean-r6-cleanup.txt` 验证最终命名容器已删除；`final-clean-r6-host-source-post-check.txt` 验证宿主源码内容未被容器构建修改（排除动态验收文件）。

**备注：** `final-clean-r2-host-candidate-git.txt` 固化了本轮开始时的脏候选和递归 pin：allocator `fb7c19c…`、`lwext4_rust` `5e6d52e…`、嵌套 `lwext4` `9729d185…`。独立审查结论见 `final-clean-r6-independent-review.md`：接受 build matrix 与 RV64 启动冒烟，但明确不把 QEMU 的空 LTP suite 误报为 LTP/完整运行时通过；构建后新建的 Cargo registry 目录为已记录的非阻塞后态 caveat。无 `.rs` 改动，故 `docs/` 的 `code_paths` 不命中；`AI-Usage-Report.md` 已列当前模型，未引入新 AI 工具或需要新增的重大案例。未新增 reference：本次发现是 disposable-container 验收编排细节，已由完整命令、原始日志和失败记录覆盖。

## 统一固定 Rust nightly 并移除正常构建路径的 Rustup 变更

**涉及文件：**
- `rust-toolchain.toml` — 根目录固定 `nightly-2026-05-10`，声明 `rust-src`、`llvm-tools-preview` 及 RV64/LA64 target。
- `Makefile`、`os/Makefile`、`os/make/{rv64,la64}.mk`、`user/Makefile` — 保留既有目标名、串行架构顺序与 `lang_items.rs` 变体复制；删除所有正常路径（含递归 arch Makefile）的 `rustup override/default/target add/component add`，并用全局 `.NOTPARALLEL:` 固化包括直接双目标调用在内的串行性。所有 Cargo 路径只依赖只读 preflight（缺失时明确失败），绝不隐式依赖 setup；根 Makefile 仅新增显式 `toolchain-setup` 与只读 `toolchain-preflight`。
- `scripts/rustup-setup.sh`、`scripts/rustup-preflight.sh` — 前者为唯一 Rustup provision 入口；后者仅读取 `$RUSTUP_HOME/toolchains` 的已安装 manifest/target 目录，不调用 Rustup，空环境会给出可操作的 setup 提示。
- `README.md`、`docs/08_testing/README.md`、`docs/architecture/2026-07-18-mangocore-contract-{map.md,matrix.yaml}`、`docs/architecture/2026-07-18-verify-contract-map.sh`、`docs/{09_debug/timer-timekeeping-contrast-experiment-20260618.md,10_plan/lwext4-upstream-fixes.md}` — 同步统一工具链、显式 setup/只读 preflight 与串行构建原因；历史 Work Log 未改。
- `docs/Work_Log/evidence/2026-07-18/20-toolchain-pin-*` — 最终归档 Docker-local 副本、唯一代理 setup 命令、命令/退出码、preflight/provision、递归 trace 和串行构建证据；trace 中的 preflight 调用为预期只读门禁，mutation regex 无匹配。

**验证：**
- Docker 临时 `RUSTUP_HOME`/`CARGO_HOME`：setup 前 `make toolchain-preflight` exit 2，明确提示运行 `make toolchain-setup`；前后 SHA-256 清单一致，证明 preflight 不改源码、Rustup 或 Cargo 状态。
- 仅 `make toolchain-setup` 使用 `http_proxy`/`https_proxy=http://172.17.0.1:7890`；setup 和 setup 后 preflight 均 exit 0。
- Docker-local `make -n -C os rv64-kernel-build-only` 与 `make -n -C os la64-kernel-build-only` 均只出现预期的只读 preflight；`CARGO_NET_OFFLINE=true` 下 RV64 后 LA64 build-only 均 exit 0，trace 与实际递归 build log 均无 Rustup mutation。
- `sh -n scripts/rustup-{setup,preflight}.sh`、`bash -n docs/architecture/2026-07-18-verify-contract-map.sh` 通过；相关文档不再引用旧 nightly/override。

**备注：** 未改 Rust 代码、target triple、linker、Cargo source、CI、Docker/Compose、产物拓扑或 QEMU 行为；未运行 QEMU（本任务仅验证工具链与构建路径）。`code_paths` 审查未命中 Makefile/脚本/根 toolchain；AI-Usage-Report 已涵盖当前模型与 agent，无需更新。未新增 reference：Rustup manifest host 后缀匹配为本脚本实现细节，非跨子系统模式。

## LA64：以受控 Clang/LLD wrapper 替换 GNU linker driver

**涉及文件：**
- `scripts/loongarch64-clang-lld.sh` — 新增可执行 wrapper；将 PATH 限定为镜像系统路径并固定 `/usr/bin/clang --target=loongarch64-linux-gnu -fuse-ld=lld`，再以 `"$@"` 原样转发 Cargo 参数。
- `cargo-config/{os,user}/config.toml` — 两份 canonical Cargo 模板的 LA64 linker 均指向 wrapper。
- `{os,user}/.cargo/config.toml` — 两份当前实际消费的 Cargo 配置同步指向 wrapper；两种 target triple 和全部既有 rustflags 未改。
- `docs/Work_Log/evidence/2026-07-18/{01..16}-*` — 保存候选 toolchain provisioning、Clang/LLD syntax probe、wrapper argv、clean Docker-local LA64 build、ELF metadata、命令/退出码、container mount、配置 checksum 与新鲜性数据。

**验证：**
- 既有 `07-toolchain-2026-red-*` 保留 GNU ld 2.41 对 `R_LARCH_CALL36` (`0x6e`) 的 RED；本次 `CARGO_TERM_VERBOSE=true` 预检查确认原始 Cargo 命令使用 `-C linker=loongarch64-linux-gnu-gcc`，且未丢失 `-Tsrc/linker.ld`、`-nostdlib`、`-static`。
- 镜像内 Clang/LLD 均为 18.1.3；`clang --target=loongarch64-linux-gnu -fuse-ld=lld -###` 实测调用 `ld.lld -m elf64loongarch`。临时 fake Clang argv 捕获确认 wrapper 依次加入 target/LLD 参数，并逐项保留带空格的下游参数。
- 仅在 Docker `/tmp` 的独立 `RUSTUP_HOME`/`CARGO_HOME` 中通过临时代理 provision `nightly-2026-05-10`、`rust-src`、`llvm-tools-preview` 与 LA64 target；随后清除 Rust 输出并以 `CARGO_NET_OFFLINE=true LA_TOOLCHAIN=nightly-2026-05-10 make la64-kernel-build-only` 串行构建，exit 0。
- 候选日志实际选择 wrapper、没有 `unsupported relocation type 0x6e`；产物为静态 `ELF64`、`Machine: LoongArch` executable。未运行 RV64 或 QEMU（本任务明确禁止）。
- 独立安全审查指出未限定 PATH 的 clang 解析风险后，wrapper 固定为 `/usr/bin/clang` 并限制 PATH；新 clean LA64 candidate build（`15-*`）再次 exit 0、无 `0x6e`，ELF SHA-256 为 `1d9eb4d01fc67f6b840958bd34a1efec2f6e8e451dec60d8e67b0f16be1888e0`。真实 wrapper `-###` 明确调用 `/usr/bin/ld.lld`。
- `16-candidate-docker-manifest.txt` 将最终 build 的容器 cgroup ID、`/tmp` 副本、临时 Rust homes、离线变量、命令、exit、ELF/哈希和 `0x6e=ABSENT` 绑定到同次运行；`16-container-id-and-mount.txt` 记录相同容器及本工作区到 `/app` 的挂载。

**备注：** 当前干净副本的预修改候选 build 也 exit 0，因此没有把历史 GNU `0x6e` fixture RED 误报为该源码构建的当前 RED；历史 fixture 仅证明 GNU ld 能力缺口。wrapper 已仅 stage（无 commit/push）。文档 `code_paths` 影响审查仅命中架构合同图的模板复制描述，内容仍准确；AI-Usage-Report 已列当前模型和 agent，无需更新。

## lwext4_rust：嵌入 vendor 树转换为递归固定 Git 子模块

**涉及文件：**
- `.gitmodules` — 在既有 buddy 条目之外新增 `dependency/lwext4_rust`，URL 固定为 `https://github.com/Mango-Iced-Americano/lwext4_rust`，未写入 branch。
- `dependency/lwext4_rust` — 以 mode `160000` gitlink 固定到 `5e6d52e9707d8d756ad612395ea746af3975d14e`；其 `c/lwext4` 递归 gitlink 固定到 `9729d185c017a3448815de6baf33ae93ff3878e2`，不再保留 vendor C 副本。
- `docs/Work_Log/evidence/2026-07-18/14-lwext4-recursive-submodule-*` — 记录迁移前父状态指纹、发布远端 SHA 链、清单等价性、Docker 离线构建、QEMU N/A 与清理证据。

**验证：**
- 转换前在 disposable Docker 检出中确认发布 Rust commit 的 tree gitlink 为指定 C SHA；递归 checkout 后 outer/nested HEAD、origin URL、`git submodule status --recursive` 与两个 SHA 完全一致。
- `13-lwext4-rust-wrapper.manifest.sha256` 49 项（仅允许 outer `.gitmodules` URL 重写）和 `13-lwext4-c-source.manifest.sha256` 92 项均为 0 失败；`git diff --check` 与 staged 版本均 exit 0。
- Docker 隔离临时副本执行 `env -u http_proxy -u https_proxy CARGO_NET_OFFLINE=true make -C os rv64-kernel-build-only MODE=release` exit 0；日志没有 Git 更新/fetch 标记，生成 `kernel-rv` SHA-256 `812f14f0f98621e202357716d1478c8229e46c4bc0545906354738bdfc444ef5`。

**备注：** 未改 Rust/C 实现、Cargo/lockfile、Makefile、Docker/CI、工具链或用户代码；未运行 LA64 或 QEMU。迁移前后（文档证据写入前）未暂存 diff 指纹相同，既有 buddy gitlink 保持不变。无 `.rs` 改动，`code_paths` 不适用；AI-Usage-Report 已列当前模型，未新增工具或重要 AI 案例。未新增 reference：递归子模块固定 SHA 与生成物清理均为本任务专属执行记录。

## Wave 1 Task 1：冻结嵌入 lwext4 源等价清单

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/13-lwext4-*-manifest.sha256` — 固化嵌入 `c/lwext4` C 源、外层 Rust/build-wrapper 及三个父集成消费者的 SHA-256 基线。
- `docs/Work_Log/evidence/2026-07-18/13-lwext4-exclusions.actual.tsv` — 逐项记录唯一允许的生成物排除及理由。
- `docs/Work_Log/evidence/2026-07-18/13-lwext4-freeze-receipt.md`、`13-lwext4-parent-state-pre.txt` — 记录 Docker 只读挂载、集合覆盖、嵌入 Git 上下文和父树改动基线。

**验证：**
- disposable Docker 容器将 MangoCore 挂载为只读 `/src`，仅 `/tmp` scratch 可写；C 92 项 + wrapper 50 项恰好覆盖全部 142 项合格常规输入，145 条（含 3 个父集成文件）`sha256sum -c` 均通过。
- 实际排除 122 项，仅为 `build_lwext4-rv64` 生成目录及 `liblwext4-riscv64.a`；`.git`、`target`、`Cargo.lock` 未在当前树命中，规则仍明确记录。
- 未执行转换、构建、Cargo、Make、子模块/远程/提交/push；预写入 staged/unstaged diff 指纹已持久化，后续 postflight 将逐字比对。

**备注：** 无 `.rs` 源码改动，未命中 `code_paths`；AI-Usage-Report 已列出当前模型且无新增工具/重要案例，无需更新。该条仅建立后续 C/Rust/gitlink 迁移的等价性门槛。

## lwext4_rust：子模块转换前置审计阻断

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/12-lwext4-submodule-blocker.md` — 固化指定上游基线、递归 C gitlink、全部源码差异分类、生成物排除理由和约束互斥证明。

**验证：**
- Docker `os-dev` 内以临时代理克隆指定上游，`HEAD` 精确为 `f9e3de7b0485429104e6b06ae0795e18f68ec957`；递归初始化的 `c/lwext4` 固定为 `58bcf89a121b72d4fb66334f1693d3b30e4cb9c5`。
- `mangocore-submodule` 远端分支不存在；未创建 commit、未执行 push，未修改 `.gitmodules`、`dependency/lwext4_rust` 或任何内核/用户源码。
- 审计确认嵌套 C 子模块存在稀疏文件与 freestanding libc 等本地源码差异。该仓库只能记录其 gitlink；在禁止推送 `gkostka/lwext4` 且要求保留递归子模块的条件下，不能无损把这些差异放入唯一允许推送的 `Mango-Iced-Americano/lwext4_rust` 分支。

**备注：** 按“无法安全分类或无损表示时在转换前停止”的门禁终止；未运行 RV64 构建，因为尚未产生可验证的转换结果。文档影响审查：没有 `.rs` 源码改动，未命中 `code_paths`；AI-Usage-Report 已列出当前模型，没有新增工具或重要 AI 案例。

## buddy_system_allocator：修复当前 worktree 的失效子模块 gitdir

**涉及文件：**
- `dependency/buddy_system_allocator` — 清理失效的旧 gitdir，重新注册本地子模块管理目录，并 detached checkout 固定提交 `fb7c19c09162b07f7f45d11a939b96c84d358193`。
- `docs/Work_Log/evidence/2026-07-18/11-buddy-system-allocator-submodule-red-evidence.txt` — 保存修复前的 stale gitdir RED 证据与用户改动保护检查。
- `docs/Work_Log/evidence/2026-07-18/11-buddy-system-allocator-submodule-cleanup-receipt.txt` — 保存修复命令和最终元数据验收结果。

**验证：**
- `GIT_MASTER=1 git submodule status` exit 0，输出仅引用当前 worktree 的固定 SHA，无 `/home/pxy/projects/Mango/.git/worktrees/...`。
- 子模块 `status --porcelain` 为空，`HEAD` 精确为 `fb7c19c09162b07f7f45d11a939b96c84d358193`，origin URL 为 `https://github.com/Mango-Iced-Americano/buddy_system_allocator`。
- `.gitmodules` URL/path 与 index 的 mode `160000` gitlink 一致；未执行 Docker、构建、提交或推送。

**备注：** 父工作树仍保留此前已有的其他 staged/unstaged 改动；本次未修改这些内容。

## buddy_system_allocator：固定提交 Git 子模块与离线 RV64 验证

**涉及文件：**
- `.gitmodules` — 新增 `dependency/buddy_system_allocator` 子模块，URL 为 `https://github.com/Mango-Iced-Americano/buddy_system_allocator`。
- `dependency/buddy_system_allocator` — 新增 mode `160000` gitlink，固定为 `fb7c19c09162b07f7f45d11a939b96c84d358193`；不保留复制的 allocator 源树。
- `os/Cargo.toml` — 使用该 gitlink 的相对 path；保留 `default-features = false` 和 `features = ["metadata_heap"]`。
- `os/Cargo.lock` — Docker 内 `cargo generate-lockfile --offline` 后保留 path package 形式（`buddy_system_allocator 0.13.0` 无 Git source）。
- `docs/09_debug/buddy-allocator-scan-drift.md` — 将两处已过时的 `os/vendor` allocator 路径同步为子模块路径。
- `docs/Work_Log/evidence/2026-07-18/10-buddy-system-allocator-submodule-*.txt` — 固定提交、许可证/API、Docker 离线构建和清理的持久证据。

**验证：**
- 在 Docker Compose `os-dev` 临时容器中，唯一带 `http_proxy`/`https_proxy=http://172.17.0.1:7890` 的 `git submodule add` 成功；命令结束后代理变量为空。
- Git 索引记录 `.gitmodules`（mode `100644`）和 `dependency/buddy_system_allocator`（mode `160000`、SHA `fb7c19c09162b07f7f45d11a939b96c84d358193`）；checkout 的 `HEAD`、origin URL 与该 SHA 一致。
- 固定 checkout 的包元数据为 `buddy_system_allocator 0.13.0`、edition `2021`、MIT；LICENSE 首行是 `Copyright 2019-2020 Jiajie Chen`，并直接导出 `MetadataHeap`、`PageOrder`、`PageRun`、`AllocError`。
- `CARGO_NET_OFFLINE=true cargo generate-lockfile -vv` exit 0；离线 Cargo metadata 显示 dependency `source: null`、path `/app/dependency/buddy_system_allocator`、`uses_default_features: false`、features 仅 `metadata_heap`。
- 干净的容器临时工作副本串行执行 `env -u http_proxy -u https_proxy CARGO_NET_OFFLINE=true make rv64-kernel-build-only MODE=release` exit 0（2m15s），产出 RV64 ELF 与 `kernel-rv`；30,022 行日志不含 `Updating git repository`、目标 URL、`git+https://github.com/Mango-Iced-Americano/buddy_system_allocator` 或 `git fetch`。

**备注：** 未运行 LA64 或 QEMU。此前 `09-buddy-system-allocator-green-rv64-offline-build.*` 是复制/旧 crates.io 0.8.0 路径的历史记录，不能作为当前子模块的验收依据；本条目与 `10-*` 证据取代其对当前依赖状态的含义。Docker 临时构建目录、日志和命名容器均已删除；仅受管 `os-dev` 服务保留。文档影响审查已同步活动调试文档中的两处旧路径；更早 Work Log/legacy 中的 `os/vendor` 引用保留为当时状态的历史证据。技术报告不绑定依赖位置，无需修改。无 `.rs` 改动，`code_paths` 无需同步；AI-Usage-Report 已列出 GPT-5.6-terra，未新增工具或重大 AI 案例。

## Docker `os-dev`：宿主代理 Git HTTPS 诊断

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/08-proxy-git-*` — 容器端点推导、TCP/HTTPS 代理探测、临时环境变量下的 Git `ls-remote` 原始输出、结果、清理回执与 DoneClaim。

**验证：**
- 默认路由 `192.168.1.1:7890` 被拒绝（exit 1），因此未被误报为 Docker host 代理；`host.docker.internal` 未解析。
- Docker bridge gateway `172.17.0.1:7890` TCP exit 0，代理 HTTPS 请求返回 HTTP 200 / curl exit 0。
- 在 `/tmp`，清除 `GIT_DIR`/`GIT_WORK_TREE` 且仅对单命令设置代理变量后，`GIT_MASTER=1 git ls-remote --refs https://github.com/Mango-Iced-Americano/buddy_system_allocator` exit 0；`feat/metadata-heap` 广告的 tip 精确为 `fb7c19c09162b07f7f45d11a939b96c84d358193`。

**备注：** 未改 Docker/Compose、Git 配置或工作区；未运行 clone/fetch/pull/checkout/commit/push。代理凭据未提供且日志已脱敏。远端可达和 branch-tip 一致不构成 fork 快照、来源或 API 兼容性的验证，vendoring 仍受独立 provenance/API 门禁阻断。无 `.rs` 改动，`code_paths` 不适用；仅保留 managed `os-dev`，临时变量无持久化状态。

## 工具链迁移 Phase 1：Docker RED 运行时证据门禁

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/07-toolchain-2026-red-*` — Compose 重建、镜像 digest、`/app` 挂载、工具/链接器版本、RV64 cache-free Git-resolution RED、LA64 GNU ld `0x6e` 能力 RED、配置 N/A 与清理回执。

**验证：**
- `docker compose pull os-dev`、`docker compose up -d --force-recreate --no-build os-dev` 均 exit 0；运行中容器使用 `zhouzhouyi/os-contest@sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`，并确认本工作区挂载为 `/app`。
- RV64 在空的容器本地 `CARGO_HOME`、离线模式下 exit 101，原始日志明确报 `buddy_system_allocator` 的 `fb7c19c0` Git checkout 无法解析；未依赖宿主缓存或网络 fetch。
- GNU ld 2.41 对容器本地 `R_LARCH_CALL36`（数值 `0x6e`）fixture 输出 `unsupported relocation type 0x6e`，exit 139；`clang`/`ld.lld` 18.1.3 的存在已记录，未修改链接器配置。

**备注：** 当前重建镜像没有 `nightly-2026-05-10`：`nightly-2025-05-20` 缺 LA target，`nightly-2025-01-18` 在新 naked API 处先失败。因此不能将 fixture 误报为当前源码已实际到达 linker 的完整构建 RED。所有容器本地副本、Cargo homes 和 core 均已删除；仅受管理的 `os-dev` 服务保留。无 `.rs` 源码改动，`code_paths` 不适用；AI-Usage-Report 无新增工具或重要案例。

## Todo 0.5：LoongArch64 trap 裸函数 API 迁移（候选工具链）

**涉及文件：**
- `os/src/hal/arch/loongarch64/trap/mod.rs` — 仅迁移 `__rfill()`：`#[naked]` 改为 `#[unsafe(naked)]`，以 `core::arch::naked_asm!` 替换裸函数内的 `asm!`，并移除新宏不接受且由裸函数语义隐含的 `options(noreturn)`；保留原汇编文本、标签、CSR/寄存器指令、操作数、符号和控制流。保留该模块普通 Rust 函数中的 `asm!` 导入。
- `docs/Work_Log/evidence/2026-07-18/05-loongarch-trap-naked-*` — 保存 Todo 0.4 RED、两次只读源挂载→容器本地副本候选构建的原始日志、最终产物、源差异/哈希、诊断扫描及清理回执。

**RED / GREEN：**
- RED：`04-loongarch-naked-final-la64-naked-diagnostic-scan.txt:6-13` 在 `trap/mod.rs:47,54` 报 `#[naked]` 未包裹 `unsafe(...)` 与 E0787（裸函数禁止 `asm!`）。
- GREEN：最终候选 `nightly-2026-05-10`（Rust 1.97.0-nightly）串行执行 `timeout --preserve-status 900s make la64-kernel-build-only LA_TOOLCHAIN=nightly-2026-05-10 MODE=release`，exit 0；最终原始日志中不含 E0787、`trap/mod.rs` 编译错误或 `could not compile`，并产出 `os`/`os.bin`。未运行任何 legacy、RV64 或 QEMU 命令。

**验证与边界：**
- Miri：N/A。该路径为 `#![no_std]` 的 LoongArch 目标专用 `naked_asm!`；Miri 不执行 inline assembly，也不能解释该目标硬件控制流。本次是编译器 API 表面迁移，编译验证是适用门禁。
- `/src` 以 `rw=false` 挂载，复制到容器 `/tmp/mangocore-todo05-loongarch-trap-naked-final` 后才构建；构建前后源 SHA-256 一致，容器工作副本和容器均已删除。
- 文档 `code_paths` 搜索命中 trap/HAL 架构与 syscall 文档；本次未改变 trap 行为、接口或架构说明，内容仍准确，无需同步。`AI-Usage-Report.md` 已列出 `GPT-5.6-terra`，无新工具或重要案例，无需更新。未新增 reference：该 Rust API 迁移模式已由 Todo 0.4 与本条目覆盖。

## Todo 0.4：LoongArch64 裸函数 API 迁移（候选工具链）

**涉及文件：**
- `os/src/hal/arch/loongarch64/boot.rs` — 将 `_start` 的 `#[naked]` 改为 `#[unsafe(naked)]`，并以 `core::arch::naked_asm!` 替换 `asm!`；删除 `naked_asm!` 不支持且已由裸函数语义隐含的 `options(noreturn)`。DMW CSR 配置、栈地址/大小加载、`csrrd`、`jirl` 及符号操作数逐字保留。
- `docs/Work_Log/evidence/2026-07-18/04-loongarch-naked-*` — 保存 Todo 0.3 RED 之后的两次 API 诊断、最终候选 LA64 原始日志、候选工具链、只读源挂载/本地副本、产物元数据、源哈希、对抗检查与清理回执。

**RED / GREEN：**
- RED：Todo 0.3 的 `03-toolchain-compat-candidate-la64-build.stderr.log:36381-36459` 在 `boot.rs` 报 `#[naked]` 未包裹 `unsafe(...)` 和 E0787（裸函数中禁止 `asm!`）。
- 中间候选构建准确暴露 `naked_asm!` 不接受 `options(noreturn)`，仅移除此不兼容选项后重跑。
- 最终候选 `nightly-2026-05-10`（`rustc 1.97.0-nightly (82bee9650 2026-05-09)`）串行执行 `timeout --preserve-status 900s make la64-kernel-build-only LA_TOOLCHAIN=nightly-2026-05-10 MODE=release`：exit 2，但 `boot.rs` 的两项旧诊断均已消失；仅保留范围外 `src/hal/arch/loongarch64/trap/mod.rs:47,54` 的同类 `#[naked]` / `asm!` 错误。未扩展修复范围。
- LA64 未通过，故按任务条件未运行 RV64 邻接回归（`status=not_run`），未运行 QEMU。

**验证与边界：**
- Miri：N/A。该 `#![no_std]` LoongArch 目标专用裸汇编不能由 Miri 执行；本次仅迁移编译器 API，不对 Rust 内存安全路径作 Miri 声明。
- `/src` 以 `rw=false` 挂载后复制到 `/tmp/mangocore-todo04-final` 构建；`boot.rs` 构建前后 SHA-256 相同，`git diff --check` exit 0。
- LA64 ELF 产物：N/A（build exit 2）；容器工作副本、独立 Rustup/Cargo home 均已删除，容器已删除。
- `code_paths` frontmatter 扫描未命中 `os/src/hal/arch/loongarch64/boot.rs`；无需同步模块文档。AI-Usage-Report 无新增工具或重要案例，无需更新。

## Todo 0.3：nightly-2026-05-10 最小源码兼容修复与隔离双架构验证

**涉及文件：**
- `os/src/fs/ext4/direntry.rs`、`os/src/fs/ext4/extent.rs` — 仅将 `core::intrinsics::size_of` 导入替换为稳定的 `core::mem::size_of`。
- `os/src/lang_items.rs`、`os/src/lang_items.rs.la`、`user/src/lang_items.rs`、`user/src/lang_items.rs.la` — 仅移除在候选 nightly 中已失效的 `PanicInfo::message().unwrap()`；`.rv` 变体未改动。
- `docs/Work_Log/evidence/2026-07-18/03-toolchain-compat-*` — 只读 `/src` 挂载、两个容器本地副本及独立 Rust homes 的 provisioning、四个串行 build-only 单元、完整原始 stdout/stderr、产物、源哈希、矩阵及清理证据。

**RED 证据：**
- 既有 `00-toolchain-2026-compat-retry-result.txt`：RV64 exit 2，`rustc internal compiler error: Nullary intrinsic size_of must be called in a const block`；LA64 exit 2，`PanicMessage has no unwrap method`（E0599，`user/src/lang_items.rs:5`）。

**验证：**
- 候选 `nightly-2026-05-10`（`rustc 1.97.0-nightly (82bee9650 2026-05-09)`）在独立 `/tmp/mangocore-candidate` 串行执行：RV64 `make rv64-kernel-build-only RV_TOOLCHAIN=nightly-2026-05-10 MODE=release` exit 0，`kernel-rv` 与 ELF SHA-256 相同；LA64 同工具链 exit 2，但已越过 user panic handler（user release profile 完成），停止于未改动的 LoongArch 裸函数迁移错误：`#[naked]` 需 `#[unsafe(naked)]`，且 naked function 中 `asm!` 需改为 `naked_asm!`（E0787）。
- 旧 pinned 工具链在独立 `/tmp/mangocore-legacy` 串行执行：RV64 `make rv64-kernel-build-only MODE=release` exit 0；LA64 exit 2，旧 `nightly-2024-05-01` 的 `PanicInfo::message()` 是 `Option<&Arguments>`，移除 unwrap 后在 `user/src/lang_items.rs:11,14` 报 E0277（不可 `Display`）。这是与候选 API 直接冲突的已记录兼容限制，未添加版本分支或 workaround。
- 两份源哈希在构建前后相同；Docker inspect 记录 `/src` 为 `rw=false`；构建只写入容器本地副本/Rust homes。未运行 QEMU（本任务禁止）。

**备注：** `code_paths` frontmatter 扫描没有命中；模块地图/合同地图对“架构变体覆盖基础 lang_items 文件”的说明仍准确，无需同步。AI-Usage-Report 无新的工具或重要案例。未新增 reference：本次冲突已由该 Work Log、原始矩阵和版本化证据完整记录，且任务范围禁止采用版本条件 workaround。

## Todo 0.2 重试：只读 `/src` 副本上的 nightly-2026-05-10 双架构资格测试

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/00-toolchain-2026-compat-retry-*` — `/src` 只读挂载、复制、候选工具链、RV64/LA64 完整原始 stdout/stderr、失败单元元数据、校验和与清理证据。

**验证：**
- 启动前 digest 为 `sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`；容器 inspect 在任何复制前确认 `/src` 的 `RW=false`。
- `cp -a /src /tmp/mangocore-candidate` exit 0；所有构建写入该容器本地副本和容器本地 Rust homes。
- RV64 串行先运行，exit 2：`size_of` nullary intrinsic 必须在 const block 中，`os` 无法编译（291 warnings）。
- LA64 在 RV64 结束后运行，exit 2：`user/src/lang_items.rs:5` 对 `PanicMessage` 调用 `.unwrap()` 触发 E0599。
- 正式候选 provisioning exit 0，`rustc 1.97.0-nightly (82bee9650 2026-05-09)`；没有成功的 kernel cell，故 ELF/file/SHA/program-header 元数据均为 N/A。
- 未执行 QEMU；容器已删除。

**备注：** 初始 setup wrapper 在复制成功后提前结束，RV Makefile 随后于同一容器本地 Rust home 安装候选 nightly；此过程及之后 exit 0 的正式 provisioning 均保留在原始日志中，未重跑或掩盖两个失败。无 `.rs` 源码修改，故无 `code_paths` 同步项；AI-Usage-Report 无需更新。

## Todo 0.2：隔离候选工具链兼容性资格验证（阻断）

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/00-toolchain-2026-compat-*` — 固化 digest、无挂载容器、候选 provisioning 原始 stdout/stderr、`/app` 预检、两架构 N/A 原始日志、产物 N/A、哈希、对抗性结论及清理回执。

**验证：**
- 启动前镜像为 `zhouzhouyi/os-contest@sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`，容器 image ID 一致且 mounts 为 `<none>`。
- 容器内 `cp -a /app /tmp/mangocore-candidate` 前置条件失败：该 digest 的镜像没有 `/app`；因此严格遵守“只在副本构建”，没有改用宿主挂载或其他源目录。
- `nightly-2026-05-10` 下载后显示 `rustc 1.97.0-nightly (82bee9650 2026-05-09)`，但完整 rustup 命令 exit 1（空的隔离 `CARGO_HOME` 缺 rustup shim），按失败保留。
- RV64、LA64 的指定 `make ... MODE=release` 均未调用，产物元数据为 N/A；未执行 QEMU、重试或其他 nightly。

**备注：** 这是环境前置条件失败而非编译兼容性结论。容器已删除。没有 `.rs` 改动，故无 `code_paths` 同步项；AI-Usage-Report 无需更新。

## Todo 0.1：一次性 `nightly-2026-05-10` rustup 可用性探测

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/00-toolchain-2026-{image-and-container,rustup-stdout-stderr,inventory-stdout-stderr,cleanup-receipt,matrix-and-commands,adversarial-guards,provenance-and-host-mutation}.txt` — 固化镜像 digest、无挂载容器、每条 rustup 命令的原始输出/退出码、安装清单、清理回执与对抗性门禁。

**验证：**
- 在 digest `sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d` 的 `zhouzhouyi/os-contest:20260510` 无挂载 disposable 容器内，候选 `nightly-2026-05-10` 已安装（`rustc 1.97.0-nightly (82bee9650 2026-05-09)`）。
- `rust-src`、`llvm-tools-preview`、`rustfmt`、`clippy` 与三个目标各自独立添加均 exit 0，且 post-probe inventory 全部列出。
- 初始 `rustup toolchain install` 的最终 exit 为 1，但原始输出已明确显示 toolchain 已安装；失败来自刻意隔离的空 `CARGO_HOME` 缺少 rustup shim，未被误报为成功或不可用。
- `docker rm -f` 后 container inspect exit 1，确认容器已删除；未执行 Cargo、Make 或 QEMU。

**备注：** 仅证明供应/安装资格；未采用候选、未改 Rust pin、Compose/CI/Make/Cargo/源码/vendor/输出镜像或 `.omo`。本次没有 `.rs` 改动，故不存在 `code_paths` 文档同步项；AI-Usage-Report 无新增工具或重要案例需要登记。

## Wave 0 Todo 3：中断构建仅清理

**涉及文件：**
- `os/src/lang_items.rs`、`user/src/lang_items.rs` — 仅恢复中断的 RV64 构建覆写的生成文件到 HEAD，并按 `03-rv64-release-pre-manifest.txt` 恢复原始 mtime。
- `docs/Work_Log/evidence/2026-07-18/03-interrupted-build-cleanup-receipt.txt` — 差异识别、精确恢复命令、哈希/mtime 与最终 Git 状态回执。

**验证：**
- `GIT_MASTER=1 git diff --exit-code -- os/src/lang_items.rs user/src/lang_items.rs` ✅（exit 0）。
- 未执行 Docker、QEMU、构建、下载/安装、提交或任何非上述两份产品源文件的恢复。

**备注：** 最终工作树仍保留 `.omo/boulder.json`、`docker-compose.yml` 和既有 docs/evidence/architecture 状态；本次未触碰它们。

## Wave 0 Todo 1: Freeze external contracts and repository map

**涉及文件：**
- `docs/architecture/2026-07-18-mangocore-contract-map.md` — 主合同映射文档，记录所有公有 Make 目标、消费者、输入/输出产物、架构/Profile、Docker 镜像、QEMU 驱动拓扑，以及未知条目的 Owner/退出条件
- `docs/architecture/2026-07-18-mangocore-contract-matrix.yaml` — 机器可读的 YAML 矩阵（v2 schema），枚举所有根 Makefile + os/Makefile 目标（~115 个），含 status/consumer/description
- `docs/architecture/2026-07-18-verify-contract-map.sh` — 只读验证脚本（v3），四项门禁：stale-state（exact commit + 严格 dirty-path manifest）、coverage（纯 sed 提取 Makefile 目标，无 make -pn 污染；精确集合比对，comm -23/-13 检测遗漏/陈旧条目）、negative probe（自动删除一个 os 目标重新验证，确认 exit nonzero）、factual（关键合同声明验证）

**v3 修正（最终 targeted repair）：**
- 目标提取改用纯 `sed` 文本解析，消除 `make -pn` 递归子 make 污染（root 只看到 root 的 18 个目标，os 只看到 os 的 56+2 个目标）
- YAML 比对改为 `comm` 精确集合差集：`comm -23`（Makefile 有、YAML 缺=遗漏）+ `comm -13`（YAML 有、Makefile 缺=陈旧）
- 增加 `os_makefile_extra_targets()` 处理特殊目标：`rv64`（来自 `$(ARCHS)` 展开）、`user`（来自 initramfs 前置条件隐式解析）
- 严格 dirty-path manifest：`\\.omo/boulder\\.json|docs/architecture/|docs/Work_Log/` — 任何未声明路径（tracked M 或 untracked ??) 均为硬失败
- YAML git_hash 字段精确解析（`awk '/^metadata:/.../^  git_hash:/{print $2}'`），文档 hash 同理
- CI 消费者标注修正：regression/bugscan/ktest/check-fast/unittest 全部标注 NOT consumed by CI
- judge 作用域明确声明：所有 `judge/judge_*.py`（23 个可执行评分脚本），非 judge 文件单独列出
- os/Makefile `all` 产出标注 `rootfs-rv.img, rootfs-la.img`
- 增加 `rv64` 目标到 os/Makefile 文档表和 YAML

**验证：**
- v3 验证脚本 87/87 通过，exit 0（零预期失败漏洞）
- 四项门禁全部 PASS：stale-state（exact commit + 严格 manifest）、coverage（root 18/18 + os 58/58 精确集合匹配 + judge 46 项）、negative probe（删除 `laclean`→exit 1 检测到遗漏）、factual（12 项声明全部正确）
- 覆盖范围：根 Makefile 18 目标 + os/Makefile 56 目标 + 2 特殊目标（rv64, user）+ CI + judge 23 脚本 + rv64.mk + la64.mk + 6 profile 差异
- 未知条目 11 条，每条有 Owner 和退出条件
- 未修改任何产品构建/启动代码。未执行 Cargo、QEMU、Docker build。未提交。

**验证命令（只读）：**
```bash
# 主验证（exit 0 = 通过）
bash docs/architecture/2026-07-18-verify-contract-map.sh
```

### Wave 0 Todo 2：官方容器来源与构建变异基线

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/02-{commands,git-hash,container-id,config,manifests,freshness}.txt` — 官方镜像摘要、容器挂载、工具链/环境、Git、Cargo/vendor、前后变异和新鲜性证据。
- `docs/Work_Log/evidence/2026-07-18/02-qemu-{output,head-tail}.txt` — Todo 2 明确禁止 QEMU 的 N/A 证据。

**验证：**
- 官方本地镜像 `zhouzhouyi/os-contest@sha256:5c04dbc38562b1cd578c33c9cd321d4731cb8cdd00c82b2320a4350754faa6b0` 下，串行 `make -C os rv64-kernel-build-only MODE=release` 退出 0（2m17s；197 warnings）。
- 记录到现有构建会写入两份 tracked `lang_items.rs`、增加 source/user tree 条目并改变容器 `RUSTUP_HOME`；vendor 路径未出现在 post-build Git 状态。
- 未执行 LA64、QEMU、setup 或配置注入；现有 RV64 构建图实际执行了 `rustup component add rust-src`，日志证明发生 rust-src 下载/安装及 RUSTUP_HOME 变异。

**备注：** 首次完整 RUSTUP_HOME 内容清单在 120 秒超时，作为显式 hung-command 观察保留；镜像是本地 cached/missing-pull 状态，未宣称仓库端新鲜。清理仅移除了临时容器，未回滚观测到的工作树变异。

### Wave 0 Todo 2：仅恢复基线构建生成的 tracked 源变异

**涉及文件：**
- `os/src/lang_items.rs`、`user/src/lang_items.rs` — 仅以 `git restore --source=HEAD --worktree` 恢复 RV64 基线构建复制造成的变异。
- `docs/Work_Log/evidence/2026-07-18/02-{commands,git-hash,manifests,freshness}.txt` — 追加前后 Git 状态、source/vendor/RUSTUP_HOME 清单和清理回执。

**验证：**
- 两条指定路径的 `git diff --exit-code` 为 0；vendor 前后内容清单相同。
- 该清理步骤未额外执行构建、QEMU、下载、安装或提交；未触碰 `.omo/`、文档、Cargo 配置、target 输出及其他用户状态。

### Wave 0 Todo 2：验证器要求的原始 provenance 与 mtime 重测

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-18/02-raw-{docker-image-inspect,container-mount,rust-toolchain,required-tools}.txt` — 有界原始镜像、挂载、Rust/rustup 和必需工具输出。
- `docs/Work_Log/evidence/2026-07-18/02-rv64-baseline-result.txt` — 唯一允许的 RV64 重测、前后 mtime/hash、原始日志定位与恢复回执。

**验证：**
- RV64 窄构建 exit 0；随后仅恢复 `os/src/lang_items.rs` 和 `user/src/lang_items.rs`，并精确恢复预构建 mtime。
- 产物 `kernel-rv` 与内核 ELF 均晚于恢复后的源/配置输入；vendor 哈希未变。

### Todo 0: Update dev container image tag from 20260104 to 20260510

**涉及文件：**
- `docker-compose.yml` — `image:` 字段从 `zhouzhouyi/os-contest:20260104` 改为 `20260510`
- `docs/architecture/2026-07-18-mangocore-contract-map.md` — 同步更新 Development container 条目

**操作：**
- `docker pull zhouzhouyi/os-contest:20260510` → 成功，exit 0
- 镜像 digest: `sha256:85dec949df7cef41fd03d30c6ad69f952204540e18d2c62bced9d2e262fef12d`
- Image ID: `60e9bfa0ecdc`, 大小 19.6GB, 构建时间约 2026-05-10
- `docker compose config` → 确认 resolved image 为 `zhouzhouyi/os-contest:20260510`

**Toolchain 清单（容器内采集）：**
- 默认 rustc: `1.86.0-nightly (6067b3631 2025-01-17)`，LLVM 19.1.7
- 默认 cargo: `1.86.0-nightly (088d49608 2025-01-10)`
- Active toolchain: `nightly-2025-01-18-x86_64-unknown-linux-gnu` (default)
- 已安装 toolchains: stable, nightly-2024-02-03, 2024-05-01, 2024-08-01, 2025-01-18, 2025-02-01, 2025-05-20
- 已安装 targets: `riscv64gc-unknown-none-elf`, `riscv64imac-unknown-none-elf`, `loongarch64-unknown-linux-gnu`, `loongarch64-unknown-none`, `x86_64-unknown-linux-gnu`
- QEMU: 10.0.2 (both riscv64 and loongarch64)
- gcc: 13.3.0, make: 4.3, python3: 3.12.3
- OS: Ubuntu 24.04.4 LTS (Noble)
- **2026-dated nightly: NOT available** — 最新 nightly 为 2025-05-20。如需 2026 nightly 需手动 `rustup toolchain install`

**证据：**
- `docs/Work_Log/evidence/2026-07-18/00-docker-update-{pull,compose-config,rust-toolchain,system-info,git-diff,container-cleanup}.txt`

**备注：**
- 首次 pull 300s 超时（镜像较大），重试 600s 成功
- CI workflows (`.github/workflows/`) 保持 `20260104`，待后续迁移
- 所有临时容器已 stop+rm，无残留
