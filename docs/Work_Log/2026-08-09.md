## 2026-08-09

### TCP/UDP close 后 smoltcp SocketSet 缓冲回收

**涉及文件：**
- `os/src/net/socket/{mod.rs,inet/datagram/udp.rs,inet/stream/{inner.rs,mod.rs}}` — UDP/TCP 最后一个 `Arc` 析构只发布按 route 去重的回收请求；TCP 不再在 `Drop` 中取得 `DeviceStack` 锁，实际 close/abort 移至 worker。
- `os/src/net/config.rs`、`os/src/task/manager.rs` — worker 立即移除 UDP，按 smoltcp `poll_at()` 和 15 秒硬 deadline 重驱动 TCP close；generation timer 只唤醒 worker，route/stack 缺失和 binding 缺失均记录 warning。
- `os/src/kernel_tests/net_smp.rs` — 新增无后续流量的 8 轮 UDP 大缓冲 close ktest，每轮要求 SocketSet UDP 数与 pending 回到基线。
- `docs/06_net/device-stack-and-poll.md`、`docs/00_overview/AI-Usage-Report.md` — 同步回收协议、并发边界和 AI 根因分析记录。

**验证：**
- Docker：`make kernel ARCH=rv64 PROFILE=regression` ✅；随后串行 `make kernel ARCH=la64 PROFILE=regression` ✅。
- Docker RV64：`make -C os rv64-ktest PROFILE=regression KTEST=net_smp KTEST_QEMU_TIMEOUT=120` ✅，`8 passed, 2 skipped, 0 failed`；`net_smp::dropped_udp_buffers_reclaim_without_traffic` 通过。
- heap 代理结论：每轮创建的 UDP socket 含 RX/TX 各 64 KiB smoltcp buffer；8 轮 close 后 SocketSet UDP 数均回到基线，未保留待删 route，因而该路径不再单调保留 payload。未运行 netperf TCP_CRR/heap-used 长测（共享 BuildStorm QEMU 正在占用 CPU）。

**备注：** 首次全量 RV64 ktest 因默认 30 秒总 QEMU budget 在无关的 ext4_another #86 后被 timeout，未抵达 net 组；上述 120 秒 focused net_smp 是本批运行时结论。未暂存、未提交。

### develop 与 SMP PageCache/MM 静态回归比对

**涉及文件：**
- `os/src/fs/page_cache.rs`、`os/src/fs/ext4_another/page_cache.rs`、`os/src/fs/reclaim.rs`、`os/src/task/processor.rs` — 只读比较 `develop`（`a0a37247`）与指定基线 `d7b868b5` 的脏页水位、写回、clean eviction 和 CPU0 reclaim 调度。
- `os/src/mm/frame_allocator.rs`、`os/src/mm/heap_allocator.rs`、`os/src/mm/address_space.rs` — 只读比较 frame OOM recovery、运行时堆和 ELF/exec 内存压力路径。

**验证：**
- 静态：`git merge-base --is-ancestor develop d7b868b5` 为真；`frame_alloc()` 与 heap `recover_for_inner()` 仍在分配失败后调用 `oom_handler()`，未发现 SMP 直接跳过 OOM recovery 的差异。
- 未运行 QEMU 或构建：用户要求本轮仅作静态定位；无源码修改，因此不适用 LSP/双架构构建。

**备注：** `d7b868b5` 之后工作树已推进到 `c09354e4`；本条仍以用户指定基线作结论。PageCache 的 free-ratio gate 被 SMP 冲突处理移除，但该 gate 只会使 develop 在空闲帧充足时延后写回，不能解释 SMP 更早 ENOMEM；clean eviction/reclaim worker 调度在两侧等价。未暂存、未提交。

### L4 零盘文件压力调查：确认 RamFS 容量边界，未伪造 PageCache 修复

**涉及文件：**
- 无保留源码修改。临时用户态 `fs_pressure` probe 与 focused LTP 配置已在本次调查结束前删除。

**验证：**
- Docker RV64 L4 基线：`make test ARCH=rv64 PROFILE=regression` ✅，`24 passed, 0 failed, 1 skipped`。
- Docker RV64、`QEMU_MEMORY=128M` 临时 pressure probe：第 3 个 8 MiB 文件的第 786 页 `write()` 返回 `-12`；WARN 日志显示 `frame_reserve` 剩余 0，`oom_handler()` 只回收匿名 VMA，未出现 kernel panic。
- 运行时/源码交叉确认：regression QEMU 不附加块盘；`scripts/build_initramfs.sh` 只创建 `/sdcard` 目录，故其位于 root RamFS。每个新 4 KiB 写直接由 `RamFSInode::alloc_page()` 取得 `frame_alloc()` 的物理页；它不是可写回、可 clean-evict 的 ext4 PageCache。该 ENOMEM 是 RAM-backed 文件数据无法回收时的正确容量边界，不能据此修改 PageCache 或把 ENOMEM 吞掉。
- 正常 profile 的 `fallocate05` focused 尝试被当前本地测试盘阻断：runner 仍读取镜像内原 `os_test.conf` 的 `mask=0x003`，且 `/musl`/`/glibc` 缺少基础脚本；没有把它误报为 LTP 结论。

**备注：** L4 零盘无法等价覆盖 `/dev/vdb2` 的持久 ext4 写回/clean eviction。后续必须使用包含完整 LTP 资产的测试盘复现 `fallocate05`，或增加明确拥有非 RAM backing 的 regression fixture；在该 RED 前不对 `frame_alloc()` 直接调用 FS reclaim，避免在未知调用方锁上下文引入反向锁序。Linux 6.6 对非页对齐 truncate 由 page-cache 清零与文件系统 block-tail 持久化共同保证，现有 L4 truncate 覆盖保持通过。未暂存、未提交。

### 动态内核堆与 256 页预取上限：消除 BuildStorm 早期堆 OOM

**涉及文件：**
- `os/src/mm/heap_allocator.rs`、`os/src/mm/frame_allocator.rs`、`os/src/mm/mod.rs` — 保留 8 MiB bootstrap heap；帧分配器建立后从 fresh pool 永久保留连续 DRAM backing，建立第二个不重叠的 runtime `MetadataHeap`。目标容量为 `clamp(usable_memory / 10, 64 MiB, 1 GiB)`；分配优先 runtime，释放按 backing 地址路由，统计合并两区。
- `os/src/hal/{mod.rs,arch/mod.rs,arch/riscv/config.rs,arch/loongarch64/config.rs}`、`os/src/fs/procfs/files/config.rs`、`os/src/panic_diag.rs`、`os/src/random.rs` — 将固定 `KERNEL_HEAP_SIZE` 语义拆为 bootstrap 常量和运行时容量查询；I/O bounce 上限固定为既有 256 KiB，避免将运行时容量引入 const 路径。
- `os/src/fs/page_cache.rs`、`os/src/fs/ext4_another/page_cache.rs`、`os/src/kernel_tests/page_cache_sync.rs` — backend 批量 read staging 上限为 256 页（1 MiB）；大于上限的预取拆批；another_ext4 防御性拒绝超限调用；新增 257 页回归 ktest。
- `docs/04_mm/{README.md,initialization-and-kernel-space.md}`、`docs/03_fs/page-cache.md` — 同步运行时堆初始化顺序、永久 backing 约束和预取限批。

**验证（Docker，RV64/LA64 严格串行）：**
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=page_cache_sync` ✅，TAP `1..6`、`pass 6`；新增 257 页限批用例通过。启动日志显示 1 GiB QEMU 下 `kernel heap: target=106704896 usable=1073340416 bytes`。
- `make kernel ARCH=rv64 PROFILE=normal` ✅；随后 `make kernel ARCH=la64 PROFILE=normal` ✅。
- BuildStorm 专用 RV64 kernel 构建 ✅；临时 qcow2 x0 overlay 的实机 QEMU 运行到 `BUILDSTORM_TOOLCHAIN ok` 与 `BUILDSTORM_MINIBUILD ok`，未见 `heap_alloc alloc failed` 或 OOM。其后 cargo/rustc 创建工作目录返回 `EFAULT`，未将其表述为本工作包已修复。

**备注：** `MetadataHeap` 没有 add-memory API，不能重建 bootstrap heap，因为帧分配器的 `Vec` 元数据已从其中分配并长期存活；因此采用双 backing 而非重建。未暂存或提交。

### CPU 数量动态探测：FDT `/cpus` → 运行时 CPU 数（Linux NR_CPUS/nr_cpu_ids 双轨制）

**涉及文件：**
- `os/src/hal/firmware/mod.rs` — `MemoryRegionBuf` 新增 `cpu_count` 字段；新增 `cpu_count()` 读入口；`populate_from_static`（2K1000 板级回退）显式清零。
- `os/src/hal/firmware/fdt.rs` — 新增 `count_cpus()`：DFS 遍历 `/cpus` 深度 2 子节点，判据为节点名恰为 `cpu`（排除 `cpu-map`）或 `device_type == "cpu"`，截断到 `MAX_CPUS`；在 `parse_memory_regions` 成功后尽力调用（解析失败不回退固件发现）。
- `os/src/smp.rs` — 新增 `runtime_cpu_count()`（`min(FDT 探测, MAX_CPUS)`，探测失败回退 `CONFIGURED_CPU_COUNT`，恒 ≥1）；`expected_online_mask()`/`configured_cpu_count()` 从 const 改普通 fn 用运行时值；AP 启动循环、`send_ipi`、`secondary_main`、`synchronize_kernel_mapping` 等语义门禁用运行时值；`cpu_diagnostics`/`mark_cpu_online`/`cpu_id`/`try_local_per_cpu`/`task_state` 等数组界断言改 `MAX_CPUS`；`register_cpu_entry` 因在固件解析前运行改按 `MAX_CPUS`（纯数组防护），拓扑门禁交给 `secondary_main`。
- `os/src/hal/arch/riscv/mod.rs`、`os/src/hal/arch/loongarch64/mod.rs` — `IdleStacks` 数组与 `enter_secondary_idle` 断言改用 `MAX_CPUS`（固定 8 份，Linux `__per_cpu_offset[NR_CPUS]` 模式）。
- `docs/01_architecture/boot-and-trap.md` — 构建/验证小节说明运行时 CPU 数来自 FDT `/cpus`，`MANGO_CORE_NUM` 仅作兜底。

**验证（RV64/LA64 严格串行，Docker 容器）：**
- `make kernel ARCH=rv64 PROFILE=normal CORE_NUM=4` ✅；随后 `make kernel ARCH=la64 PROFILE=normal CORE_NUM=4` ✅。改动文件无新增 warning（既有 warning 基线无变化）。
- **单镜像多核数（核心价值）**：CORE_NUM=4 编译的 RV64 镜像分别跑 `-smp 1/2/4/8` → `configured=1/2/4/8`、`online_mask=0x1/0x3/0xf/0xff` 全对齐；CORE_NUM=8 镜像跑 `-smp 8` → `0xff`。LA64 同款 `-smp 1/2/4/8` → `0x1/0x3/0xf/0xff`。
- 用户 buildstorm 场景：`cd /app/os && make rv64-run CORE_NUM=4 IMAGE_ROLE_RV64_COMPETITION_X0=../fs-img-dir/sdcard-rv-pub.img` → 正常进入内核（`configured=4 online_mask=0xf`）、initramfs/VFS/mount 全流程无 panic。
- 排查过程发现两个问题并修复：① QEMU FDT `/cpus` 含 `cpu-map` 拓扑节点，原 `starts_with("cpu")` 判据误计 → 改为精确名 `cpu` + `device_type`；② `register_cpu_entry` 在固件解析前调用，原按运行时值会误 park 冷启动 hart（如 8 核下 boot hart 7 ≥ 编译期 4）→ 改按 `MAX_CPUS`。

**备注：** `make lint` 因 baseline 过期失败（`ipi_send_failures`、`discover_early_resources` 等 dead-code warning 来自 205a2cd5 之后、当前 HEAD 之前提交，与本次改动无关，`git show HEAD` 已确认这两处死代码在 HEAD 即存在）。未暂存或提交；`os/src/task/threads.rs` 未动。

### 干净 pub 镜像的 BuildStorm EIO 安全复验（受 cargo 启动挂起阻断）

**涉及文件：**
- `docs/Work_Log/evidence/2026-08-09/buildstorm-eio-verify-rv64-qcow2-final.log` — 最终 RV64、4 核 QEMU 的完整串口日志；x0 为临时 qcow2 覆盖层。
- `docs/Work_Log/evidence/2026-08-09/buildstorm-eio-verify-rv64-qcow2-debug.log` — 2 核 debug 诊断日志，记录 `cargo --version` 后的 futex 挂起状态。
- `docs/Work_Log/evidence/2026-08-09/buildstorm-eio-verify-rv64-image-integrity.log` — 原始 pub 镜像的只读 debugfs 与最终磁盘空间记录。

**验证：**
- Docker：`make toolchain-preflight` ✅（`nightly-2026-05-10`）；`make kernel ARCH=rv64 PROFILE=normal` ✅。随后为与用户命令一致的 SMP QEMU 重建 `CORE_NUM=4 LOG=off make kernel ARCH=rv64 PROFILE=normal` ✅（仅既有 warning）。
- 安全运行：x0 使用 `/tmp/buildstorm-eio-verify-final-rv64.qcow2`，其 raw backing 固定为 `/app/fs-img-dir/sdcard-rv-pub.img`；最终 overlay 仅 708 KiB，退出时已删除。最终 4 核启动确认 `configured=4`、`online_mask=0xf`。
- **未通过/未能判定 EIO 修复：** BuildStorm 到达 `STEP2 toolchain-check`，`rustc --version` 成功输出，但随后 `cargo --version` 的 guest 进程停在 futex 等待；1200 s 后 host timeout（exit 124）。因此未进入 `STEP3 cargo new`，没有 `BUILDSTORM_MINIBUILD ok`，也没有实际的 `BUILDSTORM_COMPILE` 结果行。最终日志未出现 `Input/output error`、`BRIDGE EIO` 或 page-cache writeback EIO，但因 `/tmp/minibuild` 尚未被访问，这不能作为 EIO 修复的 PASS 结论。debug 日志显示等待任务为 `pid=48/tid=50`，在 `futex` 后无后续 I/O syscall；该新阻断与历史 page-cache EIO 尚未建立因果关系，未扩大修改范围。
- 镜像完整性：原始 `sdcard-rv-pub.img` 仍为 15,032,385,536 bytes、`2026-07-19 15:47:40 +0800`；只读 `debugfs` 可列出根目录及 `/work/tgoskits`。最终宿主可用空间 36 GiB（96% 已用）。

**备注：** 未使用原始镜像作为可写盘，未修改 `buildstorm_testcode.sh`、未暂存或提交；这次只产生证据与 Work Log。需要先独立处理或绕开 cargo toolchain-check 的 futex 挂起，再以同一 qcow2 覆盖层完成 `/tmp/minibuild` 判定。

### BuildStorm `/tmp/minibuild` 写路径 EIO 调试（待干净 pub 镜像复验）

**涉及文件：**
- `os/src/fs/ext4_another/page_cache.rs` — backend 保留其唯一 I/O 所需的 `logical_size: Arc<AtomicUsize>`，不再弱升级整个 `InodeLifetime`。继续保持 filesystem 为 `Weak`，避免恢复 `fs → lifetime → dirty PageCache → backend → fs` 环。

**根因分析：** 当前 worktree 的 weak-lifetime 版本让 dirty PageCache 在临时 VFS inode 已销毁后仍可由 `sync_lifetimes()` 取得，但 backend 在 `read_page`、`read_pages`、`write_pages` 或 `npages` 中升级 lifetime 失败，直接返回 `SyscallErr::EIO`。这条路径绕过 `ext4_another/errno.rs`，因此用户日志没有 `[ext4_another] BRIDGE EIO`；它对应 buildstorm 的 `sync in chroot` 后首次 `/tmp/minibuild` unlink/create/lookup EIO。修复仅保持大小原子，既覆盖 writeback 所需状态，又不重新形成 lifetime 与 dirty cache 的引用环。

**验证：**
- Docker：用户给定的根目录命令在本 checkout 中不存在 proxy target（`make: *** No rule to make target 'rv64-run'`）；等价执行 `/app/os` 的 `make rv64-run CORE_NUM=4 IMAGE_ROLE_RV64_COMPETITION_X0=../fs-img-dir/sdcard-rv-pub.img` 重现 `sync in chroot` 后 `rm: cannot remove '/tmp/minibuild': Input/output error`，且未见 bridge EIO。
- Docker：修复后的 isolated RV64 release/debug kernel build（`CORE_NUM=1`，避免覆盖用户产物）✅，仅有既有 warning。
- **BLOCKED：** 当前可写 `sdcard-rv-pub.img` 已在后续 debug rebuild 的 ext4 journal recovery 阶段返回 `[ext4_another] BRIDGE EIO: raw=EIO`，无法挂载 `/sdcard`；以临时 single-core kernel 配置启动 supplied four-hart QEMU 又因 nonzero OpenSBI boot hart 在 kernel 输出前超时。故尚未将 `BUILDSTORM_MINIBUILD ok` 或 EIO 消失报为通过；需要从干净 pub 镜像重新运行同一命令复验。

**备注：** syscall 430 是 RISC-V Linux generic ABI 的 `fsopen(2)`；mount 工具探测新 mount API 时收到 ENOSYS 后继续 fallback，和 EIO 不同源。未改 buildstorm 脚本、未暂存或提交；未命中除 Work Log 外的 `code_paths` 文档，AI-Usage-Report 无需更新。

### CI clippy features 对齐真实内核构建（补 oom_handler）

**涉及文件：** `.github/workflows/ci.yml` — clippy job 两条命令 features 对齐 `os/make/rv64.mk:102` / `la64.mk:118`：rv64 改为 `riscv block_virt oom_handler`（`riscv` 本身不含 oom_handler，必须显式加），la64 改为 `boot_la_qemu block_virt_pci oom_handler`（`boot_la_qemu` 已含 oom_handler，显式列出保持一致）。真实构建缺省 `LOG_OPTION=log_off`，clippy 不补 log feature（无 cfg 语义差异）。

**验证：** Docker 容器内 `cargo clippy --features "riscv block_virt oom_handler" -- -W warnings` 退出 0、无 error；`do_deep_clean`（`mm/address_space.rs` 与 `task/manager.rs:584` 均 `#[cfg(feature = "oom_handler")]`）正常编译。旧集合 `riscv block_virt` 与新集合均 0 error，新集合覆盖 swap/zram/deep-clean 真实代码路径，warning 量不增（3635→3627）。la64 仅改 ci.yml 文本，未本地编译。os/make 内无 clippy 目标（仅根 Makefile `check-fast` 的 host 便捷目标，不镜像内核构建，未动）。未暂存或提交。

### P1：清除 CI clippy 阻断并让 CI 合同与 QEMU 管道 fail-closed

**涉及文件：**
- `os/src/fs/page_cache.rs` — 为仅由 `with_bytes_mut` 的写锁与 HRTB 闭包约束的私有 raw-slice helper 加窄域 `#[allow(clippy::mut_from_ref)]`；不能改为 `&mut self`，否则会破坏 `PageEntry` 的共享所有权模型。
- `os/src/smp.rs` — AP 启动枚举从常量 range 改为 `PER_CPUS.iter().enumerate().take(CONFIGURED_CPU_COUNT).skip(1)`；单核不迭代，多核仍恰好启动逻辑 CPU 1..N-1。
- `.github/workflows/ci.yml` — 移除 toolchain 合同 job 的 `continue-on-error`，并在两个 QEMU 内层 `bash -lc` 的 `make | tee` 前启用 `set -euo pipefail`；外层也使用相同 fail-closed 设置。

**验证：**
- Docker CI 同款 clippy，严格串行：RV64 `cargo clippy --features "riscv block_virt" -- -W warnings` ✅；随后 LA64 `CARGO_BUILD_TARGET=loongarch64-unknown-linux-gnu cargo clippy --features "boot_la_qemu block_virt_pci" -- -W warnings` ✅。两者均退出 0，`clippy::mut_from_ref` 与 `clippy::reversed_empty_ranges` 均不再报错（既有 warnings 未升级为 CI error）。
- Docker 严格串行内核构建：`make kernel ARCH=rv64 PROFILE=normal` ✅；随后 `make kernel ARCH=la64 PROFILE=normal` ✅。
- CI ktest job 的 `rv64-ktest`/`la64-ktest` 分别转发到各架构 makefile 的 `ktest-run`；该目标已要求 QEMU 退出 0、非空日志和 `[KTEST RESULT: PASS]`，故 grep 判定在 CI 调用链中生效。

**备注：** CI 中仅有两条 `make | tee` 管道，均已补齐内层 pipefail；toolchain 合同是所有主 job 的 `needs`，不应 soft-fail。未修改 unsafe 实现或运行时语义，因此未新增 Miri 测试；direct-map helper 的硬件路径不适用于 host Miri。已检查 `code_paths` 文档和 AI-Usage-Report：本次仅 lint/CI 合同改动，现有文档及已列出的 GPT-5.6-terra 工具记录无需更新。未暂存或提交。

### P1：对齐 OOM 深度回收 ktest cfg，并以结果标记判定 ktest

**涉及文件：**
- `os/src/kernel_tests/mm.rs` — 将 `test_shared_futex_pin_blocks_reclaim` 及其 `KernelTest` 注册项置于 `#[cfg(feature = "oom_handler")]`；测试内两个 `do_deep_clean()` 调用不再在无 OOM feature 的 clippy 配置中引用被 gate 的 API。
- `os/make/rv64.mk`、`os/make/la64.mk` — `ktest-run` 将 QEMU 输出分别写入 `/tmp/ktest-{rv,la}.log`，保留并显示 QEMU 退出状态，同时要求非空日志包含固定字符串 `[KTEST RESULT: PASS]`；缺失标记或 QEMU 非零退出均使 make 失败。

**验证：**
- Docker CI 同款 RV64 clippy（`cargo clippy --features "riscv block_virt" -- -W warnings`）修改前稳定报 `kernel_tests/mm.rs:110,128` 的 E0599；修改后这两项 E0599 消失。命令仍被现有未提交的 `os/src/fs/page_cache.rs:585` `clippy::mut_from_ref` 和 `os/src/smp.rs:1767` `clippy::reversed_empty_ranges` 阻断，未修改用户工作区。
- Docker CI 同款 LA64 clippy（`CARGO_BUILD_TARGET=loongarch64-unknown-linux-gnu cargo clippy --features "boot_la_qemu block_virt_pci" -- -W warnings`）同样未出现 E0599，仍仅被上述两项既有 clippy deny 阻断。
- Docker：`make -C os ktest-run ARCH=rv64 PROFILE=normal` 退出 0；147 total、137 passed、10 skipped、0 failed，日志含 `[KTEST RESULT: PASS]` 且 make 输出 `=== KTEST PASS ===` ✅。
- Docker：在 RV64 ktest 后串行 `make kernel ARCH=la64 PROFILE=normal` ✅。

**备注：** 当前 `mm.rs` 只有一个包含两处 `do_deep_clean()` 调用的测试函数，故同时 gate 该函数和唯一注册项；未删除 `AddressSpaceInner::do_deep_clean` 的 API gate，未改 ktest 用例，未暂存或提交。已检查 `docs/` 的 `code_paths` 命中，现有测试说明不需为构建判定实现改写；本次规则已由 AGENTS.md 覆盖，未新增 reference 或 AI-Usage-Report 条目。

### 修复 merge 回退导致的 devtmpfs、remount RDONLY 与 PID1 persistent mount 缺失

**涉及文件：**
- `os/src/syscall/fs/sys_mount.rs` — 恢复 `devtmpfs` 到共享 `DEV_FS` 的分支；将 `RDONLY` 纳入 `MS_REMOUNT` 的 non-atime 覆盖集，使 VFS mount flag 通过现有 Release-store / 写路径 Acquire-check 原子切换。
- `user/src/bin/init.rs` — PID1 复用内核 `mount_boot_block_devices()` 已建立的 `/sdcard` 与 `/tools`，改调 `setup_persistent_mounts()`；BuildStorm chroot 同样不再重挂 `/dev/vda`。
- `user/src/bin/init/mounts.rs` — 为成功的伪文件系统 mount 输出可审计日志，运行时直接证明 devtmpfs 成功挂载。
- `os_test.conf` — 验证期间临时设为 `mask=0x007, skip_apk=true`，结束后恢复 `mask=0x003, skip_apk=false` 并重新注入 RV64 镜像。

**验证：**
- Docker `make toolchain-preflight`：`nightly-2026-05-10` ✅。
- Docker 串行编译：RV64 `make build ARCH=rv64 PROFILE=normal`，随后最终源码的 `make image ARCH=rv64 PROFILE=normal && make run ARCH=rv64 PROFILE=normal` ✅；LA64 随后 `make build ARCH=la64 PROFILE=normal` ✅。
- RV64 QEMU（`mask=0x007, skip_apk=true`）：输出 `[init] mount devtmpfs at /dev`、`/tmp is bind-mounted from ext4 /sdcard/tmp`、`/etc is bind-mounted from tools disk`，并完成 `/tools/{bin,sbin,lib,usr}` bind；未见 mount `-19` 或 `-16`，未见 ENOSPC 风暴 ✅。
- 该本地 x0 镜像不含 `/musl`/`/glibc` 内的 `basic_testcode.sh`、`busybox_testcode.sh`、`lua_testcode.sh`，六个组均为 shell 的 `No such file or directory`（exit 32512）；因此 basic/busybox 与 lua 不能在此镜像上判定恢复，未将其误报为通过。

**备注：** 首次 `make run` 仅因先前执行 `make build` 未生成 `rootfs-rv.img` 失败；确认 `make image` 才生成该运行资产后重跑成功。无源码 workaround 或 SMP 调度改动，未提交。

### 修复 ktest 收尾 another_ext4 PageCache writeback EINVAL

**涉及文件：**
- `os/src/fs/ext4_another/page_cache.rs` — `AnotherExt4PageCacheBackend` 对 filesystem 和 inode lifetime 都改持 `Weak`；实际 I/O 时短暂升级，打破 `fs → lifetime → dirty PageCache → backend → fs/lifetime` 强引用环。
- `docs/Work_Log/evidence/2026-08-09/pc-writeback-{rv64,la64}-core4-final.log` — 双架构 4 核完整 ktest 的修复后原始串口输出。

**根因：** another_ext4 的 dirty lifetime 强持有 PageCache，而 PageCache backend 又强持有 filesystem 与 lifetime。独立 ktest fixture 离开作用域后，该环仍保留已删除 inode 的 PageCache；最终全局 flush 对 inode 12 的 8192-byte/64-byte 残留 cache 写回，backend 返回 `EINVAL`。该错误首次由 `ext4_another::fsync_and_syncfs_surface_flush_failures` 后的全局 flush 暴露。

**验证：**
- Docker focused RED：`KTEST=ext4_another CORE_NUM=4 ... ARCH=rv64` 在 20/20 TAP 通过后仍输出两次 `page-cache writeback failed: EINVAL`。
- Docker focused GREEN：相同 RV64 命令 20/20 TAP 与 `[KTEST RESULT: PASS]`，无 writeback EINVAL/EIO。
- Docker 串行完整 4 核：RV64 `147 passed, 0 failed`，LA64 `139 passed, 0 failed`；两者均进入 `committing filesystems before shutdown` 后直接输出 `[KTEST RESULT: PASS]`，无 writeback 错误 ✅。

**备注：** 修复保留 PageCache 对 backend 错误的传播，不吞掉或跳过写回；只移除造成 detached fixture 常驻的 ownership cycle。未修改任何 ktest 用例，未提交。

### smp merge state 双架构总回归验证

**涉及文件：**
- `os_test.conf` — 仅在派生 x0 注入期间临时设为 `mask=0x003, skip_apk=true`，验证后恢复原值。
- `docs/Work_Log/evidence/2026-08-09/{rv64-ktest-core4,rv64-regression,la64-derived-basic-busybox-skip-apk,la64-regression,la64-ktest-core4}.log` — 保留本次高风险 QEMU 运行的完整原始输出。

**验证：**
- Docker toolchain preflight：`nightly-2026-05-10` ✅。
- RV64 单核派生 x0：`mask=0x003, skip_apk=true`，basic-musl/basic-glibc/busybox-musl/busybox-glibc 均 `exit_code=0` ✅。
- RV64 8 核：先以 `CORE_NUM=8` 重建内核（避免 derived-comp 复用旧的单核产物），再运行相同派生 x0；`online_mask=0xff`，四组均 `exit_code=0` ✅。
- RV64 regression：`24 passed, 0 failed, 1 skipped, 25 total`，包含 `wait_restart` PASS ✅。
- LA64 派生 x0：`CONF_BLK_MODE=virt_pci`，FDT QEMU fallback、PID1 与四个 basic/busybox 组均成功；最终的 `skip_apk=true` 重注入运行中四组均 `exit_code=0` ✅。
- LA64 regression：`24 passed, 0 failed, 1 skipped, 25 total`，`STATE=PASS STATUS=0`，PID1 修复后完整进入用户回归 ✅。
- RV64 4 核完整 ktest：147/147 TAP PASS，page_cache #72--#76 全 PASS，未见 heap OOM；但收尾 page-cache commit 返回 `EINVAL`，最终 `[KTEST RESULT: FAIL]`。
- LA64 4 核完整 ktest：139/139 TAP PASS，page_cache #72--#76 全 PASS，未见 heap OOM；同样在收尾 page-cache commit 返回 `EINVAL`，最终 `[KTEST RESULT: FAIL]`。

**备注：** 两架构相同的 ktest 收尾失败说明 `page_cache::global_flush_releases_registry_before_writeback` 的单测本体已通过，但 ktest runner 在“committing filesystems before shutdown”路径仍有独立的 `EINVAL`；本工作包未做无根因 workaround。RV64/LA64 regression 和两次派生 x0 均未复现历史 `smp::user_task_reschedules_and_sets_affinity` 或 net_smp 停顿。宿主 Python 为 3.8，无法解析注入脚本使用的 `match` 语法；已在确认 `/app` 为当前工作树 bind mount 后，在 Docker Python 3.12 中执行同一 conf-inject 命令。

### 修复 RV64 QEMU virt `/soc` early-MMIO 深度过滤启动回归

**涉及文件：**
- `os/src/hal/firmware/fdt.rs` — early-MMIO 改为只接受根设备或根总线直接子设备中同时具有非空 `compatible` 与 `reg` 的节点；`reserved-memory` 仍优先保留，PCI host `ranges` 复用同一设备判据。深度超过 2 的节点继续排除，避免将子总线局部 `reg` 偏移作为物理 MMIO。
- `os/src/kernel_tests/platform_fdt_snapshot.rs` — 新增 QEMU virt FDT 回归断言：PLIC、UART 与 VirtIO 必须是 `/soc` 直接子节点且含 MMIO range。
- `docs/01_architecture/hal-and-platform.md` — 记录 root/`/soc` 两层 early-MMIO 设备边界及深层 `reg` 排除理由。

**验证：**
- Docker QEMU 10.0.2 DTB 实测：`/soc` 为 depth 1；`rtc@101000`、`serial@10000000`、8 个 `virtio_mmio@...`、`plic@c000000` 与 `pci@30000000` 均为 depth 2。
- Docker RED：旧过滤下 `make -C os ktest-run ARCH=rv64 PROFILE=normal` 输出 `early_mmio=2`、`Hello, world!` 后超时（exit 124）。
- Docker GREEN：`KTEST_QEMU_TIMEOUT=120 KTEST_CMDLINE="mango.mode=ktest mango.test=platform_fdt_snapshot mango.test.repeat=1 mango.test.timeout_ms=5000 mango.test.failfast=1" make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅，`early_mmio=9`，6/6 PASS。
- Docker 串行：对派生 x0 注入 `mask=0x003, skip_apk=true` 后，`make build ARCH=rv64 PROFILE=normal && make -C os rv64-derived-run` ✅；PID1 进入 test-runner，basic-musl/basic-glibc/busybox-musl/busybox-glibc 均 `exit_code=0`。随后 `make build ARCH=la64 PROFILE=normal && make -C os la64-derived-run` ✅，同四组均 `exit_code=0`。

**备注：** 保留 Oracle 遗留修复的多 `/memory` 收集、`.bss.boot` 快照和 LA64 FDT fallback；未修改 LA64 `LA64_QEMU_FDT_PADDR` 或 PID1 临时映射修复。`os_test.conf` 的临时 `skip_apk=true` 已恢复为 `false`；未触碰已有 AI-Usage-Report 修改。

### Oracle 静态审查：FDT early-MMIO、快照与 ext4 隔离门禁

**涉及文件：**
- `os/src/hal/firmware/fdt.rs` — early-MMIO 仅采纳根级设备节点的 `reg`，保留 `reserved-memory` 子树和根级 PCI host `ranges`；收集所有根级 `memory` 节点，按起始物理地址排序并在重叠时 fail-closed。
- `os/src/hal/firmware/mod.rs` — 将全零构造的 2 MiB `FDT_SNAPSHOT` 从 `.data.boot` 移至 `sbss` 前的 `.bss.boot`，避免占用镜像 data 文件空间。
- `os/src/fs/page_cache.rs` — 删除无调用者的 `write_without_balance()` 与每次正常写入都会读取的 `suppress_balance` 原子标志；所有写入恢复统一的 dirty-page balance。
- `scripts/{check,test_check}_another_ext4_isolation.sh` — 对齐当前 HTTPS submodule URL、gitlink pin 和 `another` 默认 backend；fixture 不再 fetch 已在本地存在的 git object，继续覆盖零/mixed/bypass/good 四种 gate 场景。
- `docs/01_architecture/{README,hal-and-platform}.md`、`docs/10_plan/vf2-fdt-dw-mmc-roadmap.md` — 同步 `.bss.boot` NOBITS 快照与根级 memory 收集约束。

**验证：**
- Docker：`make toolchain-preflight` ✅ `nightly-2026-05-10`。
- Docker 串行：`make kernel ARCH=rv64 PROFILE=normal` ✅，随后 `make kernel ARCH=la64 PROFILE=normal` ✅。
- `sh scripts/test_check_another_ext4_isolation.sh` ✅ 四个 hermetic 场景通过；`sh scripts/check_another_ext4_isolation.sh` ✅。
- Docker：对 RV64/LA64 派生 x0 注入 `mask=0x003, skip_apk=true`。RV64 `make -C os rv64-derived-run` 在打印 FDT/MM 平台信息与 `Hello, world!` 后 900s 超时，未进入 PID1；保留为失败证据，未扩大到调度/启动语义修改。LA64 `make -C os la64-derived-run` ✅ 完成 FDT fallback、MM/PCI、PID1，并使 basic-musl/basic-glibc/busybox-musl/busybox-glibc 全部 `exit_code=0`。

**备注：** firmware carveout 兜底无需修改：正常 FDT 路径已保留 DTB、memreserve 与 `reserved-memory`，frame allocator 另行排除 linker kernel image；FDT 不可用时 RV64 fail-closed，LA64 2K1000 继续使用已有静态 DRAM/carveout 回退。未修改 LA64 QEMU FDT fallback 或 PID1 临时映射修复；未提交、未暂存本批改动。

### P1：POSIX MQ 与阻塞记录锁的 SA_RESTART typed outcome

**涉及文件：**
- `os/src/fs/vfs/posix_lock.rs` — 为 `posix_lock_set()` 新增能区分普通 errno 与内部 restart marker 的 `PosixLockSetError`；F_SETLKW/F_OFD_SETLKW 的 `WaitResult::Interrupted` 清理 wait graph 后返回 `RestartSys`，由 syscall 边界原样生成 `-512`。
- `os/src/syscall/fs/common.rs` — fcntl POSIX/OFD record-lock 调用方通过 typed outcome 生成 syscall 返回值，避免把 restart marker 硬塞进 `SyscallErr`。
- `os/src/syscall/process/ipc.rs` — `mq_wait_send_ready()` 与 `mq_wait_receive_ready()` 在中断时返回 `RestartSys`；阻塞的 mq_send/mq_timedsend/mq_receive/mq_timedreceive 因而可由 `do_signal` 按 SA_RESTART 决定重启或转为 EINTR。O_NONBLOCK 的 EAGAIN 和 SysV sem/msg 的 EINTR 路径未改。

**验证：**
- Docker：`make kernel ARCH=rv64 PROFILE=normal` ✅，随后串行 `make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker：`make test ARCH=rv64 PROFILE=regression` ✅；`24 passed, 0 failed, 1 skipped, 25 total`，`[L4 REGRESSION RESULT: PASS]`。

**备注：** 信号中断前已释放 record-lock/mqueue 的业务锁；`-512` 仅在内核 syscall 返回路径传播，`do_signal` 负责 SA_RESTART 判定，默认处理仍对用户态呈现 EINTR。已检查命中的 IPC/record-lock 文档，当前叙述不含冲突的中断承诺，无需同步；现有可复用规则已由 AGENTS.md 覆盖，未改动 references 或用户已有的 AI-Usage-Report 修改。

### 补齐 LTP 双 libc 配置解析与 waitpid SA_RESTART 回归

**涉及文件：**
- `user/src/bin/test_runner/config/parse.rs` — 识别 `ltp_libc=both` 并选择 `LtpLibc::Both`；新增解析断言，防止配置静默回退至默认 glibc。
- `user/src/bin/regression/regression_wait_restart.rs` — 新增 SIGUSR1 打断 `waitpid` 的正负控制：`SA_RESTART` 必须完成原 wait 并返回目标 child PID；无 flag 必须先返回 `EINTR`，随后正常 reap。
- `user/src/bin/regression/main.rs` — 将 `wait_restart` 注册为 TAP #24，总数 24→25，并保持破坏性 `clone_vm_second_slot` 探针为最后一项。

**验证：**
- Docker：`make toolchain-preflight` ✅ `nightly-2026-05-10`。
- Docker：`make test ARCH=rv64 PROFILE=regression` ✅；`[regression_wait_restart] PASS: SA_RESTART restarts, default returns EINTR`、`ok 24 wait_restart`，最终 `24 passed, 0 failed, 1 skipped, 25 total` 和 `[L4 REGRESSION RESULT: PASS]`。
- Docker：在 RV64 regression 完成后串行执行 `make build ARCH=la64 PROFILE=regression` ✅。

**备注：** 正控制在旧的 `wait4` EINTR 行为下会在第一次 wait 返回 `EINTR` 后失败；本批未改动既有 `process_manager.rs` 修复。未命中其他文档的 `code_paths`，无需同步；本次为局部配置/回归覆盖，不新增跨任务经验条目。

### 修复 regression net 用例失败 + ktest FAT32 堆耗尽 OOM（含 socketpair fd 泄漏）

**涉及文件：**
- `os/src/main.rs` — 回归模式（零盘）也调用 `net::config::init()`（退化为 loopback + null eth，不探测 NIC/不跑 DHCP）并常驻 net-poll worker，修复 `regression_net_tcp_accept`/`regression_net_udp` 在零盘回归中无 smoltcp 栈而失败/panic；worker 同时保证关闭的 UDP/TCP socket 的 128 KiB smoltcp 缓冲被 `drain_pending_socket_removals` 回收，避免回归/ktest 中 socket 渐进泄漏。
- `os/src/net/syscall/socketpair.rs` — 复核 `socket_type` 未知位（`from_bits_truncate` 会静默丢弃），返回 EINVAL；`sv` 写回 EFAULT 时摘除并锁外释放已分配的两个 fd（修复每次非法调用泄漏 2 fd + 2×64 KiB ring buffer）。
- `os/src/kernel_tests/mem_block.rs` — `MemBlockDevice` 改为按 `BLOCK_SZ` 分块存储，避免 34 MiB FAT32 fixture 的单个 `Vec` 请求被 buddy 取整到 64 MiB（order-24）而要求整堆为空。
- `os/src/fs/page_cache.rs` — `FatPageCacheBackend` 增加 `sector_to_parent` 换算，与 cdc17728 之后 `bitmap.rs` 的 4096 单位编址约定一致（此前仍把 512 扇区号直接当设备块号传给 `read_block`）。
- `os/src/fs/fat32/fat_inode.rs` — 实现 `create_with_attrs` 直接委托 `create`，避免默认实现先 `create` 再 `set_metadata`（FatInode 未实现 → ENOSYS）导致 `open(O_CREAT)` 在 FAT 卷上整体失败。
- `os/src/drivers/block/partition.rs` — `BlockSizeAdapter` 支持跨父块的多 child 读改写（`EasyFileSystem::open` 以 PAGE_SIZE 缓冲读 BPB）。
- `os/src/fs/mod.rs` — `adapt_filesystem_device` 对 FAT32 不再包 `BlockSizeAdapter`（FAT32 已自行换算 512→4096，双适配会读到错误扇区）；ext4 保持原适配。
- `os/src/kernel_tests/fs_fat_smp.rs` — `open_fat`/`MountedFatFixture::mount` 直接传裸设备（去掉二次适配）；`verify_cluster_table` 自行做扇区→父块换算。

**验证：**
- Docker：`make test ARCH=rv64 PROFILE=regression` ✅ 全部 24 项 PASS（含 `net_unix_pair` 的 seqpacket、invalid_inputs，`net_tcp_accept`，`net_udp`；此前 #10 invalid_inputs 失败、#11/#13 因零盘无 net 栈失败/panic）。
- Docker：`KTEST=fs_fat_smp make -C os ktest-run ARCH=rv64 PROFILE=normal CORE_NUM=4` ✅ `[KTEST RESULT: PASS]`。
- Docker：`CORE_NUM=4 KTEST=all make -C os ktest-run ARCH=rv64 PROFILE=normal` 越过原 FAT32 34 MiB OOM，page_cache 组 72-76 全 PASS，fs_smp/net_smp 一路通过；剩余在 net_smp 用户探针用例处偶发 SMP 停顿（既有 flaky，见备注）。
- Docker：`make kernel ARCH=rv64 PROFILE=normal` ✅；`make kernel ARCH=la64 PROFILE=normal` ✅。

**备注：** 完整 4-core ktest 仍会偶发 `smp::user_task_reschedules_and_sets_affinity` 失败与 net_smp 用户探针用例停顿——这是既有 SMP 探针时序 flaky，按任务约束未改动 SMP 调度/等待语义。`page_cache::global_flush_releases_registry_before_writeback` 在本次所有 rv64 运行（隔离、完整 ktest、前序 ext4_another_lifetime 组合）均 PASS；其历史 EINVAL 与 FAT32 fixture 堆耗尽同源（堆耗尽/碎片导致写回路径资源不足），已随堆问题消失。回归 seqpacket 用例多次运行均 PASS，历史超时应为 `invalid_inputs` 用例泄漏 fd/socket 造成资源积累与偶发时序放大，相关泄漏已修复。

### 修复 LA64 PID1 临时 ELF 映射的 PGDH 别名冲突


**涉及文件：**
- `os/src/hal/arch/loongarch64/config.rs` — 为临时 ELF 映射定义独立的 `KERNEL_PROGRAM_BASE`；从 `MMAP_BASE + 4 GiB` 开始，避开同一 PGDH 中低地址固件/PCI 恒等映射的 39 位别名，并用编译期断言约束 canonical 和栈窗口边界。
- `os/src/mm/kernel_space.rs` — LA64 临时映射改用该架构常量；`insert_program_area()` 保留 `MemoryError`，避免调用处将失败压缩为 `()` 而丢失诊断原因。
- `docs/04_mm/initialization-and-kernel-space.md` — 记录 LA64 临时 ELF 窗口和低地址资源映射的别名隔离条件。

**验证：**
- Docker RED：`make -C os la64-derived-run` 在 PID1 前稳定 panic：`src/fs/vfs/file.rs:1960`，详细错误为 `Err(AlreadyMapped)`；当时 `kernel_program_base() == MMAP_BASE == 0xffffff8000000000`，其 VA[38:0] 为零。
- Docker：`make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker：以 `mask=0x003, skip_apk=true` 注入派生 x0 后执行 `make -C os la64-derived-run` ✅。越过 PID1 ELF 映射，basic-musl/basic-glibc/busybox-musl/busybox-glibc 均以 `exit_code=0` 结束，QEMU 命令退出 0，未见 `insert_program_area` panic。

**备注：** 三级 PGDH 页表只索引 VA[38:12]；高半区 `MMAP_BASE` 的低 39 位会和低地址恒等映射别名。FDT 资源发现使这个既有布局错误变为可见，修复只调整 LA64 临时窗口，不改 VFS/exec 语义。此前 `make -C os ktest-run ARCH=la64 PROFILE=normal` 的 `page_cache::global_flush_releases_registry_before_writeback` 失败保持独立、未处理。

### 修复 RV64 OpenSBI HSM 的 AP 物理入口地址

**涉及文件：**
- `os/src/smp.rs` — `bring_up_secondary_cpus()` 将高半区链接的 `_start` 经已冻结的 `image_paddr` 转换为 HSM 要求的 SATP=bare 物理入口；保持逻辑 CPU 到硬件 hart 的既有可逆映射及 opaque=0 语义。
- `docs/01_architecture/boot-and-trap.md` — 记录 RV64 HSM 启动地址的物理/虚拟地址边界。

**验证：**
- Docker RED：`CORE_NUM=4 make -C os ktest-run ARCH=rv64 PROFILE=normal`，HSM probe 与 `hart_start` 均返回成功，但传入 `0xffffffc000200040` 后仅 CPU0 online，panic 为 `expected=0xf online=0x1 missing=0xe`。
- Docker：`CORE_NUM=4 KTEST=smp KTEST_QEMU_TIMEOUT=600 make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅。cold boot hart=2，`online_mask=0xf`；`34 passed / 0 failed`，`[KTEST RESULT: PASS]`，无 AP online BACKTRACE。
- Docker：注入 `mask=0x003, skip_apk=true` 后清理共享 Cargo target 并以 `CORE_NUM=8 make kernel ARCH=rv64 PROFILE=normal` 构建；`CORE_NUM=8 make -C os rv64-derived-run` ✅。cold boot hart=5，`configured=8 ... online_mask=0xff`；basic-musl/basic-glibc/busybox-musl/busybox-glibc 均以 `exit_code=0` 结束。

**备注：** 根因是合并后的高半区链接与旧的 `_start as usize` HSM 调用之间缺少地址域转换；OpenSBI 接受该参数却在 bare 地址空间无法取指，故 BSP 仅等待超时。完整 146 项 4-core ktest 继续暴露既有 `page_cache::global_flush_releases_registry_before_writeback` 失败及后续堆耗尽，未在本 AP 启动工作包扩大处理范围。

### smp×develop 合并的双架构回归与 LA64 QEMU FDT 启动修复

**涉及文件：**
- `os/src/hal/arch/loongarch64/entry.asm` — 保存 LA64 的 `a2` EFI system-table handoff，而不是无关的 `a1` 命令行寄存器。
- `os/src/hal/firmware/mod.rs` — 当 QEMU direct boot 实际遗漏 EFI handoff 时，只在 `MissingSystemTable` 分支使用并验证 QEMU virt 固定 `0x0010_0000` FDT；无效 DTB 仍 fail-closed。
- `docs/01_architecture/boot-and-trap.md` — 同步 LA64 QEMU 的已验证 fallback 契约。

**验证：**
- Docker：`make -C os rv64-derived-run`，`mask=0x003`，单核 basic+busybox ✅（所有已选组 exit_code=0）。
- Docker：`CORE_NUM=8 make -C os rv64-derived-run` ❌。OpenSBI cold boot hart=6 后 780s 无 kernel 输出；同一启动路径的 `CORE_NUM=4 make -C os ktest-run ARCH=rv64 PROFILE=normal` 在 30s 内 panic：`secondary CPU online timeout: expected=0xf online=0x1 missing=0xe`，有完整 panic diagnostics/backtrace，未进入 TAP（0 passed / 0 skipped / 0 test-failed）。
- Docker：`make test ARCH=rv64 PROFILE=regression` ❌。TAP 已通过 #1–#9，`regression_net_unix_pair` 的 `seqpacket_round_trip` 后超时，`[L4 REGRESSION RESULT: FAIL] exit_code=143`。
- Docker：LA64 变更后 `make build ARCH=la64 PROFILE=normal` ✅；`make -C os la64-derived-run` 已越过原 `MissingSystemTable` panic，完成 FDT、MM、PCI、网络与 x0/x1 挂载，随后在 PID1 ELF 临时映射 `src/fs/vfs/file.rs:1960` 的 `insert_program_area(...).unwrap()` panic。
- Docker：`make -C os ktest-run ARCH=la64 PROFILE=normal` 已越过 FDT 启动点并执行 91/139 项；截至 30s harness timeout 为 **90 passed / 0 skipped / 1 failed**，失败仍是 `page_cache::global_flush_releases_registry_before_writeback`（global writeback failed），无 BACKTRACE。`make test ARCH=la64 PROFILE=regression` 也越过 FDT 启动点，但在相同 `file.rs:1960` PID1 ELF 映射 panic，判定 `STATE=ENTRY_FAILURE`。

**备注：** QEMU 10.0.2 此 direct-ELF invocation 的 `a2` 实测为 0，故 EFI 查找返回 `MissingSystemTable`；固定 FDT fallback 仅在该精确条件且完整 capture/parse 成功后生效。尚未修改 RV64 AP 启动和 LA64 `insert_program_area` 的新失败，避免在本回归工作包扩大修改范围。

### 修复 RV64 ktest 的 PCI 64-bit MMIO aperture 启动映射停滞

**涉及文件：**
- `os/src/mm/{page_table.rs,mapper.rs,kernel_mapper.rs,kernel_space.rs}` — 为内核 FDT MMIO 引入 2 MiB 对齐区间映射；保持 LA64 4 KiB 回退。
- `os/src/hal/arch/riscv/sv39.rs` — 支持二级叶子 PTE、正确解析其页内 VPN 偏移，并防止向已有 huge leaf 下继续创建子页表。
- `os/src/kernel_tests/ext4/block_device.rs` — 把仅验证块读写/隔离的内存 fixture 降至 1 MiB，避免超过 32 MiB 内核堆。
- `docs/04_mm/{page-table-and-tlb.md,initialization-and-kernel-space.md}` — 同步 FDT MMIO 来源和 SV39 2 MiB 映射语义。

**验证：**
- Docker：`make -C os ktest-run ARCH=rv64 PROFILE=normal` ✅ 越过原卡点；FDT PCI memory64 aperture `[0x400000000, 0x800000000)` 不再逐 4 KiB 建表，30s QEMU 时限内完成 144 项 ktest。
- Docker：同次 ktest 结果为 **133 passed / 10 skipped / 1 failed**；原 64 MiB heap fatal 已消失，新增暴露 `page_cache::global_flush_releases_registry_before_writeback` 返回 `EINVAL`，最终为 `[KTEST RESULT: FAIL]`，尚未归因，未伪造 PASS。
- Docker：`make kernel ARCH=la64 PROFILE=normal` ✅。

**备注：** 诊断 `boot_trace!` 已清理。ktest 失败项位于 page-cache writeback，未在本启动映射工作包中修改；需独立复现并归因后处理。

### 修复 FDT VirtIO-MMIO 跨类型探测重置设备导致的 RV64 启动卡死

**涉及文件：**
- `os/src/drivers/net/virtio_net.rs` — 仅在读取到 `DeviceType::Network` 后创建网卡驱动；跳过块设备时保留只读 transport，避免其 Drop 重置块设备。
- `os/src/drivers/block/virtio_blk.rs` — 对称保留跳过的网卡 transport，避免块设备枚举在 Drop 时重置已初始化的 virtio-net。
- `os/src/main.rs` — 保留 develop 的内核块设备挂载，同时将 `net::config::init()` 前移到块设备挂载之前。
- `docs/06_net/device-adapter.md` — 记录 VirtIO-MMIO FDT 跨类型探测的生命周期约束。

**验证：**
- Docker：`make kernel ARCH=rv64 PROFILE=normal` ✅（release 链接完成，2m30s）。
- Docker：`timeout 300 make -C os rv64-derived-run PROFILE=normal` ✅ 越过 VirtIO 初始化；`vda`/`vdb` 成功发现并挂载，DHCP 获得 `10.0.2.15`，PID1 与 `test-runner` 启动，读取 `/sdcard/os_test.conf` 的 `mask=0x003`。

**备注：** test-runner 后续尝试从 Alpine 镜像下载 `e2fsprogs` 时 DNS 临时失败，因此 basic/busybox 未实际开始；该失败发生在用户态包下载，已不再是内核 VirtIO 启动阻塞。所有临时 `[debug]` 输出已移除。
