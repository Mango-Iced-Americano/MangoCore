diff --git a/os/src/hal/arch/loongarch64/boot.rs b/os/src/hal/arch/loongarch64/boot.rs
index 238de9c1..5c1ac69c 100755
--- a/os/src/hal/arch/loongarch64/boot.rs
+++ b/os/src/hal/arch/loongarch64/boot.rs
@@ -3 +3 @@
-//! `_start` 设置启动栈并跳转到 Rust 侧 `rust_main`。
+//! `_start` 按 CPU ID 选择启动栈并跳转到 Rust 侧 `rust_main`。
@@ -8,0 +9,3 @@ use crate::config::BOOT_STACK_SIZE;
+// Phase 1 fixes the QEMU topology ceiling at eight contiguous CPU IDs.
+const MAX_CPUS: usize = 8;
+
@@ -14,2 +17,2 @@ unsafe extern "C" fn _start() -> ! {
-    // kernel. No Rust stack or references exist yet; the assembly only programs
-    // DMW registers, initializes `$sp`, and tail-jumps to `rust_main`.
+    // kernel. No Rust stack or references exist yet; the assembly programs DMW,
+    // validates the CPU-local ID, assigns its bounded stack, and enters Rust.
@@ -18,2 +20,0 @@ unsafe extern "C" fn _start() -> ! {
-            // 以下内容看不懂的话
-            // 可以去看龙架构手册卷一 v1.11
@@ -28,2 +28,0 @@ unsafe extern "C" fn _start() -> ! {
-            # 保存栈底地址
-            la.global   $sp, {boot_stack}
@@ -31,2 +30,5 @@ unsafe extern "C" fn _start() -> ! {
-            # 将立即数boot_stack_size即引导栈的大小加载到临时寄存器t0中
-            li.d        $t0, {boot_stack_size}
+            # CPUID is CPU-local, so it can be read before any shared stack exists.
+            csrrd       $a0, 0x20
+            # Reject an ID without a reserved slot before assigning or using $sp.
+            li.d        $t0, {max_cpus}
+            bgeu        $a0, $t0, 2f
@@ -33,0 +36,8 @@ unsafe extern "C" fn _start() -> ! {
+            # Every legal CPU starts from the same array base.
+            la.global   $sp, {boot_stack}
+            # Stack N owns [base + N * size, base + (N + 1) * size).
+            addi.d      $t1, $a0, 1
+            # Multiplication keeps the stride tied to BOOT_STACK_SIZE.
+            li.d        $t0, {boot_stack_size}
+            mul.d       $t0, $t0, $t1
+            # Stacks grow down, so $sp begins at the slot's exclusive upper bound.
@@ -35 +45 @@ unsafe extern "C" fn _start() -> ! {
-            csrrd       $a0, 0x20
+
@@ -38,0 +49,4 @@ unsafe extern "C" fn _start() -> ! {
+
+            # An invalid CPU has no safe stack and cannot call Rust or logging.
+        2:
+            b           2b
@@ -39,0 +54 @@ unsafe extern "C" fn _start() -> ! {
+            max_cpus = const MAX_CPUS,
@@ -49 +64,2 @@ unsafe extern "C" fn _start() -> ! {
-pub(crate) static mut BOOT_STACK: [u8; BOOT_STACK_SIZE] = [0; BOOT_STACK_SIZE];
+pub(crate) static mut BOOT_STACK: [u8; BOOT_STACK_SIZE * MAX_CPUS] =
+    [0; BOOT_STACK_SIZE * MAX_CPUS];
