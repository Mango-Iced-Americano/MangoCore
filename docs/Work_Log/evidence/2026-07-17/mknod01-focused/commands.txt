Commands executed (in order):

1. Rebuild RV64 full image:
   docker exec e10937008fee bash -c 'cd os && make rv64-only'

2. Inject os_test.focus.conf into sdcard-rv.img:
   docker exec e10937008fee bash -c 'cd os && make conf-inject CONF_ARCH=rv64 CONF_BLK_MODE=virt CONF_FILE=../os_test.focus.conf'

3. Run QEMU focused LTP test:
   docker exec e10937008fee bash -c 'cd os && make rv64-run'
   Exit status: 0
   Timeout: 300s (harness) — actual completion in ~60s
