lmbench availability baseline
Captured: 2026-07-23

Command requested by the task:
  docker compose exec -T os-dev bash -c "ls /app/fs-img-dir/sdcard-rv.img && debugfs -R 'ls -l /lmbench' /app/sdcard-rv.img 2>/dev/null | head -5"

Result:
  ls: cannot access '/app/fs-img-dir/sdcard-rv.img': No such file or directory

Follow-up against the actual virt image:
  docker compose exec -T os-dev bash -c "debugfs -R 'ls -l /lmbench' /app/sdcard-rv.img 2>/dev/null | head -5"

Result:
  no output; /lmbench is not present on /app/sdcard-rv.img.

Conclusion: lmbench is not available on the active rv64 test image. mask=0x008 was not injected or run.
