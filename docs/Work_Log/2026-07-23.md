## 2026-07-23

### 泛化 source-purity 变体复制夹具

**涉及文件：**
- `scripts/test-source-purity-make-contract.sh` — 扫描器从历史 `lang_items` 特例扩展到任意 `*.rs.rv`/`*.rs.la` 到活动 Rust 源的复制；对抗夹具改用虚构 `dummy_config` 名称，避免历史路径在活跃契约脚本中出现。

**验证：**
- Docker：`sh scripts/test-source-purity-make-contract.sh` ✅；夹具的五种 `cp` 拼写均被拒绝。

**备注：** 历史 Work_Log 与归档诊断日志仍原样保留旧构建命令，未作为活跃构建路径或契约输入。

### 恢复零盘 ktest 的 lwext4 块适配回归

**涉及文件：**
- `os/src/kernel_tests/ext4/{block_device,byte_bridge,mounted_filesystem}.rs`、`ext4.rs` — 将 ext4 ktest 按内存块设备、字节桥与已挂载实例职责拆分；恢复 2KiB bridge、未对齐分区 RMW/尾块及 flush forwarding 覆盖。
- `docs/10_plan/ext4-lwext4-migration-audit-20260718.md` — 记录零盘 ktest 的可执行范围；依赖 PID1/持久卷或已删除 power-cut hook 的 partial-writeback、nested-symlink、orphan crash/recovery 保持 deferred。

**验证：**
- Docker：`make kernel ARCH=rv64 PROFILE=normal` ✅；随后串行 `make kernel ARCH=la64 PROFILE=normal` ✅。

**备注：** `lw_path_isolation` 在 ktest 的零盘 profile 输出显式 `[KTEST SKIP]`，不再把缺失 block device 隐藏为未说明的通过；有盘 fixture 仍会打开 lwext4 并检查路径与 device identity。

### 恢复 2K1000LA 参数化构建入口

**涉及文件：**
- `os/make/arch/la64-settings.mk`、`os/make/la64.mk`、`os/Makefile` — 恢复四个 2K1000LA uImage 入口；通过 `ARCH=la64` facade、image-role 产物根和非变异 linker 选择构建。
- `os/src/main.rs` — 仅为 `board_2k1000` 嵌入 LoongArch 早期入口，并显式忽略允许失败的随机熵初始化结果。

**验证：**
- Docker：`make kernel ARCH=la64 PROFILE=normal BOARD=2k1000` ✅（静态编译；使用 `linker-2k1000.ld`）。

**备注：** 2K1000 使用 `0x90000000` 的 uImage load/entry 地址；QEMU 继续使用 `0x9000000090000000`。未恢复旧式 `lang_items` 复制或历史启动拓扑。

### 合并 develop 并保持 cleanup 构建合同

**涉及文件：**
- 合并 `develop` 到 `chore/clean-up`；保留单文件 `lang_items`、initramfs/PID1 分层和 image-role 构建合同。
- `os/{Cargo.toml,src/fs/mod.rs,src/fs/filesystem.rs}` — 兼容动态挂载的块设备适配与合并后的随机依赖，未恢复旧启动特性。
- `os/src/hal/arch/loongarch64/trap/mod.rs` — 更新裸函数汇编语法以匹配固定 nightly。
- `scripts/build_rootfs.sh` — 不再复制已删除的 `initproc` 二进制。

**验证：**
- Docker：RV64/LA64 kernel build-only ✅；RV64/LA64 userspace ✅；RV64/LA64 ktest 18/18 ✅；RV64 regression 6/6 ✅。
- Docker：image-role（T4）、QEMU matrix（T5）、init lifecycle contract ✅；`run_full_test.py --dry-run` ✅。

**备注：** develop 的 lwext4 专属 ktest 用例要求与 cleanup 的 initramfs-only 挂载策略不兼容，保留 cleanup 的 18 项稳定 ktest 集。

### tools disk 改为 staging + mke2fs 填充

**涉及文件：**
- `os/make/tools-disk.mk` — 移除 loop mount/umount；在临时 staging 目录完成原有文件、`/etc`、测试程序和软链接布局后，用 `mke2fs -d` 生成 ext4 payload，并保留 MBR 包装。
- `os/make/tools.mk` — 为两种架构的 tools disk 增加对应 `user` 前置构建，固定输出到 `user/target`，使 `inet_test`、`fs_test`、`unix_test` 可被打包。
- `docs/Work_Log/evidence/2026-07-23/tools-disk-{make-all,rv64-ktest}.log` — Docker 构建与 ktest 的当前执行日志。

**验证：**
- Docker（`pxy-cleanup-os-dev-1`）：`mke2fs -h` 确认支持 `-d root-directory`；完整 staging 输入的临时 `mke2fs -d` 构建成功 ✅
- Docker：`make all` 已确认 RV64 tools disk 在 user build 后复制 `inet_test`、`fs_test`、`unix_test`，并完成无 loop 的 ext4/MBR 构建；双架构内核编译在容器命令 600 秒时限内尚未结束，未验收为通过。

**备注：** Docker `/bin/sh` 是 dash；`set -e` 会在软链接循环内失败的 `ln` 到达循环尾部前退出，因此对原本允许失败的链接显式使用 `|| true`。本次为构建脚本修改，不命中源码模块文档的 `code_paths`；无需更新 AI 工具清单。

### 仓库清理与构建脚本归位

**涉及文件：**
- `scripts/{build_rootfs,build_initramfs,inject_os_test_conf}.sh`、`os/make/*.mk`、`os/Makefile`、CI 与合同脚本 — 将构建/注入入口迁至 `scripts/`，保留从 `os/` Make 目录调用的语义。
- `user/compat/ltp_proto_compat.c`、`docs/00_overview/assets/`、`docs/plans/repository-rebaseline.md` — 归位兼容源码、评审资产和执行计划。
- `.gitignore`、`.omo/`、`docs/Work_Log/evidence/` — 删除失效状态和重复物，仅保留官方 T3 证据白名单，并消除 tracked-but-ignored 文件。

**验证：**
- Docker（`pxy-cleanup-os-dev-1`）：`make -C os -n fs-img`、`make -C os -n conf-inject`、T4 image-role contract、T5 QEMU command matrix、init lifecycle contract ✅
- Docker：`make -C os rv64-kernel-build-only PROFILE=normal && make -C os la64-kernel-build-only PROFILE=normal` ✅
- Docker：RV64 `make -C os rv64-ktest PROFILE=normal` ✅（18/18 PASS）
- `git ls-files -ci --exclude-standard` 与 `git diff --check` ✅

**备注：** 本次仅重定位入口和清理仓库状态，未改变内核或用户态逻辑；无需更新可复用调试经验或 AI 工具清单。

### test_runner 模块化准备与持久化 /etc

**涉及文件：**
- `user/src/bin/test_runner.rs`、`user/src/bin/test_runner/` — 建立薄入口，并按 process、config、bootstrap、groups、ltp、instrumentation、smoke 拆分运行器实现。
- `user/src/bin/initd.rs`、`user/src/bin/regression_init.rs` — tools disk 的 `/etc` bind mount 与 regression tmpfs 最小 `/etc` 回退。
- `os/make/tools-disk.mk` — 构建时预置核心 `/etc` 文件。
- `scripts/test-init-lifecycle-contract.sh` — 更新生命周期与模块布局契约。

**验证：**
- Docker：`make user ARCH=rv64 PROFILE=normal` ✅
- Docker：`make user ARCH=la64 PROFILE=normal` ✅
- Docker：`scripts/test-init-lifecycle-contract.sh` ✅
- Docker：RV64 `ktest-run` ✅（18/18 PASS）

**备注：** 证据容器：`c238e449081e`，挂载 `/home/pxy/projects/MangoCore-cleanup -> /app`；/etc 由 PID1 在 tools disk 可用时 bind mount，不再由 runner 生成。
