## 2026-07-23

### another_ext4 writeback 跨批次 journal transaction 延迟提交

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — journal direct-range writeback 不再立即调用 `commit()`；最多聚合 32 个 metadata home block，并在阈值或 journal 空间压力下提交。
- `dependency/another_ext4/src/ext4/journal_transaction.rs` — journal context 保存无生命周期绑定的 deferred metadata images；后续 writeback 读取并合并这些镜像，flush 时只执行一次 payload/commit barrier。保留既有 P10 deferred checkpoint 机制，并新增聚合提交回归测试。
- `dependency/another_ext4/src/ext4/journal.rs` — fsync/sync/unmount 的 device flush 先提交 deferred transaction，再完成 P10 checkpoint；direct metadata 路径先强制提交，避免读取未提交的 allocation state。
- `dependency/another_ext4/src/ext4/mod.rs` — direct metadata gate 在进入 legacy path 前关闭 deferred journal batch。

**验证：**
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make rv64-kernel-build-only'` ✅
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make la64-kernel-build-only'` ✅

**备注：** 容器 `pxy-mangocore-os-dev-1`，挂载 `/home/pxy/projects/MangoCore -> /app`。未运行 QEMU；新增 unit regression 由 bare-metal 构建解析但未单独执行。未更新 reference：该模式仅适用于 another_ext4 的 journal transaction 生命周期，且现有 P10 记录已覆盖 checkpoint 规则。

### Restore P8-P11 to journal_transaction.rs after git checkout revert

**涉及文件：**
- `dependency/another_ext4/src/ext4/journal_transaction.rs` — 恢复 P8（flush 合并 5→4：active-SB 不含 flush，payload 后统一 flush）；P9（deferred checkpoint：新增 `DeferredCheckpoint` 结构体、`force_checkpoint()` 方法、`complete_deferred_checkpoint()` 函数，commit 只做 sync phase，checkpoint/clean-SB 延迟到 `force_checkpoint` 或下次 commit 前执行）；P10（跨提交累积 checkpoint，同名 home block 保留最新版本）；P11（journal 连续物理块 `write_blocks()` 批处理）。仅移除 P12 相关代码（batch leader/pending queue）。

**验证：**
- `make rv64-kernel-build-only` ✅
- `make la64-kernel-build-only` ✅
- `cargo check`（another_ext4 lib）✅

**备注：** 容器 `pxy-mangocore-os-dev-1`，挂载 `/home/pxy/projects/MangoCore -> /app`。`cargo test` 因其他模块（extent.rs、low_level.rs、inode.rs）测试代码中 `prepared_extents`/`generation` 字段遗漏等无关问题无法通过。未运行 QEMU。



### another_ext4 journal commit batch leader

**涉及文件：**
- `dependency/another_ext4/src/ext4/journal_transaction.rs` — journal transaction 不再在 staging 阶段独占 writer；commit 时由 CAS 选出 batch leader，其他提交进入带完成信号的 pending queue。leader 为已收集的多个 transaction 连续写 descriptor/payload 后统一 flush，再连续写 commit record 后统一 flush；每条记录仍有独立 sequence、descriptor、commit record 和 deferred checkpoint 累积。中途 I/O 失败会 poison journal，并以相同失败结果唤醒 batch 内及排队等待者。新增四事务 batch 的两次 flush 回归测试。

**验证：**
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make rv64-kernel-build-only'` ✅
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make la64-kernel-build-only'` ✅

**备注：** 容器 `pxy-mangocore-os-dev-1`，挂载 `/home/pxy/projects/MangoCore -> /app`。仅合并 journal payload/commit 的 durability barrier；deferred checkpoint 和 clean-tail 时序保持原逻辑。未运行 QEMU。

### another_ext4 journal 跨提交累积延迟 checkpoint

**涉及文件：**
- `dependency/another_ext4/src/ext4/journal_transaction.rs` — 删除每次新 commit 前完成旧 checkpoint 的路径；把连续事务的 home-block 镜像合并到一个 deferred state（共享 home block 保留最新版本）。ring 空间压力前强制一次全量 checkpoint；失败立即 poison journal。新增覆盖多 commit 合并、空间压力与强制 checkpoint 失败的单元测试。
- `dependency/another_ext4/src/ext4/journal.rs` — `flush_device()`、sync/fsync 与卸载路径统一强制 deferred checkpoint；失败同时 poison `Ext4`，避免继续元数据修改。

**验证：**
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make rv64-kernel-build-only'` ✅
- `docker compose exec -T os-dev bash -lc 'cd /app/os && make la64-kernel-build-only'` ✅
- `docker compose exec -T os-dev bash -lc 'cd /app/dependency/another_ext4 && cargo fmt --check -- src/ext4/journal_transaction.rs src/ext4/journal.rs'` ✅

**备注：** 容器 `pxy-mangocore-os-dev-1`，挂载 `/home/pxy/projects/MangoCore -> /app`。每个 commit 仍保留 payload-before-commit 的两个 barrier；删除的是旧 checkpoint 的两个额外 barrier。未运行 QEMU。

### another_ext4 journal commit 连续物理块批处理写入

**涉及文件：**
- `dependency/another_ext4/src/ext4/journal_transaction.rs` — `commit_journal()` 中 descriptor + payload 写入从逐块 `write_bytes()` 改为 `write_blocks()` 批处理：将物理地址连续的 journal 块分组，每组一次 `device.write_blocks()` 调用。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make rv64-kernel-build-only 2>&1 | tail -5'` ✅ (kernel compiled; objcopy ENOTDIR is pre-existing toolchain issue)
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make la64-kernel-build-only 2>&1 | tail -5'` ✅

**备注：** commit record 仍然单独写入，保留原有的两个 flush barrier（payload→commit、commit→durability）。不修改公开 API。

### another_ext4 fallback 范围分配延迟元数据提交

**涉及文件：**
- `dependency/another_ext4/src/ext4/alloc.rs` — 新增批量数据块分配：聚合所有命中 block group 的 bitmap，合并共享 GDT block 镜像，所有数据块清零后各写一次 bitmap/GDT、一次 superblock，并在发布 extent 前完成 flush；失败时回滚元数据镜像。
- `dependency/another_ext4/src/ext4/extent.rs` — 支持把已初始化且已发布 allocation metadata 的连续物理范围映射为一个 extent。
- `dependency/another_ext4/src/ext4/low_level.rs` — fallback 先收集 hole，再批量分配；连续逻辑/物理 run 以完整 `block_count` 安装 extent，避免每个缺失块调用一次 `alloc_block()`。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/dependency/another_ext4 && cargo fmt && cd /app/os && make rv64-kernel-build-only && make la64-kernel-build-only'` ✅

**备注：** 容器 `fba442b0132e`，挂载 `/home/pxy/projects/MangoCore -> /app`。未运行 QEMU 吞吐基准，未声明新的性能数值；AI-Usage-Report 已列出当前模型和 Oracle，无需更新。

### another_ext4 journal 范围分配合并元数据提交

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — journal mode 对 16–256 块、可直接 append 的 buffered write 复用连续范围分配，在单个 JBD2 transaction 中暂存 bitmap、GDT、superblock 与 inode；不适用或 ENOSPC 时保持 legacy 逐块回退。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make rv64-kernel-build-only 2>&1 | tail -5 && make la64-kernel-build-only 2>&1 | tail -5'` ✅

**备注：** 新数据先写入未发布的数据块；后续 journal commit 的首个 flush 在 metadata commit point 前使数据持久化，保持 data → metadata → commit 顺序。容器 `fba442b0132e`，挂载 `/home/pxy/projects/MangoCore -> /app`；未运行 QEMU 性能测试，未声明新的吞吐结果。AI-Usage-Report 已包含当前模型，无需更新。

### another_ext4 直接范围分配合并真实数据写入

**涉及文件：**
- `dependency/another_ext4/src/ext4/low_level.rs` — 新增携带数据的 buffered-write API；直接范围分配在输入覆盖完整块范围时按 8 块批次写入真实数据，并返回已持久化状态；旧 API 继续作为无数据兼容包装。
- `os/src/fs/ext4_another/page_cache.rs` — 将写回 staging buffer 传给新 API；仅在直接范围未写入真实数据（重写、非整块 EOF 或回退）时调用 `write_data_only()`。

**验证：**
- `make rv64-kernel-build-only` ✅
- `make la64-kernel-build-only` ✅

**备注：** 直接范围事务仍在发布 inode extent 前执行其可靠 flush，保持数据先于元数据的持久化顺序；数据不足整段时保留原零填充再写入路径。

### another_ext4 同步竞态与 I/O 回归修复

**涉及文件：**
- `os/src/fs/ext4_another/lifetime.rs` — 用尺寸 generation 加 CAS 清除替代布尔 dirty 标记，保留同步提交期间的并发写入通知。
- `os/src/fs/ext4_another/mutations.rs` — 页缓存复制完成后递增 generation；单 inode 同步仅在 generation 未变时清除它。
- `os/src/fs/ext4_another/page_cache.rs` — 删除批量 `read_pages()` staging-buffer 覆盖实现，回退到逐页 `read_page()` 默认路径。
- `dependency/another_ext4/src/ext4/low_level.rs` — 直接范围零填充不再强制中间 flush，并更新回归测试以断言 flush 延后。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make rv64-kernel-build-only 2>&1 | tail -5'` ✅
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make la64-kernel-build-only 2>&1 | tail -5'` ✅

**备注：** 页面数据仍由紧随分配的 `write_data_only()` 覆盖；最终 `sync()` 的设备 flush 保持数据持久化屏障。

### another_ext4 端到端连续读批处理

**涉及文件：**
- `os/src/fs/ext4_another/blockdev.rs` — 覆盖 `read_blocks()`，将连续物理块缓冲区一次交给 Mango `BlockDevice`。
- `os/src/fs/ext4_another/page_cache.rs` — 覆盖 `read_pages()`，单次读取连续页的 staging buffer 后分发并补零 EOF 尾页。
- `dependency/another_ext4/src/ext4/data_write.rs` — 将既有 extent 范围查询结果暴露给同级读路径复用。
- `dependency/another_ext4/src/ext4/low_level.rs` — 缓存 extent 范围，并针对每个连续物理 extent run 调用一次 `read_blocks()`。

**验证：**
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make rv64-kernel-build-only 2>&1 | tail -5'` ✅
- `docker exec pxy-mangocore-os-dev-1 sh -lc 'cd /app/os && make la64-kernel-build-only 2>&1 | tail -5'` ✅

**备注：** 未改变 another_ext4 的公开 API；尾部非整块读取仍保留单块路径，避免向 `read_blocks()` 传递非块对齐缓冲区。
