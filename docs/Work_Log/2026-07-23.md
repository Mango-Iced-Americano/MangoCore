## 2026-07-23

### 仓库清理与构建脚本归位

**涉及文件：**
- `scripts/{build_rootfs,build_initramfs,inject_os_test_conf}.sh`、`os/make/*.mk`、`os/Makefile`、CI 与合同脚本 — 将构建/注入入口迁至 `scripts/`，保留从 `os/` Make 目录调用的语义。
- `user/compat/ltp_proto_compat.c`、`docs/00_overview/assets/`、`docs/plans/repository-rebaseline.md` — 归位兼容源码、评审资产和执行计划。
- `.gitignore`、`.omo/`、`docs/Work_Log/evidence/` — 删除失效状态和重复物，仅保留官方 T3 证据白名单，并消除 tracked-but-ignored 文件。

**验证：**
- Docker（`pxy-cleanup-os-dev-1`）：`make -C os -n fs-img`、`make -C os -n conf-inject`、T4 image-role contract、T5 QEMU command matrix、init lifecycle contract ✅
- Docker：`make -C os rv64-kernel-build-only PROFILE=normal && make -C os la64-kernel-build-only PROFILE=normal` ✅
- Docker：RV64 `make -C os rv64-ktest PROFILE=normal` ✅（18/18 PASS）
- `git ls-files -ci --exclude-standard` 与 `git diff --check` ✅

**备注：** 本次仅重定位入口和清理仓库状态，未改变内核或用户态逻辑；无需更新可复用调试经验或 AI 工具清单。

### test_runner 模块化准备与持久化 /etc

**涉及文件：**
- `user/src/bin/test_runner.rs`、`user/src/bin/test_runner/` — 建立薄入口，并按 process、config、bootstrap、groups、ltp、instrumentation、smoke 拆分运行器实现。
- `user/src/bin/initd.rs`、`user/src/bin/regression_init.rs` — tools disk 的 `/etc` bind mount 与 regression tmpfs 最小 `/etc` 回退。
- `os/make/tools-disk.mk` — 构建时预置核心 `/etc` 文件。
- `scripts/test-init-lifecycle-contract.sh` — 更新生命周期与模块布局契约。

**验证：**
- Docker：`make user ARCH=rv64 PROFILE=normal` ✅
- Docker：`make user ARCH=la64 PROFILE=normal` ✅
- Docker：`scripts/test-init-lifecycle-contract.sh` ✅
- Docker：RV64 `ktest-run` ✅（18/18 PASS）

**备注：** 证据容器：`c238e449081e`，挂载 `/home/pxy/projects/MangoCore-cleanup -> /app`；/etc 由 PID1 在 tools disk 可用时 bind mount，不再由 runner 生成。
