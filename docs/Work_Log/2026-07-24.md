# 2026-07-24 Work Log

## 2026-07-24

### SMP-P1-B03：双架构 1/2/4/8 核最小 BSP/AP 启动闭环

**涉及文件：**

- `os/src/smp.rs` — 新增最小启动状态机、硬件 ID 到逻辑 CPU ID 的可逆
  映射、Release/Acquire 发布、online mask 和 5 秒有界等待。
- `os/src/main.rs` — `rust_main(hardware_id, boot_arg)` 分流 BSP/AP，
  原全局初始化路径收拢为 CPU0 独占的 `bsp_main()`。
- `os/src/hal/arch/riscv/{mod.rs,sbi.rs}` — 增加 SBI v0.2 BASE probe、
  HSM `hart_start` 和 AP `wfi` park。
- `os/src/hal/arch/loongarch64/{boot.rs,entry.asm,mod.rs}` — 统一入口 ABI，
  移除非零 CPU 的 bootstrap 永久自旋，并按 QEMU 9.2 slave boot ROM
  协议实现 mailbox + IPI 唤醒；2K1000LA 保持单核并明确拒绝多核启动。
- `os/src/hal/{arch/mod.rs,mod.rs}` — 导出双架构 AP start/park HAL 接口。
- `os/make/{rv64.mk,la64.mk}` — 参数化 QEMU 入口使用 MTTCG 和单
  socket/N core/单 thread 拓扑，ktest 接收同一 `CORE_NUM`。
- `docs/01_architecture/boot-and-trap.md`、
  `docs/10_plan/smp-8core-implementation.md` — 同步最小启动控制流、固件
  差异、逻辑 CPU 映射和 Phase 1 `partial` 边界。
- `docs/Work_Log/evidence/2026-07-24/smp-P1-B03-*` — 保存 RED、官方协议
  验证、构建/QEMU 矩阵、ELF 布局、判定状态和最终审计。

**为什么这样实现：**

- AP 不能进入历史 `rust_main()`：BSS、heap、frame allocator、驱动、FS
  和全局调度器都只有单份，重复初始化会直接破坏全局状态。因此 CPU0
  继续完整启动，AP 只允许使用独立 boot stack 和三个启动原子。
- 启动阶段值放入 `.data.boot`，而不是 BSS。LA64 AP 可能已经在 QEMU
  slave ROM/启动栈上等待；CPU0 的 `mem_clear()` 不能把 AP 正在观察的
  状态清零。
- CPU0 对 `BOOT_PHASE` 使用 Release，AP 使用 Acquire；AP 发布 online
  位同样使用 Release，CPU0 等待使用 Acquire。阶段值因此同时携带其前面
  初始化完成的可见性，普通整数或 volatile 不能提供该跨核关系。
- RISC-V 不能假设 hart0 是 BSP。显式 MTTCG 的 4 核测试出现 Boot HART
  2，8 核出现 1；比赛式 `-bios default -smp 8` 又出现 Boot HART 6。
  实际启动 hart 固定映射为逻辑 CPU0，其余硬件 ID 保持连续且可逆，HSM
  启动时再转换回真实 hart ID。
- LoongArch 的首次 no-op 假设被 RED 推翻：QEMU 9.2.1 官方 `boot.c`
  把 AP 放在 pflash slave boot ROM 中。CPU0 必须向
  `IOCSR_MAIL_SEND(0x1048)` 写入低 32 位物理入口，执行 `dbar 0` 后向
  `IOCSR_IPI_SEND(0x1040)` 发送 vector 0；AP 才会读取 mailbox 并跳转。
- AP online 后立即 park，且不打印。当前 console 尚未证明跨 CPU 安全，
  timer/IPI/trap 和调度器也未完成 PerCpu 改造；“可启动”不能偷换为
  “可并行运行任务”。

**RED 与根因：**

- 修改前 RV64 两核 OpenSBI 能枚举两个 hart，但只有 CPU0 输出，没有 AP
  online 标记；仅增加 Rust AP 分支不足以启动 HSM stopped hart。
- LoongArch 初版把 `start_secondary_cpu()` 实现为 no-op，两核在 5 秒后
  panic：`expected=0x3 online=0x1 missing=0x2`。根据 QEMU v9.2.1 官方
  `hw/loongarch/boot.c` 和 `hw/intc/loongson_ipi_common.c` 修正为
  mailbox + IPI 协议后通过。
- RV64 四核初版在 OpenSBI 日志后超时；日志显示 Boot HART ID=2，内核却
  硬编码物理 hart0 才走 BSP。加入硬件/逻辑 ID 映射后同一场景通过。
- QEMU 内核 panic 仍可能以进程状态 0 退出；最终判定同时要求期望 online
  标记、`KTEST RESULT: PASS`、无 `panicked at`，不把 QEMU 状态单独当绿。

**最终验证：**

- RV64 `CORE_NUM=1/2/4/8`：online mask 分别为
  `0x1/0x3/0xf/0xff`，waitqueue ktest 4/4 PASS ✅
- LA64 `CORE_NUM=1/2/4/8`：online mask 分别为
  `0x1/0x3/0xf/0xff`，waitqueue ktest 4/4 PASS ✅
- 比赛式 RV64 `-bios default -smp 8`、不显式写 `-accel`：
  OpenSBI 1.5.1 选择 Boot HART 6，`online_mask=0xff`，KTEST PASS ✅
- 比赛式 LA64 `-smp 8`、不显式写 `-accel`：
  `boot_hw_id=0 online_mask=0xff`，KTEST PASS ✅
- QEMU 9.2 官方文档确认：省略 accelerator 时默认 TCG；前后端支持且无
  冲突功能时 TCG 默认 multi-thread，每个 vCPU 一个宿主线程。
- 本批实际运行容器 `lzm-cagent-run` 的双架构 QEMU 均为 10.0.2；
  LoongArch 启动寄存器实现依据向后核对到 QEMU v9.2.1 官方源码。当前
  本机没有仍可访问的 9.2.1 容器，因此不把 10.0.2 运行日志冒充为
  “9.2.1 已实测”。
- 双架构 ELF 中 `BOOT_HARDWARE_ID/BOOT_PHASE/ONLINE_MASK` 均位于
  `sdata..edata`；8 槽启动栈跨度均为 `0x200000` 且上界等于 `sbss` ✅
- 2K1000LA 使用 `BOARD=2k1000 BLK_MODE=sata CORE_NUM=1` 完成单核
  release 编译 ✅；这只证明统一的双参数 `rust_main` 入口没有破坏现有
  单核构建，不宣称本阶段完成了实板运行验证。
- 收尾修正 `smp.rs` 文件头的原子数量注释后，又严格串行执行
  `make rv64-kernel-build-only CORE_NUM=1` 和
  `make la64-kernel-build-only CORE_NUM=1`，两者退出码均为 0 ✅。
- 第一版 ELF 数值断言因 `[` 不接受十六进制 RHS 失败，失败状态保留；
  修正版使用 Bash 算术表达式并以状态 0 fail-closed 通过。

**范围和剩余风险：**

- 用户本批明确解除约 50 行限制；本批是形成双架构启动闭环所需的完整
  协议改动，未据此放宽后续 SMP 批次门禁。
- 没有新增锁，也没有在等待点持锁；启动共享状态只使用固定大小原子。
- AP 尚无 PerCpu、idle context、内核 trap/IPI handler 和调度能力；
  LoongArch park 暂用 spin hint，RISC-V 用永久 `wfi`，均由 Phase 2 替换。
- console 多核锁、panic STOP、全局初始化计数和专用 `KTEST=smp` 尚未完成。
- 阶段状态：`SMP-P1-B03=pass`；整体 Phase 1 仍为 `partial`。

### SMP-P1-B02：LA64 QEMU 启动入口按 CPU 隔离 boot stack

**涉及文件：**

- `os/src/hal/arch/loongarch64/boot.rs` — 为 LA64 QEMU 固定预留 8 个
  256 KiB 启动栈；在首次设置 `$sp` 前读取并校验 `CSR.CPUID`，再按
  `BOOT_STACK + (cpu_id + 1) * BOOT_STACK_SIZE` 选择当前 CPU 的栈顶。
- `docs/01_architecture/boot-and-trap.md` — 同步双架构早期启动栈现状，
  明确 2K1000LA 和 BSP/AP 拆分边界。
- `docs/10_plan/smp-8core-implementation.md` — 将启动缺口更新为“栈已
  隔离、BSP/AP 尚未拆分”。
- `docs/Work_Log/evidence/2026-07-24/smp-P1-B02-*` — 保存修改前 RED
  证据、双架构构建、ELF 布局/反汇编、1/2 核 QEMU 输出和行数分类。

**实现思路与不变量：**

- DMW 建立后立即读取 CPU-local `CSR.CPUID`，不在多个 CPU 间共享栈；
  `cpu_id >= 8` 时在分配 `$sp`、调用 Rust 或输出日志前原地 park。
- 合法 CPU `N` 独占
  `[base + N * 0x40000, base + (N + 1) * 0x40000)`；CPU0 的栈顶与
  原单核实现相同，整个数组大小固定为 `0x200000`。
- 使用乘法和 `BOOT_STACK_SIZE` 操作数计算步长，避免汇编位移量与 Rust
  配置分叉；`$a0` 保留为 CPU ID 传给现有 `rust_main()`。
- `.bss.stack` 仍位于 `sbss` 之前，BSP 的 `mem_clear()` 不会清零 AP
  正在使用的早期栈。
- 改动仅由 `board_laqemu` 编译；2K1000LA 继续使用原 `entry.asm`
  单核入口。本批不增加锁、原子变量或新的 `unsafe` 所有权关系。
- 本批只隔离早期栈，不使 AP online：现有 LA64 bootstrap 仍会让非零
  CPU 自旋，且 `rust_main()` 尚未拆为 BSP/AP 两条初始化路径。

**最终验证：**

- `make rv64-kernel-build-only CORE_NUM=1` ✅
- `make la64-kernel-build-only CORE_NUM=1` ✅
- LA64 `CORE_NUM=2`、`KTEST=waitqueue` 专用内核构建 ✅
- LA64 ELF：`BOOT_STACK` 大小为 `0x200000`，且
  `sbss - BOOT_STACK == 0x200000` ✅
- 8 个槽均为 `0x40000`，连续、互斥且不越过 `sbss` ✅
- `_start` 反汇编确认
  `CPUID -> unsigned guard -> base -> offset -> multiply -> $sp`，非法
  CPU 进入无栈 park loop ✅
- `CORE_NUM=1` LA64 waitqueue ktest：4/4 PASS，退出码 0 ✅
- 显式执行
  `-accel tcg,thread=multi -smp cpus=2,sockets=1,cores=2,threads=1`：
  LA64 waitqueue ktest 4/4 PASS，QEMU 退出码 0，`e2fsck` 退出码 0 ✅
- 2 核运行期间抓取到 QEMU 主线程加 4 个工作线程；线程名受 Linux
  `comm` 长度截断，不能仅凭名称逐一映射 vCPU，故只作为 MTTCG 进程
  结构旁证，不作为 AP online 证明。
- 最终源码之后重新完成双架构构建、ELF 检查和 1/2 核运行；所有最终
  证据时间均晚于 `boot.rs`。构建生成的 linker/lang-items 选择已恢复。

**证据纪律与过程异常：**

- 初版 ELF 断言把 `[[ ... ]]` 放在管道组中且未显式传播失败，并曾取错
  `nm` 字段，存在假绿风险；最终脚本改为逐项失败即 `exit 1`，最终
  `layout-check-final.status=0` 才作为验收依据。
- 第一次线程快照会被后续输出覆盖；第二次尝试的 `awk` 转义错误触发
  fail-closed 并终止 QEMU，失败日志和状态保留。最终版本通过
  `ps --ppid` 定位 `timeout` 的 QEMU 子进程并单独保存线程快照。
- 中途补充源码注释后，旧 PASS 日志不再作为最终新鲜证据；本节列出的
  `*-final.*` 文件均来自最终源码。

**行数与剩余风险：**

- 关键实现新增 13 行、删除 4 行，合计 17 行；直接启动不变量注释新增
  11 行、删除 7 行，另有 3 个新增空行。原始代码 diff 为 `+27/-11`，
  未超过本批约 50 行门禁。
- `CORE_NUM=2` 只证明 CPU0 在双核拓扑下正常运行且入口栈选择没有破坏
  单核路径；日志中没有 AP online 记录，不能宣称 SMP 启动已经完成。
- QEMU ktest Make 目标仍硬编码单线程 TCG，本批 2 核验证使用完整记录的
  直接 QEMU 命令；拓扑参数统一化留给后续独立批次。
- 阶段状态：`SMP-P1-B02=pass`，整体 Phase 1 仍为 `partial`。

### SMP-P1-B01：RV64 启动入口按 hart 隔离 boot stack

**涉及文件：**

- `os/src/hal/arch/riscv/entry.asm` — 固定预留 8 个 256 KiB 启动栈；
  在首次使用栈前校验 OpenSBI `a0` 中的 hart ID，并按
  `boot_stack + (hart_id + 1) * BOOT_STACK_SIZE` 选择栈顶。
- `docs/01_architecture/boot-and-trap.md` — 记录 RV64 启动栈布局及本批
  尚未完成 BSP/AP 初始化拆分的边界。
- `docs/10_plan/smp-8core-implementation.md` — 更新启动缺口的当前状态。
- `docs/Work_Log/evidence/2026-07-24/smp-P1-B01-*` — 保存修改前 RED
  证据、双架构构建、ELF 符号/反汇编、单核 QEMU 回归和行数分类。

**实现思路与不变量：**

- 在汇编入口保留 `a0/a1`，后续仍可把 hart ID 和 DTB 参数传给 BSP/AP
  Rust 入口；只使用临时寄存器 `t0` 计算栈地址。
- `hart_id >= 8` 时在任何栈、日志或 Rust 代码被使用前进入 `wfi` park，
  防止越界 hart 污染其他内存。
- hart 0 计算出的栈顶继续等于历史符号 `boot_stack_top`，保留 panic
  backtrace 的单核栈边界语义；新增 `boot_stack_upper_bound` 只表示整个
  8 槽数组的上界。
- 整个 `.bss.stack` 数组仍位于 linker `sbss` 之前，BSP 的
  `mem_clear()` 不会清除 AP 可能正在使用的早期栈。
- 本批不启动 AP、不拆分 `rust_main()`、不修改 LA64、调度器、中断、
  锁或 QEMU 多核拓扑。

**验证：**

- `make rv64-kernel-build-only CORE_NUM=1` ✅
- `make la64-kernel-build-only CORE_NUM=1` ✅
- RV64 ELF：`boot_stack_top - boot_stack == 0x40000` ✅
- RV64 ELF：`boot_stack_upper_bound - boot_stack == 0x200000`，且
  `boot_stack_upper_bound == sbss` ✅
- RV64 `_start` 反汇编确认先执行无符号 hart 上界检查，再执行
  `(a0 + 1) << 18` 栈偏移，非法 hart 进入 `wfi` loop ✅
- `make rv64-ktest KTEST=waitqueue KREPEAT=1 CORE_NUM=1
  KTEST_QEMU_TIMEOUT=45`：4/4 PASS ✅
- LA64 构建产生的临时 linker 选择、RV64 测试产生的 `lang_items.rs`
  选择均已恢复到修改前状态；`git diff --check` ✅
- `CORE_NUM=2/4/8` 运行态：**not-run**。BSP/AP 全局初始化尚未拆分，
  当前运行多核不能形成有效验收。

**行数与风险：**

- 关键实现新增 17 行、删除 2 行，合计 19 行；直接并发/启动注释新增
  14 行，另有 4 个空行。原始 diff 为 `+35/-2`，未超过本批约 50 行门禁。
- 当前只证明 RV64 的 8 槽静态布局和 CPU0 运行回归；LA64 仍使用旧启动
  路径，AP 仍未 online，`rust_main()` 仍会无条件执行全局初始化。
- 单核 OpenSBI 日志继续显示 `Platform HSM Device: ---`；RISC-V AP
  启动批次必须先明确 HSM 能力或受控 fallback，不能静默假设可用。

### SMP-P0-B00/B01：冻结首轮基线并建立 CPU 数量构建契约

**涉及文件：**

- `os/build.rs` — 将 `MANGO_CORE_NUM` 纳入 Cargo 指纹，限定为
  `1/2/4/8`，并传递给 rustc；直接 Cargo 构建也不能绕过核数校验。
- `os/make/rv64.mk` — `CORE_NUM` 改为可覆盖的单核默认值，在 Make
  解析阶段拒绝非法值并导出 `MANGO_CORE_NUM`。
- `os/make/la64.mk` — 与 RV64 建立相同契约；未改变 2K1000LA 默认单核路径。
- `docs/Work_Log/evidence/2026-07-24/smp-P0-B01-*` — 保存环境、
  命令、完整构建/QEMU 日志、状态、补丁与行数分类。

**验证：**

- 修改前 `make rv64-kernel-build-only` ✅
- 修改前 `make la64-kernel-build-only` ✅
- 两架构 Makefile 接受 `CORE_NUM=1/2/4/8`，拒绝 `CORE_NUM=3` ✅
- 两架构均向子进程导出 `MANGO_CORE_NUM=2` ✅
- 直接执行 `build.rs`：`2` 正常传递，`3` 以状态 101 明确失败 ✅
- 修改后 `make rv64-kernel-build-only CORE_NUM=1` ✅
- 修改后 `make la64-kernel-build-only CORE_NUM=1` ✅
- `make rv64-ktest KTEST=waitqueue KREPEAT=1 CORE_NUM=1`：4/4 PASS ✅
- `make la64-ktest KTEST=waitqueue KREPEAT=1 CORE_NUM=1`：4/4 PASS ✅

**备注：**

- 本批关键代码新增 20 行、删除 2 行，合计 22 行；未修改 QEMU
  拓扑、启动栈、BSP/AP、调度或中断路径。
- 实际验证容器为 `lzm-cagent-run`，映射当前工作树到 `/app`；另一个
  compose 容器映射了不同用户的旧工作树，未用于验收。
- 当前容器双架构 QEMU 均为 10.0.2。只记录了现有 image digest，尚未
  pull/force-recreate，因此 Phase 0 环境新鲜性门禁仍为 `partial`。
- RV64 单核日志显示 OpenSBI `Platform HSM Device: ---`；后续 HSM
  启动批次必须先确认多核固件能力，不能静默降级。
- 文档同步检查：SMP 实施方案已经描述本批契约，其他命中 Makefile
  路径的测试/实板文档语义未受影响；无需修改状态。
