---
date: 2026-07-24
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-07-24

<a id="2026-07-24-ltp-mask800-fresh"></a>
## Fresh RV64 LTP mask=0x800 run timed out before suite completion

**Date:** 2026-07-24
**Author:** AI:openai/gpt-5.6-terra
**Tags:** [area:fs, type:validation, status:blocked]
**Mango Gate:** skill loaded=yes; references=none — requested test execution, not debugging or performance analysis.

**Summary:** Restored the RV64 test image and successfully injected the requested LTP-only configuration. The 900-second run did not emit an LTP completion marker; its log ends with `qemu-system-riscv64` receiving SIGTERM. This is not a passing LTP result. The log records 332 done, 22 failed, and 216 skipped LTP cases. The recorded failures include the requested excluded `eject-tests`, `evm_overlay`, `force_erase`, and `fs_bind01`–`fs_bind17` cases, so the exclusion behavior requires separate investigation before interpreting this run as an LTP regression result.

**Files:**
- `docs/Work_Log/evidence/2026-07-24/ltp-mask800-fresh.log` — summary, failure lines, injected configuration, and complete 31,884-line raw run log.
- `docs/Work_Log/evidence/2026-07-24/ltp-mask800-fresh.{git-hash,container-id,config,commands-and-status,qemu-output,qemu-head-tail}.txt` — persistent run metadata and log extracts (`qemu-output` uses `.log`).

**Verification:**
- Configuration injection — ✅ `[conf-inject] done`, `CONFIG_INJECTED`.
- RV64 LTP mask `0x800` — ⛔ no `COMP TEST GROUP END ltp`, `initproc ... done ... ltp`, or `run_selected_groups done` marker before the 900-second run ended.
- Evidence freshness — ✅ combined artifact timestamp `06:26:21 +08:00` is later than raw log timestamp `06:23:37 +08:00`.

**Notes:** Container `fba442b0132e…` mounted `/home/pxy/projects/MangoCore` at `/app`; the test used repository `e282cde5` and `dependency/another_ext4` `fc9f31e`. The detached prescribed command did not retain its process exit status; the raw log is the authoritative termination evidence.

<a id="2026-07-24-poweroff-durability-completed"></a>
## Power-off durability test — COMPLETED ✅

**Date:** 2026-07-24
**Tags:** [area:fs, type:validation, status:completed]

**Summary:** Executed full power-off durability chain: Boot1 with iozone writes → abrupt QEMU kill (45s) → e2fsck journal recovery → Boot2 with basic tests → e2fsck clean. All phases passed.

**Chain:**
- `poweroff-boot1-v2.log`: iozone 4-process writes active, QEMU killed (SIGTERM) ✅
- `poweroff-e2fsck-recovered.log`: e2fsck -fy recovers journal + cleans orphans → e2fsck -fn CLEAN (no errors) ✅
- `poweroff-boot2-clean.log`: Second boot → sdcard+tools mount without EIO → basic tests musl/glibc exit 0 ✅

**Results:** Filesystem survives abrupt power loss. Journal recovery restores consistency. No structural corruption. Second boot mounts both sdcard and tools without EIO.

<a id="2026-07-24-1925-ext4-uninit-bitmap"></a>
## 19:25 — Materialize uninitialized ext4 block bitmaps

**Date:** 2026-07-24 19:25 +08:00
**Author:** AI:openai/gpt-5.6-terra
**Tags:** [area:fs, type:bugfix]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** Materialize `EXT4_BG_BLOCK_UNINIT` bitmaps before every data-block allocation path. The initializer reserves superblock/GDT/reserved-GDT metadata, every group descriptor's block/inode bitmap and inode-table ranges (including flex_bg relocation), marks the final-group tail unavailable, clears `BLOCK_UNINIT`, and recalculates its checksum.

**Files:**
- `dependency/another_ext4/src/ext4_defs/block_group.rs` — expose block-bitmap initialization state transitions.
- `dependency/another_ext4/src/ext4/alloc.rs` — share the metadata-reserving initializer across direct-range, batch-zeroed, and single-block allocation; stage a materialized transaction bitmap before selecting a range.

**Verification:**
- `make rv64-kernel-build-only` — ✅
- `make la64-kernel-build-only` — ✅
- RV64 QEMU iozone (musl + glibc) — ✅ both groups exited 0; zero `EIO`/`Input/output error` matches. Evidence: `evidence/2026-07-24/iozone-zero-eio.log`.
- e2fsck — ✅ `e2fsck -fn /app/sdcard-rv.img` completed all five passes. Evidence: `evidence/2026-07-24/e2fsck-fn.log`.
- RV64 second boot — ✅ mounted `another_ext4` and started iozone without EIO. Evidence: `evidence/2026-07-24/second-boot.log`.
- KTest 12/12 — ✅ all `ext4_another` cases passed. Evidence: `evidence/2026-07-24/ktest-12-pass.log`.

**Notes:** The requested iozone command placed `DISK_RV=...` after `timeout`, where it is treated as an executable. The completed run exports `DISK_RV` before `timeout`. meta_bg remains rejected by existing mount feature validation; the initializer correctly covers all currently mountable layouts, including flex_bg.
