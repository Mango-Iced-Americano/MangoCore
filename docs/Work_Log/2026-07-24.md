## 2026-07-24

### 修复 LA64 competition PID1 首次用户态恢复

**涉及文件：**
- `os/src/main.rs` — 在块设备注册和 PID1 入队前保留启动阶段诊断标记。
- `os/src/task/mod.rs` — 记录 `INITPROC` lazy_static 构造与入队完成。
- `os/src/task/task.rs` — 记录 PID1 的 ELF、地址空间和 TCB 创建阶段。
- `os/src/task/processor.rs` — 记录首次调度循环、取任务及首次上下文切换。
- `os/src/hal/arch/loongarch64/trap/mod.rs` — 直接跳转到 `__restore`，避免 LA64 static link 中 `strampoline` 解析到内核 trap stub 后将 restore 地址错误重定位。

**验证：**
- Docker toolchain preflight ✅
- `make -B build ARCH=rv64 PROFILE=normal BUILD_ROOT=/app/build-final` ✅
- `make -B build ARCH=la64 PROFILE=normal BUILD_ROOT=/app/build-final` ✅
- LA64 competition QEMU：PID1 输出 `[initd] pseudo-fs mount framework ready` ✅
- RV64 competition QEMU：PID1 输出 `[initd] pseudo-fs mount framework ready` ✅

**证据：** `docs/Work_Log/evidence/2026-07-24/`

**备注：** competition QEMU 消费 staged `kernel-la`；验证时以独立 BUILD_ROOT 完整构建后显式发布该产物，避免旧 staged kernel 掩盖源码修改。
