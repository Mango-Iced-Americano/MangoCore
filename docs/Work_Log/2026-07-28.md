## 2026-07-28

### 在清零 BSS 前保留 FDT 内存发现

**涉及文件：**
- `os/src/main.rs` — 在 `mem_clear()` 前解析并保存 FDT 内存区域，避免 QEMU 将 DTB 放在内核 BSS 内时丢失动态 DRAM 布局。
- `os/src/mm/frame_allocator.rs` — 合并重叠的 kernel/firmware 排除区，正确处理被内核镜像覆盖的 DTB 范围。
- `os/src/mm/kernel_space.rs` — 不重复映射已完全属于 kernel image 的 firmware 保留区，避免 `AlreadyMapped`。

**验证：**
- Docker（`pxy-mangocore-os-dev-1`，`/home/pxy/projects/MangoCore -> /app`）：`make kernel ARCH=rv64 PROFILE=normal` ✅。
- Docker：RV64 `make -C /app/os ktest-run ARCH=rv64 PROFILE=normal` ✅（53 passed, 0 failed；ext4_another 21 项均通过）。
- Docker：`make kernel ARCH=la64 PROFILE=normal` ✅。
- Docker：LA64 `make -C /app/os ktest-run ARCH=la64 PROFILE=normal` ✅（53 passed, 0 failed；ext4_another 21 项均通过）。
- 可验收完整输出与命令状态：`docs/Work_Log/evidence/2026-07-28/ext4-another-{rv64,la64}-final.*`。

**备注：** 用户提供的裸 QEMU 命令没有附加 ktest 所需的 `clean-ext4.img` drive，因此它必然让 slot 0 为空；正式 ktest profile 会创建并附加该 128 MiB ext4 盘。AI 使用报告已包含当前模型，无需更新。

### PlatformPolicy 仅保留平台启动配置

**涉及文件：**
- `os/src/main.rs` — 将共享内核启动编排恢复至唯一的 `rust_main()`；从策略读取未显式指定的默认 root 设备。
- `os/src/hal/platform/{mod.rs,qemu_riscv.rs,qemu_la.rs,vf2.rs}` — 移除重复的 `boot()` 实现，将策略缩减为名称、init 回退路径和默认 root 设备配置。
- `os/src/task/mod.rs` — `INITPROC` 优先 `/init`，缺失时改用编译期选择策略提供的 init 路径。
- `docs/01_architecture/README.md` — 同步共享启动路径与平台配置职责。

**验证：**
- Docker（`pxy-mangocore-os-dev-1`）：`make toolchain-preflight` ✅
- Docker：RV64、LA64、2K1000 指定 `cargo check` 均 ✅（分别 1040、1050、1049 个存量 warning）。
- `cargo fmt --check` 未执行：固定 nightly 未安装 `rustfmt` 组件。

**备注：** `root=` 和 `mango.root=` 仍优先于策略默认值；未发现需沉淀的通用模式；AI 使用报告已包含当前模型，无需更新。

### 固件 DTB 信任边界与 2K1000 平台回退

**涉及文件：**
- `os/src/hal/firmware/{mod.rs,fdt.rs}` — 仅允许 `RiscvFdt` 协议使用 DTB；验证 FDT magic/大小，保留静态固件 carveout，并将 DTB 页加入保留区。
- `os/src/hal/platform/{mod.rs,fallback.rs}` — 2K1000 复用 LA 策略，并提供 `ns16550a` 串口静态目录。
- `os/src/hal/platform/loongarch64/2k1000.rs` — 将内部块大小对齐为 4 KiB，以满足 `another_ext4` 的块契约。
- `docs/00_overview/AI-Usage-Report.md` — 记录本次 Oracle 安全审查与验证证据。

**验证：**
- Docker（`pxy-mangocore-os-dev-1`，`/home/pxy/projects/MangoCore -> /app`）：`make toolchain-preflight` ✅
- Docker：RV64、LA64、2K1000 指定 `cargo check` 均 ✅（分别 1041、1051、1050 个存量 warning）。
- `cargo fmt --check` 未执行：固定 nightly 未安装 `rustfmt` 组件。

**备注：** 原始 2K1000 检查先暴露 `another_ext4::BLOCK_SIZE == BLOCK_SZ` 断言失败；对齐后该目标完成编译。已检查命中的 RNG/实板历史文档，其内容不涉及块大小或本次 firmware 策略，无需修改。

### FDT VirtIO MMIO 有序探测与标准 root 参数

**涉及文件：**
- `os/src/hal/firmware/fdt.rs` — 按 MMIO 基址升序保存 FDT 设备目录，并将 `virtio,mmio` 作为中性的 `Other` transport 分类。
- `os/src/drivers/block/virtio_blk.rs` — 按 compatible string 收集、排序并逐项探测 virtio-mmio 块设备；保留 `probe_rv64()` 回退。
- `os/src/drivers/net/virtio_net.rs` — 以相同的 compatible string 和地址顺序探测网络设备；LA64 PCI 路径未改动。
- `libs/mango-kernel-core/src/bootargs.rs` — 支持标准 `root=`，并规定其优先于 `mango.root=`；新增对应单元测试。
- `docs/06_net/device-adapter.md` — 同步 virtio-mmio transport 分类与驱动探测语义。

**验证：**
- Docker：`cargo test -p mango-kernel-core test_standard_root --lib` ✅（2 个 root= 优先级单元测试通过）。
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（Finished；1041 个存量 warning）。

**备注：** FDT 枚举不再以树遍历顺序决定 x0/x1；静态回退目录和 LA64 PCI 探测路径保持不变。未发现需沉淀的通用模式；AI 使用报告已包含当前模型。

### bootargs root 设备名选择

**涉及文件：**
- `os/src/fs/mod.rs` — `mount_boot_block_devices()` 接收 `BootConfig`，先发布启动块设备注册表，再将 `root=/dev/<name>` 解析为注册表键并挂载到 `/sdcard`；未找到时保持 `devs[0]` 回退。
- `os/src/hal/platform/{qemu_riscv,qemu_la,vf2}.rs` — 将已解析的 `boot_config` 传递至块设备挂载路径。
- `docs/03_fs/init-and-rootfs.md` — 同步启动时的命名 root 选择和 `/tools` 保持原有策略。

**验证：**
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1041 个存量 warning）。
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check-la cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1051 个存量 warning）。

**备注：** `root=/dev/vda`、`root=/dev/vdb`、`root=/dev/mmcblk0` 均去除 `/dev/` 前缀后查注册表；未命中时仍用 x0。未发现需沉淀的通用模式；AI 使用报告已包含当前模型，无需更新。

### 启动块设备命名注册表

**涉及文件：**
- `os/src/fs/boot_block.rs` — 使用全局 `BTreeMap` 保存启动块设备名称到 `Arc<dyn BlockDevice>` 的映射；新增解析 API，并在既有 `/dev/vda`、`/dev/vdb` 及分区/别名节点注册后同步登记。
- `os/src/fs/mod.rs` — 发布 `boot_block` 模块，使内核其他模块可调用 `resolve_block_device()`。

**验证：**
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1046 个存量 warning）。

**备注：** 已检查 `docs/` 的 `code_paths`，没有文档引用 `os/src/fs/boot_block.rs`；AI 使用报告已包含当前模型，无需更新。未发现需沉淀的通用模式。

### HAL v2 PR3.7 — VirtIO 驱动接入 DeviceManager

**涉及文件：**
- `os/src/drivers/block/virtio_blk.rs` — 新增按 `DeviceKind::VirtioBlock` 枚举 MMIO 的探测入口，并保留 DMA 池初始化和既有硬编码探测回退。
- `os/src/drivers/block/mod.rs` — RV64 块设备初始化优先使用 `platform_info()` 的设备目录；未发现设备时继续调用 `probe_rv64()`。
- `os/src/drivers/net/{virtio_net.rs,mod.rs}` — 新增目录驱动的 virtio-mmio 网络探测，优先注册它；固定 `VirtIONetWrapper::new()`、LA64 PCI 和 2K1000 GMAC 路径均保留。
- `docs/06_net/device-adapter.md` — 记录 DeviceManager-first 与旧探测回退语义。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1041 个存量 warning）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check-la cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1051 个存量 warning）

**备注：** 不删除 MMIO 常量或旧探测函数；未发现需沉淀的通用模式。`docs/06_net/device-adapter.md` 是唯一命中且需要同步的模块文档；AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR3.6 — 板级静态设备目录

**涉及文件：**
- `os/src/hal/platform/fallback.rs` — 为 RV64 QEMU 提供 UART、8 个 virtio-mmio 槽位、PCI ECAM/BAR 和 PLIC 的静态目录；为 LA64 QEMU 与 VisionFive 2 提供最小 UART 目录；按编译期板级 feature 选择目录。
- `os/src/hal/platform/{mod.rs,info.rs}` — 注册静态目录模块，并让 `PlatformInfo::from_static()` 在启动阶段持有活动板卡的设备描述符。
- `os/src/kernel_tests/platform.rs` — 静态回退测试改为验证各板卡目录数量，并验证 RV64 QEMU 的 `0x1000_8000` 槽位被标为 `VirtioNet`。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1042 个存量 warning）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check-la cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1050 个存量 warning）
- `cargo fmt --check` 未执行：固定 nightly 工具链未安装 `rustfmt` 组件。

**备注：** 静态 MMIO 地址逐项与既有 QEMU 板级常量一致；未修改板级文件或既有驱动常量。未发现匹配本次源文件 `code_paths` 的模块文档；AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR3.5 — DeviceManager 只读查询视图

**涉及文件：**
- `os/src/hal/device/{mod.rs,manager.rs}` — 新增取得 `Vec<DeviceInfo>` 所有权的 `DeviceManager`，提供兼容串、设备类型、MMIO、块设备、串口、全量和数量查询；没有变更接口。
- `os/src/hal/mod.rs` — 发布 `device` 模块。
- `os/src/kernel_tests/platform.rs` — 按既有 `KernelTest` 注册模式覆盖精确 compatible 匹配、类型/控制台查询和无 MMIO 查询。
- `docs/01_architecture/hal-and-platform.md` — 同步 HAL 目录结构和 `DeviceManager` 公共查询接口。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1042 个存量 warning）
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1050 个存量 warning）
- `cargo fmt --check` 未执行：固定 nightly 工具链未安装 `rustfmt` 组件。

**备注：** 未发现适合沉淀的通用调试模式；`docs/` 仅 HAL 架构文档命中并已同步；AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR4.2 — PlatformPolicy 接管完整启动编排

**涉及文件：**
- `os/src/main.rs` — `rust_main()` 仅选择编译期平台策略并委派 `PlatformPolicy::boot()`；保留 `mem_clear()`、模块声明与链接所需的 `global_asm!`。
- `os/src/hal/platform/{qemu_riscv,qemu_la,vf2}.rs` — 三种策略在 `mm::init()` 和启动日志后初始化 `PlatformInfo`，再继续既有机器、设备和调度启动流程。
- `docs/01_architecture/README.md` — 启动路径和源码索引改为反映策略接管。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1042 个存量 warning；`main.rs` 无 unused import warning）
- Docker：`CARGO_TARGET_DIR=/tmp/mango-check-la cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1050 个存量 warning；`main.rs` 无 unused import warning）

**备注：** 已检查 `docs/` 的 `code_paths`，未发现精确匹配本次变更路径的 frontmatter；AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR3.4 — PlatformInfo 单例与 DTB bootargs 优先级

**涉及文件：**
- `os/src/hal/firmware/mod.rs` — 在 firmware 模块边界以 crate 可见性重新导出 post-heap FDT 平台信息构建器。
- `os/src/hal/platform/mod.rs` — 以 `UnsafeCell<Option<PlatformInfo>>` 实现一次性 post-heap 平台信息单例；在单线程启动阶段写入，后续只读，并提供非 panic 的命令行访问器。
- `os/src/main.rs` — 在 `mm::init()` 后、`bootargs::load()` 前初始化平台信息。
- `os/src/bootargs.rs` — 将命令行优先级设为 DTB `/chosen/bootargs`、编译期 `MANGO_CMDLINE`、内建默认值。
- `docs/01_architecture/hal-and-platform.md` — 同步 PlatformInfo 的 FDT 构建、单例初始化和命令行优先级说明。

**验证：**
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1042 个存量 warning）
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1050 个存量 warning）

**备注：** `PlatformInfo` 中的 `String` 被单例永久持有，因此 `platform_cmdline()` 返回的 `&'static str` 在初始化后有效；Miri 组件未安装，无法在当前固定工具链执行其检查。

### HAL v2 PR3.3 — Post-heap FDT 设备枚举

**涉及文件：**
- `os/src/hal/firmware/fdt.rs` — 使用 FDT fork 的深度优先节点迭代器枚举所有含 `compatible` 属性的子节点，读取首个可表示的 `reg` MMIO 区域，并分类串口、VirtIO MMIO、PLIC 与 PCI host；无 `reg` 的设备保留 `None`。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅

**备注：** FDT fork 使用 `all_nodes()` 提供完整深度优先遍历，而非旧版节点递归 API；未发现其他 `docs/` 文档的 `code_paths` 引用此源文件，AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR3.2 — Post-heap FDT PlatformInfo 构建器

**涉及文件：**
- `os/src/hal/firmware/fdt.rs` — 以 fork 的 fallible byte-slice API 完成 pre-heap `/memory/reg` 固定容量解析，并在 post-heap 构建含 DTB model、`/chosen/bootargs`（及编译期回退）的 `PlatformInfo`；设备枚举仍留给 PR3.3。

**验证：**
- Docker：`make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅
- Docker：`cargo fmt --check --manifest-path os/Cargo.toml` 未执行：固定 nightly 工具链未安装 `rustfmt` 组件。

**备注：** FDT fork 的实际 API 是 `Fdt::new_unaligned_fallible(&[u8])`、`root().memory().reg().iter::<u64, usize>()`，而非上游 `fdt` 的简化访问器；已保持无分配 pre-heap 路径和所有缺失/格式错误输入的失败回退。未发现 `docs/` 的 `code_paths` 引用此源文件；AI 使用报告已包含当前模型，无需更新。

### HAL v2 PR3.2 — PlatformPolicy 编译期选择器

**涉及文件：**
- `os/src/hal/platform/mod.rs` — 定义最小 `PlatformPolicy` 接口，并按板级 feature 在编译期选择唯一策略。
- `os/src/hal/platform/{qemu_riscv,qemu_la,vf2}.rs` — 添加三种受支持板卡的启动编排桩；在 PR4.2 委派前保留与 `rust_main()` 相同的启动顺序。
- `os/src/kernel_tests/platform.rs` — 注册活跃板卡策略名称的内核自测。

**验证：**
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅

**备注：** `rust_main()` 尚未委派给策略，启动运行时行为不变；未发现文档 `code_paths` 与本次源文件匹配，AI 使用报告已包含当前模型和工具，无需更新。

### HAL v2 PR3.1 — PlatformInfo 静态回退模型

**涉及文件：**
- `os/src/hal/platform/{mod.rs,info.rs}` — 新增 post-heap owned `PlatformInfo`、`DeviceInfo`、固件来源和设备类别枚举；静态构造器复制 `RawBootInfo` 并一次性解析编译期命令行。
- `os/src/hal/mod.rs` — 发布 `platform` 模块。
- `os/src/kernel_tests/{mod.rs,platform.rs}` — 按现有 `KernelTest` 注册模式增加静态回退、设备类别和非 MMIO 设备 L3 测试。
- `docs/01_architecture/hal-and-platform.md` — 同步平台信息模块、公共导出和源码索引。

**验证：**
- Docker：`cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1039 个存量 warning）
- Docker：`cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1047 个存量 warning）
- `cargo fmt --check` 未执行：固定 nightly 工具链未安装 `rustfmt` 组件。

**备注：** 未解析 FDT 节点、未变更 `rust_main()`，静态设备目录填充和 `PlatformPolicy` 保留给后续 PR。

### HAL v2 + FDT 平台抽象重构 — 方案规划与 fdt crate fork

**涉及文件：**
- `.omo/plans/hal-v2-fdt-platform-abstraction.md` — 新增，完整 6 阶段实施方案
- `docs/Work_Log/2026-07-28.md` — 新增，本条目

**背景：**
Oracle 结合 MangoCore 当前代码库做了架构分析，确认以下关键点：
1. `rust_main()` 当前无参数 — entry.asm 丢弃 a0 (hartid) 和 a1 (dtb_paddr)
2. `StackFrameAllocator` 已支持多 region，只需将 `MEMORY_REGIONS` 从 const 改为动态
3. `BootConfig` 已有 `root`/`init` 字段但未接线
4. `devfs` 已支持动态设备节点注册
5. LA64 entry.asm 的 DMW 窗口切换是保存启动参数的**关键风险点**

**方案要点：**
- 6 个 PR：boot 协议 → FDT memory 解析 → PlatformInfo → PlatformPolicy → DeviceManager → root 选择
- 目录结构：`hal/boot/`, `hal/firmware/`, `hal/platform/`, `hal/device/`
- Pre-heap 阶段只解析 memory 节点（无 alloc），post-heap 完整解析 FDT
- 静态回退路径覆盖所有阶段
- 参考 DragonOS 的 FDT wrapper 模式

**fdt crate：**
- Fork `repnop/fdt` → `https://github.com/Mango-Iced-Americano/fdt`（MPL 2.0，与 GPLv3 兼容）
- 策略：临时使用 fork，修改通过 PR 回流上游

**验证：**
- 架构规划阶段，无编译/测试操作

**备注：**
- PR1（boot 协议）可作为独立合入点，不改变现有行为
- VF2 的 `UbootGo` 协议未对齐标准 DTB ABI，留到 PR7 解决

### HAL v2 Phase 1 — 固件启动参数保留

**涉及文件：**
- `os/src/hal/boot/mod.rs` — 新增 `BootProtocol`、`RawBootInfo` 和早期汇编可写的启动信息存储。
- `os/src/hal/mod.rs` — 导出 boot 模块及其公共启动信息接口。
- `os/src/hal/arch/riscv/entry.asm` — 在设置启动栈前保存 OpenSBI 的 `a0`/`a1`。
- `os/src/hal/arch/loongarch64/entry.asm` — 在 DMW 窗口切换前保存 U-Boot 的 `a0`/`a1`。
- `os/src/hal/arch/loongarch64/boot.rs` — 在 QEMU DMW 设置和 `csrrd` 覆盖 `$a0` 前保存传入寄存器。
- `os/src/main.rs` — 以 ABI 参数进入 `rust_main`，最先保存启动信息并在控制台就绪后输出诊断。
- `docs/01_architecture/hal-and-platform.md` — 同步 HAL boot 接口与目录说明。

**验证：**
- `docker compose exec -T os-dev make toolchain-preflight` ✅（固定 `nightly-2026-05-10` 可用）
- `docker compose exec -T os-dev make kernel ARCH=rv64 PROFILE=normal` ❌（exit 2；工作区缺少 `dependency/lwext4_rust/c/elf-linux-gnu.cmake`）
- `docker compose exec -T os-dev make kernel ARCH=la64 PROFILE=normal` ❌（exit 2；同一缺失的 lwext4 子模块文件）
- `docker compose exec -T os-dev make lint` ❌（exit 2；Cargo 无法读取 `/app/dependency/lwext4_rust/Cargo.toml`）

**备注：**
- `SAVED_BOOT_INFO` 与汇编写入单元均位于 `.data.boot`，避免后续 `mem_clear()` 清除已捕获的固件参数。

### HAL v2 PR2 — 固件内存区域提供者

**涉及文件：**
- `os/src/hal/firmware/{mod.rs,fdt.rs,static_provider.rs}` — 新增 pre-heap 固定容量内存区域缓冲、FDT 解析 stub 及静态配置回退提供者。
- `os/src/hal/arch/{riscv,loongarch64}/config.rs` — 将架构 DRAM 与固件保留区常量明确标记为 `_FALLBACK`，保留原有布局与断言。
- `os/src/mm/frame_allocator.rs` — 在帧区域枚举、物理 RAM 判断、回收区验证和预分配容量中改用 firmware provider。
- `os/src/main.rs`、`os/src/hal/mod.rs` — 在 `mm::init()` 前填充 firmware memory buffer 并发布模块。
- `os/Cargo.toml` — 添加禁用默认 feature 的 FDT fork 依赖，供后续真实解析实现使用。
- `docs/01_architecture/hal-and-platform.md` — 同步两阶段 firmware memory discovery 设计。

**验证：**
- `docker compose exec -T os-dev make kernel ARCH=rv64 PROFILE=normal` ❌（exit 2；工作区缺少 `dependency/lwext4_rust/c/elf-linux-gnu.cmake`）
- `docker compose exec -T os-dev make kernel ARCH=la64 PROFILE=normal` ❌（exit 2；同一缺失的 lwext4 子模块文件）
- `docker compose exec -T os-dev make lint` ❌（exit 2；Cargo 在更新 FDT fork 后仍因缺少 `/app/dependency/lwext4_rust/Cargo.toml` 中止，未进入本次源码诊断）
- `git diff --check`（PR2 文件范围）✅

**备注：**
- 当前 `fdt::parse_memory_regions()` 按 PR2 范围固定返回 `false`；启动路径因此使用已验证的静态回退配置。

### 编译环境修复 — lwext4_rust submodule 初始化

**问题：** `make kernel` 失败 exit 2，缺少 `dependency/lwext4_rust/c/elf-linux-gnu.cmake`。

**根因：** `dependency/lwext4_rust` submodule 已注册但目录为空（`git submodule status` 显示 commit hash 但无 checkout）。

**修复：** `git submodule update --init dependency/lwext4_rust`

**重新验证（在 Docker 容器内）：**
- `cargo check --features 'board_rvqemu log_off block_virt oom_handler' --target riscv64gc-unknown-none-elf` ✅（1039 warnings，均为存量）
- `cargo check --features 'board_laqemu log_off block_virt_pci oom_handler loongarch64' --target loongarch64-unknown-linux-gnu` ✅（1047 warnings，均为存量）
- PR1 + PR2 双架构 cargo check 零新增错误
