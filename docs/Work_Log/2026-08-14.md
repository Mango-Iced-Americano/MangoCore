---
date: 2026-08-14
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-14

<a id="2026-08-14-1414-rv-satp-buildstorm"></a>
## 14:14 — RV SATP 修复后干净 BuildStorm 验证

**Date:** 2026-08-14 14:14 +08:00
**Author:** AI:Codex | Human:lzm
**Tags:** [area:mm, area:riscv64, area:buildstorm, type:performance, type:validation]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 在 `rv-satp-fix` 当前未提交 diff 上，以官方 RV pub golden 的全新 qcow2 overlay、独立 x1 工具盘副本、8 CPU/8 GiB、MTTCG、release、`LOG=off`、无 `perf_diag` 完成一次正式 BuildStorm。完整编译成功且错误模式扫描干净；计时 2339 秒，相对同机旧精确样本 2434 秒缩短 95 秒（3.90%），相对用户描述的约 2400 秒缩短约 61 秒（2.54%）。

**Files:**
- `docs/Work_Log/evidence/2026-08-14/rv-satp-fix-buildstorm.log` — 完整宿主构建与 QEMU 串口日志（gitignored 本地证据）。
- `docs/Work_Log/evidence/2026-08-14/rv-satp-fix-buildstorm-manifest.md` — 输入、命令、hash、marker、退出边界与性能对比。

**Verification:**
- Docker `make buildstorm ARCH=rv64 MODE=release CORE_NUM=8 QEMU_MEMORY=8G LOG=off` — ✅ 宿主 release 内核干净重建；QEMU 正式 workload 完成。
- `Platform HART Count: 8`、`configured=8 online_mask=0xff`、PID1 all-CPU affinity — ✅。
- `BUILDSTORM_PRECLEAN ok target=riscv64gc-unknown-linux-musl` — ✅。
- `BUILDSTORM_TOOLCHAIN ok`、`BUILDSTORM_MINIBUILD ok` — ✅。
- `BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=2339.00 cores=8 bytes=1681000 arch=riscv64` — ✅。
- `#### OS COMP TEST GROUP END buildstorm-glibc ####` — ✅。
- panic、kernel exception、OOM、BadAddress、ASID stale、TLB/shootdown timeout、`BUILDSTORM_* fail` 扫描 — ✅ 无匹配。
- `git diff --check` — ✅。

**Notes:** 新 worktree 首次正式入口在 QEMU 前暴露缺失 x1 工具盘，补入与既有正式环境 hash 相同的独立副本后从新 overlay 重跑；该次未进入 guest，不纳入结果。BuildStorm profile 将官方脚本直接 exec 为 PID1，业务 END 后没有 MangoCore shutdown 调用，QEMU 会 idle；确认完整成功 marker 和无错误后精确终止本轮专用 QEMU。仓库副本的 judge 缺少同目录 baseline 配置并回退到占位 400 秒，其 60 分输出无效；本轮只报告可审计的 elapsed 与历史同机对比，不把占位分数当官方成绩。

<a id="2026-08-14-1259-rv-satp-no-switch"></a>
## 12:59 — RISC-V SATP no-switch 与 ASID epoch 修复

**Date:** 2026-08-14 12:59 +08:00
**Author:** AI:Codex | Human:lzm
**Tags:** [area:mm, area:riscv64, type:bugfix, type:performance]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md, debugging-patterns.md

**Summary:** 在 `origin/develop@df17b5ac2496` 上建立 `rv-satp-fix`，让 RV64 普通用户陷阱不再双切 SATP，并补齐共享内核映射、ASID epoch、TLB shootdown 与页表根生命周期协议。

**Files:**
- `os/src/hal/arch/riscv/{entry.asm,config.rs,kern_stack.rs,sv39.rs,trap/trap.S,trap/mod.rs}` — 建立高半区物理直映和共享 supervisor 根；普通 trap entry/restore 移除 `satp`/`sfence.vma`；实际 MM 切换改为 IRQ-off 的 ASID epoch 安装事务。
- `os/src/mm/{address.rs,page_table.rs,mapper.rs,kernel_mapper.rs,kernel_space.rs,address_space.rs,tlb.rs,mmu_gather.rs,heap_trace.rs}` — 用户根共享 Sv39 root[256..511)，root[511] 保持私有；补齐 2 MiB 映射、页表角色约束、per-MM epoch 和 lagging-target 全刷降级；zombie 只提前退休叶映射，根/中间页保留到最终 drop。
- `os/src/task/{processor.rs,manager.rs,process.rs}` — 新根安装完成前 pin 旧 MM，切回内核根后才 deactivate/drop，并把 zombie 清理移动到 idle owner 收尾。
- `os/src/hal/firmware/fdt.rs`、`os/src/drivers/**`、`os/src/hal/platform/jh7110_cache.rs` — RAM/MMIO/DMA 改走受 64 GiB 上限保护的高半区 direct map；修正 FDT 页对齐/重叠和 DMA 边界检查。
- `os/src/kernel_tests/smp.rs` — 并发 PTE 测试用 RAII 保证 software active mask 与真实 SATP 安装同生命周期，覆盖 rollover 后的 installed-epoch 契约。
- `docs/01_architecture/*`、`docs/04_mm/*`、`docs/05_process/*` — 同步 no-switch trap、共享页表、高半区 direct map、ASID epoch 和 zombie 页表生命周期。

**Verification:**
- Docker `make kernel ARCH=rv64 PROFILE=normal` — ✅ final release build。
- Docker `make kernel ARCH=la64 PROFILE=normal` — ✅ final release build。
- Docker `make ktest ARCH=rv64 PROFILE=normal CORE_NUM=2 KTEST=smp KTIMEOUT_MS=10000 KTEST_QEMU_TIMEOUT=90` — ✅ 39/39。
- Docker `make ktest ARCH=la64 PROFILE=normal CORE_NUM=12 KTEST=smp KTIMEOUT_MS=15000 KTEST_QEMU_TIMEOUT=180` — ✅ 39/39；12 CPU online，VirtIO PCI block/net 正常。
- Docker `make lint` — ✅ RV64/LA64 × debug/release 四格均匹配 warning baseline。
- `rg -n "csrw[[:space:]]+satp|sfence\\.vma" os/src/hal/arch/riscv/trap/trap.S` — ✅ 无匹配。
- `git diff --check` — ✅。

**Notes:** 首轮 RV SMP 为 37/39：test30 只调用 `activate_on()` 却未真实安装 SATP，rollover 后被正确降级为 Full；修正 harness 后全绿。test18 的一次 owner 观察失败是 develop 祖先上已有的 kernel-only 亲和性时序窗口，与 SATP 路径无直接语义关系，本轮复跑通过，未扩大修复范围。可选 SATA profile 的 PCI/DMA 地址仍不在本次 normal-profile 64 GiB direct-map 合同内，后续启用该 profile 时需单独迁移。
