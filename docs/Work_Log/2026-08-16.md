---
date: 2026-08-16
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-16

<a id="2026-08-16-jh7110-gmac-fdt-probe-l1"></a>
## JH7110 GMAC L1：默认内核 FDT 探测与 GMAC0 安全边界

**Date:** 2026-08-16 +08:00
**Author:** AI:GPT-5.6-terra | Oracle design review | Human:lzm
**Tags:** [area:rv64, area:net, area:firmware, area:driver, type:feature, type:validation]
**Mango Gate:** skill loaded=yes; references=none（功能实施，非调试/性能任务）

**Summary:** 将 JH7110 GMAC 从 `gmac_probe` 编译期隔离的固定板级初始化迁移为默认编入的
FDT 驱动。RV64 网络初始化对同一 `DeviceManager` 先尝试 VirtIO，再以 binding 精确匹配的
GMAC probe 回退；QEMU FDT 无节点时 discovery 只遍历已冻结的 `DeviceInfo`，返回 `None`，不读
GMAC 或 CRG MMIO。L1 仅接受 GMAC0 `0x16030000` 和非零首个 `interrupts` cell；GMAC1
`0x16040000` 显式跳过，尚未解析 clocks/resets/syscon phandle。

**Files:**
- `os/Cargo.toml`、`os/src/drivers/net/mod.rs` — 将 `gmac_probe` 加入默认 feature；按架构
  收窄 GMAC module cfg；运行时采用 VirtIO 优先、GMAC FDT fallback，保留 PCI fallback。
- `os/src/drivers/net/gmac_jh7110/{probe.rs,mmio.rs}` — 新增无 MMIO 的 binding discovery；以
  `GmacMmio { base }` 封装实例寄存器，拒绝非 GMAC0 与不足 DMA status 覆盖范围的 `reg`。
- `os/src/drivers/net/gmac_jh7110.rs`、`{phy,ring,ktest}.rs` — PHY/链路、DMA ring、MAC 配置和
  IRQ ack 全部接收同一实例 MMIO；FDT IRQ 取代 IRQ 7 fallback；PHY 成功与 link 配置后才启动
  DMA/中断，避免失败路径释放已运行 ring；IRQ handler 仅 W1C status 并发布 RX pending。
- `os/src/kernel_tests/{gmac_probe,gmac,mod}.rs` — 以 synthetic FDT 覆盖三个合法 compatible
  组合及 disabled/missing reg/short range/missing-or-zero IRQ/GMAC1/裸 DWMAC 的 fail-closed
  情形；无实板硬件的既有 GMAC ktest 返回 TAP SKIP。

**Verification:**
- Docker 串行 `make build ARCH=rv64 PROFILE=normal` 与 `PROFILE=regression` — ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal CORE_NUM=4 KTEST=gmac_probe` — 3/3 PASS；
  live QEMU FDT 无 GMAC，启动日志保持 `No net device`，没有 GMAC probe/MMIO 日志。
- 完整 RV64 4-core ktest — 176 PASS、9 SKIP、0 FAIL、185 total；8 个实板 GMAC 用例均为
  `SKIP no initialized JH7110 GMAC hardware`，新 discovery 3/3 PASS。
- `make test ARCH=rv64 PROFILE=regression` — 28/28 PASS。
- `make kernel ARCH=la64 PROFILE=normal` — ✅；默认 `gmac_probe` 未使 LA64 编译 JH7110 module。
- `make lint` — ✅：RV64 debug/release 188/0/30，LA64 debug/release 191/0/31，均匹配基线。

**Notes:** 未进行实板 TFTP/boot，本工作包不外推为 VF2 PHY、DMA 或 PLIC 运行时验收。L2
仍需解析 clocks/resets/syscon phandle；L3 才支持 GMAC1/eth1 与多实例 IRQ context。没有命中
相关 `code_paths` 文档的过时内容；`AI-Usage-Report.md` 已记录本模型和 Oracle，未新增工具。

<a id="2026-08-16-rv64-per-cpu-plic-context"></a>
## RV64 L2：FDT 驱动的 per-CPU PLIC context 与 AP 页表后 SEIE

**Date:** 2026-08-16 +08:00  
**Author:** AI:GPT-5.6-terra | Oracle review | Human:lzm  
**Tags:** [area:riscv64, area:firmware, area:smp, area:irq, type:feature, type:validation]  
**Mango Gate:** skill loaded=yes/reused; references=debugging-patterns.md

**Summary:** 将 RV64 PLIC 从单一 BSP context 扩展为 FDT `interrupts-extended`
驱动的 per-CPU context 表。解析链为 PLIC entry phandle → `riscv,cpu-intc` →
父 CPU `reg` hart → 冻结的稀疏 logical CPU；标准 supervisor external interrupt
9 优先，同一 hart 缺少 9 时才采用兼容值 11。解析、引用或 MMIO 范围不满足契约时
fail-closed，仅回退既有 BSP context，绝不推断 AP context。实现按 Oracle 审查后的
边界拆分全局 `init_controller()` 与当前 CPU `init_local_context()`：controller 先
quiesce 全部已发布 context，local 初始化后才允许 claim/complete。AP 继续在
bootstrap 阶段只开 SSIE，待安装本 CPU 内核页表并刷新 TLB 后才访问高半区 direct-map
PLIC MMIO 并开启 SEIE。默认设备 handler 仍绑定 logical CPU0，因此没有扩大网络
IRQ 或 smoltcp 数据面迁移范围。

**Files:**
- `os/src/hal/firmware/{fdt.rs,mod.rs}`、`fdt/plic_context_tests.rs` — FDT PLIC
  topology 解析、稀疏 hart/phandle 单元覆盖，以及 RV64 cfg 限定。
- `os/src/hal/arch/riscv/plic.{rs,mmio.rs,dispatch.rs}` — context 发布、本地 MMIO
  初始化、targeted handler enable、current-CPU claim/complete。
- `os/src/hal/arch/riscv/mod.rs`、`os/src/smp.rs` — BSP/AP 的 PLIC、SSIE/SEIE 和
  内核页表激活时序。
- `docs/01_architecture/{riscv64-platform.md,boot-and-trap.md}` — 实现边界与启动时序。

**Verification:**
- `make kernel ARCH=rv64 PROFILE=normal`、`make build ARCH=rv64 PROFILE=normal`、
  `make build ARCH=rv64 PROFILE=regression` — ✅。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal CORE_NUM=2/4/8` — ✅；
  `make -C os -f make/rv64.mk net-irq-run CORE_NUM=1/8` — ✅；
  `make test ARCH=rv64 PROFILE=regression` — ✅。
- `make kernel ARCH=la64 PROFILE=normal` — ✅（串行在 RV64 后执行）。
- `make lint` 四格 — ✅：RV64 debug/release 均为 188/0/30，LA64 debug/release
  均为 191/0/31（first-party/maintained/vendor）。

**Notes:** lint 首先发现 RV64 的 PLIC dead import/dead wrapper，随后发现共享
`fdt.rs` 的 RV64-only topology 在 LA64 构建产生 dead-code/import warning；已从根因
删除死代码并按 `target_arch = "riscv64"` 限定该解析组。`cargo fmt --check` 仍无法运行，
因为 pinned `nightly-2026-05-10` 容器工具链未安装 `cargo-fmt` 组件。

<a id="2026-08-16-publish-la-fix-rv-satp-fix"></a>
## 发布批次：la-fix 与 rv-satp-fix 合并入 GitHub develop/main

**Date:** 2026-08-16 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:riscv64, area:loongarch64, area:buildstorm, type:merge, type:validation]

**Summary:** 将 `la-fix`（LA buildstorm boot 段停滞修复：pipe 锁重入、
exit_signal_hint）与 `rv-satp-fix`（RV64 BuildStorm 优化：qemu-gated
fence.i、128 KiB demand readahead、prezero pool 256→2048、scheduler tick
100→50 Hz、定点时间换算、cbo.zero 清零）以两个 `--no-ff` merge 提交合入
基于 `origin/develop`（7c865ab43）的发布分支。注意 rv-satp-fix 的
tick/prezero/readahead 是**双架构共享参数**，合并后 LA 侧行为同样改变，
因此双架构都重新做了完整验证。唯一冲突为两分支同时新建
`docs/Work_Log/2026-08-16.md`（add/add），按条目合并保留双方内容。

**Files:**
- `os/src/task/process.rs`、`os/src/task/task.rs`、`os/src/fs/dev/pipe.rs` —
  la-fix 的 exit_signal_hint 与 pipe 锁重入修复。
- `os/src/task/manager.rs`（SCHED_TICK_NS 20ms）、`os/src/timer.rs`（定点
  换算）、`os/src/mm/frame_allocator.rs`（prezero 2048 + cbo.zero）、
  `os/src/fs/page_cache.rs`（demand read 128 KiB）、
  `os/src/hal/arch/riscv/`（fence.i gate、cbo.zero）— rv-satp-fix 优化。
- `docs/Work_Log/2026-08-16.md` — add/add 冲突按条目合并。

**Verification:**
- LA64 release 编译 3m20s ✅；RV64 release 编译 3m30s ✅（串行）。
- LA12 `KTEST=timer,smp,waitqueue`：61/61 PASS。
- RV8 `KTEST=timer,smp,page_cache_sync`：50/50 PASS。
- 完整双架构 buildstorm：见下方结果。

**备注:** 发布分支为 `publish-20260816`；本地 `develop` 因
`build/develop-check` worktree 占用而保持旧值，push 后由 worktree 自行
快进。

<a id="2026-08-16-la-exit-signal-hint-sigchld"></a>
## LA 评测 boot 段停滞根因：线程组收尾丢 leader exit_signal，父进程等不到 SIGCHLD

**Date:** 2026-08-16 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:loongarch64, area:signal, area:process, area:buildstorm, type:bugfix, type:liveness]

**Summary:** 官方 LA buildstorm 评测日志停在隐藏 boot 段
（`boot arceos-helloworld in qemu` 的两条 unsupported syscall 之后）直至
50 分钟墙钟被杀，而 RV64 同段瞬间完成。定位为：线程组退出时最后完成
进程级收尾的可能是非 leader sibling，其 TCB `exit_signal` 为空；旧
`finish_exit()` 按该线程的 exit_signal 通知父进程，导致以
`timeout --foreground` 包装内层 QEMU 的父进程永远收不到 SIGCHLD，
脚本停在 `wait`（内层 QEMU 主线程持续 `clock_gettime`/`futex`/`ppoll`
正是 timeout 的 sigsuspend 等待循环）。修复：PCB 增加进程级
`exit_signal_hint` 快照，非 `CLONE_THREAD` clone 时写入 exit_signal、
非 leader exec 接管时恢复 SIGCHLD，`finish_exit()` 一律按快照通知父进程。

**Files:**
- `os/src/task/process.rs` — PCB 新增 `exit_signal_hint` 原子字段与
  setter/getter；`finish_exit()` 改用 `exit_signal_hint()` 通知父进程。
- `os/src/task/task.rs` — `sys_clone()` 非 `CLONE_THREAD` 时记录
  exit_signal；`become_group_leader()` 恢复 SIGCHLD。

**Verification:**
- 定向复现（la64-minimal 镜像注入模拟官方 boot 段脚本：
  `timeout --foreground 300` 包装内层 qemu + 轮询 `kill -0`）：
  修复前稳定卡死，修复后单轮 + 3 轮循环共 4/4 `rc=0`、
  `Hello, world!`、`buildstorm_testcode.sh exit_code=0`。
- LA64 release 编译 ✅；RV64 release 编译 ✅（双架构串行）。
- LA12 `KTEST=waitqueue,smp`：58/58 PASS。
- RV64 `KTEST=waitqueue,smp`：首轮 57/58（`smp::user_task_reschedules_
  and_sets_affinity` IPI 探针超时），smp 单测连续重跑 3/3 全 39/39
  PASS，判定为 MTTCG 偶发 flaky，与本次修改无关。
- 完整 LA buildstorm（官方 pub 镜像、12C/8G、LOG=off）：
  `BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=1624.00 cores=12
  bytes=1714568 arch=loongarch64`，`buildstorm_testcode.sh exit_code=0`，
  `run_selected_groups done`，qemu exit=0（06:45:55–07:16:10）。

**备注:** 进程级语义必须存在进程级 owner；依赖"最后执行收尾的线程碰巧
携带正确状态"在任意 sibling 可完成收尾的协议下必然有窗口。Linux 语义即
线程组退出通知信号取自 leader。

<a id="2026-08-16-la-pipe-poll-ring-reentry"></a>
## LA buildstorm 静默卡死根因：Pipe::poll 在 ring 锁内析构对端 Arc

**Date:** 2026-08-16 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:loongarch64, area:pipe, area:scheduler, area:buildstorm, type:bugfix, type:liveness]

**Summary:** 专项 LA12 fork/pipe 压测复现了 deferred publish 化后残留的
buildstorm prebuild 静默卡死。根因不是 RESCHEDULE IPI 丢失，而是管道
轮询路径的锁重入：`Pipe::poll`（以及 splice/tee）持有 `PipeRingBuffer`
的 ring `Mutex` 时调用 `all_write_ends_closed()`/`all_read_ends_closed()`；
旧实现用 `Weak::upgrade().is_none()` 判断对端存活。对端仍存活时，
`upgrade()` 产生的临时 `Arc<Pipe>` 在语句末尾被立即 drop，`Pipe::drop`
又反向获取同一把 ring `Mutex`，形成单核自死锁。修复为使用
`Weak::strong_count() == 0`，不构造强引用、不在锁内触发对端析构。

**复现与定位：**
- 自定义 LA12 initramfs fork+pipe 工作负载（父进程持写端存活时读/poll
  空管道）在修复前稳定复现：round 0 后无进展，所有 AP 停在 idle。
- Ctrl-O 现场快照显示 CPU0 无 current、本地 queued=1；QEMU monitor
  `info registers -a` 显示 CPU0 PC=0x80291e24，位于 `<Pipe as Drop>::drop`
  的 ticket-Mutex 自旋段，ticket=9 等待 serving=8；其余 11 个核均停在
  `__mango_cpu_idle_interrupt_exit`。
- disassembly 定位到 `Drop for Pipe` 第二分支（writable 端 drop →
  `peer_read_end()` → 再次 lock ring），与 `poll` 中临时写端 Arc
  drop 的调用链完全一致。
- 同类锁内 `upgrade().is_none()` 调用同时存在于 splice/tee 的 ring 锁
  路径，一并由 `strong_count` 修复覆盖。

**Files:**
- `os/src/fs/dev/pipe.rs` — `all_write_ends_closed()` /
  `all_read_ends_closed()` 改用 `Weak::strong_count()`；注释记录禁止在
  ring Mutex 内 `upgrade()` 的原因。
- `os/src/kernel_tests/waitqueue.rs` — 新增
  `waitqueue::pipe_poll_live_peer_no_ring_reentry` 回归用例。

**Verification:**
- LA12 `KTEST=waitqueue,smp`：58/58 PASS。
- 修复后同机 fork+pipe 定向压测（2000 轮 poll 空管道 + 子进程写端存活）：
  PASS。
- 完整 LA buildstorm 无追踪连续 3 轮（12C/8G、官方 pub 镜像、LOG=off）：
  1. `BUILDSTORM_COMPILE ok=true elapsed_s=1606.00 bytes=1714568`，qemu exit=0
  2. `BUILDSTORM_COMPILE ok=true elapsed_s=1596.00 bytes=1714568`，qemu exit=0
  3. `BUILDSTORM_COMPILE ok=true elapsed_s=1572.00 bytes=1714568`，qemu exit=0
  三轮均完整通过 prebuild/编译/END；无 panic、无
  `failed to publish kernel stack`、无 shootdown/ack Timeout。

<a id="2026-08-16-la-inner-qemu-loginfo-slow"></a>
## LA 内层 QEMU 启动 ArceOS 极慢排查：LOG=info 追踪放大，LOG=off 正常

**Date:** 2026-08-16 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:loongarch64, area:buildstorm, area:qemu, area:trace, type:performance, type:debugging]

**Summary:** LA 内层 QEMU（MangoCore guest 内运行 qemu-system-loongarch64 启动
ArceOS hello）在 `LOG=info` 内核上极慢，180s 观察窗内只输出到 OVMF 早期
PROGRESS CODE，最终被测试脚本 kill（`rc=137`），拿不到 `Hello, world!`。
相同内核源码、相同 overlay 和 12C/36G 外核配置下，`LOG=off` 连续 3 轮均在
约 137–143s 内完成内层启动并 `rc=0`。因此这是 syscall 追踪开销问题，不是
新的调度/IPI/锁卡死。

**证据：**
- LOG=off（buildstorm kernel, 12C/8G）：`MANGO_LA64_LONG_WAIT rc=0
  start=30 end=163 loops=25`，即内层约 133s。
- LOG=off（12C/36G × 3）：内层分别约 138s、137s、137s，均 `rc=0`。
- LOG=off 且外层关闭 MTTCG：内层约 143s，仍 `rc=0`。
- LOG=info（12C/36G）：内层 `MANGO_LA64_LONG_BG uptime=40` 后，
  30/60/90/120/150/180s 输出行数仅 4/6/12/15/19/21，180s 时
  `MANGO_LA64_LONG_ALIVE`，脚本 kill，`MANGO_LA64_LONG_WAIT rc=137`；
  194s 内日志 29.8 万行，其中 `[syscall] clock_gettime` 21.0 万条、
  `futex` 4.9 万条、`ppoll` 1.7 万条。

**结论/处置：**
- 正式 50 分钟评测必须使用 `LOG=off` 构建；`make buildstorm` 默认 `LOG ?= off`
  已满足。`LOG=info` 只用于局部 bug 定位，不能作为 LA 正式评测内核。
- 以 LOG=off 的 LA buildstorm 编译 1572–1606s + 内层 QEMU 137–143s + 外层
  启动约 30s 计，总预算约 29–30 分钟，低于 50 分钟墙钟。
- 若仍需要在 LOG=info 下观察内层 QEMU，可先只跑 boot 阶段，不建议跑完整
  BuildStorm。
<a id="2026-08-16-rv-perf-round3"></a>
## RV64 第三轮性能包：定点时间换算、CBO.ZERO、更大 readahead/prezero、QEMU gate fence.i

**Date:** 2026-08-16 +08:00
**Author:** AI:Codex | Human:lzm
**Tags:** [area:riscv64, area:mm, area:timer, area:scheduler, area:buildstorm, type:performance, type:validation]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md

**Summary:** 在 `rv-satp-fix` 合入 `main` 最终修复后，实现并验证一组 RV64
BuildStorm 优化。production 干净 BuildStorm 从同口径 1862s 降至
**1784s（-78s，-4.19%）**；`[axbuild]` 计时段从 1814.00s 降至
**1736.32s（-77.68s）**。仍慢于官方 RV B=1616s 参考线 168s（10.4%），
但本轮是固定窗口之外首个在完整计时段稳定复现的正向包。

**Changes:**
- `os/src/timer.rs` — `ticks_to_ns/us/ms` 与 `ns_to_ticks_ceil` 改为一次预计算
  `u64 fixed-point` 倍率；RV64 不再在每条 trap 时间记账路径调用 `__udivti3`。
- `os/src/hal/arch/riscv/{mod.rs,trap/mod.rs}`、`os/src/mm/frame_allocator.rs` —
  FDT 全部 CPU 报告 Zicboz 时页清零使用 `cbo.zero`；平台信息发布前仍保持标量清零，
  能力缺失平台 fail-closed。trap-return `fence.i` 按平台模型门控：QEMU TCG 默认
  省略，真实硬件/未知平台默认执行，`mango.rv.trap_return_fence_i=on|off` 可覆盖。
- `os/src/fs/page_cache.rs`、`docs/09_debug/perf_diag.md` — demand read 窗口
  16→32 页（64→128 KiB）。
- `os/src/mm/frame_allocator.rs` — prezero 高水位 256→2048 页，idle tick 补充
  2→8 页，event wake 补充 32→128 页。
- `os/src/task/{manager,processor}.rs` — scheduler tick 10ms→20ms，文档同步。
- `os/Cargo.toml` — release 增加 `codegen-units = 1`、`panic = "abort"`。

**Verification:**
- 双架构 production `cargo check` — ✅。
- RV64 `KTEST=timer,smp` 42/42、`KTEST=timer,smp,page_cache_sync` 50/50 — ✅。
- Docker `make lint` 四格 — ✅（RV 190/0/30，LA 192/0/31）。
- production 完整 BuildStorm（同官方 golden、RV8/8 GiB/MTTCG、LOG=off、无
  `perf_diag`）— ✅，`BUILDSTORM_COMPILE ok=true elapsed_s=1784.00`，完整 END，
  panic/exception/OOM/BadAddress/shootdown timeout 扫描无匹配。
- 同镜像 bootarg `mango.rv.trap_return_fence_i=off` 先行样本 — ✅ 1818s；
  默认策略改为 QEMU gate 后的正式样本 — ✅ 1784s。

**Notes:** 单样本 78s 改善仍受历史 BuildStorm 噪声（±30s 量级）影响，但方向与
固定窗口 `task_run_slice` 增量、`frame_prezero_refill_ticks` 减半一致。官方基线
1616s 的剩余缺口主要在 8 核 TCG 下 `core`/`std` 长串行段与后续大量中小 crate；
后续候选应优先评估 trap FPU lazy save、文件映射 fault-around 上限再提高、以及
Cargo 并行段 scheduler wake/steal 时延。

**Round 4 negative:** 把 demand read 提高到 64 页（256 KiB）、prezero 高水位提高到
4096 页、dentry cache 提高到 1024 项、scheduler tick 放宽到 40ms 的组合样本为
1812s（`axbuild done 1765.83s`），比本提交组合 1784s 慢 28s，已全部回退。该负结果
说明 128 KiB/2048 页/20ms 是本机当前较优拐点，不能继续线性放大这些参数。

**Linux/QEMU 对照复核（`20260812-rv-linux-final-03`）：** 同一宿主与 QEMU 10.0.2、
RV8/8 GiB/MTTCG 的 Linux RV64 正式 BuildStorm 为 `elapsed_s=2113.96`，
`axbuild done 2079.12s`，完整 END。当前 MangoCore 1784s / 1736.32s 已比该 Linux
对照快 **329.96s / 342.80s**；Linux timed 期宿主 QEMU 总 ticks 为 11122.9 CPU-s，
平均仅 5.25 核。Linux perf 热栈显示其开销集中在 trap entry/exit（约 35k 样本）、
page fault/MM（约 35k）、SBI RFENCE/ecall（约 9k）、mprotect/munmap/exit/exit_mmap
（约 13k）和 `clear_page`/khugepaged；其 timed 期 `pgfault` 4.00M、磁盘读仅 4528
请求/826k sectors。MangoCore 已把 RFENCE/TLB 和页故障路径优化到更低，说明本地
1784s 已经越过 Linux 参照线；与官方 B=1616s 的剩余 168s 更可能包含评测机/宿主
差异。后续内核侧仍可参考 Linux 热栈，优先做进程退出/mprotect/munmap 批处理，
但预期上限约 20–40s，不足以单独填平 168s。

## VF2 实板 regression 验证（2026-08-16 晚）

**Tags:** [area:vf2, area:regression, area:testing, type:validation]

**Summary:** 将 regression 内核（内嵌 initramfs，无盘模式）经 TFTP 部署到 VF2 实板（`kernel-rv-image` → `booti 0x40200000 - $fdtcontroladdr`），全套 28 项通过：`[L4 REGRESSION RESULT: PASS]`（27 passed + 1 skipped），随后 watchdog 正常复位回 U-Boot。

**关键观察：**
- L2 per-CPU PLIC：VF2 DTB 的 `interrupts-extended` **不可用**（U-Boot 提供的 DTB 无标准结构）→ 正确 fail-closed 回退 `context=2`（实板公式 `(hart-1)*2+2`），外部中断链路在实板正常工作（无 `failed to register`）。
- 4 核 SMP（`configured=4 harts=1,2,3,4`）下全套通过：fs_test 76/76、unix_test 14/14。
- inet_test auto profile 实板验证：无 eth0（regression 内核未编 GMAC 驱动）→ 探测失败 → 正确整体 SKIP，不误报。
- 实板真硬件 ~12s 跑完全部（QEMU MTTCG 需 ~8 分钟）。

**备注：** 之前一次误读日志（tail 看到复位后 U-Boot SPL 输出）误判"fs_test 卡死"，实为全部通过。GMAC 仍未编入 regression 内核（`gmac_probe` feature 隔离），实板网络相关测试待 GMAC 改造为 FDT 探测后补。

## DHCP 写入 resolv.conf + 实板 GMAC 真实外网验证（2026-08-16 深夜）

**Tags:** [area:net, area:vf2, area:gmac, type:bugfix]

**Summary:** 实板 GMAC 跑 inet_test auto 发现 DNS 探测一直失败。根因：resolv.conf 是构建时硬编码（QEMU SLIRP 的 10.0.2.3 或显式 8.8.8.8），**没用 DHCP 拿到的 DNS**；换平台就错。抓包确证板子 DNS 查询发往 10.0.2.3（无响应）。根治：内核 DHCP Configured/Deconfigured 时写 /etc/resolv.conf（Linux dhclient 语义），用 DHCP 分配的 resolver。QEMU 环境自动写 SLIRP DNS、实板自动写 DHCP DNS。

**Files:** `os/src/net/config.rs`（`write_resolv_conf`，`vfs_lookup_absolute` + `File::new_without_open` + `truncate_size(0)` + `write` 重建，只反映当前租约）。

**实板验证（dnsmasq 提供 DHCP/DNS/NAT）：**
- 板子 eth0 由 dnsmasq 分到 `192.168.200.89`，DNS `192.168.200.1` → resolv.conf 自动写入。
- inet_test auto 从 SKIP 变为**真实 36/38**：`baidu/bilibili DNS+TCP+HTTP`、`udp_external_dns`、`route03` 全部 TPASS（真实出站 8.8.8.8 → 公网）。
- 剩余 2 个 TFAIL：`tcp_send_recv`（baidu 某节点立即 RST，errno=104）、`http_get`（no data）——CDN 节点抖动，与 QEMU 侧同现象；external 已降级 skip（这 2 个是 fail 后的 skip）。
- 全套 `28 passed, 0 failed, 0 skipped`（无 inet SKIP）。

**环境搭建（宿主机做 NAT 网关）：** eth1(192.168.200.1) 接板子，eth0(192.168.110.220) 外网口；`dnsmasq --conf-file=/tmp/dnsmasq-vf2.conf`（dhcp-range 192.168.200.50-100、dhcp-option 3/6=192.168.200.1、server=8.8.8.8）；现有 iptables NAT `0.0.0.0/0→MASQUERADE` 已覆盖板子流量出外网，ip_forward=1。

**已知遗留：** VF2 实板热启动不稳定（约半数 watchdog 复位后早期无输出，需手动断电重启），与早期 buildstorm 同类，待单独排查。
