## 2026-08-08

### 合并决策：删除通用 I/O fallback（WAIT_IO_FALLBACK_MS 10ms 兜底定时器）

**决策：** smp × develop 合并批次中删除 `WAIT_IO_FALLBACK_MS` 10ms 兜底定时器（manager.rs 的 `wait_event_impl` 全部调用点改为纯事件驱动，无 fallback 参数）。

**依据：** FS/Net 生产者通知已补齐（pipe 用 `notify_events_at_most` 唤醒读写端、splice/tee 走 `WaitQueue::wait_until_interruptible`、PageCache 用 `state_waiters` + generation 通知、another_ext4 用 `MetadataMutationWait`），10ms 兜底轮询不再需要；develop 侧（5d535be0）也删除了同类 fallback。此前的"暂不删除"决策（2026-08-03）随 producer 通知完成而撤销。

**验证：** 删除后 `wait_until_io`/`wait_event` 等全部走事件驱动；AGENTS.md「通用 I/O fallback 已删除」表述同步更新。

### 合并 os/src/fs/page_cache.rs：smp 骨架 + develop 写路径优化吸收

**问题与根因：**
- `git merge develop` 中 `os/src/fs/page_cache.rs` 有 86 处冲突标记（两侧从同一祖先独立演化成两套不同实现）。
- smp 侧以 `op_gate`/`state_waiters`/WaitQueue 睡眠重试 + write lease 为并发基础；develop 侧以单核自旋（100 次 CAS + `spin_loop()`）为前提，直接吸收会回归。

**修改（以 smp 版为骨架整体重建）：**
- 以 `38f73ac4:os/src/fs/page_cache.rs` 为基底写入工作区，消除全部冲突标记。
- 移植 develop 的三个薄包装，全部走 smp 的 WaitQueue 睡眠重试外壳：
  - `write()`（`Option<usize>` 签名）与 `write_with_after_copy()`（after_copy 在全部 lease commit 后、脏页节流前执行一次）。
  - `write_with_before_copy()`（test-only seam：populate/租约获取后、字节复制前回调，失败时 abort 已获取租约）。
  - `write_without_balance()` + `suppress_balance: AtomicBool`（抑制全局脏页节流）。
- 移植 `WriteStageCycles`（lookup/lease/copy/commit 四阶段计时），经 `record_write_stages` 汇入现有 smp 侧 perf 计数器（lease 并入 lookup，不依赖 develop 独有 perf 函数）。
- 新增 `read_user`/`write_user` 命名别名（ktest 与 develop 调用方依赖）。
- 未吸收：develop 的 100 次 CAS 自旋、无 WaitQueue 事件轮询、`PageEntries: RwLock<Vec>`（可选，低优先级，smp 的 `Mutex<Vec<Option<Arc<PageEntry>>>>` 已自洽）。

**保留的 smp 机制：** `op_gate`、`state_waiters` + generation 通知、WaitQueue 睡眠（`wait_for_write_lease`）、write lease（`try_lock_for_write`/`commit_write`/`abort_write`）、filemap API（`filemap_fault_wait`/`frame_for_filemap_read`/`copy_page_for_private`/`try_frame_for_write`/`register_file_vma`/`unregister_file_vma`/`map_page`/`unmap_page`/`map_count`）、`read_at_user`/`write_at_user`。

**验证：**
- `grep -c '^<<<<<<<' os/src/fs/page_cache.rs` = 0；`spin_loop` 计数 = 0。
- 容器内 `rustc` 语法检查通过（仅 E0432/E0433 独立编译解析错误，无语法错误）。
- `/tmp` 内用 smp 版本恢复其余冲突文件后 `cargo check --features "riscv block_virt oom_handler"`：page_cache.rs 0 error；剩余错误均为独立环境中 `board_*` cfg/config 常量缺失，与本次修改无关。
- 说明：当前整个 merge 尚未完成（perf.rs、fs/mod.rs 等其余 UU/AA 文件未解决），全量构建需等其余冲突文件解决后进行。
### 性能观测阶段 0：剩余 MM/task hook 接入（进行中）

本批在前一批 VM/filemap/exec 最小计数器之上，继续接入 PageCacheFaultWait、帧分配器、堆分配器、MM
激活、唤醒/窃取、运行片段和 ELF 分阶段计时，并将字段接入 `/sys/kernel/stats/taskq`、`/sys/kernel/stats/vm`。
计数器仍为 profile-aware relaxed atomic；默认 feature-off 路径提供 no-op/stub，不改变同步和调度语义。

当前已完成 RV64/LA64 feature-on 与 feature-off Docker release 编译；ktest 和 BuildStorm 采样待本批提交前执行。

### 性能观测阶段 0：VM/filemap/exec 计数器补齐

**目标与范围：**
- 按 `MangoCore_SMP_MM_Task_Optimization_Plan.md` 的阶段 0 最小闭环，补齐地址空间锁、filemap 后端读取、exec
  direct/fallback 和同/异 MM 切换的低扰动计数器；不引入串口逐事件打印，不改变原有同步语义。

**涉及文件与修改：**
- `os/src/task/perf.rs`：增加 profile-aware AtomicUsize 计数器、reset 路径和 feature-off no-op/stub。
- `os/src/mm/address_space.rs`：记录读/写锁等待、持锁时长，以及释放写锁后 TLB flush 的时长。
- `os/src/mm/filemap.rs`：记录三类 fault 入口、PageCache ready/retry、后端读取次数/时长及 VM 锁内归因。
- `os/src/task/processor.rs`：记录 same-MM/different-MM 调度切换。
- `os/src/syscall/process/exec.rs`：记录 direct loader、ENOSYS 和 fallback 路径。
- `os/src/fs/sysfs/files/diag.rs`：将上述字段暴露到 `/sys/kernel/stats/vm`。
- `docs/09_debug/perf_diag.md`：补充计数器定义、profile 所属和采样方法。

**验证：**
- 代码静态检查已执行 `git diff --check`；RV64 Docker 编译使用独立 compose 容器和
  `BUILD_ROOT=/app/build-stage0-rv64`，与正在运行的 BuildStorm 容器隔离，release 链接通过。
- `perf_diag` feature-on release 编译按架构串行通过：RV64 `build-stage0-rv64-diag`、LA64
  `build-stage0-la64-diag`；两次均进入 `perf_stats` 实现路径，未出现阶段 0 hook 的编译错误。
- 独立 RV64 QEMU ktest（`KTEST=waitqueue`，6 项）通过：6 passed、0 skipped、0 failed，
  `[KTEST RESULT: PASS]`；启动日志确认 `mango.mode=ktest`、initramfs 解包和正常关机。
- RV64/LA64 产物分别生成 `kernel-rv`/`kernel-la`；`git diff --check` 通过。

**备注：**
- `filemap_ready_hit` 表示 `frame_for_filemap_read()` 返回可用 frame，不宣称磁盘缓存命中；exec/MM 切换计数器
  使用 `core` profile，VM/filemap 使用 `memory_io` profile。
- 本批只覆盖阶段 0 的最小证据域；frame/heap、wake/steal 等后续计数器不在本次范围。

### FS/SMP 修复批次 24：清除 PageCache/another_ext4 临时自旋与死注册表

**问题与根因：**
- `PageEntry::try_lock_for_write()` 对同页写者或 writeback 竞争固定自旋 100 次，失败后把
  PageCache 内部竞争与后端真实 `EAGAIN` 混为同一个 errno；SMP 高竞争下会浪费 CPU，
  也可能让普通文件写向用户态泄漏虚假 `EAGAIN`。
- PageCache 和 another_ext4 metadata admission 在没有 `current_task` 时无限 `spin_loop()`；
  scheduler lifecycle drain 恰好在 idle 栈执行，不能安全睡眠，也不应无限占住 CPU。
- 全局同步迁移到 `BackendLifecycle` 后，another_ext4 的 `EXT4_REGISTRY` 只写不读，仍在
  每次可写挂载时积累 Weak 条目。

**涉及文件与修改：**
- `os/src/fs/page_cache.rs`：写租约改为一次 CAS；Busy 与后端错误使用内部类型分离，释放
  `op_gate` 后按目标页状态进入 WaitQueue。多页 kernel/UserBuffer 写在复制前取得全部目标
  页租约，所有失败路径释放已取得租约；UserBuffer 短拷贝按实际提交前缀返回。
- `os/src/fs/page_cache.rs`：无任务状态等待直接返回瞬时 `EAGAIN`，由 lifecycle drain 后续
  轮次重试；增加 waiter count，正常无竞争写入发布进度时不获取 WaitQueue 锁。
- `os/src/fs/ext4_another/fs.rs`：删除已无读取者的实例 registry；metadata admission 的无任务
  路径改为可重试错误，正常任务继续使用 generation + interruptible WaitQueue。
- `docs/03_fs/page-cache.md`：同步原子 flags、直接 UserBuffer I/O、事件驱动写租约和无任务
  lifecycle 重试契约。

**验证：**
- Docker `6b17b5297342` 挂载当前工作区 `/home/lzm/projects/MangoCore-smp-final-merge-20260806 -> /app`。
- 串行 `make kernel ARCH=rv64 PROFILE=normal`：通过，release 链接完成（2m18s）。
- 串行 `make kernel ARCH=la64 PROFILE=normal`：通过，release 链接完成（2m33s）。
- RV64 单核 `KTEST=page_cache`：5 passed、0 failed，验证后端 `EAGAIN` 仍单次传播且 Dirty
  不丢失、registry 锁外写回、UserBuffer 错误及后端重入。
- RV64 8 核 `KTEST=fs_smp CORE_NUM=8 QEMU_MEMORY=8G`：7 passed、0 failed，覆盖同页并发
  写、writeback redirty、write/truncate 顺序和不同页并行推进。
- `make lint`：未通过；现有 warning baseline 同时报告 16 个与本批无关的新条目和多个
  stale 条目。本批修改的 `page_cache.rs` 没有新增 warning，并消除了既有
  `private_interfaces` warning；未擅自重录全仓 baseline。
- `git diff --check`：通过。

**证据：**
- `docs/Work_Log/evidence/2026-08-08/fs-cleanup-rv64-page-cache-*`
- `docs/Work_Log/evidence/2026-08-08/fs-cleanup-rv64-fs-smp-*`

**备注：**
- 无任务上下文不再等待：后台卸载失败时 `BackendLifecycle` 保持 Dying 并重新入队，下一次
  scheduler drain 继续执行；这利用正式生命周期协议，不是固定次数重试或忙等。
- legacy ext4/swap 的历史 TODO 不属于本批 another_ext4/PageCache 临时适配清理范围。

### FS/SMP 修复批次 21：消除跨层轮询并修正 rseq 兼容声明

**问题与根因：**
- another_ext4 事务 metadata gate 的瞬时 `EAGAIN` 被 syscall、PageCache 和后端适配层
  分别用 `yield`/固定次数循环重试，既会在 8 核竞争下放大 CPU 消耗，也会让通用
  syscall 出现 another_ext4 专用行为。
- 创建特殊文件和符号链接时，通用 VFS 先发布目录项、再设置最终 uid/gid/mode；后一步
  失败会留下部分成功的对象。
- `rseq` 只登记用户区而未接入任务迁移、信号中止和 cpu_id 更新，却返回成功；SMP 下
  libc 可能据此使用过期 CPU 编号。

**涉及文件与修改：**
- `dependency/another_ext4/src/ext4/{mod.rs,low_level.rs}`、`src/lib.rs`：metadata gate
  释放后发布进度通知，并增加 guard 释放时序单元测试。
- `os/src/fs/ext4_another/`：统一在文件系统适配层等待 metadata gate 的 generation
  变化；truncate、时间戳、namespace mutation、reclaim 和 PageCache backend 复用同一
  事件驱动入口。
- `os/src/fs/page_cache.rs`、`os/src/task/manager.rs`：为 Loading/Writeback 状态增加精确
  通知 WaitQueue；等待前释放 `op_gate`，移除 PageCache 对后端 `EAGAIN` 的轮询。
- `os/src/fs/vfs/{index_node.rs,mount.rs}`：增加通用 `create_with_data_and_attrs()` 与
  `symlink_with_attrs()`，another_ext4 在单个 namespace transaction 内写入最终属性。
- `os/src/syscall/fs/`：普通文件 mutation 恢复为单次 VFS 调用；仅流式 I/O 保留原有
  waitqueue 阻塞语义，不再包含 another_ext4 专用重试或注释。
- `os/src/syscall/process/rseq.rs`：完整 SMP rseq 协议实现前 fail-closed 返回 `ENOSYS`，
  让 libc 使用兼容路径，避免虚假成功。

**验证：**
- Docker 内串行 `make kernel ARCH=rv64 PROFILE=normal`：通过（约 2m27s）。
- Docker 内串行 `make kernel ARCH=la64 PROFILE=normal`：通过（约 2m32s）。
- RV64 `KTEST=waitqueue`：6 passed、0 failed，`[KTEST RESULT: PASS]`。
- RV64 `KTEST=ext4_another`：20 项因没有磁盘 fixture 全部显式 SKIP；该结果只证明测试
  框架正常启动，不能作为磁盘功能通过证据。
- another_ext4 新增 notifier focused test：1 passed。
- another_ext4 全量 library test：137 passed；2 个既有失败
  `commit_flush_failure_never_publishes_or_checkpoints`、
  `direct_range_plan_filters_small_or_oversized_writes` 可单独复现，与 notifier 修改无关。
- `git diff --check`：通过。

**备注：**
- per-page `try_lock_for_write` 的短 CAS 自旋仍存在，它不是本批消除的跨层后端轮询；
  后续 PageCache 状态机迁移需单独评估。
- 完整磁盘一致性和 BuildStorm 正式测试继续暂停，待剩余 develop 分层迁移完成后使用
  fresh pub 镜像回归。

### FS/SMP 修复批次 22：下沉 syncfs 策略并收紧 UserBuffer 错误语义

**问题与根因：**
- `sys_syncfs` 在 syscall 层按 another_ext4、旧 ext4 分支选择同步流程，使 VFS 外围代码
  知道具体后端，并且 another_ext4 的实例级屏障与其他文件系统语义难以统一。
- PageCache 和 another_ext4 的直接 UserBuffer 路径会用描述符长度静默截短请求；写路径
  可能先修改前缀再把一个无效请求当作成功，偏离 develop 的全有或全无契约。
- PageCache 旧测试仍要求在后端 `EAGAIN` 后内部固定次数重试，与批次 21 已明确的错误
  所有权相冲突。

**涉及文件与修改：**
- `os/src/fs/vfs/file_system.rs`、`mount.rs`：增加通用实例级 `FileSystem::sync()` 并由
  MountFS 透传；默认实现仅为尚未迁移实例 registry 的后端保留兼容性全局 flush。
- `os/src/fs/{ext4,ext4_lwext4,ext4_another}/`：三个持久化后端分别实现实例级同步；
  legacy ext4 与 lwext4 先快照各自 cache registry，再在 registry 锁外写回，another_ext4
  复用其 data/metadata/journal/flush 屏障。
- `os/src/syscall/fs/sys_syncfs.rs`：删除所有具体 ext4 类型识别和 another_ext4 专用注释，
  仅解析 fd 所属文件系统并调用 VFS `sync()`。
- `os/src/fs/page_cache.rs`、`ext4_another/inode.rs`：请求长度超过 UserBuffer 时，在加载、
  lease 或 mutation 前返回 `EFAULT`，不再静默截短。
- `os/src/kernel_tests/page_cache_sync.rs`：把旧的后端轮询断言改为“单次调用传播 EAGAIN、
  保留 Dirty、显式后续调用可成功”，并增加短 UserBuffer 不修改缓存状态/内容的回归测试。

**验证：**
- Docker 内串行 `make kernel ARCH=rv64 PROFILE=normal`：通过（约 2m21s）。
- Docker 内串行 `make kernel ARCH=la64 PROFILE=normal`：通过（约 2m34s）。
- RV64 `KTEST=page_cache`：5 passed、0 failed，`[KTEST RESULT: PASS]`；包含 registry 锁外
  写回、后端 EAGAIN 所有权、短 UserBuffer 原子失败以及后端重入测试。
- `git diff --check`：提交前执行。

**备注：**
- legacy ext4 的 UserBuffer fallback 仍先完成用户内存拷贝再进入 inode transaction，避免
  把可能缺页的用户访问带入 inode 锁；不能用表面上的“零拷贝”直接替换。
- 全局 `sync(2)` 仍有具体后端枚举；彻底下沉需要先提供稳定的全局文件系统实例快照，
  与本批 `syncfs(2)` 的单实例语义分开迁移。

### FS/SMP 修复批次 23：全局 sync 改用 BackendLifecycle 快照

**问题与根因：**
- `syncfs(2)` 已在批次 22 下沉到 VFS，但 `sync(2)` 仍直接遍历 legacy ext4 registry 并
  调用 another_ext4 全局函数，syscall 外围仍知道具体后端。
- `FileSystem::sync()` 的默认全局 PageCache flush 会让内存文件系统也扫描全局 registry；
  若直接用于全局逐实例同步，会产生重复扫描和错误的责任归属。

**涉及文件与修改：**
- `os/src/fs/vfs/mount.rs`、`vfs/mod.rs`：增加 `sync_all_backends()`，在生命周期 registry
  锁内只快照 `Arc<dyn FileSystem>`，随后释放锁并逐实例同步；一个后端失败不阻止其他
  后端，保留首个错误用于诊断。
- `os/src/fs/vfs/file_system.rs`：内存文件系统的默认 `sync()` 改为 no-op，持久化后端必须
  显式声明自己的数据、元数据和设备屏障。
- `os/src/fs/fat32/efs.rs`：FAT32 尚无实例级 PageCache registry，故由 FAT 后端自身显式
  保留全局 PageCache flush 兼容路径，而不是由 syscall 或通用 VFS 猜测。
- `os/src/syscall/fs/sys_sync.rs`：删除 legacy ext4 registry 和 another_ext4 类型分支，仅
  调用通用生命周期同步入口；移除已无调用者的 another_ext4 全局 sync 包装函数。
- `os/src/syscall/process/misc.rs`：删除 shutdown 路径中 another_ext4 预关闭、全局
  PageCache flush 和 legacy ext4 枚举的重复阶段，只调用一次通用 backend teardown；避免
  another_ext4 被关闭后又进入全局写回、随后再次关闭。
- `FileSystem::on_umount()` 默认委托 `sync()`；FAT32 因而也在通用 teardown 中完成兼容
  屏障，三个 ext4 后端继续用各自覆盖实现保证 journal/cache/设备注销顺序。
- `docs/03_fs/vfs-core.md`：同步 FileSystem 实例同步及全局生命周期快照契约。

**验证：**
- Docker 内最终串行 `make kernel ARCH=rv64 PROFILE=normal`：通过（约 2m20s）。
- Docker 内最终串行 `make kernel ARCH=la64 PROFILE=normal`：通过（约 2m23s）。
- RV64 `KTEST=page_cache`：5 passed、0 failed，`[KTEST RESULT: PASS]`；关机阶段所有
  filesystem teardown 同时通过（在 shutdown 去重修改后重新构建并运行）。
- `git diff --check`：提交前执行。

**备注：**
- 此批完成了 syscall 同步路径去具体后端化。FAT32 的兼容性全局 flush 仍有额外扫描，
  待其拥有实例级 inode/PageCache registry 后可局部化，不影响当前持久化正确性。
- 未恢复 BuildStorm；继续按既定顺序完成剩余 lifetime/专项测试迁移后再使用 pub 镜像。

### develop 合并冲突解决：riscv arch 层 SMP 基础设施回填 + develop 启动协议整合

**背景：** `git merge develop` 进行中（未提交）。合并原则：smp 持有全部 SMP 基础设施，
develop 提供 reset/watchdog reboot、`configure_runtime_console`（VF2 动态 console）与
Image 格式（image_header.S / RAW_* 槽）；最终目标是 smp 合入 develop（设备自动探测），
smp 并发语义优先。

**涉及文件与修改：**
- `os/src/hal/arch/riscv/mod.rs` — 保留 smp 全部 SMP 基础设施（IDLE_STACKS、
  enter_secondary_idle、start_secondary_cpu、send_ipi、install_cpu_local/cpu_local_ptr、
  remote_user_tlb_invalidate_range、ASID allocator、RFENCE probe）；新增 `pub mod reset;`
  （develop 的 watchdog reboot）；移除已被 develop 删除的 rv_board 引用（platform/riscv 目录删除）。
- `os/src/hal/arch/riscv/entry.asm` — 整合两套入口：develop 的 RAW_* 槽保存 + 早期页表
  建置 + 高半区虚拟跳转；smp 的 MAX_CPUS 边界检查 + per-CPU boot stack 槽 + boot_park；
  保留 `.bss.bootstrap_page_table`（develop）与 `.bss.boot`（smp FDT 快照）。
- `os/src/hal/arch/riscv/sbi.rs` — 超集合并：develop 的 CONSOLE_BASE/SIZE/REGISTER_SHIFT
  原子 + configure_runtime_console + SBI SRST reboot + shutdown；smp 的 SBI v0.2
  HSM/IPI/RFENCE 扩展（hart_start/send_ipi/remote_sfence_vma_asid/init_rfence）+
  panic_console_write + machine_shutdown。
- `os/src/hal/arch/riscv/trap/mod.rs` — 以 smp 版本为主体（__kern_trap、per-cpu trap
  context、IPI fast path、timer quiesce、run_task_safe_point）；并入 develop 的
  pagefault perf stage 记录、notify_signal_waiters 与 arm_pagefault_return。
- `os/src/hal/arch/riscv/linker.ld` — 以 develop 版为准（KERNEL_LINK_VADDR 高半区 +
  .image_header）；`.bss.boot`（smp FDT 快照）与 `.bss.bootstrap_page_table`（develop 早期
  页表）同时保留在 sbss 之前。
- `os/src/hal/arch/riscv/linker-rvqemu.ld`、`linker-vf2.ld` — develop 已删除（FDT 取代
  board 文件），保留 develop 的删除结果（git rm）。
- `os/src/hal/arch/mod.rs` — feature→target_arch cfg 基础上，smp 并发 API 与 develop 的
  configure_runtime_console/reboot/shutdown 全量导出（loongarch64 分支同步超集）。
- `os/src/hal/mod.rs`、`os/src/main.rs` — 共享入口：保留 smp 的 register_cpu_entry 分流 +
  develop 的 save_boot_info/populate_memory_regions/configure_runtime_console 协议；
  消除 init_platform 双重初始化。

**验证：**
- 全部处理文件 `grep -c "^<<<<<<<"` 为 0。
- riscv/mod.rs 保留 IDLE_STACKS/start_secondary_cpu/send_ipi/enter_secondary_idle 等
  SMP 符号；sbi.rs 保留 configure_runtime_console/reboot；entry.asm 保留 RAW_* 槽与
  image_header 引用。
- 交叉符号导出一致（arch/mod.rs riscv 分支 ↔ riscv/mod.rs 及子模块；hal/mod.rs 调用面）。
- 未执行完整编译：合并仍在进行中，fs/mm/net/task 等大量冲突文件不在本工作包范围。

**备注：** 未 git add/commit（合并未完成，需用户批准）。
