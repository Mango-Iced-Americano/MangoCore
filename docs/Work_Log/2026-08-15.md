---
date: 2026-08-15
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-15

<a id="2026-08-15-rv64-plic-virtio-net-irq"></a>
## RV64 PLIC 与 virtio-net 外部中断链路

**Date:** 2026-08-15
**Author:** AI:GPT-5.6-terra
**Tags:** [area:hal, area:net, area:testing, type:bugfix]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md

**Summary:** 补齐 RV64 PLIC→virtio-net→CPU0 deferred poll/wakeup 链路，并以真实 UDP `recvfrom` 阻塞/唤醒验证外部中断不再依赖 scheduler tick fallback。

**Files:**
- `os/src/hal/arch/riscv/{plic.rs,mod.rs,trap/mod.rs}` — 初始化 supervisor PLIC context、开放 SEIE，并在 trap 中 bounded claim/dispatch/complete 外部中断。
- `os/src/task/{manager.rs,processor.rs,mod.rs}`、`os/src/net/config.rs` — 在 CPU0 task/idle 安全点消费 deferred external network work；为 negative control 提供 tick fallback gate。
- `os/src/drivers/net/virtio_net.rs` — 增加 IRQ 计数和已初始化 virtio-net 查询；无 NIC 的默认 ktest 不再误跑 focused 场景。
- `os/src/kernel_tests/{net_irq.rs,probe.rs,mod.rs}`、`user/src/bin/regression/main.rs`、`os/make/{rv64,la64}.mk` — 新增 hostfwd UDP focused QEMU 测试及用户态 `recvfrom` probe。
- `lint-baseline/{rv64,la64}-{debug,release}.txt` — 刷新已过期的四格 warning snapshot；同时移除 probe 中三项未使用常量。
- `docs/01_architecture/riscv64-platform.md`、`docs/06_net/device-adapter.md` — 同步 PLIC/SEIE 初始化顺序和 virtio-net IRQ 到 CPU0 deferred work 的边界。

**Verification:**
- `make test ARCH=rv64 PROFILE=regression` — ✅
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` — ✅ 153 passed, 11 skipped, 0 failed。
- focused `net-irq-run` — ✅ `CORE_NUM=1` 与 `CORE_NUM=8` 均确认 SEIE/tick fallback 关闭时 recvfrom 保持阻塞，恢复 SEIE 后由 virtio-net IRQ 唤醒。
- `make kernel ARCH=la64 PROFILE=normal` — ✅
- `make lint` — ✅ rv64/la64 × debug/release 四格基线一致。

**Notes:** 仅 RV64 具备本次 PLIC 外部中断实现；LA64 完成编译对称性验证。默认 ktest 不附加 virtio-net，因此该 focused 用例按设备可用性跳过。Miri 未安装于 pinned nightly，未执行。
