---
date: 2026-08-15
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-15

<a id="2026-08-15-evalsim-parallel"></a>
## make all 双架构并行 + 模拟评测机全流程 + 50 分钟预算核算

**Date:** 2026-08-15 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:riscv64, area:loongarch64, area:buildstorm, area:cagent, type:validation]

**Summary:** 按用户要求并行执行双架构构建与评测机形态 QEMU 全流程。构建并行化安全依据：
`rv64_all`/`la64_all` 的内核与用户 cargo 均使用 per-arch `CARGO_TARGET_DIR`
（`BUILD_ROOT/{rv64,la64}/release/normal/{kernel,user}`），`build.rs` 只写 OUT_DIR，
共享的只有只读 nightly 工具链；因此两个 make 进程并行不产生共享状态竞争。
`publish-compatibility` 保持串行收尾。

**结果（评测机形态 QEMU：RV 16G/8C、LA 36G/12C，官方 pub 镜像 + 仓库 os_test.conf
mask=0x6DDF，双 guest 并行）：**
- RV64：cagent 10/10；`BUILDSTORM_COMPILE ok=true elapsed_s=1935.00 bytes=1681000`；
  完整 END、无 panic。svvptc 门控激活（`[mm] FDT per-hart svvptc: all enabled CPUs`）、
  SBI IPI probe 缓存生效。
- LA64：cagent 10/10；`BUILDSTORM_COMPILE ok=true elapsed_s=2827.00 bytes=1714568`
  （LOG=info 追踪拖慢；无追踪同机基线约 1718-1725s）；完整 END、无 panic。
- LA64 首轮在 prebuild 阶段卡死一次（build 输出 0 字节、3 核空转 2.5h）——
  与官方 eager panic 同处 buildstorm 早期 fork 阶段，deferred 化后表现为静默停滞；
  复跑通过。**残余风险记录**：LA12 早期 fork/publish 存在低概率 liveness 窗口，
  需专项 fork-storm 压测，未在本轮闭合。
- 内层 QEMU boot：官方 pub 镜像缺 `opensbi-riscv64-generic-fw_dynamic.bin`（本地
  模拟缺口，评测机镜像自带——官方输出 `run=OK` 证明）；注入 firmware 后内层
  QEMU 10.0.11 成功启动 ArceOS 并打印 `Hello, world!`（fresh_map flush=on/off 两组
  对照均到达同一 BIOS 报错点，排除 fresh-map 对内层 QEMU 的破坏）。

**50 分钟总预算核算（guest 时间 ≈ 评测机墙钟）：** 合并 fresh-map 后 RV 编译
1935s（本机；用户独立测量 1858s），预算构成 ≈ 启动 ~1min + cagent ~2min +
编译 31-32min + 内层 boot ~2min（不计时但占预算）≈ **36-38min，余量 ~12min**。
官方评测前一轮 2527s 编译使总额贴近 47-50min；fresh-map 合入后按同比例折算
评测机编译约 2100-2200s，总额约 40-42min，**预计不会超过 50min**。

<a id="2026-08-15-rv-satp-fix-merge"></a>
## rv-satp-fix 合入 main（30f4b1971 + merge 1a9278015）

**Date:** 2026-08-15 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:riscv64, area:mm, area:tlb, type:merge, type:validation]

**Summary:** 用户确认 1858s 结果后，将 `rv-satp-fix` 7 个提交（fresh-map
no-flush svvptc 门控 + spurious-fault 快路、SBI IPI probe 缓存、rng 导入修复、
monitor LA64 匹配）合入 main；合入前先提交了本日决赛四项修复包（LA64 deferred
publish、listen backlog、跨页写截断、tty 原子写）。合并冲突仅
`docs/Work_Log/2026-08-15.md`（add/add），按两边条目合并保留。

**验证（合并后）：**
- Docker 串行 `make kernel ARCH=rv64/la64 PROFILE=normal MODE=release LOG=off` — ✅。
- Docker `make lint` 四格 — ✅（RV64 190/0/30、LA64 192/0/31）。
- RV8 `KTEST=smp` — ✅ PASS；LA12 `KTEST=smp` — ✅ PASS。

<a id="2026-08-15-fresh-map-production"></a>
## fresh-map no-flush production 化 + 最终成绩 1858s

**Date:** 2026-08-15 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:riscv64, area:mm, area:tlb, area:buildstorm, type:performance, type:fix]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md

**Summary:** 把 fresh-map no-flush 从 `perf_diag` 实验开关提升为 production 默认
能力门控：所有 enabled CPU 的 FDT 都明确报告 Svvptc 时启用（评测 QEMU 通过），
否则 fail-closed；并加入双架构 spurious-fault 快路防御真硬件负缓存。production
完整 BuildStorm **1858s**，对比 2196s 基线 **-338s（-15.39%）**，零 panic。

**Files:**
- `os/src/mm/user_mapper.rs` — gate 提升：production 编译、默认按
  `platform_supports_svvptc()` 启用；`perf_diag` 仍可
  `mango.rv.fresh_map_flush=on/off` 覆盖做 A/B。
- `os/src/mm/address_space.rs` — `do_page_fault` 首行新增 spurious-fault
  快路：PTE 已 valid 且权限满足时本地单页失效后直接 Completed，不进
  VMA/COW 分类。
- `os/src/hal/arch/{riscv,loongarch64}/mod.rs`、`hal/arch/mod.rs`、
  `hal/mod.rs` — 新增 `local_user_fault_tlb_invalidate_page`：RV64 用无
  ASID 单页 `sfence.vma`（rs2=x0，fault 本地 hart 多刷无害）；LA64 用
  当前 CSR.ASID 页失效（fault 时当前 hart 必在运行该 MM）；riscv 侧新增
  `platform_supports_svvptc()` 公开能力判定。
- `scripts/monitor_buildstorm_peak.py` — ownership 校验接受
  qemu-system-loongarch64（修复 LA 窗口 QEMU 泄漏：此前 LA 的 QEMU 因 exe
  名不匹配被 refused_signal，300s 后成为孤儿持续运行并污染后续窗口）。

**Verification:**
- 双架构四格 check（production/perf_diag）全过；lint 四格基线不变
  （RV 190fp / LA 192fp）。
- RV64 production `KTEST=timer,smp` 42/42 PASS；启动日志
  `[mm] FDT per-hart svvptc: all enabled CPUs`（gate 默认启用）。
- production 完整 BuildStorm（同官方 golden、RV8/8GiB/MTTCG、LOG=off）：
  `BUILDSTORM_COMPILE ok=true elapsed_s=1858.00`，完整 END 标记，panic/
  exception/BadAddress/shootdown timeout 零匹配。对比同机 2196s 基线
  -338s（-15.39%）；比 perf_diag 内核 1956s 快 98s（探针开销）。

**Notes:** 前一轮 1956s（perf_diag）与本次 1858s（production）都是单次干净
样本，未做统计重复；-338s 的置信边界仍受单样本方差约束，但幅度（15.4%）
远大于历史窗口噪声（±30s）。负缓存快路在 QEMU TCG 上不会触发（QEMU 无负
缓存），属真硬件防御路径，未在 QEMU 上获得运行覆盖。窗口驱动过程中修复了
monitor 的 LA exe 名硬编码与脚本 `$!` 指向错误，后续 LA 窗口不再泄漏 QEMU。

<a id="2026-08-15-e1-fresh-map-ab"></a>
## fresh-map no-flush A/B（E1）+ IPI probe 缓存（E4）+ LA8 配对（E2）+ VirtIO transport（E3）

**Date:** 2026-08-15 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:riscv64, area:loongarch64, area:mm, area:tlb, area:sbi, area:buildstorm, area:virtio, type:performance, type:instrumentation]
**Mango Gate:** skill loaded=yes; references=harness-patterns.md

**Summary:** 在 `3a85b3983` 上实现并验证四组实验：(E4) SBI IPI extension 探测
改为 BSP 一次性缓存 + 最少诊断计数器；(E1) `perf_diag` 专属
`mango.rv.fresh_map_flush=off` gate 跳过 invalid→valid fresh-map 的 TLB 失效；
(E2) LA8 诊断窗口与 RV8(fresh-off) 配对；(E3) RV64 virtio-mmio vs virtio-pci
双镜像 300s A/B（顺带修复 RV64 从未构建过 `block_virt_pci` 的 rng 导入冲突）。

**Files:**
- `os/src/hal/arch/riscv/sbi.rs` — `init_ipi()` 启动期一次探测并缓存
  `IPI_AVAILABLE`；`send_ipi` 不再每次 BASE probe（原为每次 doorbell 两次 ecall）。
- `os/src/hal/arch/riscv/{mod,time}.rs` — `platform_supports_isa_ext` 泛化
  （Sstc 复用）；boot 打印 `[mm] FDT per-hart svvptc: all enabled CPUs`
  （本机 QEMU 10.0.2 每 hart 的 FDT 均报告 svvptc，production marker 未打印）。
- `os/src/task/perf.rs` — 新增 `TRAP_COUNT/TRAP_TOTAL_TICKS`（cause 分类 +
  handler 区间计时，per-CPU entry 槽 + 10s 阈值防御）、`IPI_PROBE_CALLS/
  IPI_SEND_CALLS/IPI_SEND_TICKS_TOTAL`、`FRESH_MAP_TOTAL/FRESH_MAP_FLUSH_SKIPPED`；
  全部 `perf_stats` gate，production 零开销。
- `os/src/hal/arch/{riscv,loongarch64}/trap/mod.rs` — 双架构 `record_trap_enter/
  record_trap_exit_ticks` 插桩（LA 侧补齐，供配对比较）。
- `os/src/mm/user_mapper.rs` — `perf_diag` + RV64 专属 gate：`map_page` 成功
  fresh map（严格 invalid→valid）时不再 `record_change`（默认仍记录，行为不变）。
- `os/src/drivers/rng/mod.rs` — 删除 `fill_entropy` 中与 mmio VirtioHal 冲突的
  `block_virt_pci` 导入（熵源固定走 mmio bus.2，pci 导入为无用代码）。
- `os/src/fs/sysfs/files/diag.rs` — 导出 trap/IPI/fresh-map 计数器。
- `docs/Work_Log/evidence/2026-08-14/{run_buildstorm_window,compare_windows}.*` —
  窗口驱动/对比脚本（gitignored）：同一诊断镜像 + `-append` 运行期 cmdline 切换
  变体、monitor 300s 硬窗口或完整模式；产物目录挂机械盘
  `/home/lzm/buildstorm-final-mount/MangoCore-rv-satp-fix-worktree-build-20260815`。
- 窗口日志：`evidence/2026-08-14/{fresh-on,fresh-off,virt-pci-window,la8-window}.log`
  与 `fresh-off-full.log`（完整 BuildStorm，gitignored）。

**Verification:**
- 双架构四格：RV64/LA64 × `perf_diag` check 与 production check 全过；
  lint debug/release 基线不变（RV 190fp / LA 192fp）。
- RV64 `KTEST=timer,smp` 42/42 PASS（IPI 缓存后 doorbell 路径正常）。
- E1 300s 同镜像 A/B（唯一变量 `mango.rv.fresh_map_flush`）：
  `tlb_page` 719,817→16,895（-97.65%）；RFENCE 5,394→2,913 次（-46%）、
  pages -72.6%、targets -60%；`pagefault_ticks_total` 109.8→82.6 CPU-s
  （-24.8%）；`ecall_trap_cost` -4.3%（窗口噪声级）；两侧均到 `core` crate。
- E1 完整 BuildStorm（perf_diag 内核 + off）：**1956s**，`BUILDSTORM_COMPILE
  ok=true`，全程 `fresh_map_total=10,091,569` 且 `fresh_map_flush_skipped`
  =10,091,569（100% skip），panic/异常/超时零匹配。对照 production 基线
  2196s 观测差 **-240s（-10.9%）**；同口径 fresh-on 完整对照组因 guest 内
  cargo jobserver EFAULT 偶发失败（见备注），按用户指示跳过重跑。
- E2：LA8 300s 窗口（LA64 时钟 100MHz）配对：`page_faults` 242K vs RV 266K
  接近，但 LA `pagefault_ticks_total` 188.6 CPU-s vs RV(fresh-off) 82.6 CPU-s
  （每 fault 约 0.78ms vs 0.31ms）；LA `tlb_page` 533,510（每 fault ~2.2 次
  本地 sfence）vs RV 16,895；LA `tlb_shootdown_sync_ticks_total` 12.55 CPU-s
  vs RV 0.67 CPU-s，其中 `kernel_reason_task_publish` 10.1 CPU-s（LA 仍 eager
  协议，`kernel_task_sync_deferred_enabled()` 对非 RV 恒 false）。
- E3：RV64 `BLK_MODE=virt_pci` 首次构建成功（修复 rng 导入冲突后）；QEMU
  virt 机器 PCIe ECAM 经高半区 direct map 正常（`[PCI] ECAM base:
  0xffffffd030000000`），探测到 virtio-blk-pci。300s A/B（mmio vs pci，均
  fresh-on）：`pagefault_ticks` -11.2%、`ecall_trap_cost` +4.0%、run-slice
  -2.3%、syscall 数相等——全部在 300s 窗口噪声边界（fence A/B 曾见 ±30%
  同步 ticks 波动），**transport 差异不显著，不值得完整 BuildStorm**。
- 新 boot marker 生效：`[smp] SBI IPI extension available (probe cached)`、
  `[mm] FDT per-hart svvptc: all enabled CPUs`。

**Notes:**
- **trap_total_ticks 计数器判定不可用**：累计值超出物理 CPU-s 上限（300s×8
  核窗口出现 4e4 CPU-s），fault Retry/文件 I/O 阻塞与 per-CPU entry 槽在
  调度切换下错配，10s 阈值未能完全防御；结论一律使用 `ecall_trap_cost`/
  `pagefault_ticks`/`tlb_*` 代理，`TRAP_COUNT`（纯计数）仍有效。修复需
  per-task entry（TrapContext 槽）或排除阻塞区间，留待后续。
- **新发现一（评测稳定性风险）**：guest 内 cargo 长时间编译偶发
  `failed to acquire jobserver token / Bad address (os error 14)`（EFAULT），
  3 次完整 BuildStorm 中 1 次命中（fresh-on-full 两次尝试均失败于 260s 与
  919s）；与 fresh-map gate 无关（fresh-off 成功）。嫌疑在 MangoCore 的
  pipe/poll/read uaccess 路径（B57-B59 改造范围），单独定位。
- **新发现二（文档与代码不一致）**：AGENTS.md 声称 LA64 已默认 deferred
  task-mapping ack，实际 `smp.rs:727-729` 对非 RV 恒 `false`（eager），且无
  `mango.la.kernel_task_sync` bootarg 分支；LA8 窗口 `kernel_full=276`、
  publish sync 10.1 CPU-s 与代码一致。AGENTS 描述可能对应 develop 分支未
  合入本 worktree 的改动。
- **新发现三**：RV64 的 `block_virt_pci` feature 此前从未构建过（rng 导入
  冲突是第一个拦路点）；E3 证明 virtio-blk-pci 在 RV 可运行。
- E1 进入 production 的剩余步骤：gate 提升为 production 能力门控（复用
  per-hart FDT svvptc 判定）+ spurious-fault 快路（真硬件负缓存防御）+
  gate 激活下的 `smp` ktest 39/39。按用户指示，同口径 fresh-on 完整对照
  暂缓。
- 窗口运行环境：新容器 `rv-satp-fix-window-1`（`/app`=worktree，
  `/app/build`=机械盘 build 目录，机械盘 3.4T）；宿主 sdb2 空间紧张期间
  出现两次 ENOSPC，均在用户清理后恢复。

## 官方决赛输出四项异常定位

**Date:** 2026-08-15 +08:00  
**Author:** AI:Codex | Human:lzm  
**Tags:** [area:buildstorm, area:cagent, area:loongarch64, area:riscv64, type:debugging]

**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Evidence:** 官方附件 `Riscv输出 (1).txt` 与 `LoongArch输出 (1).txt`。

- RV CAgent 只有 `factorial` 为 `fail`，其余 9 项成功；LA 只有 `fs-usage`、
  `fs-readwrite` 失败。LA 的两个失败均在 1 秒内结束，初步指向这两个命令共同
  使用的 `awk` 子进程/相关 syscall；由于脚本删除了临时输出文件，仍需复现确认，
  不能归因于评分解析。
- RV BuildStorm 在 `BUILDSTORM_BEGIN` 后报告
  `elapsed_s=2527.00`、`artifact ... bytes=1681000`、`run=OK`；该耗时覆盖 guest
  内 `cargo xtask arceos build`，不是 MangoCore 内核编译耗时。历史同机干净样本为
  2196/2221/2339 秒，说明该指标受评测机 CPU、QEMU 调度和 guest 冷 target/cache
  状态影响，单个 2527 秒样本不足以证明 RV 优化回归。
- RV 评测脚本确实进入了 `boot arceos-helloworld in qemu`，随后只输出两个兼容性
  syscall 提示和 `BUILDSTORM_RESULT ... run=OK`；内层 QEMU 串口没有被脚本转发，
  因此看不到 ArceOS 的 `Hello, world!` 不等于没有启动。
- LA 在 `TOOLCHAIN_RESULT`、`MINIBUILD_RESULT` 后、尚未出现 `BUILDSTORM_BEGIN`
  前触发 `failed to publish kernel stack to CPU 3: Timeout { expected: 26,
  observed: 25 }`（`os/src/task/manager.rs:400`）。这是 eager kernel-global
  TLB/IPI 确认真实超时；panic 诊断和普通串口共用同一输出，故日志混入只是症状，
  buildstorm 未完成的直接原因是该 panic。

**Next fix boundary:** 不通过屏蔽日志或放宽评测判定绕过；应分别复现 RV CAgent
并发 `popen/awk` 路径、确认 LA `awk`/文件统计失败的具体 syscall，并修复 LA
远端 kernel-stack 发布确认协议后再做双架构正式门禁。

## 官方决赛四项异常的根因修复：LA64 deferred publish、cagent listen 槽、跨页写截断

**Date:** 2026-08-15 +08:00
**Author:** AI:DeepSeek | Human:lzm
**Tags:** [area:loongarch64, area:riscv64, area:smp, area:net, area:uaccess, area:cagent, area:buildstorm, type:bugfix, type:performance]
**Mango Gate:** skill loaded=yes; references=debugging-patterns.md, harness-patterns.md

**Summary:** 在上一轮定位基础上完成四个根因级修复并双架构复测。(1) LA64 远端任务
内核栈发布与 RV64 一样改为目标侧延迟确认，消除 eager IPI/ack 的 5 秒超时 panic；
(2) `listen(2)` 尊重用户 backlog（上限 64），修复官方 CAgent 10 并发客户端超过
固定 8 槽导致的 connect 被拒；(3) `write_from_user`/`pwrite_from_user` 不再把惰性
缺页误判为缓冲区终点，修复跨页写丢失尾部字节（日志行合并的根因）；(4) tty 小写入
在单次 OUTPUT_LOCK 临界区内完整发出，消除 64 字节 chunk 交错。

**涉及文件：**
- `os/src/smp.rs` — `kernel_task_sync_deferred_enabled` 双架构默认 true；
  `perf_diag` 增加 `mango.la.kernel_task_sync=eager|deferred` A/B 开关；`mapping_retire`
  仍全 CPU 同步等待。
- `os/src/net/socket/{mod.rs,inet/stream/{lifecycle,inner,mod}.rs,inet/datagram/udp.rs,inet/raw/raw.rs,packet.rs,netlink/mod.rs,unix/{stream,datagram}/mod.rs}`、
  `os/src/net/syscall/listen.rs` — `Socket::listen(backlog)` 透传；`MAX_LISTEN_BACKLOG=64`
  替换固定 8 槽上限；Unix stream listen 同样尊重 backlog。
- `os/src/syscall/fs/common.rs` — `write_from_user`/`pwrite_from_user` 四个 chunk 循环在
  `user_accessible_len == 0` 时按单页 fault-in 重试而非返回前缀。
- `os/src/fs/dev/tty.rs` — ≤256 字节写（含 ONLCR 展开）在单次 `write_bytes_atomic` 内
  原子发出；更大写回退原 chunk 路径，锁序不变。
- `AGENTS.md`、`docs/01_architecture/lock-order.md`、`docs/09_debug/perf_diag.md` —
  同步双架构 deferred 协议与 uaccess 惰性页规则。
- `.agents/skills/mango-workflow/references/{harness-patterns,debugging-patterns}.md` —
  沉淀 listen 槽容量、惰性页截断与官方脚本证据保留三条模式。

**验证：**
- Docker 串行 `make kernel ARCH=rv64/la64 PROFILE=normal MODE=release LOG=off` — ✅。
- Docker `make lint` 四格（RV/LA × debug/release）— ✅ 全部匹配 warning 基线
  （RV64 190/0/30、LA64 192/0/31）。
- LA12 `KTEST=smp` — ✅ 39/39（含 `kernel_stack_reclaim_waits_for_shootdown`、远程
  publish、steal/affinity race、terminal STOP）。
- LA64 评测形态 12C/36G + 官方 pub 镜像副本 `mask=0x4000` cagent 复测 — ✅ 10/10
  pass、无 panic；修复前同环境复现 `fs-readwrite reject 767ms`，保留的 agent 输出为
  `Connection failed to 127.0.0.1:8080`（listen 槽竞争），推翻 awk 假设。
- RV64 评测形态 8C/16G 同配置 cagent 复测 — ✅ 10/10 pass（含官方评测失败的 factorial）。
- LA64 `LOG=info` 定位：`write(64) -> 34`（53 字节消息返回 52）证明跨页截断；
  修复后 `[test-runner] ntpd unavailable; using fallback clock` 换行完整。

**备注：** 同机对照已完成：`make buildstorm ARCH=rv64 MODE=release CORE_NUM=8
QEMU_MEMORY=8G LOG=off`（当前 HEAD）得
`BUILDSTORM_COMPILE mode=multi ok=true elapsed_s=2234.00 cores=8 bytes=1681000 arch=riscv64`
（release 编译 35m20s，axbuild 计 2177.05s），与历史优化样本 2196/2221/2339s 一致，
证明官方评测的 2527s 是评测机/16G 内存/全 mask 单次启动的混杂因素，不是内核回归。
官方 50 分钟墙钟上限下 guest 时钟≈墙钟，完整流程约 42-48 分钟，余量仅 2-8 分钟；
内核侧已无已知额外开销，编译时长由评测机自身决定。RV 评测未见 hello world 是官方
脚本 boot 段内层 QEMU 串口不转发所致，`run=OK` 即脚本内验通过。证据日志位于
`build/cagent-repro/` 与机械盘 `/home/lzm/buildstorm-final-mount/MangoCore-smp-final-merge-20260806-build/cagent-repro/`，
未纳入 Git。
