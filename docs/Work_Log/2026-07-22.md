## 2026-07-22

### T3：规范构建入口与隔离产物图

**涉及文件：**
- `Makefile`、`os/Makefile` — 建立 root formal entrypoints，并让 `all` 串行完成 RV64、LA64 后再发布兼容产物。
- `os/make/{common,arch,rv64,la64}.mk`、`user/Makefile` — 将 kernel、user、image、initramfs、tools 产物收敛到 `build/<arch>/<mode>/<profile>`。
- `os/build_initramfs.sh`、`os/buildfs.sh` — 使用隔离的用户态 Cargo 输出作为打包输入。
- `.gitignore` — 忽略正式生成输出根 `build/`，避免构建产物进入工作树状态。
- `scripts/test-canonical-build-graph.sh` — 覆盖串行矩阵、四维输出隔离与 LA64 第二阶段失败不发布根兼容产物。

**验证：**
- Docker 内 `make -C os ARCH=rv64 PROFILE=normal rv64-kernel-build-only` ✅
- Docker 内随后 `make -C os ARCH=la64 PROFILE=normal la64-kernel-build-only` ✅
- `sh scripts/test-canonical-build-graph.sh --matrix serial` ✅
- `sh scripts/test-canonical-build-graph.sh --fixture second-stage-failure` 按约定输出 `FAIL:` 并返回非零 ✅
- `git check-ignore -v build` 确认 `build/` 由 `.gitignore` 忽略 ✅

**备注：** 未运行 QEMU；`.omo/boulder.json`、`os_test.conf`、`run_test.sh`、`.gdbinit`、`cc-codex/` 和 `docs/Work_Log/2026-07-19.md` 均未纳入本次提交。
