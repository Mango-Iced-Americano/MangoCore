## 2026-07-22

### T4：最小镜像角色合同与 boot-unblocking 边界

**涉及文件：**
- `os/make/image-roles.mk` — 新增 Make v1 角色表，集中声明 RV64/LA64 bootstrap initramfs、development x0 rootfs、保留名称的 competition sdcard、x1 tools/scratch，以及外部输入来源与 SHA-256 记录策略。
- `os/make/{rv64,la64}.mk`、`os/make/arch/*-settings.mk`、`os/make/common/orchestration.mk`、`os/Makefile` — normal/competition QEMU 消费者和 rootfs/tools 生产路径引用角色表；保持 x0+x1 ABI，regression/ktest 零盘；历史 direct x0 mutation target 显式拒绝。
- `os/inject_os_test_conf.sh` — virt/virt_pci 注入先创建命名 development 派生镜像，拒绝 official `sdcard-{rv,la}.img` 写入。
- `os/make/{tools,tools-disk}.mk` — 将 tools-disk recipe 拆出并改用独占 workspace/cleanup trap，不再挂载全局 `/tmp/tools-mnt`。
- `scripts/test-image-role-contract.sh`、`docs/architecture/image-role-contract.md` — 新增静态合同、四个拒绝 fixture 及中文角色说明。

**验证：**
- Docker：`sh scripts/test-image-role-contract.sh` 通过；`swapped-drive`、`third-drive`、`missing-payload`、`mutate-official-x0` fixture 均被拒绝 ✅
- Docker：`sh -n scripts/test-image-role-contract.sh os/inject_os_test_conf.sh` 通过；未启动 QEMU、未构建或修改外部 sdcard ✅
- Docker（显式继承容器 `$HOME/.rustup`/`$HOME/.cargo`）：`make -C os rv64-kernel-build-only` 后 `make -C os la64-kernel-build-only` 串行通过；仅有既有 warning ✅

**备注：** 开始阶段在 Docker `/app` 运行既有 T1 isolation 时，因 compose bind mount 未提供 Git worktree 元数据而以 `repo root is not a Git worktree` 阻塞；本轮未绕过或改写该门禁。T5 launcher、内核 mount policy、外部镜像下载及完整 image pipeline redesign 均未进入范围。

### T10：Formal lint gate with machine-readable warning baseline

**涉及文件：**
- `scripts/lint-check.sh` — Formalized warning baseline checker: runs `cargo check`, classifies warnings by ownership (first-party/maintained/vendor), compares against committed per-arch-per-mode baselines; exits nonzero on new first-party warnings.
- `scripts/test-lint-gate-fixture.sh` — Adversarial test fixture: injects a dead-code warning into first-party source, verifies `make lint` rejects it, restores source and baseline.
- `lint-baseline/{rv64,la64}-{debug,release}.txt` — Four committed machine-readable baselines (175-176 fp, 35-36 mt per cell).
- `os/Makefile` — `lint` target delegates to `lint-check.sh` with `validate-formal-inputs` guard; `lint-format` added for separate formatting check.
- `Makefile` (root) — `lint` target provides `PROFILE=normal` default and forwards to OS layer without redundant validation.
- `os/src/main.rs` — Replaced crate-level `#![allow(dead_code)]`, `#![allow(unused_assignments)]`, `#![allow(unused_variables)]` with justified comments; new dead code must be annotated per-item.
- `scripts/lint-check.sh` — Fixed awk pattern collision with summary lines (`first-party: 175` matched as data entry); added `[^ \t]` guard after category prefix.
- `.gitignore` — Added `user/tools/*/bin/` and `user/tools/*/lib/` for lint-check stub ELF binaries.

**验证：**
- Docker: `sh scripts/lint-check.sh --arch rv64 --mode debug` → PASS (175 fp, 35 mt, 0 vd) ✅
- Docker: `sh scripts/lint-check.sh --arch rv64 --mode release` → PASS (175 fp, 35 mt, 0 vd) ✅
- Docker: `sh scripts/lint-check.sh --arch la64 --mode debug` → PASS (176 fp, 36 mt, 0 vd) ✅
- Docker: `sh scripts/lint-check.sh --arch la64 --mode release` → PASS (176 fp, 36 mt, 0 vd) ✅
- Docker: adversarial injection (dead_code in bootargs.rs) → `make lint` exit 1 with "new first-party warning" ✅
- Docker: fresh full rebuild from clean cache → all 4 cells PASS ✅

**备注：** 未运行 QEMU。Cargo formatting (`lint-format`) separated from lint gate — not part of T10 warning baseline contract. The awk summary-line collision is recorded in debugging-patterns.md as a general multi-section text parsing lesson.

### T3：formal clean 边界与 current-HEAD 证据刷新

**涉及文件：**
- `os/make/{rv64,la64}.mk`、`user/Makefile`、`os/Makefile` — architecture clean 删除其传入 KERNEL_OUTPUT_ROOT/LWEXT4 root；user clean 删除 USER_OUTPUT_ROOT；OS clean 显式传递 USER_OUTPUT_ROOT，消除对 `os/target`、`user/target` 的未作用域 `cargo clean`。
- `scripts/test-canonical-build-graph.sh` — 在 formal/architecture/root clean 前创建 output、target 与 unrelated sentinels；验证 bounded 删除和 legacy direct user clean 默认行为。
- `.omo/evidence/task-3-repository-rebaseline-20260721.md`、`docs/Work_Log/evidence/2026-07-22/t3-formal-facade-docker.md` — 记录 Oracle RED、current HEAD、UTC、container ID、mount、命令状态、raw-log 引用和 protected fingerprints。

**验证：**
- Docker current HEAD：T1、完整 T2、全部 facade/entrypoint 与 T3 matrix 均 exit 0；second-stage fixture 预期 exit 1 ✅。
- Docker：OS clean → RV64 build → LA64 build → root clean 串行 exit 0；`os/target`/`user/target` sentinels及 unrelated compat sentinel 保留，边界输出与四个 named artifacts 按预期删除 ✅。

**备注：** 未运行 QEMU。验证前后 protected dirty path 和 fingerprint 不变；raw logs 保留在命名 Docker container，详见 evidence。

### T3：对称 clean 路径与 root compatibility artifact 清理

**涉及文件：**
- `Makefile`、`os/Makefile` — OS clean 显式执行 RV64 和 LA64 clean route；root clean 仅删除 BUILD_ROOT 和 COMPAT_OUTPUT_DIR 下四个已发布 artifact，避免宽泛删除。
- `scripts/test-canonical-build-graph.sh` — 使用临时 BUILD_ROOT/COMPAT_OUTPUT_DIR sentinels 验证双架构 clean route、精确 artifact 删除及 unrelated 文件保留。
- `scripts/test-root-build-contract.sh` — 覆盖 formal build 的缺失、非法和多值 ARCH/PROFILE 拒绝，同时保留 `all` evaluator 语义断言。
- `.omo/evidence/task-3-repository-rebaseline-20260721.md`、`docs/Work_Log/evidence/2026-07-22/t3-formal-facade-docker.md` — 记录 Oracle RED、完整 Docker status ledger 和前后 protected dirty 状态。

**验证：**
- Docker：T1、完整 T2、facade/expanded entrypoint、T3 serial matrix 全部 exit 0；second-stage fixture 按约定 exit 1 ✅。
- Docker：临时 BUILD_ROOT 上执行 OS clean → RV64 build → LA64 build → root clean 串行 exit 0；四个 compatibility sentinels 删除且 unrelated sentinel 保留 ✅。

**备注：** 初始 Oracle RED 显示 `ARCHS = rv64` 使 OS clean 漏掉 LA64，且 root clean 不移除 compatibility artifacts。未运行 QEMU；测试前后仅保留相同 8 个 protected dirty 路径。

### T3：formal facade 显式输入与 Oracle evidence 修复

**涉及文件：**
- `Makefile`、`os/Makefile` — formal `build`、`kernel`、`user`、`image`、`run`、`test`、`check`、`lint` 要求显式且唯一的 ARCH/PROFILE；新增参数化 `ktest-build-only` facade，保留架构命名 legacy alias 的默认行为。
- `scripts/test-{root-build,root-kernel,root-user,root-image,canonical-entrypoint}-contract.sh` — 将 root recipe 断言迁移到现有完整转发接口，并覆盖 root/OS `check`、`ktest-build-only` 的缺失、非法和多值参数拒绝。
- `.gitignore`、`.omo/evidence/task-3-repository-rebaseline-20260721.md`、`docs/Work_Log/evidence/2026-07-22/t3-formal-facade-docker.md` — 仅反忽略本次持久 Docker 证据，并修正 protected dirty baseline、记录 RED/ GREEN、Docker 环境和完整验证命令。

**验证：**
- Docker：六个 root/OS facade 与 canonical entrypoint shell contracts 均 exit 0 ✅。
- Docker development container：`make toolchain-setup`、`make toolchain-preflight` 后，RV64 → LA64 `kernel-build-only` 串行完成 ✅。
- 未运行 QEMU；编译只产生既有 Rust warning，没有新 error。

**备注：** 根因是 `PROFILE ?= normal` 和 OS orchestration 默认 ARCH 使 formal facade 无法识别省略输入。以 `$(origin ...)` 区分调用方显式参数与 Makefile 默认值；此为构建入口契约，不新增通用 debugging reference。AI-Usage-Report 已列出 Sisyphus 与 GPT-5.6-terra，无需更新。

### T3：full acceptance matrix 与 protected-dirty 复核

**涉及文件：**
- `scripts/test-make-layering-contract.sh` — 将 facade 结构断言从重复的内联 filter 改为“共享 validator 定义 + 每个 facade 调用”的等价契约。
- `.omo/evidence/task-3-repository-rebaseline-20260721.md`、`docs/Work_Log/evidence/2026-07-22/t3-formal-facade-docker.md` — 记录环境诊断、T1/T2/T3、facade/entrypoint 与八项 ktest/check 的逐条 exit status，以及前后 protected dirty 快照。

**验证：**
- Docker：T1 rebaseline isolation、完整 T2、T3 serial matrix 均 exit 0 ✅；second-stage-failure fixture 预期 exit 1 ✅。
- Docker：root build/kernel/user/image、normal run 和 expanded entrypoint contracts 均 exit 0 ✅。
- Docker：RV64/LA64 × normal/regression 的四项 `ktest-build-only` 与四项 formal `check` 均串行 exit 0 ✅。

**备注：** Compose service 未挂载 linked-worktree common Git metadata，且 bind mount 触发 Git ownership 检查；临时验证容器以同路径挂载 common Git 并只在容器内设置 `safe.directory` 后完成验收。前后 protected dirty 集合一致，未运行 QEMU。

### T3：修复 ktest 与 formal check 的隔离构建入口

**涉及文件：**
- `os/make/{rv64,la64}.mk` — 添加无 QEMU 的 `ktest-build-only` 和架构级 `check`；二者以 profile 选择的 CPIO 为 prerequisite，并向 Cargo 传入绝对 CPIO、user root 与 user mode。
- `os/Makefile` — formal `check` 委派到对应架构实现，明确传递 ARCH/PROFILE/isolated roots；RV64/LA64 ktest wrapper 也转发 profile。
- `os/build.rs` — 用 `MANGO_USER_OUTPUT_MODE` 定位用户态 payload，避免 `cargo check` 的 debug profile 与 Make 选择的 release/debug 用户输出混淆。
- `scripts/test-canonical-entrypoint-contract.sh` — 新增 RV64/LA64 × normal/regression 的 ktest/check 正向 trace 与非法 ARCH/PROFILE 反向合同。
- `.omo/evidence/task-3-repository-rebaseline-20260721.md` — 记录 Oracle 复现、Docker 命令、挂载、hash 与验证结果。

**验证：**
- Docker：T1、完整 T2、canonical graph T3 及新增 entrypoint contract 全部通过 ✅。
- Docker：RV64/LA64 的 normal/regression `ktest-build-only` 与 formal `check` 均完成编译 ✅。
- 未运行 QEMU；second-stage-failure fixture 仅按预期验证非零失败和无 publication。

**备注：** 根因是 ktest 与 formal check 未显式携带 build.rs 的隔离输入；另一个隐含问题是 Cargo check 固定使用 debug profile，不能用它推导用户产物 mode。没有新增通用 reference，AI-Usage-Report 无需更新。

### T3：通过 OUT_DIR 生成隔离 initramfs 与预载汇编

**涉及文件：**
- `os/build.rs` — 从 Make 传入的绝对 `MANGO_INITRAMFS_CPIO` 与 `MANGO_USER_OUTPUT_ROOT` 验证输入、声明 Cargo freshness，并在 `OUT_DIR` 生成安全转义的 `initramfs.S`/`preload_app.S`；缺失 CPIO 明确失败。
- `os/src/main.rs`、`os/src/preload_app{,-rv}.S` — `global_asm!` 保持 `target_arch` 选择，但只 include 生成汇编；删除不再使用且固定指向 `../user/target` 的 tracked preload stubs。
- `os/Makefile`、`os/make/{rv64,la64}.mk` — 将 profile 选定的绝对 CPIO 与 user 输出根传给 Cargo；direct build-only alias 也转发 `INITRAMFS_PROFILE=$(PROFILE)`。
- `scripts/test-normal-initramfs-source-purity-contract.sh` — 先 RED 后 GREEN，锁定 OUT_DIR include、绝对隔离输入、无 tracked `../user/target` `.incbin` 以及 regression profile 转发。
- `docs/Work_Log/evidence/2026-07-22/t3-initramfs-outdir-docker.md` — 保存 Docker 镜像、container ID、挂载映射、命令和结果摘要。

**验证：**
- Docker：T1 rebaseline isolation、T2 source-purity/linker/initramfs/layering/toolchain/purity-delta contracts 全部通过 ✅。
- Docker：T3 canonical serial matrix 通过；second-stage-failure fixture 预期 `FAIL:`/非零且未发布兼容产物 ✅。
- Docker：RV64、LA64 的 normal 与 regression 实际 kernel build，以及 clean/order/repeat/debug→release 六组串行序列全部通过 ✅。
- Docker：缺失 `MANGO_INITRAMFS_CPIO` 明确报错；未运行 QEMU。

**备注：** 根因是 tracked 汇编消费者仍假设 `../user/target`，与 T3 的 `build/<arch>/<mode>/<profile>/user` 隔离根不一致。生成汇编将选择、校验和 freshness 绑定到同一次 Make 选择；现有 Rust warning 为基线 warning。无需更新 references 或 AI-Usage-Report。

### T3：规范构建入口与隔离产物图

**涉及文件：**
- `Makefile`、`os/Makefile` — 建立 formal entrypoints，并让 `all` 串行完成 RV64、LA64 后再发布兼容产物。
- `os/make/{common,arch,rv64,la64}.mk`、`user/Makefile` — 将 kernel、user、image、initramfs、tools 产物收敛到 `build/<arch>/<mode>/<profile>`。
- `os/build_initramfs.sh`、`os/buildfs.sh` — 使用隔离的用户态 Cargo 输出作为打包输入。
- `.gitignore` — 忽略正式生成输出根 `build/`。
- `scripts/test-canonical-build-graph.sh` — 覆盖串行矩阵、四维输出隔离与 LA64 第二阶段失败不发布根兼容产物。

**验证：**
- Docker 内 `make -C os ARCH=rv64 PROFILE=normal rv64-kernel-build-only` ✅，随后 LA64 同命令 ✅。
- `sh scripts/test-canonical-build-graph.sh --matrix serial` ✅。
- `--fixture second-stage-failure` 按约定输出 `FAIL:` 并返回非零 ✅。
- `git check-ignore -v build` 确认 `build/` 由 `.gitignore` 忽略 ✅。

**备注：** 未运行 QEMU；未纳入 `.omo/boulder.json`、`os_test.conf`、`run_test.sh`、`.gdbinit`、`cc-codex/` 和 `docs/Work_Log/2026-07-19.md`。

### 加固 purity-delta 的 gitlink 与非 ASCII 路径快照

**涉及文件：**
- `scripts/test-rebaseline-purity-delta.sh` — gitlink HEAD 查询为单次 `safe.directory` 配置，兼容 Docker linked-worktree 的 ownership 检查；`git ls-files --stage` 固定 `core.quotePath=false`，使 UTF-8 tracked 路径按真实路径而非 C-style 转义文本比较。
- `docs/Work_Log/evidence/2026-07-22/purity-delta-docker.txt` — 归档临时 Docker 的 image、挂载、工具链环境、完整命令和退出状态。

**验证：**
- Docker：`sh -n scripts/test-rebaseline-purity-delta.sh` ✅
- Docker：`tracked-mutation`、`gitlink-movement` fixture 均预期输出 `FAIL:` 且 exit 1 ✅
- Docker：RV64 linker `no-board`、`dual-board` fixture 均预期输出 `FAIL:` 且 exit 1 ✅
- Docker：source-purity、normal RV64 linker、normal initramfs、make layering、toolchain 合同均 exit 0 ✅
- Docker：`sh scripts/test-rebaseline-purity-delta.sh --serial-kernel-builds` 强制 RV64 → LA64 完成；前后 allowlist/fingerprint、tracked 文件（含 UTF-8 路径）、gitlink HEAD 与配置保持不变 ✅

**备注：** 临时容器必须在宿主机同一绝对路径挂载 linked worktree 与 common `.git`，以保留 submodule 相对 gitdir；同时显式导出固定 Rustup/Cargo homes。现有 Rust warning 为基线 warning；未运行 QEMU。此模式是构建合同专用细节，未新增 references；AI 使用报告已有本会话工具和当天 Oracle 记录，无需更新。

### 完成双架构源码纯净构建契约

**涉及文件：**
- `os/make/arch/{rv64,la64}-settings.mk`、`os/make/{rv64,la64}.mk` — lwext4 CMake overlay、缓存与 archive 改为 `target/lwext4/<arch>` 声明输出；移除 normal、ktest、regression 对 linker、initramfs stub 与 lwext4 vendor 的写入。
- `scripts/test-normal-rv64-linker-source-purity-contract.sh` — 直接探测 `os/build.rs`，验证 RVQEMU-only 与 VF2-only 选择各自 linker，并为 no-board/dual-board 保留非零 `FAIL:` fixture。
- `scripts/test-source-purity-make-contract.sh`、`scripts/test-rebaseline-purity-delta.sh` — 拒绝 profile 构建配方写入 tracked 输入；串行 RV64→LA64 后比较所有 tracked 文件和 gitlink identity。

**验证：**
- Docker：`sh scripts/test-source-purity-make-contract.sh && sh scripts/test-normal-rv64-linker-source-purity-contract.sh && sh scripts/test-normal-initramfs-source-purity-contract.sh && sh scripts/test-make-layering-contract.sh && sh scripts/test-toolchain-make-contract.sh && sh scripts/test-rebaseline-purity-delta.sh --serial-kernel-builds` ✅
- Docker：`sh scripts/test-normal-rv64-linker-source-purity-contract.sh --fixture no-board` 与 `--fixture dual-board` 均输出 `FAIL:` 且 exit 1 ✅
- Docker：`make -C os rv64-kernel-build-only` 后 `make -C os la64-kernel-build-only` 串行完成 ✅

**备注：** 既有 Rust warning 为基线 warning；本任务未运行 QEMU，因此没有 QEMU evidence。该模式已由 AGENTS.md 的源码纯净约束覆盖，未新增 references 或 AI 使用报告条目。

### Reject staged rebaseline candidates

**涉及文件：**
- `scripts/test-rebaseline-isolation.sh` — 对 tracked porcelain 状态检查 index 列；除 untracked `??` 外，任何非空 index 列都在 allowlist/disposition 处理前以 `FAIL:` 拒绝。新增 `staged-allowlisted` fixture，证明 outside-rebaseline 且指纹匹配的 `M  protected.txt` 不能绕过隔离门。

**验证：**
- 修改前临时 Git fixture：allowlisted、SHA-256 匹配的 `M  protected.txt` 错误输出 `PASS:`，exit 0（RED）
- `sh -n scripts/test-rebaseline-isolation.sh` ✅
- 默认及显式 `--repo-root .` 的 rebaseline isolation happy 命令 ✅
- `--fixture staged-unowned` 与 `--fixture staged-allowlisted` 均输出 `FAIL:`，exit 1 ✅
- 临时 Git matrix：未暂存 allowlisted、untracked allowlisted、未暂存删除保持 PASS；缺失条目、畸形/非法条目、指纹变化、reconcile-before、错误 DELETED 与 rename 均保持 FAIL ✅
- `git diff --check` ✅

**备注：** 仅执行 POSIX shell 合同测试；未执行 Docker、Cargo 或 QEMU。未形成新的调试/性能经验，未更新 references 或 AI 使用报告。

### Enforce rebaseline isolation contracts

**涉及文件：**
- `scripts/test-rebaseline-isolation.sh` — 默认将仓库根目录解析为当前 Git worktree 顶层，保留显式 `--repo-root`；逐项验证 allowlist 路径，已协调的干净文件与 `HEAD` 一致，脏候选仍必须匹配 allowlist 指纹，`DELETED` 条目必须缺席；staged-unowned fixture 现在以 `FAIL:`/非零结束。

**验证：**
- `sh -n scripts/test-rebaseline-isolation.sh` ✅
- `sh scripts/test-rebaseline-isolation.sh --allowlist .omo/rebaseline-allowlist.txt --verify-fingerprints` ✅
- `sh scripts/test-rebaseline-isolation.sh --allowlist .omo/rebaseline-allowlist.txt --repo-root . --verify-fingerprints` ✅
- `sh scripts/test-rebaseline-isolation.sh --fixture staged-unowned` ✅（预期 `FAIL:`，exit 1）
- 临时 Git fixture：缺失 non-DELETED 条目、畸形条目、非法 disposition、指纹变化、`reconcile-before` 脏路径与仍存在的 `DELETED` 条目均输出 `FAIL:` 且非零 ✅
- `git diff --check` ✅

**备注：** 仅执行 POSIX shell 合同测试；未执行 Docker、Cargo 或 QEMU。未形成新的调试/性能经验，未更新 references 或 AI 使用报告。

### Correct direct OS run facade coverage

**涉及文件：**
- `scripts/test-normal-run-facade-contract.sh` — root 检查继续使用 `make -C "$repo_root"`，OS valid/invalid 矩阵改为直接执行 `make -C "$repo_root/os"`；新增基线断言，验证 OS 相对 preflight 且拒绝 root logo，防止将 `os` 误作 root Make 目标。

**验证：**
- `sh scripts/test-normal-run-facade-contract.sh .` ✅（包含 direct-OS baseline、root/OS valid/invalid 矩阵、root exactly-once、`-j8` early rejection 与 legacy `comp`）
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- root build/kernel/user/image facade contracts ✅

**备注：** 未执行 Docker、Cargo、QEMU 或 artifact-producing target；AI 使用报告的既有 Oracle 审查记录无需更新，未形成新的调试/性能经验。

### Normal-only canonical run facade

**涉及文件：**
- `Makefile`、`os/Makefile` — 新增 `validate-run` 并将 generic `run` 限定为 `ARCH=rv64|la64` 与 `PROFILE=normal`；root 显式转发，OS 仅选择对应架构既有 `run`。root `run` 以 target-scoped `.NOTPARALLEL` 串行 prerequisite，避免重复 logo/preflight。
- `scripts/test-normal-run-facade-contract.sh` — 新增 root/OS valid、invalid、`-j8` early-rejection、一次性 root setup 与 legacy `comp` dispatch 的 dry-run contract。

**验证：**
- `sh scripts/test-normal-run-facade-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- root build/kernel/user/image facade contracts ✅
- Oracle 复审 ✅：初次审查发现 root 将 logo/preflight 同时作为 prerequisite 和 recursive recipe 调用，导致各执行两次；修复后验证 RV64/LA64 均为 `logo=1`、`root_preflight=1`、`exact_dispatch=1`，非法并行输入无副作用。

**备注：** generic run 目前明确仅支持 normal；competition、regression、ktest、development、debug 保持独立 legacy/profile 路径。未执行 Docker、Cargo、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Root image facade

**涉及文件：**
- `Makefile` — 新增 phony root `image`，以 quoted `ARCH`/`PROFILE` 转发至 OS image facade；OS 层保留 `PROFILE=normal` 校验及 `fs-img` 选择。
- `scripts/test-root-image-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 normal 和缺失、非法、多值参数拒绝。

**验证：**
- `sh scripts/test-root-image-contract.sh .` ✅
- `sh scripts/test-root-user-contract.sh .` ✅
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `sh scripts/test-root-build-contract.sh .` ✅

**备注：** 未执行 Docker、镜像构建、Cargo、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Root user facade

**涉及文件：**
- `Makefile` — 将被 `user/` 目录掩盖的 root `user` target 定义为 phony，并以 quoted `ARCH`/`PROFILE` 转发到 OS facade。
- `scripts/test-root-user-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 normal 与缺失、非法、多值参数拒绝。

**验证：**
- `sh scripts/test-root-user-contract.sh .` ✅
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `sh scripts/test-root-build-contract.sh .` ✅

**备注：** 仅验证 root facade；未执行 Docker、Cargo、QEMU 或 artifact-producing target。未形成新的调试/性能经验，未更新 references。

### Root kernel facade

**涉及文件：**
- `Makefile` — root `kernel` 保留 read-only preflight，并以 quoted `ARCH`/`PROFILE` 转发到 OS facade；OS 层保持唯一参数校验权威。
- `scripts/test-root-kernel-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 × normal/regression 及缺失、非法和多值输入拒绝。

**验证：**
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `make -n ARCH=<rv64|la64> PROFILE=<normal|regression> kernel`（四种组合）✅
- `sh scripts/test-root-build-contract.sh .` ✅
- `sh scripts/test-kernel-output-root-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅

**备注：** 仅 dry-run 验证 facade；未执行 Docker、Cargo、QEMU 或 artifact-producing target。未形成新的调试/性能经验，未更新 references。

### Root build facade

**涉及文件：**
- `Makefile` — 新增无 prerequisite 的 `build` facade，仅委托 `$(MAKE) -C os all`；保留 evaluator `all` 的 setup、root preparation 与下游构建顺序。
- `scripts/test-root-build-contract.sh` — 新增独立 POSIX dry-run contract，验证 `build` 不 provision，及 `all` 在 `-j8` 下仍按 setup → preparation → OS all 执行。

**验证：**
- `sh scripts/test-root-build-contract.sh .` ✅
- `make -n build` ✅
- `make -n -j8 all` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅

**备注：** Oracle 复审要求补强 `.PHONY`、唯一 recipe 与单次委托断言；修订后复审通过。本切片只定义 root canonical facade，不执行 Docker、构建、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Kernel output-root build contract

**涉及文件：**
- `os/make/common/orchestration.mk` — 声明默认值为 `target` 的 `KERNEL_OUTPUT_ROOT`。
- `os/make/arch/{rv64,la64}-settings.mk` — 由输出根推导内核 ELF/BIN 路径，同时保持直接架构 Makefile 入口兼容。
- `os/Makefile`、`os/make/{rv64,la64}.mk` — 仅向 normal kernel Cargo build 传递 `CARGO_TARGET_DIR`，并保留 `../kernel-rv`、`../kernel-la` 稳定 alias。
- `scripts/test-kernel-output-root-contract.sh` — 新增独立的 dry-run contract，覆盖双架构 build-only 与 facade 入口。

**验证：**
- `sh scripts/test-make-layering-contract.sh .` ✅
- `sh scripts/test-kernel-output-root-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-normal-rv64-linker-source-purity-contract.sh .` ✅
- `sh scripts/test-normal-initramfs-source-purity-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- Docker: `RUSTUP_HOME=/root/.rustup CARGO_HOME=/root/.cargo make toolchain-preflight && make rv64-kernel-build-only && make la64-kernel-build-only` ✅（按 RV64 → LA64 串行）

**备注：** 未修改 user/rootfs/initramfs/lwext4 输出路径；现有 Rust warning 为基线 warning。本次未形成新的调试或性能经验，未更新 references；现有 AI Usage Report 已记录本会话使用的 Sisyphus 与 GPT-5.6-terra，无需新增工具条目。
