## 2026-07-22

### 加固 purity-delta 的 gitlink 与非 ASCII 路径快照

**涉及文件：**
- `scripts/test-rebaseline-purity-delta.sh` — gitlink HEAD 查询为单次 `safe.directory` 配置，兼容 Docker linked-worktree 的 ownership 检查；`git ls-files --stage` 固定 `core.quotePath=false`，使 UTF-8 tracked 路径按真实路径而非 C-style 转义文本比较。
- `docs/Work_Log/evidence/2026-07-22/purity-delta-docker.txt` — 归档临时 Docker 的 image、挂载、工具链环境、完整命令和退出状态。

**验证：**
- Docker：`sh -n scripts/test-rebaseline-purity-delta.sh` ✅
- Docker：`tracked-mutation`、`gitlink-movement` fixture 均预期输出 `FAIL:` 且 exit 1 ✅
- Docker：RV64 linker `no-board`、`dual-board` fixture 均预期输出 `FAIL:` 且 exit 1 ✅
- Docker：source-purity、normal RV64 linker、normal initramfs、make layering、toolchain 合同均 exit 0 ✅
- Docker：`sh scripts/test-rebaseline-purity-delta.sh --serial-kernel-builds` 强制 RV64 → LA64 完成；前后 allowlist/fingerprint、tracked 文件（含 UTF-8 路径）、gitlink HEAD 与配置保持不变 ✅

**备注：** 临时容器必须在宿主机同一绝对路径挂载 linked worktree 与 common `.git`，以保留 submodule 相对 gitdir；同时显式导出固定 Rustup/Cargo homes。现有 Rust warning 为基线 warning；未运行 QEMU。此模式是构建合同专用细节，未新增 references；AI 使用报告已有本会话工具和当天 Oracle 记录，无需更新。

### 完成双架构源码纯净构建契约

**涉及文件：**
- `os/make/arch/{rv64,la64}-settings.mk`、`os/make/{rv64,la64}.mk` — lwext4 CMake overlay、缓存与 archive 改为 `target/lwext4/<arch>` 声明输出；移除 normal、ktest、regression 对 linker、initramfs stub 与 lwext4 vendor 的写入。
- `scripts/test-normal-rv64-linker-source-purity-contract.sh` — 直接探测 `os/build.rs`，验证 RVQEMU-only 与 VF2-only 选择各自 linker，并为 no-board/dual-board 保留非零 `FAIL:` fixture。
- `scripts/test-source-purity-make-contract.sh`、`scripts/test-rebaseline-purity-delta.sh` — 拒绝 profile 构建配方写入 tracked 输入；串行 RV64→LA64 后比较所有 tracked 文件和 gitlink identity。

**验证：**
- Docker：`sh scripts/test-source-purity-make-contract.sh && sh scripts/test-normal-rv64-linker-source-purity-contract.sh && sh scripts/test-normal-initramfs-source-purity-contract.sh && sh scripts/test-make-layering-contract.sh && sh scripts/test-toolchain-make-contract.sh && sh scripts/test-rebaseline-purity-delta.sh --serial-kernel-builds` ✅
- Docker：`sh scripts/test-normal-rv64-linker-source-purity-contract.sh --fixture no-board` 与 `--fixture dual-board` 均输出 `FAIL:` 且 exit 1 ✅
- Docker：`make -C os rv64-kernel-build-only` 后 `make -C os la64-kernel-build-only` 串行完成 ✅

**备注：** 既有 Rust warning 为基线 warning；本任务未运行 QEMU，因此没有 QEMU evidence。该模式已由 AGENTS.md 的源码纯净约束覆盖，未新增 references 或 AI 使用报告条目。

### Reject staged rebaseline candidates

**涉及文件：**
- `scripts/test-rebaseline-isolation.sh` — 对 tracked porcelain 状态检查 index 列；除 untracked `??` 外，任何非空 index 列都在 allowlist/disposition 处理前以 `FAIL:` 拒绝。新增 `staged-allowlisted` fixture，证明 outside-rebaseline 且指纹匹配的 `M  protected.txt` 不能绕过隔离门。

**验证：**
- 修改前临时 Git fixture：allowlisted、SHA-256 匹配的 `M  protected.txt` 错误输出 `PASS:`，exit 0（RED）
- `sh -n scripts/test-rebaseline-isolation.sh` ✅
- 默认及显式 `--repo-root .` 的 rebaseline isolation happy 命令 ✅
- `--fixture staged-unowned` 与 `--fixture staged-allowlisted` 均输出 `FAIL:`，exit 1 ✅
- 临时 Git matrix：未暂存 allowlisted、untracked allowlisted、未暂存删除保持 PASS；缺失条目、畸形/非法条目、指纹变化、reconcile-before、错误 DELETED 与 rename 均保持 FAIL ✅
- `git diff --check` ✅

**备注：** 仅执行 POSIX shell 合同测试；未执行 Docker、Cargo 或 QEMU。未形成新的调试/性能经验，未更新 references 或 AI 使用报告。

### Enforce rebaseline isolation contracts

**涉及文件：**
- `scripts/test-rebaseline-isolation.sh` — 默认将仓库根目录解析为当前 Git worktree 顶层，保留显式 `--repo-root`；逐项验证 allowlist 路径，已协调的干净文件与 `HEAD` 一致，脏候选仍必须匹配 allowlist 指纹，`DELETED` 条目必须缺席；staged-unowned fixture 现在以 `FAIL:`/非零结束。

**验证：**
- `sh -n scripts/test-rebaseline-isolation.sh` ✅
- `sh scripts/test-rebaseline-isolation.sh --allowlist .omo/rebaseline-allowlist.txt --verify-fingerprints` ✅
- `sh scripts/test-rebaseline-isolation.sh --allowlist .omo/rebaseline-allowlist.txt --repo-root . --verify-fingerprints` ✅
- `sh scripts/test-rebaseline-isolation.sh --fixture staged-unowned` ✅（预期 `FAIL:`，exit 1）
- 临时 Git fixture：缺失 non-DELETED 条目、畸形条目、非法 disposition、指纹变化、`reconcile-before` 脏路径与仍存在的 `DELETED` 条目均输出 `FAIL:` 且非零 ✅
- `git diff --check` ✅

**备注：** 仅执行 POSIX shell 合同测试；未执行 Docker、Cargo 或 QEMU。未形成新的调试/性能经验，未更新 references 或 AI 使用报告。

### Correct direct OS run facade coverage

**涉及文件：**
- `scripts/test-normal-run-facade-contract.sh` — root 检查继续使用 `make -C "$repo_root"`，OS valid/invalid 矩阵改为直接执行 `make -C "$repo_root/os"`；新增基线断言，验证 OS 相对 preflight 且拒绝 root logo，防止将 `os` 误作 root Make 目标。

**验证：**
- `sh scripts/test-normal-run-facade-contract.sh .` ✅（包含 direct-OS baseline、root/OS valid/invalid 矩阵、root exactly-once、`-j8` early rejection 与 legacy `comp`）
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- root build/kernel/user/image facade contracts ✅

**备注：** 未执行 Docker、Cargo、QEMU 或 artifact-producing target；AI 使用报告的既有 Oracle 审查记录无需更新，未形成新的调试/性能经验。

### Normal-only canonical run facade

**涉及文件：**
- `Makefile`、`os/Makefile` — 新增 `validate-run` 并将 generic `run` 限定为 `ARCH=rv64|la64` 与 `PROFILE=normal`；root 显式转发，OS 仅选择对应架构既有 `run`。root `run` 以 target-scoped `.NOTPARALLEL` 串行 prerequisite，避免重复 logo/preflight。
- `scripts/test-normal-run-facade-contract.sh` — 新增 root/OS valid、invalid、`-j8` early-rejection、一次性 root setup 与 legacy `comp` dispatch 的 dry-run contract。

**验证：**
- `sh scripts/test-normal-run-facade-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- root build/kernel/user/image facade contracts ✅
- Oracle 复审 ✅：初次审查发现 root 将 logo/preflight 同时作为 prerequisite 和 recursive recipe 调用，导致各执行两次；修复后验证 RV64/LA64 均为 `logo=1`、`root_preflight=1`、`exact_dispatch=1`，非法并行输入无副作用。

**备注：** generic run 目前明确仅支持 normal；competition、regression、ktest、development、debug 保持独立 legacy/profile 路径。未执行 Docker、Cargo、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Root image facade

**涉及文件：**
- `Makefile` — 新增 phony root `image`，以 quoted `ARCH`/`PROFILE` 转发至 OS image facade；OS 层保留 `PROFILE=normal` 校验及 `fs-img` 选择。
- `scripts/test-root-image-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 normal 和缺失、非法、多值参数拒绝。

**验证：**
- `sh scripts/test-root-image-contract.sh .` ✅
- `sh scripts/test-root-user-contract.sh .` ✅
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `sh scripts/test-root-build-contract.sh .` ✅

**备注：** 未执行 Docker、镜像构建、Cargo、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Root user facade

**涉及文件：**
- `Makefile` — 将被 `user/` 目录掩盖的 root `user` target 定义为 phony，并以 quoted `ARCH`/`PROFILE` 转发到 OS facade。
- `scripts/test-root-user-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 normal 与缺失、非法、多值参数拒绝。

**验证：**
- `sh scripts/test-root-user-contract.sh .` ✅
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `sh scripts/test-root-build-contract.sh .` ✅

**备注：** 仅验证 root facade；未执行 Docker、Cargo、QEMU 或 artifact-producing target。未形成新的调试/性能经验，未更新 references。

### Root kernel facade

**涉及文件：**
- `Makefile` — root `kernel` 保留 read-only preflight，并以 quoted `ARCH`/`PROFILE` 转发到 OS facade；OS 层保持唯一参数校验权威。
- `scripts/test-root-kernel-contract.sh` — 新增 dry-run matrix，覆盖 RV64/LA64 × normal/regression 及缺失、非法和多值输入拒绝。

**验证：**
- `sh scripts/test-root-kernel-contract.sh .` ✅
- `make -n ARCH=<rv64|la64> PROFILE=<normal|regression> kernel`（四种组合）✅
- `sh scripts/test-root-build-contract.sh .` ✅
- `sh scripts/test-kernel-output-root-contract.sh .` ✅
- `sh scripts/test-make-layering-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅

**备注：** 仅 dry-run 验证 facade；未执行 Docker、Cargo、QEMU 或 artifact-producing target。未形成新的调试/性能经验，未更新 references。

### Root build facade

**涉及文件：**
- `Makefile` — 新增无 prerequisite 的 `build` facade，仅委托 `$(MAKE) -C os all`；保留 evaluator `all` 的 setup、root preparation 与下游构建顺序。
- `scripts/test-root-build-contract.sh` — 新增独立 POSIX dry-run contract，验证 `build` 不 provision，及 `all` 在 `-j8` 下仍按 setup → preparation → OS all 执行。

**验证：**
- `sh scripts/test-root-build-contract.sh .` ✅
- `make -n build` ✅
- `make -n -j8 all` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅

**备注：** Oracle 复审要求补强 `.PHONY`、唯一 recipe 与单次委托断言；修订后复审通过。本切片只定义 root canonical facade，不执行 Docker、构建、QEMU 或 artifact-producing target；未形成新的调试/性能经验，未更新 references。

### Kernel output-root build contract

**涉及文件：**
- `os/make/common/orchestration.mk` — 声明默认值为 `target` 的 `KERNEL_OUTPUT_ROOT`。
- `os/make/arch/{rv64,la64}-settings.mk` — 由输出根推导内核 ELF/BIN 路径，同时保持直接架构 Makefile 入口兼容。
- `os/Makefile`、`os/make/{rv64,la64}.mk` — 仅向 normal kernel Cargo build 传递 `CARGO_TARGET_DIR`，并保留 `../kernel-rv`、`../kernel-la` 稳定 alias。
- `scripts/test-kernel-output-root-contract.sh` — 新增独立的 dry-run contract，覆盖双架构 build-only 与 facade 入口。

**验证：**
- `sh scripts/test-make-layering-contract.sh .` ✅
- `sh scripts/test-kernel-output-root-contract.sh .` ✅
- `sh scripts/test-source-purity-make-contract.sh .` ✅
- `sh scripts/test-normal-rv64-linker-source-purity-contract.sh .` ✅
- `sh scripts/test-normal-initramfs-source-purity-contract.sh .` ✅
- `sh scripts/test-toolchain-make-contract.sh .` ✅
- Docker: `RUSTUP_HOME=/root/.rustup CARGO_HOME=/root/.cargo make toolchain-preflight && make rv64-kernel-build-only && make la64-kernel-build-only` ✅（按 RV64 → LA64 串行）

**备注：** 未修改 user/rootfs/initramfs/lwext4 输出路径；现有 Rust warning 为基线 warning。本次未形成新的调试或性能经验，未更新 references；现有 AI Usage Report 已记录本会话使用的 Sisyphus 与 GPT-5.6-terra，无需新增工具条目。
