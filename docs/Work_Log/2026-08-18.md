---
date: 2026-08-18
timezone: Asia/Shanghai
schema: work-log-v2
---

# Work Log — 2026-08-18

<a id="2026-08-18-legacy-buddy-abe"></a>
## legacy free-list buddy 对照实验：allocator 是当前性能提升的主导贡献者

**Date:** 2026-08-18 +08:00
**Author:** AI:Sisyphus | Human:pxy
**Tags:** [area:mm, area:heap, area:performance, type:measurement, type:validation]

**结果：** 用 `legacy_buddy_heap` feature 把全局堆切回**最原始的 free-list 线性扫描
buddy**（`buddy_system_allocator::Heap<32>`，无 bitmap / 无 metadata / 无 slab，新增
`os/src/mm/heap_allocator_legacy.rs`），内核其它部分完全不变，同 workload（basic+busybox
pre + 全量 lmbench，4 窗口，rv64）复测。退化**立即复现**：`Process fork+execve`
5,147→**7,512 (+46%，逐窗口单调)**、`Pipe latency` 664→**930 (+40%，单调)**、
`fork+exit` 4,786→6,909 (+44%)；而 `null syscall`/`stat`/`read`/`write` 两类后端均平稳
（读取类免疫，与原报告一致）。证明**当前的全面提升主要来自 allocator 优化本身**，
9.2 的 metadata+slab 数据不是「内核其它优化」造成的表观差距。

**涉及文件：**
- `os/Cargo.toml` — 新增 `legacy_buddy_heap = []` feature（默认关闭）。
- `os/src/mm/heap_allocator_legacy.rs` — 新增独立 legacy 全局堆后端（`Heap<32>` +
  `add_to_heap` 挂 runtime，无 slab），含 GlobalAlloc/alloc_error_handler/统计等同名 API。
- `os/src/mm/mod.rs` — legacy feature 开启时用 re-export 伪模块顶替 `heap_allocator`，
  默认路径零改动。
- `docs/09_debug/buddy-allocator-scan-drift.md` — 第 9 节新增 9.4 对照实验表格与结论。
- `docs/Work_Log/evidence/2026-08-18/drift-lmbench-rv64-legacy-freelist.log` — 完整日志。

**验证：**
- 默认（无 feature）与 legacy（`EXTRA_FEATURES=legacy_buddy_heap`）双 build 均 ✅。
- legacy GlobalAlloc 首版有 double-lock 死锁（GlobalAlloc 内先 `self.heap.lock()` 又调
  内部 `self.alloc()` 再次 lock，首分配即挂）——已改为单次持锁内联操作后正常启动。
- legacy 启动自检：256 次 4K alloc/dealloc 全页对齐、无损坏。
- 8 窗口（musl W0–W3 + glibc W0–W3）复测完整跑完，数据见 9.4。

**关键教训（derived 盘累积状态污染对照）：** 实验初期多次 `conf-inject`（每次
`e2fsck -fy` + debugfs）会在 derived 盘上累积 metadata 状态，导致 `vda`（ext4 root）
挂载报 `another_ext4 BRIDGE EIO`——盘本身 `e2fsck -n` clean，且**连默认 metadata 内核
都稳定挂载失败**（一度误判为 legacy allocator 独有 bug）。**从 official 重新派生 + 一次性
注入 conf 后挂载恢复**。凡做 allocator/性能 A/B 前，必须重新生成干净 derived。

**Mango Gate：** `mango-workflow loaded=yes`；references=`harness-patterns.md`
（性能 A/B 前验证运行环境/镜像新鲜度，排除盘面状态污染后再归因 allocator）。

<a id="2026-08-18-drift-remeasure-metadata-heap"></a>
## drift 复测：metadata buddy+slab 下 lmbench 扫描退化验证 + drift 配置解析补全

**Date:** 2026-08-18 +08:00
**Author:** AI:Sisyphus | Human:pxy
**Tags:** [area:mm, area:heap, area:performance, type:measurement, type:validation]

**结果：** 用与原 `docs/09_debug/buddy-allocator-scan-drift.md` 完全相同的 workload
（basic+busybox pre-workload + 全量 lmbench，4 窗口，QEMU rv64）在**当前 metadata
buddy + slab 分配器**（`dependency/buddy_system_allocator` 的 `MetadataHeap`）下复测。
8 个窗口（musl 4 + glibc 4）数据全部采集：`Simple open/close` 117.6→126.0 µs、
`Process fork+exit` 4,140→4,111 µs、null syscall ~20 µs、`Simple stat` 92.7→93.2 µs，
**无任何窗口间单调退化**，且相比原报告 bitmap guard 修复后数据全面快约 1.7–2.5×。

**涉及文件：**
- `user/src/bin/test_runner/config/parse.rs` — 补全 `drift_windows` / `drift_measure`
  两个配置键的解析（此前只解析 `drift_pre_mask`，导致 `drift_measure=full` 始终被忽略，
  drift 模式实际只跑 `lat_syscall null`）。修复后 drift 全量 lmbench 测量恢复。
- `docs/09_debug/buddy-allocator-scan-drift.md` — 标题下新增现状更新标注
  （当前已是 metadata buddy+slab，bitmap guard 为历史实现），文末追加第 9 节
  「现状复核（2026-08-18）」：复测配置、musl/glibc 各 4 窗口完整数据表、与原报告对比。
- `os_test.conf` — 临时切换为 drift_window 复测配置（运行后已恢复原 CI 基线）；原配置
  备份于 `os_test.conf.bak-drift`。
- `docs/Work_Log/evidence/2026-08-18/drift-lmbench-rv64-8window.log` — 完整 QEMU 串口日志（305 KB）。

**验证：**
- Docker 容器 `mango-pxy-os-dev-1`（为当前仓库独立起，避开 `/home/lzm/projects/MangoCore`
  的既有 `mangocore-os-dev-1` 容器）内：`make toolchain-setup` → `make build ARCH=rv64
  PROFILE=normal` ✅ → `make -C os conf-inject CONF_ARCH=rv64 CONF_BLK_MODE=virt
  CONF_FILE=../os_test.conf` 注入 drift 配置到 `sdcard-rv-derived.img` ✅ →
  `make rv64-derived-run`，QEMU 完整跑完 8 窗口（musl W0–W3 + glibc W0–W3）✅。
- 原始 lmbench 行如 `Simple open/close: 117.6170 microseconds` 逐窗口正确输出，无卡死。

**备注：** 运行收尾有 `ext4_another BRIDGE EIO` 磁盘后端关闭警告（数据全部采集后的
已知良性 shutdown 行为）。`drift_libc=musl` 本次未生效（`parse.rs` 当时只补了
`drift_windows`/`drift_measure`，`drift_libc` 仍为默认 both），因此 glibc 4 窗口作为
免费补充数据一并记录；musl 部分即与原报告完全对齐。未暂存、未提交。

**Mango Gate：** `mango-workflow loaded=yes`；references=`harness-patterns.md`
（渐进退化复测先确认基础设施完整——drift 配置键解析缺失会让测量静默降级为 null，
复测 workload 前必须先验证 `drift_measure=full` 真正生效）。
<a id="2026-08-18-2k1000-ftp-ssd-upload"></a>
## 2K1000LA 上板脚本 FTP 写入 SSD

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, area:net, area:fs, type:feature]

**结果：** `scripts/boot_2k1000_tftp.py` 新增 `--ftp-upload`/`--ftp-destination`，完成
现有 TFTP 启动后自动进入 MangoCore Shell，探测 `eth0` 地址，在宿主机启动一次性 HTTP
服务，由板端 BusyBox `wget` 把文件拉取到隐藏临时路径；板端核对文件长度和 SHA-256 后才
改名到最终 SSD P4 路径并执行 `sync`。`ftp-upload` 名称保留兼容旧调用，但不再启动板端
FTP 服务；失败不会覆盖已有目标文件。

**涉及文件：**
- `scripts/boot_2k1000_tftp.py` — FTP 上传参数、Shell 自动化、临时文件校验/提交和交互向导。
- `scripts/test-init-lifecycle-contract.sh` — 固化 HTTP 拉取入口、`--upload-only` 和 SHA-256 校验合同。
- `docs/07_driver/2k1000-gmac.md` — 增加 HTTP 写入 SSD 用法、路径和网络边界说明。

**验证：** 宿主 Python 3.9 与 Docker `os-dev` 内 `py_compile`（缓存定向到临时目录）、CLI
`--help`、FTP 目标路径边界检查、ANSI Shell 提示符识别、AST/控制流静态检查、生命周期合同
和目标文件 `git diff --check` 均通过。未执行真实 2K1000 传输，需在带 `mainline` Shell 和
已挂载 `/persist` 的实板上验收。

**备注：** `--ftp-destination` 硬限制在 `/persist`，默认端口 `2121`，默认自动探测板端
`eth0` 地址；可用 `--ftp-board-ip` 覆盖。上传采用宿主机临时 HTTP 服务和板端 BusyBox
`wget`，不依赖板端 FTP 支持。

**Mango Gate：** `mango-workflow reused`；references=`none — 纯工具功能`；无 `.rs` 修改，
已同步 2k1000 GMAC 与完整测试磁盘文档；未发现需沉淀的新通用模式。

<a id="2026-08-18-2k1000-p3-command-p4-apk-bridge"></a>
## 2K1000LA P3 用户态入口与 P4 APK 桥接

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, area:fs, area:userspace, type:feature]

**结果：** P3 根布局不再把 Python 入口链接到旧 `/tools`，改由 `/tests/cpython` 的私有
musl closure 启动；curl 由 P3 内私有 glibc closure 启动。rcS 自动尝试挂载已验证的 P4 到
`/persist`，只补齐缺失的 APK state/repository/keys 目录，不清空已有数据库或 `world`。
首次联网 Alpine 安装保持为显式 `mango-apk-bootstrap`，以确保离线启动不会被包仓库阻塞。

**验证：** 生命周期合同覆盖 P3 launcher、P4 mount/marker 合同；候选 P3 镜像将通过
ext4 镜像级路径和 symlink 检查后才进入实板写入流程。

**Mango Gate：** `mango-workflow reused`；references=`debugging-patterns.md`；P3/P4 文档同步中。

<a id="2026-08-18-vscode-rust-analyzer-settings"></a>
## 补齐 VSCode rust-analyzer JSON 配置

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:tooling, type:debugging]

**结果：** `.vscode/settings.json` 现同时链接根 workspace、`os` 和 `user` 两个独立
Cargo 项目；默认使用 RV64 target 及当前有效的 `riscv`、`block_virt`、`oom_handler`
features。关闭保存时自动 Cargo check，避免宿主机绕过 Docker 执行内核构建，同时保留
rust-analyzer 诊断/proc-macro 支持并排除大型产物目录。

**验证：** JSON 解析、RV64 配置下 `os`/`user` 两个 linked project 的 Cargo metadata
和 `git diff --check` 均通过。无 `.rs` 修改，不触发 `code_paths` 文档同步。

**Mango Gate：** `mango-workflow reused`；references=`debugging-patterns.md`；
未发现需要新增的可复用模式。

<a id="2026-08-18-rust-analyzer-toolchain-repair"></a>
## 修复 VSCode rust-analyzer 工具链缺失

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:toolchain, type:debugging, type:tooling]

**结果：** 根目录 `rust-toolchain.toml` 要求的 `nightly-2026-05-10` 在宿主机
Rustup 中只有不完整目录，缺少 manifest，导致 rust-analyzer 执行 `cargo metadata`
失败。已卸载损坏安装并重新安装同一 nightly、`rust-src`、`llvm-tools-preview`、
`clippy` 及 RV64/LA64 目标。

**验证：** `cargo -V`、`rustc -Vv`、组件/目标清单和根目录
`cargo metadata --format-version 1 --manifest-path Cargo.toml --no-deps` 均通过。
未修改源码；没有 `.rs` 文件变更，不触发 `code_paths` 文档同步。

**Mango Gate：** `mango-workflow loaded`；references=`debugging-patterns.md`；
未发现需要新增的可复用模式。

<a id="2026-08-18-2k1000-ssd-serial-monitor"></a>
## 2K1000LA SSD 启动串口监听模式

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, type:tooling, type:safety]

**结果：** 一键上板向导新增 `monitor（仅打开串口，可交互）`，非交互入口为 `--monitor-only`。
该路径不要求镜像，不编译、不配置网络/TFTP，也不主动进入 U-Boot prompt 或发送中断启动
字符；它显示并记录板端输出，同时把用户的手工键盘输入转发给开发板，供 SSD A 槽启动后
操作 shell。`Ctrl-] q` 仅在宿主关闭 monitor，不影响开发板运行。

**验证：** Python compile、CLI help、生命周期静态合同和相关 diff whitespace 检查通过。

**Mango Gate：** `mango-workflow reused`；references=`debugging-patterns.md`；板级磁盘文档已同步。

<a id="2026-08-18-2k1000-local-root-p4-recovery"></a>
## 2K1000LA 本地持久根、自动启动与 P4 恢复

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, area:block, area:fs, area:net, type:debugging, type:validation]

**结果：** P3 已重建为无 journal 的完整持久根，包含 BusyBox `/sbin/init`、inittab、rcS、
基础 `/etc` 和 `/boot/kernel-A.ui`。U-Boot 已保存“P3 A 槽 → TFTP → 原 bootcmd”三级启动，
保留 3 秒调试截停窗口；本地启动和 TFTP 调试均已实板验证。旧 P4 的 `apk-root` I/O error
定位为 ext4 extent 元数据损坏，已用受限的原位替换模式重建为干净无 journal P4。

**根因与安全边界：** 首版 P3 由宿主默认 ext4 参数生成，带 journal；首轮可启动，但硬复位后
当前 another_ext4 不能 replay journal，停在后端初始化。生成器现强制 `^has_journal`。
P4 原始 SATA 块读取稳定，而损坏 inode 的 extent 指向 physical block 0，因此不归因于 SSD
硬件。旧 P4 内容只得到两份约 64 MiB 的部分取证 tar，随后按授权重建；不能将其表述为完整备份。

**实板证据：** 最终 uImage 为 `16,639,472` B，SHA-256
`1fd7fef817e161f8ecfdc6cb378ae504d05e9d185c9607173b6cf870cf1ed33c`，U-Boot CRC32
`e32f750f`。P3 写入 `/root/persist-v2`、sync、硬复位并本地自动启动后，SHA-256
`193bc0922d3a50bf1cef5eab4baf065c8f85bc4625340f8edf9c3c6fe7b7be5a` 不变。P4 以 16 个
256 MiB 块写入并逐块读回一致，MBR CRC32 保持 `6538e5cb`；`apk-root` 和状态哨兵可读，
RW 写入 `REBUILD_OK`、sync、卸载、RO 重挂后读回成功。GMAC DHCP 获得 `192.168.2.2/24`，
网关 `192.168.2.1` 三次 ping 全通；公网 `1.1.1.1` 不通属于当前上游/桥接边界。

**回归：** `/tests/fs_test` 通过 74/76，核心 1–72 和 proc mount 均通过；仅两个性能项因
fixture 缺 `/tmp/bb` 失败。Unix socketpair 的 DGRAM/STREAM 通过；named STREAM connect
返回 `-111` 并使父进程停在 accept，是独立待修回归。旧 `la64-2k1000-core-tests` 目标仍请求
已删除 feature `sata_scratch_rw`，未通过恢复废弃 feature 绕过。

**宿主门禁：** Docker 内 `make toolchain-preflight`、Python `py_compile` 和
`scripts/test-init-lifecycle-contract.sh` 通过；P3/P4 均由 e2fsck 完整五阶段只读检查为 clean，
`dumpe2fs` 确认两者 feature 均不含 `has_journal`。P3/P4 镜像 SHA-256 分别为
`b533f9cf80144926b97d53aa75a1444c4d35e42054cce242c0bbd278c37899f6` 和
`ff8bceeb48efa3968bd8df2e30284a773f971872dc7d3116226e4361dc2c298e`；`git diff --check` 通过。

**涉及文件：** P3/P4 生成与受限写入脚本、本地启动配置脚本、LoongArch 主线 GMAC/DHCP
构建入口、P3 `/etc` fixture、生命周期合同，以及 FS/AHCI/工作日志文档。

**Mango Gate：** `mango-workflow reused`；references=`debugging-patterns.md`（新增“先区分
inode/extent 元数据损坏与介质读失败，再决定取证或重建”的可复用诊断模式）；文档已同步。

<a id="2026-08-18-2k1000-one-click-ssd-kernel-install"></a>
## 2K1000LA 一键安装 SSD 内核 A 槽

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, area:block, type:feature, type:safety]

**结果：** 一键上板脚本新增显式 `ssd-install`/`--install-ssd-kernel` 模式，可把编译或
指定的 LoongArch uImage 固化为 SSD P3 `/boot/kernel-A.ui`。该入口不会尝试未经验证的
ext4 单文件原地修改，而是在 Docker 中重建完整 768 MiB P3，再复用受限写入器分三块更新；
交互和非交互路径均强制要求 P4 完整备份 ID 与 `0xA80800` 固定 LBA 确认。

**涉及文件：**

- `scripts/boot_2k1000_tftp.py` — 新增 SSD 安装模式、交互向导选项、Docker P3 生成编排和写入器参数传递。
- `scripts/write_2k1000_p3.py` — 写前绑定 P3 manifest 与所选内核 SHA-256，写后从 SSD 重载 A 槽并校验长度、CRC、架构和 uImage checksum。
- `scripts/test-init-lifecycle-contract.sh` — 锁定安装入口、备份/边界确认和内核验证合同。
- `docs/03_fs/2k1000-full-test-disk.md` — 记录交互/非交互用法、完整 P3 覆盖语义和 TFTP 调试保留路径。

**验证：**

- `python3 -m py_compile scripts/boot_2k1000_tftp.py scripts/write_2k1000_p3.py scripts/make_2k1000_tools_partition.py` ✅
- `bash scripts/test-init-lifecycle-contract.sh` ✅
- `--help` 静态入口检查与 `git diff --check` ✅
- 对现有 768 MiB P3 payload 执行 manifest/SHA-256 绑定校验，确认其
  `kernel-A.ui` 与所选 mainline uImage 一致 ✅
- 错误 LBA 的 fail-closed 探针在镜像、Docker、串口访问前退出 ✅

**边界：** 本工作包未占用串口、未执行 SCSI write，也未改变 U-Boot 环境；真实写盘仍须
使用已经完成并可从 P4 读取的 P3 备份。安装会替换完整 P3，持久业务数据应放在 P4
`/persist` 或先行备份。未引入新的 AI 工具，不需更新 `AI-Usage-Report.md`；没有 `.rs`
修改，不触发 `code_paths` 同步。可复用的“整分区写入必须有精确 LBA 与可验证回滚点”规则
已存在于 `debugging-patterns.md`，本次不重复沉淀。

**Mango Gate：** `mango-workflow loaded=yes`；references=`debugging-patterns.md`。

<a id="2026-08-18-defense-script-5min"></a>
## 决赛答辩 PPT 五分钟讲话稿

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:docs, type:presentation]

**结果：** 逐页检查《答辩初稿4.pptx》的 19 页内容、演讲者备注与
Keynote 渲染结果，将原约 7 分 40 秒的讲述压缩为约 4 分 47 秒的逐页讲话稿。
概况、增量和测试分层等罗列页只保留总结句，章节页直接略过，把主要时间留给
Buddy 退化的现象、观测、根因、修复以及 BuildStorm 优化证据链。

**涉及文件：**
- `docs/10_plan/答辩初稿4-5分钟讲话稿.md` — 新增带页码、时长、略过标记和现场提示的可直接照读稿。

**验证：**
- 静态检查 PPTX 的 19 页正文与演讲者备注。
- 通过 Keynote 临时导出 19 张 PNG 并逐页视觉核对；未修改原 PPTX。
- 稿件标注累计时长 4:47，为换页与现场停顿预留约 13 秒。

**Mango Gate：** `mango-workflow loaded=yes`；references=`none — 纯文稿整理`；
未修改代码，无需编译、QEMU 或 `code_paths` 文档同步，不需经验沉淀。

<a id="2026-08-18-2k1000-mainline-persistent-root-pass"></a>
## 2K1000LA 默认持久根实板闭环

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:loongarch64, area:board, area:block, area:fs, type:debugging, type:validation]

**结果：** `la64-2k1000-mainline` 已在真实 2K1000LA 上把 SATA P3
`/dev/sda3` 作为默认可写根，PID1 从 initramfs 完成运行期挂载绑定后 chroot，并执行
P3 的静态 `busybox sh -i`。写入标记、`sync`、立即复读、硬件 RESET、重新 TFTP 启动和
第二次只读复核全部通过。

**从首轮失败到最终布局：**

- 修复前 panic 位于 `SataBlock::new -> pci_init().expect("AHCI new failed")`。恢复固定
  `00:08.0`、DMW2 MMIO、BAR/CAP/PI、连续低端 DMA 和错误传播后，实板识别
  `0014:7a08`、BAR0 `0x400e0000`、SSD `TS32GMTS400/S0322B`、`62533296` sectors。
- 首轮 `root=/dev/sda1` 成功挂载但 PID1 拒绝切根；盘面确认 P1 只有官方
  `glibc/musl/os_test.conf` 测试载荷。P4 虽有 `apk-root`，读取其 `bin/sbin` 返回 I/O
  error，未将可疑卷提升为默认根。P3 包含可执行 `bin/busybox`、bash、运行库和工具，故
  mainline 明确改用 `root=/dev/sda3`。
- P3 的 `/bin/sh -> bash` 能被 exec，但未进入可交互状态。最终 PID1 在 init/initproc
  之后显式执行 `/bin/busybox sh -i`，再保留 bash 作为更后的兜底；根完整性检查也要求
  BusyBox 可执行。

**实板证据：**

- 最终 uImage：`16,643,360` B，SHA-256
  `fd2504c76a9b4cfa6d16b36c5db83ac0c4cbbba8a0587804f8a0fa280efda7cf`；TFTP 长度一致，
  CRC32 `82690790`，两轮 `iminfo` 和 payload checksum 均通过。
- 两轮均输出 `[sata] AHCI ready`、`sda probe: 4 partitions`、
  `[ext4] backend: another_ext4`、`root device mounted at sdcard`、
  `mainline persistent SATA root active` 和 `mainline exec /bin/busybox sh -i`。
- 首轮执行：
  `echo MANGO_P3_PERSIST_20260818_V1 > /MANGO_PERSIST_DEFAULT`、`/bin/busybox sync`，随后
  `cat` 得到相同内容。
- 硬件 RESET 后重新从同一镜像启动，未改写文件，`cat /MANGO_PERSIST_DEFAULT` 再次得到
  `MANGO_P3_PERSIST_20260818_V1`，输出 `__PERSIST_REBOOT_READ_DONE__`。标记文件保留在
  P3 根目录作为可人工复核的验收证据。
- 串口原始日志位于本次宿主会话的
  `/private/tmp/mango-2k1000-mainline-busybox.log` 和
  `/private/tmp/mango-2k1000-mainline-persist-reboot.log`；后者 SHA-256
  `8ae66853f1f33015848414418829b713724bfcd01dde18c46b4094882bfe40d6`。

**代码与门禁：** SATA `BlockDevice` 发布可靠 flush，partition 透传该能力，默认
another_ext4 在可写 open 时拒绝无可靠 flush 的设备；生命周期静态合同锁定 P3 selector、
another_ext4、SATA flush 和 BusyBox PID1 兜底。Docker 中 RV64→LA64 normal 串行构建、
LA64 mainline uImage、bootargs 32/32、another_ext4 journal 34/34、orphan 13/13、flush 9/9
和生命周期合同均通过。another_ext4 全库仍有一个与本工作包无关的既有 direct-range
阈值断言失败（141/142），未扩大修改。

**Mango Gate：** `mango-workflow loaded=yes/reused`；references=
`debugging-patterns.md`（板级驱动合并回归按通用核心/板级适配器分层追溯）；驱动、启动、
根文件系统和测试文档已同步。

<a id="2026-08-18-vf2-cli-net-param-and-board-validation"></a>
## VF2 上板验证（新环境 bring-up）+ vf2_cli.py 地址参数化

**Date:** 2026-08-18 +08:00
**Author:** AI + 人工上板
**Tags:** [area:board, area:testing, area:build, type:validation, type:refactor]
**Mango Gate:** skill loaded=yes；references=debugging-patterns.md（docker/tftp/NetworkManager 环境坑）

**Summary:** 切换到 develop（8a48ef88）并在新笔记本环境完成 VF2 实机上板：
regression 镜像 `[L4 REGRESSION RESULT: PASS]`，28 项中 27 passed / 0 failed /
1 skipped（外网不可达）。同时将 `scripts/vf2_cli.py` 的 TFTP 地址从硬编码改为
参数化 + `--auto-net` 自动探测，适配笔记本移动网络场景。

**Files:**
- `scripts/vf2_cli.py` — 新增 `--board-ip`/`--server-ip`/`--auto-net` 参数；
  `resolve_net()` 解析优先级：显式 flag > 自动探测（板子 `printenv ipaddr` +
  宿主机有 carrier 的以太网卡 IP）> 模块默认；`_ensure_network`/`_run_tftp_load`
  参数化；新增 `_host_link_ip()`/`_board_ipaddr()`/`_same_subnet()`。

**Verification:**
- develop 内核构建：Docker `zhouzhouyi/os-contest:20260510` 内
  `make kernel ARCH=rv64 PROFILE=regression` ✅（3m06s，Image 6.19MB）。
- VF2 实机上板：`vf2_cli.py boot kernel-rv-image`；`[L4 REGRESSION RESULT: PASS]`，
  27/28（`inet_test` 外网 SKIP），FS 76/76，unix_test 14/14；测试后 SBI SRST
  自动重启回 U-Boot ✅。transcript: `/tmp/opencode/vf2-boot.log`。

**备注（新环境 bring-up 经验）：**
- 工具链：20260510 镜像内 rustup 组件记录与实际文件不同步（rustc 可用但
  rust-src/riscv64 target/clippy 缺失）→ `rustup toolchain uninstall` 后
  `install --profile complete` + `target add` 解决；`rustup-setup.sh` 要求显式
  RUSTUP_HOME（compose exec 需 `-e RUSTUP_HOME=/root/.rustup`）。
- 网络：docker 163 镜像源失效会导致 pull 卡死（curl 探测源可用性定位）；
  NetworkManager 会清掉手动 `ip addr add` 的静态 IP（改走
  `nmcli connection modify ... ipv4.method manual` 固化）；板子 U-Boot 残留旧
  网段 ipaddr 无碍（脚本 `_ensure_network` 每次 setenv 覆盖）。
- TFTP 服务为 socket 激活，inactive 时需 `systemctl start tftpd.socket`；
  ufw 需放行 69/udp。
<a id="2026-08-18-2k1000-upload-only"></a>
## 2K1000LA 已运行系统直接上传模式

**Date:** 2026-08-18 +08:00
**Author:** AI:Codex:GPT-5 | Human:lzm
**Tags:** [area:board, area:tools, type:debugging]

**结果：** `scripts/boot_2k1000_tftp.py` 新增 `--upload-only`（别名
`--ftp-upload-only`）。板子已从 SSD 启动并停在 P3 shell 时，脚本只配置宿主网卡、
串口连接和临时 HTTP 服务，由板端 BusyBox `wget` 拉取文件到 `/persist`；跳过内核
编译、TFTP、U-Boot 命令和复位。原有 `--ftp-upload` 启动后上传流程保持兼容。

**补充修复：** 实板 `upload-only` 在 `/bin/busybox mkdir -p /persist` 停住；P4 已经挂载时
该无意义的挂载点创建会进入当前 MountFS/ext4 的阻塞路径。脚本现在对 `/persist` 根目录
直接跳过 `mkdir -p`，只有嵌套目标目录才创建。

**验证：** `python3 -m py_compile scripts/boot_2k1000_tftp.py`、帮助文本检查、
`git diff --check` 通过；修复后尚未进行实板上传。

**Mango Gate：** `mango-workflow reused`；references=`debugging-patterns.md`。
