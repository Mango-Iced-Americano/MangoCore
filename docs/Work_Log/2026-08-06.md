## 2026-08-06

### 将 develop 集成结果并入最新 smp 主开发分支

**涉及文件：**

- 以 `origin/smp@952dddcc` 为第一父基线，合入
  `codex/smp-develop-integration@eb2d7e53`，保留双方提交历史。
- FS/Net 源码、focused ktest 和对应架构文档的冲突统一采用集成分支中基于更晚
  `smp-fs-net` 修订且已经验证的版本，避免把旧锁协议与新协议逐块拼接。
- 保留 `origin/smp` 独有的 FS/Net 计划、执行台账、验收工作日志、网络接口文档补充，
  并把双方可复用的 debugging pattern 合并为并列条目。
- `docs/06_net/architecture.md` — 根据只读审查补充 normal 启动中
  `PID1 -> [net-poll] worker -> run_tasks()` 的发布顺序和原因。

**验证：**

- 合并后源码与构建配置相对 `eb2d7e53` 无差异；新增差异仅为上述文档和历史记录。
- DeepSeek max effort 本地只读审查 ✅：未发现启动顺序、PageCache/ext4/PortRegistry
  锁协议、focused 测试注册或文档语义方面的合并阻塞项；工作树指纹保持不变。
- Docker `CORE_NUM=8` RV64/LA64 normal kernel build ✅/✅（153.569 s / 175.592 s）。
- Docker `CORE_NUM=8 KTEST=smp` RV64/LA64 ✅/✅（150.617 s / 147.454 s）。
- Docker `CORE_NUM=8 KTEST=fs_smp` RV64/LA64 ✅/✅（141.417 s / 140.674 s）。
- Docker `CORE_NUM=8 KTEST=net_smp` RV64/LA64 ✅/✅（150.237 s / 148.487 s）。
- 六次 QEMU focused 门禁均确认 `online_mask=0xff`、`KTEST RESULT: PASS`，无
  panic、fatal trap、FAIL 或 runner source mutation；`git diff --check` ✅。

**备注：**

- 新 Docker 容器首次预检因未 provisioning `nightly-2026-05-10` 拒绝执行；按项目合同
  运行 `make toolchain-setup` 后恢复。第二次构建在内核 Cargo 阶段前发现新 worktree 的
  `buddy_system_allocator` submodule 未初始化；执行 `git submodule update --init` 后恢复。
  两项均归类为环境准备，不是源码回归，原始失败 job 仍保存在本地 `cc-codex`。
- 完整初赛矩阵未重复运行：最终生产源码与此前已经通过双架构 8 核 `mask=0x003` 的
  `eb2d7e53` 相同，本轮以双架构编译和三套冲突相关 focused ktest 验证合并完整性。

**证据：** `docs/Work_Log/evidence/2026-08-06/smp-final-merge-summary.md`

### BuildStorm x0 chroot 启动 profile

**涉及文件：**

- `user/src/bin/init.rs`、`user/src/syscall.rs` — 识别 `profile=buildstorm`，只挂载 x0 到 `/sdcard`，准备并 bind `/proc`、`/sys`、`/dev`、`/tmp` 后执行 chroot；发布全核 `sched_setaffinity`，优先执行官方 `/glibc/buildstorm_testcode.sh`，再回退 `/sbin/init`、`/init`、`/bin/sh`，不绑定 tools 盘。
- `os/Makefile`、`os/make/{rv64,la64,qemu-profiles}.mk`、根 `Makefile` — 增加 `buildstorm` QEMU 入口，默认 `CORE_NUM=8`、`QEMU_MEMORY=8G`，使用独立 buildstorm 输出目录避免 Cargo profile 缓存串线，并将 `profile=buildstorm` 编入内核启动参数。
- `os/src/fs/procfs/files/cmdline.rs` — `/proc/cmdline` 改为返回实际内核 bootargs，使 PID1 能识别 BuildStorm profile。
- `os/src/fs/sysfs/files/mod.rs` — 发布 `/sys/devices/system/cpu/{online,present,possible}` 的 8 核范围，供 `nproc` 与兼容脚本读取。
- `docs/01_architecture/initialization-flow.md`、`docs/03_fs/init-and-rootfs.md` — 同步启动拓扑与 tools 隔离约束。

**验证：**

- Docker toolchain preflight ✅；RV64/LA64 `make kernel PROFILE=normal CORE_NUM=8 QEMU_MEMORY=8G` 串行编译 ✅/✅。
- 8c/8G QEMU 兼容阶梯启动到 initproc，确认 `online_mask=0xff`、x0/tools 双盘挂载和 LTP 执行；45 秒超时为有界 smoke 截断，非构建失败。
- 使用 `/home/lzm/projects/MangoCore-smp-final-merge-20260806/sdcard-rv-pub.img(1).gz` 与 `sdcard-la-pub.img(1).gz` 官方 x0：RV64/LA64 均在 8c/8G 下确认 bind 四个伪文件系统、进入 `/sdcard` chroot，且 tools 盘未被挂载到根文件系统。
- 官方 BuildStorm 脚本已从 RV64/LA64 两架构启动并输出 `BUILDSTORM_TOOLCHAIN ok`；两架构日志均确认 `sched_setaffinity(all) enabled`，LA64 进一步观测到 `BUILDSTORM_COMPILE ... cores=8`。RV64 在 mini-build 阶段即报告失败并停止有界会话；LA64/已观测的 compile 阶段均暴露 Rust/libgit2 的 OpenSSL entropy 初始化失败（`entropy source strength too weak`），因此未宣称全阶梯通过。
- `rustfmt --check user/src/bin/init.rs`、`git diff --check` ✅。

**备注：**

- 复用现有 `debugging-patterns.md` 的挂载/chroot 与伪文件系统边界经验，本轮不新增 reference。

### 将 QEMU DHCP 迁移到可响应中断的 CPU0 poll worker

**涉及文件：**

- `os/src/net/config.rs` — 删除 QEMU 启动期 5 秒同步 DHCP 循环，改为保留
  `dhcpv4::Socket` 并在目录发布后预置首轮 poll 请求；CPU0 worker 的真实
  smoltcp 扫描放入受控 IRQ-on 窗口，退出窗口且释放网络锁后再进入
  任务安全点。
- `docs/06_net/{README,architecture,device-stack-and-poll,dhcp,debugging}.md` — 同步
  常驻 DHCP 租约、首轮 pending 消费、worker IRQ 窗口和故障诊断语义；明确
  VirtIO 发送路径轮询 used ring，临时开中断是为了保持 timer/IPI 可响应，
  不把 completion 误写成依赖 hard IRQ handler。

**验证：**

- DeepSeek 以只读、max effort 模式串行执行 Docker 门禁并复核锁边界；
  RV64/LA64 `CORE_NUM=8` normal kernel build ✅/✅（138.97 s / 145.53 s）。
- Docker `CORE_NUM=8 mask=0x003` RV64/LA64 初赛回归 ✅/✅（383.99 s /
  386.16 s）；两者均为 exit 0、无 forbidden marker、无 source mutation，且确认
  `online_mask=0xff`、DHCP `deferred -> configured`、basic/busybox 四组结束标记与
  `run_selected_groups done`。
- Docker `rustfmt --check src/net/config.rs` ✅；`git diff --check` ✅。QEMU 门禁后对
  Rust 源文件只收紧了注释描述，其余变化仅为文档，没有修改已验证的语义。

**备注：**

- 未迁移队友分支中的 CPU0-only VirtIO 断言；双架构 syscall 本就在 IRQ-on
  窗口执行，AP 上的 `poll_now()` 仍可以安全做单次有界扫描。
- 未引入新 HAL `irq_enabled()` API、raw-egress 队列或默认开启的热路径计数器。
  现有 `debugging-patterns.md` 已覆盖“硬中断只发布、任务上下文消费”与锁外
  安全点边界，本轮不重复新增 reference。

### FAT32 簇表变更事务化并补齐双架构 8 核一致性测试

**涉及文件：**

- `os/src/fs/fat32/bitmap.rs` — 把空闲缓存与扫描游标合并进卷级
  `FatMutationState`；allocation 按“候选簇先写 EOC、再链接前驱”发布，free 按
  “保留尾先断链、再 tail-first 写 FREE”回收，完整覆盖一个 FAT 变更事务。
- `os/src/kernel_tests/{fs_fat_smp,probe,mem_block}.rs`、
  `os/src/kernel_tests/ext4/block_device.rs`、`os/src/kernel_tests/mod.rs` — 复用内存块设备，
  在 ktest 内构造合法 67,000 数据簇 FAT32 卷；CPU1/CPU2 用户探针经真实
  `ftruncate` syscall 并发执行整链/后缀 alloc-free，重挂载后检查环、双归属、越界和
  不可达已分配簇。

**验证：**

- DeepSeek 只读审查确认卷级锁覆盖搜索、占用发布、链指针更新和回收缓存，
  `dealloc_clus()` 的 tail-first 输入与 `Fat::free()` 约定一致。
- Docker `CORE_NUM=8` RV64/LA64 normal kernel build ✅/✅（140 s / 145 s）。
- Docker `CORE_NUM=8 KTEST=fs_fat_smp` RV64/LA64 focused ktest ✅/✅（152 s / 148 s）；
  两者均为 1 passed、0 skipped、0 failed，启动日志确认 `online_mask=0xff`。
- `rustfmt --check`（五个本批 Rust 文件）✅；`git diff --check` ✅。focused 门禁后仅按
  rustfmt 建议调整了一处常量表达式换行，没有语义改动，未重复运行 QEMU。

**备注：**

- 首版探针每轮附带 `unlinkat`，LA64 暴露目录删除失败；该操作不经过本批
  `Fat::alloc/free` 临界区。最终测试收窄为同一 inode 上反复 truncate，避免把目录项
  生命周期噪声混入簇表事务验收；LA64 FAT unlink 另作独立问题处理。
- 测试卷完全由 ktest 内存构造；没有放宽生产 BPB 校验、增加测试专用生产接口、
  修改 initramfs 打包脚本或携带外部 FAT 镜像。
- 已检查 FAT32 与 ktest 文档组织；本批没有改变生产挂载入口和磁盘布局，无需同步
  `docs/03_fs/2k1000-full-test-disk.md`。现有调试参考已覆盖“先隔离失败层级再收窄测试”
  的方法，本轮不重复新增 reference。

### 收口整页物理内存引用并实现 per-CPU CPU 时间账户

**涉及文件：**

- `os/src/mm/address.rs` — 删除安全返回 `&'static mut [u8]` 的
  `get_bytes_array()`，以 unsafe scoped `with_bytes[_mut]()` 将 slice 生命期限制在单次调用中。
- `os/src/mm/{address_space,filemap,vma,frame_store}.rs`、`os/src/fs/{ramfs,vfs/file}.rs`、
  `os/src/syscall/process/{clone,ids}.rs` — 按 frame 是否已发布区分 scoped slice 与 raw
  copy；filemap/VMA 初始页改为先填充未发布 frame、再安装 PTE，并增加只针对
  无 PTE frame 的失败回滚。
- `os/src/task/{processor,task}.rs`、`os/src/hal/arch/{riscv,loongarch64}/trap/mod.rs` —
  在 `CpuTaskState` 内累加 user/system/idle 微秒时间；trap return 采用“安全点前结算
  system、恢复汇编前开启 user”的两段边界，idle 快照以 sequence 协议包含未闭合区间。
- `os/src/fs/procfs/files/stat.rs` — 将内部微秒按 Linux `USER_HZ=100` 转换，输出
  aggregate 和每个 configured `cpuN` 的 user/system/idle；暂无独立账户的字段继续为 0。
- `docs/04_mm/address-space-and-vma.md`、`docs/05_process/{scheduler,time-sched-rlimit}.md`、
  `docs/10_plan/smp-8core-implementation.md` — 同步内存发布顺序、计时边界、idle 口径和
  SMP 实施状态。

**验证：**

- `git diff --check` ✅；全仓库 `get_bytes_array(` 与旧
  `update_process_times_leave_trap` 调用扫描均为空。
- DeepSeek 先执行只读并发/接口审查，确认安全 `'static mut` 整页通道为高风险缺口，
  并建议 per-CPU user/system/idle 与 `USER_HZ=100` 输出口径。
- Docker `CORE_NUM=8` RV64 normal kernel build ✅（137 s）；LA64 normal kernel build ✅
  （140 s）。两次 exit 0，构建前后 tracked diff 指纹均为
  `90b1c99d5ad338ca5d61faa2f45b5338415b08cdc8de9f15475288086dd78da0`，无 mutation。
- Docker `CORE_NUM=8` RV64/LA64 CPU-time QEMU gate ✅/✅；两者 exit 0、无 panic/
  timeout/forbidden marker，覆盖 process/thread clock、`getrusage` 和 `times`。
- Docker `CORE_NUM=8 mask=0x003` RV64 初赛回归在中间源码上四组 marker 完整、
  `online_mask=0xff`；最后的 trap 尾段计时调整之后未重跑，因此只记为参考，不作为冻结源码
  的初赛验收证据。LA64 同轮用例运行完成且 exit 0，但执行期间发生后续收尾修改，
  Runner 按证据纪律标记 mutation，同样不计为最终 PASS。

**备注：**

- 当前 idle 表示 CPU 执行本地 idle 调度上下文的时间，包含 CPU0 在该上下文内
  完成的 housekeeping；它不是纯硬件 halt 时间。
- `/proc/stat` 的 CPU 行拓扑已有双架构 8 核回归，本轮新增时间数值未专门从
  procfs 文本回读；`ctxt/intr`、nice/iowait/IRQ 分类和 `/proc/[pid]/stat` 的分项口径仍是
  后续独立任务。
- 本轮没有新建生产源文件，也没有添加仅供测试的生产字段。现有
  `debugging-patterns.md` 已覆盖 SMP uaccess 的 frame 存活与映射同步边界，
  因此不重复新增 reference 条目。

### 溯源 LA64 8 核全量测试卡在 lmbench

**涉及文件：**

- 未修改生产代码；新增诊断证据
  `evidence/2026-08-06/smp-full-8g/la64-lmbench-hang-root-cause.md`。

**验证：**

- 全量日志确认 LA64 停在 `lmbench-glibc` 首个 syscall 延迟输出后；QEMU 进程持续
  高 CPU 且无 panic、timeout 或 group-end marker。
- 同内核、同官方 LA64 glibc 二进制的 focused 8 核/8 GiB 对照中，默认校准的
  `lat_syscall read` 可返回；完整 musl lmbench 后继续执行 glibc null/read 也可返回，
  排除 `/dev/zero`、read wrapper 和 lmbench calibration 的稳定阻塞。
- 对卡住的 QEMU 进行多轮 GDB 快照：CPU4/CPU5 固定在 `__alltraps` 保存指令，
  `$ra=0x802c5af0` 定位到 `AddressSpace::activate_on()` 的 ASID rollover 返回点；
  CPU4 的 `ESTAT.ECODE=2` 且 `BADV` 位于 LA64 内核栈顶，结合 CPU5 在同一
  prologue 和栈 guard 上的后续异常，证明正在发生递归 Page Invalid Store。

**备注：**

- 根因是 LA64 `trap_return()` 过早安装 user EENTRY，而后续 ASID rollover 和
  TLB shootdown ack 等待会临时开中断；`__restore` 尚未把 TrapContext 写入
  `CSR_SAVE` 时若收到中断，内核异常会误进用户 trap prologue，并把内核栈指针
  当作 TrapContext 写爆到 guard page。
- 这是初始 LA64 trap-return 排序与后续 SMP ASID/TLB 同步语义组合出的竞态；
  lmbench 只是在长序列耗尽 ASID 后把它触发出来。当前状态为 `red`，本轮只诊断、
  未实施修复，也未终止仍用于保留现场的 QEMU。

### 核对 RV64 8 核/8 GiB 全量运行结果

**涉及文件：**

- 未修改生产代码；复核既有
  `evidence/2026-08-06/smp-full-8g/qemu-rv64.log`。

**验证：**

- RV64 从 16:55:14 运行到 18:19:58，共 5,084 秒（84 分 44 秒）。basic、
  busybox、lua、lmbench、iozone、libcbench、netperf、iperf、libctest 和
  cyclictest 的 musl/glibc 组均出现结束 marker。
- `ltp-musl` 执行至第 922 个用例 `removexattr01` 时，内核在
  `ext4_xattr_set_entry()` 第 327 行触发 `LoadPageFault`，坏地址为 0；没有
  `ltp-musl` 结束 marker，也没有进入 `ltp-glibc`。
- 外层记录的 `QEMU_RC=0` 来自 panic shutdown 路径，不能覆盖日志中的 kernel
  panic；本次 RV64 全量判定为 `red/partial`，不是 PASS。
- DeepSeek 以只读、max-effort 模式独立追踪 syscall、VFS/lwext4 wrapper 和 C
  finder 控制流，定位到 `ext4_xattr_remove()` else 分支把已初始化的
  `ibody_finder.s` 误写成未初始化的 `block_finder.s`。工作树 status/diff 指纹在
  审查前后保持一致，模型未修改或提交仓库文件。
- GPT/Codex 对照官方 LTP `removexattr01`（先 set 一个小 `user.test`，再 remove）、
  本地反汇编和 lwext4 upstream master `58bcf89a` 复核：测试必然命中 inode-body
  分支，而崩溃指令正是首次解引用错误 finder 的 `s->first`，核心定因成立。

**备注：**

- 崩溃指令 `0x8043edd4` 是对错误 finder 的 `s->first` 首次解引用；直接原因和
  上游根因均已定位，但尚未修改代码或运行 focused GREEN 验证。
- DeepSeek 报告的核心结论可靠，但附录曾把 `use_block` 条件写成“ibody 找到且有
  block”，实际应是“ibody 未找到、存在 block 且 block 内找到”。报告还漏掉了
  “属性不存在且无 xattr block”应返回 `ENODATA` 的相邻语义缺口；因此后续修复不能
  机械照抄报告后就直接宣称 removexattr 语义完整。
- 现有 harness reference 已明确“退出码必须与 panic/完成 marker 联合判定”，
  本轮不重复新增经验条目。

### 修复 RV64 removexattr 崩溃与 LA64 ASID rollover trap-return 竞态

**涉及文件：**

- `dependency/lwext4_rust/c/lwext4/src/ext4_xattr.c` — inode-body 删除路径改用
  已初始化的 `ibody_finder.s`；inode body 与 external block 均不存在时返回
  `ENODATA`。
- `os/src/hal/arch/loongarch64/trap/{mod.rs,trap.S}` — Rust 返回阶段保持 kernel
  EENTRY；用户入口作为 `$a3` 传入，在 `CSR.SAVE` 已指向 TrapContext 且 IRQ 仍关闭
  后由 `__restore` 安装。
- `docs/01_architecture/{hal-and-platform,loongarch64-platform,trap-and-syscall-entry}.md`
  — 同步四参数恢复 ABI 与 EENTRY 交接时序。

**验证：**

- DeepSeek 在 Docker 中严格串行执行 `CORE_NUM=8` normal kernel build：RV64/LA64
  均 exit 0（148 s / 141 s），完整日志归档。
- RV64 `-smp 8 -m 8G` 精确执行 LTP `removexattr01`：启动为
  `online_mask=0xff`，musl/glibc 各 1 TPASS、0 failed，原 panic 未复现。
- LA64 release ELF 反汇编确认 `CSR.SAVE` 写入先于用户 EENTRY，后者又先于 GPR
  恢复和 `ertn`。LA64 `-smp 8 -m 8G KTEST=smp` 中
  `asid_rollover_flushes_before_reuse` PASS，未出现 rollover TLB timeout 或递归
  kernel trap fault。
- LA64 SMP 整组为 33/34、`KTEST RESULT: FAIL`；唯一失败是独立的
  `running_affinity_waits_for_owner_handoff` owner 检查，本轮不掩盖、不越界修复。
- DeepSeek max-effort 只读复核三条 xattr 分支、`csrwr` 副作用、`$a0..$a3`
  ABI 和 EENTRY/SAVE/ERTN 时序，报告无阻塞 finding；GPT 接受核心结论并纠正其把
  affinity 失败直接称为历史问题、把 test 24 预期 PageModifyFault 称为用户错误的表述。
- `git diff --check` ✅。

**备注：**

- 首轮 focused 配方曾因内核仍按默认 `CORE_NUM=1` 构建而作废；第二轮又暴露
  inline runner 的固定目录窗口没有枚举到该用例；第三轮 suite 配置缺少
  `ltp_suites` 被 fail-closed 拒绝。最终证据使用新鲜 8 核产物和
  `ltp_suites=syscalls`，不把三次空跑计为测试通过。
- 本轮终止了已完成 GDB 取证、持续卡死三小时的旧 LA64 QEMU，释放派生镜像后才
  执行 GREEN 回归。
- 完整 LA64 `0xFFF` 前置序列与 external-block/missing-xattr 动态专项尚未重跑，
  本轮总状态为 `partial`；细节见
  `evidence/2026-08-06/smp-rv-la-root-fixes/summary.md`。
