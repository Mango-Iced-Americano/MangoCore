## 2026-08-02

### Linux 风格设备树 model 与 VisionFive 2 SD-root PID1 交接

**涉及文件：**
- `os/src/fs/procfs/files/{mod,device_tree}.rs` — 注册只读 `/proc/device-tree/model`，内容直接来自不可变 `PlatformInfo.model`（无 model 时为空）。
- `user/src/{syscall,usr_call}.rs` — 增加 syscall 51 的 `chroot()` 用户态封装。
- `user/src/bin/init/{vf2,mounts}.rs`、`user/src/bin/init.rs` — 仅精确匹配 `starfive` 与 `visionfive` token 的 PID1 分支：确认 `/sdcard` 含 `/bin` 或 `/etc`，将 `/proc`、`/sys`、`/dev`、`/dev/shm`、`/run`、`/tmp` bind 到 SD 根，`chdir`→`chroot`→`chdir`，随后执行 `/sbin/init`，失败回退 `/bin/sh` 和既有 rescue loop。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）`make toolchain-preflight` ✅。
- `make kernel ARCH=rv64 PROFILE=normal` ✅、`make kernel ARCH=la64 PROFILE=normal` ✅（严格串行；在最终模块拆分后复跑）。
- `make build ARCH=rv64 PROFILE=normal` ✅（含用户态 init）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device` ✅（10 passed、0 failed）。
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed、0 failed、1 known skip）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal` 运行至既有的 test 30 后 `HEAP ALLOCATION FAILED`；未改动 PageCache/MM，未在本轮处理。
- 正常 RV64 QEMU 启动观察到原有 `[init] pseudo-fs mount framework ready`、持久 `/tmp` bind 和 `[init] PID1 profile=normal runner=/test-runner`，无 `VF2` 日志；后续复跑因前次限时 QEMU 残留占用 rootfs 写锁，在 guest 启动前被拒绝，已清理残留进程。

**备注：** 未运行实板。`docs/03_fs/2k1000-full-test-disk.md` 已是 draft 且只记录 2K1000LA 固定 MBR 镜像，不描述此 VF2 专用路径；历史调试文档同理，无需改写。该分支的预期实板日志顺序为 model 识别、SD 根检查、六个 bind-mount、chroot、`/sbin/init`（失败时 `/bin/sh`）。

### 实板验收：GPT 分区发布 + ext4 挂载完整链路

**验证（VisionFive 2 实板）：**
- `/dev/mmcblk0` 与 `/dev/mmcblk0p1` 均出现在 devfs。
- `dd if=/dev/mmcblk0p1 bs=1080 count=1` 读出 ext4 magic `0xEF53`（偏移 1080），确认发布的是真实 ext4 分区（LBA 2048）而非 GPT 头。
- `mount -t ext4 /dev/mmcblk0p1 /mnt` **SUCCESS**（不再 EROFS，也不再 EINVAL）。
- `ls /mnt` 显示完整文件系统（`bin/boot/dev/etc/home/lib/musl/usr/...`）。

**验证命令：**
```sh
ls /dev/mmcblk0*            # 检查设备节点发布
dd if=/dev/mmcblk0p1 bs=1080 count=1 2>/dev/null | od -A d -t x1 | head -1   # 检查 ext4 魔数 0xEF53
mount -t ext4 /dev/mmcblk0p1 /mnt && ls /mnt
```

**涉及 commit：** `ed104df6` 覆盖 GPT 分区解析 + dw-mshc reliable flush 修复；配合此前 naming/role/root= 链（`8e7e3113`、`396b84f2`）。

**备注：** 至此块设备规范化四步重构在实板闭环：驱动命名（vd*/mmcblk*）、通用分区扫描、root= 选择器、GPT 保护性 MBR 识别与 ext4 挂载全链路验证通过。

### dw-mshc 声明可靠 flush：修复 VF2 实板 ext4 挂载 EROFS

**根因：** `os/src/fs/ext4_another/fs.rs` `Ext4FileSystem::open` 检查 `block_device.supports_reliable_flush()`；dw-mshc 驱动未实现 `supports_reliable_flush()`/`flush()`，BlockDevice trait 默认值（`false`/`FlushUnsupported`）导致 ext4 挂载返回 EROFS（"Read-only file system"）。QEMU virtio-blk 实现两者为 true，故 QEMU 下 ext4 挂载正常。

**修复：** dw-mshc 写路径本身可靠——`write_sector`（mmio.rs）发 CMD24+DAT_WR 后 `wait_card_ready`（CMD13 轮询）确认卡就绪才返回，写入已落介质；板上写自检（末块写+读回）PASS。因此：
- `fn supports_reliable_flush(&self) -> bool { true }` — 返回的写即持久。
- `fn flush(&self) -> BlockDeviceResult { Ok(()) }` — 无宿主写缓存需排空，no-op 成功。

`PartitionBlockDevice` 委托父设备 flush，故 `mmcblk0p1` 自动生效。

**涉及文件：**
- `os/src/drivers/block/dw_mshc.rs` — BlockDevice impl 新增 `flush()`/`supports_reliable_flush()`；未改动 mmio.rs/sd.rs 数据路径、jh7110.rs、partition.rs、ext4_another。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）`make kernel ARCH=rv64 PROFILE=normal` ✅。
- `make kernel ARCH=la64 PROFILE=normal` ✅（严格串行）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device` ✅（10 passed、0 failed，含 `partition_flush_error_propagates`/`partition_rejects_unsupported_flush`）。
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed、0 failed、1 known skip）。

**备注：** 未跑实板（由 orchestrator 部署）；下一板启动 `mount -t ext4 /dev/mmcblk0p1 /mnt` 应不再 EROFS。Roadmap 文档 `docs/10_plan/vf2-fdt-dw-mmc-roadmap.md` 未涉及 flush 语义，无需同步。

### GPT 分区表扫描与保护 MBR 过滤

**涉及文件：**
- `os/src/drivers/block/partition.rs` — 检测保护性 `0xEE` MBR 后读取 GPT 头和跨 4KiB 块的分区数组；发布有效 GPT 分区，并在 GPT 头无效时过滤保护性条目。
- `os/src/kernel_tests/block_device.rs` — 新增合成 GPT 盘测试（LBA 2048、2048 扇区）及无效 GPT 头的保护 MBR 回退测试。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）：`docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=rv64 PROFILE=normal"` ✅。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=la64 PROFILE=normal"` ✅（严格串行）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device"` ✅（10 passed、0 failed）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression"` ✅（22 passed、0 failed、1 skip）。

**备注：** GPT 条目使用 512B LBA；真实 VisionFive 2 卡的首分区将以 `mmcblk0p1` 发布并指向 LBA 2048。已检查引用 `partition.rs` 的镜像文档：其描述的专用 2K1000LA 镜像明确为 MBR，仍与实现一致；无需改写。当前 Rust LSP 未安装且此前已拒绝安装。

### root= 命令行选择器 + mango.tools 移除

**涉及文件：**
- `libs/mango-kernel-core/src/bootargs.rs` — `BootConfig` 新增 `root_from_cmdline` 标志；`root=` 优先于 `mango.root=`，两者命中时均置显式标记。
- `libs/mango-kernel-core/src/bootargs_tests.rs` — bootargs 解析测试（root 优先级、显式 `root=initramfs`、默认未显式选择）。
- `os/src/main.rs` — 删除平台默认 root 覆写，不再以 `default_root()` 覆盖解析后的命令行配置。
- `os/src/fs/boot_block.rs` — `mount_boot_block_devices` 显式 root 语义：显式 `root=initramfs` 保留 initramfs（跳过 `/sdcard`）；其他非空显式 root 经 `resolve_block_device`（剥除单个 `/dev/` 前缀、拒绝空/`.`/`..`）解析并挂载，解析不到或挂载失败只打印错误、不回退；未显式提供 root 时回退首原始块设备（`get_block_device(0)`）。
- `docs/03_fs/init-and-rootfs.md` — 同步 root 选择器语义与驱动命名模型。
- 用户决定移除 `mango.tools=`/`tools` 字段：内核只发布 `/dev` 节点，挂载 `/tools` 由 initramfs 用户态按需执行（对齐 Linux 语义）；`os/src/` 与 `libs/` 无 `mango.tools`/`mount_tools_disk` 残留。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）：`cargo test -p mango-kernel-core` ✅。
- `make kernel ARCH=rv64 PROFILE=normal` ✅、`make kernel ARCH=la64 PROFILE=normal` ✅（严格串行）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device` ✅（8 passed、0 failed）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=dw_mshc` ✅（5 passed、0 failed）。
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed、0 failed、1 skip）。

**备注：** 此为块设备规范化四步重构的 Step 3。显式 root 选择器解析不到设备或设备无可挂载文件系统时，不执行首盘回退，保持 initramfs 可用（对调试与 CI 友好）。

### 块设备驱动声明命名风格并移除探测顺序角色

**涉及文件：**
- `os/src/drivers/block/{block_dev,descriptor,mod}.rs` — 新增驱动声明的字母/十进制命名风格，按风格独立计数；删除 `BlockDeviceRole`、角色查询及空盘 panic，空盘回退为 `DummyBlockDevice`。
- `os/src/drivers/block/{virtio_blk,virtio_blk_pci,dw_mshc}.rs` — virtio 声明 `vd` 字母序列，DesignWare MMC 声明 `mmcblk` 十进制序列。
- `os/src/fs/{boot_block,mod}.rs` — 对所有原始盘扫描 MBR；本阶段将首原始盘挂载到 `/sdcard`（`root=initramfs` 时跳过），将首分区挂载到 `/tools`，并移除旧的角色导出。
- `os/src/kernel_tests/{block_device,block_publication}.rs`、`os/src/kernel_tests/ext4_another/{mapped_overwrite,ownership,fixtures,sync}.rs`、`os/src/kernel_tests/ext4_another_lifetime.rs` — 覆盖 `vda`/`vdb`/`mmcblk0` 及分区分隔符；原角色测试改用首原始盘。
- `docs/03_fs/init-and-rootfs.md`、`docs/10_plan/vf2-fdt-dw-mmc-roadmap.md` — 同步临时挂载选择与驱动命名模型。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）`make toolchain-preflight` ✅（`nightly-2026-05-10`）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=rv64 PROFILE=normal"` ✅。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=la64 PROFILE=normal"` ✅（严格串行）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device` ✅（5 passed、0 failed，含命名/分区测试）。
- `make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=dw_mshc` ✅（5 passed、0 failed）。
- `make test ARCH=rv64 PROFILE=regression` ✅（22 passed、0 failed、1 known skip）；零盘 QEMU 未因 `BLOCK_DEVICE` 初始化 panic。

**备注：** 此为四步重构的 Step 1；未实现 `root=` 或 `mango.tools=` 命令行选择器。Rust LSP 未安装且此前已拒绝安装；容器固定工具链也未安装 `rustfmt`。未发现需要沉淀的通用调试经验；`AI-Usage-Report.md` 已登记当前模型，无需修改。

### VF2 reboot 与非 4KiB 对齐 MBR 分区修复

**涉及文件：**
- `os/src/syscall/process/misc.rs` — `sys_reboot` 在权限和命令校验后，对 restart/restart2/poweroff 调用 `finish_test_run()`；其余已支持命令仍为 no-op 成功返回。
- `os/src/drivers/block/partition.rs` — MBR 探测仅保留空项、扩展分区和磁盘范围过滤；分区设备为非 4KiB 起点使用跨父块读改写，保留精确的 512B-LBA 起点。
- `os/src/kernel_tests/block_device.rs` — 覆盖非 4KiB 对齐分区接受、越界拒绝以及非对齐起点数据偏移。

**验证：**
- Docker `mangocore-os-dev-1`（`/root/projects/MangoCore -> /app`）`make toolchain-preflight` ✅（`nightly-2026-05-10`）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=rv64 PROFILE=normal"` ✅。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make kernel ARCH=la64 PROFILE=normal"` ✅（严格串行）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make -C os ktest-run ARCH=rv64 PROFILE=normal KTEST=block_device"` ✅（8 passed、0 failed）。
- `docker exec mangocore-os-dev-1 bash -c "cd /app && make test ARCH=rv64 PROFILE=regression"` ✅（22 passed、0 failed、1 skip）。

**备注：** `finish_test_run()` 在实板路径输出 `[exit] rebooting to firmware (real board)` 后调用既有 watchdog reboot；QEMU 保持 SBI shutdown。未改动命名/角色/root 选择器、Makefile、测试配置或 VF2 驱动路径；本次模式已由 AGENTS.md 的块层语义约束覆盖，无需新增 reference，现有模块文档无需改写。
