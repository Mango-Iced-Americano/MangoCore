## 2026-07-20

### lang-items source-purity 回归防护加固

**涉及文件：**
- `scripts/test-source-purity-make-contract.sh` — 递归、排序扫描第一方 `Makefile`、`GNUmakefile` 与 `*.mk`（排除 `.git`、`dependency` 与未跟踪的 `user/tools` 下载缓存），并以隔离 fixture 覆盖 plain、`-@cp`、`@ cp`、`$(CP)`、`/bin/cp` 和带引号路径。
- `.github/workflows/ci-main.yml`、`.github/workflows/ci-develop.yml` — 在 active `toolchain-contracts` job 中执行 source-purity 合同。
- `docs/01_architecture/module-map.md` — 更新文档元数据日期。

**验证：**
- `sh scripts/test-source-purity-make-contract.sh` ✅：真实工作树无复制，fixture 中每一种规避写法均被拒绝，并输出 `file:line`。
- Docker（`c238e449081e4c68d07b193d7e7e7c357406503eaab22305cc763a4d9c2e1161`，`/home/pxy/projects/MangoCore-cleanup -> /app`）在显式 Rustup/Cargo homes 下完成 preflight、RV64、随后 LA64 build；六个 lang-items 文件 SHA-256 前后不变。
- 完整命令、fixture 覆盖、容器元数据及 hash 见 `docs/Work_Log/evidence/2026-07-20/39-lang-items-source-purity-hardening.md`。未运行 QEMU。

**备注：** 活跃 CI 尚未在本地触发；该静态合同不执行 Docker 构建或 QEMU。没有新增 reusable reference；`AI-Usage-Report.md` 无需更新。

### 删除遗留 lang-items 复制配方

**涉及文件：**
- `os/Makefile` — 删除 debug、run、regression、gdb、注入、ktest 与 fast-check 等入口中共 33 条将 `.rv`/`.la` 复制到 tracked active `lang_items.rs` 的命令。
- `scripts/test-source-purity-make-contract.sh` — 将检查从 normal OS target 扩展到全部 `os/Makefile` recipe，覆盖 `@cp`、分号命令与反斜杠续行。
- `AGENTS.md`、`README.md`、`CLAUDE.md`、`how-to-run.md`、`docs/01_architecture/module-map.md` — 同步为 `target_arch` + `#[path]` 编译期选择变体的实际行为。

**验证：**
- RED：扩展后的 source-purity 合同在清理前以 exit 1 报告 33 条 legacy copy 命令。
- Docker（`c238e449081e4c68d07b193d7e7e7c357406503eaab22305cc763a4d9c2e1161`，`/home/pxy/projects/MangoCore-cleanup -> /app`）：显式 `RUSTUP_HOME=/root/.rustup CARGO_HOME=/root/.cargo` 下，`sh scripts/test-source-purity-make-contract.sh`、`make -C os toolchain-preflight`、`make -C os rv64-kernel-build-only`、随后 `make -C os la64-kernel-build-only` 均通过。
- six-file SHA-256 前后完全一致；完整命令、hash 和容器元数据见 `docs/Work_Log/evidence/2026-07-20/38-lang-items-source-purity.md`。未运行 QEMU。

**备注：** active `os/src/lang_items.rs` 与 `user/src/lang_items.rs` 及全部变体文件均未编辑或删除。没有新增可复用 reference；`AI-Usage-Report.md` 无需更新。

### Root-only provisioning final remediation and review

**处置结果：**
- 完成全部模块的 provisioning 扫描、缺失/空 HOME、部分 HOME 和完整 HOME 覆盖矩阵，以及 `make -j` 顺序合同检查。
- 保留 root-only provisioning 边界：只有根目录 `make all` 可自动 setup 和 preflight，lower-level 入口仍为 preflight-only。
- 验证首次安装后立即重复执行的 fast path，确认重复执行不重新安装。
- 三套 shell 合同均通过：toolchain Make 合同、rustup setup 合同、rustup preflight 合同。
- `ci-main.yml` 和 `ci-develop.yml` 均运行这三套合同作业；`docs/08_testing` 元数据已更新。

**证据：**
- `docs/Work_Log/evidence/2026-07-20/32-toolchain-make-contract.log`
- `docs/Work_Log/evidence/2026-07-20/33-rustup-setup-contract.log`
- `docs/Work_Log/evidence/2026-07-20/34-rustup-preflight-contract.log`
- `docs/Work_Log/evidence/2026-07-20/35-ci-toolchain-contract.log`
- `docs/Work_Log/evidence/2026-07-20/36-toolchain-contract-review.log`
- `docs/Work_Log/evidence/2026-07-20/37-toolchain-contract-evidence-freshness.log`

**复审：** code-quality re-review PASS，individual security review PASS。未声明 QEMU 通过。本次不需要新增 reusable reference，也不需要更新 `AI-Usage-Report.md`。

### Root-only provisioning contract audit hardening

**涉及文件：**
- `scripts/test-toolchain-make-contract.sh` — 动态扫描全部第一方 Make/module（排除 `dependency/`），拒绝直接 `rustup toolchain install` 与非 root `all` 的 setup 路径，并执行 HOME 覆盖和 `make -j all` 顺序合同。
- `scripts/test-rustup-setup.sh` — 覆盖首次安装后立即重复执行时的 preflight-only 快路径。
- `.github/workflows/ci-main.yml`、`.github/workflows/ci-develop.yml` — 在两个活动工作流中执行三套 Rustup/toolchain shell 合同。
- `docs/08_testing/README.md` — 更新 testing 文档元数据日期。

**验证：**
- `sh scripts/test-toolchain-make-contract.sh` ✅（含动态 Make/module 扫描、缺失/空 HOME 默认/部分/完整覆盖和 `make -j 8 all` 顺序）
- `sh scripts/test-rustup-setup.sh` ✅（含 install-then-immediate-repeat）
- `sh scripts/test-rustup-preflight.sh` ✅
- 原始 shell 合同输出：`docs/Work_Log/evidence/2026-07-20/32-toolchain-make-contract.log`、`33-rustup-setup-contract.log`、`34-rustup-preflight-contract.log`

**备注：**生产 Make 行为已正确，本轮为审查发现的覆盖与 CI 审计缺口加固，未执行 Docker、真实 Rustup、构建或 QEMU。

### 根目录 `make all` 自动 provisioning 工具链

**涉及文件：**
- `Makefile` — 由根目录评测入口派生对应的 `RUSTUP_HOME` 与 `CARGO_HOME`，在需要时允许自动执行工具链 setup 和 preflight。
- `scripts/rustup-setup.sh` — 保留 `/root/.cargo/bin/rustup`，并在空的 `/root/.rustup` 下安装固定 nightly 工具链。
- `scripts/test-toolchain-make-contract.sh` — 锁定 root-only 自动 provisioning 与 lower-level preflight-only 边界。
- `scripts/test-rustup-setup.sh` — 锁定 rustup setup 的目录、安装和重复执行合同。
- `AGENTS.md`、`README.md`、`CLAUDE.md`、`how-to-run.md`、`docs/08_testing/README.md` — 同步工具链入口、容器验证和使用约束。

**验证：**
- 相关 shell 合同完成 RED→GREEN，合同脚本通过；没有把自动安装行为扩展到所有 direct target。
- 有效的全新 Rustup Docker 验证从空的 `/root/.rustup` 开始，由 root `make all` provision `nightly-2026-05-10`，同时保留 `/root/.cargo/bin/rustup`。RV64 先于 LA64 串行通过，两个 kernel 产物均为新生成且非空。完整证据见 `23-retry-container-id.txt`、`24-retry-rustup-state-reset-output.log`、`25-retry-root-make-all-output.log`、`26-retry-build-order.log`、`26-retry-post-build-output.log` 和 `31-retry-final-outcome.txt`。
- 第二次 setup 走重复执行 fast path，exit 0，零输出且没有重新安装。命令记录见 `27-retry-fast-setup-command.txt`。
- root 入口单独派生 homes，并可能 provision；直接 lower-level targets 仍然只执行 preflight，不自动 provisioning。本次不声明 QEMU 通过，也不声明每个 direct target 都能 provision。

**备注：**
- 早先尝试无效的原因是删除了 `/root/.cargo/bin/rustup`，不是 root bootstrap 失败。该次结果不作为验证依据。
- source-worktree 的全局状态基线曾因并发文档写入而变化。有效验证容器只挂载了可丢弃的 `/app` 副本，因此不能据此声称共享 source worktree 保持全局未变化。
- 本次不需要新增 reusable reference，也不需要更新 `AI-Usage-Report.md`。
