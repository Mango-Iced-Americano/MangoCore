## 2026-07-20

### PageCache Loading lease：复制前不可见、批量读取不覆盖赢家

**涉及文件：**
- `os/src/fs/page_cache.rs` — 写入页从准备到 payload/valid-mask 发布期间保持 `Loading`；成功后才转为 `Dirty` 并计入脏页，失败恢复既有状态或移除新页。读者与竞争写入对 `Loading` 返回 `EAGAIN`，batch prefetch 仅发布仍为空的槽。
- `os/src/kernel_tests/page_cache.rs` — 增加复制前重入读/回写、batch prefetch 重入赢家、短 `UserBuffer` 写入不修改既有页三项回归。

**验证：**
- Docker RV64 `make rv64-ktest KTEST=page_cache EXT4_BACKEND=another`：4/4 通过，见 `page-cache-userbuffer-green-rv64-ktest.log`。
- Docker 串行 RV64/LA64 kernel build 均通过，见 `page-cache-visibility-green-rv64-kernel-build.log` 和 `page-cache-userbuffer-green-la64-kernel-build.log`。
- Oracle 最终 GO：确认 Loading lease、赢家保留与 `write_user(len > src.len()) → EFAULT` 的突变前预校验。

**备注：** 本轮不声称 mmap 与普通写路径完全串行化；范围仅为 PageCache 同步读写、回写和预读发布时序。

### PageCache 缺页后端重入不再持有 entries 锁

**涉及文件：**
- `os/src/fs/page_cache.rs` — `get_or_create_entry()` 将缺页处理拆为短暂查找、无 PageCache 锁的页帧分配与后端读取、以及保持 `entries → inner` 顺序的空槽发布；并发发布者优先，未发布候选页不影响缓存元数据。
- `os/src/fs/mod.rs` — 重导出 `PageCacheBackend`，使 crate 内核测试能够实现 PageCache 后端夹具。
- `os/src/kernel_tests/{mod,page_cache}.rs` — 注册同索引单次重入回归：后端首次 `read_page()` 重入同一 `PageCache::frame_for_read()`，验证不会死锁且读取到固定 payload。

**验证：**
- Docker 串行 `make rv64-kernel-build-only EXT4_BACKEND=another` ✅：`page-cache-lock-rv64-kernel-build.log`。
- Docker 串行 `make la64-kernel-build-only EXT4_BACKEND=another` ✅：`page-cache-lock-la64-kernel-build.log`。
- Docker RV64 `make rv64-ktest KTEST=page_cache EXT4_BACKEND=another` ✅：`page_cache::read_page_reenters_same_cache` 为 `1 passed, 0 failed`，完整串口见 `page-cache-lock-rv64-ktest.log`。
- 容器挂载、dirty revision 与测试新鲜性元数据见 `docs/Work_Log/evidence/2026-07-20/page-cache-lock-{container-mounts,git-hash,freshness}.txt`。

**备注：** 此测试必须在修复后运行；旧实现会在后端重入时再次获取 `entries` 的非重入 spin lock。没有引入 Loading 占位页或 single-flight 等待机制，重复的无锁读取竞争以最终空槽发布为线性化点。

### PageCache EOF 发布顺序修复

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — 活跃 inode 强持有其 PageCache；lifetime registry 仍仅保留弱引用，避免脏页过早释放且不形成 Arc 环。
- `os/src/fs/page_cache.rs` — 保留 `PageCache::write()` 包装行为，新增 crate-visible `write_with_after_copy()`，在复制数据与发布 valid 位后、性能统计和脏页节流前执行一次回调。
- `os/src/fs/ext4_another/mutations.rs` — another_ext4 `write_at()` 在回调中以已校验的 `end` 执行 `logical_size.fetch_max(..., AcqRel)`。
- `os/src/kernel_tests/ext4_another_lifetime.rs` — 新增非页对齐 EOF 扩展、单 inode `sync()`、drop 后冷查找的持久化回归。

**验证：**
- 受控 ownership toggle 将原有两个 lifecycle 用例由 RED 转为 2/2 GREEN；正式无环 ownership 修复后，同一组仍通过。
- EOF 回归基线为 3/3 GREEN；仅在 PageCache copy 与 EOF 发布之间插入一次 `writeback_all()` 时变为 2/3 RED，移除诊断调用并应用 callback 修复后，Docker RV64 隔离 QEMU 恢复为 3/3 GREEN：`docs/Work_Log/evidence/2026-07-20/another-wave1-eof-green-rv64-qemu-output.log`。
- Docker 串行 `make rv64-kernel-build-only EXT4_BACKEND=another` ✅，`make la64-kernel-build-only EXT4_BACKEND=another` ✅。
- 最终运行前后源 `sdcard-rv.img` 与 `disk.img` SHA-256 不变；候选 x0 副本变化符合测试写入预期，候选 x1 不变。

**备注：** 回调只执行原子 EOF 更新，不引入 I/O、锁、writeback 或其他后端路由变更。Oracle 审核指出的独立并发 writeback 竞争不在本轮同步调用链修复范围内。

### another_ext4 `0x003` x1 EIO 根因确认：损坏的工具盘 journal/orphan 状态，clean toggle 通过

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/rv64-tools-eio-ext4-state.log` — 原始 `disk.img` 的 x1 分区状态：`needs_recovery`、`not clean with errors` 和 orphan inode。
- `docs/Work_Log/evidence/2026-07-20/rv64-tools-eio-clean-e2fsck.log` — 仅对隔离副本执行 `e2fsck -fy`；记录损坏 journal transaction、orphan list、目录引用和位图修复，最终为 clean。
- `docs/Work_Log/evidence/2026-07-20/rv64-tools-eio-clean-{build,inject,metadata,qemu}.log` — `EXT4_BACKEND=another` 重建、`0x003` 注入、容器/revision 元数据与同参数显式 clean x0/x1 QEMU 串口。
- `docs/Work_Log/evidence/2026-07-20/rv64-tools-eio-debug-journal.md` — 分区几何、假设和隔离测试资产的记录。

**验证：**
- 原盘 x1 分区 LBA/尺寸、4 KiB block 换算与 ext4 magic 均正确；但 `tune2fs` 显示 `needs_recovery`、`Filesystem state: not clean with errors`、`First orphan inode: 12666`。
- 隔离副本经 `e2fsck -fy` 后为 clean；Docker RV64 `EXT4_BACKEND=another`、相同 `mask=0x003`、显式 clean x0/x1 QEMU 中，`/tools` 和所有 bind mount 成功建立，无 `mount_block_fs ... EIO`。
- basic/busybox 的 musl/glibc 四段均 `exit_code=0`，最终出现 `run_selected_groups done`。外层 zsh 对保留变量 `status` 的退出码记录错误发生在 QEMU 完成之后；验收以持久完整串口的完成标记为准。
- 未运行 LA64 `0x003` 或 LTP；无需 Rust 分区适配器修复或错误的回归测试。

**备注：** 根因是损坏的 x1 输入镜像，不是 `PartitionBlockDevice` offset/length 映射；常规 `rv64-run` 还将 x1 硬编码为 `../disk.img`，故 toggle 使用等价 QEMU 命令显式指向 clean 副本。

### another_ext4 `0x003` 接受门：RV64 x1 tools 盘 EIO 首错，停止在 LA64/LTP 前

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/another-mode-003.conf` — `mode=run`、`mask=0x003` 的 basic+busybox 专用配置。
- `docs/Work_Log/evidence/2026-07-20/rv64-003-*` — RV64 隔离 x0/x1 候选盘、源/候选 SHA-256、后端选择、注入、完整 QEMU 串口、首尾摘要、命令状态、容器/revision 元数据与首错分类。

**验证：**
- Docker RV64：`EXT4_BACKEND=another`、隔离 `SDCARD_RV`/`DISK_RV`、`mask=0x003`。basic 与 busybox 的 musl/glibc 工作负载均到达 `exit_code=0`，最终出现 `run_selected_groups done`。
- 但在用户态启动前，x1 tools 盘 ext4 挂载失败：`[kernel] mount_block_fs: ext4 mount failed: EIO`，`/tools` 留空；该首错优先于包装层 QEMU 退出 0，不能验收为通过。
- 未运行 LA64 `0x003`（包括 `DISK_LA` 覆盖）或 focused LTP。

**备注：** 分类为 `tools_disk_mount_failed` / `boot_mount_x1`。按接受门规则停止，后续只能在修复或明确排除隔离 `DISK_RV` 的挂载失败后重启此门。

### another_ext4：元数据权限写回修复，LA64 basic 恢复执行

**涉及文件：**
- `os/src/fs/ext4_another/inode.rs` — 实现 `IndexNode::set_metadata()`；保留磁盘 inode 的文件类型，仅写回 `S_IALLUGO` 权限、UID/GID 和时间字段到 `another_ext4::SetAttr`。
- `os/src/kernel_tests/ext4_another.rs` — 新增跨独立重挂载的权限位持久化回归，覆盖 `0644 → 0777` 的 metadata 更新。
- `docs/Work_Log/evidence/2026-07-20/another-mode-*` — RED/GREEN ktest、双架构构建、LA64 basic 专用镜像、配置、完整 QEMU 日志和容器/revision 元数据。

**验证：**
- Docker LA64 `EXT4_BACKEND=another` ktest：新增 `ext4_another::metadata_mode_persists_across_independent_mounts` 在修复前 RED（`set_metadata rejected a mode update`），修复后 GREEN。
- Docker：`rustfmt --check os/src/fs/ext4_another/inode.rs os/src/kernel_tests/ext4_another.rs` ✅；串行 `make rv64-kernel-build-only EXT4_BACKEND=another` ✅；`make la64-kernel-build-only EXT4_BACKEND=another` ✅。
- Docker LA64 QEMU：干净候选 `SDCARD_LA`/`DISK_LA` 与 `mask=0x001` 下，musl、glibc basic 均 `exit_code=0`，最终出现 `run_selected_groups done`；无 `Permission denied`。

**备注：** 同一 another_ext4 ktest 组的既有 `fsync_and_syncfs_surface_flush_failures` 仍失败（修复前后均失败），与本次 metadata 权限路径无关，未扩大本轮范围。未运行 `0x003` 或 LTP。

### Wave 5 LA64 basic：候选 DISK_LA 覆盖生效，测试脚本无执行权限而停止

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/la64-001-tools-override-*` — clean sdcard/tools 候选副本、`DISK_LA` 覆盖命令预览、构建/注入/QEMU 完整日志、哈希、权限检查及首错分类。
- `docs/Work_Log/evidence/2026-07-20/la64-001-debug-journal.md` — 包装脚本 0 返回掩盖脚本执行失败的验收判据记录。

**验证：**
- Docker LA64：`make -C os la64-only EXT4_BACKEND=another` 与候选盘注入 `0x001` 均退出 0；`EXT4_BACKEND_FEATURE := ext4_another_backend`，无 ext4→ramfs 回退。
- QEMU x1 明确使用 `DISK_LA=../docs/Work_Log/evidence/2026-07-20/la64-001-tools-override-disk-la.img`，未使用被 PID 432637 占用的源 `disk-la.img`；源 sdcard、源工具盘与根 `os_test.conf` 前后哈希/内容一致。
- guest 已进入 basic，但 musl/glibc 都在执行 `./run-all.sh` 时报告 `Permission denied`。源与候选 `/musl/basic/run-all.sh`、`/glibc/basic/run-all.sh` 均为 `0644`；包装层仍返回 0，故该 QEMU 退出码不能验收为 PASS。

**备注：** 分类为 `workload_not_executed_permission_denied`，已将自动生成的 PASS 标记作废。按首个真实工作负载失败停止：未运行 RV64/LA64 `0x003` 或 focused LTP；未终止 PID 432637、未修改默认后端或测试夹具。

### Wave 5 恢复预检：报告锁释放但运行态仍被 PID 432637 占用

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/la64-001-resume-lock-preflight.txt` — 恢复前 Docker 容器内的 `disk-la.img` 锁检查。
- `docs/Work_Log/evidence/2026-07-20/la64-001-resume-{commands-and-status,failure-classification,block-owner-state,integrity-check}.txt` — 预检退出状态、现行锁持有者、首错分类与源产物完整性。

**验证：**
- 2026-07-19T18:09:44Z 的恢复预检仍由 PID 432637 持有 `/app/disk-la.img`；分类为 `shared_tools_image_lock_preflight`，QEMU 未启动。
- 未创建新的候选盘、未构建/运行 LA64 basic，未运行 RV64/LA64 `0x003` 或 focused LTP；严格门禁未被跳过。
- `sdcard-rv.img`、`sdcard-la.img` 和根 `os_test.conf` 再次校验不变。

**备注：** 仅当容器内 `fuser /app/disk-la.img` 无持有者时，才能从新的 LA64 clean candidate 继续；本轮未终止外部 PID，也未改变默认后端。

### Wave 5 LA64 工具盘锁所有权核验：外部进程，未终止

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/la64-001-ownership-inspection.txt` — PID 432637 的启动时间、命令行、cgroup、cwd 与锁持有证据。
- `docs/Work_Log/evidence/2026-07-20/la64-001-ownership-assessment.txt` — 可终止性裁决与恢复前提。

**验证：**
- PID 432637 仍持有 `/app/disk-la.img` 写锁，启动于 2026-07-17 12:09:39，早于本轮 LA64 尝试。
- 实际命令行使用源 `sdcard-la.img`，不使用 Wave 5 候选 `docs/Work_Log/evidence/2026-07-20/la64-001-sdcard-la.img`；因此不能由本轮创建。
- 未发送信号、未重试 QEMU；锁未释放，LA64 basic 和后续 `0x003` 保持停止。

**备注：** 阻塞仅可由该既有 QEMU 的所有者退出/释放锁后解除；恢复时必须重新确认锁释放，再从干净 LA64 候选镜像继续。

### another_ext4 候选验收：RV64 basic 通过，LA64 启动前因共享工具盘锁停止

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/another-ext4-basic.conf` — basic `mask=0x001` 的专用注入配置。
- `docs/Work_Log/evidence/2026-07-20/rv64-001-*` — Docker 构建、候选盘注入、完整 QEMU 串口、状态、后端选择与镜像校验和。
- `docs/Work_Log/evidence/2026-07-20/la64-001-*` — LA64 构建与注入证据、QEMU 启动失败完整日志、锁持有者取证和分类记录。

**验证：**
- Docker RV64：`make -C os rv64-only EXT4_BACKEND=another`、干净候选盘注入 `0x001`、QEMU 均退出 0；musl/glibc basic 均 `exit_code=0`，`run_selected_groups done`，无 ext4→ramfs 回退。
- 后端确认：构建数据库记录 `EXT4_BACKEND_FEATURE := ext4_another_backend`，候选 `kernel-rv` 含 `another_ext4` 诊断字符串。
- Docker LA64：`make -C os la64-only EXT4_BACKEND=another` 与候选盘注入 `0x001` 均退出 0；QEMU 启动前因既有 PID 432637 持有共享 `/app/disk-la.img` 的写锁而退出 2，guest 未启动。
- 已按首错停止：未运行 RV64/LA64 `0x003` 或任何 LTP；源 `sdcard-rv.img`、`sdcard-la.img` 哈希与根 `os_test.conf` 均保持不变。

**备注：** LA64 首错分类为 `environment_shared_tools_image_lock`，不是候选 `another_ext4` 执行结果；详见 `la64-001-lock-inspection.txt` 与 `la64-001-failure-classification.txt`。未终止既有 QEMU、未重试，候选镜像保留于当天证据目录。

### Wave 4 another_ext4 最小可写路径与持久化验收

**涉及文件：**
- `os/src/fs/ext4_another/blockdev.rs` — 将 another_ext4 写入与 flush 映射到真实、可失败的 Mango `BlockDevice`。
- `os/src/fs/ext4_another/{fs,inode,page_cache,mutations,namespace}.rs` — 可靠 flush 门控、Mango PageCache 数据写回、inode 大小提交、同步/卸载顺序及基础命名空间变更。
- `os/src/syscall/fs/sys_syncfs.rs` — another_ext4 的实例级同步错误不再被静默吞掉。
- `os/src/kernel_tests/ext4_another.rs` — 写入/截断、命名空间跨独立 mount 持久化与 fsync/syncfs 失败传播测试。

**验证：**
- Docker RV64：`make -C os rv64-kernel-build-only EXT4_BACKEND=another` ✅；focused QEMU ktest 5/5 ✅。
- Docker LA64：`make -C os la64-kernel-build-only EXT4_BACKEND=another` ✅；focused QEMU ktest 5/5 ✅。
- 原始串口输出、dirty revision、容器挂载映射见 `docs/Work_Log/evidence/2026-07-20/another-ext4-wave4-*`。

**备注：** 可写挂载在读取介质前拒绝无可靠 flush 的设备；常规数据仅经 Mango PageCache 的 `write_data_only` 写回，随后提交 inode 元数据并在 fsync/syncfs 时 flush。缩小截断保证逻辑大小与可见数据，但 vendored another_ext4 未公开物理块回收 API；本轮未声明物理空间回收。

### Wave 3 another_ext4 只读 QEMU 验收通过（双架构）

**涉及文件：**
- `os/make/rv64.mk` — ktest QEMU 附加可覆盖的 x0/x1 VirtIO 块设备。
- `os/make/la64.mk` — ktest QEMU 附加可覆盖的 x0/x1 PCI 块设备，并使用既有 LA64 `-kernel` 启动形式。
- `docs/Work_Log/evidence/2026-07-20/another-ext4-wave3-*` — Docker 命令、串口日志、fixture 构造、退出状态与源镜像校验和。

**验证：**
- RV64：`/sdcard` 和 `/tools` 均挂载；`ext4_another` 无回退、只读读取、变更 `EROFS` 三项 ktest 全部通过。
- LA64：同一最小只读 fixture 语义下，`/sdcard` 和 `/tools` 均挂载；三项 ktest 全部通过。
- 项目原始 x0/x1 镜像的 SHA-256 前后匹配；所有 fixture 仅存在于容器 `/tmp` 且已清理。

**备注：** 原始镜像副本被 another_ext4 的 readonly recovery gate 正确拒绝为 `EROFS`，因此未做 journal recovery；改用 `^has_journal` 的临时最小 ext4 fixture。未修改 writable/journal 语义、默认后端或任何源镜像。

### Wave 3 another_ext4 只读 QEMU 验收（RV64 阻塞）

**涉及文件：**
- `docs/Work_Log/evidence/2026-07-20/another-ext4-wave3-readonly/` — Docker 容器、配置、干净镜像副本、完整串口日志、命令状态和校验和证据。

**验证：**
- Docker 内执行 `make -C os rv64-ktest KTEST=ext4_another KTIMEOUT_MS=30000 EXT4_BACKEND=another LOG=warn`，内核已启动并进入 ktest。
- `ext4_another::rejects_unclean_or_invalid_media_without_fallback` 通过。
- 读取与 EROFS 变更用例均因 `ktest requires a clean ext4 block device in slot 0` 未运行；串口同时确认 x0/x1 未附加，`/sdcard` 与 `/tools` 均跳过挂载。
- 原始 `sdcard-rv.img`、`sdcard-la.img` 的运行前后 SHA-256 一致；容器临时副本已清理。

**备注：** 按“捕获精确失败/根因即停止”规则，未启动 LA64。根因是 ktest QEMU 夹具未附加 x0/x1 块设备；未修改内核、Makefile、默认后端或源镜像。
