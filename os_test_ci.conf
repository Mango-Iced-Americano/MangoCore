mode=run
# CI 扫问题专用：全量跑、不排除任何用例（mask=0x6FDF 含 netperf，ltp_exclude 清空），
# 让 panic/阻塞用例在 CI 上暴露以便修复；跑分请用 os_test.conf（排除阻塞、能正常跑完出分）。
mask=0x6FDF
ltp_runner=suite
ltp_libc=both
ltp_suites=syscalls,fs_bind
ltp_exclude=
ltp_exclude_musl=
ltp_exclude_glibc=
ltp_include=
ltp_from=
