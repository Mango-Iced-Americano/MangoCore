Task 3 PageCache registry evidence

Changes made:
- Added `PAGE_CACHE_REGISTRY: Mutex<Vec<Weak<PageCache>>>` in `os/src/fs/page_cache.rs`.
- Added `register_page_cache(&Arc<PageCache>)`.
- Added `pub fn list_page_caches() -> Vec<Arc<PageCache>>`.
- Added `pub fn flush_all_page_caches() -> Result<usize, SyscallErr>`.
- `PageCache::new()` now registers each cache with the global registry.

Lock ordering:
- `list_page_caches()` holds the registry lock only while collecting/upgrading live weak refs.
- `flush_all_page_caches()` calls `list_page_caches()` first, then performs `writeback_all()` after the registry lock has been released.
- No global registry lock is held during backend block I/O.

Search checks:
- Registry/flush symbols found in `os/src/fs/page_cache.rs`:
  - `PAGE_CACHE_REGISTRY`
  - `register_page_cache`
  - `list_page_caches`
  - `flush_all_page_caches`
- `DirtyBlockDevice` / `dirty_block_device` in `os/src/fs/page_cache.rs`: no matches.

Build verification:
- rv64 kernel build: PASS.
- la64 kernel build: PASS.
