Task 2 ext4 override evidence

Search checks:
- ext4 explicit `sync_fs()` implementation exists:
  - `os/src/fs/ext4/ext4fs.rs:754`
- ext4 explicit `IndexNode::sync()` / `IndexNode::datasync()` implementations: not present yet.

Interpretation:
- This is acceptable for T2 because T2 establishes VFS/File contracts and bridge methods.
- Ext4 inode-specific sync/datasync behavior is planned in T9.
- The current ext4 `sync_fs()` still delegates to the existing dirty block path; replacing that architecture is planned by T13/T14.

Build verification:
- rv64 kernel build: PASS.
- la64 kernel build: PASS.

Outstanding downstream work:
- T7 must wire syscall fsync/fdatasync/sync to the File/VFS bridge.
- T9 must implement ext4 IndexNode sync/datasync semantics.
- T13/T14 must move sync_fs away from DirtyBlockDevice.
