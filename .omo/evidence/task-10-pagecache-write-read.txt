Task 10 ext4 PageCache dirty write evidence

Implemented:
- Changed `Ext4OSInode::write_at()` in `os/src/fs/ext4/ext4fs.rs`:
  - OLD: called `self.ext4fs.write_at()` directly (another_ext4 path), then invalidated page cache.
  - NEW: calls `self.ext4fs.prepare_cached_write()` (T11) to pre-allocate extents + commit i_size,
    then writes data through `PageCache::write()` (marks pages dirty).
  - Removed the old `pc.invalidate_range()` call — no longer needed since PageCache now
    owns the dirty data.
- For non-regular-file writes (symlink creation via ext4fs.write_at directly), the old
  direct path still applies; T10 only changes the VFS-level IndexNode::write_at path.

Build verification:
- rv64 kernel build: PASS.
- la64 kernel build: PASS.
