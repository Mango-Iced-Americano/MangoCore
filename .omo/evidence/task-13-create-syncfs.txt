Task 13 BlockCache metadata flush integrated into sync_fs

Implemented:
- Modified `Ext4FileSystem::sync_fs()` in `os/src/fs/ext4/ext4fs.rs`:
  - Added `self.cache_mgr.lock().flush_dirty_blocks(&self.real_block_device)?;`
  - This flushes BlockCache dirty metadata blocks (inode tables, bitmaps, etc.) directly
    to the real block device before the DirtyBlockDevice shim flush.
  - Kept `self.flush_dirty_blocks()` as transitional safety net until T14 removes the shim.

Build verification:
- rv64 kernel build: PASS.
- la64 kernel build: PASS.
